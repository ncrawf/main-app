# Surface Inbox — Unassigned Gems

Plane: P5/P4 intake buffer. Purpose: when the repo sweep finds a real surface/projection/dashboard/profile/marketing/analytics gem that does **not** yet have a confirmed home (or is ambiguous between surfaces), it lands HERE — never force-fit into a guessed surface or a domain contract.

Each entry: `source doc + excerpt/summary · likely surface(s) · uncertainty · needs (Nick-confirm / merge / maybe-stale)`.

Promotion rule: an inbox item becomes a real surface/projection contract only when it is Nick-seeded, clearly repo-evidenced, or required by a defined projection/capability. Aliases (e.g. "provider dashboard" vs "provider workspace") are merged here before any fork.

---

## Open entries

*(none yet — populated during the repo-first sweep, `repo-sweep-deposit` todo)*

## Resolved / promoted
