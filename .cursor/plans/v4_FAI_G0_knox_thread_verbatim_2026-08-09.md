# v4 — FAI — Gate-0 source preservation — KNOX THREAD (VERBATIM)

Document type: `evidence_or_ingestion` — **immutable verbatim source object**
Authority: **NONE.** Preservation confers no authority (`D0THES-GRD-036`). Nothing in this file is doctrine, decision, contract or architecture.
Status: **`awaiting_operator_population`** · `immutable_once_pasted` · `never_default_loaded` — **this file is EMPTY. It claims no preservation until the operator pastes.** **It blocks G0 acceptance; it does NOT block running PRE-0.**
Domain(s): `architecture_governance` · `cross_cutting`
Lifecycle role: the independent-reviewer record of the 2026-08-09 session — the review passes that rejected R0/R1/R2/R3 and the decision that the missing cross-loop architecture system is real. Preserved as the second leg of the trifecta record.
Source-of-truth relationship: **source, not authority.** Knox is a third-party review instance, not a human teammate and not an owner. **Agreement between Opus and Knox is not corroboration** (`AGENTS.md`). Where this thread and a committed carrier differ, **the carrier controls.**
Supersedes: nothing. Superseded by: none.
Manifest action: `add_tier4` **PROPOSED** — catalog row owed at the integrator transaction, **not landed**.
Review gate: `user_knox_required`

---

## Handling rules — read before using this file

1. **Never default-loaded.** Open only when a consuming question requires the primary review record.
2. **Immutable.** Do not edit, summarize or annotate the pasted content. Corrections belong in a consuming artifact that cites this one.
3. **Contains claims that were contested and some that were corrected.** Preserved deliberately, including: the Palantir cross-enrollment peering claim (**unresolved between the two seats and routed to the Evidence Plane**), the initial architecture-memory-control-plane home suggestion (**self-corrected**), and the `EVRUN-2026-000008` state correction (**Knox was right; Opus was wrong**).
4. **This thread is review, not `E2`.** No independent-adversary gate was run in this session.

---

## Provenance

| Field | Value |
|---|---|
| Seat | KNOX (ChatGPT review instance — third-party AI, not a human teammate) |
| Session | 2026-08-09 |
| Thread lock | `INS-G2-OPERATING-SUFFICIENCY` → `OMNI-FOUNDATIONAL-ARCHITECTURE-INSTALL` |
| Review objects | PR #14 at R1 `342e5346…` (blob `057ac155…`), R2, R3 `42f1b884…` |
| Verdicts issued | R0 `NOT ACCEPTED · STRONG R0` → R1 bounded R2 required → R2 direction passes → **R3 REJECTED** → decision: open the foundational arc |
| Relay markers | `knox = …` / `at knox` — relayed by Nick, evaluated on merits |
| Pasted by | **pending** |

---

## VERBATIM THREAD — paste below this line, do not edit above it

<!-- BEGIN VERBATIM KNOX THREAD -->

<!-- END VERBATIM KNOX THREAD -->
