# v4 — C4.4 Knowledge Reservoirs & Source-Estate Architecture Formulation Pass — July 2026 Reality — GATE-0 CHARTER

Document type: `plan_or_roadmap` (arc charter — Gate-0 deliverable; list + purpose, not prose essays)
Authority: `analysis_nonbinding` — propose-only pre-spine architecture pass (`GRD-036`/`GRD-043`). Binds NOTHING. Names herein are PLANNING VOCABULARY, not minted primitives.
Status: `G1_closed · G2_closed · G3_ACCEPTED · G4_KNOX_ADJUDICATED_PASS_30_OF_32 · no_kill_criteria · G5_not_started · analysis_nonbinding · not_promoted` — G1 + G2 normalized on the 2026-07-31 C4.4 curated artifact-landing onto `main`; **G3 §R reference architecture ACCEPTED 2026-08-01** (Nick operator acceptance + Knox independent remote-byte verification) on branch `analysis/c4-4-g3-reference-architecture` off `origin/main a87d305`. **★ G4 ADJUDICATED 2026-08-01: fixtures + Knox rubric were frozen in `v4_C4_4_fixture_suite_and_adversarial_results.md`; a fresh repo-connected Gemini ran read-only (frozen commit `133d286`, no repo writes) → Knox adjudication = `PASS · 30/32 · K1–K10 NOT triggered`; two of three alleged defects false-positive (already prohibited by frozen §R), one non-blocking finding `C44-G4-R01` (ranking/selection/exclusion influence) routes to G5/C5/Runtime. G3 unchanged (no amendment).** Accepted/adjudicated within the nonbinding arc, NOT merged, nothing promoted, outer checkpoint #15 UNCHANGED. **G5 is ELIGIBLE after Knox verifies the adjudication-close; G5 NOT started.** **Authoring-time banner (historical):** `gate_0_ACCEPTED (Nick 2026-07-19 + Knox) · opus_patches_applied · G1_in_progress · G2_not_started` — Nick issued explicit operator acceptance of the patched charter 2026-07-19 ("yes"); Knox (reviewer) accepted + authorized G1; G1 external pass was gated on Gemini-blind-packet relay (independence order).
Domain(s): architecture_governance, cns_orchestration, ai_substrate, evidence_processing, federation, cross_cutting
Lifecycle role: charter + gate map for the C4.4 pass **chartered to resolve or disposition the deferred `FWREG-007` questions** (CNS framework + Knowledge Reservoirs family) AND first-class the previously-unmodeled **governed Source Estate / Artifact Corpus** surfaced by the federation-onboarding pressure (Nick 2026-07-19 + Knox correction + Knox Gate-0 ruling).
Source-of-truth relationship: consumes `doctrine/cns_and_knowledge_reservoirs_frontier_2026-06-06.md` (the prior dense depth — do NOT re-derive), `FWREG-006`/`FWREG-007`, spine-shape §§7–8; feeds Task D (`v4_C4_2`) + v4 spine §§7–8 + C5. Does NOT supersede the outer checkpoint.
Supersedes: none (activates `FWREG-007`; widens it with the Source-Estate finding)
Superseded by: none
Manifest action: `add_tier2` (catalog row + read-graph consult route added same pass)
Review gate: `user_knox_required`
agent_read_rule: `consult_if_routed`

---

## §0 CURRENT STATE + HARD STOPS (read first)

> **★★★ NEWEST STATE (2026-08-02 PM — run-1 executed → HOLD → bounded patch applied → rerun armed; supersedes the "awaiting the frozen three-angle test" banner immediately below for gate position).** The frozen three-angle test **RAN** (run-1) and **Knox adjudicated `HOLD`** — **G3 remains ACCEPTED · G4 remains PASS 30/32 · NO seventh constitutional class; the six-class constitution, S2 axis model, Foundry model, and Simulation boundary all SURVIVE.** Run-1 quality: Angle A NONCONFORMANT (used the packet's own §G5-10 change-receipts as blocking architecture; no complete 12-example/P01–P19 tables; override untraced); Angle B NONCONFORMANT AS COMPARATOR EVIDENCE (checked the wrong C3.8 G4 path; continued past an integrity mismatch; no web, vendor claims from memory; S2 golden-state/policy category errors — its gap hypotheses retained ONLY after Knox reclassification); Angle C PARTIAL/HOLD (six outputs produced; O4/O6 PARTIAL because the two proof programs + the two-speed law were prompt-supplied, not packet-sourceable). **ONE bounded G5 integration patch was applied** on `analysis/c4-4-g3-reference-architecture` (commit **`bcb2b19`**, packet blob **`674fffa`**; advances from `865afad`/`d01978f`): NEW **§I.3A** operational decision-and-override lineage seam (two-speed law, controlling front door, full override trace) · NEW **§I.9A** executable proof programs named + sourceable (I.9A.1 F-Self Intelligence Foundry Pilot EPP · I.9A.2 Enterprise Bootstrap Steel Thread EPP) · NEW **§I.9B** 10 enterprise operational interface obligations (no new S-class) · NEW **§I.9C** admission-operations model + 9-item scale/future-risk register (debt-classified). **G3 blob `e364acb` + G4 carrier `07f8662` UNCHANGED; no new class/plane/domain/doctrine-file; `main` untouched; #15 untouched.** Raw run-1 outputs preserved unedited on `analysis/c4-4-g5-three-angle-test-orchestration` under `.cursor/plans/c4_4_g5_tests/results/` (Angle A/B/C RAW). **★ THE AUTHORITATIVE, REPINNED run-2 prompts + runbook are the files in `.cursor/plans/c4_4_g5_tests/` (repinned to `bcb2b19`/`674fffa`, with per-angle rerun discipline embedded); the §17/§19 in-charter freeze-pin copies below are RUN-1 HISTORICAL.** **NEXT = relay the three repinned `c4_4_g5_tests/` prompts to three fresh read-only contexts (run-2) → Knox adjudicates.** Everything below this line is preserved as history / detail.
>
> **★★ NEWEST STATE (2026-08-02) — supersedes the 08-01 banner below for gate position; the 08-01/07-31 banners are preserved as history.** **G5 IS UNDERWAY and is now a Knox-remote-verified CANDIDATE awaiting the frozen three-angle test.** Sequence since 08-01: G5 disposition/handoff packet (`v4_C4_4_disposition_ledger_and_v4_handoff.md`) authored → multiple Knox HOLD/rework cycles (artifact-identity, orthogonal S2 taxonomy, Foundry whole/component recomposition, Simulation-Lab boundary, adoption-time normativity, comparator-by-mechanism, F-Self pilot, downstream authoring contracts, executable authorability-test spec) → type+freeze patch → **candidate commit `b1ce1e8`** (one file) → **Knox remote-byte verification PASS** → **state-normalization commit `865afad`** (branch `analysis/c4-4-g3-reference-architecture` tip). `main` `a87d305` UNCHANGED; G3 blob `e364acb` + G4 carrier `07f8662` + adjudication-close `6388d60` UNCHANGED; catalog/read-graph/FWREG/**this charter's committed copy** NOT yet synced (deliberate — sync is a G5-acceptance step). **What is now SETTLED:** the six constitutional classes (S1 Source Estate · S2 Knowledge Reservoir · S3 Domain-Owned State · S4 Evidence Workbench · S5 Compiled Projection · S6 Mission Context); S2 as a typed orthogonal coordinate model (NOT a roster); working names (Knowledge Reservoirs · Intelligence Foundry [whole] vs Foundry maintenance machinery [component] · Retrieval Rails · Context Router); the Simulation-Lab data-plane boundary. **Still open (small, routed):** clinical-literature body public label · Foundry public surface name (Palantir collision) · lesson-form physical/access partitioning (→ C5) · exact C5 schemas. **★ 2026-08-02 — the three-angle freeze is AUTHORIZED by Knox at commit `865afad` (pins in §17); Angle B is UPGRADED from a shallow 14-row review to a bidirectional enterprise-substrate / bootstrap / continuous-learning pressure test** because the real competitive danger is enterprise-estate imprint (Palantir/big-tech territory), NOT care-native physics (where OMNI is already strong). The competitive frame, the upgraded Angle B spec, the must-own-vs-buy/wrap boundary, the adjudication classification, and the three post-close tracks are recorded in **§18** so no downstream agent is blind to them. **★ 2026-08-02 pre-freeze correction (independent Gemini pressure → Knox adjudication):** Angle A gains a mandatory operational-override mutation; **Angle B is REPLACED by v2** (enterprise-*composition falsification + executable-proof-design*, explicitly NOT an enterprise-capability proof — an LLM matrix cannot prove throughput/latency/entity-resolution/scale); Angle C gains an implementation non-goal + a two-speed cross-output law. Gemini surfaced one real load-bearing seam — the **two-speed operational override loop** (fast = S3 owner-state/config + immutable S1 evidence, consumed by the next context draw with NO wait for S2; slow = S2 admission for reusable generalization) — captured in **§18.8**, NOT a 7th class. The three corrected copy-ready prompts are frozen in **§19**; the durable-routing map (so nothing dies at gate-close) is **§18.9**; the "green light ≠ enterprise-proven" caveat is **§18.10**. **NEXT = relay §19.A / §19.B-v2 / §19.C to three fresh read-only contexts → Knox adjudicates → §18.6 classification → §18.7 tracks.** See `v4_C4_4_disposition_ledger_and_v4_handoff.md` for the full G5 packet.
>
> **★ LIVE STATE (2026-08-01): G1 CLOSED · G2 CLOSED · G3 §R ACCEPTED (Nick + Knox) · G4 KNOX-ADJUDICATED = PASS · 30/32 · K1–K10 NOT triggered · one non-blocking finding `C44-G4-R01` routed forward · `analysis_nonbinding` · NOT promoted · NOT merged to `main` · outer checkpoint #15 UNCHANGED.** G3 was authored → Knox-reviewed (PASS-WITH-REQUIRED-AMENDMENTS) → amended (1–5) + normalization-cleaned → Knox-verified on pushed remote bytes → **ACCEPTED**. G4 then: fixtures + Knox rubric FROZEN → fresh repo-connected **Gemini** ran read-only (frozen commit `133d286`, no repo writes, submission preserved verbatim in the G4 carrier §G4-C) → **Knox adjudicated** = PASS 30/32, no kill criterion (§G4-D/E/F). Two of three alleged defects are false positives already prohibited by the frozen §R (M01 retraction-erasure, M03 corpus-poisoning); one real socio-technical gap adopted non-blocking as `C44-G4-R01` (ranking/selection/exclusion influence governance). **G3 is unchanged — no amendment.** **The next authorized action is Knox verification of this adjudication-close, then an explicit G5 opening (separate later transaction). Opus does NOT begin G5 here.** All "G3 NOT STARTED / G4 fixtures-frozen-Gemini-not-started / requires authorization" language in the banners below is HISTORICAL.
>
> **★ CURRENT-STATE NORMALIZATION (2026-07-31 curated landing — now partly historical; see the 2026-08-01 banner above).** This charter landed on `main` via the C4.4 curated artifact-landing transaction. **State at landing: G1 CLOSED · G2 CLOSED · G3 NOT STARTED · `analysis_nonbinding` · NOT promoted.** G1 (prior-depth & July-2026 reality map) and G2 (taxonomy constitution + reference architecture) closed within the nonbinding pre-spine arc. The accepted Agent-Runtime capture was accepted **only as the bounded C4.4-G2 consumption bridge** — the wider Agent Runtime formulation + `FWREG-010` remain OPEN. *(G3 was NOT started as of this 07-31 banner; it has since been authorized + authored — see the 08-01 banner above.)* Outer checkpoint #15 UNCHANGED. The Gate-0 authoring-time bullets that follow are preserved as history: where they say "This authored Gate 0 only / No G1 / G2 not started," read them as the state at charter authoring, superseded by these banners.

- **Outer gate not driven by C4.4.** As of 2026-07-22 `EVRUN-2026-000012` is now CLOSED (`6a74485`) and a sibling `C4.5` (Temporal Integrity / `FWREG-015`) Gate-0 has landed; the boot checkpoint is maintained by the checkpoint agent (was `HANDOFF_2026-07-19_taskd_interim_checkpointed_evrun12_active.md`). This pass is an **operator-activated (Nick 2026-07-19) parallel pre-spine input** under the operator-controlled queue — it does **NOT** repoint the outer checkpoint and does NOT jump the gate.
- **This authored Gate 0 only.** No G1. No taxonomy, no reference architecture, no schema, no folders, no contracts, no registry, no vendor pick, no runtime, no `KnowledgeReservoir`/`SourceEstate` service, no empty future files, **no new architecture plane.**
- **Propose-only** (`GRD-036`); nothing here binds thesis/contracts/security/surfaces.
- **G1 was authorized through Nick + Knox acceptance (Nick operator "yes" 2026-07-19).** No gate beyond G1 auto-starts; **G2 requires separate G1 closure + acceptance.**
- **Reactor untouched/frozen.** C3.8 is an INPUT, not reopened.

## §0.5 Completion envelope — definition of DONE + explicit non-goals (Knox-accepted 2026-07-19)

**C4.4 is DONE when it has:** (1) distinguished the constitutional species + authority boundaries (source estate · reservoir · domain memory · workbench · projection · mission context); (2) defined a technology-neutral reference architecture + plug-point model; (3) produced fixed-fixture adversarial results; (4) routed EVERY conclusion into v4 spine / C5 / Agent-Runtime / Build-OS / domain contracts / procurement / bounded-deferral / rejection (zero unrouted).

**C4.4 explicitly does NOT:** exhaustively mine the OMNI corpus · fully solve every memory/context/learning use case · mint runtime agents · select vendors · build Source-Estate or Reservoir infrastructure · author implementation schemas. (Anti-`GRD-043`/anti-premature-librarianship bound — this arc is a formulation, not a mini-thesis or a build.)

## §1 Why this is authorized now (not a detour)

1. **`FWREG-007`'s revisit trigger has FIRED** — it deferred formulation until "the neural-network/agentic outside-learning batch is ingested + analyzed OR context-architecture work begins." Waves 4–6 (`EVRUN-000005/000006/000011`) did the first; the Agent-Runtime capture did the second. (Trigger fired ≠ questions resolved — this pass is chartered to resolve/disposition them.)
2. **The accepted v4 spine-shape §8 explicitly requires this** — "MUST consume the frontier doc and RESOLVE its 6 open Qs — do NOT re-derive; the depth exists." §7 (runtime/context/knowledge routing) and the §2 covered-thin guard (provider/staff/inventory/business learning → §8, first-class, NOT under Patient-CNS) also route here.
3. **"Foundry" already exists in-estate** as *Intelligence Foundry* (spine §8 title) — the reservoir(passive body) vs Foundry/compiler/maintainer(active machinery) split is a RECONCILIATION of two existing OMNI concepts, not an invention.

## §2 The corrected frame — the category error this pass exists to prevent

**Failure mode identified 2026-07-19 (Nick), ruled by Knox, owned by Opus:** an early framing answered only "where do extracted facts commit" (D7/Identity/Observation/Clinical-Memory/Federation) and treated a federation's inbound estate (a 3,000-patient spreadsheet, 10 PDFs, 50,000 files) as *exhaust on the way to domain records*. **That is wrong.** Routing extracted facts does not exhaust the source artifact. The inbound corpus is a **first-class governed estate in its own right** with its own lifecycle (preserve · challenge · re-read · re-extract · search · relate · partition · share · revoke · prove), whether or not it is ultimately called a "reservoir."

**Two independent realities must remain independently representable — for the duration of their lawful custody, retention, audit, and use obligations:**
- **What OMNI received** (the source estate) — cannot evaporate when
- **what OMNI concluded/extracted/adopted/rejected/routed** from it is produced.

Authorized **deletion, pseudonymization, legal hold, archival, or destruction must be explicit, additive, and lineage-preserving** — OMNI does not promise to retain all bytes forever, but the two realities must not silently collapse into one.

**Receipt truth ≠ content truth (candidate law):** a manifest may authoritatively prove *what OMNI received, from whom, and when* — it does NOT make every claim inside the package accurate or adopted.

This is OMNI's own layered-accountability physics (`D7 artifact → Observation → Clinical-Memory assertion → adoption → action`; each references, never duplicates) extended **upstream** to the corpus, plus **corpus-level parents** OMNI does not yet model. It is also the Evidence Plane's own object discipline (`GRD-040`: immutable global-ID source · index · re-runnable analysis runs; one source ↔ many runs; reprocessing expected) generalized from the R&D workbench to the **operational/federation ingress path (source-admission layer — NOT a new plane).** The manual wave-ingestion grind is the *first pilot* of this, not the final design.

**Fan-out, not three disjoint pipes.** The same PDF may simultaneously remain a canonical source artifact, support 3 Observations, back a provisional Clinical-Memory assertion, prove a historical bill, stay searchable, expose a contradiction, later yield an operator-learning candidate, join a legal export, and be re-extracted in 2 years by a better model.

**Cardinality (candidate — pressure, don't adopt):**
```
one ingress_package     → N source_artifacts
one source_artifact     → M extraction outputs
one extraction output   → 0..N candidate domain records
one candidate record    → 0..1 authorized domain adoption
one source_artifact     → many indexes / projections / lesson candidates
every derived object     → lineage back to exact source bytes + processing version
```
**Duplication law (qualified — Knox patch 8):** *No silent semantic duplication and no accidental second authority.* Every replica, derivative, export, cache, materialized view, or jurisdictional placement stays **typed, purpose-scoped, lineage-linked, and subordinate to the canonical artifact identity** — non-authoritative unless its owning domain separately commits it. The workbook must not become 3,000 uncontrolled "originals"; technical copies (DR, legal export, cached processing, replication) are NOT inherently architectural corruption. Successful extraction ≠ complete migration; adoption never deletes the source relation.

## §3 Scope

**IN scope (the pass must model / adjudicate all of these):**
- **Source Estate / Artifact Corpus** (co-first-class with Reservoirs): `ingress_package`/`source_collection`, package manifest + declared vs received completeness, `source_artifact` (immutable while retained), source-system identity + as-of snapshot, custody/access/PHI-sensitivity/legal-basis/retention, original hierarchy + relationships, quarantine + partial failure, `ingestion_run`/`extraction_run` versions, unadopted-material honest representation, reprocessing, incremental/corrective re-imports, package closure with visible exceptions.
- **Knowledge Reservoirs** family (the deferred `FWREG-007`) — families, authority classes, passport, principal/tenant partitions, admission/promotion, maintenance missions.
- **Foundry / compiler / maintainer** (active machinery) vs reservoir (passive body).
- **Context Router** (purpose- + authority-scoped draw) and its relationship to CNS / Agent Runtime.
- **Domain-adoption boundaries** (what leaves the estate and becomes owning-domain truth, and how).
- **Evidence Workbench** reconciliation (the current `ingestion/` lanes — see §7.1).
- **Enterprise data-plane comparison** (the widened competitive gate — §5/§8 G1).
- **Continuous/passive ingress operating model** (replacing the human-heavy wave grind) — the "passive agents mine sources → propose gems → gate" posture.
- **Save-case → learning-candidate** internal-experience path (Knox's fixture).
- **★ The OMNI Build/Architecture/Evidence corpus (THIS estate) as the reference fixture + recommended FIRST dogfood** (see §9 F-Self + §16): 4 months of chat threads, agent transcripts, ~hundreds of MD files, ~300 ingested videos, decision/guardrail ledgers, semantic-loss failures — OMNI already hand-runs a primitive gem-extraction+promotion pipeline over it (catalog · decision ledger · guardrail digest · read-graph · handoffs · agent-transcripts). Best proof case because its failure modes (leak-at-pivots, re-derivation, graveyards) are exactly ours. **Recommendation only — no build committed at G0.**

**OUT of scope (hard stops — §0):** minting names/schemas/folders/contracts/registries/runtime/vendor picks; the medical-literature build (`FWREG-006` stays reserved; this pass places the boundary, doesn't build it); reopening C3.8; authoring spine/thesis prose; minting a new plane; reusing the reserved domain word "Intake" for ingress/migration/estate operations (use ingress · source admission · estate acquisition · migration · synchronization · capture).

## §3.1 C4.4 ↔ Agent-Runtime seam (binding — Knox-accepted 2026-07-19)

**C4.4 owns the knowledge-maintenance MISSION classes** — permitted inputs/operations, output artifacts, the proposal-vs-commit authority ceiling, and proof/lineage/rollback/review + promotion/invalidation boundaries — named as *missions*: `contradiction-scanning · source-verification · candidate-reconciliation · routing · maintenance`. **Agent-Runtime / `FWREG-010` owns** the named actor/role taxonomy, `agent_definition`/`agent_runtime_profile`, harness behavior, memory/context execution policy, delegation, credentials/tools, and runtime lifecycle/authority enforcement. **C4.4 must NOT mint a `ScoutAgent`/`CapperAgent`/knowledge-agent hierarchy** — it describes the missions; Agent-Runtime (consuming G2) instantiates the actors that perform them. This keeps the seam the §4 sequencing exists to protect.

## §4 Sequencing (staged — Knox Gate-0 decision)

```
C4.4 G1 (prior-depth + external mechanism recovery)
  → C4.4 G2 (taxonomy + constitutional boundaries)
     → Agent-Runtime depth (FWREG-010) CONSUMES the accepted G2 boundary
        → C4.4 G3 (reference architecture INTEGRATES Agent Runtime as consumer)
           → C4.4 G4 (adversary) → C4.4 G5 (disposition + handoff)
              → Task-D final admission/closure
```
Task D may continue on **unrelated** enterprise-pressure lanes, but it **must not close** its knowledge / memory / source-estate / context-routing / Foundry / artifact-plane conclusions before consuming **C4.4 G5**. This prevents (a) Agent Runtime inventing its own memory/context architecture before C4.4 separates the species, and (b) C4.4 designing a Context Router + maintainer model without the actual Agent-Runtime consumer.

## §5 Source posture (this Gate-0 pass) + fixed source sets

**Read fully this pass:** `cns_and_knowledge_reservoirs_frontier_2026-06-06.md`; `FWREG-006`/`007` rows; spine-shape §§1/2/7/8; `D7_documents_consent_media_contract.md` §1.5–§12; guardrail digest; comparator registry; System Map vNext; operator/collaboration model; current checkpoint handoff; catalog + read-graph anchors for the wiring.
**Consulted (subagent-recovered this session):** evidence router + outside-learning pipeline doctrine; the C4 loop/harness/Polaris captures; waves/EVRUN history; scenario libraries; TRT/Dan/Reactor; BIZOPS contract + surfaces.
**Verified by grep:** NO first-class corpus-level object (`ingress_package`/`source_collection`/`migration_manifest`/`source_artifact` parent) exists in any build-facing contract — real gap, not merely "not found."
**NOT inspected this pass (→ G1 recovery):** the 9 reservoir/memory source packets (`EVSRC-2026-000227` LangSmith · `000238`/`000239` Karpathy/OKF · `000243` Engram · `000262` LLM-wikis · `000266` production agent memory · `000287` OpenWiki · `000313` Karpathy-wiki-vs-OpenBrain · `000314` CQ/lesson-commons); legacy 3-layer System Map; off-repo controlling plan.

**Fixed internal source set (G1 recovery):** the frontier doc + the 9 packets above (navigate via the `EVRUN-000003/000005/000006/000011` concept registries; reopen load-bearing packets; NEVER author from anchor ledgers) + D7/Observation/Clinical-Memory/CNS/Identity/RBAC/Federation/Settings/D6/BIZOPS/OFC boundaries + Agent-Runtime/Platform/Care/Accountability captures + field/case work + **the OMNI estate itself** (as the F-Self fixture material).

**Fixed external source set (G1 reality — MECHANISM-COMPLETE bounded set; controlling unit = mechanism coverage, NOT vendor/doc count; expected ~24–36 primary current documents; expandable ONLY via a recorded coverage-gap exception). Three stacks:**
- **A. Data / artifact plane:** Palantir Foundry (datasets/media-sets) · Microsoft Fabric/OneLake/Purview/Azure AI Search · Databricks/Unity Catalog volumes · AWS S3/Glue/Lake Formation/Bedrock KB · Google BigQuery/Dataplex/Vertex AI Search · IBM watsonx.data (+ intelligence) · Snowflake · Elastic.
- **B. Knowledge / context plane:** LangChain/LangGraph · LlamaIndex · Letta · Chroma · Glean · OpenAI (File Search + data-agent) · Anthropic (CLAUDE.md hierarchy + Skills + MCP) · Google · xAI · Pinecone/Weaviate · OKF/OpenWiki/CQ.
- **C. Semantic / action plane:** Palantir Ontology/AIP · ServiceNow · Salesforce · Microsoft · IBM.
- **C-health (FROZEN healthcare comparator set — confirm before G1; no open-ended "relevant incumbents"):** Epic (Cosmos / Care Everywhere) · Oracle Health (Cerner) · Abridge · Microsoft/Nuance DAX · Innovaccer · Commure · Datavant · Health Gorilla · Particle Health · Health Catalyst.

## §6 Candidate planning vocabulary + the six layers (NOT minted)

Layers the architecture must separate: **(1) ingress/collection** · **(2) artifact custody** · **(3) interpretation/normalization** · **(4) domain adoption** · **(5) reusable knowledge/experience** · **(6) indexes/projections/mission-context**. Cross-cutting mechanisms: storage · catalog · lineage · access · indexing · versioning · quality · reprocessing · retention · revocation · search · compilation · promotion.

Candidate handles (planning only): `ingress_package`, `source_collection`, `source_artifact`, `ingestion_run`, `extraction_run`, `knowledge_reservoir`, `reservoir_passport`, `context_router`, `compiled_projection`, `retrieval_index`, `mission_context`, `maintainer/compiler`, `promotion_gate`.

## §7 Three taxonomy options to ADJUDICATE at G2 (do NOT settle in G0)

- **A.** Raw corpus = a `Source Reservoir` subtype (one reservoir class among many). **[currently least favored]** — calling immutable received evidence a reservoir risks collapsing source custody and curated reusable knowledge.
- **B.** Raw corpus = an adjacent **Source Estate** that *feeds* Knowledge Reservoirs (`Source Estate → interpretation/extraction → Reservoirs and/or domain-adoption candidates → projections + mission context`). **[strongest working hypothesis — preliminary bias, Opus + Knox]**
- **C.** A wider parent (**"Governed Context Estate"**) containing source corpora + reservoirs + compiled projections + mission context, domain memory separately owned. **[may survive ONLY as a navigational umbrella — NOT automatically an ontological parent / new owning layer unless the work proves non-duplicative constitutional value; else it becomes an "everything-context" god-category].**

**G2 must adjudicate using more than "source is fixed; reservoir is curated."** Test each candidate against: authority relationship · custody · mutation law · retention/deletion · source correction · ownership · admissibility · aging/freshness · reprocessing · and whether the object is **evidence vs accepted knowledge vs domain state vs a view**. Immutability is retention-qualified: *a captured source version is immutable while retained; corrections and later exports are additive; its bytes/custody/visibility/retention remain policy-governed.*

**D7 honesty finding (grounded this pass):** D7 owns the per-artifact durable physics well (custody, integrity gate, one-canonical-many-grants, fingerprint dedup, upload_event, open `document_kind`, 5-disposition, federation-ready per-artifact lineage). **Gaps the pass must resolve:** (a) `patient_document` is patient-subject-shaped — a 3,000-patient workbook / multi-subject / non-patient artifacts (bills, device manuals, formularies, training files) strain it → test a general `source_artifact`/`artifact` parent with subject-via-relationship; (b) NO corpus-level object (package/collection/manifest → completeness/exceptions/quarantine/incremental+corrective import/closure); (c) NO corpus-scale reprocessing/`extraction_run` lineage; (d) NO honest "received-but-never-adopted" model. Do NOT declare D7 sufficient because it can store a PDF.

## §7.1 The FIVE orthogonal taxonomies the "8 ingestion types" question forces (G2/G3)

The current 10 `ingestion/` lanes are **Evidence-Plane provenance lanes for the R&D external-evidence workbench** (lane = who produced it + why, `GRD-037`). They are NOT OMNI's universal ingestion constitution — they do not classify federation/domain migration, patient-document import, continuous EHR feeds, device telemetry, operational logs, provider-private knowledge, agent-trace lessons, clinical-literature corpora, model/skill/prompt supply-chain artifacts, or federated knowledge exchange. The pass must separate FIVE orthogonal axes (Knox patch 4 — form ≠ delivery mode):
1. **Provenance / source family** (patient/provider/operator/partner/vendor/regulator/educator/system-export/device/agent/runtime/public).
2. **Admission purpose** (tenant migration · ongoing sync · care evidence · operational execution · legal retention · reference learning · clinical knowledge · experience learning · federated exchange · model/build supply chain).
3. **Destination / authority state** (source-only · provisional normalized · adoption candidate · domain truth · reservoir candidate · compiled projection · lesson candidate · policy/skill candidate · rejected/quarantined).
4. **Information form** (package · file · document · table · row · image · audio · video · model artifact · external reference).
5. **Delivery / temporal lifecycle** (one-time snapshot · incremental package · correction package · continuous feed · event stream · remote/federated pointer · generated derivative).

One thing can be many axes at once (a spreadsheet + a snapshot; a PDF + part of an incremental correction package; an external pointer + a continuously synchronized source). File type never determines architecture.

## §8 Gates (provisional — confirm labels vs namespace at G0 acceptance)

| Gate | Purpose | Output | Acceptance / stop condition |
|---|---|---|---|
| **G0** (this) | Charter, fixed source sets, roles, hard stops, corrected frame | this doc | Nick + Knox accept (patched) before any research |
| **G1** | Internal prior-depth recovery + July-2026 reality map across all THREE (+health) stacks | reality map + **multi-axis mechanism-coverage matrix** | HARD GATE: every material mechanism carries THREE axes — **semantic relation** (`already_owned·sharpens·genuine_gap·conflicts·reject`) × **current realization** (`built·partial·stub·absent·unverified`) × **implementation posture** (`OMNI_native·named_plug_point·buy_or_wrap·interchangeable_rail·implementation_only·not_applicable`) — plus primary source · capability owner · authority gap · affected OMNI home · exceeds-OMNI-build? · exceeds-OMNI-architecture? · replacement/switching boundary. "Already modeled" NEVER means "already built." Per-mechanism, not per-vendor. No undispositioned mechanism; no vendor checkbox; no generic summary. |
| **G2** | Taxonomy + constitution + Evidence-lane reconciliation | candidate model | adjudicate A/B/C (§7 criteria); the six layers; the five taxonomies (§7.1); D7-parent question. **Each `FWREG-007` open Q closed as `resolved` \| `bounded-deferred` (owner + precise unresolved issue + destination + trigger + what may safely proceed) \| `rejected` — "explicitly open" alone is NOT a disposition.** |
| **G3** | Reference architecture + pluggability | tech-neutral architecture | landing/quarantine · storage · package/manifest · catalog · classification · entity-resolution · extraction/normalization · domain-admission interfaces · lineage · indexing · retrieval · reprocessing · correction · retention · revocation · federation · **build/buy/wrap per mechanism**; no god-store; **scope-fence honored** (own the governance semantics; plug in lakehouse/vector/graph rails — `GRD-041`/`GRD-033`/`GRD-034`/`GRD-028`) |
| **G4** | Frozen fixtures + independent adversary | scored fixture suite | Gemini adversary (fixtures + rubric frozen first) + Knox adjudication across care/build/business/federation/privacy/staleness/execution |
| **G5** | Disposition + v4/Task-D/C5 handoff | final ledger | zero unrouted concepts; every finding → spine-line \| C5 \| contract-implication \| Build-OS \| product \| open-review \| parked \| rejected \| procurement-only |

## §9 Fixture suite (freeze before G4 grading)

Knox's 12 (clinical-literature retraction/conflict · patient-inapplicable similar study · high-value save-case near-miss · low-value save-case noise · formulary/vendor/pricing/sourcing disagreement · operator-private alpha non-leak · build-agent repeated mistake → lesson/eval/skill · federated lesson publish→admit→revoke · stale fluent wiki · patient-preference-vs-consent/inventory/safety · outcomes-pattern-without-evidence → policy · retrieval/index/vendor swap without lineage loss) **PLUS**:

**F0 — Federation Source-Estate Admission Fixture (G2 taxonomy breaker + G4 adversary):** a federation supplies 3,000-patient spreadsheet + 50,000 mixed PDFs/images + 20yrs bills/claims + 1992–2026 aftercare + device manuals/training files + provider CO₂-treatment preferences + old & current formularies + vendor price sheets + an exported EHR folder tree + duplicates + patient collisions + unknown patients + corrupt/password-protected files + non-patient docs in patient folders + a later incremental export + a correction package invalidating part of the first. The architecture must prove: original package reconstructable · stable artifact identity + custody · original organization/relationships preserved · PHI tenant+purpose-scoped · corrupt/suspicious quarantined not dropped · extraction versioned + repeatable · candidate-association ≠ confirmed-identity · no silent-semantic duplication (typed/lineage-linked copies OK) · owners adopt only what they own · unadopted material honestly represented · searchable source ≠ adopted truth · historical instructions ≠ active policy · provider preference ≠ clinical doctrine · vendor doc ≠ current formulary · receipt truth ≠ content truth · package completeness + partial-failure visible · reimport/correction/supersession preserve history · retention/deletion/export/legal-hold differ by artifact · whole estate reprocessable by a newer model · retrieval obeys relationship/purpose/consent/operator boundaries · indexes rebuildable without losing the corpus.

**F-Self — OMNI's own Build/Architecture/Evidence Source Estate (continuous proof thread G1–G5, NOT a G4-only fixture):** the actual estate — 4 months of chat threads + agent transcripts + ~hundreds of MD files + ~300 ingested videos + decision/guardrail ledgers + the wave-5 semantic-loss failure. **Per-gate use:** **G1** = representative *stratified species census* (species · custody · authority class · discovery capability · lineage gaps · manual burden · ≥1 known semantic-loss/leak-at-pivot specimen) — NOT exhaustive gem mining; **G2** = classify representative specimens (chats · uploads/screenshots/transcripts · source packets · interpretations · working analyses · proposals/rejections · canonical doctrine/contracts · handoffs/projections · commits/receipts · superseded artifacts) against the candidate taxonomy; **G3** = trace ONE end-to-end flow (raw conversation → candidate extraction → dedup/reconcile → routed promotion gate → accepted/rejected → regenerated non-authoritative projection); **G4** = adversarially test semantic loss / false promotion / stale confident synthesis / contradiction erasure / privacy+alpha leakage / source disappearance / graveyards / correlated-agent false confidence; **G5** = disposition whether the Self-Estate is the recommended FIRST implementation target + its downstream build-entry triggers (no build authorized in C4.4). Law throughout: passive mining PROPOSES (source-linked), never promotes.

**F-Inv — Promoted-knowledge invalidation (G4; DISTINCT from stale-wiki + federated-revoke):** a source-backed gem has ALREADY been promoted into doctrine/spine; later its originating source is found superseded, contextually misread, or dependent on an invalid assumption. The architecture must: identify every promoted dependent artifact · preserve historical lineage (what was decided AND what actions were taken under the former accepted state) · FLAG, not silently rewrite, the canonical content · route reconsideration to the owning authority/gate · identify downstream uses + projections · regenerate affected projections ONLY after disposition. Tests invalidation reaching an already-promoted constitutional home — the case our own estate will actually hit.

## §10 Quadrifecta roles + independence controls (Knox patch 9)

- **Opus:** repo-native recovery + dedup + architecture author + current-state/implementation verifier + governance execution.
- **Nick:** operator/provider/business reality (medspa/formulary/vendor/federation-onboarding), adoption/burden judgment, acceptance gates.
- **Knox:** independent architecture challenger, source-authority reconciler, rubric author, adjudicator, anti-god-object/anti-flatten pressure.
- **Gemini (optional 4th leg — TWICE only, independence-controlled):**
  - **Separate FRESH contexts for G1 and G4** (no carryover).
  - **G1:** receives the FROZEN external questions + source classes, **NOT** OMNI's candidate design → reports what current systems actually do + where they fail.
  - **G4:** Opus freezes the candidate architecture + fixture answers FIRST; Knox freezes the rubric BEFORE seeing the adversary submission; Gemini attacks with the strongest alternatives (Foundry ontology/action · Glean enterprise search · Google/OpenAI memory · file-wikis · federated commons).
  - **Gemini does not self-grade.** Any external factual correction is made in adjudication, never by rewriting the adversary submission.
  - No four-agent ceremony on small gates.

## §11 Minimum future artifact set (NAME only; create only on gate entry — do NOT create empty files)

1. `v4_C4_4_knowledge_and_source_estate_formulation_plan.md` — **this charter (created).**
2. `v4_C4_4_prior_depth_and_july_2026_reality_map.md` — G1.
3. `v4_C4_4_taxonomy_constitution_and_reference_architecture.md` — G2/G3 (core deliverable).
4. `v4_C4_4_fixture_suite_and_adversarial_results.md` — G4.
5. `v4_C4_4_disposition_ledger_and_v4_handoff.md` — G5.

No sixth "miscellaneous" doc. No registry, no per-reservoir docs, no schema, no folder tree.

## §12 Candidate laws to PRESSURE (affirm / revise / reject — never auto-adopt)

Reservoirs supply · maintainers tend · routers assemble · CNS coordinates · owning domains decide+commit. Storage ≠ authority. Retrieval ≠ admissibility. Repetition ≠ independent evidence. **Receipt truth ≠ content truth.** Source ≠ normalized ≠ adopted ≠ decision ≠ projection ≠ mission-context (never collapse). A captured source version is immutable while retained; deletion/pseudonymization/legal-hold/archival is explicit, additive, lineage-preserving. No silent semantic duplication / no accidental second authority (every replica typed·purpose-scoped·lineage-linked·subordinate). Source evidence stays recoverable beneath every synthesis. Contradictions survive until an authorized process resolves/scopes them. A fluent synthesis must disclose omissions/uncertainty/source-cutoff/compiler-version/stale-deps. Higher consequence → closer to current authoritative source. Every durable write has principal/purpose/owner/scope/authority-state. Conversation → memory candidates, not automatic memory. Case → lesson candidate, not policy. Lesson ≠ policy ≠ clinical truth. Advice becomes supply-chain material once an agent can execute it. Revocation propagates ≥ as aggressively as publication. Human contestability + agent legibility are co-requirements. Federated publication ≠ universal trust (locally admitted). Provider/operator private alpha never becomes platform-common by silent extraction. Passive capture allowed · passive promotion forbidden. Waves = audit/capping windows, not the unit of learning. RAG/vector/graph/wiki/long-context/SQL/FTS/agentic = replaceable realization, not the constitution. **The system must get cheaper, more faithful, and less human-intensive per source over time** (scored acceptance criterion).

## §13 Hard stops (restate)

No spine/thesis prose · no C5/schema/contract edits · no runtime build · no permanent folder tree/registry · no vendor selection · no service · no medical-PDF ingestion build · no empty future files · no new broad video wave · no new architecture plane · no reuse of the domain word "Intake" for ingress/migration/estate ops · no authoring from memory or anchor ledgers · no treating chronology/polish/vendor-vocab as authority · no silent checkpoint repoint · no auto-start of G2 (G1 authorized 2026-07-19; G2 needs separate G1 closure + acceptance) · no producing/sharing Opus external-mechanism findings until the Gemini blind packet is confirmed relayed.

## §14 Governance receipt (this pass — durable; the chat summary is NOT a substitute)

- **Charter path (created):** `.cursor/plans/v4_C4_4_knowledge_and_source_estate_formulation_plan.md` (passport above; Knox-patched 2026-07-19).
- **Catalog row added:** `01_master_corpus_catalog.md` (`analysis_nonbinding`, `add_tier2`, `consult_if_routed`, `user_knox_required`).
- **Read-graph route added:** `04_manifest_read_graph.md` route **`9j`** (`consult_if_routed`).
- **`FWREG-007` change:** status `watch → candidate`; annotated ACTIVATED as the C4.4 arc (trigger fired) + WIDENED to first-class the Source Estate. `FWREG-006` unchanged (`watch`; boundary placed here, not built).
- **Outer checkpoint:** UNCHANGED — `EVRUN-2026-000012` remains active; read-graph #15 + AGENTS NOT repointed.
- **Repository state (updated 2026-07-22):** working tree IS on `evidence/evrun-000012-care-commerce-hardening`. The C4.4 doc files + catalog rows remain **LOCAL/UNCOMMITTED** and must NOT be committed onto that branch (Knox); the read-graph `9j` + `FWREG-007` edits were already **swept into that branch by concurrent checkpoint/C4.5 commits** (a fait accompli, not a C4.4-initiated commit). No C4.4-initiated commit/push/branch-change. Knox reviews the submitted content, not verified committed bytes.
- **Source-coverage receipt:** §5 (read-fully / consulted / grep-verified / not-inspected). No file claimed read merely because search returned it. No corpus-level object exists (grep-verified absence).
- **Artifacts created this pass:** exactly ONE (this charter). Docs #2–#5 named, NOT created. No schema/folder/registry/runtime.
- **G1 status:** NOT started. No gate auto-starts.
- **Next authorized action:** await Nick + Knox final acceptance of the patched charter → then G1 (with Gemini's independent blind mechanism map).
- **Stop declaration:** Gate 0 complete + patched per Knox ruling. STOPPED.

## §15 Risks

- **Under-scope:** repeating the original category error — designing the intelligence drawn FROM reservoirs without the governed estate information first LANDS in. (This charter's whole §2 correction.)
- **Over-scope / scope-fence (critical):** over-rotating into "OMNI is a data platform / must match Databricks/Snowflake." OMNI owns the **governance semantics** (taxonomy, authority, partition, lineage, admission/promotion, purpose-of-use, contradiction, freshness, revocation, context-admissibility, evaluation); lakehouse storage/compute + vector/graph engines are **plug-in rails** (`GRD-033`/`GRD-034`; the `GRD-041` "Evidence Plane ≠ universal data store" fence, generalized). Beating LangChain/Palantir = out-governing them, NOT out-building their infra (`GRD-028`).
- **Temporal seam:** as-of/effective/recorded/known time + incremental feeds + corrections + supersession intersect `FWREG-015` (Time in OMNI) — CONSUME/point to it, do not re-derive.
- **Naming ossification:** keep all names planning-vocabulary until G5 (frontier doc's own instruction). Do not mint a plane.

## §16 Gate-0 completion

Charter authored + patched per Knox Gate-0 ruling (11 patches + staged sequencing + A/B/C bias); governance wiring done (§14); Protocol §9 stop report delivered in the relay. **STOP for Nick + Knox final acceptance. Do not begin G1.** *(Historical — G1–G4 have since closed and G5 is a Knox-verified candidate; see the §0 NEWEST-STATE banner + §17.)*

## §17 Execution sequence from the G5 candidate to C4.4 close (the written plan — NOT chat-only)

**Where we are:** G5 candidate committed (`b1ce1e8`) + state-normalized (`865afad`) on `analysis/c4-4-g3-reference-architecture`; Knox has remote-byte-verified. No architecture rounds remain before testing. The remaining work is **proof, not authoring.**

**Step 1 — Freeze the three test packets** (Knox owns; pins against the normalization commit): the packet blob + G3 blob `e364acb` + G4 blob `07f8662` + the App-E §E.2 read-set repo paths (all `<commit>:<path>`) + the two evidence blobs (OpenEvidence `7cb62ed…`, Simulation `a709c5d…`). Nothing runs until pins are set.

**Step 2 — Run three small, frozen, independent, read-only tests** (no new authors, no competing architecture essays):
- **Angle A — constitutional/type adversary** → output = a **mutation ledger only** (every I.4 example + App-A row representable in the declared model · no invalid/compound coordinates · S1/S2/S3 authority separated · Foundry/Router/CNS/Runtime/Simulation gain no hidden commit power · Part I ↔ appendices consistent). No rewrite, no verdict.
- **Angle B — UPGRADED enterprise-substrate / bootstrap / continuous-learning adversary (2026-08-02)** → NOT a shallow re-review of the 14-row table; a **bidirectional** pressure test (OMNI→incumbent AND incumbent→OMNI) plus an enterprise-bootstrap fixture and a 2030/2035 failure forecast, covering ≥18 mandatory mechanisms. Output = a **bidirectional 18-row mechanism matrix + bootstrap trace + OMNI boundary map + material-gap ledger** only. No vendor decision, no rewrite, no verdict. **Full spec + why it exists = §18.**
- **Angle C — downstream authorability (the App-E test)** → a fresh repo-connected agent, no chat history, produces the six outputs (spine §8 · Source-Estate C5 · Knowledge-Reservoir C5 · Intelligence-Foundry-Pilot build-entry · Simulation-Lab boundary charter · comparator build/buy/wrap matrix). Agents do **not** self-adjudicate.

**Step 3 — Knox adjudicates.** All required outputs must PASS; PARTIAL = HOLD. Any failure → **patch only the implicated packet section** (no new G-stage, no broad research).

**Step 4 — G5 acceptance + bounded control-plane sync + C4.4 close** (only after all three angles pass): record G5 acceptance; sync catalog row + read-graph #9j + `FWREG-006/007` + **this charter's committed copy** + the checkpoint handoff **in one closeout commit**; preserve G3/G4 unchanged; push; leave `main` untouched pending the later curated-landing decision.

**Step 5 — hand off downstream:** C4.4 G5 feeds v4 spine §§7–8 (formal ratification of the family label + class placements), the C5 Source-Estate / Knowledge-Reservoir / retrieval-index contracts (to be minted), Agent-Runtime `FWREG-010`, and Task-D (which must consume G5 before closing its knowledge/memory/source-estate/context/Foundry conclusions).

**Hard stops still in force:** no architecture/taxonomy/comparator change · no G3/G4 edit · no promotion · no `main` merge · no checkpoint repoint · no force push · charter/catalog/read-graph/FWREG sync happens ONLY at Step 4, in one closeout commit.

## §18 Enterprise-substrate competitive frame + upgraded Angle B + post-close tracks (Nick + Knox, 2026-08-02 — durable so no downstream agent is blind)

**Why this section exists:** Angle B was authorized as a comparator check, then Nick named the real strategic danger and Knox ruled it upgrades Angle B before G5 acceptance. This is **not** a reopen of C4.4, a new architecture layer, or a new competitor arc — it is a harder proof step + a recorded forward posture. It does not modify the frozen G5 packet (`d01978f` @ `865afad`).

### §18.1 The real competitive danger (named 2026-08-02)
OMNI is **not** primarily at risk on care-native physics (appointment · refill · observation · care-loop) — it is already unusually deep there. OMNI is at risk on **enterprise-substrate bootstrap + continuous learning**: discovering/connecting an existing enterprise estate (hundreds of sources · decades of docs · local vocabularies · SOPs · staff roles · undocumented workflow variants · overrides · microdecisions · operational events · vendor systems · action + correction history · changing authority), modeling its operational reality, letting the enterprise **act through** the model, and capturing ongoing operational change **back** into the system. That is **Palantir's actual center** (Ontology maps sources → real-world objects/properties/links → wired to Actions/functions/security/apps; the action-log turns submitted actions + edits into analyzable objects) — **not** "enterprise search." This is where OMNI, Anthropic-style-runtime, and the big platforms actually compete, and where a ground-up bootstrap is most exposed.

### §18.2 What is already accounted for vs what is NOT closed
- **Already done (not starting from nothing):** C3.8 enterprise-AI-OS pass compared OMNI to tenant ownership · enterprise memory/context · ontology/semantic layer · policy engines · agent runtimes · lineage · continuous assurance · BYOM · tool ecosystems · governed data-value loops — and flagged OMNI thin on enterprise-memory ownership + a semantic-layer legibility gap; it ratified layered tenant ownership · open rails / non-coercive portability · a composed semantic layer (not a god-domain) · supply-chain proof · substrate pluggability · continuous assurance · five separated governed learning loops. C4.4 then added Source Estate · Knowledge-Reservoir admission/reconsideration · integrated Intelligence Foundry · retrieval/context separation · non-authoritative compiled projections · Simulation placement · a 14-mechanism comparator · the F-Self pilot.
- **NOT closed = the end-to-end operating composition** from enterprise bootstrap → continuous learning. The gap is **not** a seventh S-class; it is a **composition proof**: the pieces may already be distributed across Source Estate / domain events / CNS / Platform Loop / Accountability / Agent Runtime / Build OS / domain contracts / connector governance — but that distribution must be **shown to compose**.

### §18.3 Candidate missing mechanism — "enterprise operational imprint" (TEST CONCEPT ONLY — do NOT mint an object/domain)
How an enterprise imprints into OMNI over time: bulk estate acquisition · connectors/APIs · file/corpus intake · CDC / event streams · entity resolution · semantic mapping · local terminology + configuration · workflow observation · policy/SOP ingestion · operational-decision + action/override capture · provenance · temporal/as-of · exception handling · process variation · feedback · reprocessing. Current I.8 has rows for semantic model / action-commit / lineage / retrieval / memory / operations — it does **not** yet isolate **workflow/process/microdecision observation** and **enterprise-bootstrap lifecycle** as first-class mechanisms. Angle B must attack whether these compose.

### §18.4 Upgraded Angle B — scope (read-only adversary; no rewrite, no vendor pick, no verdict; exact frozen relay held by Knox at freeze-time)
Four tests: **(A) OMNI→incumbent** — per mechanism: strongest incumbent + BUY/WRAP/PARTNER/HOST_ON + the semantic/authority boundary OMNI must retain + the switching boundary proving OMNI isn't trapped. **(B) incumbent→OMNI** — per stack: what it exposes as an OMNI gap · what OMNI correctly rejects (hidden authority/ownership/lock-in/liability) · what an incumbent could compose faster than OMNI's paper architecture · what stays healthcare-native OMNI. **(C) enterprise-bootstrap fixture** — multi-location practice/hospital (decades of files · several EHR/CRM/ERP · local SOPs · workforce records · vendor manuals · patient records · inconsistent identifiers · undocumented workflow variants · overrides · exceptions · continuously changing ops) traced bulk-acquisition → continuous-sync → semantic-mapping → workflow-observation → action/override-capture → knowledge-candidate → admission → projection/context → governed-action → outcome → reconsideration, with explicit S1/S2/S3/S5/S6 placement + OMNI-native vs bought/wrapped/partnered/hosted vs currently-absent. **(D) 2030/2035 failure forecast** — brittle assumptions · missing mechanisms · scale cliffs · governance bottlenecks · vendor-dependence · coming commodities.
**Mandatory 18 mechanisms:** 1 source-acquisition/connectors · 2 package/manifest · 3 continuous-sync/CDC · 4 catalog/lineage · 5 ontology/semantic-mapping · 6 entity-resolution · 7 workflow/process/microdecision observation · 8 action/commit · 9 temporal/as-of · 10 access/policy/markings · 11 retrieval/context/ranking · 12 memory/skills/persistence · 13 knowledge-admission/reconsideration · 14 projections/wiki · 15 runtime/scheduler/workers/queues/sandbox · 16 evaluation/simulation/monitoring/rollback · 17 Build-OS/maintenance-cadence · 18 federation/portability.
**Returns:** bidirectional 18-row matrix (mechanism · strongest incumbent · incumbent advantage · OMNI advantage · missing OMNI bootstrap capability · BUILD/BUY/WRAP/PARTNER/HOST_ON/DEFER · owner/destination · confidence) + bootstrap-fixture trace + OMNI boundary map + material-gap ledger + exact source posture/citations.

### §18.5 Default ownership posture (Knox 2026-08-02 — pressure at Angle B, ratify at spine/Task-D; NOT yet binding)
| Layer | Default posture |
|---|---|
| Patient/provider/operator/federation **authority**; consent/purpose/permeability/recipient-sovereignty; domain-owned clinical/operational truth; received-vs-concluded; knowledge admission/reconsideration/use-ceilings; Context-Router authority + proof; ranking/selection/exclusion proof; action/commit boundaries | **OMNI-native** |
| Object storage/WORM/backup/lakehouse compute; OCR/parsing/doc-conversion; CDC/ETL/connectors; workload identity/secrets/sandboxing; model APIs + generic agent orchestration | **Buy / wrap / host** |
| Generic catalog + lineage engines; vector/lexical/graph retrieval + reranking; MPI/de-id/matching; generic policy/ReBAC engines | **Wrap, keep replaceable — OMNI keeps semantic/policy MEANING** |
| Enterprise ontology/action substrate; process-mining / operational-workflow discovery | **Hybrid — Task-D must test Palantir/partner/host/build (thin native layer only if OMNI-specific semantics require it)** |

### §18.6 Adjudication classification (Knox owns; classify BEFORE any edit — prevents another sprawling cycle)
**Constitutional blocker** (S1/S2/S3 collapse · missing authority owner · Foundry can silently commit · bootstrap needs a 7th class) → **G5 HOLD, patch architecture.** · **G5 integration defect** (right G3 mechanism exists but packet misstates/omits) → **patch only G5.** · **Required downstream interface/contract** (connector governance · workflow-observation contract · operational-decision lineage · Runtime↔Foundry scheduling seam · C5 ontology adapter) → **G5 passes only with exact owner + destination + trigger + falsifier.** · **Implementation/procurement debt** (buy CDC · wrap Purview · evaluate Palantir · use LangGraph persistence · procure workload identity) → **route Task-D / procurement / Build-Entry; does NOT reopen the constitution.** · **No defect** (incumbent better-implemented but OMNI boundary already correct) → **maturity honesty only.**

### §18.7 Post-close tracks (after all three angles PASS — replaces "wing the next steps in chat")
- **Track 1 — constitutional + contract realization:** spine §§5/7/8 ratification · Source-Estate C5 · Knowledge-Reservoir C5 · retrieval/index/context C5 · reconsideration/invalidation contract · exact semantic + authority enumerations.
- **Track 2 — runtime + build realization:** full Agent-Runtime `FWREG-010` · Runtime↔Foundry maintenance-mission interface · Platform-E&V + Simulation boundary · F-Self Build-Entry Gate · first internal Intelligence-Foundry pilot.
- **Track 3 — enterprise strategy + composition (Task-D REQUIRED section/packet):** Palantir host/build/partner/wrap · MSFT/AWS/Google/Databricks substrate · LangGraph/Anthropic/OpenAI runtime · ontology/action strategy · enterprise acquisition + continuous-sync · process/microdecision observation · switching/portability · what OMNI owns permanently vs refuses to rebuild. **Track 3 does NOT auto-become another pre-spine architecture arc.**
- The three tracks **converge** at the F-Self pilot and later at the first real enterprise-onboarding fixture.

### §18.8 ★ The two-speed operational-decision/override seam (Gemini surfaced it 2026-08-02; Knox ruled it — the single most load-bearing new insight of this pass)
**The real seam Gemini found:** OMNI currently reads, directionally, `action/override → S1 evidence → candidate → S2 admission → future projection/context`. If that is the *only* path, OMNI is **too slow and too ceremonial for live enterprise operation** — a clinic override would have to wait for Foundry admission to take effect, and Palantir/big-platform systems that reflect the action into the active model instantly would out-maneuver it. **This is a genuine competitive latency risk, not a nitpick.**
**The wrong cure (REJECTED):** minting a 7th class — an "instantaneous/local/working/shadow operational memory" store. That blurs runtime state, domain truth, local configuration, and reusable knowledge, and creates a runtime-owned durable authority (which the Agent-Runtime capture explicitly forbids).
**The correct cure (Knox — a TWO-SPEED law the existing S1–S6 already support):**
- **FAST clock — operational adaptation (no S2 required):** an authorized action/override commits its **operative outcome immediately to its correct owner** — owning-domain **S3** state (e.g., the schedule), and/or **Settings/local-policy/configuration** state if an authorized config changed, and/or **Runtime operational state** for an in-flight temporary condition — **plus immutable S1 decision/run evidence** (proposed action · actual action · override + reason · actor + authority · context/source refs · policy/model/config versions · effective+recorded time · one-time/temporary/continuing scope · resulting domain change · later outcome). The **next Context Router draw consumes the current S3/config state immediately. The clinic never waits for S2 admission to honor its own committed state.**
- **SLOW clock — reusable generalization (S2):** separately, `override+outcome evidence → candidate extraction → recurrence/pattern + applicability envelope → review → possible S2 admission → possible policy/config proposal → owner adopts or rejects`. A repeated override **must not silently become policy** because a model saw a pattern; operator-private behavior must not leak to network/platform-common without a separate gate.
- **Load-bearing law:** *immediate operational adaptation uses owner-committed S3/config state + immutable S1 evidence; reusable generalization uses S2 admission; neither waits for the other, neither substitutes for the other.* Planning name = **"operational decision-and-override lineage seam"** (a descriptive obligation, NOT a new object). The G5 packet already carries the broad distinctions (S1 evidence · S2 admission · S3 owner-state · Foundry inputs from operational incidents · Context Router · owner commit) **but does not yet make this two-speed seam explicit enough to trust** — so the tests must probe it, and if under-specified it is a **G5 integration defect or required downstream interface** (owning-domain/CNS/Runtime contract), **not a 7th class.**

### §18.9 Durable-routing map — where each insight LANDS at closeout (so "wait until D" cannot evaporate)
Nick's standing demand: these insights must be reachable by future Task-D / Runtime / enterprise-onboarding agents **without discovering a closed arc's charter.** Knox's answer: the charter is the *short-term working lock*; at closeout §§17–18 are **compacted** to (current result · durable destinations · exact pointers · unresolved proof obligations) and the substance is **distributed to homes future agents actually read.** This map is authored NOW so the deferral is designed, not vague — but is **executed only at the Step-4 closeout commit** (no control-plane write before G5 acceptance).

| Substance | Durable home (at closeout) | Control-plane pointer |
|---|---|---|
| Two-speed override/learning **law** (§18.8) | accepted **G5 packet** (architecture-level) + **spine §§7/8** + **C5** authoring obligations | read-graph #9j + FWREG |
| Exact action/override **record mechanics** | owning-domain / **CNS** / **Agent-Runtime** contracts | FWREG-010 + contract catalog rows |
| Enterprise-bootstrap **competitive thesis** (§18.1–18.4) | **Task-D** REQUIRED enterprise-composition section/packet | catalog + read-graph route to Task-D |
| build/host/wrap/partner **decisions** (§18.5) | **Task-D** + procurement disposition | FWREG + open-review |
| **F-Self** Intelligence-Foundry pilot | **Build Entry / Build OS** | Build-Entry registry |
| **Enterprise-Bootstrap Steel Thread** | **Task-D** spike charter → post-spine/C5 **Build Entry** | FWREG + Build-Entry |
| status + ordering | charter §§17–18 (compacted at close) | AGENTS + read-graph #15 (at outer repoint, later) |

**Is Task-D the right home?** Partly. Task-D is the correct home for the *enterprise strategy + composition/procurement decision*, but the **architecture-level two-speed law is NOT deferrable to D** — it lands in the accepted G5 packet + spine/C5 (Track 1), because it is constitution, not strategy. And the *proof* that any of this works is **not D at all** — it is the F-Self pilot + Enterprise-Bootstrap Steel Thread under **Build Entry** (Track 2). So the honest map is: **law → G5/spine/C5 (now/Track 1); strategy → Task-D (Track 3); proof → Build Entry (Track 2).** D is one of three homes, not the single bucket.

### §18.10 Non-proof caveat (MUST appear in the C4.4 closeout receipt verbatim-equivalent)
**"C4.4 accepts an architecture and authoring packet. It does NOT prove enterprise implementation. A green Angle B means the architecture exposes the right boundaries and defines credible proof obligations — it does NOT mean OMNI has proven it can operate a hospital like Palantir. Enterprise readiness requires the F-Self pilot and the enterprise-bootstrap steel thread under Build Entry."** This sentence exists to stop a future agent (or future-us) reading a successful A/B/C result as operational victory.

**Bottom line (recorded so it is not lost):** C3.8 answered much of the posture; C4.4 answered much of the constitution; Agent-Runtime + Build-OS hold much of the execution framing. **What is not yet proven is their end-to-end enterprise composition.** The upgraded Angle B (v2) now falsifies the *architecture + names the executable proof obligations*; the two-speed seam (§18.8) is tested now and becomes a contract seam later; and §18.9 maps every insight to a durable home so none of it dies at gate-close.

## §19 Frozen three-angle test prompts — CORRECTED, ready to relay (durable so the run is not chat-only)

> **★ AUTHORITATIVE COPY-READY PROMPTS LIVE AS STANDALONE FILES — send those verbatim, not the condensed index below:**
> - `.cursor/plans/c4_4_g5_tests/C44_G5_ANGLE_A_PROMPT.md`
> - `.cursor/plans/c4_4_g5_tests/C44_G5_ANGLE_B_V2_PROMPT.md`
> - `.cursor/plans/c4_4_g5_tests/C44_G5_ANGLE_C_PROMPT.md`
> - `.cursor/plans/c4_4_g5_tests/C44_G5_THREE_ANGLE_RUNBOOK.md` (who-gets-what · independence rules · output handling · preservation rule)
>
> The §19.A/§19.B/§19.C blocks below are a **condensed index only** — they reference "Original Angle A" and "the 10 pinned objects" and are NOT safe to relay standalone. Use the files above for the actual run.

**⚠ RUN-1 HISTORICAL — these §19 in-charter pins/prompts are the run-1 versions. For run-2 use the repinned authoritative files in `.cursor/plans/c4_4_g5_tests/` (target commit `bcb2b19` / packet blob `674fffa`, with per-angle rerun discipline). Do NOT run the §19 copies below against the old `865afad`/`d01978f` pins.**
>
**Freeze pins (RUN-1 — identical for all three; SUPERSEDED by the repinned files):** repository `ncrawf/main-app` · commit `865afadc7fe0e908acc1f5125006d1d00326c69b` · G5 packet blob `d01978fc39939d09f33fbb95b87866a6f823d1e0` · G3 blob `e364acbad3352457eb8c761d287e91787ea71eea` · G4 blob `07f866207836a22c103505835f80aa495a623101` · Runtime bridge `de5b9a1fc7bf9ff77797d28a53a6fef9ced3ed34` · OpenEvidence `7cb62ed01af289babe625510315e36ef29203f95` · Simulation `a709c5de9be8e47ab6ac47780a0d6abc08a39977` · main `a87d3057b5c224e2b7c660ef040f868fa402e5f8`. Three fresh contexts; none sees this conversation; none writes the repository; none issues the final verdict; Knox adjudicates. Corrections applied vs the pre-Gemini drafts: **Angle A** = +operational-override mutation; **Angle B** = fully replaced by **v2** (composition-falsification + executable-proof-design, NOT capability-proof); **Angle C** = +implementation non-goal + two-speed cross-output law.

### §19.A — Angle A (constitutional/type adversary) — final
Original Angle A (verify pins → read ONLY the G5 packet `d01978f`, G3 `e364acb`, G4 `07f8662` → attack the 14 surfaces: every I.4 axis/enum/cardinality/example; every App-A P01–P19 coordinate; Part-I↔Part-II consistency; S1 receipt/custody vs S2 admission vs S3 operative commit; adoption-time normativity [an S2 unit may never make itself required/prohibited]; admission_scope vs S1/D7 custody; access_policy_refs[] + one-canonical-many-grants; principal/tenant/operator/federation isolation + permeability; Foundry-whole vs Foundry-machinery; Router/CNS/Runtime/Simulation ceilings; correction/reconsideration/revocation/historical-reconstruction; F-Self state transitions; downstream mini-contract compatibility; stale/contradictory appendix architecture). Return per-mutation `C44-A-NNN` {section+shortest quote · event sequence · constitutional distinction · survives? · blocking text if fails · severity candidate · smallest correction · confidence} + failed-mutation ledger + full I.4 conformance table + full P01–P19 table + Part-I/II contradiction ledger + source posture. NO rewrite / NO score / NO verdict / NO repo writes.
**ADDED required mutation — operational override:** *A scheduling agent proposes action X; an authorized clinic manager rejects X for a local constraint not represented in current reusable knowledge.* Trace WITHOUT inventing a 7th class: (1) where the proposal is recorded; (2) where the override/reason is recorded; (3) which owner commits the actual operational state; (4) how the next run sees the current local state immediately; (5) what remains S1 evidence; (6) what may become an S2 candidate; (7) what review precedes reuse; (8) how one-time vs temporary vs continuing local behavior is distinguished; (9) how outcome + later reconsideration link; (10) how the local override is prevented from silently becoming platform-common/network knowledge. **A surviving defect exists if:** the clinic must wait for S2 admission before using its own committed override · reason/context/version lineage is lost · the override becomes reusable knowledge automatically · a runtime-owned memory becomes durable authority · or no owner can represent the local continuing state. STOP: `ANGLE A COMPLETE. NO VERDICT ISSUED. AWAITING KNOX ADJUDICATION.`

### §19.B — Angle B v2 (enterprise-composition adversary) — REPLACES the old Angle B verbatim
Role = independent enterprise-substrate / ontology-action / AI-runtime / process-intelligence / proof-program adversary. **You are NOT proving OMNI works at enterprise scale — a text review cannot prove throughput, latency, reliability, entity-resolution accuracy, sync correctness, or production operability.** You test: (1) is the architecture compositionally complete enough to specify; (2) is OMNI irrationally rebuilding commodity infra; (3) do authority + portability boundaries stay vendor-independent; (4) does the packet name executable proof obligations adequate to test the unproven claims later. No rewrite / no vendor selection / no PASS-HOLD. Verify pins. **Read fully at the commit:** G5 packet · G3 · G4 · C4.4 prior-depth map · C3.8 G2 convergence/translation map · C3.8 G4 disposition · Agent-Runtime & Harness capture · Build-OS layer model · Build-OS rollout sequence · Build Entry Gate · comparator registry · spine-shape §8. External current official primary sources allowed ONLY for Palantir · Microsoft · AWS · Google · IBM · Databricks · LangChain/LangGraph · Anthropic · Glean · OpenAI · OpenEvidence · Tesla, each tagged {source · evidence type · documented-capability|marketing|research|inference}; vendor language is evidence never OMNI authority.
**TEST 1 bidirectional mechanism pressure** — per mechanism: (A) OMNI→incumbent {strongest incumbent · BUY/WRAP/PARTNER/HOST_ON · irrational-to-rebuild · OMNI-owned semantic/authority boundary · switching+exit proof}; (B) incumbent→OMNI {actual OMNI omission/under-spec · what incumbent implements faster · hidden ownership/authority/liability to reject · healthcare-native OMNI responsibility the incumbent can't remove}. Every row distinguishes maturity: architecture-represented | contract-specified | implementation-evidence | evaluated | production-proven | unverified. **No implementation capability gets a PASS from prose.**
**TEST 2 enterprise-bootstrap fixture** — multi-location care operator/hospital {multiple EHR/CRM/ERP · file/doc archives · HL7/FHIR + event feeds · patient/provider/workforce records · local SOPs/policies · vendor/device manuals · identity collisions · undocumented workflow variants · human overrides · exceptions · continuous change · source corrections · permission changes}. Trace bulk-acquisition → manifest/custody → continuous-sync → identity/entity-resolution → semantic-mapping → workflow/process observation → action+override capture → owner-state commit → candidate generalization → S2 admission → projection/context → governed action → outcome → reconsideration. At each stage: S1/S2/S3/S5/S6 placement · owner · authority · temporal proof · failure mode · OMNI-native vs bought/wrapped/partnered/hosted · implementation maturity · required contract · required executable proof.
**TEST 3 operational decision/override TWO-SPEED loop** — fixture: scheduling agent proposes X; authorized manager selects Y for a local undocumented constraint. FAST clock: proposed-action record · authority evaluation · actual action/override · reason · context+source refs · policy/model/config versions · effective+recorded time · one-time/temporary/continuing scope · owning-domain S3 or config commit · immutable S1 decision/run evidence · immediate availability to next authorized context draw. SLOW clock: candidate extraction · recurrence/pattern · applicability envelope · S2 admission · optional policy/config proposal · authorized adoption · outcome+reconsideration. Test that: clinic does NOT wait for S2 to honor current committed state · one local override does NOT become universal knowledge · runtime working memory does NOT become durable authority · operator-private behavior does NOT leak · network promotion requires a separate gate. Do NOT propose a 7th class unless you can prove the current classes cannot represent the trace.
**TEST 4 executable proof-program DESIGN (you do not execute)** — (A) F-Self Intelligence-Foundry Pilot {bounded corpus · actors · interfaces · state transitions · failure injections · proof metrics · entry criteria · exit criteria · Build-Entry deps}; (B) Enterprise-Bootstrap Steel Thread {bounded synthetic enterprise fixture · input formats+source systems · continuous-change feed · identity collisions · workflow/action/override fixture · correction+revocation · scale assumptions · latency/reliability assumptions · failure injections · vendor-switching test · architecture/contract prerequisites · Build-Entry deps · measurable success+stop criteria}. Task-D owns composition/procurement + spike charter; spine/C5 own constitutional+contract prerequisites; Build Entry authorizes execution; Task-D may NOT bypass implementation gates by calling it a "spike."
**Mandatory 18 mechanisms:** 1 source-acquisition/connectors · 2 package/manifest/closure · 3 continuous-sync/CDC/events · 4 catalog/lineage · 5 ontology/semantic-mapping · 6 entity-resolution · 7 workflow/process/microdecision observation · 8 actions/commit/override-lineage · 9 temporal/as-of · 10 access/policy/markings · 11 retrieval/context/ranking/exclusion · 12 runtime memory/skills/persistence · 13 knowledge admission/reconsideration · 14 projections/wiki · 15 runtime scheduler/workers/queues/retries/sandbox · 16 evaluation/simulation/monitoring/release/rollback · 17 Build-OS/maintenance-cadence · 18 federation/portability/cross-sovereign. **Return ONLY:** (1) 18-row bidirectional matrix {mechanism · strongest incumbent · incumbent advantage · OMNI represented-architecture · missing OMNI mechanism/interface · implementation maturity · BUY|BUILD|WRAP|PARTNER|HOST_ON|DEFER · permanent OMNI-owned boundary · switching proof · owner/destination · confidence}; (2) enterprise-bootstrap trace; (3) operational-override two-speed trace; (4) F-Self + Steel-Thread proof-program specs; (5) OMNI boundary map {must-own · build-on-rails · buy/wrap · partner/host · reject}; (6) gap ledger each ∈ {constitutional-blocker candidate · G5-integration-defect candidate · required downstream contract/interface · executable-proof debt · implementation/procurement debt · watch/no-defect}; (7) 2030/2035 failure forecast; (8) exact source posture. NO architecture rewrite / NO vendor selection / NO claim of enterprise capability / NO superiority-moat / NO final verdict. STOP: `ANGLE B v2 COMPLETE. ARCHITECTURE AND PROOF OBLIGATIONS ONLY. NO IMPLEMENTATION CAPABILITY CLAIMED. AWAITING KNOX ADJUDICATION.`

### §19.C — Angle C (downstream authorability) — final
Fresh repo-connected OMNI author, NO chat history. Read ONLY the 10 pinned objects (G5 packet · G3 `e364acb` · G4 `07f8662` · prior-depth map · reservoir frontier `doctrine/cns_and_knowledge_reservoirs_frontier_2026-06-06.md` · spine-shape §8 · comparator registry · Build Entry Gate · OpenEvidence `7cb62ed` · Simulation `a709c5d`), all at `865afad` except the two evidence blobs pinned by blob+path. Do NOT open any other file / search the repo / use chat history / use the web / invent a new class/domain/plane/store/vendor decision. **Produce six outputs:** 1 Spine §8 detailed outline · 2 Source-Estate C5 contract outline · 3 Knowledge-Reservoir C5 contract outline · 4 OMNI Intelligence-Foundry Pilot (F-Self) Build-Entry outline · 5 OMNI Simulation-Lab boundary charter · 6 comparator BUILD/BUY/WRAP/PARTNER/HOST_ON matrix. **Mandatory consistency:** identical S1–S6 model in every output · identical S2 axes+cardinality in outputs 1 & 3 · S2 cannot make itself operative · binding force needs an owner-state adoption link · Intelligence-Foundry-whole ≠ Foundry maintenance machinery · Foundry + Agent-Runtime never commit domain truth · Simulation artifacts are S1 · Simulation eval/release = Platform-E&V/Build-OS · patient context = S3/S6 · observed care outcomes = S1/S3 until generalized+admitted · admission_scope ≠ artifact custody · access_policy_refs[] plural/request-time/default-deny · no false build/maturity claims · no superiority-moat. Each output: exact source pointers · accepted base decisions · G5 candidate decisions · open decisions · prohibited collapses · required proof · maturity language · explicit uncertainties. Also return a NONBINDING conformance matrix + per-claim source pointers + uncertainties + any mandatory concept you could not source. Do NOT issue PASS/HOLD; Knox adjudicates.
**ADDED — NON-GOAL:** this test proves downstream *specification authorability only*. It does NOT prove code-implementability · production scale · runtime correctness · enterprise-bootstrap capability · vendor superiority · moat. Output 4 (F-Self) + Output 6 (comparator matrix) must NAME the later executable proof programs + Build-Entry prerequisites; their presence does NOT count as executing/passing those proofs.
**ADDED — CROSS-OUTPUT LAW:** immediate local operational adaptation must NOT depend on S2 admission. Where an authorized action/override changes current behavior: owner-state/config commits in S3 or the appropriate governance home; immutable decision/run evidence stays S1; reusable generalization is a separate S2 candidate+admission path. Any output that invents a durable runtime-owned "instant memory" authority, or uses S2 as live owner-state, FAILS. STOP: `ANGLE C COMPLETE. NO VERDICT ISSUED. AWAITING KNOX ADJUDICATION.`

### §19.D — after the raw outputs return
Do NOT send them to Opus for patches and do NOT edit them. Bring all three raw outputs to Knox. Knox classifies every finding ∈ {constitutional blocker · G5 integration defect · required downstream interface/contract · executable-proof debt · implementation/procurement debt · no defect} (§18.6) → that classification alone decides whether anything changes in G5. If all three pass: G5 accepted → charter §§17–18 compact into the bounded closeout commit → catalog + read-graph #9j + FWREG-006/007 + charter + handoff synchronize (§18.9) → G3/G4 untouched → C4.4 closes → work proceeds through the three tracks (§18.7). **The closeout receipt MUST carry the §18.10 non-proof caveat.**
