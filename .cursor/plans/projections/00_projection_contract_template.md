# <Projection Name> — Projection Contract

Plane: **P4 Projection** (a reusable READ-MODEL) · **owns NO canonical truth**
Status: `stub` | `drafted`
Source-of-truth relationship: a Projection Contract answers *how is reality composed for viewing/decision.* It **composes** domain truth (P1) into a coherent read-model that multiple surfaces (P5) share, so they don't each re-derive (and drift). It is the structural guard that keeps metrics/profiles/context-packets from becoming fake truth stores. Indexed in `OMNI_Surface_Map_vNext.md`.

---

## §1 Purpose
*What it composes, and for whom/what decision.*

## §2 Composes (source domains + fields — references only)
*The domains + fields it reads. References, never copies-as-truth.*

## §3 Owns nothing — the binding rule
*Explicit: this projection stores no canonical fact. If a value here becomes the source of truth, that is the bug — move it to its owning domain. (T0-15 projection ≠ authority.)*

## §4 Derived metrics / fields
| field | definition | source | derivation |
|---|---|---|---|

## §5 Freshness + lineage + explainability
*How fresh; how the value is traceable to source truth; how it's explained.*

## §6 Who may view
*RBAC + Federation scope. For workforce/operator projections: staff-context ≠ patient-context — different authority/privacy/purpose-of-use constraints.*

## §7 Consumed by (which surfaces)

## §8 Recovered design / prior gems

## §9 Source docs

## §10 Open questions (→ `08`)
