# v4 — C4-pre Late Builder / Structural Gap Register (the idea-field; consume, don't relitigate)

Document type: `plan_or_roadmap` (a **gap-intake register** — the preservation home for candidate structural / builder-facing / enterprise-facing gaps surfaced in the late C4-pre session; NOT doctrine, NOT spine prose, NOT a sweep, NOT a memo) · Authority: `analysis_nonbinding` (`GRD-036`). **Binds nothing.**
Status: `active` 2026-07-06 (Nick + Opus + Knox). Sits with the C4-pre memo/packet + watch list + `08`; **the pre-spine HOLD is LIFTED (2026-07-06 — START Draft 0; this register is the disposition CHECKLIST used DURING chunked worktables + the post-draft pressure pass, NOT a prerequisite convergence project — handoff §3 THE BUILD PLAN).** **2026-07-06 additions: `§ EVRUN-000002 PROMOTION-GATE CLOSURE RESIDUE` (rows V1–V6, wave-2 video corpus's un-closed promotion gate) + `§ C4-PRE BET-CHECK EDGE RESIDUE` (rows E1–E7, the 2026-07-05 bet-memo/packet's declared-open items + open trajectories) + a candidate (not-promoted) session-close observation. All tracked as concepts / open trajectories with tradeoffs — NOT deductions or constraints (do-not-box-future-us framing in that section). + `§ V3 INTEGRATED THESIS TRACE RESIDUE` (rows VT1–VT3 — the abandoned v3 thesis: §C never un-paused though its trigger fired · edition read IN FULL = authored §1→§B, only §C unwritten · **gold inventory + the load-bearing finding that C3.8/bet-check RE-DERIVED much of what v3 §A/§B/§7.x/§3.5 already authored** · now wired back into read-graph #9a) + `§ CNS → POLARIS TRACE RESIDUE` (rows CP1–CP2 — CNS IS defined [canonical contract exists]; frontier "no framework" is stale/narrow; **CP2 CNS↔Polaris RESOLVED + WIRED [C4.1 §1a + CNS §2.1, Knox-patched]: engine[NN §B] → executive[CNS] → alignment-gate[Polaris] → commit[domains]; Polaris = the NAMED trust layer composing v3 §A/§B+, NOT a rename of CNS, NOT the NN, NOT same structure**) + `§ ARC TRACES` (AR-C35 — C3.5 CLEAN/well-homed [the good pattern: closed with a disposition ledger, no leak]; AR-XWALK — cross-body net-new overlap un-crosswalked [= V6/VT3 at arc scope]).**
Registered (New-Artifact §5): catalog row (`01`) · read-graph #9a (v4-authoring input set) · referenced by `v4_C4_spine_authoring_plan` + `..._fresh_agent_readiness_prompt` + `..._thesis_synthesis_plan`. Standing flag: git identity unset.

> **★ WHY THIS EXISTS (Nick's actual problem):** the operator keeps surfacing raw gold; the machine kept deciding per-gem whether it earns a tiny `08` row — flattening the field and forcing relitigation every 6 weeks. **This register is the *idea field*; `08` is the *decision queue*.** The spine/thesis/contract/Build-OS agents **MUST load this** and disposition each row at the relevant section. That is how these stop getting re-discovered from scratch.

> **★ HONESTY RULE (Nick's distrust, correctly aimed):** no one has re-read all of `system_map_three_layers_60706286.plan.md` (the deep legacy map), every contract, the full **EVRUN video corpus**, or the **C3.5 hospital / C3.6 oncology / C3.7 / C3.8 enterprise arcs + the last-few-days add-ons**. **Regulatory/licensing especially: we went DEEP before (WI4 + arcs + video ingestion) — assume prior depth exists somewhere, VERIFY, do NOT re-derive from scratch.** **Several of these are probably already covered there and are DUPLICATE-RISK.** So every row carries a `verification_status` — most are `candidate (unverified vs system-map/contracts)`. **A row is NOT a proven gap until verified.** The pattern this session: ~5 of ~6 "gaps" turned out already-deep (storage, temporal, reservoirs, build, flywheel). Assume the same here: **verify before treating any row as a true absence.**

## Status enum
`candidate` (unverified) · `likely-thin` · `known-partial` · `known-covered→carry-forward` · `duplicate-risk` · `elevation` (exists but not first-class) · `verified-gap`.

## Discipline (how future-us uses this — stops the relitigation)
Every future "what else is missing?" item lands HERE with a status. Before minting an `08` row or writing spine prose about it: (1) check `known existing anchors` + system-map + contracts; (2) set `verification_status`; (3) route to its destination. `08` gets ONLY decision-grade rows; the register holds the full field.

## ★ MANDATORY DISPOSITION PASS (the "consume, don't admire" rule — Knox)
A future agent may NOT say "I read the register." Before any v4 spine prose, it MUST produce a **row-by-row disposition table**: `row → known-anchor(s) checked (incl. system-map + EVRUN corpus + arcs) → covered-cite / duplicate / thin / true-gap → spine impact → thesis impact → C5/contract destination → disposition`. **Every row — ALL of them (A–U + the V-series EVRUN residue + the E-series bet-check residue + any later additions; this register is LIVING) — is consumed, routed, or rejected-with-reason before spine draft 0.** This is not a sweep or a new arc — it's the minimum that stops relitigation. (Wired into `v4_C4_fresh_agent_readiness_prompt.md`.)

---

## The register (late C4-pre builder/structural cluster)

| ID | Candidate gap / concept | Why it matters / failure-mode if missed | Known existing anchors (check these FIRST) | verification_status | Primary destination (spine §, contract, C5) | 08 row? | blocks spine? |
|---|---|---|---|---|---|---|---|
| **A** | **Financial truth / accounting ledger** — double-entry GL, revenue-recognition, processor↔bank↔ledger reconciliation, refunds/chargebacks/disputes, sales-tax, financial close, owner P&L, cash-flow, 1099/payout reporting | Owners live/die by the books; Stripe webhooks ≠ accounting truth | D6 commerce · system-map 1I (commerce/stripe) · 1H (analytics) · BIZOPS | `likely-thin` (D6 = transactions, not "the books") | spine §3b · D6/BIZOPS · **C5 finance/ledger contract** | **REV-204** | no |
| **B** | **RCM / claims / insured-financial machine** — eligibility, benefits, prior-auth, claims, denials, appeals, ERA/EOB posting, superbills, patient-responsibility, payer fee-schedules, medical-necessity | "cash+insurance = one substrate" is hollow without a home; separates real-medicine from cash-pay-toy | D6 · system-map 1L (labs) / jurisdiction · Settings policy · Federation | `likely-thin` | spine §3b · D6/OFC/Settings/Federation · **C5 RCM plan** | **REV-204** (paired) | no |
| **C** | **Regulatory-PRODUCT certification / health-IT compliance** — ONC cert (if applicable), e-prescribing/EPCS product reqs, info-blocking / 21st-Century-Cures / TEFCA / USCDI, audit exports, patient-access exports | The *software* becomes regulated, distinct from the *operator's* license (REV-202); month-6 catastrophic if unnamed. **Nick's flag: a past agent likely already said "register X bc EMR-adjacent" — CHECK system-map/handoffs/WI4 before treating as new.** | WI4 (law-as-authority) · D7 · Federation · P35 · system-map (search "info-blocking"/"ONC"/"Cures"/"certification") | `candidate` (unverified — likely partially in WI4/system-map) | spine §11/§6 · WI4 · **C5 compliance/product-cert path** | **REV-205** | no |
| **D** | **Clinical-safety content dependency** — DDI, drug-allergy, dosing limits, pregnancy/renal/hepatic warnings, contraindication databases (First Databank/Medi-Span), protocol/order-set content lifecycle | Beautiful authority gates still fail if the safety content underneath is absent/stale/unlicensed | duplicate-therapy guardrail (thesis §12.7) · rules-templates engine (1Q) · standard_of_care_object (C3.5) · Medical-Literature reservoir (FWREG-006, future) | `known-partial` (guardrail exists; licensed-content dependency thin) | Clinical-Memory/Observation/RBAC/AI-substrate/Settings · **C5 licensed-content strategy** | **REV-206** | no |
| **E** | **Pricing engine / fee-schedule truth** — cash/membership/bundle rates, payer fee-schedules, discounts, price-at-time-of-service (temporal), quote-vs-final, provider/location-specific, price-versioning | Boundary-fluid visits collapse if pricing isn't governed + temporal | Settings `catalog_item` · D6 (prices) · BIZOPS | `candidate` (catalog exists; pricing-ENGINE depth unverified) | spine §3b · D6/Settings/BIZOPS | no (register) | no |
| **F** | **Rx / e-prescribing / pharmacy execution** — eRx transmission, EPCS (controlled substances), pharmacy routing, dispense status, refill workflows, substitution, med-history import | Hims-like categories AND real medicine hit this fast | Surescripts (vendor lane) · system-map 1G · OFC | `candidate` (Surescripts named; execution pipe unverified) | OFC/P35/Federation/D7/Clinical-Memory · **C5 Rx contract** | no (register) | no |
| **G** | **Provider credentialing / payer enrollment / clinical-roster truth** — NPI, DEA, state license, scope-of-practice, supervision, payer enrollment, credentialing status, medical-director role, facility affiliation | "provider can commit" depends on this being true | RBAC · BIZOPS · WI4 · REV-202 | `candidate` (ties REV-202; roster-truth depth unverified) | Federation/RBAC/BIZOPS/WI4 · REV-202 | via REV-202 | no |
| **H** | **Inventory / supply / device / lot / cold-chain** — injectables, devices, supplies, lot#, expiration, cold-chain, room/device availability, procedure kits, reorder thresholds, procurement, recalls | Aesthetic/procedure/peptide/HRT/surgery-center all touch physical reality; cold-chain = the peptide/medspa wedge | spine-shape §3b (names inventory "first-class" — but NO contract) · BIZOPS · OFC · D5 · C3.5 resource_twin | `likely-thin` (named first-class, not contracted) | spine §3b · **C5 inventory/supply contract** | **REV-208** | no |
| **I** | **Implementation / migration / cutover / legacy-data onboarding** — legacy-EMR import, Mindbody/Boulevard import, spreadsheets, patient-merge, service-menu import, provider-schedule import, historical-chart import, local-term semantic reconciliation, go-live cutover, rollback, data-quality reports | Every real clinic arrives messy; if onboarding can't absorb legacy chaos, GTM dies | Identity (1J merge) · D7 · WI6 (semantic) · Settings · Build-OS · REV-200 §8 (cross-org export flagged) | `likely-thin` (Mindbody was competitor-EVIDENCE, not import-tooling) | Identity/D7/WI6/Settings/Build-OS · **C5 implementation plan** | **REV-207** | no |
| **J** | **AuthN / identity-proofing / session security** — login, MFA, SSO, IdP, session mgmt, device trust, patient-portal login, staff provisioning, password reset, IAL2 identity-proofing, surrogate/caregiver auth | RBAC says what an actor MAY do; AuthN proves the actor IS who they claim — distinct | Identity (1J) · `patient_identity_verifications` (data-layers §2) · RBAC (1D) · Security | `known-partial` (patient-verify exists; staff/provider authN + session unverified) | Identity/RBAC/Security · **C5 auth platform** | no (register) | no |
| **K** | **SRE / platform operations / reliability** — uptime, on-call, incident-response, latency SLOs, queue-depth, retry, RPO/RTO, backups, DR, ops-observability, status-page, customer-comms | Care systems fail *operationally*, not just doctrinally | **data-layers §8 flags deferred Section 1W (SLO catalog: latency/queue-depth/RPO-RTO)** · Build-OS · Security | `known-partial` (Section 1W named-deferred) | Build-OS/Security · **C5 platform-ops** (carry Section 1W forward) | no (register) | no |
| **L** | **Product analytics / behavioral telemetry** — clicks, views, funnels, drop-off, feature-usage, session behavior, campaign attribution, A/B, product signals, operator-usage analytics | Powers GTM/product-learning/alpha — but can become surveillance/extraction if ungoverned | **data-layers §8 flags open `product_analytics_events` (fits neither audit_events nor timeline)** · operating_metrics · REV-201/alpha-firewall | `known-partial` (self-flagged open, §8) | operating_metrics/marketing_attribution/D7 · data-value loops · REV-201/REV-193 | no (register) | no |
| **M** | **Support / success / service-desk** — support tickets, CS tasks, tenant questions, feature-requests, implementation blockers, escalations, internal KB, SLAs | A real platform is *operated*, not just built | WI8 (universal feature-request intake → Build-OS) · BIZOPS | `candidate` (feature-request intake named; support-desk thin) | Build-OS/BIZOPS/Support-surface · product_signal pattern | no (register) | no |
| **N** | **Enterprise procurement / subprocessor / security-review** — BAAs, subprocessor list, security questionnaires, DPA, vendor-risk, SOC2/HITRUST posture, customer audit-rights, DPTs, procurement artifacts | Selling to serious clinics/health-systems/enterprise requires this machinery | **data-layers §8 flags subprocessor RTBF (1V)** · messaging (BAA refs) · WI4 · Security | `likely-thin` (SOC2/HITRUST posture + procurement artifacts) | WI4/Security/D7 · **C5 procurement/compliance** | no (register) | no |
| **O** | **Knowledge Reservoirs / governed knowledge bodies** — clinical, operator, build, threat, regulatory, semantic-mapping, outcome-learning, model/eval, sales/product knowledge | OMNI's flywheel (REV-201) depends on governed reservoirs, NOT model memory | **`doctrine/cns_and_knowledge_reservoirs_frontier_2026-06-06.md` (medium-deep: binding law + reservoir contract + 8-member roster + 6 open Qs)** · REV-200 | `known-partial → DEPTH-PACKET` (frontier doc exists; spine §8 must CONSUME + resolve 6 open Qs — NOT re-derive) | spine §8 · CNS · **C5 reservoir contracts** | (spine §8 consume) | no |
| **P** | **Economics / value / incentive AXIS** — who pays, who benefits, who's steered, who owns alpha, who monetizes which loop, what incentives bend care, what compounding is allowed | Trust (§A) + AI (§B) are named axes; economics/value-flow is scattered even though adversaries attack it | C3.8 G4 5-loop data-value economy · care-neutral firewall (C3.7) · D6 · BIZOPS · WI8 · REV-201 | `elevation` (exists scattered; not first-class like Trust/AI) | spine §3b + §C cross-cutting thread (like the time-axis) · ties REV-201 | via REV-201 | no |
| **Q** | **Raw OMNI-ingestion → interpret → act (OMNI's defining HEART — "it says it in the name")** — receive ANY raw artifact and pivot to governed info + action: screenshot of an OMNI message/event (the musician sampling his own work) · photo of a skincare item (size/shape/lettering → identify) · photo of a laser's treatment settings → record to the right chart field · after-photo w/ burns → identify significance + next action · voice · spreadsheet · text · language | The operator's DAILY 2026 reality (Nick, today); if OMNI can't turn a photo/screenshot into a governed chart entry + action, it fails its defining promise (omni = all) | multimodal-evidence-substrate (registry 098/099) · artifact-representation laws (178) · imaging-Observation seam (157) · media→observation pipeline · D7 media · atomic/spoken/raw-labs intake · system-map 1O media | `likely-partial → ELEVATE` (multimodal-evidence EXISTS in corpus; "screenshot-of-ANYTHING → identify → route → ACT" as the DEFINING heart is under-elevated at §1) | spine §1 (what OMNI IS) + §8 Sense + §13 surfaces · D7/Observation/Clinical-Memory/AI-substrate · **REV-209** | **REV-209** | no |
| **R** | **Offline / degraded-connectivity operation** — care happens where wifi/cell fails; graceful degradation · local-first · sync-on-reconnect · fail-safe | Hard-fail offline = unusable in procedure rooms / rural | 184 fail-safe-on-disconnect · model-placement/edge (142) · data-layers | `candidate` | C5 platform-ops; spine notes fail-safe posture | no | no |
| **S** | **Fraud / abuse / gaming detection** — controlled-substance-seeking patients · upcoding providers · benchmark-gaming operators · intake-gaming | Trust substrate is attackable from the INSIDE; care-harm + legal exposure | documentation-integrity/upcoding-guard (143) · human-fraud-at-intake (123 `participant_verification_state`) · RBAC | `known-partial` | RBAC/CNS/AI-substrate · C5 | no | no |
| **T** | **Consent granularity / patient-facing "who-can-see-what" UX + dynamic re-consent** | Substrate has `consent_grant`; the patient-facing surface + dynamic re-consent is thin | thesis REV-003 · D7 consent · §7.5 grants | `known-partial` (REV-003 flags it) | spine §13 surfaces · D7 · REV-003 | no | no |
| **U** | **Communication compliance** — TCPA (SMS) · CAN-SPAM · consent-to-contact · quiet-hours · opt-out | Real outreach = legal landmines | messaging contract (consent tiers §6.1) · CNS cadence caps · WI4 | `known-partial` | Messaging/CNS · WI4 · C5 | no | no |

---

## § Broad spine-pressure statements (the "how does this cluster change the spine?" answer — candidate spine-lines)
The cluster mostly doesn't add sections — it pressures the spine to STATE truths it currently omits. Candidate lines (fresh agent adjudicates placement + verifies against prior depth):
1. **Logical truth must compile into a physical substrate.** OMNI's laws are meaningless if persistence / tenancy-isolation / audit-event-spine / retention / export / replayability / operational-proof cannot enforce them. (Not "Postgres" — the *law* that ownership → enforceable physical substrate.) → §3/§3b · REV-200.
2. **Regulation/licensing = versioned authority INPUTS, not one-time setup.** *OMNI verifies licensing/regulation/payer/product-cert on a cadence, flags changes, updates gates, and pushes consequences into Federation/RBAC/Settings/Build-OS.* (Nick's exact mechanic.) → §A/WI4 · REV-202/205 + credentialing + RCM.
3. **Insurance is not an alien later-world.** Cash/insurance/membership/retail/VBC = payment+authority REGIMES over ONE care substrate; RCM/claims = staged-build DEPTH, not a separate ontology. → §3b · REV-204.
4. **Business/financial truth is SUBSTRATE truth, not analytics decoration.** The books (ledger/rev-rec/reconciliation/tax/P&L) are first-class. → §3b · REV-204.
5. **Clinical safety knowledge is sourced/versioned/licensed/governed — model output does NOT substitute for safety content.** → §B/Reservoirs/Clinical-Memory · REV-206.
6. **Implementation/migration = the Sense loop applied to the operator's EXISTING reality** (ingest the legacy mess → semantic-reconcile → source-authority → stage as candidate/context/truth). Not plumbing. → §8 Sense + §C · REV-207.
7. **Raw OMNI-ingestion is OMNI's defining heart** — receive ANY raw artifact → interpret → route → govern → ACT. → §1 (what OMNI IS) · REV-209/row Q.

## `08` decision-grade rows (the high-friction subset — full depth lives in the rows above)
Already minted: **REV-201** governance-compatible flywheel (naming) · **REV-202** regulated operating entity · **REV-203** distribution/capital. New this pass: **REV-204** operator-financial-truth / accounting-ledger + RCM boundary (rows A+B) · **REV-205** product-regulatory / certification / eRx-EPCS (row C) · **REV-206** clinical-safety-content dependency (row D) · **REV-207** implementation/migration/legacy-onboarding (row I) · **REV-208** inventory/supply/device/cold-chain (row H). The rest (E/F/G/J/K/L/M/N/O/P) live in this register + point to existing homes (Section 1W, §8 opens, reservoir frontier, REV-201/202); mint an `08` row only if a section author finds a true decision.

## § EVRUN-000002 PROMOTION-GATE CLOSURE RESIDUE (2026-07-06, Nick + Opus — trace-method dig, NOT a new arc)
> **★ WHY THIS IS HERE (so it stops being re-discovered):** the wave-2 video corpus (`EVRUN-2026-000002`, 110/110) closed itself out declaring its OWN promotion gate — **7 spine-law-elevation open-Qs + a keystone-pressure + a security-gap + "drive 194 tensions + ~87 reconciliation-watch items into thesis-v4/contracts/Build-OS."** That gate was **never run as itself.** Only the C3.1 lens audit pulled a slice forward (→ `08` REV-179/180/181/183). Method used = **trace-don't-scan**: take the registry's own declared obligations, follow each to termination, `verify-before-gap`. Result = the residue below. **This is decision-STATUS leakage, not lost ideas** (most concepts survived as working material; their "is-this-spine-grade?" question dissolved). Reconciled against `08` + C3.1 audit + C3.5 F2 matrix + C3-method note; NOT re-derived from the corpus.

| ID | Item (registry origin) | What the trace found | verification_status | Destination / disposition | 08? |
|---|---|---|---|---|---|
| **V1** | EVRUN-000002 promotion gate (the whole thing) | Declared 7 laws + keystone-pressure + 139-gap + 194 tensions + ~87 watch "for the trifecta / drive into v4"; only C3.1's slice ran; the rest deferred **wholesale** onto the fresh spine author, un-closed. | `verified` (process leak) | A bounded **pre-spine promotion-gate closure** before the spine author boots — mint the missing `08` rows below; do NOT re-adjudicate 194 tensions now (that IS spine-author work, and is already a spine acceptance criterion). | n/a (meta) |
| **V2** | `GRD-036` capture-time keystone-pressure (reg T121 / src 166) | **Highest-priority residue.** Explicitly *"FLAG for Knox/Nick — keystone-pressure"*: high-risk sources (PHI/copyrighted/ToS-prohibited/unlicensed) may need gating at **capture-time**, not only promotion. No `08` row; not reflected in the Evidence Router or guardrail digest. Overlaps C3.8 supply-chain/data-governance (un-crosswalked). | `verified-gap` | Evidence-Plane doctrine (`00_evidence_router.md`) + guardrail digest + §A; **mint `08` row** (candidate `capture_time_rights_gate` / `data_liability_shadow`). Crosswalk vs C3.8 G4 supply-chain before minting (may be one finding). | **mint** |
| **V3** | `architecture_is_data_efficiency` (src 160) | Genuinely thin — appears only in the registry + raw sources; nowhere in the v4 estate, no `08` row, no C3.x carry. | `likely-thin` (verified) | §B / spine §6 (structure shifts the scaling curve — the substrate must not relearn known structure) OR explicit drop-with-reason. | mint-or-drop |
| **V4** | `value_from_action_law` (src 155, Nigam Shah/FURM) | **Carried, not orphaned** — a working primitive in the C3.5 F2 matrix (P12 AI-substrate + SUP-181/271/350). BUT the registry's specific ask — *elevate to a NAMED spine law* — got no trifecta decision. | `elevation` (exists, not first-class; decision missing) | Spine §4 (Decide/Act) / §14 (eval) — make the elevation decision (named law vs primitive). Reconcile vs REV-184 (decision-substrate) — adjacent, not identical. | decide |
| **V5** | `workflow_lane_as_architecture_unit` (src 198, final batch) | Concept carried in the C3-method note (*"the model call is not the unit of architecture; the workflow lane is"*), but landed in the FINAL fold **after** C3.1 ran → no elevation decision, no `08` row. | `elevation` (concept carried; decision missing) | Spine §7/§10 (runtime/context routing) — elevation decision. | decide |
| **V6** | 194 tensions (T1–T194) + ~87 reconciliation-watch items | All "routed" **internally** in the registry §3T/§6 (resolution note beside each), but **none promoted** to `08`/contracts/`03`. Deferred wholesale to the spine author (already a spine acceptance criterion — so by-design, not orphaned) — BUT **un-crosswalked vs the C3.5/6/7/8 arcs**, several of which minted overlapping primitives (e.g. `value_from_action`↔C3.5; RL-reward T39/45/46↔C3.7 firewall; T121↔C3.8). | `known-partial` (deferred by design; crosswalk missing) | Spine authoring (adjudicate per acceptance gate). **Flag the ~5–8 `direct_conflict` load-bearing tensions** (T90 care-denial · T72 rubber-stamp · T121 capture-time · T183 auto-promote · T194 data-flywheel · T39/45/46 RL-reward) for VISIBILITY so they aren't buried in §3T. | flag-visible |
| — | (already handled — recorded so no one re-chases) | `evaluation_shapes_system_law`→**REV-179** · `runtime_cost_dominates_law`→**REV-180** · `memory_routing_vs_capability_routing`+`multi_axis_scaling`→**REV-183** · 139 classic-security gap→**REV-181**. | `known-covered` | — | done |

**Discipline note (the ~5-of-6 pattern, live):** this dig started at "3 orphaned laws" and verification knocked it to ~1.5 truly-thin (V2 keystone-pressure + V3) + 2 elevation-decisions-missing (V4/V5) + 1 deferred-by-design (V6). **The field is mostly carried; it is the decision-STATUS that leaks.** That is the thing the spine author must inherit as *labeled-open*, not re-discover.

## § C4-PRE BET-CHECK EDGE RESIDUE (2026-07-06, Nick + Opus — trace of the 2026-07-05 bet-memo/adjudication-packet's OWN declared-open items)
> **★ WHY (contrast to the EVRUN residue above):** the bet-check session **closed its own loop well** — it wrote an output-contract (packet §4) + decision-rule (§5) + mint-list and *executed* it (G-A resolved-in-run · G-B/G-C→REV-194 · G-D→REV-193 · G-F→decided-principle-#2 · packet mints→REV-191/192/193/194 all landed). The residue below is what its OWN §12/§13 flagged as still-open and that has **no home** — smaller than the EVRUN leak precisely because this session executed its mint-list instead of declaring a future gate (see the principle note at bottom). Traced 2026-07-06 vs `08` + watch-list WI10–13 + spine-shape §1.5.
>
> **★ FRAMING (Nick, 2026-07-06 — do NOT box future-us):** these rows (esp. E5–E7) are **tracked trajectory patterns** — we explored them in detail (unknowns named · advantages + disadvantages enumerated); the **DECISION is withheld**. They are NOT vague "open concepts" (they ARE mapped), and NOT deductions / conclusions / constraints (nothing is decided). Capturing the *mapped option-space* keeps it from being re-discovered; it does NOT pre-decide it. The spine/GTM/C5 author picks among the mapped paths; nothing here forecloses.

| ID | Item (origin) | What the trace found | verification_status | Destination / disposition | 08? |
|---|---|---|---|---|---|
| **E1** | G-E competitive counter-response (bet memo §13) | **Mislabeled, effectively un-war-gamed.** §13's update maps G-E → T4, but **T4 is *internal* multi-tenant neutrality** ("power rival derms without leaking alpha") — a DIFFERENT question than G-E's actual content: *"if OMNI's honest-gravity/portability pitch works, incumbents COPY it or crush via bundling."* T3 pressures a *static* aggregator, not an adversary that copies our move. Never actually run; no REV. **Sharpest residue of this batch.** | `verified-gap` (mislabel → un-run) | Bounded pressure (Segment 2 this session) → spine non-goals/strategy (composes onto §11 line 8 structural-impossibility) + `08` if a real decision surfaces. | **candidate** |
| **E2** | G-G phasing / sequencing strategy (bet memo §13) | The time-horizon LENS (Day-1/scaling/mature) got applied *across* trials (tests robustness) — but the memo's actual ask, *"OMNI's window + phasing strategy vs the wave is unnamed,"* is **still unnamed and un-homed** (no REV, not in register/watch). Defensibly GTM/C5-deferred, but currently evaporation-eligible. | `known-partial` (lens applied; strategy unnamed) | Mark as GTM/C5-deferred **with a home** (this row) so it can't vanish; revisit at GTM/roadmap. | no (register-home) |
| **E3** | §12 kill-criteria (11 death-conditions) | Homed **only by reference** — they live in memo §12; WI13 *points* at them. Not operationalized as a standing falsification list anyone monitors. For a bet this size, "a fresh agent must remember to check the memo" ≠ a tracked kill-list. | `known-partial` (referenced, not operationalized) | Spine acceptance / a standing kill-criteria checklist the spine author (and future pressure agents) monitor; keep the WI13 pointer. | no (spine-gate) |
| **E4** | G-H reconnect to C3.8 5 data-value loops (bet memo §13) | Partial — tied via the alpha-ladder L0–L5 → REV-193 (firewall) + REV-201 (flywheel/reservoirs). The **explicit** membrane-flow → 5-loop reconnection is thin (lives in G4, not re-integrated with the membrane frame). | `known-partial` | Spine §3/§C + C3.8 G4 5-loop economy; reconcile membrane-flow ↔ 5 loops at authoring. | via REV-201 |
| **E5** | OMNI-as-acquisition-target (surfaced by the G-E war-game, 2026-07-06) | The **genuinely-new** thing G-E surfaced: incumbents can't *organically* copy honest-gravity (barred by their P&L), so Epic/frontier's real counter is **defensive ACQUISITION** — buy the membrane, neutralize it. That's an unnamed **strategic fork** (independent vs exit), not a spine matter but a corporate-strategy reality that shouldn't ambush a later decision. | `verified-gap` (unnamed fork) | Corporate strategy — ties REV-202 (operating entity) + REV-203 (distribution/capital); register-home now, candidate `08` if a real decision surfaces. NOT spine. | candidate |
| **E6** | Tracked trajectory pattern — deep-vs-shallow governance demand (G-E + §8 demand-crux + §12 converge here) | Several attacks (bundling-crush, shallow-copy, "worse-but-faster," demand-crux, "governance invisible until breach") converge on one recurring question: **is *deep governed-context-movement* a felt/demanded value, or is *shallow export* good enough — and where?** **Explored** (the convergence is mapped; unknown = the answer, likely varies by segment/time/actor). **Decision withheld.** | `tracked-trajectory` (mapped; undecided) | Keep the mapped question visible for the spine/GTM author to decide; do NOT pre-decide it as a single kill-criterion. Ties §8 · §10 · §12 · dimensionality §5. | tracked |
| **E7** | Substrate depth vs surface friction — and the low-dim's role (Nick, 2026-07-06) | Two things to keep separate, both **tracked trajectory patterns (explored; decision withheld)**: (a) **substrate depth** (governed spine under everything) vs **surface friction** (what the user feels per episode) — mapped framing = one uniformly-rich substrate that *computes* lighter/heavier surface regimes; (b) **what the low-dim/simple work IS** — trajectory-A: *practice reps on the one substrate that build toward Mayo-grade* (Nick's "minoxidil as practice for Mayo — it's all one, can't get there without it") · trajectory-B: *concede low-dim as commodity annuity* (memo §5/§10). **Advantages/disadvantages enumerated; NOT decided.** A-advantage: one substrate, reps harden it, no silo (packet T2 success). A-risk: surface friction on simple care could slow us (T6/§12). B-advantage: focus capital on defensible high-dim. B-risk: fragments into two systems (T2 FAIL) + forfeits the practice path. | `tracked-trajectory` (both mapped; undecided) | Carry the mapped paths + tradeoffs to the spine/GTM author to decide; do NOT box. Composes onto dimensionality §5 + memo §3 (Mayo-for-solo) + §8 (felt-experience/governed-underneath). | tracked |

> **★ Candidate observation (NOT a law — for the spine author to consider, `analysis_nonbinding`):** the two digs *suggest* a pattern worth watching: the bet-check closed cleanly because it executed its own mint-list; the EVRUN gate leaked because it declared a future gate that never ran. *Possible* session-close habit: execute your own mint-list before stopping, or hand open items to `08`/register as labeled-open — don't exit on a declared-but-unrun gate. **Tracked as a candidate, not promoted to doctrine; do not box future-us.**

## § V3 INTEGRATED THESIS TRACE RESIDUE (2026-07-06, Nick + Opus — trace of the "abandoned partial v3 thesis" Nick flagged)
> **★ WHAT THE TRACE FOUND:** the v3 integrated thesis (`omni_thesis_v3_integrated_2026-06-05.md`) was authored **in parts** and got through roughly **§1–§8** (its own checkpoint note: §7.1–§7.5.4 preserve-batch DONE · §7.6 CNS · §7.7 P4/P5 bridge ["HIGHEST watch"] · §7.8 · §8 loops with Reconciliation-Ledger Lanes 2&3 LANDED — all marked **"AWAITING CHECKPOINT REVIEW"**) — then **abandoned when the project pivoted to the v4 C2→C3→arcs→C4 sequence.** Nick's memory is exact. Two dangling threads, both the SAME status-drift pattern as V1 (a trigger/gate fired, the follow-through never ran). Verified vs the spine file, the integrated edition, EVRUN-000001/002 registries, the pipeline doctrine, and the v4 #9a input set — NOT re-derived.

| ID | Item | What the trace found | verification_status | Destination / disposition | 08? |
|---|---|---|---|---|---|
| **VT1** | Thesis **§C never un-paused** though its trigger fired | §C (Governed Capability Exchange / Permeability — the whole external-world + agent-interop + membrane/portability spine) was PAUSED 2026-06-05 pending "the first outside-learning analysis run's §C-impact triage." **Those runs (EVRUN-000001/002) RAN and explicitly flagged "§C unpause SIGNAL"** (agent-economy · front-door-governed-agent · capability-topology all hit §C). But the v3 spine STILL reads `§C ⏸ PAUSED` — because v3 authoring was abandoned at the pivot. **NOT lost** (v4 heavily flags §C as the PRIMARY landing zone for membrane/portability — bet memo, watch list §682, C3.8 handoff §35) — but formally stuck "paused pending a run that already completed." Same leak as V1. | `verified-gap` (status-drift; trigger fired, un-pause never ran) | v4 spine authors §C anyway (already the plan) — but should **consume the EVRUN §C-impact triage + "unpause signal" rows** as input, and formally clear the stale PAUSE so no agent treats §C as still-blocked. | note/clear-pause |
| **VT2** | Mid-authored v3 integrated prose (§1–§8 + Reconciliation Ledger) **not in the v4 load path** | Real authored care-first integrated prose (§1–§8, incl. the §7.7 "HIGHEST watch" P4/P5 bridge + Ledger Lanes 2&3) sits "awaiting checkpoint review," abandoned. The v4 `#9a` input set's Frame = v2 (preservation) + v3 integrated *spine* §0 — it did **NOT** list the partially-authored *integrated edition*. **DECIDED (Nick, 2026-07-06): cut-off-and-dropped, NOT intentional supersession.** v3 existed for a reason and likely holds **gems/gold**; it would be a mistake to obey v2 + pluck-from-EVRUNs instead. It may be **stale in areas → flag-and-verify, do NOT discard.** | `known-covered→carry-forward` (decided: required v4 consultable) | **WIRED:** added `omni_thesis_v3_integrated_2026-06-05.md` §1–§8 + Reconciliation Ledger to read-graph **#9a (2) Frame** as a REQUIRED consultable (mine for gems; flag-stale-don't-discard; do NOT obey v2-only). v4 spine author consults it section-by-section. | wired |

| **VT3** | v3 integrated edition read IN FULL (2026-07-06) — **gold inventory + overlap-with-later-sessions** | **The abandoned edition is authored §1 → §B** (RESUME MARKER confirms: §6.x, §7.1–§7.8, §8/§8.5/§8.6, §A, §B all DONE, "awaiting checkpoint review"); **ONLY §C is unwritten** (paused, VT1) + §10–§15 + Final-Assembly-Gate remain. Depth is very high. **★ Load-bearing OVERLAP observation (tracked, NOT a deduction): a large share of what C3.8 + the bet-check treated as net-new was ALREADY deeply authored in v3** — see gold list below. The spine author must **reconcile v3's authored substrate WITH the arc/bet-check findings** (consolidate, don't re-derive), and must NOT treat arc/bet-check lines as greenfield. | `known-covered→carry-forward` (rich; overlaps later work; PEER not benchmark) | v4 spine reconciles v3 and post-v3 **as peers** at each section — consult BOTH the v3 counterpart AND the arc/bet-check/Polaris/REV deltas, reconcile into one; **NOT v3-superseded (don't re-derive) and NOT v3-as-benchmark (don't just layer on top).** v3 = substrate/care-physics depth; post-v3 = strategic/enterprise/consumer/AI-native/GTM. Flag genuine stale v3 spots. | reconcile-as-peers |

**v3 GOLD INVENTORY (read-in-full 2026-07-06 — the "gems and gold" Nick expected; each is authored doctrine, not a stub):**
- **§A Trust/Authority/Permeability axis** — decomposition table (Identity/Federation/RBAC/D7/CNS-Meta/owning-domain) + 4-way admissibility spine + `GRD-035` "no single domain owns a cross-cutting concern" + **`capability_envelope` ≠ `delegated_authority_envelope` ≠ `capability_contract`** (three-object family) + trust-relocated-not-eliminated + **non-human-authority-decomposed-NOT-AI's** + two-gate "a match never authorizes." **← this IS the "agent-native edge / open-rails-closed-authority membrane" the bet-check re-surfaced (WI9/§11 line 3/6). Deeply pre-authored.**
- **§B AI substrate axis** — substrate-vs-care boundary **structurally enforced** (RLS + capability + type-system + audit, not policy) + Capability/Model/Tool plane (AI #12) + **Context Router** (context = routed strategy) + autonomy A0–X/HITL/least-agency + `model_version_of_record` anti-collapse trinity + AI-owns-capability-NOT-authority. **← this IS the "AI Alpha Firewall / BYOM / candidate-only" material (bet-check T6). Pre-authored.**
- **§7.5.1 per-event ownership — 7 orthogonal dimensions + the anti-collapse system** (surface/channel/operator/clinical/commerce/custody/coordination + source_authority/clinical_adoption_state/model_version_of_record). **← this IS C3.8's "layered tenant-ownership delta."**
- **§7.4 boundary modes + `care_team_graph` + `degraded_safety_state` + patient-partitioning discipline.** **← this IS "boundary-fluid / computed regimes" (bet-check T2 / WI12), pre-authored.**
- **§7.3 care_commitment** (4-level framework · threshold rule "not every event is a care commitment" · ambiguity-as-commitment · 5 scopes) · **§7.7 master projection doctrine** ("projections expose · capabilities act · domains commit · audit proves" + degrade-declare states) **← "one felt experience, many governed authorities underneath" (bet memo §8).**
- **§3.5 comparator pressure-test** (Q1–Q13: Henry Ford/Hims/RealSelf/Cursor/Apple/Amazon/Tesla/Air-travel/Restaurant-SaaS/Meta/Whole Foods) + **structural-impossibility framing** ("Henry Ford CAN'T become OMNI without giving up institutional gravity"). **← this IS bet memo §2 "structural-impossibility positioning," authored in far more depth.**
- **§8 two interlocking governed loops** (Sense/Act + authority gates + 12-modality universal flow + "loops touch outside via §C, classified-before-counting") + **workforce-as-subject** · **§6.5/§6.6/§6.7/§6.8** (hub-and-spoke brand arch · visible-provider "OMNI creates safety so providers can be visible" · 5-tier operator vocabulary · operator-pluralism/anti-institutional-gravity) · **Constitutional Reconciliation Ledger (4 lanes) + Final Assembly Gate** (the anti-flatten authoring machinery).

**v3 STALE / MISSING (honest — it predates the later work; flag-and-layer, don't discard):** no §C (unwritten) · predates the **enterprise-AI-OS lens (C3.8)** · predates the **consumer-front-door / demand-crux / "the alpha" naming (bet-check)** · predates **Polaris** naming (WI7/C4.1) · predates **reflexive-build / Build-alpha (REV-199)** · predates **raw-ingestion-heart (REV-209)** · predates the **dimensionality-DISCOVERED** framing (though §7.4 boundary modes is the closest pre-built kin). So: **v3 = deep on substrate physics + care doctrine; the later sessions = the strategic/GTM/enterprise/consumer/AI-native layers on top.** Neither supersedes the other; they COMPOSE — which is exactly why v3 had to come back into the load path.

> **★ Cross-trace pattern (now four digs — candidate observation, NOT a law):** EVRUN promotion gate (V1), §C un-pause (VT1), the v3 edition abandonment (VT2/VT3), vs the clean bet-check (E-section): **work is rarely *lost*; what leaks is the FOLLOW-THROUGH on a declared gate/trigger/pause once the project pivots — and the deepest cost is silent RE-DERIVATION** (later sessions rebuilt what v3 already had, because v3 fell out of the frame). The bet-check avoided it by executing its mint-list; the others pivoted before closing. (Tracked; do not box.)

## § CNS → POLARIS TRACE RESIDUE (2026-07-06, Nick + Opus — trace of the "CNS thing → Polaris" thread Nick flagged)
> **★ WHAT THE TRACE FOUND:** **Polaris (C4.1) is well-homed** — locked 2026-07-04, registered (catalog + read-graph #9a + WI7 + handoff), in the load path; not a residue. **CNS is the residue.** Two findings, one of them the SAME trigger-fired-follow-through-didn't pattern as V1/VT1. Verified vs the frontier doc, C4.1, `contracts/CNS_orchestration_contract.md`, v3 §B, spine-shape §5/§8, spine-authoring §1#5.

| ID | Item | What the trace found | verification_status | Destination / disposition | 08? |
|---|---|---|---|---|---|
| **CP1** | CNS framework — richly PROCESSED (contract + EVRUN-000001 dynamic-CNS cluster + §2A family A); "undefined" was wrong | **Correction (deep-read 2026-07-06, incl. EVRUN-000001 registry finally read):** CNS is NOT undefined. Sources: (a) `contracts/CNS_orchestration_contract.md` (canonical — 3 scopes · universal flow · orchestration_run/cns_decision/context_packet/resolver/trace_lineage · rules-templates §9.2 · marketing §9.3 · workforce §9.4 · safety-orch §10.2); (b) v3 §B (Context Router, autonomy A0–X); (c) **EVRUN-000001 cluster row 33 "CNS as live dynamic system" + §2A family A** — `neural_network_CNS_doctrine` + `CNS_activation_state` · `state_compute_overlap` · `signal_kick`/`recurrent_orchestration_loop` · `convergence_candidate` · `context_resonance`/`suppression_pathway` · `trainable_coupling` · `time_domain_CNS` (anchored 087/085/088/059); (d) REV-176. The frontier doc's "CNS has no framework" is **stale/narrow** — genuine gap = family-naming + reservoir roster + the 6 frontier Qs. Revisit trigger (neural-network batch = EVRUN-000002) **fired 06-13**. | `known-partial` (CNS richly processed; narrow frontier gap; stale "don't-solve-yet" status) | Spine §5 = CNS framework — build ON the contract + EVRUN-000001 §2A family A (do NOT re-derive); §8 = reservoir family + 6 Qs; clear the frontier doc's stale status. | note/clear-status |
| **CP2** | CNS ↔ Polaris — **RESOLVED (proposed; pending Nick+Knox ratification, 2026-07-06)** — grounded in CNS-contract §1/§3/§10 + C4.1 §1 | **They are DISTINCT and coexist. CNS is NOT renamed to Polaris; the neural network is NEITHER; they do NOT share structure.** **★ Cross-ref (deep-read 2026-07-06): the "is CNS/OMNI a neural network?" question is PRE-PROCESSED in EVRUN-000001 (`neural_network_CNS_doctrine`, row 33 + §2A A) — CNS = a "governed system-level neural network" as a SOFTWARE-DYNAMICS METAPHOR (keep the CNS name · not literal chips · not symbolic-rebrand · not router-demotion · bounded GRD-029). The resolution ALIGNS (CNS = the dynamic orchestration/nervous-system layer; "neural network" = metaphor for CNS specifically, NOT the whole of OMNI [= operating network] and NOT Polaris) — it was built from summaries and under-cited this doctrine; now cited.** Four roles on ONE pipeline: **(1) AI substrate / neural network (§B) = the ENGINE** (pluggable model layer; generates candidates via extraction) → **(2) CNS = the EXECUTIVE / active orchestration layer** (assembles context, routes candidates through resolver/policy/authority, drives owning-domain commits; 3 scopes; orchestration_run/cns_decision/context_packet/trace_lineage; §3.7 Layer 3) → **(3) Polaris = the ALIGNMENT GATE** (the NAMED composition of pre-existing invariants — source_authority_map · RBAC · REV-184 · semantic reconciliation · D7 · legal/policy overlays · capability/model envelopes · proof · projection≠authority — that candidates pass BEFORE commit; NO objects of its own; it NAMES the gate already embedded in CNS's "candidate → resolver/policy/authority → owning-domain commit" step) → **(4) owning domain / authorized actor = COMMIT.** *CNS moves; Polaris keeps it true; the model is the engine; domains commit.* **Polaris is not a new mechanism — it's the legibility NAME for CNS's alignment gate + the cross-cutting invariants.** | `RATIFIED + WIRED` (Nick + Knox 2026-07-06) | **DONE — wired** into C4.1 **§1a** + CNS contract **§2.1**. Trifecta refinements folded: Polaris is **cross-cutting (fires at every seam), NOT linear/"a step after CNS"**; **Polaris COMPOSES, does not enforce by itself** (owners enforce; no objects of its own — anti-god-domain `GRD-026`/`GRD-035`); **Polaris ≈ the NAME for v3 §A+§B+WI6+WI4+proof+REV-184** (not net-new; the v3↔post-v3 reconciliation in action); **"neural network" → governed operating network/organism** (CNS = nervous-system-not-cortex; literal NNs = AI substrate); brand teeth intact (ask OMNI daily · Polaris = proof-moment + boardroom "powered by Polaris" · Stripe-Radar/AWS-IAM pattern). **Knox 3-patch pass folded (2026-07-06): (1) Polaris = strategically-new-as-NAMED-trust-layer, not "not-new" [don't minimize]; (2) CNS "resolves candidates through the Polaris-named constraint stack," NOT "invokes Polaris" [no central enforcer]; (3) ratification levels — the RELATIONSHIP is locked, the metaphors (≈§A/§B · "around the arrows" · operating-organism) are refined/tunable.** NOT deferred to spine author. | wired |

## § ARC TRACES (2026-07-06, Nick + Opus — the C3.5/6/7/8 pressure arcs; one at a time)
> **★ WHY:** trace each arc's terminus (declared net-new + open-review) vs homes, same method. **First result flips the session pattern:** the arcs that ran a disposition-ledger/mint-list closed CLEANLY (no leak) — the leak was specific to bodies that declared a future gate/trigger and pivoted (video corpus V1, v3 §C VT1, CNS CP1).

| ID | Arc | What the trace found | verification_status | Disposition | 08? |
|---|---|---|---|---|---|
| **AR-C35** | C3.5 hospital/EHR (G1→G4 + G4.1) | **CLEAN / well-homed — the GOOD pattern.** G4 closed with a full **disposition ledger** (every family P1–P40 → CONFIRMED/EXTEND/NET-NEW/OPEN-REVIEW/BUILD-OS; "nothing lost/inflated"), a **verified desk-check** (G4.1, all 15 contracts read, net-new honestly shrank 11→6), **open-review routed to `08`** (→ REV-184–190), and an **artifact-use map** (§11) preventing graveyarding. It executed its own disposition — same discipline as the bet-check (E-section). Verdict: survivability + superiority PASS; substrate "mostly held"; small sharp net-new (`administration_event` · `movement_state` · `external_capability`/P35 · attention-economy · `ai_consent_scope` · `degraded_mode` · simulation-harness). | `known-covered` (closed w/ integrity ledger; NO leak) | Homed as a v4 pressure input (read-graph #9a: C3.5 G4§11+G4.1; spine-authoring §1#4). Cite **G4.1** (not G4 alone) for disposition. | done |
| **AR-C36** | C3.6 oncology/trials (G1→G4) | **CLEAN / well-homed — same good pattern.** Resolved C3.5's homeless-three (P29 `trial_protocol` = cross-domain contract 10-teeth · P28 RWE = own sub-plane REV-174 · P35 ladder). Closed with readiness verdict (§0.5: architectural-YES / build-NO / operational-UNTESTED — honest 3-way split) + disposition ledger + open-review→`08` (§7) + artifact-use map (§8.5). Net-new = 4 (`source_authority_map` [field-level index, ~80% composition] · `knowledge_partition` [blinding≠RBAC] · `trial_protocol` · `chain_of_identity` [cell-therapy]) + 5 extend; **ALCOA++/Part-11 = REUSE not net-new** (substrate held). | `known-covered` (closed w/ ledger; NO leak) | Homed as v4 pressure input (read-graph #9a C3.6 G§8.5). `source_authority_map` overlaps CNS §7.5.1/Polaris (→ AR-XWALK). | done |
| **AR-C36-gap** | C3.6 "composition asserted, not traced" → **DEEP-TRACED 2026-07-06 (Nick + Opus); all 3 COMPOSE, zero net-new** | Traced now (not deferred): **(1) Endpoint-adjudication/BICR** = REV-184 governed resolution (blinded committee = `decision_owner`; two `consult_opinion`s + `dissent_state` + adjudicator) on a shared `observation`, under `knowledge_partition`, committing to the endpoint plane not care plane (`source_authority_map` = literally D-135). **(2) AE-causality/attribution** = `derived_assertion` (investigator judgment + attribution enum) → fires a timed `care_obligation` per `trial_protocol`'s AE/SAE-rules tooth → outbound report via P35. **(3) GCP delegation/site-activation** = RBAC `delegated_authority_envelope` (v3 §A; trial-scoped, training-gated via BIZOPS `training_competency`, PI-e-signed D7) + operator `site-activation` state + `trial_protocol` approval/activation. **All compose; C3.6's assertion was correct.** | `verified-composes` (deep-traced; NO net-new) | **ONE small delta:** `trial_protocol` teeth should explicitly carry a *delegable-task taxonomy* (refinement, not a new object). Caveat: reasoning-trace (like C3.6's 28 E-rows); ultimate proof = harness + contract build. NOT deferred to spine author. | done (1 teeth-refinement) |
| **AR-C37** | C3.7 trial-access/accrual (G1→G4) | **CLEAN / well-homed — 3rd clean arc.** Same discipline (3-way readiness · delta package §2 · product laws §3 · open-review→`08` §6 · artifact-use map §7). Load-bearing = **firewall-invariance** (accrual/revenue/retention must NOT bend patient-facing clinical recommendation — structural economically-blind engine; ties Polaris + C3.8 fee-firewall + "the alpha"). Small net-new (`trial_match_candidate` · `cohort_slot_state` · `honest_null_terminal`) + extensions; `screening_yield_funnel`=REV-174 projection; `research_watch_trigger`=subscriber; care-quality primitives route OUT (`GRD-026`). **★ Its biggest finding — the §4 governed decision/resolution primitive — was routed out and BECAME REV-184 (CLOSED, 7 spine lines; field-set→C5); it even answered "pattern-vs-object?" correctly (pattern + domain records, not a god-object). No leak; the finding GRADUATED.** | `known-covered` (closed; §4→REV-184 closed) | Homed as v4 pressure input (read-graph #9a C3.7 G§7; REV-184 §0 canonical). | done |
| **AR-C38** | C3.8 enterprise-AI-OS (G1a→G4) | **CLEAN / exemplary — 4th clean arc; the MODEL closeout.** §1 disposition ledger routes EVERY G1b/G2/G3 finding into 6 categories (validated / spine-delta / contract-extension / C5 / open-review / reject; `GRD-043` met, no unrouted tension) + §2 operator-ratified posture decisions (tenant-ownership · reject-lock-in · BYOM · supply-chain-proof-fabric · continuous-security-LAW · semantic-layer-composed-not-god-domain) + §3 data-value 5-loop economy + §4 worked example. Verdict: enterprise pressure did NOT break core physics (48 breakers 15/20/5/8). Bet-check already built on it (Verdict B). | `known-covered` (exemplary closeout; NO leak) | Homed: read-graph #9a #10 (G4 = primary C3.8 input) + handoff. Known C5-owed: `AI/Model-Lineage contract` draft (§1C) + continuous-security lane-shape (open-review §2.5) — both properly deferred. | done |
| **AR-DONE** | **ARC TRACK COMPLETE (4/4 clean) — the session pattern is now definitive** | **All six bodies traced. Two piles: LEAKED** (declared a gate/trigger, pivoted before closing) = video corpus (V1–V6) · v3 §C/edition (VT1–VT3) · CNS framework (CP1) — **vs CLEAN** (ran a disposition ledger / mint-list) = bet-check (E) · C3.5 · C3.6 · C3.7 · C3.8 (AR-C35→C38). **The differentiator is not topic or freshness — it is whether the body executed its own routing before the project pivoted.** The four arcs + bet-check all carry disposition ledgers + artifact-use maps + open-review→`08`; they did not leak. | `verified` (pattern complete) | The one live consolidation task that survives = **AR-XWALK** (cross-body net-new overlap, un-crosswalked). Everything else is homed/resolved/wired. **★ 2026-07-06 (Nick + Knox): AR-XWALK is NOT a prerequisite convergence project — it is executed INSIDE the per-section spine worktables + the post-draft pressure pass. Do NOT build a spine-before-the-spine.** | — |
| **AR-XWALK** | Cross-arc / cross-body net-new OVERLAP — un-crosswalked (NOT new; = V6/VT3 restated at arc scope) | C3.5's net-new **overlaps other bodies, un-reconciled:** P35 external-capability ↔ bet-check **agent-native edge** ↔ v3 §A membrane · attention-economy (P5) ↔ **video-registry** memory/context net-new · `ai_consent_scope`/`ai_decision_log` ↔ **Polaris** invariants · P39 simulation-harness ↔ **REV-199 reflexive-build** ↔ Build-OS/video workstream (**C3.5 §7 itself flags this reconciliation is owed**). No one has cross-walked all bodies' net-new against each other. **★ Input-set correction (deep-read 2026-07-06): the cross-walk's PRIMARY baseline is `EVRUN-2026-000001`'s registry §1 (~40 clusters) + §2A (net-new families A–J) — the cumulative baseline that EVRUN-000002 is built *on top of* — NOT just the wave-2 registry. Any cross-walk that starts at 000002 mis-scopes it (000002 dedups AGAINST 000001 §2A).** | `known-partial` (same overlap as V6/VT3, arc scope) | Spine author reconciles net-new across ALL bodies: **EVRUN-000001 §2A (baseline) + EVRUN-000002 §2A (wave-2 delta) + 4 arcs (P1–P40) + bet-check + v3 §A/§B** at the relevant sections — dedupe, don't re-mint. Ties V6 (194 tensions un-crosswalked) + VT3 (v3 re-derivation). **★ 2026-07-12: the two NEW loop captures were cross-walked (bounded, pre-Task-D) in the `§ AR-XWALK — LOOP-CAPTURE CROSS-WALK` section below; the full-estate cross-walk stays a during-authoring activity.** | flag (full-estate) · done (loop-captures §below) |

## § AR-XWALK — LOOP-CAPTURE CROSS-WALK (bounded · Platform-weighted · executed 2026-07-12, pre-Task-D)
> **★ SCOPE (per Nick + Knox, do not let this become a spine-before-the-spine):** this is the **bounded** cross-walk of the TWO new loop captures only (`v4_C4_platform_loop_capture.md` + `v4_C4_governed_reporting_resolution_capture.md`) — the freshest, un-dispositioned, highest-reinvention-risk islands, created after AR-DONE. **The full-estate AR-XWALK (row above) stays a during-authoring activity** (do not converge everything now). Verdicts use the 5-scale (Knox), mapped to this register's enum: **EXISTS_DEEPER**≈`known-covered/duplicate-risk` · **SHARPENS_EXISTING**≈`known-partial` · **NOVEL_COMPOSITION**≈`elevation` (new composition of existing parts — the category that matters most) · **GENUINELY_NEW**≈`verified-gap` · **CONFLICTS_OR_REPLACES**=new. **No silent novelty: "no prior home found" is recorded explicitly.** Preservation: cite-and-defer to deeper homes; do NOT copy prior prose into the captures.
>
> **★ HEADLINE (what the cross-walk learned):** the Platform Loop's reinvention risk is **NOT mostly duplication — it is mostly *un-homed new surface* (release/deployment/reconciliation mechanics have no prior estate home) + one naming CONFLICT** (`09`). The Accountability Loop is mostly **NOVEL_COMPOSITION over existing physics** (Knox's "parts covered, composition new" — correct). Net: cite-and-defer where deeper homes exist (Federation topology, §B routing, degraded-mode, provenance spine, SLO carry-forward); preserve the genuinely-new release/runtime mechanics + the healthcare hard-laws; fix the `09` collision.

### Platform Loop (deep pass)
| # | capture concept | prior home(s) | verdict | canonical home / action | spine / C5 consequence |
|---|---|---|---|---|---|
| PX1 | **Platform Loop as PARENT** over Build-OS/Release/Runtime | no "Platform Loop" in System Map; recursive-3-loop framing = WI15 (new this session) | **NOVEL_COMPOSITION** | keep the parent composition (WI15) | spine: name Care·Platform·Accountability as coupled loops · C5: Platform contract(s) |
| PX2 | **"Build-OS" = `09` detail child** (the capture's claim) | `09`/`10`/`11` = the **meta-build-of-OMNI** system (`09` 5-layer model Truth/Execution/Command/Runtime-Proof/Governance · `10` rollout sequence · `11` build-entry gate) — a DIFFERENT referent than the product's change→release-candidate control plane | **CONFLICTS_OR_REPLACES** (two systems, one name) | **RESOLVED 2026-07-12 (Knox):** the product plane renamed **Platform Change Engineering (PCE)**; **`09`/`10`/`11` preserved UNCHANGED** as the orthogonal OMNI Build-OS meta-system that **governs** PCE (not a child). Both captures updated (header/relationship/§8/§12/§5/§18/seams). `09` overlaps PCE only at its Execution+Runtime-Proof layers (= REV-199 reflexive-build) — governance relationship, not identity. | spine: build-of-OMNI (meta) vs product-change-engineering are distinct · C5: contracts must not collide on "Build-OS" |
| PX3 | Build/Release/Runtime custody separation + release-ops lifecycles (release/deployment/rollout/recall/migration) | **none in estate** (product has no release/deployment model); Apollo comparator = external (§20) | **GENUINELY_NEW** (no prior home — recorded) | preserve; this is real new surface | C5: new Release-Ops + Runtime-Ops contracts |
| PX4 | Platform identity / execution-provenance graph (capability_version·service_version·release·deployment) | care-side provenance EXISTS_DEEPER: `source_authority_map` (C3.6) · `ai_decision_log` · `trace_lineage` · `model_version_of_record` (§B/§9.1) · data_layers `replayability_preserved`/`source_assertion_id`. **Deployment/version provenance NOT covered anywhere.** | **NOVEL_COMPOSITION** | cite-and-defer to the provenance spine; add ONLY the deployment/version-artifact graph | spine: the provenance backbone Care/Accountability read · C5: catalog/registry + provenance |
| PX5 | change_set / change_item / risk-tier model | none (Apollo external); risk axes echo REV-184 blast-radius+reversibility + C3.5 P40 | **GENUINELY_NEW** (mechanism) / risk-axes **SHARPEN** REV-184 | preserve; **align risk axes to REV-184** (blast-radius/reversibility/trust_horizon) — do not fork the risk vocabulary | C5: Release contract |
| PX6 | desired-vs-observed state + reconciliation + precedence laws | none in estate (Apollo external); precedence composes Federation operator-neutrality + source-authority + REV-184 world-model-honesty (stale-source = trust_horizon) | **NOVEL_COMPOSITION** | preserve; **anchor precedence laws to Federation + REV-184** (stale/unreachable-source rule = trust_horizon), don't re-derive | C5: Runtime/reconciliation |
| PX7 | `operational_plan` (the control-loop unit) | none Platform-side; CNS `orchestration_run`/`cns_decision`/`resolver`/`trace_lineage` = the CARE analog | **NOVEL_COMPOSITION** | reconcile shape with CNS orchestration primitives (don't fork the orchestration vocabulary); suppression/safe-halt = Task-D pattern, not canon | C5: reconcile vs CNS contract |
| PX8 | runtime service-health CONTRACT (liveness/readiness/**correctness**/**freshness**/degraded/last-known-good…) | **EXISTS_DEEPER (partial):** data_layers **Section 1W SLO catalog (DEFERRED)** + open-Q#4 SLO ownership + late-builder row K (SRE) + REV-200 carry-forward. correctness/freshness echo the 3-gate verification (Observation data-fidelity) + world-model-honesty | **SHARPENS_EXISTING** | promote 1W's carry-forward UP into this capture — **this is the home 1W/REV-200 pointed at** | spine: health ≠ HTTP-200 · C5: Runtime + carry Section 1W |
| PX9 | SLO/SLA/error-budget + the **HARD safety law** (budgets never authorize a patient-safety breach) | SLO mechanics: 1W/row-K/REV-200 (deferred). Hard-law: echoes C3.7 firewall-invariance + REV-184 safety-floor | SLO = **SHARPENS_EXISTING**; hard-law = **GENUINELY_NEW** (healthcare-native moat) | SLO: cite 1W/row-K. **Hard-law: spine-grade — a moat line** (§20 healthcare-physics) | spine: the "no wrong-medication budget" law |
| PX10 | environment / operator / Federation topology | **EXISTS_DEEPER:** Federation 6-tier (`deployment·legal_entity·brand·site·location·venue`, System Map FED; **C3.5 no-new-vocab closure binding**) + data_layers **1U tenancy** (`org_id`/RLS) + residency | **EXISTS_DEEPER** | **cite-and-defer; do NOT re-mint topology** (C3.5 closure). Only `environment`-class (dev/test/staging/prod) + environment-registry is Platform-genuine (§5 OPEN) | C5: Federation + 1U + environment registry |
| PX11 | AI runtime-router functional boundary (§19) | **EXISTS_DEEPER:** v3 §B Context Router + `capability_envelope` + AI-substrate frame `DEC-034` + model gateway | **SHARPENS_EXISTING** | cite §B; the Build/Release/Runtime split of routing policy is the sharpening; physical home = Task-D open | spine: §B · C5: AI/model-lineage |
| PX12 | care/accountability cross-loop seams + `response_projection` | Accountability capture §18/§21 + REV-184 + §7.7 (projections expose/capabilities act/domains commit/audit proves) | **NOVEL_COMPOSITION** | keep; reciprocal with Accountability §18/§21 (now seam-kind-consistent) | spine: seam typing · C5: seam contracts |

### Accountability Loop (targeted pass)
| # | capture concept | prior home(s) | verdict | canonical home / action | spine / C5 consequence |
|---|---|---|---|---|---|
| AX1 | coordinator ≠ ledger (process ≠ durable state) | CNS-orchestrates/domain-commits (candidate≠commit) + OFC own-state-delegate-truth pattern | **NOVEL_COMPOSITION** | cite CNS + OFC; new *application* of a known discipline, not a new law | spine: process≠truth · C5: coordinator/ledger homes (Task-D) |
| AX2 | admission (status ≠ disposition; 8 dispositions) | REV-184 resolution gate (admission ≈ a §8-gate specialization) + C3.5 P22 grievance/anon + admit_provisional protected-reporting | **SHARPENS_EXISTING** | cite REV-184 + C3.5 P22; admission = the report-specialization of the authority gate | spine: cite REV-184 |
| AX3 | domain projection ≠ accountability overlay ≠ composed view (**domain-status ≠ coordination-status**) | §7.7 projection≠truth/authority + §7.5.1 per-event ownership + source_authority_map | **NOVEL_COMPOSITION** | cite §7.7; the status-independence invariant is the keeper | spine: the invariant · C5: overlay contract |
| AX4 | `response_obligation` first-class + own lifecycle + **terminal-vs-exception law** | OFC `care_obligation` (obligation lifecycle) + REV-184 | **SHARPENS_EXISTING** (extends OFC to accountability obligations) | cite OFC; **reconcile: response_obligation = OFC extension vs sibling** (Task-D/C5) | C5: OFC vs new obligation home |
| AX5 | recipient-scoped `response_case` (recipient_scope enum) | ServiceNow customer-case = external comparator; C3.6/C3.7 party/cohort scoping; **no direct estate home** | **GENUINELY_NEW (pending dedup)** — explicitly a Task-D open (capture §23: response_case vs obligation-set-as-fundamental) | record no-prior-home; **Task-D decides shape** | Task-D hinge |
| AX6 | persisted correlation + derived `matter_graph` | `matter_graph` echoes `trace_lineage` + `relationship_graph_projection` (EVRUN-000004 §0.5); persisted merge/split-decision is new | **NOVEL_COMPOSITION** | cite trace_lineage/relationship_graph_projection; persisted correlation-decision = the new piece (not a revived coordination_case) | C5: correlation ledger |
| AX7 | responsibility graph + accepted-custody / no-orphan / delegation laws | Federation grants + RBAC + operator topology + on-call + jurisdiction | **NOVEL_COMPOSITION** | cite Federation/RBAC; custody/no-orphan/delegation/federation-handoff laws = keeper | spine: no-orphan law · C5: assignment contract |
| AX8 | out-of-band operation + reconciliation authority | **EXISTS_DEEPER:** C3.5 P16 `degraded_mode` + REV-184 §R3.9/§R3.11 (`world_model_trust`·`gate_holder_posture`·post-hoc reconciliation) + source_authority_map | **EXISTS_DEEPER** | **cite-and-defer; do NOT re-derive degraded-mode.** Accountability-intake out-of-band = the specialization | spine: cite REV-184 + C3.5 P16 |
| AX9 | personal remedy ≠ technical repair (commitment preserved to recipient while domains execute) | C3.8 G4 moat (patient remedy) + REV-184 + OFC; NHS-PSIRF comparator | **NOVEL_COMPOSITION** / **GENUINELY_NEW** | keep; the "we'll replace the med by Tuesday + reassess — and it cannot disappear" invariant is the keeper | spine: remedy≠repair · C5: remedy-commitment |

### Stop condition + HONEST SCOPE (per Knox — do NOT overstate this)
Every load-bearing concept in both captures now has a **prior-home search · verdict · canonical destination · action** (above). **"No prior home found" is recorded explicitly** (PX3, AX5). Result: **CONFLICT PX2 RESOLVED (Build-OS→PCE rename; `09`/`10`/`11` preserved) · 2 genuinely-new-un-homed (PX3, AX5) · ~10 novel-compositions to cite-and-defer + preserve · ~5 exists-deeper to cite (PX8/PX10/PX11, AX8, + provenance PX4) · the healthcare hard-laws (PX9) preserved as moat.**

**★ SCOPE (updated 2026-07-12 — the Platform source-carrier gap is now CLOSED, bounded):** the initial capture cross-walk cited video clusters at name-level only and had not read `10`/`11` or the EVRUN registries. **That gap has since been closed by a bounded Platform source-carrier reconciliation** — `09`/`10`/`11` + FWREG-008 (July Build-OS taxonomy) read directly, + all **four EVRUN concept registries** extracted (two explore agents), + `data_layers_reconciliation_v1` — landed as **Platform capture §SBI** (inheritance table + hard-law inherit list + 5-way "runtime" disambiguation + meta-vs-product reconciliation + naming verdict). Headline findings: (1) the corpus has **NO name** for the create/validate plane and **rejects `agentic_sdlc_redesign`** → **"Engineering & Validation" recommended** (PCE not accepted); (2) the corpus itself **blends** meta-Build-OS with the product plane (`agent_lifecycle_frame` merges all three) — OMNI's separation is a reconciliation the corpus had not made; (3) a hard-law inherit list (pass=evidence-never-commit · reliability=harness-not-model · robotics-guarantee-does-not-transfer-to-care · etc.). **Still owed:** the **full-ESTATE** cross-walk (all bodies' net-new, not just the two captures) remains a **during-authoring** activity (row above). Task D CONFIRMS/EXTENDS the §SBI pass; it no longer first-discovers it.

Two captures ready for Task D as **estate consumers** (naming-reconciled; E&V pending final word). Full-estate AR-XWALK (row above) remains a during-authoring activity — NOT run here.

## Stop / authority
`analysis_nonbinding` (`GRD-036`); a gap-intake register — binds nothing. **The spine/thesis/contract authors MUST load this and disposition each row (verify → cover-cite / carry-forward / route-to-C5 / mint-08) — no row is a proven gap until verified vs system-map + contracts.** **This disposition is the CHECKLIST run DURING chunked Draft-0 worktables + the post-draft pressure pass — NOT a gate to finish before writing (2026-07-06 build decision; handoff §3 THE BUILD PLAN).** Does NOT reopen C3.8; spine authoring STARTS NOW (the pre-spine HOLD is lifted).
