# v4 — C4 Readiness Bridge (lane-preserver; NOT the C4 plan)

Document type: `plan_or_roadmap` (a tiny lane-preserver / gate note; NOT the C4 authoring package, NOT a section map, NOT prose)
Authority: `analysis_nonbinding` — preserves the C4 lane; binds nothing (`GRD-036`).
Status: `active` 2026-06-13 (**Lane-6 L6-B status correction** from the non-enum `lane_preserved_pending_C3_5_G4`: the C3.5-G4 blocking dependency is FULFILLED and this bridge no longer controls admission; it is **retained as historical/runway navigation** — its source-base def, method framing, and REV-184 read-order guard remain current. **Forward pointer:** the canonical authoring-input set is now read-graph **#9a**; current C4/spine state lives there + the current checkpoint handoff). **Deliberately minimal** (Nick+Knox: don't build the lane while C3.5 is still v4-forming). Controlling plan: `~/.cursor/plans/wave-2_source_scaffolding_654989a0.plan.md` → C4.

## Why this exists
We considered writing the C4 planning package now (current grounded agent) to de-risk the agent switch. Decision: **NOT yet.** C3.5 is explicitly v4-forming and may reshape the spine; a "provisional" package would start behaving like a settled container before C3.5 finishes. So this note only **preserves the lane**.

## The rules (until C3.5 G4 returns)
1. **C4 is BLOCKED until C3.5 Gate 4 (the G handoff) returns.** No spine prose, no thesis prose, no final section map authored before then.
2. **After C3.5 G4:** the grounded **continuity agent** produces the real C4 authoring plan (the controlling plan's named future gates: `v4_spine_authoring_plan` · `v4_spine_shape_plan` · `v4_thesis_synthesis_plan` · `v4_thesis_shape_plan`), integrating the C3.5 G findings.
3. **Then a fresh v4 authoring agent boots** and must FIRST pass a **C4 Author Readiness Note** (below) — reviewed by Nick (+ Knox) — before writing any spine prose. (Mirrors the C3 comprehension gate; this is what makes the mid-stream agent switch safe.)

## C4 Author Readiness Note — what the fresh agent must prove (gate, defined now)
Before any C4 prose, the fresh v4 agent produces a short note demonstrating it understands:
- what **C2** closed (the accepted full-estate source base; `v4_C2_source_base_declaration.md`);
- what **C3** ratified (Option B: reuse v3 spine-first + bidirectional method + v4 augmentations; no pre-stage Foundry);
- what **C3.1** says must NOT evaporate (the ~22 lens families; the 4 covered-thin forced first-class; the open-review items);
- what **C4.0** says must NOT flatten (no section from a routing pointer alone; pull the richest full-body articulation; depth/mechanism/tension/ownership-law preserved);
- what **C3.5** found (the G handoff: hospital/EHR gravity disposition, market-posture × architecture-obligation, the honest readiness verdict, contract pressure, primitive gaps, C5 deferrals);
- what **C3.6** found (oncology/trials **clinical-substrate readiness**: architecturally poised with a *bounded* 10-item delta package — `source_authority_map`, `trial_protocol`, `knowledge_partition`, `chain_of_identity`, `administration_event`, IP `custody_chain`, `outcome_intelligence`/RWE, coverage-analysis, `trial_subject`, P35 ladder — but NOT build- or regulatory-ready as-built; necessary-not-sufficient; see `v4_C3_6G`);
- what **C3.7** found (oncology trial-**access**/accrual wedge: the load-bearing **economically-blind, posture-invariant recommendation/display firewall** across clinic/patient/site/sponsor/academic + OMNI's own economics; small net-new set [`trial_match_candidate`, `cohort_slot_state`, `honest_null_terminal`, `screening_yield_funnel` as a `REV-174` projection, bounded translational cluster]; care-substrate **route-outs**; honest-null as a product law; see `v4_C3_7G`);
- the **Governed Resolution Lifecycle** (`08` `D0THES-REV-184`, the flagship — **read its §0 canonical law FIRST; §1–§R3.12 are derivation only — do NOT resurrect Round-1's narrower clinical-decision framing or the "OMNI-mediated vs observed" binary**, CONVERGED/ready_for_signoff): the missing Decide layer at the thesis-§8 authority gate between Sense and Act, **actor-agnostic** (human/AI/protocol/team/system = `DEC-034` concrete). **7 named spine lines:** governed-resolution-lifecycle · non-action-as-commit · blast-radius-authority · disagreement-as-escalation · outcome-reads-original-context · **world-model honesty (`predicted_state = candidate, not truth`; OMNI participates in reality, does not source it)** · **`trust_horizon` (authorized-autonomy ≤ trusted-horizon — absorbs verifiability+freshness+reversibility)**. Two orthogonal axes: **`gate_holder_posture`** (who held the gate) × **`world_model_trust`** (what OMNI knows). Ownership: CNS=record/lifecycle (process-state-not-truth) · RBAC=authority/blast-radius/reversibility gate · CM=truth+commit · OFC=obligations-from-stance · freshness=read off `source_authority_map`. **REV-184 MUST be signed off + its spine lines carried before any v4 spine section touching decisions/orders/care-state/AI-runtime.** Single highest flatten-risk; do NOT collapse into a generic "decision" bullet.
- what **v4 IS** in its own words: a governed care/business **execution** substrate — NOT just the CNS, NOT just Epic/EMR-less-central, NOT just AI agents; Epic = benchmark not ceiling; overlay = GTM posture not architecture ceiling;
- what it is **NOT allowed to do yet** (no contracts, no repo restructure, no skipping the depth/coverage rules).
**Pass = Nick (+ Knox) review.** Fail = a "beautiful framework" with the right names but lost depth → back to the note.

## Required inputs (the runway, in load order)
`v4_C2_source_base_declaration.md` · `v4_C3_method_recovery_ratification_note.md` · `v4_C3_1_lens_coverage_audit.md` · `v4_C4_0_depth_preservation_protocol.md` · the C3.5 arc outputs (`v4_C3_5A..G` incl. `F2–F5`, convergence-check, topology-reconciliation, `G4.1`) · the C3.6 oncology/trials arc outputs (`v4_C3_6A..G`) · the controlling plan (HOME) current-state banner · plus the full estate C2 names.

**Per-artifact read-depth routing for the three pressure-test arcs lives in their OWN handoffs — `v4_C3_5G4` §11, `v4_C3_6G` §8.5, `v4_C3_7G` §7 — deliberately NOT duplicated here (no parallel meta-doc, no drift). This bridge points; it does not copy.**

**★ "SOURCE BASE" — canonical definition (do NOT leave ambiguous):** "the source base" = the **full 9-category estate in `v4_C2_source_base_declaration.md`** (CLOSED 2026-06-13, current, living per `GRD-036`). It **INCLUDES the VIDEO CORPUS** (EVSRC sources + §3 Review-001 Knox reads + §3 Review-003 Opus extractions + EVRUN-000001/000002 registries + tension register + anchor ledger). **C4 authoring MUST actively MINE the video corpus/registry (the workbench), not just cite the §2A summary** — proven load-bearing twice in REV-184.

**★ C4 FRAMING (the recurring correction):** C4 = author the v4 spine **from the FULL source base + the execution-substrate frame (ORIENTATION §2)** — NOT a synthesis of the recent pressure arcs. **C3.5/C3.6/C3.7 + REV-184 are required pressure INPUTS, not the frame, the section hierarchy, or the source base.** REV-184's 7 spine lines MUST be carried (§0 canonical for claims; the round sections are derivation/evidence for C4.0 Depth Packets — never resurrect Round-1 clinical-only framing); its field-set/state-machine is C5, not C4.

**Open-review queue is now POPULATED (not deferred):** the C3.5/C3.6/C3.7 decisions are canonical rows in `08_open_review_queue.md` — **`D0THES-REV-184` (flagship: Governed Resolution Lifecycle — CLOSED / signed-off; a confirmed v4 spine-grade law; carry its 7 spine lines into C4, field-set/state-machine → C5), `REV-185` (econ-blind firewall invariant), `REV-186` (C3.6 trial-execution homes), `REV-187` (C3.7 access deltas + GTM-posture decision), `REV-188` (C3.5 net-new owner decisions), `REV-189` (care-substrate route-outs — NOT a research lane), `REV-190` (translational/PDX 2040 leave-door-open).** The fresh v4 agent loads these from `08`; it does NOT re-derive them ad hoc.

## Out of scope (here)
No C4 authoring plan, no section map, no synthesis/thesis-shape plans, no prose. Those are produced AFTER C3.5 G4 by the continuity agent, then authored by the fresh agent past the readiness gate.
