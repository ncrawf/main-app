# v4 — C3.5E: Deep-Trace Matrix (execution/action-loop inner test)  (SHELL)

Document type: `plan_or_roadmap` (C3.5 arc artifact E) · Authority: `analysis_nonbinding` (`GRD-036`)
Status: `shell_pending_population` 2026-06-13 — **populated by the SEPARATE C3.5 pressure-test agent**.
Gate: **G3 (Trace + Gap)**. Purpose: run the execution/action loop against the ≥25 deep-trace + ≥10 red-team cases from D. This is the INNER test — it runs only AFTER the reality field (A–C) and library (D) pass.

> SHELL — fill below. The action loop is one inner mechanism, not the whole arc.

## Trace template (per selected HCASE)
`source event → evidence/artifact → observation → clinical assertion → candidate → resolver/policy → authority gate → action envelope → owning-domain commit → documentation/proof → obligation/follow-up → learning/eval` — and the 9 questions: can OMNI (1) determine what action is owed · (2) identify who owns it · (3) identify the authority gate · (4) propose it safely · (5) route it · (6) commit/write-back · (7) prove it · (8) track what remains owed · (9) learn/evaluate without silent policy mutation. Name the OMNI **domains/control-planes touched** and where the trace **breaks** under hospital physics.

## Deep traces (≥25)
_(populate — by HCASE id)_

## Red-team / breaker traces (≥10)
_(populate — the cases designed to break OMNI)_

## Cross-cutting break patterns
What repeatedly breaks across traces (feeds F gaps).
