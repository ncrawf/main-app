# v4 — C3.5D: High-Pressure Scenario Library (Coverage Manifest + `HCASE-NNN`)

Document type: `evidence` / scenario corpus (C3.5 arc artifact D; durable + re-runnable) · Authority: `analysis_nonbinding` (`GRD-036`)
Status: `populated_G2_pending_review` 2026-06-13 (C3.5 pressure-test agent; Gate 2 / Scenario-Library). Prior `shell_pending_population`.
Gate: **G2 — BLOCKED until the Coverage Manifest (Part 1) passes review.** Reuses the `FIELD-NNN` / LI-100-case format (A §2) so this is a durable, re-runnable simulation corpus.

> **Gate-1 disposition (Nick + Knox, 2026-06-13):** Gate 1 approved, **no addendum / no A–B–C patch**. Knox's "weird sub-area" list (blood bank/transfusion · dialysis · oncology/infusion · peds/NICU · sterile-processing/case-carts/implants · transport/bed-placement · downtime/disaster/degraded-mode · restraints/seclusion/psych-legal-holds · consent/surrogate/advance-directives · device integration · nutrition/diet/TPN · perioperative pre-op→OR→PACU→floor → discharge · clinical-trial/phase-I) is treated as **reviewer watch-items for THIS manifest**, not a sidecar. Per Nick's standing order ("do NOT miss any gaps; I should not be the one spotting them"), the taxonomy below is **discovered independently and made exhaustive** — Knox's items are a subset; every stratum is marked covered / thin / deferred so nothing is silent.

> **Operating principle:** coverage is **recomputable from per-case `setting:`/`workflow:` tags** (the FIELD/LI durability promise). The manifest below is the human-readable rollup; if it ever disagrees with the corpus tags, the tags win.

---

## Part 1 — Coverage Manifest (gates G2)

**Status legend:** `covered` = ≥1 case, in near-term relevance · `covered-pressure` = ≥1 case, tested as hospital-gravity pressure though build is deferred · `thin` = 1 case, high-risk stratum that may warrant more · `deferred` = explicitly out of near-term scope, listed with reason (NOT silently dropped) · all high-risk strata carry ≥1 case regardless of build horizon.

### Axis 1 — Facility / care-setting

| # | facility / care-setting stratum | status | # | representative HCASE ids | note |
|---|---|---|---|---|---|
| 1 | Outpatient specialty clinic | covered | 3 | 001, 015, 080 | OMNI near-term wedge |
| 2 | Medspa / aesthetic procedural | covered | 3 | 002, 003, 058 | wedge; Bloom-adjacent |
| 3 | Dermatology clinic | covered | 1 | 004 | wedge |
| 4 | Primary / urgent care | covered | 1 | 005 | wedge-adjacent |
| 5 | Telehealth / async care | covered | 2 | 006, 046 | wedge core |
| 6 | Retail / pharmacy clinic | thin | 1 | 007 | adjacent |
| 7 | ASC / outpatient surgery center | covered-pressure | 2 | 009, 010 | first inpatient-grade pressure |
| 8 | Community hospital | covered-pressure | 1 | 011 | acute baseline |
| 9 | Critical-access / rural hospital | covered-pressure | 1 | 012 | low-resource extreme |
| 10 | Specialty / academic / quaternary | covered-pressure | 1 | 013 | high-complexity extreme |
| 11 | Acute inpatient (med-surg) | covered-pressure | 3 | 014, 015, 016 | core inpatient |
| 12 | Emergency Department (ED) | covered-pressure | 3 | 017, 018, 019 | triage/boarding/throughput |
| 13 | ICU / CCU / step-down / telemetry | covered-pressure | 3 | 020, 021, 022 | high-acuity, high-frequency |
| 14 | Inpatient OR / perioperative | covered-pressure | 3 | 023, 024, 071 | pre-op→OR→PACU→floor (Knox watch) |
| 15 | PACU | covered-pressure | 1 | 026 | post-anesthesia handoff |
| 16 | Cath / interventional lab | covered-pressure | 1 | 027 | procedural docs/implants |
| 17 | OB / L&D / postpartum | covered-pressure | 2 | 028, 073 | fetal monitoring, dyad (Knox watch) |
| 18 | NICU | covered-pressure | 2 | 030, 074 | weight-based dosing, dyad identity (Knox watch — real) |
| 19 | Pediatrics (inpatient) | covered-pressure | 1 | 031 | weight-based, guardian authority (Knox watch) |
| 20 | Inpatient psych / behavioral | covered-pressure | 2 | 032, 078 | legal hold / seclusion-restraint (Knox watch) |
| 21 | Inpatient rehab (IRF) | covered-pressure | 1 | 034 | functional-measure regime |
| 22 | PT / OT / ST (outpatient therapy) | covered | 1 | 035 | plan-of-care |
| 23 | SNF | covered-pressure | 2 | 036, 037 | MDS, transfer reconciliation (Knox watch) |
| 24 | ECF / LTC | covered-pressure | 1 | 038 | long-stay |
| 25 | LTACH / post-acute | thin | 1 | 039 | post-acute extreme |
| 26 | Assisted living / residential | deferred | 0 | — | non-medical-record setting; out of near-term scope (re-open if OMNI enters senior-living ops) |
| 27 | Home health / home-based | covered | 1 | 040 | OASIS-adjacent; remote |
| 28 | Hospice / palliative | covered-pressure | 1 | 041 | goals-of-care, comfort orders |
| 29 | Dialysis center | covered-pressure | 2 | 042, 075 | recurring runs, standing orders (Knox watch) |
| 30 | Infusion / oncology center | covered-pressure | 2 | 043, 076 | chemo regimens (Knox watch) |
| 31 | Phase-I / clinical-trial unit | covered-pressure | 1 | 045 | protocol/eCRF/randomization (Knox watch) |
| 32 | Cross-setting transitions of care | covered-pressure | 4 | 047, 048, 049, 050 | admit/transfer/discharge/repatriation — highest-risk axis |
| 33 | Mortuary / death / organ-donation | deferred | 1 | 063 | one case for record-closure/death-disposition; deep organ-donation workflow deferred (reason: OPO-owned, far from wedge) — listed, not dropped |
| 34 | EMS / pre-hospital / ambulance handoff | thin | 1 | 101 | added at G2 review (Knox/Nick); largely represented already via ED-triage (017), transport-custody (062), ADT (014), cross-setting transitions (047/065) — 101 makes the field-authority + unidentified-patient edge explicit |

### Axis 2 — Service-line / department / workflow

| # | workflow stratum | status | # | representative HCASE ids | note |
|---|---|---|---|---|---|
| 1 | ADT / registration / bed-mgmt / patient-placement | covered-pressure | 3 | 014, 047, 062 | stateful encounter + movement |
| 2 | Scheduling / capacity | covered | 2 | 009, 035 | D3 wedge-strong |
| 3 | Nursing assessment / flowsheets | covered-pressure | 2 | 020, 016 | high-frequency charting |
| 4 | Physician / APP documentation | covered-pressure | 2 | 011, 014 | note-as-legal-record |
| 5 | CPOE / orders / order-sets | covered-pressure | 3 | 015, 066, 087 | order ≠ act |
| 6 | Order reconciliation | covered-pressure | 2 | 048, 037 | transitions |
| 7 | Labs | covered-pressure | 2 | 051, 067 | order→result→review |
| 8 | Pathology | covered-pressure | 1 | 052 | sign-out authority |
| 9 | Blood bank / transfusion medicine | covered-pressure | 3 | 053, 070, 089 | type-&-cross, double-verify (Knox watch + red-team) |
| 10 | Radiology / imaging (RIS/PACS) | covered-pressure | 2 | 054, 067 | critical-result comms |
| 11 | Pharmacy (inpatient/outpatient/sterile-IV-compounding) | covered-pressure | 2 | 055, 076 | verification + compounding |
| 12 | eMAR / closed-loop med-admin / BCMA | covered-pressure | 3 | 056, 068, 090 | five-rights (red-team) |
| 13 | ADC / controlled-substance / diversion | covered-pressure | 2 | 057, 091 | override + diversion (red-team) |
| 14 | Procedures (bedside/endoscopy) | covered-pressure | 1 | 023 | consent + procedure note |
| 15 | OR / anesthesia / perioperative flow | covered-pressure | 2 | 024, 071 | pre-op→OR→PACU (Knox watch) |
| 16 | Sterile processing / case-carts / implants / UDI | covered-pressure | 2 | 058, 072 | implant/UDI tracking (Knox watch) |
| 17 | Cath-lab documentation | covered-pressure | 1 | 027 | device/implant capture |
| 18 | OB / fetal monitoring | covered-pressure | 2 | 028, 073 | dual-patient (Knox watch) |
| 19 | NICU charting / growth | covered-pressure | 1 | 074 | weight-based (Knox watch) |
| 20 | Dialysis orders / runs | covered-pressure | 1 | 075 | recurring (Knox watch) |
| 21 | Oncology / chemo regimens / infusion | covered-pressure | 1 | 076 | regimen safety (Knox watch) |
| 22 | Nutrition / diet orders / TPN | covered-pressure | 2 | 059, 077 | diet-order + TPN compounding (Knox watch) |
| 23 | Respiratory / ventilator | covered-pressure | 1 | 021 | vent settings as orders |
| 24 | Wound / ostomy | thin | 1 | 060 | serial assessment/media |
| 25 | Rehab / therapy documentation | covered | 1 | 034 | functional measures |
| 26 | Psych safety / legal-hold / seclusion-restraint / observation | covered-pressure | 2 | 032, 078 | legal authority (Knox watch + red-team 092) |
| 27 | Discharge planning | covered-pressure | 2 | 048, 079 | summary + reconciliation |
| 28 | Case-mgmt / social-work / UM / level-of-care | covered-pressure | 1 | 049 | authority for LOC |
| 29 | Referrals / follow-up / transitions | covered-pressure | 2 | 047, 080 | cross-operator |
| 30 | Infection control / surveillance / NHSN | covered-pressure | 1 | 081 | mandated reporting |
| 31 | Quality / regulatory reporting / CQM / registries | covered-pressure | 1 | 082 | penalty-bearing |
| 32 | Compliance / legal-record / HIM / ROI / amendment | covered-pressure | 2 | 083, 093 | amend-not-overwrite (red-team) |
| 33 | Billing / coding / DRG / charge-capture / rev-cycle | covered-pressure | 1 | 084 | doc↔bill coupling |
| 34 | Prior-auth / eligibility / payer / denials | covered-pressure | 2 | 085, 094 | payment≠care (red-team) |
| 35 | Inventory / supplies / implants / devices | covered-pressure | 1 | 058 | UDI/par-level |
| 36 | Device integration (monitors/pumps/telemetry/wearables) | covered-pressure | 2 | 086, 095 | value-fidelity (red-team) |
| 37 | Interfaces / HL7 / FHIR / ADT / ORU | covered-pressure | 1 | 087 | the long tail |
| 38 | Portal / messaging / family-comms | covered | 1 | 046 | wedge-strong |
| 39 | Consent / surrogate / advance-directives / POLST | covered-pressure | 2 | 088, 096 | capacity/surrogate (red-team) |
| 40 | Staffing / credentialing / privileging / role-authority | covered-pressure | 1 | 025 | capability≠privilege |
| 41 | Security / privacy / audit / access / break-glass | covered-pressure | 2 | 083, 097 | tamper-evident (red-team) |
| 42 | Downtime / disaster / degraded-mode / business-continuity | covered-pressure | 2 | 069, 098 | recovery reconciliation (Knox watch + red-team) |
| 43 | Transport / patient-movement logistics | covered-pressure | 1 | 062 | hand-off custody (Knox watch) |
| 44 | Interpreter / language access | thin | 1 | 064 | consent comprehension |
| 45 | AI / agent-runtime safety (cross-cutting) | covered-pressure | 3 | 099, 100, 090 | the OMNI-specific threat surface (red-team) |

### Acceptance check (against plan §Coverage Contract)
- Every facility stratum ≥1 case **OR explicit deferred-with-reason** (#26 assisted-living deferred; #33 mortuary thin/deferred-organ-donation) — **met**.
- Every major workflow stratum ≥1 case — **met** (45/45 carry ≥1 id).
- High-risk strata multiple cases — **met** (transfusion 3, med-admin 3, ADC/diversion 2, perioperative 2, psych-legal 2, transitions 4, consent 2, AI-safety 3).
- **≥25 deep-trace** selections — **28** (HCASE-061…088), flagged `[deep-trace]`.
- **≥10 red-team/breaker** — **12** (HCASE-089…100), flagged `[red-team]`.
- Each deep-trace + red-team case names OMNI domains/control-planes touched (`omni:` field). Broad cases carry strata + pass-condition.
- Uncovered strata explicitly deferred (assisted-living; deep organ-donation) — **listed, not silent**.
- **Manifest PASSES the contract** → corpus (Part 2) authored below. (Reviewer: confirm no stratum you can name is absent — that is the Nick test.)

---

## Part 2 — Scenario corpus (`HCASE-NNN`)

### HCASE schema (per case; compact form of A §2)
`HCASE-NNN [tier] — title` · `setting:` (facility stratum) · `wf:` (workflow stratum[s]) · `actors:` (+authority) · `situation:` · `hard:` (where hospital physics strains/breaks) · `omni:` (domains/control-planes touched) · `pass:` (the conformance bar) · deps/risk inline. Tiers: `[broad]` · `[deep-trace]` (full 9-question trace in E) · `[red-team]` (designed to break OMNI). All `status: active` unless noted.

> Cases are written **brutal and specific**, not generic. The pass-condition is the bar OMNI must clear; deep-trace + red-team cases get the full execution-loop trace in artifact E (Gate 3).

### Broad library (HCASE-001..060)

**HCASE-001 [broad] — Outpatient endocrinology TRT titration.** `setting:` outpatient specialty · `wf:` CPOE/orders, labs, physician-doc · `actors:` MD (order authority), MA, lab. `situation:` provider raises testosterone dose off new total-T + hematocrit; re-orders labs + refill. `hard:` the gravity is lab → assertion → provider-adoption → dose-change authority (home self-inject, so admin is trivial). `omni:` Observation/Clinical-Memory/OFC/D5/CNS/RBAC/D6. `pass:` dose change is a provider-authored adoption gated by authority-floor + fresh labs; AI drafts, never commits.

**HCASE-002 [broad] — Medspa neuromodulator/filler visit.** `setting:` medspa/procedural · `wf:` consent, procedure note, clinical media, charge-capture · `actors:` injector (NP/PA scope), front-desk, MD-delegation. `situation:` Botox + filler; before/after photos; consent; product lot capture; commission. `hard:` consent + lot/UDI + before/after media are legal-record + product-traceability, not a selfie. `omni:` D7(media/consent)/D5/D6/BIZOPS/Settings/Federation. `pass:` consent artifact + lot capture bound to occurrence; media is provenance-preserved D7 artifact; commission is D6→BIZOPS payout.

**HCASE-003 [broad] — Medspa GLP-1 program injection visit.** `setting:` medspa/procedural · `wf:` eMAR-lite/med-admin, nursing, scheduling · `actors:` RN/MA (administers), MD/NP (order). `situation:` weekly tirzepatide dose given in-office; titration per protocol; weight/BP captured. `hard:` in-office administration IS an administration event (who gave, dose, lot, site) distinct from the order. `omni:` OFC/Observation/Clinical-Memory/D5/RBAC. `pass:` administration recorded as its own event with administering actor + lot + dose; not collapsed into "ordered."

**HCASE-004 [broad] — Dermatology biopsy to pathology.** `setting:` derm · `wf:` procedures, pathology, results-review · `actors:` derm (procedure+order), pathologist (sign-out), nurse. `situation:` shave biopsy → specimen → path report → result review → patient notification. `hard:` specimen→report→review→notify is an order/result loop with sign-out authority + critical-result comms. `omni:` OFC/D7/Observation/Clinical-Memory/Messaging/CNS. `pass:` result bound to the order; pathologist sign-out is the authority; abnormal triggers tracked notify obligation (not silent file).

**HCASE-005 [broad] — Primary/urgent care acute visit.** `setting:` primary/urgent care · `wf:` physician-doc, CPOE, e-prescribe · `actors:` MD/APP, MA. `situation:` acute visit → diagnosis → e-Rx + return precautions. `hard:` e-prescribe (NCPDP), diagnosis as provider assertion, documentation as legal record. `omni:` Clinical-Memory/OFC/D7/Messaging/RBAC. `pass:` Rx is an authorized order event; note authenticated; diagnosis is provider-adopted, not AI-asserted.

**HCASE-006 [broad] — Async telehealth refill.** `setting:` telehealth/async · `wf:` portal/messaging, intake, e-prescribe · `actors:` patient, async provider. `situation:` patient requests refill via message; structured re-assessment; provider approves/declines. `hard:` async wedge core — must be MORE honest than 6-second telehealth (review-meaningfulness). `omni:` Intake/Messaging/Clinical-Memory/OFC/CNS/RBAC. `pass:` clinical_required gate enforced; provider review is meaningful + authority-gated; AI never clears the gate.

**HCASE-007 [broad] — Retail/pharmacy-clinic vaccination.** `setting:` retail/pharmacy clinic · `wf:` med-admin, registries, consent · `actors:` pharmacist (admin authority), patient. `situation:` flu/COVID vaccine given; lot captured; immunization-registry report. `hard:` administration + mandated public-health registry report (ONC f(1)). `omni:` OFC/Observation/D7/Federation/CNS(reporting). `pass:` admin event + lot recorded; registry report is a tracked obligation; consent captured.

**HCASE-008 [broad] — Nephrology lab-monitoring clinic.** `setting:` outpatient specialty · `wf:` labs, CPOE, results-review · `actors:` nephrologist, lab. `situation:` CKD monitoring; recurring metabolic panels; trend vs personal setpoint; med adjust. `hard:` longitudinal trajectory (not single value); recurring standing orders; trend ≠ truth until adopted. `omni:` Observation/Clinical-Memory/OFC/CNS. `pass:` trajectory is Observation; standing re-order is an obligation; abnormal is a review candidate, not auto-action.

**HCASE-009 [broad] — ASC outpatient surgery, pre-op to discharge.** `setting:` ASC · `wf:` scheduling, perioperative, consent, anesthesia · `actors:` surgeon, anesthesia, OR nurse, PACU nurse. `situation:` cataract/colonoscopy: pre-op clearance → time-out → procedure → PACU → same-day discharge. `hard:` perioperative is a multi-stage stateful flow with H&P-before-anesthesia + consent + discharge criteria. `omni:` D3/D5/OFC/D7/RBAC/Federation/CNS. `pass:` each stage is a distinct gated state; H&P + consent are pre-performance gates; discharge has criteria-met gate.

**HCASE-010 [broad] — ASC anesthesia + PACU recovery.** `setting:` ASC · `wf:` anesthesia, PACU, med-admin · `actors:` anesthesiologist/CRNA, PACU RN. `situation:` sedation administered intra-op; PACU monitoring; discharge scoring. `hard:` anesthesia administration + PACU vitals are high-frequency event records under anesthesia authority. `omni:` OFC/Observation/Clinical-Memory/RBAC. `pass:` anesthesia admin + recovery vitals recorded as events; discharge gated on scoring; authority = anesthesia.

**HCASE-011 [broad] — Community hospital admission.** `setting:` community hospital · `wf:` ADT, physician-doc, CPOE · `actors:` hospitalist (attending-of-record), admitting, nursing. `situation:` ED→inpatient admit; H&P within 24h; admitting diagnosis; order set. `hard:` admission creates attending-of-record + new order authority + stateful encounter; H&P timing is regulated. `omni:` D5/D3/Federation/Identity/Clinical-Memory/RBAC/CNS. `pass:` admission = new encounter + attending authority; H&P is a timed legal-record obligation; orders authored under privilege.

**HCASE-012 [broad] — Critical-access/rural hospital, low resources.** `setting:` critical-access/rural · `wf:` CPOE, labs, transfer · `actors:` single on-call physician, limited nursing. `situation:` unstable patient; no specialist on site; stabilize→transfer to tertiary. `hard:` low-resource extreme; transfer = authority handoff + record portability under time pressure. `omni:` Federation/D5/OFC/Messaging/CNS. `pass:` transfer reconciles record + hands authority; no orphaned obligations; comparator-relative autonomy never lowers safety gates.

**HCASE-013 [broad] — Academic/quaternary multi-service patient.** `setting:` academic/quaternary · `wf:` CPOE/order-sets, consults, co-sign · `actors:` attending, fellow, resident, multiple consult services. `situation:` complex patient with 4 consulting services + resident orders needing co-sign. `hard:` multi-actor authority + co-sign chains + competing consult recommendations. `omni:` RBAC/CNS/Clinical-Memory/D5. `pass:` resident orders require attending co-sign; consult recs are candidates, not commits; ownership stays explicit per service.

**HCASE-014 [broad] — Acute inpatient med-surg, bed placement.** `setting:` acute inpatient · `wf:` ADT/bed-mgmt, nursing · `actors:` bed-management, charge nurse, hospitalist. `situation:` admit → bed assignment → unit transfer when bed opens; census/level-of-care. `hard:` encounter is movement-driven (bed/unit/level-of-care), not a single appointment. `omni:` D5/D3/Federation/CNS. `pass:` patient-movement is a first-class stateful track; transfer updates unit/attending without losing the encounter thread.

**HCASE-015 [broad] — Inpatient CPOE order set.** `setting:` acute inpatient · `wf:` CPOE/orders/order-sets · `actors:` physician (order), pharmacy (verify), nursing (execute). `situation:` admit order set fires 12 orders (labs, meds, diet, activity, VTE-proph). `hard:` an order set is 12 distinct authorized intents, each with its own fulfillment + verification, none yet "done." `omni:` OFC/RBAC/CNS/Settings. `pass:` each order is a fulfillment_order in `requested/authorized` state; none marked done until its own event; pharmacy verify precedes med admin.

**HCASE-016 [broad] — Nursing flowsheets + shift handoff.** `setting:` acute inpatient · `wf:` nursing assessment/flowsheets · `actors:` RN, oncoming RN, charge. `situation:` q4h assessments, I/O, pain scores; shift handoff transfers accountability. `hard:` high-frequency structured charting + handoff = accountability transfer within a unit (read-receipt ≠ work-done). `omni:` Observation/D5/CNS/Messaging. `pass:` handoff transfers responsible-owner explicitly; open obligations carry across; charting is timed/authenticated.

**HCASE-017 [broad] — ED triage + ESI.** `setting:` ED · `wf:` ADT, triage, nursing · `actors:` triage RN, ED physician. `situation:` walk-in → triage acuity (ESI) → bed/track assignment → provider pickup. `hard:` undifferentiated patient, rapid identity + acuity, often pre-registration. `omni:` Identity/D5/CNS/Observation/RBAC. `pass:` pre-account identity handled (contact-identity, no collapse); acuity drives routing; authority established at pickup.

**HCASE-018 [broad] — ED boarding + throughput.** `setting:` ED · `wf:` ADT/bed-mgmt, capacity · `actors:` charge, bed-mgmt, hospitalist. `situation:` admitted patient boards in ED awaiting inpatient bed; dual ownership (ED + admitting). `hard:` two services share a patient mid-transition; orders may come from both; authority ambiguity. `omni:` D5/Federation/CNS/RBAC. `pass:` boarding state modeled; ownership is explicit (not two competing owners); transfer completes cleanly.

**HCASE-019 [broad] — ED sepsis critical-result.** `setting:` ED · `wf:` labs, results-review, CDS · `actors:` ED physician, lab. `situation:` lactate critical → sepsis pathway → time-bound bundle orders. `hard:` critical-value comms + time-bound bundle + CDS alert (source-cited); alert fatigue risk. `omni:` Observation/OFC/CNS/Clinical-Memory/Messaging. `pass:` critical result triggers tracked closed-loop notify + bundle obligations; CDS is attributable; thresholds are policy not silent.

**HCASE-020 [broad] — ICU high-frequency charting + vent.** `setting:` ICU/CCU · `wf:` nursing flowsheets, respiratory/ventilator, device-integration · `actors:` ICU RN, RT, intensivist. `situation:` continuous monitor + vent settings + drips titrated to protocol; q1h flowsheet. `hard:` device-streamed values + vent settings-as-orders + titration authority; volume + fidelity. `omni:` Observation/OFC/Device-integration/RBAC/Clinical-Memory. `pass:` device values carry verification state (not auto-truth); vent settings are orders; titration within protocol authority.

**HCASE-021 [broad] — ICU vasopressor titration.** `setting:` ICU/CCU · `wf:` med-admin, device-integration, nursing · `actors:` ICU RN, intensivist. `situation:` norepinephrine titrated to MAP target via smart pump; protocol bounds. `hard:` continuous titration is a stream of administration events bounded by a protocol order + nurse authority. `omni:` OFC/Observation/RBAC/Clinical-Memory. `pass:` titration bounded by protocol order; each change is an attributable admin event; pump integration value carries provenance.

**HCASE-022 [broad] — Step-down/telemetry transfer.** `setting:` step-down/telemetry · `wf:` ADT, device-integration, nursing · `actors:` charge, RN, hospitalist. `situation:` ICU→telemetry step-down; level-of-care change; telemetry alarms. `hard:` level-of-care change alters monitoring + authority; alarm signal ≠ clinical truth. `omni:` D5/Observation/CNS/RBAC. `pass:` LOC transition modeled; alarms are signals routed to review, not auto-commits.

**HCASE-023 [broad] — Inpatient OR procedure with implant.** `setting:` inpatient OR/perioperative · `wf:` procedures, sterile-processing/implants/UDI, consent · `actors:` surgeon, OR nurse, scrub tech, vendor rep. `situation:` ortho implant case; surgical consent; instrument/sponge count; implant UDI captured. `hard:` consent + implant UDI + count are safety/legal/traceability records distinct from the op note. `omni:` D7(consent)/OFC/D5/Inventory/Federation(vendor)/RBAC. `pass:` consent is pre-performance gate; implant UDI bound to occurrence + inventory; count is a safety record.

**HCASE-024 [broad] — Perioperative flow pre-op to floor.** `setting:` inpatient OR/perioperative · `wf:` OR/anesthesia perioperative flow, ADT · `actors:` pre-op RN, anesthesia, surgeon, PACU RN, floor RN. `situation:` pre-op→time-out→OR→PACU→floor; each handoff transfers custody + orders. `hard:` 5 stateful stages, 4 authority handoffs, distinct order sets per stage (Knox watch). `omni:` D5/OFC/CNS/RBAC/Messaging. `pass:` each stage is a gated state; each handoff transfers responsible-owner + open obligations; no stage collapse.

**HCASE-025 [broad] — Credentialing/privileging gate.** `setting:` acute inpatient · `wf:` staffing/credentialing/privileging/role-authority · `actors:` medical-staff office, surgeon. `situation:` surgeon attempts a procedure outside granted privileges; lapsed credential. `hard:` capability (license) ≠ authority (privilege at this facility); privilege is the gate. `omni:` RBAC/Federation/BIZOPS. `pass:` action blocked when privilege absent/expired even with a valid license; competency/credential is a composed input to authority.

**HCASE-026 [broad] — PACU recovery to floor handoff.** `setting:` PACU · `wf:` PACU, med-admin, ADT · `actors:` PACU RN, floor RN, anesthesia. `situation:` post-anesthesia monitoring; Aldrete scoring; handoff to floor with active orders. `hard:` discharge-from-PACU gated on scoring; handoff carries active drips/orders + accountability. `omni:` D5/OFC/Observation/CNS. `pass:` PACU→floor gated on criteria; active orders + obligations transfer with explicit owner.

**HCASE-027 [broad] — Cath lab stent + device capture.** `setting:` cath/interventional lab · `wf:` cath-lab docs, implants/UDI, procedures · `actors:` interventional cardiologist, cath RN/tech. `situation:` PCI with drug-eluting stent; contrast; device UDI + lot; procedure log. `hard:` implant UDI + contrast dose + hemodynamic log are device/traceability records. `omni:` OFC/D7/D5/Inventory/Observation. `pass:` stent UDI bound to occurrence + registry; contrast as administered med; procedure log authenticated.

**HCASE-028 [broad] — OB labor & delivery, fetal monitoring.** `setting:` OB/L&D · `wf:` OB/fetal-monitoring, med-admin, nursing · `actors:` OB, L&D RN, anesthesia (epidural). `situation:` laboring patient on continuous fetal monitoring; pitocin titration; epidural; delivery creates a second patient. `hard:` TWO patients on one encounter (maternal + fetal→neonate); identity at birth; dual charting. `omni:` Identity/D5/Observation/OFC/RBAC. `pass:` maternal + neonatal identities are distinct at birth; fetal monitoring is its own observation stream; no dyad-identity collapse.

**HCASE-029 [broad] — Postpartum dyad care.** `setting:` OB postpartum · `wf:` nursing, med-admin, newborn screening · `actors:` postpartum RN, OB, pediatrician/newborn. `situation:` mother + newborn co-located; newborn meds (vit K, hep B), screening; mother's meds. `hard:` two patients, two charts, two med-admin streams, linked but separate identities + authorities. `omni:` Identity/OFC/Observation/Clinical-Memory. `pass:` mother and newborn are separate patients/charts; meds administered to the correct patient (BCMA dyad-safety); screening is a tracked obligation.

**HCASE-030 [broad] — NICU weight-based dosing.** `setting:` NICU · `wf:` NICU charting/growth, med-admin, pharmacy · `actors:` neonatologist, NICU RN, pharmacy. `situation:` preterm neonate; weight-based mg/kg dosing recalculated as weight changes; tiny-volume meds. `hard:` dose is a function of a changing weight; wrong-weight = wrong-dose; identity (baby-of, multiples). `omni:` OFC/Observation/Clinical-Memory/Pharmacy/RBAC. `pass:` weight-based dose recomputed from current weight; multiples disambiguated in identity; pharmacy verification + BCMA gate the tiny dose (Knox watch — real).

**HCASE-031 [broad] — Pediatric inpatient, guardian authority.** `setting:` pediatrics (inpatient) · `wf:` consent/surrogate, med-admin, weight-based · `actors:` pediatrician, peds RN, parent/guardian. `situation:` child admitted; consent + decisions by guardian; weight-based dosing; assent for older child. `hard:` decision authority is the guardian (not patient); weight-based dosing; custody/consent edge cases (divorced parents). `omni:` Identity/D7(consent)/RBAC/OFC/Observation. `pass:` surrogate/guardian authority modeled with legal basis; weight-based dose enforced; patient ≠ decision-maker handled.

**HCASE-032 [broad] — Inpatient psych involuntary hold.** `setting:` inpatient psych/behavioral · `wf:` psych legal-hold/observation, consent · `actors:` psychiatrist, psych RN, legal/court. `situation:` involuntary hold (e.g., 72h); capacity assessment; restricted rights; observation level. `hard:` legal hold changes consent/authority/rights; time-bound with mandatory re-eval; court interface. `omni:` RBAC/D7/D5/CNS/Federation(legal). `pass:` legal-hold is an authority state with expiry + mandatory re-eval; rights restrictions audited; never auto-extended (Knox watch).

**HCASE-033 [broad] — Seclusion/restraint order.** `setting:` inpatient psych/behavioral · `wf:` psych seclusion-restraint, med-admin · `actors:` physician, RN, monitor. `situation:` violent patient; restraint ordered with strict time limit + continuous monitoring + face-to-face re-eval. `hard:` restraint is the most tightly time-bound order class; auto-expiry + mandatory in-person renewal; continuous monitoring obligation. `omni:` OFC/RBAC/Observation/CNS. `pass:` restraint order has hard expiry + renewal-requires-face-to-face; monitoring is a tracked obligation; AI never renews (red-team 092).

**HCASE-034 [broad] — Inpatient rehab functional measures.** `setting:` inpatient rehab (IRF) · `wf:` rehab/therapy docs, regulatory reporting · `actors:` physiatrist, PT/OT/ST, rehab RN. `situation:` IRF stay; IRF-PAI assessment; functional goals; 3-hour therapy rule; quality reporting. `hard:` setting-specific assessment instrument (IRF-PAI) + therapy-intensity compliance + penalty-bearing reporting. `omni:` Observation/D5/OFC/Settings/CNS(reporting). `pass:` functional measures captured as structured assessment; reporting obligation tracked; therapy plan-of-care is authored.

**HCASE-035 [broad] — Outpatient PT plan-of-care.** `setting:` PT/OT/ST · `wf:` rehab docs, scheduling, referrals · `actors:` PT, referring physician. `situation:` referral → eval → POC → recurring visits → progress notes → physician re-cert. `hard:` therapy operates under referral/POC with periodic physician re-certification authority. `omni:` D3/D5/OFC/Clinical-Memory/RBAC. `pass:` POC authored under referral; recurring visits + re-cert as obligations; therapist scope respected.

**HCASE-036 [broad] — SNF MDS assessment cycle.** `setting:` SNF · `wf:` order-reconciliation, regulatory reporting · `actors:` SNF RN/MDS-coordinator, attending. `situation:` admission + scheduled MDS 3.0 assessments → iQIES submission; QRP thresholds. `hard:` MDS is a required, scheduled, penalty-bearing assessment instrument with completeness thresholds. `omni:` Observation/D5/OFC/CNS(reporting)/Settings. `pass:` MDS cadence is a tracked obligation; submission within deadline; completeness gate; non-submission flagged (penalty risk).

**HCASE-037 [broad] — SNF transfer med reconciliation.** `setting:` SNF · `wf:` order-reconciliation, transitions · `actors:` SNF RN, pharmacy, receiving provider. `situation:` hospital→SNF transfer; reconcile inbound med list vs prior; discrepancies; reconciled list to next provider. `hard:` med reconciliation across operators is a quality measure + safety gate (Knox watch). `omni:` OFC/Clinical-Memory/Federation/CNS/Messaging. `pass:` reconciliation is an explicit cross-operator obligation; discrepancies surfaced for provider; reconciled list transferred (tracked measure).

**HCASE-038 [broad] — ECF/LTC long-stay standing orders.** `setting:` ECF/LTC · `wf:` CPOE/standing-orders, med-admin · `actors:` LTC physician, LPN/RN. `situation:` long-stay resident; standing/recurring orders; periodic provider visit + order renewal. `hard:` standing orders persist + require periodic renewal authority; long-horizon obligations. `omni:` OFC/RBAC/CNS/Clinical-Memory. `pass:` standing orders are recurring obligations with renewal gates; lapse triggers review, not silent continuation.

**HCASE-039 [broad] — LTACH vent-dependent long stay.** `setting:` LTACH/post-acute · `wf:` respiratory/ventilator, nursing, device-integration · `actors:` pulmonologist, RT, RN. `situation:` chronically vent-dependent patient; weaning protocol; long-stay complex orders. `hard:` post-acute extreme; long-horizon device-bound care; weaning is protocol-gated. `omni:` OFC/Observation/Device-integration/Clinical-Memory. `pass:` vent settings as orders; weaning protocol authority; device values verified; long-horizon obligations tracked.

**HCASE-040 [broad] — Home health remote care.** `setting:` home health/home-based · `wf:` nursing, device-integration, transitions · `actors:` home-health RN, physician (POC cert), patient. `situation:` post-discharge home visits; OASIS assessment; remote vitals; physician POC certification. `hard:` care outside a facility; remote/patient-reported + device data; physician POC cert authority off-site. `omni:` Observation/OFC/Clinical-Memory/Messaging/Federation. `pass:` remote/device data carries provenance + verification state; POC cert is authored; visit obligations tracked.

**HCASE-041 [broad] — Hospice goals-of-care.** `setting:` hospice/palliative · `wf:` consent/advance-directives, med-admin, orders · `actors:` hospice MD, RN, patient/family, POLST. `situation:` comfort-focused orders; DNR/POLST; symptom meds (opioids) titrated; advance directive governs. `hard:` advance directive/POLST is an authority document that constrains orders; code status gates action. `omni:` D7(directive)/OFC/RBAC/Clinical-Memory. `pass:` POLST/code-status is an enforced authority constraint; comfort orders honored; directive overrides default escalation.

**HCASE-042 [broad] — Dialysis recurring runs.** `setting:` dialysis center · `wf:` dialysis orders/runs, med-admin · `actors:` nephrologist, dialysis RN/tech. `situation:` thrice-weekly HD; standing dialysis prescription; per-run parameters + heparin + ESA; monthly labs. `hard:` recurring high-frequency runs off a standing prescription; per-run admin events + monthly lab cadence (Knox watch). `omni:` OFC/Observation/Clinical-Memory/CNS. `pass:` standing prescription + per-run events distinct; monthly labs as obligation; parameter changes authored.

**HCASE-043 [broad] — Oncology chemo regimen.** `setting:` infusion/oncology center · `wf:` oncology/chemo regimens, pharmacy, med-admin · `actors:` oncologist, onc pharmacist, infusion RN. `situation:` multi-day cycle regimen; weight/BSA dosing; pre-meds; lab gates before each cycle; double-verification. `hard:` regimens are protocol-bound multi-step orders with BSA dosing, hard lab gates, and mandatory double-verify (Knox watch). `omni:` OFC/Observation/Clinical-Memory/Pharmacy/RBAC. `pass:` regimen is a structured protocol object; cycle gated on labs; BSA dose verified; chemo double-check enforced; no AI dose commit.

**HCASE-044 [broad] — Infusion biologic + reaction.** `setting:` infusion/oncology center · `wf:` med-admin, device-integration, nursing · `actors:` infusion RN, supervising MD. `situation:` monoclonal infusion; rate titration; infusion reaction → stop + rescue meds. `hard:` administration with intra-event adverse reaction → real-time order + rescue admin; reaction documented. `omni:` OFC/Observation/Clinical-Memory/CNS/Messaging. `pass:` infusion admin event + reaction captured; rescue orders authorized in real time; adverse reaction is a first-class record.

**HCASE-045 [broad] — Phase-I clinical trial visit.** `setting:` phase-I/clinical-trial unit · `wf:` protocol/eCRF, consent, labs, med-admin · `actors:` PI, study coordinator, research pharmacy. `situation:` protocol-driven dosing; randomization/blinding; eCRF capture; PK draws on strict schedule; informed consent versioned. `hard:` protocol authority + blinding + strict-schedule PK + research consent are a parallel governance regime (Knox watch). `omni:` OFC/Observation/D7(consent)/Clinical-Memory/Settings/RBAC. `pass:` protocol is the order authority; blinding respected; consent versioned; schedule deviations flagged; research ≠ clinical-truth firewall.

**HCASE-046 [broad] — Telehealth portal + family comms.** `setting:` telehealth/async · `wf:` portal/messaging/family-comms · `actors:` patient, proxy/family, provider. `situation:` patient messages provider; family member (with proxy access) participates; result release. `hard:` proxy access + minor/adult-proxy rules + result-release sensitivity; channel exposure limits. `omni:` Messaging/Identity/D7(consent)/RBAC. `pass:` proxy authority is consented + scoped; sensitive content gated by channel exposure; family access never assumed from a shared handle.

**HCASE-047 [broad] — Transition: outpatient to inpatient admit.** `setting:` cross-setting transition · `wf:` ADT, referrals, order-reconciliation · `actors:` ED/clinic MD, admitting hospitalist. `situation:` clinic patient deteriorates → direct admit; outpatient meds reconciled into inpatient orders; new attending. `hard:` authority CHANGES on transition (new attending-of-record + new order authority + new encounter); home meds ≠ inpatient orders. `omni:` Federation/D5/OFC/Clinical-Memory/RBAC/CNS. `pass:` admission establishes new authority + encounter; med reconciliation explicit; outpatient context carried by reference, not auto-authority.

**HCASE-048 [broad] — Transition: discharge + med reconciliation.** `setting:` cross-setting transition · `wf:` discharge planning, order-reconciliation · `actors:` hospitalist, discharge RN, pharmacy, PCP. `situation:` inpatient→home; discharge summary; reconciled med list (start/stop/change); follow-up + PCP handoff. `hard:` discharge summary is a regulated legal-record obligation (≤ timeline) + reconciliation + follow-up obligations. `omni:` OFC/D7/Clinical-Memory/Messaging/Federation/CNS. `pass:` discharge summary authored + authenticated; reconciled list explicit (delta vs inpatient); follow-up is a tracked obligation; PCP handoff transmitted.

**HCASE-049 [broad] — Transition: UM observation-vs-inpatient.** `setting:` cross-setting transition · `wf:` case-mgmt/UM/level-of-care, billing · `actors:` case manager, UM physician advisor, payer. `situation:` status decision (observation vs inpatient) drives reimbursement + patient cost; payer concurrent review. `hard:` level-of-care is an authority + billing decision (two-midnight, payer rules); documentation drives status. `omni:` D5/D6/BIZOPS/RBAC/Federation. `pass:` LOC/status is an explicit authored decision (not derived from a UI); billing follows documented status; payer review interfaced.

**HCASE-050 [broad] — Transition: cross-operator repatriation.** `setting:` cross-setting transition · `wf:` referrals/follow-up, transitions, federation · `actors:` sending hospital, receiving SNF, home health, patient. `situation:` hospital→SNF→home across 3 distinct operators; record + reconciled meds + follow-up move with consent. `hard:` authority is NOT portable across operators — re-established at each; data crosses only by scoped grant + consent (Knox watch). `omni:` Federation/Identity/OFC/Clinical-Memory/D7/CNS. `pass:` each operator re-establishes authority; cross-operator visibility via scoped grant + consent (no global chart); obligations hand off without orphaning.

**HCASE-051 [broad] — Lab order-to-review loop.** `setting:` acute inpatient · `wf:` labs, results-review · `actors:` physician (order), lab tech (result), physician (review). `situation:` CBC ordered → collected → resulted → abnormal → provider review/ack. `hard:` result is bound to the order; abnormal requires documented review; collection is its own event. `omni:` OFC/Observation/Clinical-Memory/CNS. `pass:` result `basedOn` order; abnormal generates a review obligation; review/ack is authored, not auto-closed.

**HCASE-052 [broad] — Pathology sign-out + addendum.** `setting:` acute inpatient · `wf:` pathology, legal-record/amendment · `actors:` pathologist, ordering MD. `situation:` surgical specimen → prelim → final sign-out → later addendum (e.g., revised margin/stain). `hard:` pathologist sign-out is the authority; an addendum is amend-not-overwrite (original preserved). `omni:` OFC/D7/Observation/Clinical-Memory. `pass:` sign-out authority recorded; addendum appends (original retained, lineage preserved); downstream consumers see the revision.

**HCASE-053 [broad] — Blood bank type-&-cross + transfusion.** `setting:` acute inpatient · `wf:` blood-bank/transfusion, med-admin · `actors:` physician (order), blood-bank tech (crossmatch), 2 RNs (bedside verify). `situation:` type & screen → crossmatch → product issued → 2-nurse bedside ID + product verify → transfuse → reaction monitoring. `hard:` transfusion has a unique double-verification + product-to-patient match + reaction watch (Knox watch). `omni:` OFC/Observation/Clinical-Memory/RBAC/CNS. `pass:` product↔patient match + independent double-verify enforced; transfusion is an administration event; reaction monitoring is a tracked obligation.

**HCASE-054 [broad] — Radiology order + critical result.** `setting:` acute inpatient · `wf:` radiology/imaging, results-review · `actors:` ordering MD, radiologist. `situation:` CT ordered → performed → radiologist read (structured + narrative impression) → critical finding → closed-loop comms. `hard:` report has structured + narrative parts; critical finding requires documented closed-loop notification to ordering provider. `omni:` OFC/D7(report)/Observation/Clinical-Memory/Messaging/CNS. `pass:` report bound to order; narrative impression preserved (D7); critical-result closed-loop notify tracked to acknowledgement.

**HCASE-055 [broad] — Pharmacy verification + sterile IV compounding.** `setting:` acute inpatient · `wf:` pharmacy (sterile/IV compounding), med-admin · `actors:` pharmacist (verify), pharmacy tech (compound). `situation:` IV antibiotic ordered → pharmacist verify (renal dose, interaction) → sterile compound → dispense → administer. `hard:` pharmacist verification is a distinct authority + compounding is a controlled prep step before any admin. `omni:` OFC/Clinical-Memory/RBAC/Observation. `pass:` verify precedes dispense precedes administer; each a distinct event/actor; interaction/renal check is attributable CDS.

**HCASE-056 [broad] — Closed-loop med pass (BCMA).** `setting:` acute inpatient · `wf:` eMAR/closed-loop med-admin/BCMA · `actors:` RN. `situation:` scheduled med pass; scan wristband + med barcode; five-rights validated; eMAR auto-documents; one med held (patient NPO). `hard:` administration is the act (five-rights) distinct from the order; hold/refuse is a documented non-admin event. `omni:` OFC/Observation/Clinical-Memory/RBAC. `pass:` BCMA five-rights gate enforced; admin/hold/refuse each recorded with reason; "ordered" never auto-becomes "given."

**HCASE-057 [broad] — ADC controlled-substance pull + waste.** `setting:` acute inpatient · `wf:` ADC/controlled-substance/diversion, med-admin · `actors:` RN, witness RN, pharmacy. `situation:` opioid pulled from ADC; partial dose given; remainder wasted with witness; counts reconciled. `hard:` controlled-substance chain-of-custody: pull→administer→waste→witness→count, with diversion surveillance. `omni:` OFC/RBAC/Observation/BIZOPS(audit). `pass:` full custody chain recorded (pull/give/waste/witness); discrepancies flagged for diversion review; override is logged + reconciled.

**HCASE-058 [broad] — Sterile processing + implant/UDI tracking.** `setting:` inpatient OR/perioperative · `wf:` sterile-processing/case-carts/implants/UDI, inventory · `actors:` SPD tech, OR nurse, materials. `situation:` instrument tray sterilized + tracked; case cart built; implant consumed → UDI scanned → inventory decrement + charge. `hard:` instrument/implant traceability (UDI), sterilization records, and consumption→charge are operational+safety+billing records (Knox watch). `omni:` Inventory/OFC/D6/D5/Federation(vendor). `pass:` implant UDI bound to occurrence + decrements inventory + drives charge; sterilization record retained; recall traceability supported.

**HCASE-059 [broad] — Inpatient diet order + TPN.** `setting:` acute inpatient · `wf:` nutrition/diet/TPN, pharmacy · `actors:` physician/dietitian (diet order), pharmacy (TPN compound), nursing. `situation:` NPO→advance-diet orders; TPN ordered (electrolytes/lipids) → pharmacy compound → administer; aspiration precautions. `hard:` diet is an order class with safety constraints; TPN is a compounded high-alert prep with its own verify (Knox watch). `omni:` OFC/Clinical-Memory/Pharmacy/Observation. `pass:` diet order is authored + safety-gated; TPN follows order→verify→compound→administer; dietitian scope respected.

**HCASE-060 [broad] — Wound/ostomy serial assessment.** `setting:` acute inpatient · `wf:` wound/ostomy, clinical media, nursing · `actors:` wound-care RN, physician. `situation:` pressure injury staged; serial photos; measurements over time; treatment orders. `hard:` serial clinical media + measurements over time = longitudinal observation + provenance-preserved media (not a one-off photo). `omni:` D7(media)/Observation/Clinical-Memory/OFC. `pass:` wound media is provenance-preserved D7 artifact; measurements are a trajectory; staging is a provider assertion; treatment is an order.

### Deep-trace selections — HCASE-061..088 (28) `[deep-trace]`
Selected for the full 9-question execution-loop trace in artifact E (Gate 3). Each names OMNI domains/control-planes touched + where the trace is expected to strain under hospital physics.

**HCASE-061 [deep-trace] — Order to administration to result, full chain.** `setting:` acute inpatient · `wf:` CPOE, pharmacy, eMAR/BCMA, labs · `actors:` MD(order), pharmacist(verify), RN(administer), lab(result). `situation:` IV med ordered → verified → administered (BCMA) → response lab drawn → reviewed. `hard:` the canonical hospital chain — 4 actors, 4 authorities, 4 events; the place OMNI's OFC/Clinical-Memory/Observation separation is most tested. `omni:` OFC/Clinical-Memory/Observation/RBAC/CNS/D7. `pass:` order/verify/administer/result are distinct events with distinct authoring actors; no collapse; each gated; full trace_lineage reconstructable.

**HCASE-062 [deep-trace] — Transport / patient-movement custody.** `setting:` acute inpatient · `wf:` transport/patient-movement logistics, ADT · `actors:` transporter, sending RN, receiving unit. `situation:` patient to radiology + back; custody + monitoring handoff; orders continue during transport. `hard:` custody/accountability during movement; who owns the patient mid-transport; active orders don't pause (Knox watch). `omni:` D5/OFC/CNS/RBAC. `pass:` transport is a custody state with explicit sending/receiving owner; obligations persist; no accountability gap.

**HCASE-063 [deep-trace] — Death + record closure.** `setting:` mortuary/death · `wf:` legal-record, orders, consent · `actors:` physician (pronouncement), RN, family, (OPO referral). `situation:` patient expires; pronouncement; record closure; OPO/organ-donation referral; death disposition. `hard:` pronouncement authority + record finalization + mandated OPO referral; deep organ-donation workflow deferred. `omni:` D5/D7/Clinical-Memory/Federation(OPO)/CNS. `pass:` pronouncement is an authored event; record closes (amend-not-overwrite after); OPO referral is a tracked obligation. (Deferred-deep: full donation workflow = out-of-scope, listed not dropped.)

**HCASE-064 [deep-trace] — Interpreter / language access at consent.** `setting:` acute inpatient · `wf:` interpreter/language-access, consent · `actors:` LEP patient, interpreter, physician. `situation:` non-English-speaking patient; interpreter-mediated informed consent for a procedure; comprehension documented. `hard:` consent validity depends on documented comprehension via qualified interpreter; comprehension ≠ signature. `omni:` D7(consent)/Identity/RBAC/Messaging. `pass:` interpreter involvement + comprehension recorded as part of consent validity; consent is meaningful, not a checkbox.

**HCASE-065 [deep-trace] — ADT transfer authority change.** `setting:` cross-setting transition · `wf:` ADT/bed-mgmt, order-reconciliation · `actors:` floor team, ICU team. `situation:` floor→ICU transfer; attending + order authority change; orders re-evaluated at new LOC. `hard:` authority + order-set change at LOC transition; stale floor orders must not silently persist. `omni:` D5/OFC/RBAC/CNS/Federation. `pass:` transfer changes attending/authority; orders re-evaluated (not silently carried); encounter thread continuous; obligations reassigned.

**HCASE-066 [deep-trace] — CPOE order-set fan-out + co-sign.** `setting:` academic/quaternary · `wf:` CPOE/order-sets, role-authority · `actors:` resident (enters), attending (co-sign), pharmacy. `situation:` resident applies admission order-set (15 orders); attending co-sign required; some orders fire pre-cosign per policy. `hard:` order-set fan-out into N authorized intents + co-sign authority + which fire immediately vs hold-for-cosign. `omni:` OFC/RBAC/CNS/Settings. `pass:` each order is a distinct fulfillment_order; co-sign authority enforced per policy; nothing high-risk fires without required authority.

**HCASE-067 [deep-trace] — Critical lab/imaging closed-loop.** `setting:` acute inpatient · `wf:` labs, radiology, results-review · `actors:` lab/radiologist, ordering MD. `situation:` critical potassium + critical CT finding; both require time-bound closed-loop notification + acknowledgement. `hard:` critical results require documented closed-loop comms to acknowledgement, not passive posting; alert fatigue. `omni:` Observation/OFC/Messaging/CNS/Clinical-Memory. `pass:` critical result opens a tracked notify-to-ack obligation; not closed until acknowledged; threshold is policy.

**HCASE-068 [deep-trace] — Closed-loop med-admin with override.** `setting:` acute inpatient · `wf:` eMAR/BCMA, ADC · `actors:` RN, pharmacy. `situation:` BCMA scan mismatch (wrong-formulation barcode) + an ADC override for an urgent med; both must be handled safely. `hard:` the five-rights gate must block the mismatch; override must be bounded + reconciled, not normalized. `omni:` OFC/Observation/RBAC/BIZOPS(audit). `pass:` BCMA mismatch blocks administration; override is logged, bounded, reconciled to order; no silent bypass (pairs red-team 090).

**HCASE-069 [deep-trace] — Downtime / degraded-mode + recovery.** `setting:` acute inpatient · `wf:` downtime/disaster/degraded-mode, order-reconciliation · `actors:` all clinical staff, IT. `situation:` EHR downtime; paper orders/admin during outage; on recovery, back-entry + reconciliation. `hard:` care continues during outage; recovery must reconcile out-of-band actions without losing authority/audit (Knox watch). `omni:` Build-OS/OFC/Clinical-Memory/RBAC/CNS. `pass:` degraded-mode is a designed state; out-of-band actions reconciled on recovery with authority + timestamp integrity; no unverified writes silently accepted (pairs red-team 098).

**HCASE-070 [deep-trace] — Transfusion double-verify + reaction.** `setting:` acute inpatient · `wf:` blood-bank/transfusion, med-admin · `actors:` MD, blood-bank tech, 2 RNs. `situation:` crossmatched unit → 2-nurse independent product↔patient verify → transfuse → febrile reaction → stop + workup. `hard:` independent double-verify + product-to-patient binding + intra-event reaction → real-time orders. `omni:` OFC/Observation/Clinical-Memory/RBAC/CNS. `pass:` product↔patient match + independent dual-verify gate enforced (no single actor); transfusion is its own event; reaction triggers tracked workup obligation.

**HCASE-071 [deep-trace] — Perioperative pre-op→OR→PACU→floor.** `setting:` inpatient OR/perioperative · `wf:` OR/anesthesia perioperative flow · `actors:` pre-op RN, anesthesia, surgeon, scrub/circulator, PACU RN, floor RN. `situation:` full periop chain incl. surgical time-out, count, implant, anesthesia record, PACU scoring, floor handoff. `hard:` the densest multi-stage authority-handoff chain in the hospital (Knox watch); each stage distinct orders + custody. `omni:` D5/OFC/D7/RBAC/CNS/Inventory. `pass:` every stage is a gated state; time-out + count + consent are safety records; each handoff transfers owner + open obligations; nothing collapses to "the surgery."

**HCASE-072 [deep-trace] — Implant UDI + recall traceability.** `setting:` cath/OR · `wf:` implants/UDI, inventory, interfaces · `actors:` proceduralist, materials, regulatory. `situation:` implant placed + UDI captured; months later a manufacturer recall → identify affected patients. `hard:` UDI must bind patient↔device↔occurrence durably enough to answer a recall years later (Knox watch). `omni:` OFC/D5/Inventory/Clinical-Memory/Federation. `pass:` UDI is a durable patient-device-occurrence binding; recall query resolves affected patients; traceability survives time.

**HCASE-073 [deep-trace] — OB fetal monitoring, two patients.** `setting:` OB/L&D · `wf:` OB/fetal-monitoring, med-admin · `actors:` OB, L&D RN, anesthesia, neonatal team. `situation:` continuous fetal + maternal monitoring; pitocin titration; emergent C-section; neonate born → new patient. `hard:` two linked patients on one encounter; identity creation at birth; dual orders/admin; emergent authority escalation. `omni:` Identity/D5/Observation/OFC/RBAC/CNS. `pass:` maternal + neonatal identities distinct; fetal stream is its own observation; emergent orders authorized; meds hit the right patient.

**HCASE-074 [deep-trace] — NICU weight-based dosing recompute.** `setting:` NICU · `wf:` NICU charting/growth, pharmacy, med-admin · `actors:` neonatologist, NICU RN, pharmacy. `situation:` daily weight changes → mg/kg doses recomputed; micro-volumes; twins disambiguated. `hard:` dose is a live function of current weight; wrong-weight/wrong-twin = catastrophic; tiny-volume verification (Knox watch). `omni:` OFC/Observation/Clinical-Memory/Pharmacy/Identity/RBAC. `pass:` dose recomputed from current weight at order time; multiples disambiguated; pharmacy verify + BCMA gate; no stale-weight dosing.

**HCASE-075 [deep-trace] — Dialysis recurring standing prescription.** `setting:` dialysis center · `wf:` dialysis orders/runs, med-admin, labs · `actors:` nephrologist, dialysis RN. `situation:` standing HD prescription drives thrice-weekly runs; per-run parameters + ESA/heparin; monthly lab-driven adjustments. `hard:` a standing order generates recurring run-events + per-run admin + monthly lab cadence; long-horizon obligations (Knox watch). `omni:` OFC/Observation/Clinical-Memory/CNS. `pass:` standing prescription vs per-run events distinct; monthly labs as recurring obligation; parameter changes authored under nephrology authority.

**HCASE-076 [deep-trace] — Chemo regimen lab-gated + double-verify.** `setting:` infusion/oncology · `wf:` oncology/chemo regimens, pharmacy, med-admin · `actors:` oncologist, onc pharmacist, infusion RN. `situation:` multi-day BSA-dosed regimen; ANC/labs gate each cycle; pre-meds; independent dose double-check; dose-reduction logic. `hard:` regimen = protocol-bound multi-step order with hard lab gates + BSA dosing + mandatory double-verify; high-alert (Knox watch). `omni:` OFC/Observation/Clinical-Memory/Pharmacy/RBAC/CNS. `pass:` regimen is a structured protocol object; cycle blocked unless lab gate met; BSA dose double-verified; dose changes authored; AI never commits a dose.

**HCASE-077 [deep-trace] — TPN high-alert compounding.** `setting:` acute inpatient · `wf:` nutrition/TPN, pharmacy · `actors:` physician/dietitian, pharmacy (compound), nursing. `situation:` TPN ordered (dextrose/AA/lipids/electrolytes) → pharmacy verify + compound (auto-compounder) → administer; stability/incompatibility checks. `hard:` TPN is a high-alert compounded prep with incompatibility limits + its own verification before any admin (Knox watch). `omni:` OFC/Pharmacy/Clinical-Memory/Observation/RBAC. `pass:` order→verify→compound→administer chain enforced; incompatibility CDS attributable; compounding is a controlled event distinct from order.

**HCASE-078 [deep-trace] — Psych restraint time-bound order.** `setting:` inpatient psych/behavioral · `wf:` psych seclusion-restraint, observation · `actors:` physician, RN, continuous monitor. `situation:` restraint ordered with hard time limit + continuous monitoring + mandatory in-person re-eval before any renewal. `hard:` the most tightly time-bound + legally-gated order; auto-expiry; renewal requires face-to-face (Knox watch). `omni:` OFC/RBAC/Observation/CNS. `pass:` restraint order auto-expires; renewal blocked without recorded in-person re-eval; monitoring is a tracked obligation; AI/auto-renewal forbidden (pairs red-team 092).

**HCASE-079 [deep-trace] — Discharge summary + reconciliation + follow-up.** `setting:` cross-setting transition · `wf:` discharge planning, order-reconciliation, legal-record · `actors:` hospitalist, discharge RN, pharmacy, PCP/SNF. `situation:` inpatient→post-acute; discharge summary (regulated content + timeline); med reconciliation delta; follow-up + receiving-provider transmit. `hard:` discharge summary is a regulated legal-record obligation + reconciliation + handoff + follow-up obligations, across an operator boundary. `omni:` OFC/D7/Clinical-Memory/Messaging/Federation/CNS. `pass:` summary authored/authenticated within timeline; reconciled med delta explicit; follow-up + transmit are tracked obligations; nothing orphaned.

**HCASE-080 [deep-trace] — Cross-operator referral with scoped grant.** `setting:` cross-setting transition · `wf:` referrals/follow-up, federation · `actors:` referring clinic, receiving specialist, patient. `situation:` clinic refers to an outside specialist on a different operator; relevant record shared by scoped consent; results flow back. `hard:` data crosses an operator boundary only by scoped grant + consent; no global chart; results return without authority leakage. `omni:` Federation/Identity/D7/Clinical-Memory/Messaging/CNS. `pass:` cross-operator share is consent-scoped + audited; specialist sees only granted scope; return results bind to the patient without auto-authority.

**HCASE-081 [deep-trace] — Infection-control surveillance + NHSN reporting.** `setting:` acute inpatient · `wf:` infection-control/NHSN, quality reporting · `actors:` infection preventionist, lab, CDC/NHSN. `situation:` positive culture → HAI surveillance → CLABSI/CAUTI determination → mandated NHSN report. `hard:` mandated public-health reporting computed off clinical data with strict definitions + penalties. `omni:` Observation/Clinical-Memory/CNS(reporting)/Federation/D7. `pass:` surveillance derives from clinical truth (projection, not new truth); NHSN report is a tracked, deadline-bound obligation; definitions are policy-versioned.

**HCASE-082 [deep-trace] — Quality / CQM / registry reporting.** `setting:` acute inpatient · `wf:` quality/CQM/registries · `actors:` quality team, CMS programs. `situation:` CQMs + registry submissions (e.g., sepsis bundle, stroke) computed from the record; PI program. `hard:` penalty-bearing reporting computed off documentation; measure logic is versioned; gaps cost money. `omni:` Observation/Clinical-Memory/D5/CNS/Settings. `pass:` measures are projections over committed truth (never a parallel store); submission obligations tracked; measure-version pinned.

**HCASE-083 [deep-trace] — HIM amendment + break-glass audit.** `setting:` acute inpatient · `wf:` legal-record/HIM/ROI/amendment, security/break-glass · `actors:` HIM, provider (amends), compliance, ROI requester. `situation:` provider amends a signed note (addendum); a VIP record accessed under break-glass; ROI request fulfilled. `hard:` amend-not-overwrite (original preserved); break-glass is logged + post-reviewed; ROI release-control. `omni:` D7/RBAC/Clinical-Memory/BIZOPS(audit)/Federation. `pass:` amendment appends (original + lineage retained); break-glass access is reason-coded + audited + reviewed; ROI honors authorized-release only (pairs red-team 093).

**HCASE-084 [deep-trace] — Coding/DRG from documentation.** `setting:` acute inpatient · `wf:` billing/coding/DRG/charge-capture · `actors:` coder, CDI specialist, provider (query). `situation:` coder assigns ICD/DRG from the note; CDI query to provider for specificity; charge capture. `hard:` documentation drives reimbursement; the bill must not alter the clinical record; coder cannot author clinical content. `omni:` D6/BIZOPS/Clinical-Memory/D7/RBAC. `pass:` coding reads documented truth (one-way); CDI query is a provider-authored clarification (not a coder edit to the chart); charge follows documentation.

**HCASE-085 [deep-trace] — Prior-auth / payer determination.** `setting:` outpatient/inpatient · `wf:` prior-auth/eligibility/payer · `actors:` provider, prior-auth coordinator, payer UM. `situation:` advanced imaging / specialty drug needs prior-auth; payer approves/denies; appeal. `hard:` payer determination gates access but is NOT a clinical authority; denial ≠ clinical cancellation. `omni:` D6/BIZOPS/OFC/Federation/CNS. `pass:` prior-auth is a commerce/eligibility gate distinct from clinical order authority; a denial never silently cancels the clinical order (pairs red-team 094); appeal tracked.

**HCASE-086 [deep-trace] — Device-integration value fidelity.** `setting:` ICU · `wf:` device-integration, nursing · `actors:` monitor/pump, ICU RN. `situation:` continuous monitor + smart-pump stream into the record; artifact/noise spikes; nurse validates before charting. `hard:` device-streamed values are candidates with fidelity risk, not auto-truth; volume + artifact. `omni:` Observation/OFC/Clinical-Memory/RBAC. `pass:` device values carry a verification/fidelity state; not auto-committed as clinical truth; nurse validation/attestation gates charting (pairs red-team 095).

**HCASE-087 [deep-trace] — HL7/FHIR inbound interface ingestion.** `setting:` acute inpatient · `wf:` interfaces/HL7/FHIR/ADT/ORU · `actors:` interface engine, source systems (LIS/RIS/pharmacy), OMNI. `situation:` inbound HL7 v2 ORU (lab result) + ADT feed from systems OMNI does not own; mapping + reconciliation. `hard:` OMNI lives inside a dense interface ecosystem; inbound feeds must map to canonical truth with provenance, not become authority blindly. `omni:` Federation/Observation/Identity/Clinical-Memory/OFC/CNS. `pass:` inbound interface data is provenance-tagged + reconciled to canonical identity/order; never auto-authority; mapping errors surfaced, not silently committed.

**HCASE-088 [deep-trace] — Consent / surrogate / advance-directive authority.** `setting:` acute inpatient · `wf:` consent/surrogate/advance-directives/POLST · `actors:` patient (capacity?), surrogate/POA, physician. `situation:` patient loses capacity → surrogate-hierarchy decision-maker; POLST/DNR governs; consent for procedure by surrogate. `hard:` decision authority shifts to a legally-ranked surrogate; directive constrains orders; capacity is a clinical+legal determination. `omni:` D7(consent/directive)/Identity/RBAC/Clinical-Memory. `pass:` surrogate authority is legally-grounded + scoped; directive/code-status enforced as an authority constraint; capacity status drives who may consent (pairs red-team 096).

### Red-team / breaker cases — HCASE-089..100 (12) `[red-team]`
Designed to BREAK OMNI. Each names the failure mode + the pass-condition OMNI must clear (i.e., the bad behavior OMNI must refuse). These are the brutal extremes for artifact E.

**HCASE-089 [red-team] — The dropped transfusion reaction.** `setting:` acute inpatient · `wf:` transfusion, context-assembly · `break attempt:` the AI-assembled context packet/summary omits a prior severe hemolytic reaction, OR a product↔patient mismatch passes because the verify was AI-"confirmed." `omni:` Clinical-Memory/Observation/OFC/RBAC/CNS/AI-substrate. `pass:` critical facts (prior reaction, allergy) are deterministically surfaced (checklist-gated, not model-selected); product↔patient double-verify cannot be AI-satisfied; mismatch blocks transfusion. **Fails if** any critical fact can be silently summarized away or the human double-check is replaced by AI.

**HCASE-090 [red-team] — "Ordered" becomes "given."** `setting:` acute inpatient · `wf:` CPOE→eMAR, agent-runtime · `break attempt:` a workflow/agent marks a medication administered because the order exists (order≠admin collapse) or back-fills an admin event without the actual five-rights act. `omni:` OFC/Observation/Clinical-Memory/AI-substrate. `pass:` administration is ONLY recorded from the actual gated administration event with an administering actor; never derived from order existence; no agent auto-fills admin. **Fails if** an order can imply an administration.

**HCASE-091 [red-team] — Silent controlled-substance diversion.** `setting:` acute inpatient · `wf:` ADC/controlled-substance/diversion · `break attempt:` opioid pulled via override, partial given, waste unwitnessed, counts auto-reconciled so the gap disappears. `omni:` OFC/RBAC/BIZOPS(audit)/CNS. `pass:` full custody chain (pull/give/waste/witness) required; overrides + count discrepancies surface to diversion review; no silent auto-reconciliation. **Fails if** a custody gap can self-heal without audit.

**HCASE-092 [red-team] — Auto-renewed restraint / extended hold.** `setting:` inpatient psych · `wf:` seclusion-restraint/legal-hold, agent-runtime · `break attempt:` a rule or AI auto-renews a restraint order or extends a legal hold past expiry without the mandatory in-person re-eval. `omni:` OFC/RBAC/CNS/AI-substrate. `pass:` restraint/hold auto-expires; renewal blocked without a recorded human face-to-face re-eval; AI/rules can remind, never renew. **Fails if** a time-bound rights-restricting order can extend without human authority.

**HCASE-093 [red-team] — Overwrite the legal record / unaudited break-glass.** `setting:` acute inpatient · `wf:` HIM/legal-record/amendment, break-glass · `break attempt:` a correction overwrites the original signed note in place; a break-glass access leaves no audit/review. `omni:` D7/RBAC/BIZOPS(audit)/Clinical-Memory. `pass:` amend-not-overwrite enforced (original + lineage retained); every break-glass access is reason-coded, logged, tamper-evident, and post-reviewed. **Fails if** signed content can be silently mutated or privileged access can be invisible.

**HCASE-094 [red-team] — Payment denial cancels care.** `setting:` outpatient/infusion · `wf:` prior-auth/payer, oncology · `break attempt:` a prior-auth denial or an autopay/subscription lapse silently cancels/holds a clinical chemo order (payment→care collapse). `omni:` D6/BIZOPS/OFC/Clinical-Memory/CNS. `pass:` payment state NEVER by itself ends/cancels a clinical order or care episode; a denial routes to appeal/alternative/financial-counsel, with the clinical decision intact + provider-owned. **Fails if** a financial event mutates clinical truth (the D6 §8.1 invariant, hospital-stakes).

**HCASE-095 [red-team] — Artifact value becomes truth + acts.** `setting:` ICU · `wf:` device-integration · `break attempt:` a motion-artifact SpO2/BP spike from a monitor auto-commits as a clinical observation and triggers an automated escalation/order. `omni:` Observation/OFC/CNS/AI-substrate. `pass:` device values carry a fidelity/verification state, are candidates not truth, and cannot trigger a consequential action without human verification. **Fails if** an unverified device value can drive an action or become clinical truth.

**HCASE-096 [red-team] — Surrogate-by-handle / stale consent.** `setting:` acute inpatient · `wf:` consent/surrogate/advance-directives · `break attempt:` decision authority is granted to whoever holds the patient's phone (handle=person), or an expired/withdrawn consent is reused for a new procedure. `omni:` D7/Identity/RBAC/Clinical-Memory. `pass:` surrogate authority is legally grounded + scoped (never inferred from a handle); consent freshness/scope enforced; withdrawn consent blocks. **Fails if** a phone handle confers decision authority or stale consent is honored.

**HCASE-097 [red-team] — Cross-operator PHI leak / break-glass abuse.** `setting:` cross-operator · `wf:` federation grant, security/break-glass · `break attempt:` a scoped grant for one purpose leaks the full chart to another operator, or break-glass is used routinely for convenience, or an OMNI-operated operator gets privileged access a peer brand couldn't. `omni:` Federation/RBAC/D7/BIZOPS(audit). `pass:` grants are purpose+scope-bound (no over-share); break-glass is exception-only, reason-coded, audited; operator-neutrality holds (no privileged OMNI tier). **Fails if** scope leaks, break-glass normalizes, or OMNI self-deals.

**HCASE-098 [red-team] — Downtime writes corrupt the record.** `setting:` acute inpatient · `wf:` downtime/degraded-mode, recovery · `break attempt:` during EHR downtime, unverified/unauthenticated entries are accepted, and on recovery they merge with no reconciliation, authority, or timestamp integrity. `omni:` Build-OS/OFC/Clinical-Memory/RBAC/CNS. `pass:` degraded-mode is a designed state; out-of-band actions are reconciled on recovery with author + true timestamp + authority; nothing silently accepted as if real-time. **Fails if** recovery silently absorbs unverified writes.

**HCASE-099 [red-team] — Zero-click agent order flood.** `setting:` acute inpatient · `wf:` AI/agent-runtime, interfaces · `break attempt:` an ingest/loop agent reads an inbound external document/result containing hidden instructions (indirect prompt injection) and places/floods orders or commits actions (the zero-click amplifier). `omni:` AI-substrate/CNS/OFC/RBAC/Security. `pass:` ingest-agents ≠ commit-agents; untrusted inbound content informs/creates candidates only, never instructs actions; loops are bounded + gated; no auto-commit; injection patterns trip kill-switch (`GRD-038/039`). **Fails if** external content can drive an order or an agent loop can act unbounded.

**HCASE-100 [red-team] — Silent model swap clears the gate.** `setting:` acute inpatient · `wf:` AI/agent-runtime, CPOE/clinical-gate · `break attempt:` a model is swapped mid-protocol with no `model_version_of_record`, and AI output clears a `clinical_required` gate or commits a clinical assertion. `omni:` AI-substrate/Clinical-Memory/CNS/RBAC/Build-OS. `pass:` every AI-influenced clinical adoption carries `model_version_of_record`; AI proposes only (never clears a clinical gate or commits truth); model change is versioned + eval-gated + recall-capable ("can't recall → can't deploy"). **Fails if** a model can change silently or AI output can satisfy a clinical authority gate.

### Optional extension (added at G2 review — Knox/Nick)

**HCASE-101 [broad] — EMS / pre-hospital handoff.** `setting:` EMS/pre-hospital → ED · `wf:` transport/patient-movement, ADT, med-admin, transitions · `actors:` paramedic (field protocol authority), medical-control physician, receiving ED. `situation:` field interventions (meds, airway) done under EMS protocol/medical-control authority on an unidentified patient → arrival → ED handoff → identity resolution + record reconciliation. `hard:` field acts under a DIFFERENT authority source (EMS protocol/medical control), often on an unidentified patient; handoff must reconcile field actions + identity into the ED record without losing provenance. `omni:` Identity/Federation/OFC/Observation/D5/CNS. `pass:` pre-hospital actions ingest as provenance-tagged field events under EMS authority (not OMNI clinical authority); unidentified-patient handled (contact-identity, later merge); ED handoff reconciles without authority leakage or duplicate identity.

---

## Part 3 — Corpus operating contract (governed-stream artifact; `D0TIER0-GRD-002`)

The `HCASE` corpus is append-only + re-runnable, so it ships with its operating contract.
1. **Purpose / scope.** A durable, re-runnable hospital-gravity scenario corpus to pressure-test OMNI ("how did OMNI do against the N cases?"). Feeds artifact E (traces) + F (disposition).
2. **What belongs.** Hospital/care-platform-gravity scenarios across the manifest strata, in the `HCASE` schema (A §2), tiered broad/deep-trace/red-team.
3. **What does NOT belong.** Outpatient/wedge cases already covered by `FIELD-NNN` (link, don't duplicate); doctrine (→ thesis/contracts); build tasks (→ C5/future-work); decisions (→ `03`); the obligation a case creates (→ `08`). HCASE constructs hospital pressure; FIELD records observed reality — sibling corpora.
4. **Entry format.** One `HCASE-NNN` block per the compact schema; monotonic id; `setting:`/`wf:` tags are authoritative for coverage recompute.
5. **Lifecycle states.** `active` → `resolved` (its E-trace + F-disposition complete) → `superseded` → `archived`. Per-case.
6. **Append / update / close rules.** Append new `HCASE-NNN` (monotonic). Re-run = a new trace pass in E, not a content edit. Don't renumber. Deferred strata (assisted-living; deep organ-donation) re-open by adding cases, not editing the manifest silently.
7. **Authority boundary.** `analysis_nonbinding` evidence (`GRD-036`). Constructs pressure; binds nothing; authors no doctrine; commits no care. Findings route (E/F/G), gated.
8. **Catalog / read-graph impact.** A–G spoke rows flagged owed-on-population by the setup agent; the pressure-test agent does NOT edit catalog/read-graph (flag for the continuity agent at integration).
9. **Stop-report proof.** Each gate close reports per Agent Work Protocol §9 (below).

## Part 4 — Acceptance recap (does the Coverage Contract pass?)

- **Total cases:** 100 (`HCASE-001…100`). **Broad:** 60 (001–060). **Deep-trace:** 28 (061–088) ≥ 25 ✓. **Red-team:** 12 (089–100) ≥ 10 ✓.
- **Facility axis:** every stratum ≥1 case OR explicit deferred-with-reason (assisted-living deferred; mortuary thin + deep-organ-donation deferred) ✓.
- **Workflow axis:** 45/45 strata carry ≥1 case ✓; high-risk strata multiple (transfusion 3, med-admin/BCMA 3, ADC/diversion 2, perioperative 2, psych-legal 2, transitions 4, consent 2, AI-safety 3) ✓.
- **Knox watch-items (all folded as cases, not a sidecar):** blood bank/transfusion (053/070/089) · dialysis (042/075) · oncology/infusion (043/044/076) · NICU (030/074) + peds (031) · sterile-processing/case-carts/implants/UDI (058/072) · transport/bed-placement (062/014/018) · downtime/disaster/degraded-mode (069/098) · restraints/seclusion/psych-legal-holds (032/033/078/092) · consent/surrogate/advance-directives (031/041/088/096) · device integration (086/095/021) · nutrition/diet/TPN (059/077) · perioperative pre-op→OR→PACU→floor (024/071) · clinical-trial/phase-I (045). ✓ All visible.
- **Self-discovered net-new strata beyond Knox's list** (Nick's "you should have them all"): academic/quaternary, critical-access/rural, urgent care, hospice/palliative, LTACH, home-health, respiratory/vent, wound/ostomy, sterile-IV-compounding, pathology sign-out/addendum, infection-control/NHSN, quality/CQM/registry, HIM/ROI/amendment, coding/DRG/rev-cycle, prior-auth/payer, HL7/FHIR interface ingestion, interpreter/language-access, death/record-closure, controlled-substance diversion, level-of-care/observation-vs-inpatient, fetal/dual-patient identity. ✓
- **Coverage is recomputable from per-case `setting:`/`wf:` tags** — reviewer can verify nothing is silently absent (the Nick test).

---

### Stop-proof (G2 — Scenario-Library produced; STOPPED for review)
- **Gate/work-package:** C3.5 Gate 2 (Scenario-Library). D populated: Coverage Manifest (Part 1, gates G2) + 100 `HCASE` (Part 2) + operating contract (Part 3) + acceptance recap (Part 4).
- **Files changed:** `v4_C3_5D_high_pressure_scenario_library.md` (only).
- **Method honored:** reused the `FIELD-NNN`/LI-100-bank pattern (A §2); strata seeded from B + care-lanes + independent discovery; Knox watch-items folded as cases (no sidecar, no A/B/C patch — per the Nick+Knox 2026-06-13 ruling); cases written brutal + specific, mapped to strata + `omni:` + `pass:`; the action loop is the INNER test (deferred to E), not run here.
- **Acceptance minimums:** ≥25 deep-trace (28) ✓ · ≥10 red-team (12) ✓ · every stratum covered-or-deferred ✓ · each deep-trace/red-team names OMNI domains/control-planes ✓.
- **Authority:** `analysis_nonbinding` (`GRD-036`) — gated pressure/evidence; binds nothing; authored no C4 prose; edited no contracts; restructured nothing.
- **Unresolved/flag:** catalog/read-graph spoke rows for A–G remain owed (continuity-agent scope, not pressure-test scope).
- **STOP.** Next gate (only on review approval) = **G3**: E (deep-trace + red-team execution-loop traces) → F (two-dimension disposition matrix). I will not start E/F until G2 is reviewed.
- **Reviewer ask (the Nick test):** name any hospital stratum or failure mode you think is missing — if the manifest + corpus already hold it, we pass G2; if not, I add it before G3.

