# EVRUN-2026-000004 — Canonical Async-Care Substrate: Cross-Arc Crystallization Map

Document type: `evidence_or_ingestion` (Evidence Plane · `user_operator_research` lane · analysis run) · Authority: `analysis_nonbinding` (`GRD-036` capture-broad / promote-gated). **Binds nothing. Not OMNI truth until each finding is promoted through its destination home's review gate.**
Run id: `EVRUN-2026-000004` · run_date: `2026-07-09` (trifecta close `2026-07-11`) · analyst: `Opus` (grounded agent) · lane: `user_operator_research` · status: **`trifecta_reviewed` · `accepted_as_required_v4_spine_input` · `not_promoted`** (4 trifecta rounds, Nick + Knox + Opus). **★ Read §0.5 SETTLED STATE first — §1–§9.9 are the derivation.**
Primary specimen: `EVSRC-2026-000251` (the canonical TRT prospective-patient consult + parallel ChatGPT-screenshot loop). Sibling specimens: `EVSRC-2026-000200` (Kyle / patient-side pre-clinical workbench), `FIELD-001` (Alec / longitudinal-signal-in-messages).
Method note (`GRD-044`): this is a **cross-arc reconciliation**, not a new-source ingestion wave. It reconciles the 251 physics decomposition (its §3 Review 001 + Review 003) against the FULL accepted estate (`v4_C2_source_base_declaration.md` 9 categories) — thesis v3-integrated, the domain contracts + maps, the C3.5/C3.6/C3.7 hospital+oncology arcs, the C3.8 enterprise/Palantir arc, the C4 bet-memo/register/watch-list/Polaris layer, and the Phase-4H messaging corpus. Reconciliation is **registry/prose-to-canon** (does the estate already crystallize this?), not adjudication (that is the spine author's job at the 4-lane Constitutional Reconciliation Ledger).

> **★ WHY THIS RUN EXISTS (operator directive, 2026-07-09).** Nick flagged that a prior agent had **flattened** the ~2k-line Nick↔Knox discussion in `EVSRC-2026-000251` with a "≈80% restatement" handwave, and directed a genuine study of what the T-analysis surfaced + a **complete** (not boot-plus-5-crown-jewels) cross-check against the estate, to answer: *for each top-level mechanic surfaced, is it fully crystallized in canon, partially, or nowhere — and what can we not miss for the v4 spine + thesis?* This artifact is that answer, captured propose-only so it survives into spine authoring instead of leaking at the pivot (the exact "leak-at-pivots" failure the controlling plan warns about).

> **★ HEADLINE VERDICT (supersedes the "80%" framing).** The *governance core* the T-analysis re-derives is already crystallized in canon — sometimes almost verbatim (§7.7 *"Projections expose. Capabilities act. Domains commit. Audit proves"* = the Access/Display/Action/Commit split; §A trust/authority/permeability axis = the membrane family). **But** several **top-level object + physics** ideas the discussion pushed are genuinely **partial or unnamed everywhere**, and one is an explicit **undecided fork**. The useful output is not a percentage — it is a per-mechanic map (§3), a small precise import list (§4), a Relationship/Subject verdict (§5), and one decision to force (§6). **251 CONSOLIDATES + SHARPENS the thesis and exposes a build mandate; it does NOT fork the ontology.**

---

## §0.5 — ★ SETTLED STATE (read THIS first; §1–§9.9 are the derivation)

**WHY THIS LANDS (do not re-open / re-invent):** this run introduced **no new *foundational* primitives.** It (a) reconnected the async-care analysis to existing canon — `Governed Resolution` (REV-184) · `authorized_action` (§7/§8) · `patient_relationship` (Identity) · §A/§7.7/§C boundary model · GCE — and (b) generalized a few *root nouns* to standards (person/organization). **Net = reduced + clarified ontology, not expanded.** It DID produce real *discoveries* (not new primitives, but genuine): person-vs-patient, operator-as-role, the affirmative Radius-1 awareness default, replay-as-acceptance-criterion, the communication-event fabric build mandate. (OMNI already HAS an ontology — the 15 contracts + §7 primitives + System Map; this run *refined its nouns*, it did not discover the need for one.)

**Frozen hierarchy — CONCEPTUAL FLOW (not final transaction/schema cardinality; the cardinality is the C5 action-contract, ③):**
`signal / candidate → Governed Resolution (REV-184; resolution-state) → authorized_action(s) → owning-domain commits / records / obligations / communications / proof → projections (P4)`
**Invariant (already canon — §A four-way + candidate≠commit + §7.7): every consequential mutation crosses an authorization path and is committed by its owning domain.** (NOT yet settled: whether every domain object corresponds to its own persisted action; whether one action spans domains; whether a resolution may terminate with zero actions — all ③ below.)

### Trifecta disposition ledger (4 statuses)

**① AFFIRMED_CANON — do NOT reopen:** Governed Resolution = REV-184 (closed/signed-off) · `authorized_action` = §7/§8 · **domains own committed truth** (candidate≠commit) · boundary model = §A (law) + §7.7 (expose/act/commit/prove) + §C **Governed Capability Exchange** (the crossing; the canonical exchange) · Identity invariants (reusable-vs-scoped; match≠authorize; actor split) · **Federation = the architecture/domain term** (`network` = acceptable product/boardroom translation) · the mutation invariant above.

**② TRIFECTA_CONVERGED_DIRECTION — spine LANDS these unless it cites a concrete canon conflict:**
- `person` + `organization` = neutral bases; `patient` + `operator` = contextual **roles**.
- `subject` / `subject_ref` = generic interaction target; `research_subject` = study role.
- Radii = plain **Inner / Federated / External**.
- **★ Radius-1 affirmative default (the substantive M4 win — do not lose to least-privilege):** broad shared situational awareness (common operating picture) · role-shaped display · accountable drill-down · narrow commit.
- **Recommendation Integrity Firewall** (economically-blind recommendation/display engine; clinical AND research).
- **`relationship_graph_projection`** = P4 projection (Identity/Federation supply relationships · Messaging/D7/Observation supply events · CNS assembles · **no new truth store**).
- **`replayable_proof` = acceptance criterion:** the de-identified 251 exchange must replay end-to-end (input → identity/context correction → observation/assertion → context assembly → resolution → actions/obligations → communication → domain commits → proof).

**③ REQUIRED_RECONCILIATION — bounded C5 work w/ acceptance criteria (NOT "future agent, figure it out"):**
- **Action-Type / command contract:** define `action_type` vs action-instance; is `authorized_action` a command-envelope / object / event / all; can one action write across domains or must CNS decompose; primary effects vs deterministic side-effects; atomicity · retry · idempotency · compensation · partial-failure; **a Governed Resolution may validly terminate with ZERO downstream actions**; where authority is checked (candidate / resolution / authorization / execution / domain commit — note "emission" is a retired term, §0.5 ④). Partial home: §7 candidate-types + RBAC atoms + domain write-APIs (`protocol_definition`/`capability_envelope` do NOT cover it).
- **Identity generalization:** `identity_contract` `patient` reusable-base → role on `person`; `federation_contract` `operator` base → role on `organization`; System Map ID/FED rows (patient/operator = roles, not roots). Invariants preserved.
- **Entity-model correction:** `legal_entity` = the **juridical bearer** (a person OR organization capable of rights/duties/contracts/licenses/taxes); `legal_form` = its **TYPE** (PLLC / PC / LLC / corporation / nonprofit / gov / sole-prop). Do NOT conflate the entity with its form.
- **Communication-event fabric (M2 — build mandate, do not evaporate):** platform-thread vs care-thread; view/use/send/commit as distinct events; screenshot = degraded provenance; channel posture. Dedup vs `source_event` + §7.5.1 + messaging-contract.
- **Governed Resolution field-set + lifecycle state-machine** (REV-184 → C5).

**④ REJECTED_DO_NOT_REMINT (with reason):** `Governed Crossing` (= §C GCE) · `Boundary Regime` (= §7.7) · `Membrane Radii`/`Trust Radii`/`Operating Radii` (ruin "radius"; use plain Inner/Federated/External) · `Operational Emission`/`Operational Decompression`/`Resolution Cascade`/`Resolution Output`/`Resolution Effects`-as-foundational-primitive/`resolution_action_set` (unnecessary wrapper nouns — the relation suffices) · `stance`-as-new-field (use REV-184 resolution-state).

**★ Spine-shape INPUT (not decided here):** the "ontology-first" realization (deciding-what-exists precedes architecture) is a valuable lens, but any RE-ORDERING of the v4 spine is a **C4 spine-shape** decision (`v4_C4_spine_shape_plan`, care-first Mission Anchor) — routed there, not decided in this run.

**Metaphors (keep in prose, NOT ontology):** "membrane," "compression → decompression." The architecture words are §A / §7.7 / §C + `Governed Resolution → authorized_action`.

---

## §0 — How to read this (for Knox + the fresh v4 spine author)

1. This is `analysis_nonbinding` (`GRD-036`). It **proposes**; it promotes nothing. No thesis/contract/C5 edits were made by this run.
2. **Boot state unchanged.** Master checkpoint remains `HANDOFF_2026-07-04_c3_8_complete_pre_spine.md`; controlling plan remains `~/.cursor/plans/wave-2_source_scaffolding_654989a0.plan.md` (HOME). Boot Freshness Check passed at run start.
3. The mechanics are labelled **M1–M6** (defined in §3) — a stable handle for the trifecta discussion, NOT proposed primitive names.
4. **Anti-flatten contract for this artifact:** load-bearing claims carry a verbatim anchor + source pointer (§11). Do not compress an anchored claim into anchor-less prose (integrated-spine anti-compression rule; §9 J3).
5. **How the spine author consumes this:** as a disposition CHECKLIST run DURING chunked authoring, through the **4-lane Constitutional Reconciliation Ledger** in `omni_thesis_v3_integrated_spine.md` — never author from this file's prose; reopen the cited source packets + canon sections and reconcile (`GRD-044` authoring gate). Every M-row + the §4 import list + the §6 decision must receive an explicit disposition at spine acceptance (`GRD-043`).

---

## §1 — The specimen + the physics question

`EVSRC-2026-000251` is a real TRT prospective-patient consult conducted over iOS Messages (labs exchanged as MyChart screenshots) **while the provider ran a parallel ChatGPT-screenshot-then-confirm loop** — the live Lens-A workaround AGENTS.md names OMNI as displacing. The claim under test: *if OMNI cannot run THIS simple exchange end-to-end, governed, it hasn't earned its thesis.*

The specimen carries two analysis layers:
- **§3 Review 001** — Knox strategic read (~630 lines) + the extended Nick↔Knox discussion (~lines 1875–4654) = the "meat."
- **§3 Review 003** — Opus physics decomposition (16-move trace, both threads) + consolidation verdict §G.

The single sharpest sub-finding (preserve): the provider seeded ChatGPT with **"18 yo male"**; the patient is **46** (*"Oh lol for some reason I was thinking you were 18yo. I miss heard Trent"*). The ungoverned loop propagated a critical wrong-data error and reasoned an entire clinical frame on a false age — the exact failure identity-derived context makes structurally impossible.

---

## §2 — Method + source coverage (the completeness proof)

Reconciled against the **canonical source list** = `v4_C2_source_base_declaration.md` §2 (the full 9-category estate, catalog-reconciled, Nick+Knox-accepted 2026-06-13, living per `GRD-036`) + read-graph #9a (canonical v4-authoring input set).

**Directly read (grounded agent):** `EVSRC-2026-000251` in full (both threads + Review 001 + Review 003); `omni_thesis_v3_integrated_2026-06-05.md` §7.5.1/§7.5.3/§7.6/§7.7/§8/§8.6/§A/§B; `OMNI_Surface_Map_vNext.md`; `contracts/identity_contract.md`; `OMNI_System_Map_vNext.md` (ID/FED rows); `EVSRC-2026-000247` (Palantir) + Review 003; `v4_C3_8_G4_disposition_ledger_and_handoff.md`; `v4_C3_8_G2_convergence_and_translation_map.md`; `v4_C2_source_base_declaration.md`; controlling-plan banner.

**Read via explorer sub-agents (verbatim-anchored):** C3.5/C3.6/C3.7 hospital+oncology arcs; C4 bet-memo/adjudication/late-builder-register/watch-list/Polaris + enterprise-posture + AI-substrate-frame; Phase-4H messaging corpus + `EVSRC-2026-000200` (Kyle) + `FIELD-001` (Alec); and the completeness sweep of the previously-unread high-yield docs: the two `2026-06-03` pressure tests, `ORIENTATION-2026-06-10`, `omni_thesis_v3_integrated_spine.md` (weave map + 4-lane ledger), `cns_and_knowledge_reservoirs_frontier_2026-06-06.md`, `EVRUN-2026-000001 §2A`.

**Completeness statement.** The framing-level estate relevant to async-care substrate physics is now read across five arcs + the canonical stack. Remaining unread is **domain-specific** (EVRUN-000002 registry; competitor evidence; `designs/` day-0 scheduling; remaining domain contracts — clinical_memory/observation/intake/CNS/D5/D6/D7/OFC/rbac/federation-full), to be pulled per-mechanic at authoring time — not framing-level crown jewels.

---

## §3.0 — Why these landings (reasoning layer, not just verdicts)

*Added per operator request (do not flatten the derivation). The full back-and-forth lives verbatim in `EVSRC-2026-000251` §3 (Review 001 + Nick↔Knox discussion + Review 003); this is the compressed ARGUMENT for why each landing holds, so the spine author doesn't re-derive it from a 4,742-line source.*

- **Why the governance core is "already ours — reconcile, don't re-mint":** the T-analysis re-derived boundary=authority, candidate≠commit, projections-expose/capabilities-act/domains-commit, and the trust/permeability axis *from a fresh clinical example* — and each already sits in canon (§7.7, §A, §B, §8), sometimes near-verbatim. When an independent derivation lands exactly on existing doctrine, that is **confirmation the doctrine is right, not a discovery.** So the correct move is reconcile-to-canon; the failure mode (which the "80%" verdict risked) is re-minting duplicates of settled law.
- **Why M1 (Relationship/Subject) is NOT greenfield:** the reusable-identity-vs-scoped-relationship split (identity_contract §5) already carries a pre-account layer (`contact_identity`), a reusable human identity (`patient`/→`person`), and a broad scoped relationship (`patient_relationship` owns consents · memberships · messaging · lifecycle — *more than "care"*). So "the relationship is the object" is mostly **doctrine-landed-but-unbuilt** (`REV-144`); the genuine work is *naming* (neutral `person`/`organization`; generic target `subject_ref`) + the cross-channel stitching as a **`relationship_graph_projection` (P4)** — not a new ontology.
- **Why the broad-awareness fork is THE decision (not a footnote):** a practice is an organism — front desk / MA / provider / owner share a common operating picture, and least-privilege-by-default would *blind the organism and worsen outcomes*. Yet naive broad access = the anti-superuser breach §7.6 rejects. **Both fail.** The resolution is not a compromise but a *disaggregation*: broad **Visibility** ≠ broad **Commit**; awareness is broad-by-operational-need (within a defined authorized unit + purpose; sensitive-category/consent/partition overlays still apply), display role-shaped, drill-down accountable, commit narrow. That is exactly what **§A + §7.7 + §C** compute (expose broad · commit narrow · audit proves) — which is why the fork must be **stated as doctrine**, not assumed.
- **Why it lands as a DISTRIBUTED reconciliation (not a new plane, and not "an un-built CNS to build"):** the pieces route to existing owners — Identity/Federation own relationships + cross-boundary grants; Messaging/D7/Observation supply source events + artifacts; **CNS** assembles context + coordinates candidates/actions (its orchestration contract + the C4.1 CNS↔Polaris lock already exist); **P4** owns `relationship_graph_projection`; **§A/§7.7/§C** own permeability/exposure-action-commit-proof/GCE; owning domains commit. Minting new planes would fork the ontology — the precise guardrail `EVSRC-251` §G warns against.
- **Why the yield is small on purpose:** the anti-inflation posture is itself a finding — a single field specimen should produce *a few sharp imports + one forced decision*, not a new substrate. Inflating 251 into a new ontology would be the failure, not the success.

---

## §3 — The mechanics crystallization map (M1–M6)

> ★ **This §3 is the DERIVATION (what 251 surfaced + how it maps to canon). For SETTLED NAMING + dispositions, see §0.5.** Section titles below (e.g. M3 "operational emission", M5 "membrane family") preserve the *specimen language*; several of those names were **retired as ontology** (see §0.5 ④) — "compression/decompression" and "membrane" are kept here as **metaphor**, not proposed primitives.

Legend: **CRYSTALLIZED** (explicit in canon) · **PARTIAL** (kin exists, mechanic incomplete) · **UNNAMED** (surfaced in 251, no canon home) · **DECIDE** (open fork).

### M1 — Relationship as first-class object · neutral Subject · Relationship Graph
- **What 251 surfaced:** the *relationship* (subject↔organization, spanning care + commerce + complaint + reschedule) is the real object a practice organizes around; chart/thread/profile are projections inside it. A neutral **Subject** (prospect/lead/client/member, not "patient"). A **Relationship Graph** that stitches events/artifacts/workstreams across channels — explicitly *not a truth store* (`EVSRC-251` §Knox 3479–3543, 3628–3696, 4024–4028).
- **Canon coverage — PARTIAL (less greenfield than it first appears):**
  - `patient_relationship` (primitive #19, `identity_contract.md` §4) IS a *scoped operational relationship* owning consents · intake · memberships/packages · appointments · care_programs · messaging thread context · clinical chart context · assigned care team · communication endpoint · lifecycle (active/disengaged/lost/churned/transferred/merged) — **broader than "care."** **Doctrine landed; substrate NOT built (`REV-144`).**
  - Neutral Subject ≈ `contact_identity` (pre-account/lead/unknown-inbound; **handle≠person**) → `patient` (reusable identity-claim). The *base* exists; the *neutral naming* + client/member framing does not (OMNI is patient-terminal).
  - Relationship Graph ≈ `care_team_graph` (thesis §7.4, CNS-computed over `patient_relationship`) + Federation `care_relationship` (cross-operator, deferred v2/v3). Care-team-membership-scoped; the cross-channel event-stitching graph is unnamed.
  - Unified-experience half is CRYSTALLIZED: §7.7 *"patient sees one shelf; substrate knows who owns each item"* + Surface Map *"distributed truth, unified surface."* C3.5's own superiority headline: **"100x = relationship design, not nouns"** (33 relationship chains A–GG).
- **Verdict:** the object mostly exists (`patient_relationship`), unbuilt + patient-terminal; the stitching graph is a **CNS/projection composition** gap, not a new domain (§5 has the full dive).

### M2 — Channel-agnostic care-thread graph · event fabric · channel posture · screenshot=degraded
- **What 251 surfaced:** platform-thread ≠ care-thread (one care thread spans many channels; one channel carries many care threads); every event carries where/who/role/**viewed_by/used_by/approved_by**/authority_state; **view/use/send/commit are distinct events** ("provider peeking is an event"); screenshot = degraded provenance; **channel posture** (what each channel can safely carry) not channel wall (`EVSRC-251` §Knox 1909–2110).
- **Canon coverage — PARTIAL (designed, deferred):** `source_event` (actor+time+identity_confidence+purpose_of_use+consent_context) + per-event ownership §7.5.1 (`channel_of_record`, surface/operator/…) + messaging `conversation_scope` + `channel_policy_matrix` + `privacy_exposure_level 0–5`; `care_episode` = "longitudinal care thread"; **c2 portal chat shipped**; external-line e0 designed / e1 not built. **Stranded:** the event fabric (view/use/commit as events), channel-posture taxonomy, platform↔care-thread orthogonality (Phase-4H topology §11 *anticipated* this stratum; `REV-161` care-loop homing, `REV-142` Alec loop open).
- **Verdict:** `doctrine=AFFIRM · build=ABSENT` for the async multi-channel clinical workspace. Import the event-fabric + channel-posture + orthogonality naming; dedup against `source_event`/§7.5.1/messaging-contract.

### M3 — Compression → decision → decompression · operational emission
- **What 251 surfaced:** the physics of healthcare = broad net of actors → **compress** the situation → **authority decision** → **decompress** into role-shaped operational emissions (schedule/order/bill/task/follow-up/patient-msg) → feedback. Hospital = "a crossroads in someone's life." "Same underlying decision. Different operational emissions." (`EVSRC-251` §Nick/Knox 2886–3153, 3800–3834).
- **Canon coverage — PARTIAL:** §8 two governed loops (Sense→gate→Act) + §8.6 `fulfillment_order`/`care_obligation`; C3.5 **Chain A** (signal→candidate→authority-gate→act→proof→obligation→learn) + `notify_fan_out` + proactive-orchestration; ORIENTATION's **four loops (Sense/Decide/Act/Prove-Learn)**; **C3.7 `governed_decision_lifecycle`** adds defer/monitor/no-action/honest-null — and **251's "decision" (broader than "act") aligns with C3.7, ahead of raw C3.5 Chain A.**
- **Verdict:** the loop skeleton is CRYSTALLIZED (four loops + Chain A + C3.7 completion). **UNNAMED:** the compression/decompression *framing* and specifically **operational emission = role-shaped multi-audience fan-out from ONE commit** (today modeled as fulfillment_orders/care_obligations, not audience-shaped projections). Import as a sharpening of the four-loop model, not a new loop.

### M4 — Boundary = authority not information · Access/Display/Action/Commit · context membrane · broad-context/narrow-commit
- **What 251 surfaced:** *"the boundary is not information, it's authority"*; broad shared situational awareness / common operating picture; front-desk veteran; **Access vs Display vs Action vs Commit** as four distinct things; context **membrane** not privacy **wall**; **"Broad context. Narrow commit. Clean decompression."** (`EVSRC-251` §Nick/Knox 2886, 3013–3047, 3085–3117, 3436–3440).
- **Canon coverage — PRINCIPLE CRYSTALLIZED (×4) / affirmative default = DECIDE:**
  - §7.7 *"Projections expose. Capabilities act. Domains commit. Audit proves"* + *"projection ≠ authority — seeing something does not grant power to act on it."*
  - C3.5C **"See ≠ act ≠ review/sign ≠ release"** + full hospital actor/authority map + a named **common operating picture** ("one shared situational map, role-scoped contributions").
  - C3.8 G4 **Tesla parallel** (what the machine can-do / autonomously-does / human-manually-chooses / logs / triggers-limits) + *"legible, attributable, reviewable governed medicine."*
  - Bet layer **"open rails, closed authority"**; Polaris `projection≠authority`.
- **Verdict:** the *principle* is over-determined in canon — do NOT re-mint. The **affirmative broad-awareness default at the practice radius** is the open fork (§6).

### M5 — Membrane family · internal = external at different radii · the alpha
- **What 251 surfaced:** the membrane is *behavior between objects*, recurring at every arrow; outer(world) / inner(context-shaping) / authority(candidate↔commit) / memory(learned) / egress membranes = the **same operation at different radii / trust postures**; internal governance = external permeability at a different radius (`EVSRC-251` §Nick 3450–3451; §Knox 3455–3474, 3879–3951, 4165–4205).
- **Canon coverage — CRYSTALLIZED (outer/strategic) / PARTIAL (inner + unification):** §A **Trust/Authority/Permeability axis** ("a property that runs through every plane"); §C Governed Capability Exchange (outer boundary; handshake stack; trust-relocated; agentic last mile); §7.7 projection-exposure-gating (inner shaping); bet-adjudication **Verdict B ACCEPTED**: the **5-face membrane law**, boundary-fluid/dimensionality-aware, fractal **alpha**, agent-native edge; WI11 "open rails / closed authority."
- **Verdict:** the membrane LAW + permeability axis + alpha are CRYSTALLIZED. **UNNAMED:** (a) the **inner context membrane** as a first-class runtime operation (select/compress/redact/annotate/escalate per actor×task×action, default compressed-not-hidden), and (b) the **unification** ("same operation at different radii"). Nick's intuition is correct and un-named — import as a §A sharpening, not a new object.

### M6 — Strategic posture (Palantir / FHIR / Salesforce) · moat = vertical physics · proof = replay
- **What 251 surfaced:** the architecture class is known (Palantir ontology = objects+links + actions/functions + dynamic-security; Salesforce = relationship; FHIR = resources, whose `Communication` *"does not represent the actual flow of communication"* = OMNI's gap); OMNI's moat = the vertical practice physics; **proof = replay, not thesis** (`EVSRC-251` §Knox 4222–4644).
- **Canon coverage — CRYSTALLIZED (AFFIRM):** C3.8 tenant-ownership + Karp Review 003 (ontology=IP-boundary; value=model+app+compute; own-means-of-production; model-agnostic; take governed-substrate-value, **reject lock-in** → Dario/248 counter-pole). Bet/enterprise posture: FHIR=rail-not-authority; anti-Palantir-ontology-gravity; *"Epic standardizes the record, OMNI standardizes the operating physics"*; moat = care-governance semantics. C3.8 G2 translation map marks OMNI **weaker** on tenant-ownership/memory-ownership/portability + a **naming gap** on the semantic layer.
- **Verdict:** carry as-is. **Import from 251:** the Salesforce/CDP comparator lane + **"proof = replay/acceptance-test"** as an explicit spine acceptance gate.

---

## §4 — The genuine yield: the import list (dedup'd, propose-only)

> ★ **NAMING SUPERSEDED — see §0.5 SETTLED STATE for current terms + dispositions.** The items below are the *substance*; the naming below reflects early rounds and was corrected (esp. #5 "operational emission" and #6 "inner context membrane" — retired as ontology; see §9.9 + §0.5 ④). The substance stands; the names do not.

Everything else the T-analysis raised is already crystallized somewhere (§3) and must be *reconciled to*, not re-minted. The genuinely thin, worth-importing set:

1. **Neutral base + roles** (M1) — generalize reusable identity to `person`/`organization`; patient/operator = roles (§0.5 ②). *Naming/scope decision, not a missing primitive.*
2. **`patient_relationship` → build + broaden** (M1) — object exists in doctrine (`REV-144`, unbuilt); confirm scope covers complaint/dispute/reschedule; generalize under `person`.
3. **`relationship_graph_projection`** (M1/M2) — cross-channel stitching as a **P4 projection, not a truth store** → CNS assembles + §7.7 projections.
4. **Communication-event fabric + channel posture + platform↔care-thread orthogonality** (M2) — view/use/send/commit as distinct events; screenshot=degraded provenance; per-channel posture. Dedup vs `source_event` + §7.5.1 + messaging-contract. **(Build mandate — do not evaporate; §0.5 ③.)**
5. **Role-shaped decompression** (M3) — one `Governed Resolution` → `authorized_action`(s) → owning-domain writes (schedule/order/bill/message/follow-up/proof), each audience-shaped. ~~"operational emission"~~ **RETIRED as a primitive** (no wrapper noun; §0.5 ④); it's the Act/Prove-Learn fan-out, metaphor only.
6. **Permeability at the inner radius** (M5) — the runtime context-shaping (broad-access / compressed-default / accountable-drill-down). ~~"inner context membrane"~~ **NOT a new object** — it's §A permeability + §7.7 computed at Radius-1 (§0.5 ①/②). "Membrane" = metaphor.
7. **`replayable_proof` = acceptance criterion** (M6) — the spine must demonstrably RUN the de-identified 251 exchange governed, end-to-end (§0.5 ②).

---

## §5 — Relationship / identity deep-dive (DERIVATION)

> ★ **DERIVATION ONLY — current trifecta direction is §0.5:** `person` + `organization` = bases; `patient` + `operator` = roles; `subject`/`subject_ref` = generic target; `research_subject` = study role. (This section is the evidence for that landing, not an open gate.)

The DL-10 four-layer identity model (`identity_contract.md` §4–§7; `OMNI_System_Map_vNext.md` ID row) already carries most of M1:
- `contact_identity` — pre-account/lead/unknown-inbound; **handle≠person** (a phone/email may be family/assistant/shared/typo/fraud); ambiguous/shared/typo states first-class.
- `patient` — reusable identity-claim (one canonical row per person per namespace); L0–L4 assurance ladder.
- `patient_relationship` — scoped operational relationship (the broad bundle above); **doctrine landed, substrate unbuilt (`REV-144`).**
- `actor` — human + device/robot/external_system; only humans occupy §7.2 care-ownership roles.
- Binding invariants: **reusable-vs-scoped split** (§5); **two-gate rule** (§7 inv 2 — *"match gates linking; consent — separately — gates use/sharing; a match NEVER authorizes"*) = boundary-is-authority at the identity layer; **§6 admission guardrail** (a dimension becomes a relationship boundary only when it owns distinct operational state) = the anti-sprawl discipline Knox wanted.
- `care_relationship` + `shared_context_grant` are **Federation's** cross-operator layer (`OMNI_System_Map_vNext.md` FED row), deferred to ladder v2/v3.

**Net:** M1 is NOT greenfield. The fresh work is precisely three items: (a) **person/organization contract generalization** (patient/operator → roles; per §0.5 ③, a bounded C5 reconciliation, not an open naming question); (b) build `patient_relationship` (`REV-144`) + confirm breadth; (c) `relationship_graph_projection` (P4) composition. Also confirmed crystallized: 251's lab-card pipeline = §7.5.3 patient-source (`source_authority=patient`; `clinical_adoption_state: not_adopted→adopted`) + §8 flow + §12.1 — do not re-mint.

---

## §6 — The ONE decision to force (broad-awareness vs least-privilege)

> ★ **SETTLED (see §0.5).** The radii are **Inner / Federated / External** (three *scopes*). The incentive firewall is **orthogonal** (a law across all radii), NOT a radius. (Earlier drafts wrongly listed "Radius 3 = firewall" and a "Governed Membrane" model — both retired; §0.5 ④.)

The only place the arcs *contradict* 251. C3.5 crystallizes **minimum-necessary / least-context as a strength**; §7.6 rejects **blanket chart access**; the C4 late-builder register **withheld the decision** (E6/E7). 251/operator push **broad awareness, narrow commit, accountable drill-down** — *"that is the physics of healthcare"* (operator, 2026-07-09; binding: must NOT be flattened by a least-privilege default).

**The three radii (scopes where the boundary computes differently):**
- **R1 — Inner** (within ONE organization's authorized care/ops unit — team · floor · service line · command center · trial team; a hospital has MANY R1s): **DEFAULT = broad shared situational awareness (common operating picture), narrow commit.** ★ Full rule: *within a defined, authorized unit + purpose, default to broad awareness via role-shaped projections; sensitive-category / consent / patient-partition / legal overlays still apply; drill-down is accountable; commit stays narrow.* This is the affirmative default to ADD (canon leaned restrictive).
- **R2 — Federated** (across OMNI operators/organizations): **DEFAULT = aggregate-by-default, named-purpose-only.** §7.6's "no blanket chart access" correctly lives *here*.
- **R3 — External** (non-OMNI world: labs/pharmacies/payers/registries/external AI/MCP-FHIR rails): **open rails, closed authority.**

**Orthogonal law (not a radius): the Recommendation Integrity Firewall** — the recommendation/display engine is **economically-blind** (structurally cannot read payment/accrual/sponsor/operator/platform incentive) across *all* radii. Do NOT conflate it with internal ops awareness.

**The middle ground, made precise:** broad *awareness* ≠ broad *display-by-default* ≠ broad *commit*. At radius 1: **broad Access** (can inspect) + **compressed Display** (default role-shaped view) + **accountable drill-down** (logged/reviewable, not blocked) + **narrow Commit** (authority gates). This is *literally* §7.7's existing machinery (*"projections expose · capabilities act · domains commit · audit proves"*) applied at radius 1. **The only new doctrinal act is stating the affirmative radius-1 default — the enforcement machinery already exists.** Anti-flatten guarantee: we are not inventing a permission system; we are refusing to let least-privilege blind the practice.

---

## §7 — Where these mechanics land (routing, not new planes)

> ★ **SETTLED (see §0.5).** These mechanics land as a **DISTRIBUTED reconciliation across existing owners — NOT "an un-built CNS framework to build," and NOT new planes.** ("Relationship Graph / Context Membrane / Operational Emission" were retired as ontology; §0.5 ④.) The CNS orchestration contract + C4.1 CNS↔Polaris lock already exist; the CNS *assembles + coordinates*, it is not a new layer to invent.

- **Identity / Federation** own relationships + cross-boundary grants (`person`/`organization`, `patient_relationship`, `contact_identity`, `care_relationship`/`shared_context_grant`).
- **Messaging / D7 / Observation / domain events** supply `source_event`s + artifacts (the communication-event fabric dedups here, §0.5 ③).
- **CNS** assembles context + coordinates candidates→`authorized_action`s (law: *CNS orchestrates · Reservoirs supply governed context · owning domains commit truth*).
- **P4** owns **`relationship_graph_projection`** (a read-model, not truth).
- **§A / §7.7 / §C** own permeability / expose-act-commit-prove / Governed Capability Exchange.
- **Owning domains commit** (D3/D5/D6/D7/OFC/Clinical-Memory/Observation).
- **Execution-physics frame (M3):** ORIENTATION's four loops (Sense/Decide/Act/Prove-Learn); the resolution→action→domain-write fan-out is the Act/Prove-Learn half — a pattern over existing objects, not a primitive.
- **Reconciliation route:** the 4-lane Constitutional Reconciliation Ledger (`omni_thesis_v3_integrated_spine.md`): primitive→§7 catalog · projection/surface→§7.7 · loop/flow→§8 · contract invariants stay in contracts. Every §0.5 item routes through a lane.

---

## §8 — Do-NOT-flatten / naming discipline (guardrails, all arcs agree)

- **Do NOT mint** "Interaction Fabric / Relationship Graph / Context Membrane / Subject" as **new planes/domains** — dedup against Messaging/Surface · Identity (`contact_identity`/`patient`/`patient_relationship`) · context_packet · §A permeability axis · `care_team_graph`. Name the physics; don't fork the ontology (`EVSRC-251` §G).
- **Do NOT re-derive** the already-crystallized core (§3 M4/M6, §9) — cite canon.
- **Do NOT collapse** patient-source into clinical truth (§7.5.3 adoption gate) or make the Relationship Graph a truth store (Knox caveat; connective tissue only).
- **Anti-compression rule (binding):** keep source anchors ALIVE in the prose for load-bearing ideas — *"don't turn 'we found oil' into a nice paragraph about oil"* (`omni_thesis_v3_integrated_spine.md`).

---

## §9 — Crown-jewels inventory (substrate-law locks the spine author must not miss — from the completeness sweep)

Surfaced during this run's completeness pass; not part of M1–M6 but load-bearing (do not re-derive):
- **4-lane Constitutional Reconciliation Ledger** + **anti-compression rule** = the anti-flattening infrastructure (integrated-spine).
- **Three-envelope lock:** `capability_envelope` (model/tool, §B) ≠ `delegated_authority_envelope` (actor-for-principal, §A) ≠ `capability_contract` (invoke-across-boundary, §C).
- **"No single domain owns a cross-cutting concern"** (`GRD-035` generalization; god-domain guard).
- **Trust/authority/permeability = a cross-cutting AXIS** (same shape as the AI axis); **architectural center ≠ economic center.**
- **GCE handshake is a STACK, transport ≠ authority; trust is RELOCATED not eliminated; the "agentic last mile" is the canonical failure OMNI must own; Federation=boundary / CNS=execution / MCP=data.**
- **v4 = execution physics; four loops; repo-as-OS; OMNI Intelligence Foundry** (do not build a light Foundry before the spine).
- **The authority/commit family is OMNI's delta** — every generic AI taxonomy stops at *validation* and omits identity·consent·source-authority·human-review·policy-envelope·domain-commit·audit·revocation·visibility (`EVRUN-000001 §2A`). This is *why* M4's boundary=authority is the moat, not a footnote.
- **`agent_learning_boundary`** (self-edit/memory may NEVER touch clinical policy/authority/consent/identity) · **builder/runtime split — "the product is the boundaries"** · **outsource thinking not understanding.**
- **`capability_endpoint`/grant/node** topology; **dragon-egg** Federation arc.

---

## §9.5 — Addendum: the federation / supplement pressure (Nick↔Knox exchange, 2026-07-09)

Reviewed a same-day Nick↔Knox exchange (operator taking ~30 supplements; asking whether OMNI can survive/flourish against Big Tech via federation/MCP/interconnectedness). **Assessment (on the merits, per operator context §4):** the exchange is correct and well-reasoned but **~85% is re-derivation of already-ratified doctrine** (C3.8 enterprise posture + bet-memo **Verdict B** + watch-list **WI2/WI3** trust-to-action-moat + §A/§C + FHIR/MCP=rails-not-authority). That is **confidence-confirming corroboration** — the same physics falls out of a *consumer* example (supplements) exactly as it did from TRT/Kyle/Alec/hospital — **not new intel.** Folded findings:

- **(a) Supplement scenario = a SECOND acceptance-test specimen (consumer surface of the same physics).** "photo of supplement → ingredient extraction → match vs meds/labs/DNA/goals → risk/interaction screen → authority classification → action emission (educate/avoid/ask-provider/order-labs/refer/order/add-to-list) → memory update with authority state" is the identical M3/M4 flow as the TRT decomposition. **Strengthens M6 proof=replay:** the spine must demonstrably run BOTH the TRT clinical exchange AND the supplement consumer exchange, governed, end-to-end. Companion to `EVSRC-2026-000200` (Kyle) / `FIELD-001` (Alec); operator-articulated scenario, not a captured EVSRC (capture one if it recurs).

- **(b) "Signed context packet" = a crisp CANDIDATE NAME for the portable-context composition (sharpens M2 + §4 import #3/#7).** Knox's most concrete contribution: a patient-carried, cross-node, provenance+authority-stamped packet (e.g. a "TRT Workup Packet" a patient grants to PCP/endocrine/urology/another OMNI node). **DEDUP — do NOT mint as a new plane:** it is a composition over `context_packet` (§7.7/CNS §9.1) + §C `capability_contract` + `trust_transfer_record` (§A) + `knowledge_interchange_format` (the one genuine net-new portability leg, 238/239) + `shared_context_grant` (Federation, deferred ladder v2/v3). Route through §C at authoring time.

- **(c) GUARDRAIL (→ §8): the patient-facing "OMNI health guide" is a candidate-generating front door, NEVER a committer.** The operator's phrasing — *"your OMNI health guide confirms you should be on xyz, let's get that ordered"* — is one inch from the "universal health brain / AI-doctor" that C3.8 rejects and that invites the big-tech war. The bright seam: the guide **proposes + routes** (WI1 external/AI companion = candidate-generating actor, never authority; Polaris = computed projection; **AI never commits**); *"let's get that ordered"* is a **provider-authority commit** (or a governed protocolized pathway under provider oversight). Keep guide-proposes / node-commits explicit or the safety + defensibility story collapses.

- **(d) CAVEAT (→ §6/§10): the supplement/TRT case is the best DEMO *and* the most competitively-exposed.** Our own bet-memo **§10 concedes** that narrow, protocolizable, cash-pay Rx — *explicitly TRT/peptides* (supplements are the purest case) — is **structurally aggregator-vulnerable** (Hims/Amazon/Fullscript). So winning the supplement/TRT *transaction* is NOT the moat; the moat is the governed **longitudinal relationship + multi-service blend + execution accountability**, with these transactions as the **entry surface**. Do not let the demo be mistaken for the moat.

- **(e) OPEN, not settled:** Knox's "Future 3 (federation bottom-up) is most OMNI-native" is a directional lean on an **unresolved** question — bottom-up vs federation-up vs interfederation + the SEED/wedge are OPEN (`REV-191`; the spine names NO wedge). Reasonable, but a decision to make, not inherit.

**Net:** corroboration + one useful sharpening (signed context packet) + two lines to keep honest (guide-proposes/node-commits; transaction≠moat). No M1–M6 rewrite required.

---

## §9.6 — Trifecta Round 1: Knox review + membrane/radii/naming refinement (2026-07-10)

Knox reviewed this map (summary-level; no repo access) and returned **ACCEPT-with-surgical-patch** (not reject, not rubber-stamp). Opus disposition below. **This section REFINES the wording of §5 (identity), §6 (the decision), §7 (routing), §2 (completeness), and §4 import #7 (proof).** `GRD-042` reinterpretation-expected; names here are **CANDIDATES for the trifecta, not crowned doctrine** (operator: settle before spine, but do not lock prematurely).

### Knox's 5 map-patches — Opus disposition
1. **Do not crown "Subject" as a primitive** → **ACCEPT.** Collides with `trial_subject`/research language. Keep as naming/open-review (see §9.6 naming set).
2. **"un-built CNS framework" is stale** → **ACCEPT w/ precision.** The CNS *orchestration* IS defined (`contracts/CNS_orchestration_contract.md` + C4.1 §1a CNS↔Polaris lock); the older frontier "CNS has no framework" (FWREG-007) is superseded for orchestration. **Reframe (not erase):** the 251 mechanics land in a **CNS EXTENSION — the emission/projection/membrane layer over the existing CNS orchestration contract**, not a from-scratch CNS.
3. **Radius-1 = broad operating awareness via role-shaped projections, not blanket access** → **ACCEPT.** Sub-rule: *awareness is broad by operational need, not curiosity; display role-shaped; drill-down accountable; commit narrow.*
4. **Elevate replayable proof to a candidate spine invariant** → **ACCEPT** (see below).
5. **Qualify completeness as framing-level, not contract/build sufficiency** → **ACCEPT** (§2 already implies; now explicit: *architectural/framing readiness = yes; contract/schema/build sufficiency = downstream, per C3.6/C3.7 style*).

### The corrected boundary model (supersedes §6's flat "3 radii")
The old 3-radius model mixed **awareness scope** with an **economic firewall**. Corrected into three distinct things:

- **(1) Governed Membrane** — the boundary condition itself. **NOT a new domain/primitive that owns anything** — it is the **§A Trust/Authority/Permeability axis operationalized**, composed across Identity · Federation · RBAC · D7 · `source_authority_map` · P4 projections · P35/GCE · owning domains · CNS · Polaris · `trace_lineage` (it "lives around the arrows, not in a box"). Two levels:
  - **Membrane Law** = one constitutional invariant; every crossing obeys it (OMNI-wide coherence).
  - **Governed Crossing** = the computed per-boundary instance; decomposes into 4 facets — **visibility** (what may be seen/assembled) · **capability** (what may be invoked) · **commit** (who may make it true) · **proof** (what must remain replayable). Generalizes the C4.1 formula `f(actor × relationship-to-subject × authority × purpose × jurisdiction/consent/data-segment)`.
- **(2) Awareness Radii** — scopes where the membrane computes differently:
  - **R1 Inner Operator Radius** — broad operating awareness; role-shaped display; accountable (logged) drill-down; narrow commit.
  - **R2 Federated Radius** — named-purpose cross-**OMNI-operator** sharing; consent + grants + proof; **no blanket chart access** (this is where §7.6's restriction correctly lives).
  - **R3 External Exchange Radius** — the non-OMNI world (labs, pharmacies, payers, registries, external AI companions, MCP/A2A/API/FHIR/voice rails): **open rails, closed authority**; external systems propose/transmit/fulfill/process under capability contracts, never become authority.
- **(3) F1 — Economic / Recommendation Firewall** — an **orthogonal LAW, not a radius** (applies at every radius): revenue/retention/accrual/sponsor/payer/operator incentive must not bend patient-facing clinical/research recommendation (C3.7 economically-blind firewall).

**★ The 1,000-node answer (operator question):** there is **no single physical outer wall**. Each node computes its own **Governed Crossings** (local); the network has a federation boundary (R2); "OMNI's membrane" is the shared **Membrane Law**, not a monolith. **Same law, local computation** → operator sovereignty AND network coherence, without a god-domain. The "membrane of the entire app to the outside world" **is R3**, instantiated at every node — **not a 4th radius**.

### Candidate naming set (trifecta-pending; expect iteration)
Aim: Palantir-grade, artful, broad, useful. Top artful frame: **Substrate · Loops · Membrane** (truth / motion / crossing-governance).

| concept | candidate name | note |
|---|---|---|
| the whole boundary law | **Membrane Law** (constitutional) + **Governed Crossing** (computed instance) | not a domain; = §A axis operationalized; 4 facets: visibility/capability/commit/proof |
| neutral human identity base | **`person`** | roles/states = prospect/lead/client/member/patient; generalizes the reusable-identity layer beyond `patient` (Knox proposed `individual_identity`) |
| care-facing role | **`patient`** | keep — a role/state of a `person` |
| pre-account handle layer | **`contact_identity`** | keep (handle≠person) |
| generic focus of an interaction/event | **`subject`** | person·encounter·provider·employee·tenant·document·contract·cohort (matches System Map polaris_event) |
| clinical-trials role | **`study_participant`** | rename from `trial_subject` — frees "subject", modern-preferred term |
| who performs an action | **`actor`** | keep (+ device/robot/external_system) |
| scoped operational relationship | **`patient_relationship`** | keep; unbuilt `REV-144`; neutral generalization `relationship` = open, not crowned |
| relationship stitching | **`relationship_graph`** | P4 projection; connective tissue, NOT truth |
| decompression from a commit | **`operational_emission`** | role-shaped downstream fan-out (Act/Prove-Learn facet); lowest-confidence name |
| replayability invariant | **`replayable_proof`** | candidate spine invariant (below) |

### Replayable proof — elevated (was import #7)
Candidate spine-grade invariant (Prove/Learn loop): *Async care is governable only if its context, candidates, gates, commits, emissions, and follow-up obligations are **replayable from source-authoritative evidence.*** Not ordinary audit logging — **reconstructable care reasoning**. Ties to `trace_lineage` + REV-184 world-model-honesty + the anti-compression rule.

### Do-NOT-promote block (add to §8 guardrails)
- Do NOT promote **`relationship_graph`** as a P1 domain (it's a projection/composition).
- Do NOT crown **`subject`/`person`** as canonical primitives before naming review.
- Do NOT treat **`patient_relationship`** as new doctrine (it exists; it's unbuilt — `REV-144`).
- Do NOT say the CNS lacks a framework (route to **CNS extension**, not from-scratch).
- Do NOT use broad awareness to justify **blanket chart browsing** (R1 = role-shaped + logged + narrow-commit).
- Do NOT collapse **visibility / capability / commit / proof** into one thing.
- Do NOT turn **`operational_emission`** into Messaging-owned truth.
- Do NOT treat ChatGPT-screenshots / external-AI output as authority — evidence/candidates only.

**Round-1 net:** no new scenario batch, no formal arc — surgical refinement, then trifecta-ready. Naming remains open-review pending convergence.

---

## §9.7 — Trifecta Round 2: naming convergence (2026-07-10)

Knox Round 2 accepted the §9.6 structural model and pressured naming; Nick added per-item direction. Opus disposition below = the **refined candidate set with the reasoning preserved** (per operator: do not flatten the naming deliberation). Still trifecta-candidates, not crowned (`GRD-042`). Supersedes the §9.6 naming table where they differ.

| concept | §9.6 name | **converged candidate** | reasoning (why) |
|---|---|---|---|
| top frame | Substrate·Loops·Membrane | **Substrate · Loops · Membrane** | kept; *"Substrate holds truth. Loops move care. Membranes govern crossings."* |
| constitutional boundary | Membrane Law | **Membrane Law** | kept; solves the 1,000-node question (same law, local computation) |
| computed boundary instance | Governed Crossing | **Governed Crossing** | kept ("Governed Exchange" rejected by Nick — "exchange" implies a trade; "crossing" = governed passage) |
| facet 1 (what's seen) | visibility | **Visibility** | over "Exposure": in healthcare "exposure" = breach/pathogen (wrong connotation); Visibility = the governed dimension; "expose" stays the §7.7 verb |
| facet 2 (what's callable) | capability | **Capability** | "Invocation" rejected (dev-jargon, non-Palantir); composes with `capability_envelope`/`capability_contract` + "capabilities act" |
| facet 3 (who makes it true) | commit | **Commit** | over "Commitment": avoid overloading the real `care_commitment` primitive; matches "domains commit" |
| facet 4 (what survives) | proof | **Proof** | kept |
| radii family | Awareness Radii | **Trust Radii** | "Operating Radii" rejected (operating-room collision); "trust" = the actual differentiator across rings + ties §A; crossings fire at each ring's bound. (alt: "Coherence Radii") |
| R1 | Inner Operator Radius | **R1 Inner Operator** | kept |
| R2 | Federated Radius | **R2 Federated** | kept (OMNI-operator ↔ OMNI-operator) |
| R3 | External Exchange Radius | **R3 External Exchange** | kept (non-OMNI world; open rails, closed authority) |
| incentive firewall | Economic/Recommendation Firewall | **Incentive-Blind Recommendation Firewall** | honors C3.7's prior-pass "economically-**blind**" DNA (structural, not a policy stance); broadens economic→incentive (sponsor/payer/operator/platform). *"…incentive may not bend a patient-facing clinical or research recommendation."* |
| neutral human identity | person | **`person`** | base human identity; roles stack on it (below). `person` (identity axis) ≠ `actor` (action axis: person-when-acting OR non-human agent/device/system); orgs = `legal_entity` |
| care role | patient | **`patient`** (role/state) | a role/state of a `person`, not the base |
| pre-account handle | contact_identity | **`contact_identity`** | kept (handle≠person) |
| generic interaction-target | subject | **`focus`** | **Opus deviates from Knox (`subject_ref`) to solve Nick's alias-allergy:** `focus` (FHIR "what this is about") frees the word "subject" |
| clinical-trials role | study_participant | **`subject` / `human_subject`** | keep the **regulatory-native** term so NO alias is needed on IRB/FDA/GCP artifacts (the whole point). ⚑ confirm regulatory fit at C5/counsel |
| performer of action | actor | **`actor`** | kept (+ device/robot/external_system); roles list adds **`caregiver`** (Nick) |
| scoped relationship | patient_relationship | **`patient_relationship`** | kept; unbuilt `REV-144`; neutral generalization `relationship` = open |
| relationship stitching | relationship_graph | **`relationship_graph_projection`** (arch) / "relationship graph" (prose) | `_projection` keeps it P4, never mistaken for P1 truth |
| commit fan-out | operational_emission | **Operational Decompression** | the DECISION is the existing **Governed Resolution** (REV-184, not re-minted); its fan-out = Operational Decompression. Keeps "operational" (Nick); uses the Compression↔Decompression symmetry; rejects "cascade" (implies still-in-motion; the resolution has stopped) |
| replay invariant | replayable_proof | **`replayable_proof`** | kept, elevated to candidate spine invariant |

**★ Identity — two operator questions answered (load-bearing):**
- **One `person` = many concurrent roles + many relationships, across federations.** A person can be lead AND patient AND `subject`(trial) simultaneously, with many `patient_relationship`s across different operators/federations. This is *why* `person` is the neutral base and patient/lead/etc. are non-exclusive per-context roles — a `patient`-as-base model breaks the moment someone is also a lead and a trial subject. (Reusable-identity-vs-scoped-relationship, identity_contract §5; cross-federation = `care_relationship`/`shared_context_grant`, deferred ladder v2/v3.)
- **`person` vs `agent`:** different axes, not opposites. `person` = human identity (who care is about / holds relationships); `actor` = performer (person-when-acting, or non-human `ai_agent`/`device`/`external_system`). AI agent = an actor, never a person.

**The physics line (converged):** *Compression → Governed Resolution → Operational Decompression*, with every **Governed Crossing** computing **Visibility · Capability · Commit · Proof**, across **Trust Radii** (R1 Inner Operator / R2 Federated / R3 External Exchange), under one **Membrane Law**, with the **Incentive-Blind Recommendation Firewall** applying orthogonally at every radius, and the whole path bound by **replayable_proof**.

**Still open (trifecta):** `focus` vs `subject_ref`; `Trust` vs `Coherence` Radii; regulatory confirmation of `subject`/`human_subject` for trials; final crowning deferred until Nick + Knox converge.

---

## §9.8 — Trifecta Round 3: standards grounding + naming corrections (2026-07-10)

Operator pressure: *"what would Palantir / any serious company do here — are we reinventing primitives?"* Answer: for identity, **no — we're implementing a settled pattern and should name it the standard way.** Round-3 corrections + the standard anchors below supersede §9.7 where they differ. Still trifecta-candidates (`GRD-042`).

### ★ Standards grounding (identity is NOT reinvented)
The `person` + roles + relationships model is the classic **Party–Role–Relationship** pattern (Silverston, *Data Model Resource Book*), and its healthcare-native forms:
- **HL7 RIM:** Entity → Role → Participation (an Entity *plays* a Role; a Role *participates* in Acts).
- **FHIR:** **`Person`** links the role-resources **`Patient` / `Practitioner` / `RelatedPerson`(caregiver) / `ResearchSubject`**; `Group` for cohorts.
- **Palantir:** `Person` object type + role/link types.

**Mapping (use these; do not invent):**
| OMNI | standard equivalent |
|---|---|
| `person` (neutral human identity) | FHIR `Person` / RIM `Entity(person)` / Party(person) |
| `patient` (role) | FHIR `Patient` (a role-resource) |
| `caregiver` (role/relationship) | FHIR `RelatedPerson` |
| `provider` (role) | FHIR `Practitioner` |
| `research_subject` (trials role) | FHIR `ResearchSubject` ≡ regulatory `human_subject` (Common Rule / 21 CFR 50.3) |
| `patient_relationship` (scoped operational relationship) | RIM `Role`/`Participation` scoping |
| `legal_entity`/operator (org) | Party(organization) — NOT a `person` (Federation) |
| `subject` / `subject_ref` (generic target of an interaction/event) | FHIR resource `subject` element |
| `actor` (performer) | RIM `Participation`(performer) |

One `person` holds many concurrent roles + relationships (patient at NAKED, caregiver at OMNI-HRT, employee, research_subject, emergency contact) — that's the whole reason `person` sits *beneath* role/relationship, per the pattern. Not an edge case; the standard.

### Naming corrections vs §9.7
| concept | §9.7 | **Round-3 correction** | why |
|---|---|---|---|
| radii family | Trust Radii | **Membrane Radii** (each named by structure) | "Operating Radii" rejected (operating-room collision; "operating workspace" worse); "Trust" too boutique / doesn't scale to hospital/payer/research. Standard analog = trust/security **zones**; healthcare R1 ≈ *circle of care*. Fallback: just "Inner / Federated / External." |
| R1 meaning | small clinic | **within ONE operator's care/ops unit** (a hospital has MANY R1s: floor · service line · command center · trial team) | Knox correction; R1 is not "everyone in the building" |
| incentive firewall | Incentive-Blind Recommendation Firewall | **Care Integrity Firewall** | operator lean; "blind" retires from the *name* (trial blind/unblinding noise) but stays the *mechanism* (C3.7 economically-blind recommendation engine) |
| generic interaction target | `focus` | **`subject` / `subject_ref`** (RETRACT `focus`) | FHIR-native (`subject` element); no invention; matches operator "why not subject_ref" |
| trials role | `subject`/`human_subject` | **`research_subject`** (= FHIR `ResearchSubject` ≡ regulatory `human_subject`) | regulatory-native AND FHIR-native at once → NO alias of our own primitive; patient-facing label = "study participant" |
| post-resolution fan-out | Operational Decompression | **Resolution Output** (RETRACT decompression) | "execution" rejected by operator; keep it plain. A *pattern that produces existing domain objects* (orders/tasks/messages/obligations/records/proof) — NOT a new domain. Palantir-analog = Action effects/writebacks. Decision itself stays **Governed Resolution** (REV-184). |

**Kept from §9.7 (unchanged):** Substrate · Loops · Membrane · Membrane Law · Governed Crossing · Visibility/Capability/Commit/Proof · `person`/`patient`/`contact_identity`/`actor`(+`caregiver`) · `relationship_graph_projection` · `replayable_proof` · Governed Resolution.

### The Round-3 converged set
top frame **Substrate · Loops · Membrane** · **Membrane Law** + **Governed Crossing** (facets **Visibility · Capability · Commit · Proof**) · **Membrane Radii** (R1 Inner Operator · R2 Federated · R3 External Exchange) · **Care Integrity Firewall** (orthogonal) · identity per the Party/RIM/FHIR mapping (**`person` · `patient` · `contact_identity` · `caregiver` · `provider` · `research_subject` · `actor` · `subject`/`subject_ref` · `patient_relationship` · `relationship_graph_projection`**) · **Governed Resolution → Resolution Output** · **replayable_proof**.

**Still open (trifecta):** "Membrane Radii" vs plain Inner/Federated/External; "Care Integrity" vs "Recommendation Integrity"; final regulatory confirm on `research_subject`/`human_subject`; crowning deferred until Nick + Knox converge.

---

## §9.9 — Trifecta Round 4: DE-INVENTION pass (2026-07-10) — Round-4 convergence RECORD (SUPERSEDED by §0.5)

> ★ This is the Round-4 derivation record. **The current settled surface is §0.5** (which incorporates the CLOSE-2 cleanups). Where this section and §0.5 differ, §0.5 wins.

Operator: *"stop being cute; use what Palantir/Epic/standards use; some of these names got worse."* Correct. Round 4 **retires the minted names and maps to what already exists** (OMNI canon + FHIR/Party/Palantir). **This is the current naming state; supersedes §9.6–§9.8 where they differ.** Still trifecta-candidates (`GRD-042`).

### A. Boundary model — RETIRE the invented nouns; the terms already exist
- **KILL:** `Membrane Radii` · `Governed Crossing` · `Boundary Regime` · `Operational Decompression` · `Operational Emission` (all reinventions / trying too hard).
- **Canonical terms already in canon:**
  - **§A Trust/Authority/Permeability axis** = the boundary **law** (runs through every plane).
  - **§7.7** = the **facets**, already named as verbs: *"Projections expose · Capabilities act · Domains commit · Audit proves"* (= visibility/capability/commit/proof — do not re-mint).
  - **§C Governed Capability Exchange (GCE)** = the actual **cross-boundary movement** (ancestry: "Governed Permeability Through Governed Capability Exchange"). Use GCE, not "crossing."
- **"Membrane"** = allowed as a **prose metaphor only**, never a canonical object. Top frame stays **Substrate · Loops · Membrane** as *framing*; the architecture words are §A / §7.7 / §C.

### B. Radii — plain, no adjective
**Inner Radius (R1) · Federated Radius (R2) · External Radius (R3).** R1 = within one organization's care/ops unit (a hospital has many R1s). No "Membrane/Trust/Operating/Awareness" adjective.

### C. Entity ontology — the Party model (person + organization); `operator` is a ROLE, not the base
**Standards-ALIGNED (not standards-identical — the precise OMNI mapping is an OMNI decision).** The **Party–Role** pattern (Silverston) / **HL7 RIM Entity-Role-Participation** / **FHIR `Person` + `Organization`** *validate the separation of neutral identity from role-specific records* — FHIR `Person` is explicitly NOT used as actor/subject/performer (which strengthens our person-vs-actor split); FHIR `Organization` spans corporations/departments/practices/payers. They validate the DIRECTION; they do not dictate our exact person→role / organization→operator relationships.

| concept | term | note |
|---|---|---|
| human base | **`person`** | ~ FHIR `Person` (neutral linkage; not the operational actor/subject) |
| org base | **`organization`** | ~ FHIR `Organization` (the plain, unconfusing base) |
| **operator (a ROLE of `organization`, not a base)** | **`operator`** = the role an organization plays when admitted to run OMNI workflows | (person:patient :: organization:operator) — resolves "operator as main ontology is confusing" |
| **juridical bearer** | **`legal_entity`** | a person OR organization legally capable of bearing rights/duties/contracts/licenses/taxes |
| **legal FORM (type of the legal_entity)** | **`legal_form`** | PLLC / PC / LLC / corporation / nonprofit / government entity / sole-prop — ★ do NOT conflate the entity with its form (Knox catch) |
| admitted/liable party | **`accountable_party`** | the legal_entity (or a person, for a true sole-prop) admitted for an OMNI account |
| grouping / customer | **`enterprise`** | a grouping that may contain many organizations/operators/brands/sites/tenants |
| patient-facing identity | **`brand`** | |
| where care happens | **`site` / `location` / `venue`** | FHIR `Location` |
| software boundary | **`tenant` / `deployment`** | |
| performer of an action | **`actor`** | person-when-acting, or non-human `ai_agent`/`device`/`external_system` |

- Roles stack on the base: **person →** patient · caregiver · provider · employee · research_subject · emergency_contact; **organization →** operator · payer · vendor · lab · pharmacy · sponsor · referrer.
- **Business = an `organization`.** An organization *may itself be* a `legal_entity` or *operate through* one; **`legal_form` classifies the legal_entity** (do NOT say the legal_form *is* the legal_entity). `accountable_party` = the person or legal_entity bearing contractual responsibility for the OMNI account. **Sole-prop:** `person` = juridical/accountable party · `organization` = operating practice identity · `legal_form` = sole_proprietorship · `operator` = role held by the organization — no collapse of human and practice.

### D. Resolution → authorized_action(s) → domain writes (CORRECTED hierarchy; nothing new minted)
**★ Governed Resolution is NOT newly minted — it is `REV-184` (Governed Resolution Lifecycle), CLOSED/signed-off 2026-06-14 (Nick + Knox).** Canonical statement + 7 named lines in `v4_REV184_decision_state_reconciliation.md` §0; its **field-set + lifecycle STATE-MACHINE is already slated for a C5 contract edit.** It has a reconciliation home — do not re-mint. (`governed_resolution.stance` → use REV-184's existing **resolution-state / decision-state** — the lifecycle state: treat/no-treat/defer/monitor/consult/override/no-action-indicated. Do NOT crown "stance.")

**★ `authorized_action` is PRE-EXISTING canon too** — thesis §7 (`authorized_action`: authority verified, scope checked, audit lineage) + §8 flow (`candidate → policy_resolver → authorized_action → execution`) + Surface Map ("Actions = RBAC atoms + domain write-APIs"). The 4-round thrash (emission→decompression→cascade→output→effects) was fumbling a wrapper we do NOT need.

**Corrected hierarchy (operator caught the mis-tree; Knox re-tree adopted):**
```
governed_resolution (REV-184; resolution-state)
  → authorized_action(s)          # the performative command(s) — thesis §7/§8
      → domain writes:            # objects the action commits into owning domains
          care_obligation (§8.6)  ← the defer/monitor/recall/follow-up object
          fulfillment_order (OFC) · outbound_message (Messaging)
          evidence_record (D7)   · billing consequence (D6) · scheduling change (D3)
```
- **`care_obligation` is a domain object an action WRITES — NOT a sibling of `authorized_action`.** (My earlier "authorized_action + care_obligation" phrasing was mis-hierarchied; corrected.)
- **NO wrapper primitive.** A resolution *has* authorized_actions (one-to-many; `authorized_action.resolution_id`). **KILL `resolution_effect` / `resolution_output` / `resolution_action_set` / Operational Decompression / Emission / Cascade** — the relation suffices; no noun.
- **Non-action stays load-bearing** (REV-184 "non-action-as-commit"): a `defer/monitor` resolution-state is clinically non-intervention but the system still takes authorized_actions (write `care_obligation`, send message, record rationale/proof).
- Chain: **resolution = stance/state (REV-184) · authorized_action = performative command · domain write = committed object · commit = owning domain makes truth.**

**`action_type` — NOT minor (Knox right); sharper: it partially exists, scattered.** The typed vocabulary already lives as thesis §7 **`candidate` types** (`prescribe · message · schedule · escalate · order_lab · perform_procedure · block_action · coordination`) + ~80 RBAC **permission_atoms** + domain **write-APIs**. `protocol_definition` (clinical script) and `capability_envelope` (what a model may do) do **NOT** cover it. **C5 verification item:** unify these into one canonical `action_type` / `domain_action_contract` registry (input schema · authority reqs · owning domain · validation · resulting writes · audit · side-effects)? Partial home exists; the unification is the open question.

### E. Recommendation Integrity Firewall
Name: **Recommendation Integrity Firewall** (upgraded from plain "Recommendation Firewall" — operator + Knox converged; "Recommendation" alone risks reading as "makes recommendations better") · doctrine phrase: *recommendation integrity* · mechanism: *economically-blind recommendation/display engine* (C3.7 origin). **Scope = ALL patient-facing clinical AND research recommendations / rankings / displays / next-step guidance** (not studies-only). "Care Integrity" too broad; "Incentive-Blind…" too clunky.

### F. Identity target + trials (unchanged from §9.8)
`subject` / `subject_ref` = generic target (FHIR `subject`); **`research_subject`** = OMNI study-participation role, **mapped to FHIR `ResearchSubject`** and governed by applicable human-subject regulations (mapped, NOT semantically identical in every jurisdiction/workflow); "study participant" = patient-facing label.

### G. Open / flagged (do not thrash)
- **`federation`** name — **CLOSED: keep `federation` as the architecture/domain term** (technically standard = federated-systems; Epic analog = *Care Everywhere network*); **`network` = acceptable product/boardroom translation.** No further naming review.
- **Trifecta CONVERGED (not "deferred to another Nick+Knox naming round").** What remains is promotion through the spine/contract gate (the 4-lane ledger), not more naming.

### H. Convergence + governance + the identity verdict
Operator + Knox converged this round. **"Lock" is a spine/C5 act, not an EVRUN act:** this artifact is `analysis_nonbinding` / propose-only (`GRD-036`). Status = **CONVERGED candidate set, recommended — carried into spine authoring**, crowned only when the spine author runs it through the 4-lane reconciliation ledger + the contracts adopt it.

**★ IDENTITY VERDICT (operator asked to declare stale-vs-solid, with the argument): the prior `identity_contract` is SOLID + DELIBERATELY scoped-narrow — NOT stale, NOT a miss.**
- **Physics right (hard-won):** reusable-identity-vs-scoped-relationship (§5), match-does-not-authorize (§7 inv 2), the `contact_identity → patient → patient_relationship → actor` layering.
- **Narrowness was DELIBERATE + FLAGGED, not overlooked:** §1.5 scope note — *"land the owned-single-deployment (ladder v0) identity spine now; mark cross-namespace/cross-org (`care_relationship`, `shared_context_grant`, patient-as-MPI, federation) OPEN/deferred to ladder v2/v3"* — with `REV-143`/`REV-144` tracking it.
- **Therefore v4 is executing a KNOWN-DEFERRED generalization, not correcting an error:** patient-terminal v0 → the standard **Party/FHIR `person` + `organization`** model, `patient`/`operator` as roles. Every invariant preserved; only the root nouns generalize — on rails the contract itself laid.

**Contract-reconciliation owed at C5 (real edit, not a rename):** `identity_contract` (`patient` reusable-base → a role on `person`); `federation_contract` (`operator` base → a role on `organization`); System Map ID/FED rows updated so patient/operator stay important **roles**, not ontology roots. Governed Resolution's field-set + state-machine (REV-184) also lands here; `action_type` unification verified here.

### ★ The Round-4 converged set (current)
top frame **Substrate · Loops · Membrane** (metaphor) · boundary = **§A axis (law) + §7.7 (expose/act/commit/prove) + §C Governed Capability Exchange (crossing)** · **Inner / Federated / External Radii** · **Recommendation Integrity Firewall** · entities **`person` · `organization`** (bases) with roles [patient · caregiver · provider · employee · research_subject · **operator** · payer · vendor · lab …] · **`legal_entity`** (juridical bearer) + **`legal_form`** (PLLC/PC/LLC/corp/…) + **`accountable_party` · `enterprise` · `brand` · `site`/`location`/`venue` · `tenant` · `actor`** · **`subject`/`subject_ref` · `research_subject`** · **`patient_relationship` · `relationship_graph_projection`** · **`Governed Resolution` → `authorized_action`(s) → owning-domain commits/records/obligations/communications/proof** (NO wrapper noun; `care_obligation` is a domain object an action WRITES, not a sibling) · **`replayable_proof`** · **federation = architecture term** (`network` = product translation). *(★ current settled state = §0.5; this line = Round-4 record.)*

---

## §10 — Disposition + owed governance + next steps

- **Disposition:** **`trifecta_reviewed` · `accepted_as_required_v4_spine_input` · `not_promoted`** (4 rounds converged; `analysis_nonbinding`, `GRD-036` — promotes NO content to doctrine). The **artifact** is now a required input to v4 SPINE DRAFT 0; its **content** is consumed via the 4-lane Constitutional Reconciliation Ledger (each §0.5 disposition-ledger item gets landed/rejected-with-citation at spine acceptance, `GRD-043`).
- **How the spine author uses it:** read **§0.5 SETTLED STATE** — ① AFFIRMED_CANON (do not reopen) · ② CONVERGED_DIRECTION (land unless concrete conflict) · ③ REQUIRED_RECONCILIATION (bounded C5 w/ acceptance criteria) · ④ REJECTED_DO_NOT_REMINT (with reasons). §1–§9.9 = derivation.
- **Governance wired at closeout (edits done; commit pending git-identity):** (a) evidence provenance row **`D0UOR-EVD-002`** in `doctrine/07_evidence_ingestion_ledger.md` ✓ (`-001` was already Kyle/EVSRC-200); (b) read-graph consult route (Tier-2 #11) ✓ **and added to read-graph #9a canonical v4-authoring input set** ✓; (c) lane-index Analysis-runs row (prior) ✓; (d) added to **`v4_C2_source_base_declaration.md`** (the canonical source-base referent) ✓ + a plain reference in the **HOME controlling plan** future-gate/inputs ✓ + a required-input pointer in **`v4_C4_spine_authoring_plan.md`** ✓. Master-corpus-catalog row optional for an evidence-plane artifact (lane-index + evidence-ledger are its provenance home). Only the git commit is pending (identity unset — standing flag).
- **PHI:** the underlying `EVSRC-2026-000251` contains real PHI — de-identify before ANY promotion of specimen content; this crystallization map itself carries only de-identified physics.
- **If analysis grows** (multi-pass trifecta), keep it under this `EVRUN-2026-000004` folder (add the `GRD-044` companion artifacts — concept registry / anchor ledger / coverage — only if the run expands beyond this single map).

---

## §11 — Anchor index (load-bearing citations)

- **EVSRC-2026-000251** `_source.md`: §2B loop (136–1226) · Knox strategic read (1249–1872) · Nick multi-channel (1875) · Care-Interaction-Fabric / "startling thing" (2369–2444) · "boundary is authority" + compression/decompression (2886–3153) · Access/Display/Action/Commit (3013–3047) · membrane-not-wall (3085–3117) · entity taxonomy (3455–3881) · membrane-at-different-radii (3879–3951, 4165–4205) · Palantir/FHIR/Salesforce posture (4222–4644) · Review 003 (4659–4724) · consolidation verdict §G (4715–4722).
- **omni_thesis_v3_integrated_2026-06-05.md:** §7 primitives (1154) · §7.5.1 per-event ownership (1812) · §7.5.3 patient-source (2040) · §7.6 CNS scope + anti-blanket-chart-access (2225, 2330–2368) · §7.7 "Projections expose. Capabilities act. Domains commit. Audit proves." (~2429) + "projection≠authority" (~2411) · §8 two loops (2734) · §8.6 Act loop (2897) · §A trust/authority/permeability (2923) · §B AI substrate (2985).
- **omni_thesis_v3_integrated_spine.md:** 4-lane ledger (114–171) · three-envelope lock (126–127) · no-god-domain (164–165) · anti-compression rule (267) · §0 source stack (17).
- **contracts/identity_contract.md:** four-layer model §4 · reusable/scoped §5 · admission guardrail §6 · two-gate inv §7(2) · `REV-144` §11.
- **OMNI_System_Map_vNext.md:** ID row (61) · FED row (71).
- **v4_C3_8_G4:** tenant-ownership §2.2 · data-economy 5 loops §3 · 18-yo TRT worked example + Tesla parallel §4. **G2:** translation map (44–63) · ownership answers (67–81).
- **EVSRC-2026-000247** (Palantir) Review 003: K2 ontology=IP-boundary · K3 model+app+compute · K4 model-agnostic · lock-in-vs-Dario.
- **C3.5:** G4 "relationship design not nouns" · 5C "See≠act≠sign≠release" + actor map · F2 minimum-necessary + common-operating-picture. **C3.7:** G governed_decision_lifecycle + economically-blind firewall.
- **ORIENTATION-2026-06-10:** v4=execution physics (94–95) · four loops (163–209) · core law (1213) · Foundry (5732–5746). **cns_and_knowledge_reservoirs_frontier:** CNS-no-framework (16–18) · law (55). **EVRUN-000001 §2A:** authority/commit = IBM delta (71).
- **v4_C4:** bet-adjudication Verdict B (151) + 5-face membrane law (35) · watch-list WI1–WI13.

---

## §12 — Change log
- `2026-07-09` — `EVRUN-2026-000004` opened (per `EVSRC-2026-000251` §4 disposition + lane-index anticipation). This `00_run_and_crystallization_map` authored (Opus grounded agent): cross-arc reconciliation of the 251 mechanics (M1–M6) against the full accepted estate; import list (§4); Relationship/Subject verdict (§5); broad-awareness 3-radius decision (§6); routing (§7); guardrails (§8); crown-jewels inventory (§9). `analysis_nonbinding`, propose-only. Lane-index Analysis-runs row added. Owed evidence-ledger/catalog/read-graph rows flagged for post-Knox. **NEXT = trifecta review (Nick → Knox → Opus).**
- `2026-07-11` (CLOSE-3) — **Final editorial cleanup (Knox 3 residuals) + source-list verification.** (1) §5 retitled to DERIVATION + banner points to §0.5; "Net" line (a) neutral-Subject-naming → **person/organization contract generalization** (a bounded C5 item, not an open gate). (2) §0.5 ③ action-type: "resolution/**emission**/execution/commit" → "candidate/resolution/authorization/execution/domain commit" (retired "emission"). (3) §9.9 retitled "Round-4 convergence RECORD (SUPERSEDED by §0.5)" + residual "federation-name OPEN" → "federation = architecture term". **Fixed my own §10 over-claim:** added the required-input pointer to **`v4_C4_spine_authoring_plan.md` §1** (it was NOT actually there). **Mechanical verification (grep `EVRUN-2026-000004`):** read-graph ✓ · C2 ✓ · HOME controlling plan ✓ · evidence-ledger ✓ · spine-authoring plan ✓ (now). Trifecta CLOSED — remaining work = spine disposition ledger + bounded C5 reconciliations. Git commit pending (identity unset).
- `2026-07-11` (CLOSE-2) — **Closeout cleanup (Knox 10-point consistency pass + operator: confirm source-list inclusion).** Rewrote §6 (radii = plain Inner/Federated/External; Recommendation Integrity Firewall = orthogonal law, not "Radius 3"; Radius-1 full rule w/ purpose/sensitive-category qualifier; dropped §9.6 pointer). Rewrote §7 (killed "un-built CNS framework is the home" + retired-name usage → DISTRIBUTED landing: Identity/Federation · source domains · CNS-assembles · P4 projection · §A/§7.7/§C · owning-domain commits). Patched §3.0 to current vocabulary (person/organization · subject_ref · §A/§7.7/§C · distributed landing — not "Governed Membrane"/"focus"). §0.5: moved "no wrapper" out of ① AFFIRMED_CANON (it's a ④ rejection, not pre-existing canon) + added "domains own committed truth"; closed Federation (architecture term; `network`=product translation); frozen hierarchy marked CONCEPTUAL (not schema cardinality). §9.9: corrected entity prose (`legal_entity` = juridical bearer; `legal_form` classifies it — not "legal_form is the legal_entity"); `ResearchSubject ≡ human_subject` → **mapped-to** (not identical); closed the Federation "name open" + removed "final crowning deferred". §10 ID fix `D0UOR-EVD-001`→`-002`. **★ Wired into the source list (operator-flagged miss):** read-graph **#9a** input set (item 13) + Tier-2 #11 + **C2 `v4_C2_source_base_declaration.md`** (Cat 6) + **HOME controlling plan** + evidence-ledger `D0UOR-EVD-002` + lane index. Only the git commit is pending (identity unset).
- `2026-07-11` (CLOSE) — **Trifecta CLOSEOUT (Knox Pro-mode review + operator go).** Status → **`trifecta_reviewed` / `accepted_as_required_v4_spine_input` / `not_promoted`.** Added **§0.5 SETTLED STATE** = the handoff surface (WHY-THIS-LANDS: reduced-not-expanded ontology, no new foundational primitives + genuine discoveries; frozen hierarchy + already-canon invariant; **4-status disposition ledger** ① AFFIRMED_CANON ② CONVERGED_DIRECTION ③ REQUIRED_RECONCILIATION ④ REJECTED_DO_NOT_REMINT; surfaced the buried substance — Radius-1 broad-awareness, comm-event fabric, relationship_graph_projection, Recommendation Integrity Firewall, replayable_proof acceptance test). **Fixes (Knox catches):** §9.9 converged-line contradiction removed (no `+ care_obligation(s)` sibling, no `resolution_effect` wrapper); operative body banners (§3/§4) so retired names don't read as live; **`legal_entity` vs `legal_form`** category error corrected; FHIR claim softened to standards-ALIGNED-not-identical; action hierarchy = lock the invariant (already §A/§7.7 canon), granularity = REQUIRED_RECONCILIATION with named C5 questions. **My pushbacks (not just agreeing):** OMNI already HAS an ontology (contracts/§7/System-Map) — this refined its nouns; the "invariant to lock" is pre-existing canon; ontology-first spine RE-ORDERING = a C4 spine-shape input, not a closeout decision. **Governance wired:** `D0UOR-EVD-002` evidence row + read-graph Tier-2 #11 route + lane-index row (prior). Owed closeout-commit: read-graph #9a input-set pointer (git identity unset).
- `2026-07-11` (later) — **§9.9 D + H sharpened (operator concerns 1–3 + action_type).** (1) Governed Resolution anchored to **REV-184** (closed/signed-off; state-machine already C5-slated) — NOT newly minted; "stance" → REV-184 resolution-state (don't crown). (2) **NO wrapper** — killed `resolution_effect`/`resolution_output`/`resolution_action_set`; a resolution *has* authorized_actions (relation, not noun). (3) **Corrected hierarchy** — `care_obligation` is a domain object an `authorized_action` WRITES, not a sibling (operator caught the mis-tree). `action_type` = NOT minor; partially exists as §7 candidate-types + RBAC atoms + domain write-APIs; C5 unification question (protocol_definition/capability_envelope do NOT cover it). **Identity verdict declared:** prior contract SOLID + deliberately-scoped-narrow (§1.5 scope note + REV-143/144) — v4 executes a known-deferred generalization to Party/FHIR person+organization, not a correction.
- `2026-07-11` — **§9.9 Round-4 CLOSE (operator + Knox converged) refinements.** (D) corrected: `authorized_action` is PRE-EXISTING canon (thesis §7/§8) — the fan-out was never a new primitive; fan-out = `authorized_action` + `care_obligation` (+ records/messages/proof); non-action case load-bearing (REV-184); `resolution_effect` = optional technical wrapper only; verify `action_type` vs existing `protocol_definition`. (E) **Recommendation Firewall → Recommendation Integrity Firewall.** (H) added convergence+governance note: "lock" = spine/C5 act not EVRUN act (`GRD-036`); contract-reconciliation owed at C5 (identity_contract patient→role-on-person; federation operator→role-on-organization). Converged-set line updated.
- `2026-07-10` (later 3) — **§9.9 Trifecta Round 4 (DE-INVENTION pass) folded in — CURRENT naming state.** Operator: stop inventing, use standards. Retired the minted nouns (Membrane Radii · Governed Crossing · Boundary Regime · Operational Decompression/Emission) → mapped to existing canon (§A axis = boundary law · §7.7 expose/act/commit/prove = facets · §C Governed Capability Exchange = crossing). Radii → plain Inner/Federated/External. **Entity ontology = Party model:** bases **`person` + `organization`** (FHIR); **`operator` DEMOTED to a role of `organization`** (resolves operator-confusion); + `legal_entity`/`accountable_party`/`enterprise`/`brand`/`site`/`tenant`/`actor`. Resolution → `resolution_effect`(s) → `authorized_action` (Palantir Action-Type convention; Governed Resolution unchanged). **Recommendation Firewall** (scope = clinical AND research). `federation` name flagged OPEN (lower-priority; Epic analog Care-Everywhere-network). Supersedes §9.6–§9.8 naming where differing.
- `2026-07-10` (later 2) — **§9.8 Trifecta Round 3 folded in** (operator "what would Palantir/standard do?"). Added **standards grounding**: identity = Party–Role–Relationship / HL7 RIM Entity-Role-Participation / FHIR Person+Patient/RelatedPerson/ResearchSubject (NOT reinvented; explicit mapping table). Naming corrections: **Trust Radii → Membrane Radii** (R1 = within one operator's care/ops unit; hospital has many R1s); **firewall → Care Integrity Firewall**; **retract `focus` → `subject`/`subject_ref`** (FHIR-native generic); **trials → `research_subject`** (= FHIR ResearchSubject ≡ regulatory human_subject; no alias); **retract Operational Decompression → Resolution Output** (plain; pattern over existing domain objects; "execution" rejected by operator; decision stays Governed Resolution REV-184). Kept the rest. Names remain candidates; `GRD-042`; `analysis_nonbinding`.
- `2026-07-10` (later) — **§3.0 reasoning layer + §9.7 Trifecta Round 2 (naming convergence) folded in** (operator-directed). §3.0 = the "why we landed here" argument for the core verdicts (anti-flatten; derivation kept, not just verdicts). §9.7 = converged candidate naming set with per-name reasoning: facets **Visibility·Capability·Commit·Proof**; **Trust Radii** (R1/R2/R3); **Incentive-Blind Recommendation Firewall**; identity **person / patient(role) / contact_identity / focus(generic target) / subject·human_subject(trials, regulatory-native, no-alias) / actor(+caregiver)**; **Operational Decompression** (replacing operational_emission; decision = existing Governed Resolution REV-184); `relationship_graph_projection`; `replayable_proof` kept. Answered: one person = many concurrent roles + relationships across federations; person(identity)≠actor(action). Names remain trifecta-candidates (not crowned); open: focus-vs-subject_ref, Trust-vs-Coherence radii, regulatory confirm. `GRD-042`; still `analysis_nonbinding`.
- `2026-07-10` — **§9.6 Trifecta Round 1 folded in** (operator-directed): Knox review = ACCEPT-with-surgical-patch. Disposition: accept 4 patches + refine 1 (CNS = extension, not un-built). Corrected boundary model = **Governed Membrane (Membrane Law + Governed Crossing, 4 facets) + Awareness Radii R1/R2/R3 + F1 economic firewall (orthogonal)**; 1,000-node = "same law, local computation" (no monolithic outer wall; external membrane = R3 per-node). Candidate naming set (Substrate·Loops·Membrane; person/subject/study_participant; Membrane Law/Governed Crossing; relationship_graph; operational_emission; replayable_proof) — trifecta-pending, not crowned. `replayable_proof` elevated to candidate spine invariant. Do-not-promote block added. Refinement pointers added to §5/§6/§7. `GRD-042` reinterpretation-expected; still `analysis_nonbinding`, propose-only.
- `2026-07-09` (later) — **§9.5 Addendum folded in** (operator-directed): review of a same-day Nick↔Knox federation/supplement exchange. Verdict = ~85% corroboration of ratified posture (C3.8 Verdict B / WI2/WI3 / §A/§C), not new intel; kept (a) supplement = 2nd acceptance-test specimen (strengthens M6 proof=replay), (b) "signed context packet" candidate name for the portable-context composition (dedup-flagged to §C + `knowledge_interchange_format` + `shared_context_grant`; sharpens M2 + import #3/#7), (c) guardrail guide-proposes/node-commits (→ §8), (d) caveat transaction≠moat / low-dim aggregator-vulnerability (→ §6/§10), (e) federation-topology/wedge remains OPEN (`REV-191`). No M1–M6 rewrite.
