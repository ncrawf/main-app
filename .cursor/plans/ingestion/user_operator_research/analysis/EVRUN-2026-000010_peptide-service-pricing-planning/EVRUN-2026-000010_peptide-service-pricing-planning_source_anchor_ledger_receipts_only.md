# EVRUN-2026-000010 — Source Anchor Ledger (RECEIPTS ONLY · SKELETON)

Document type: `evidence_or_ingestion` (Evidence Plane · run receipts) · Authority: `analysis_nonbinding` (`GRD-036`). **Receipts only — NEVER author from this file (`GRD-044`).** Meaning = source packet + convergence, not these quotes.
Run: `EVRUN-2026-000010` · **STATUS: `skeleton_awaiting_capture`.** De-id tokens: `N`=provider/operator · `[CO]`=co-owner (Olivia) · `[RXREP]`=pharmacy rep (Caleb) · `chat`=Knox/ChatGPT if used (`captured_interpretation_nonbinding`). Verbatim ≤~15 words. Raw immutable per `GRD-042`/commercial flag.

## A — `EVSRC-2026-000281` anchors (populate after capture)
| anchor id | channel | verbatim (de-identified) | supports finding |
|---|---|---|---|
| _(pending capture)_ | | | |

## B — Estate cross-reference anchors (pointers, not new claims)
| anchor id | canon | used to establish |
|---|---|---|
| CANON-0.5 | `EVRUN-000004 §0.5` settled hierarchy + retired terms | naming floor; no re-mint |
| CANON-DTP | `EVRUN-000007` concept registry (`DTP-*`/`STH-*`) | reuse-before-mint for repeated physics |

> **Anti-flatten (`GRD-044`):** preserve keeper lines verbatim, not paraphrased abstractions.
