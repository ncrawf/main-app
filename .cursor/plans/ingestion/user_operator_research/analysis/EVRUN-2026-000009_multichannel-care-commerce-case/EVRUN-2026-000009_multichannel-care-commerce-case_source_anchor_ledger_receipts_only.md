# EVRUN-2026-000009 — Source Anchor Ledger (RECEIPTS ONLY · SKELETON)

Document type: `evidence_or_ingestion` (Evidence Plane · run receipts) · Authority: `analysis_nonbinding` (`GRD-036`). **Receipts only — NEVER author from this file (`GRD-044`).** The workbench is the concept registry; meaning = source packet + convergence, not these quotes.
Run: `EVRUN-2026-000009` · **STATUS: `skeleton_awaiting_capture`.** De-id tokens: `N`=provider/operator · `[PT]`=patient · `[CO]`=co-owner (Olivia) · `[RXREP]`=pharmacy rep (Caleb) · `chat`=Knox/ChatGPT (`captured_interpretation_nonbinding`). Verbatim ≤~15 words. Raw immutable per `GRD-042`/PHI+commercial flag.

## A — `EVSRC-2026-000280` anchors (populate after capture)
| anchor id | channel | verbatim (de-identified) | supports finding |
|---|---|---|---|
| _(pending capture)_ | | | |

## B — Estate cross-reference anchors (canon this run reconciles to — pointers, not new claims)
| anchor id | canon | used to establish |
|---|---|---|
| CANON-0.5 | `EVRUN-000004 §0.5` settled hierarchy + retired terms | naming floor; no re-mint |
| CANON-DTP | `EVRUN-000007` concept registry (`DTP-01…23` + `STH-01…03`) | reuse-before-mint for repeated physics |

> **Anti-flatten (`GRD-044` 15-min-agent test):** preserve keeper lines verbatim, not paraphrased abstractions.
