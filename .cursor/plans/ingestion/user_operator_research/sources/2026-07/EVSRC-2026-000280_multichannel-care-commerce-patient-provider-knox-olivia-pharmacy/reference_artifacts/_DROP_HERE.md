# DROP HERE — reference artifacts: price list · formulary list · Autopilot pricing (REF)

Drop the **commerce/catalog reference material** referenced during the case here. Group by type (subfolders or filename prefixes):
- **`price_list_*`** — the retail/service price list page(s).
- **`formulary_*`** — the compounding formulary list (e.g. Cache Valley formulary).
- **`autopilot_*`** — screenshots of the **"Autopilot" pricing platform** (TK confirm exact product — appears to be a compounding-pharmacy pricing/ordering platform).

- **Raw, immutable source** (`GRD-040`/`GRD-042`) — do not edit images.
- ⚑ Potentially commercially-sensitive vendor pricing/formulary — mask vendor identity on promotion.
- These are the **off-platform commerce/catalog truth** being worked out; describe each in `_source.md` §2REF + add §1 inventory rows. Candidate governed home = D6 Commerce / product-catalog / formulary object (§3).
