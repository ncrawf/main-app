# DROP HERE — provider ↔ patient SMS/text follow-up (channel A)

Drop the raw screenshots of the **SMS/iMessage follow-up thread with the patient (Dan H., TRT)** into this folder.

- Keep them sequential (e.g. `IMG_####.png` or `sms_01.png…`).
- These are **raw, immutable source** (`GRD-040`/`GRD-042`) — do not edit images.
- ⚑ PHI/PII HIGH — identifiers visible in-image (name, phone, labs). De-identification happens in the `_source.md` §2A text, NOT on the raw files.
- After dropping, synthesize/transcribe into `_source.md` §2A (de-identified: `[PT]` / `N`).
