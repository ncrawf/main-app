# DROP HERE — provider ↔ Knox/ChatGPT parallel AI loop (channel C) — TWO THREADS

There were **two separate ChatGPT threads** in this case (operator, 2026-07-16). Keep them in their own subfolders — do NOT mix:

- **`thread_1_clomiphene/`** — thread 1 (topic: **clomiphene** / clomiphene citrate; confirm vs enclomiphene). → transcribes to `_source.md` §2C-1.
- **`thread_2/`** — thread 2 (topic: **TK — Nick to specify**; rename the folder to `thread_2_<topic>` once known). → transcribes to `_source.md` §2C-2.

Conventions (both threads):
- Sequential + batch-labeled per thread (e.g. `gpt_c1_01.png`… for thread 1, `gpt_c2_01.png`… for thread 2). Suggested: `gpt_cN_NN_<short-desc>.png`.
- **Raw, immutable source** (`GRD-040`/`GRD-042`) — do not edit images.
- ⚑ Anything pasted INTO the AI (formulary, price list, Autopilot screens, margin math, co-owner/vendor messages) captured here = **channel-C payload** even if its content originated elsewhere (`DTP-12` — note it, do not re-file by content).
- Authority layer = `captured_interpretation_nonbinding` — a Knox/ChatGPT statement is NEVER an OMNI finding.
- After dropping, transcribe into the matching `_source.md` §2C-1 / §2C-2 block + add §1 inventory rows.
