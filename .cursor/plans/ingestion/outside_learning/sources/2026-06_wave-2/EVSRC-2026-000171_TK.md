# EVSRC-2026-000171 — <title or TK>

Document type: `source_capture` · Lane / reservoir: `outside_learning`
**Status:** `raw_dropped` → `normalized` → `reviewed` → `analyzed` → `promoted | deferred`  ·  **current: `raw_dropped`**
Authority: `evidence_nonbinding` — captured source + interpretations only; binds nothing until routed + promoted.

> **HOW TO USE THIS FILE — two kinds of marks:**
> • `TK` = a field the normalizer (Opus) fills from the screenshot — **leave it alone.**
> • **⬇️ PASTE blocks = the ONLY spots Nick touches.** Exactly **3**: transcript (§1), **Knox read (§3 Review 001)**, gut note (§3 Review 002, optional). Plus: **drop the screenshot in chat** → Opus fills §0.
> Copy this into `sources/<YYYY-MM>/`, rename `EVSRC-2026-000171_TK.md` (id = highest EVSRC + 1), add a row to `../../00_index.md`.
>
> **Per-source flow (the standard):** Nick pastes transcript (§1) + Knox's strategic read (§3 **Review 001** — paste it AS-IS, however rich Knox makes it; do NOT reformat into fields) + optional gut note (§3 Review 002). Then the agent (Opus) writes **§3 Review 003 — formal deep extraction** (formalizing Review 001, NOT rediscovering it), updates the run's **concept registry** (cross-source synthesis), the coverage matrix, and fills **§4 pointers** at closeout. **The per-source deep read lives HERE in §3 — never in a separate sidecar/extraction file** (`GRD-044`). Cross-source synthesis lives in the EVRUN concept registry.

## §0 — Source identity / metadata  *(normalizer fills from the screenshot — leave `TK`)*
- evsrc_id: `EVSRC-2026-000171`  ·  filename: `EVSRC-2026-000171_TK.md`
- source_platform: `YouTube`  ·  source_url: `TK`  ·  source_title: `TK`
- channel_or_org: `TK`  ·  speaker: `TK`  ·  published_at: `TK`
- captured_at: `2026-06-11`  ·  captured_by: `Nick`  ·  capture_method: `transcript paste + screenshot`
- content_type: `TK`  ·  source_reliability_context: `academic | practitioner | vendor | founder | investor | security-researcher | unknown`  ·  topic_tags_light: `[TK]`

## §0.1 — People / authorship / authority context  *(normalizer fills from screenshot + description + transcript — this is what makes a source a reservoir, not a transcript dump)*
- primary speaker(s):
  - name: `TK` · role_in_source: `TK` (speaker / presenter / interviewee / host / author) · affiliation_at_publication: `TK` · speaker_type: `TK` (founder | researcher | investor | operator | vendor | clinician | regulator | journalist | educator | other) · authority_context: `TK` · identity_confidence: `TK` (high_from_screenshot | inferred | unknown)
  - *(add a bullet per additional speaker)*
- publisher / channel: `TK`  ·  interviewer / moderator / host: `TK`
- event_context: `TK`  ·  perspective / conflict notes: `TK`

> **Authority is descriptive, not worship** (`GRD-039`): record *who* said it (raises relevance), but every claim still routes through evidence → interpretation → gated promotion. (Clinical reservoir extends this block heavily — authors / affiliations / journal / DOI-PMID / study-type / evidence-level / COI / retraction — `FWREG-006`.)

## §0.5 — Processing checklist
**Nick drops:** [ ] transcript → §1 · [ ] screenshot in chat · [ ] **Knox strategic read → §3 Review 001** (paste as-is) · [ ] (optional) gut note → §3 Review 002
**Agent (Opus) does:** [ ] id+filename · [ ] §0 metadata from screenshot · [ ] takes labeled · [ ] **§3 Review 003 formal deep extraction** (formalize Review 001) · [ ] update EVRUN concept registry (cross-source) · [ ] update coverage matrix · [ ] **fill §4 pointers (closeout)** · [ ] NO sidecar extraction file (`GRD-044`)

## §1 — Verbatim transcript  ·  layer: `raw_source`  ·  IMMUTABLE

⬇️⬇️⬇️  PASTE THE FULL TRANSCRIPT BELOW  (do not summarize)  ⬇️⬇️⬇️

Transcript


Search in video
0:00
0:05
So last lecture, Percy covered some of the underlying mechanics, let's call it, of parallelism.
0:11
Today, I'm going to do my favorite thing of talk about all sorts of things you can know, like knowledge,
0:17
details, and trivia about how modern parallelism for language model training works.
0:25
And what we're going to try to do is really get to grips with the many, many complexities that
0:30
are involved when you are having to train huge models on huge clusters. It's going to turn out that there
0:36
are many different parallelization strategies that you can use at once.
0:41
At the end, we will get to a slide where I say 4D parallelism, because there's four different things that you can do at once.
0:47
Actually, even more that I'll talk about. And it turns out that to parallelize at the largest scale, you'll need to use all of them or most of them.
0:56
And we'll discuss why. Part of your assignment will be figuring out, given a particular network topology and a particular model,
1:05
what is the optimal parallelization strategy for that? And then finally, I'll walk through some examples
1:10
of recent large scale training runs with details that are published and show you this stuff in action.
1:18
OK, so the very first part, I'll go through somewhat quickly. It's partially a review of basic collectives, basic networking
1:25
primitives, right. But the reason why we need all of this parallelism stuff
1:30
is because there's going to be two bottlenecks that we'll solve by going to multiple machines and multiple GPUs.
1:36
So one of it is compute. We need more compute than we can bring to bear on a single chip. And the way we solve that is just to have many, many machines
1:45
and link them up together. Right, the fastest supercomputers in the world have exaflops of compute, whereas a single GPU
1:52
is nowhere near that number. The other reason why we need to continue to scale in parallel
1:58
is because of memory, right. Models are big. We can't fit them all into one GPU, so we need to shard them into smaller pieces somehow.
2:06
So this is going to lead to complexities of having many GPUs across many different machines. And an important conceptual object
2:13
that we will deal with throughout our lecture today is the difference between intranode parallelism,
2:19
where your connections are very fast, and so you can do things that are very communication expensive.
2:25
And you're going to have slower internode communication. And because these are slow, we will have to use processes that respect the communication
2:33
constraints of our channel more, right. So we're going to have to do both of those.
2:38
And remember all of the things that we will do today. We're not actually going to be sending packets or anything
2:44
like that. We're going to be dealing with this at the level of collective communication primitives. Right, so whenever we do accounting
2:51
for how much communication is done, I might say, OK, we're going to have to do all-reduce or we're going to have to do allgather to implement
2:58
some of these operations. So in order to implement some of these efficiently,
3:03
you're going to have to go all the way to the hardware level. But our discussion today is going to be algorithmic. And so all of the networking primitives I'll talk about
3:10
is going to be at this level of a collective. And remember, Percy talked about this,
3:15
but this is important for-- very important for one part of the talk, which is going to be that the equivalence
3:21
between all-reduce and reduce-scatter plus all-gather. Right, so there's going to be one algorithm where on one hand,
3:27
you do an all-reduce, and on the other hand, you do two of these different steps. And the fact that these two have the same cost in some sense
3:35
means that we can do the algorithm on the right for free. We'll see that again later. This is just a quick refresher just
3:41
to make sure you remember what Percy said. OK, so that's it for the really basic review.
3:48
The other thing I want to touch on briefly before we get into the algorithms, which are going to be hardware and substrate agnostic,
3:55
is the difference-- oh, sorry, yes? Is there any particular reason why you are saying that old
4:00
[INAUDIBLE] reduce scatter and-- OK, the question was, is there any reason why I'm talking
4:06
about this specifically? We will-- [INAUDIBLE] Yes, yes, there are other decompositions.
4:13
This one in particular will be useful for a algorithm, which is why I'm emphasizing this particular decomposition. That's right. There's others that you could do, of course.
4:19
Yes. OK. So the other thing I will talk about before I start--
4:25
most of our discussion today will be hardware agnostic. For the most part, once you understand the basic concepts,
4:30
you'll be able to apply it to any hardware substrate. But I do want to briefly before we start,
4:36
just talk about the low level hardware stack. Both because it's interesting. But also because I think it leads
4:43
to different parallelization strategies across different model training companies.
4:48
So remember the TPU. Right, TPU is Google's accelerator. And I mentioned during the GPU lecture,
4:54
a TPU is like a lightweight GPU. But what's different is the networking. So how is the networking different?
5:00
Well, in one slide, the TPU network is what's called a toroidal mesh.
5:06
Right, so you can think about a grid and then allowing the ends of the grid to communicate with each other.
5:11
It's a little bit more complicated. It's a 3D picture. Which, Marcel, you have the visualization, right?
5:18
Can you post that in the lecture channel? Yeah. So there's a fun web visualization that you can look
5:24
at that visualizes the toroidal mesh in 3D, which is very cool. But your mental model, I think, can roughly be this picture.
5:30
You can forget the more complex one. All the chips are networked to neighbors and the neighbors wrap around, right.
5:35
So what does this let you do? This lets you have a very simple networking topology that can be indefinitely scaled up very
5:41
simply, right. The number of neighbors you have is the same, no matter how large your network gets.
5:48
In contrast, the GPU networking philosophy is very different. It's really much more of an all to all philosophy.
5:54
And the way that it's networked is it's like a tree. It's often called a fat tree. You have your GPUs connected very,
6:01
very quickly at the lowest layers. And then you have groups of GPUs that are connected in a pod. And then the pods might have spine switches
6:07
that allow them to communicate to each other. And this means that as the number of nodes grows,
6:13
the tree gets bigger and bigger. Your communication costs is going to get higher, or your communication topology will get more complicated,
6:21
because you're connecting all these all to all as you go. Now, this difference in philosophy
6:27
means that if you're communicating only to neighbors, TPUs are great. They're cost effective.
6:32
And you can make each connection more beefy, right, with the same power consumption. But if you're connecting in very random ways, very stochastic,
6:40
unpredictable ways, then this GPU networking will be more flexible, right. So there's this very different design philosophy
6:46
that you see between TPUs and GPUs. And I'll try to mention where this shows up
6:51
as we go through the parallelism lecture. And I posted this into the lecture chat last lecture.
6:58
Because someone asked, what's the difference between these two and so on? Right, but just to back up my point, Bill Daley and Jeff Dean
7:05
were talking about the different hardware and what are they good for, right. GPUs are good for things like mixture of experts, where
7:12
the communication is much less clear because tokens route to different experts. TPUs are great if you have this dense model
7:19
that you're doing very predictable partitions of. I'll talk about that in an idea called tensor parallelism later
7:27
in this lecture. And so these hardware things are good for different workloads. And this was where I was going to leave it for the lecture
7:35
until this morning, when Google decided that they were going to announce TPU8i and t this morning.
7:42
And then I looked at this as I was preparing my lecture, and I said, oh my god, TPu8i? That's a tree topology.
7:48
They've switched to a much more all to all connection. And if you think about it, it makes sense.
7:54
Modern day language models are MoEs. And if you're going to serve a MoE, you're going to have all sorts of bandwidth
8:00
just flying around as you route tokens to different experts. You probably do want much more of an all to all
8:07
connectivity where, at inference time, these communications are real bottlenecks. You want to deal with them.
8:14
And even at training time, the TPU8t, which is a training chip, the cross rack connectivity looks a lot more like a GPU.
8:22
There's much more like an all to all style switch that's being built out called the Virgo network.
8:28
That's a higher level networking stack. So I think there's really interesting things happening.
8:34
It's a little bit of a convergent evolution across both TPUs and GPUs, where really the workloads are
8:40
defining the network that we need to have. OK. And then the final thing about hardware--
8:47
I just love talking about what's happening in the Chinese AI scene-- is this idea of--
8:53
I think in the GPU lecture, someone asked, if SRAM is so good, why don't you just all use SRAM?
8:59
Only SRAM chip. And that's Grok, right? It works really well. Now, some of you might be thinking,
9:04
if all to all connectivity is good, why don't you just connect everything and connect them with really fast fiber
9:10
optic chips, right? And then you would get the Huawei Ascend 910, right?
9:15
So if you look at the specs of the Huawei Ascend, it's actually really interesting, because each chip is a lot worse than a H200.
9:23
It's a lot slower at matmuls. It's slower in many ways. But then, if you're willing to dedicate a giant rack of fiber
9:32
optic switches to connect a much larger number of chips, like in their case, 384 in a big rack,
9:39
then maybe you can deal with the fact that each chip is slower by just connecting a much, much larger
9:45
number of these guys, right. And then so I think what this shows us is a really interesting tradeoff.
9:52
Because once you look at this architecture, you realize the power consumption of this thing is insanely high.
9:58
It's four times that of the equivalent NVIDIA system. And so it's something like, well,
10:03
if you're willing to pay the power cost, you can solve a lot of communication problems
10:09
brute force, and you can scale out much more aggressively to 300 chips. But you're paying a power cost, right.
10:14
It's similar to that SRAM story of, if you want to design a hardware that is efficient to use both power and manufacturing wise,
10:21
you end up in a certain place. If you want to brute force things, you end up in a very different place. Right, so this is, I think, just a very interesting place
10:29
to learn from about these hardware design tradeoffs. OK, good. So now, just to put this all together,
10:37
the first thing you should think about is the new unit of compute is not the GPU. It's the entire data center.
10:43
And we want to have several things. We want to be able to control the amount of memory we use. We want to be able to control the amount of compute
10:50
that we can bring to bear. And we want all of these to be lossless. Right, we want to use all of the resources that we have.
10:57
And then we're going to talk about the algorithms that will enable this through all these communication
11:03
primitives components. OK, good. So now the second part of this lecture
11:10
is going to be the bulk of the content. And these are the algorithms that we will use to paralyze language models.
11:18
And there's going to be a couple of different important ideas. Actually, we should probably maybe even be grouped.
11:25
Data parallel is one set of ideas for essentially taking your data that you're going to train on
11:30
and distributing them across GPUs. So the data is the ones that move around. And then model parallelism, you're
11:36
going to chop up different parts of the model and split them around. Turns out that the boundary between these two
11:41
is a little leaky, because one of the algorithms here will actually cut parameters up, too. But we will see why I've put all of these algorithms
11:47
into these different bins, right. And conceptually, we're trying to both scale
11:53
out compute and memory, and that's why we will need a whole bunch of these different tricks.
11:59
Cool. All right. So data parallelism, I think, is the easiest to understand. It is conceptually very simple, and I
12:05
think it's the standard way that you would try to parallelize an algorithm like LLM training.
12:11
So forget that we're doing Adam. We're going to do just naive SGD for the moment. Then we have a capital B sized batch, right.
12:20
So each update step, I'm going to take capital B elements, and I'm going to sum up the gradients.
12:25
And I'll take my update, right. So the most naive form of parallelism that one can do for this is to take my batch,
12:31
B. I can cut it up across M machines. If they're divisible, each machine will get a smaller B over M sized batch.
12:39
And then what I'm going to do is each machine computes gradients, and then I will synchronize all the gradients to get my sum.
12:46
OK, great. So compute scaling wise, this is perfect. As long as there's enough examples per GPU, we're good.
12:52
Communications overhead. How much do we have to talk to each other? Well, every batch, we have to basically communicate
12:59
two times the number of parameters, right. Because what I have to do is I have to basically communicate
13:05
my gradients back and forth. OK, and then for memory scaling, there's absolutely none.
13:10
Every GPU has the same copies of the models. You need to have the same sized activations
13:16
so you don't save any memory at all in this scheme. So now what is the problem?
13:23
Clearly we've addressed compute to some extent. But we have not addressed memory at all. So let's take a look at this memory problem
13:30
and just do a little bit of accounting. Initially, it might seem that the memory situation is bad.
13:36
But it turns out our memory situation is actually just terrible. And the reason why the memory situation is terrible
13:43
is if you just do some parameter accounting, right, it just turns out you need to store a lot of stuff.
13:49
Right, and not only do we need to store a lot of stuff, it's actually more than the parameters that we need to store.
13:55
So roughly the rule of thumb-- it depends on your precision. But the rule of thumb, you might say,
14:00
is that we need something like five copies of weights and 16 bytes per parameter to store our model.
14:06
So we will need to, of course, store our model parameters. We need a place to put our gradients, right.
14:13
The accumulator before we put them into the model parameters. So we need to store those guys.
14:18
But actually, you need to maybe store more stuff. You might need to have a higher precision accumulator that you
14:24
accumulate into in SGD. This might be temporary, but you might need this. And if you're doing Adam, and this is really
14:31
where the bad stuff comes in, you're going to need to track both the first and second moments of your gradients.
14:36
And you need to track them over time, right. So you need two of these. And unfortunately, you might need
14:42
to store them in high precision depending on your stability characteristics. And these might be quite expensive, right.
14:48
And we generally call these things optimizer state. Right, the thing that you're accumulating into.
14:54
The first moment, the second moment. Right, and these are actually quite expensive. If you look at the accounting, this is most of the cost memory wise of doing an SGD update.
15:04
And so I think one of the really elegant and nice things
15:09
that you can do in this space is to start cutting up the amount of memory you need
15:15
and distributing them across the accelerators, but doing so in this structured way that gets you
15:20
these gains for basically free. So as I said in the last slide, the optimizer state
15:26
is most of your memory. So here, the optimizer state is green. That's most of your memory.
15:31
Orange is your gradients, and blue is your parameters. And notice how parameters and gradients are the same size,
15:37
and your state is bigger, right. If you do naive data parallel, you're just going to replicate all of this state across the GPUs.
15:44
And this is not so good, right. Because you've, in some sense, consumed way more memory than you need and your memory consumption
15:51
is linear with your number of accelerators. Not so good, right. What we can do instead is perhaps
15:58
we can shard the optimizer state onto different GPUs. And this would be a big savings.
16:04
If we shard different other parts of what we need to track, that would be also big savings.
16:09
Maybe we can also shard the gradients into different GPUs. Or in the extreme, maybe we can shard everything
16:15
into different accelerators. And then you can see that the implied memory gains on the very right would be quite dramatic as we go all
16:22
the way down from 120 to 1.9. Now, the main thing that we will study now
16:29
is, what is the cost of doing this? Right, you might imagine, there's no free lunch. Baseline is going to be the fastest.
16:35
And as we go downwards to here, we're going to have to pay a big communication cost in order to achieve these memory savings.
16:41
I think the remarkable thing is going to be that a lot of this will be free.
16:46
But I'm going to walk through this step by step. So let's start with what people call ZeRO stage 1.
16:51
So there's going to be three stages corresponding to each of these steps. And so stage 1 is going to be the simplest.
16:57
The only thing I will shard is my optimizer state. And I will shard it across the GPUs, OK.
17:03
Everyone is going to have both the parameters and the gradients. And so what you can think of is each worker
17:09
is going to be responsible for updating one slice of the parameter. So if I'm GPU number 0, my responsibility
17:15
is updating this very first slice over here. And I'm not responsible for any of the other updates.
17:23
So how would it work? Right, so what we're going to do is everyone computes. Everyone has a different data item, right.
17:29
So they're going to compute a full gradient on their data item. That's the first step. Now I'm going to take the gradient I computed
17:36
and I'm going to reduce scatter the gradients onto all the other machines. Because everyone else needs my gradients, right?
17:41
But they don't need all the gradients. They only need the gradients associated with their update.
17:46
Right, so every worker is going to get from me, my gradient, but they're part of the parameter space, right.
17:53
So this is going to be equivalent to a number of parameters communication costs. Because I'm sending my gradient, but I'm cutting it up
17:59
and sending it into each GPU. Now each machine is going to be able to now do their update.
18:04
They have their parameters, they have their gradients that they got from everybody else, and they have their own state that they've tracked, right.
18:10
And now, the parameters have been updated. And now I'm going to all gather them back. Right, I was responsible for, let's
18:17
say, one fourth of the parameters. So those updated parameters, I'm going to send back to everybody else, right.
18:22
So this is a all-gather, right. So this is where the equivalence is useful.
18:28
How many operations did I do? Well, in the naive data parallel case, what I had to do
18:35
was I had to do one all-reduce. Right, I had to exchange all the gradients with everybody and then do the updates, right.
18:40
So the communication cost is 2 times the parameters. Now in ZeRO stage 1, I did two operations.
18:47
One was a reduce-scatter to send the gradients out and then an all-gather to collect the updated parameters.
18:52
But we already said a reduce-scatter and an all-gather is equivalent to an all-reduce. So ZeRO stage 1 is going to have the exact same communication
19:00
characteristics as naive DDP. Right, so this was free. Free memory savings. Wonderful, right?
19:06
And we look at the memory. We've basically taken the parameters. And anything that's an optimizer state, I've divided by n GPUs,
19:12
right. Wonderful, wonderful, wonderful. Now, can we do more? Well ZeRO stage 2 is now going to say,
19:19
I'm going to split the gradients up. And then I'm going to shard that as well.
19:25
Now, this is going to be trickier because before I was relying on the fact that I can compute the entire gradient,
19:31
now I can't even materialize the whole gradient. So what do I do. Well, this turns out to just be a systems trick.
19:38
As I sweep backwards through my network, I don't need to compute the full gradient. I can send out the gradients to the associated workers as I go.
19:47
So the key step here is now, instead of materializing the gradient vector all at once, I'm
19:53
going to walk backwards through the compute graph. And after computing a layer's gradients, I'm going to immediately just reduce
19:59
that to send that to the right worker, right. And once gradients aren't needed in the backwards graph, just immediately free it.
20:05
Right, so you're now incrementally computing and sending gradients as you go. And as you can see, doing it incrementally or doing it
20:12
all at once, same thing, right? So it doesn't really make a difference. So now you can, at the end, once again all gather the parameters.
20:19
I've started the gradients in some sense without incurring any additional costs, right.
20:25
OK, so the last and the most hairy thing-- and this always, when I first learned about this, seemed like total magic--
20:31
is we've gotten something for free so far, so let's keep pushing it. I'm going to shard the parameters as well all the way
20:39
across all the states. And so we're going to use the same kind of idea that we used in ZeRO stage 2.
20:45
And the idea, remember, was I'm going to do things incrementally, right. And so what I'm going to do is I'm
20:50
going to send and receive parameters on demand while stepping through the compute graph.
20:55
And this is really tricky, because each GPU only sees a slice of both the parameters, gradients,
21:01
and optimizer states at any one time, right. And this is ZeRO stage 3, also known as FSDP.
21:07
If you've ever parallelized anything in a Torch library, you've probably dealt with this before.
21:13
So the way that it works under the hood is what you're going to do is you're going to do two all-gathers and one reduce-scatter as you go.
21:22
So we're going to step from left to right here. And this is going to be the entire update process.
21:28
So what you do is you're going to load your model. You have your two GPUs at the top and the bottom.
21:34
You're first going to gather your weights for this one layer. I'm going to forward this one layer.
21:40
And then I can free my weights, because I've already done the forward, right. I don't need these parameters anymore. Now, let's say I only have one layer.
21:47
Now I can move to the backwards process. Now to do the backwards step, I need the activations, which I've kept.
21:53
And I need the parameters for this layer, which I can all-gather on demand. I all-gather it, I do the backwards.
21:59
And then I scatter the gradients back right when I get them. Right, so I got the gradients. I reduce-scatter the gradients out.
22:04
And then I free the weights. I don't need them anymore, and I repeat this process, right. So I grab the parameters as I need them.
22:10
I compute my backwards. I send the gradients out, and I repeat this process. And then I update my weights.
22:16
All done, right. So what's the communication cost? Right, how many all-gathers did we do?
22:21
We did two all-gathers, one reduce-scatter. Also, so we have one extra all-gather, which seems bad.
22:30
That's one thing. The second thing is this seems a little crazy in terms of the overhead, because we're doing communication
22:35
all the time. Every layer, we're doing this communication. You might think that this is extremely expensive and bad.
22:43
But I think one of the things that's counterintuitive and important is
22:49
ZeRO stage 3 uses two really important ideas to make this essentially overhead free.
22:56
So the first one, which we've already seen, is this idea that we're going to sweep through the compute graph, and then we're going to do the necessary communication
23:03
and then immediately free the memory. Right, that's already an idea we've experienced. The other idea, which I haven't talked about yet,
23:10
is we're going to overlap the communication and computation cost of this process, right.
23:16
And this is hard to explain initially. So I think I've taken this plot from this paper, which
23:22
I think was explaining the PyTorch FSDP architecture. So the way that this works is in order-- so we have different
23:32
streams here. This is what's happening in the CPU. This is what's happening in the GPU for computation, and then what's happening in the GPU for communication
23:39
at the very bottom here. Now, the way FSDP works is I have to first get my parameters
23:44
before I can do my computation. So I'm going to all-gather 0, layer 0, right. And once I've all-gathered, I can now do my forward.
23:51
But as I'm doing my forward, notice that I'm requesting the next layer's parameters. I'm all gathering 1 while I'm doing this computation.
23:59
And now I all-gathered 1, I can now do forward 1, right. And I can start all-gathering 2 while I'm starting this process.
24:06
And so I'm overlapping computation and communication as I'm doing this.
24:11
And I might spend some time freeing my parameters. I might also spend some time computing another forward.
24:17
For example, if you reuse that W nought, I'm reusing this forward 0 twice.
24:24
And so what this allows you to do is if you have enough computation and your network's fast enough, by overlapping communication and computation,
24:31
right. FSDP can basically be free. Right, you do see some bubbles. These are overheads.
24:36
But if your comms is very fast and your computation is very big, right, you can easily see how the computation can run faster than the communication--
24:44
I'm sorry, the computation can take longer than the communication, and you end up having this really great improvement in memory usage
24:52
without paying for almost any amount of memory use. OK, so when we look at the different ZeRO stages,
25:01
right, DDP, the naive data parallel, costs 2 times parameters. Right, it was a single all-reduce.
25:09
The two ZeRO stages, 1 and 2, right, the ones that shard gradient and optimizer state, this is free in the literal sense,
25:15
because it has the same amount of communication cost, right. It's just the identity of all-reduce
25:21
being equivalent to two operations, right. Now, ZeRO stage 3 is technically a little bit more
25:28
communications. You need an extra all-gather, right. But it's not so bad because of the really clever ways
25:33
in which all these libraries can hide the communication cost underneath the computation. So in practice, FSDP, if you run it,
25:40
you're going to see GPU utilization that's very close to just the single GPU performance.
25:46
It is actually quite remarkable how good FSDP is. And it's also very conceptually simple.
25:51
You will actually have to write an FSDP implementation as part of your assignment.
25:57
And you can just write a wrapper that will wrap any module and turn it into its FSDP version.
26:02
Right, conceptually, all you're going to do is do a bunch of all-gathers, compute, free, and then repeat
26:07
on the backwards pass. OK, good.
26:12
Yes? [INAUDIBLE] --gradients from the next GPU to calculate the gradients
26:19
for the previous GPU, right? When you say-- so we're not--
26:25
I think what you just described, which is taking gradients from one GPU to the next, that's closer to pipelining.
26:30
Pipelining would be if one layer was on one GPU and another layer was on another GPU. That's not the case. So in all these cases, every GPU goes through the entire model,
26:42
right, from the start to finish. But the difference is, no GPU is going to hold the entire parameters at the same time.
26:49
And so what we're going to do is if I only have part of the network that doesn't have layer 0,
26:54
I'm going to demand the layer 0 parameters first and then do the full computation. And then I'll move on to the next layer, right.
27:01
So it's the same computation. It's the same single GPU computation as naive data parallel. But you can just think of it as request parameters,
27:08
free parameters in between every computational operation. Yes? So are you saying that every GPU has a part of every layer,
27:15
and that's what the all-gather's for? Yes, that's right. So the picture is here, right. Parameters, gradients, and optimizer states
27:20
are all sharded across the different GPUs. That's right. Yeah, good. Oh, yes. Why does the communication cost not
27:28
multiply by the number of layers as well?
27:34
So the number of operations multiplies by layer. So that's correct. But each of these communication pieces is smaller, right.
27:41
Because this is just one tiny MLP or something. And I'm comparing that to the cost of all-reducing a whole network.
27:47
Right, so it all adds up in some sense. Yeah, good. All right.
27:53
OK, good. So in practice, if you look at the maximum size that can fit on, let's say, a A100 GPU, if you go from the baseline,
28:04
you can not even fit a 7B model. And you go to ZeRO stage 3. You can now fit 50 billion parameter models and so on,
28:11
right. So it becomes a lot easier to fit these big models using FSDP.
28:17
Right, the improvements that you get in terms of the memory improvement
28:22
is quite significant. OK, yes. So can you go back to the previous slide?
28:29
So regarding this on 8x1 A100, what would be the limitation
28:36
on H100 or P200? OK, I can't do the mental math of multiply by 141
28:43
and divide by 80, but that would be the number for an H200. And so on and so forth. Yeah, it would be linear, right.
28:49
It wouldn't really change the math very much, though, to go from one accelerator to up. OK, so FSDP, I think, is very clean.
28:56
It's very elegant. I like it a lot. I wish I could end the lecture here. But unfortunately, we will have to proceed into uglier
29:04
and more hairy things. And there's two reasons why we have to do that. One of them, and this is an important idea
29:10
for parallelization, is that in some sense, data parallel is consuming an important resource.
29:15
And that resource is the batch size, so to speak, right. If you have a batch size of 8, you
29:20
can never have more than eight accelerators, right. And you might think, OK, let's just make
29:26
the batch size bigger and bigger and bigger as we have accelerators. You can't do that, because at a certain point,
29:33
there's something called the critical batch size, where the basically gain that you get from the additional batch
29:39
element is less than if you had taken another SGD step on that single element, right. So there comes a point where at small batch sizes,
29:46
adding extra elements is perfectly helpful. It's the same as taking more SGD steps. But at a certain point, diminishing return hits in.
29:53
An infinitely large batch size is not infinitely better than infinite steps, right. Infinite single steps.
29:59
Because of this, now we have faced a hard tradeoff. Do we want small batch sizes and let our GPUs be more idle?
30:06
Do we want big batch sizes and take the hit from optimization? Hard to say, rigt.
30:11
There's another issue that we have, which is that we still have memory issues, which is ZeRO stages 1 and 2 don't really
30:19
let you scale memory at all. And ZeRO stage 2 lets you cut up the parameter memory.
30:25
But unfortunately, there's activation memory and other kinds of memory use that this does not split up
30:31
or this does not reduce at all, right. And so we need better ways, more fine grained ways, of cutting up the model in order to really start
30:38
pushing down the memory use beyond these points, right. And so this takes us to various model parallelism ideas.
30:48
And so model parallelism is going to be this idea of essentially like FSDP,
30:53
we're going to split up the parameters across GPUs. But I think the important conceptual difference
30:59
between what we talked about and what we will talk about next is that now we are going to communicate activations.
31:05
Right, in FSDP, we cut up the parameters. But in some sense, it was just a wrapper. We were still doing the normal computation.
31:12
We were just sending parameters back and forth before doing it. Right, so parameters were the things flying around.
31:17
Now what we're going to do instead is we're going to communicate activations back and forth, right. So if, for example, one layer lives on GPU 0,
31:24
the next one lives on GPU 1. Right, what I have to communicate is the activation of the layer in between.
31:29
Right, so that's going to be the big difference. And this will involve pipeline parallel, which is cutting up
31:36
layers, tensor parallel, which is cutting up matrices, and expert parallel, which is sharding experts
31:41
into different places. Right, and Percy's talked about tensor parallel already, so you have roughly some mental model of this.
31:49
I'll start by talking about pipeline parallel or layer wise parallel.
31:54
This is, I think, conceptually very simple. Back in the days when I first learned about parallelism,
31:59
I was like, ah, yes, it's simple. Just cut up the layers and put them on different GPUs. Right, very simple thing.
32:05
And so you're going to pass activations forward in the forward pass, and then you're going to pass like partial gradients
32:10
backwards in the backward pass. Now, OK, if I do this, what will happen?
32:15
You will get a terrible looking picture like this. Very, very depressing. I have four accelerators, each handling a quarter of the model.
32:23
And so what computation happens as a function of time. So time is sweeping from left to right. And the different accelerators are the different rows.
32:30
So first, my zeroth accelerator at the bottom here, that's handling the first layer will do some computation.
32:37
And then it will stop, right. And then it will hand off the computation to the second one. And then it will do nothing for the rest of time
32:43
until the very end. And then I will basically be passing things back and forth. And one GPU will be active at a time.
32:49
And then coming backwards, one GPU will be active handling the backward pass at a time.
32:54
And so most of the time, my GPUs are idle. My utilization is truly, truly terrible.
33:00
And this is called a bubble. It's the part of the pipeline where you're not doing anything.
33:06
And the solution to this is to essentially do a batching or pipelining.
33:11
So instead of doing a simple one layer after another, you're going to have a pipeline, right. Where, in this case, in this picture here,
33:18
you have four elements to be processed. And as soon as you process one, you start on the next element,
33:24
and then you pass off the one that's already done to your next layer. Right, so you can do this to essentially pass elements
33:30
upwards and then back downwards. And so if you want to make this reasonable, then what you need
33:37
is the ratio of essentially this bubble time to the useful compute. The utilization, in some sense, is
33:43
going to be the number of stages divided by this micro-batches. And so we need a huge batch size in order
33:50
to reduce this bubble towards 0. Right, so if we want our bubble to be 0, it's going to go down as 1 over the microbatch size.
33:59
This is why I said batch sizes are useful resource. Because now we can spend it in a different way, which
34:05
is to pipeline more to reduce the amount of idle time in pipeline parallel.
34:13
OK, so pipelines are terrible. And by folklore, people say things like, parallelization code looks reasonable
34:20
and people can understand it until you implement pipeline parallel. So why do we do pipeline parallel?
34:25
Well, pipelines can save memory compared to DDP. Because we've cut up layers, and you can also compose this with data parallel.
34:31
So you can do both. But the other reason why this is a very important idea to learn,
34:37
and you will always have a pipeline in certain kinds of topologies, is that it has very good communication properties compared to both FSDP and compared to any other kinds
34:45
of things you can do. It only depends on activations, like b times s times h.
34:51
It is also point to point. One layer is going to communicate to another. It's not all to all. And so depending on how you sequence these things,
34:58
they can be a very efficient way to utilize your network. So in practice, what we're going to do
35:03
is that pipelines are going to live on the slowest networking links. So if you have multiple data centers or multiple pods that
35:11
are slower to talk to, you're going to parallelize across those two using pipelined parallel, because that's the most communication
35:17
efficient parallelism that you can bring to bear here.
35:22
OK. And as I said, pipeline performance is really dependent on batch size.
35:28
So I'll point out this paper repeatedly, but I'll mention which one this is. Now this is the Megatron paper from NVIDIA.
35:35
They have a bunch of really lovely parameter sweeps of what happens to utilization as a function
35:41
of different configurations you can do. So if you have large batch sizes and you pick a large pipeline
35:48
size, you can get pretty good utilization close to not doing pipeline parallel at all.
35:54
But you need the big batch sizes in order to be able to keep utilization up. Otherwise, pipeline parallel is going
35:59
to rapidly degrade your utilization and performance. Actually, I'm going to pause for a moment
36:05
here to ask questions, because the next slide is complicated. Yes? So you mentioned on the previous slide
36:12
that pipelines have good communication properties, like the FSPD. What is the specific mechanism for why that is?
36:19
It's a smaller amount of stuff that's being communicated. So it's batch times sequence length times hidden.
36:25
And that's almost always a smaller amount of data than attempting to communicate a whole parameter matrix, right.
36:32
So people have tried to figure out
36:38
much more clever ways of reducing the amount of pipeline sizes.
36:43
And you can do clever things that cut up the different stages of what
36:48
you're doing for different layers or different micro-batches. So you can sequence different forward pass elements
36:55
in between different backwards pass elements. And by doing clever scheduling, you can further reduce the bubble size.
37:01
This was taken from the DeepSeek paper. So by really cleverly manipulating when you're doing forward and backward computation,
37:07
you can get a little bit better in terms of the bubble size.
37:13
But I think the even more clever thing that you can do, which is called zero bubble pipelining,
37:19
is not really about scheduling per se. But it's actually about thinking carefully
37:25
about the structure of your backward pass. So in the backward pass, there's really
37:30
two things that have to happen. If you think about it, you're going backwards in your computation graph.
37:35
And as you do that, you're going to do two things. One is going to be you're going to propagate your partial derivatives further down your computation graph.
37:42
So remember, back propagation, you start at the end, and you're propagating these partial derivatives back, right.
37:47
So you have to do that. But you also want to compute the gradients for the current weight. Right, so you have two things that you
37:53
do at every node of your computation graph. You're going to propagate backwards, and you're going to compute the derivative of your weight.
38:00
The important thing about thinking about this compute graph is actually, one of these two
38:06
is really important for pipelines. And the other one, you can do whenever.
38:11
Propagating the partials backwards, right, that's very important. Because your next stage can't do any work until you've propagated
38:17
that signal back. On the other hand, the derivative with respect to the weights, that is a leaf node, so to speak, on this graph.
38:23
And I can do it whenever, right. And so the clever thing to do now is you separate these two.
38:29
Right, so you got your B's over here. These B's are the backwards, propagating backwards.
38:35
And then these W's, these W's are computing the derivative of the weights. And they can happen whenever.
38:40
So what you do is you compute the B's as quickly as you can. Right, so those come first.
38:45
And then you can defer the W's until later and do it when you have a gap in your computation. And when you do this, you can basically almost all completely
38:53
fill up the pipeline. And the zero bubble pipelining thing is very complicated or much more complicated
39:01
than you would normally like to deal with. But it allows you to almost deal with these pipelining issues almost entirely, depending on essentially the workload
39:08
involved in the W versus the B, and so on and so forth. But I think having seen this, I was like, oh, this is so clever.
39:16
Such clever things that you can do with systems. OK, so that was pipelining.
39:22
Any questions about that? Good. So I think there's a natural analogy
39:30
or a parallel you can think of. So pipeline parallel is like depth. So your depth, you cut up your network
39:36
along the depth dimension. But we have two ways that we scale, right. We've got a depth axis and we have a width axis.
39:43
So what if we start cutting up our model along width, right? So this is tensor parallel, as Percy explained before.
39:50
And it's really the simple idea that matrix multiplies can be cut up into smaller matrix multiplies, which
39:56
you then add the partial sums back together. This is really the same idea as tiling as well. So this core primitive just appears many, many times.
40:07
And so when you do this in practice, right, what does this actually look like? So this is a GeLU that's happening.
40:14
So what do you normally do? You have your x coming in as input on the very right of this slide here.
40:20
And then I have a matrix A that I multiply with. I apply my non-linearity GeLU. And then I multiply with B to downproject.
40:28
And then I output my z. Right, this is the normal serial computation that I do. Now in tensor parallel, what I would do
40:34
is I would cut out my A into two submatrices, my B into two submatrices, and then I would run the computations
40:41
in parallel, and I would gather them together at the end. Now, one thing that is important is
40:48
there's a duality between the forward and backward pass. So in the forward pass, all I do with the x is I copy them twice,
40:56
and then I run the computation forward. So f is the identity, this f function. And then once I get to the end of my parallel component,
41:03
the g function, that's an all-reduce that brings them back together, right. But in the backwards pass, as I'm doing my gradients,
41:10
this is going to flip. My g is going to be on-- sorry, my g is going to be an identity. Right, because the partial derivatives
41:15
are coming backwards. And then my f are the partials that I need to sum up in order to get my partial derivatives
41:23
at the start of this module. Right, so they're going to flip. And this duality is important if you're
41:28
going to write tensor parallel. OK.
41:33
And so you might have already noticed, right. But when we split things, some of the ways
41:39
that we are going to be column-wise. So column-wise, tensor parallel is going to happen at the inputs.
41:44
Right, so the inputs of the MLP, the projections of the attention are going to be cut up by the columns in each transformer
41:52
block. So this is column-wise cuts. And then there's going to be row-wise cuts
41:57
in the corresponding second stage. So in the downprojections of the MLP,
42:03
as well as the outputs of the attention, you're going to have a row-wise cut. And then everything that's a small layer, like a layer
42:09
norm or a non-linearity or routers in ML-- sorry, in MoEs, those are all fully replicated
42:15
across the machines. You don't really want to bother with the overhead of cutting those guys.
42:22
OK. And then, so when do we do tensor parallel? Tensor parallel is extremely computation hungry.
42:31
What you're doing is every time you have a matmul, you're going to be doing some communication. Right, you have an all-reduce.
42:37
Or in the backwards pass, you have all-reduce here. One in the G when you're going forward.
42:44
And these are activation size, and you're doing them very frequently. So this is very communication hungry.
42:50
And so you only want to do this within a node. So for GPUs, GPUs are going to be networked tightly together
42:58
in a single box. And so up to eight, you might be willing to do some tensor parallel.
43:03
Once you go beyond that, you're going to be going cross node. And those interconnects, those connections, are much slower.
43:10
And so you're going to get a big drop in performance as soon as you hit that point of going beyond a single machine.
43:16
So tensor parallel, you should remember as very communication hungry. You usually use it in the fastest interconnects
43:23
that you have at hand. And this is the point at which I'm
43:28
going to go back to the thing I said about TPUs. Right, remember for TPUs, you don't really have this distinction between fast eight GPUs
43:36
and next machines. You just have this big mesh. And so one of the big advantages that TPU people will
43:41
tell you is you can tensor parallel very large numbers compared to the GPU world, because they're
43:47
able to have high bandwidth in this very regular communication pattern over this mesh.
43:53
Right, so that's a big difference if you're parallelizing between TPUs versus GPUs. The amount of tensor parallelism you use versus pipeline parallel
44:00
will actually be quite different. So how do we compare these two. They're both model splitting strategies.
44:07
They'll both save on both parameters and potentially activations.
44:12
The pros here of tensor parallel is that there's no necessarily pipeline bubble. And if your network's fast enough,
44:19
you might be able to get full utilization. It's also low complexity. It's just cutting up some matmuls.
44:25
But the con is that the communication is much larger. It used to be that you were doing b times s times h
44:31
point to point communication. Now instead, I'm going to be doing something like a times b times s times h times roughly one all-reduce communication,
44:40
right. So this is not even just point to point. This is all to all communication that needs to happen.
44:46
So tensor parallel is great. Whenever we have high speed interconnects, every other time, we probably want to be using pipeline parallel.
44:53
OK. So the last thing I want to talk about about-- we're talking about memory use, is to really think
45:03
more carefully about memory. I think the most naive view of memory in deep learning and language modeling is something like, well, memory
45:10
is just parameters. That's this green bit over here. Right, you have a certain number of bits you need to store for your parameters.
45:16
That's it. I've already talked about optimizer state, which is important, which is this yellow box over here.
45:23
But if you look at this plot, this is actual memory use as profiled in one of the PyTorch profilers.
45:30
You'll find actually that this is not really the whole story. There's a lot of dynamic memory that needs
45:35
to exist in a computation. You need to store all the activations. These are these big red bumps over here.
45:41
And then as you compute your gradients, you need to store them in your backward sweep, right. And so really maximum memory usage
45:48
happens a little bit after the maximum activation point. After you start sweeping backwards on your gradient,
45:55
but you still need to compute a lot-- keep your activations, those are usually the maximum memory points, right.
46:00
So to reduce this, maybe we need to deal with this big red hump, this activation.
46:06
And if you start looking at more modern workloads for language
46:12
models, you start to find that as you get to bigger and bigger parameters-- forget the column that says present work.
46:17
I'll talk about that in a moment here. But if you look at just the baseline columns of this plot, as you go to bigger models, for moderately large sequence
46:24
lengths, your activations are just going to dwarf the parameter memory that you need.
46:30
And so any memory saving strategy has to reason about activations in order to be fully effective.
46:37
We can't just deal with parameter memory. That's not really sufficient for us.
46:44
So if we're storing everything-- and this is a good, I don't know, statistic or rule of thumb to start with.
46:51
If we need to store everything, the amount of activations that we need is going to be something like 34 times s times b times h,
47:00
and that's going to be sequence length times batch size times hidden dimension size plus 5 times as over h.
47:08
as is attention heads times sequence length divided by hidden dimension size.
47:14
And this sbh dependence, right, is fundamental, because we expect to have to store something
47:20
for every element of the sequence. We expect to have to store something for every batch element, and we expect to have to store something
47:26
for every hidden dim size. Right, so we're going to see this sbh term repeatedly as we try to go through different ways of reducing
47:33
activation memory and accounting for the total amount of activation memory. And in case you're curious where the terms come from,
47:39
this odd looking term, the 5 as over h is going to come from the quadratic attention terms,
47:44
including the dropout terms. And we can drop this term via recomputation if we do flash attention.
47:49
OK. Now what if we did tensor parallel?
47:55
One of the promises that I made to you in talking about model parallelism was that we could reduce activation quite a bit.
48:03
So now let's think about what happens. Well, tensor parallel is going to split out the matrix multiplies in attention and MLPs.
48:10
So which parts are those? Well, the MLPs are 24 of those 34 that I talked about before.
48:18
And the attention heads are 5 as over ht. Right, the second term over here.
48:24
Now, I can reduce that by t, my tensor parallel size. And that's good. This is not bad.
48:30
Because I've taken most of the memory, and I've made it scale linearly with the tensor parallel size t.
48:36
So that's good. But it turns out that there are other things that you do.
48:42
You might have a layer norm. You might have a dropout. You need to keep the inputs to attention and the MLP.
48:47
Right, because the inputs to each layers need to be stored as residuals for the backwards pass.
48:53
And unfortunately, these are not reduced with your tensor parallel size. Right, tensor parallel does not split the layer norm.
49:00
And therefore, it does not split the activation for these guys. So this is unfortunate, because you were hoping that if you had
49:06
1,000 different GPUs in tensor parallel because you're Google, you might be able to reduce this activation dramatically.
49:12
The unfortunate reality is that you're still going to suffer a 10 times sbh penalty for doing all of this.
49:20
And so the last part that's usually used in composition with tensor parallel
49:25
is this idea of sequence parallel. This is an extremely misleading name, because usually when I say sequence parallel, actually
49:33
there's a thing called context parallel that is actually more natural for this name. But sequence parallel is the following idea.
49:40
So what you're going to do is we have these remaining terms, 10 sbh.
49:45
And these terms are very lightweight. They're like layer norms. They're stuff that doesn't have much computation.
49:52
And what I'm going to do is I'm going to split them up somehow. And I'm going to split them up over the sequence axis
49:57
rather than over the hidden axis. And this is going to then involve
50:04
all-gathers and reduce-scatters before every operation where we need these.
50:10
And if you think about it, this is very reminiscent of FSDP. Right, we have this thing that we need. We need attention-- sorry, we need activations at some point.
50:18
We need to compute the activations going forward, and we need the activations when we sweep backwards for the derivatives, right.
50:23
But we don't need them now. So we're going to store them by splitting them up across the sequence axis and materializing them on demand.
50:31
So I would say this is conceptually very similar to the FSDP style idea, where we're storing them
50:37
in this sharded format, and then we're gathering them as we need them. And then as I said before, there's a duality to this.
50:44
So in the forward pass, g is an all-gather. g bar is a reduce-scatter. In the backwards pass, this is going to be reversed.
50:51
Right, the all-gather and reduce-scatter are going to be reversed between g and g bar.
50:57
So if I do this now, what do I get? Well, remember, with the no parallelism setting,
51:03
the very first part, what I told you was you get this activation memory. Use 34 times sbh with an attention head component.
51:11
With tensor parallel, I get to divide the MLP part and the attention part by t,
51:17
but not all these other pointwise ops that I have. If I do sequence parallel, I've split these
51:23
up by my accelerators as well, just in the sequence axis, which gives me now fully linear dependence.
51:30
If I do activation recomputation, I can even drop this second term. Because remember, this is just the storage
51:37
that I need for the softmax. So I can drop this through recomputation, which gives you sbh times 34 over t.
51:44
This is nice to remember, because this is the lower bound of what you can achieve, reasonably speaking, for normal training for activation memory.
51:53
And so if you want to do things like compute by hand whether your model will fit into a GPU, this is a good thing to remember.
51:59
You need at least this much for activation memory. You're going to need some memory for your optimizer state,
52:04
and you will need some memory for your parameters and your gradient. And once you have those things, you have a rough sense of how much memory
52:11
you're going to need for your model. OK, I'm going to stop here for a moment. I have talked through both pipeline and tensor
52:19
parallel at this point, and so people might be a little bit confused. And we can talk through a few things before I move on to expert parallel.
52:26
Good.
52:31
Oh, yes? [INAUDIBLE] 24 over t is the MLP part.
52:38
The MLP part cannot be optimized through the representation?
52:44
You can recompute the MLP as well. You can do a lot more computation than what is listed here.
52:50
But recomputation what is listed here is very computationally expensive, like a recomputation for MLP involves running the MLP again
52:58
in the backward pass, which you probably do not want to do.
53:04
Recomputation for attention is generally cheaper, yeah. Because you do it time-wise as well.
53:09
And also you don't want to pay the quadratic cost. That's quite expensive. The quadratic cost.
53:15
That's quite painful, yeah. Cool, all right.
53:21
So I'm going to now talk about the last ingredient in the standard parallelism toolbox, which
53:28
is expert parallelism, right. Now that MoEs are very standard part of the toolkit,
53:34
most big models are MoEs these days. And that allows us to take advantage
53:39
of one of their big benefits, which is expert parallelism. And expert parallelism, I think of myself
53:47
as being something analogous to tensor parallel. Right, in the sense that you're taking the MLPs,
53:53
right, the FFN components of your network. And you're going to split them up in various ways, and you'll incur a communication penalty in the same way
54:00
that you're maybe splitting up the matrices, except here you're splitting up the MLP across different devices.
54:07
And the systems behavior of tensor parallel is-- oh, sorry, expert parallel-- is roughly like tensor parallel
54:14
as well in the sense that this is a high bandwidth parallelism primitive that also reduces activation.
54:21
But one important thing is that if you're doing an MoE, you almost always prefer using an expert parallel sharding
54:30
strategy over tensor parallel. And I've taken this screenshot from the parallelism guidelines
54:36
from Megatron, which is NVIDIA's like parallelism library. It's a really wonderful library and document.
54:44
And one of the guidelines, they say is, look, if you're going to do some of parallelism, either EP or TP, you should be using EP over RP.
54:51
Why is that? Well, this is actually a good list of drawbacks for tensor parallel. If you have a matrix and then you cut up the matrix too
54:59
finely, well, the matrices are going to start to get small and your GPU utilization will suffer.
55:04
So you want your matmuls as big as possible, and tensor parallel reduces that.
55:10
For MoE layers, it's a lot easier to route sparse token activations
55:15
rather than to potentially route these dense big tensor parallel matmul activations.
55:22
And then you can do things like route tokens exactly
55:29
to the places that they need. And you can skip some computation-- or sorry, some overhead in the cases that if you're
55:35
going to have MoE anyway, you might as well have the number of experts split up over all the devices.
55:43
Now, this-- sorry, yeah. This might make it seem like, all right, EP is great.
55:49
Everyone should be doing expert apparel all the time. Unfortunately, expert parallel is still
55:54
really complicated and still very difficult. I think everything that I've said after FSDP--
56:00
tensor parallel, pipeline parallel, systems wise is quite non-trivial to get working really well.
56:06
And I'll just point to two different libraries not to tell you the details of what happens inside of them,
56:12
but to tell you, this is seriously complicated business. DeepSeek has a library called DPP,
56:18
which I think is one of my favorite things from the DeepSeek V3 era, which is basically
56:23
their own library for doing expert parallel routing and dispatching, where they're looking at really low level GPU
56:31
networking primitives in order to try to basically combine different operations or have the hardware handle
56:37
certain operations in order to make MoE routing as efficient as possible.
56:43
Similarly, NVIDIA has a library called Hybrid EP, which is a similar effort of having very efficient low level
56:51
hardware implementations of expert parallel dispatching.
56:57
And just to give you the high level thing, right, the reason why this stuff is hard is you are doing a lot of all to all dispatching.
57:04
If you look at the communications pattern of MoE, much like tensor parallel, every time you have a MLP,
57:10
you're going to have to route tokens all sorts of different places, and you need to do this in a very latency sensitive way.
57:16
Because your computation is waiting for your tokens to arrive. So reducing the latency of this dispatch
57:22
is extremely, extremely important. And one final thing that is like a fun trivia about the DPP
57:29
library is the deep sea cloaks are so intense that in order to optimize,
57:34
like every last bit of performance, they basically looked and found undocumented ptx, which
57:40
is like GPU machine code or machine code like things. And they found how to use these undocumented instructions
57:48
to further accelerate their networking communication. So really, this is the level of stuff that you need in order to get to the frontier of parallelism
57:56
efficiency. Finally, up until now, I've talked about all the parallelism
58:03
primitives in isolation, like data parallel and so on and so forth. With the implicit assumption that you can just put
58:10
them together like LEGO blocks. And this is true, for the most part, for basically all the other parallelism strategies.
58:16
It's very easy to combine, I don't know, data and tensor parallel.
58:21
Expert parallel has additional constraints that you want to be somewhat respectful of.
58:28
Usually, the naive way to do data and expert parallelism, which I think is in many of the old libraries,
58:35
is that the replicas for DP and EP are the same. So in other words, let's say I have
58:41
a data parallelism of eight. I'm going to shard my experts across those eight replicas.
58:47
And then my data will be split across those eight replicas and routed in various ways. Right, so in other words, you split your GPUs according to DP,
58:55
and then your EP is a subset of that split. Right, and this is a very natural thing to do because you're routing tokens.
59:02
But if you do this, there's a maximum bound to how far you can parallelize with EP.
59:07
And it constrains how DP and EP
59:13
And so I think this is the final complexity. I apologize that expert parallel is a little bit
59:18
really messy in many ways. But because of these complexities,
59:23
there is actually this one interesting systems thing that I'll tell you about before I stop talking about EP,
59:30
which is this following fact. Remember, MoEs only change the MLPs, right. They don't change the attention at all.
59:36
Right, so because we're cutting up the MLPs, but not the attention, this means that expert parallelism kind of applies unevenly
59:43
to the model. So when I parallelize at the expert level, I'm parallelizing the MLPs, but I'm not parallelizing
59:50
the attention, right. And so in order to parallelize the attention, maybe I want to also use tensor parallel, right.
59:57
So I want a high tensor parallel level in order to cut out my attention. But this is going to then affect my MLPs.
1:00:08
Because if I have high tensor parallel and high expert parallel, I've cut out my matrices into really tiny pieces, and my utilization is very bad.
1:00:16
So there's this weird tradeoff where you want high tensor parallel for the attention, but you want low tensor parallel
1:00:23
for the MLPs. So there's this conflict. So what do you do?
1:00:28
Well, in the last few years, people have come up with more complicated system solutions
1:00:34
that decouple the tensor parallel in the MLPs and the attention.
1:00:39
And so the attention layers get one kind of tensor parallel, and the MoE layers get another kind of tensor parallel.
1:00:45
And so now you can fully decouple the parallelism. And this leads to more complicated but more effective
1:00:51
combinations of EP and tensor parallel, as well as data parallel.
1:00:58
Cool. OK. So the last thing in this space that I want to end with
1:01:05
is context parallel. So context parallel or ring attention
1:01:11
is an idea of basically splitting activations in a very long sequence across different accelerators.
1:01:17
And you can do things like pass the activations to the device that's needed in this ring-like way following
1:01:25
the mesh topology of TPUs. And so ring attention, which I think
1:01:31
was the original paper that did this, showed that this worked really well on TPUs. Context parallel is a standard parallelization strategy
1:01:38
that does this. Both of these are used in long context extension stages and in model serving.
1:01:44
I'm not going to talk about it, mainly because I think it overlaps in concept to a lot of what we've talked about already.
1:01:51
OK. So this is the end of the second part. And I think to put everything together,
1:01:57
I've made a big table of parallelization strategies, including data parallel, which are the top two
1:02:03
rows and the various model parallelism strategies below them. And I think I've colored in red what I subjectively
1:02:10
feel like are the drawbacks of the different methods. And the reason why I've colored things in red
1:02:16
is to try to convey to you that there is no one strictly dominant parallelization strategy.
1:02:22
It's all a whole bunch of tradeoffs that you somehow have to manage gracefully in order to get a good outcome.
1:02:29
So for example, FSDP is great. It's a wonderful strategy. Does not help you with activation.
1:02:36
It consumes your global batch size in order to do parallelization. So it has limits.
1:02:41
And to address these, maybe you want to compose them with some of these other tricks that you have at hand.
1:02:46
Maybe you want to do some tensor parallel to cut down on activation memory. It also doesn't touch global batch size.
1:02:53
But if you do this, now you need fast networking and you need high bandwidth.
1:03:00
And also then to leverage your slower connections, maybe you want to use some of the other parallelization
1:03:06
strategies. Right, so you see how all of these different advantages means that there's a place for one of these parallelization
1:03:13
strategies in not every architecture, but in many large scale architectures, you use a large combination of these.
1:03:20
Yes? [INAUDIBLE] Are they still applicable to MoEs, or is that strictly--
1:03:27
Yeah. So the question was, is pipeline and, what, FSDP applicable to MoEs?
1:03:33
Yes, yes, they're absolutely applicable. If anything, the standard recommendation
1:03:39
is expert parallel is like tensor parallel. So you want to go up to 8 or fast connections.
1:03:46
People don't really follow that anymore. But even then, FSDP or data parallel in general and pipeline
1:03:52
and tensor are used extensively in all the frontier models. Yeah.
1:03:58
Good. OK, and you will do this or something like this as part of your assignment.
1:04:04
But one of the things that's cool about parallelism and I think is nice is that you can do some math, right. You can actually say, OK, how much
1:04:11
compute do I do per layer for my different sharding strategies? How much communication do I do per layer
1:04:17
for my different communication strategies? And if I have different devices allocated
1:04:22
to different communication strategies, how do these things scale? And then I can figure out for a combination of these strategies,
1:04:30
how do they scale? And you can start to make plots of, if I have a certain batch size and I
1:04:37
have a certain parallelization strategy-- so I have three different parallelization strategies here. How much am I utilizing my GPU?
1:04:45
The ratio of compute time to communications time. And as long as my compute time is longer than communications
1:04:52
time, in principle, you can just hide communications underneath your computation.
1:04:57
And so you can make plots like this where this dashed line at the very top, that's the boundary of where
1:05:03
you're efficient. Anything above that is fully utilizing your computation. Anything below that is on the bad part of your roofline.
1:05:11
You're waiting for communication to do your computation. One thing that's pretty interesting here, right.
1:05:16
So I think this should feel intuitive to you based on what I've told you. If your batch size is big enough, FSDP only is good.
1:05:24
Right, if you have a batch size of 2,000, right, per chip,
1:05:30
then you how far can you go? Well, you've got FSDP doing extremely well. You're fully compute bound, right.
1:05:37
But the important thing is, as the batch size goes down, FSDP only will hit a point where you're now communication bound.
1:05:43
Now, what do you do, right? Well, what do you do is now, in order to keep pushing efficiencies, you
1:05:49
have to incorporate MP, in this case, is tensor parallel, to the mix. Right, now you can push that curve out a little bit more
1:05:55
and be compute bound even into smaller batch size regimes, right. And you keep adding strategies to push this curve out in order
1:06:02
to get something better. And this is what people call 3D or 4D-- I don't think there's 5D--
1:06:09
parallelism. Which is when you put all these strategies together to keep your compute units fully utilized
1:06:16
under different communication topologies. There's a very simple prescription.
1:06:21
I think up until now, I've been telling you things in generalities. So it might feel complicated.
1:06:27
But I think actually the strategy for how you parallelize is very simple, actually. Which is, until your model fits into memory,
1:06:33
you're going to cut up your model by whatever means necessary, right. And how are you going to cut up your model.
1:06:38
You're going to use either tensor or expert parallel for your fast interconnect. And so if you have eight GPUs per machine,
1:06:43
you're going to use a tensor expert parallel of eight. And then you're going to pipeline parallel or Zero-3,
1:06:49
which is FSDP, the rest of the way to make your model fit. And once your model fits, then you're going to just data parallel the rest of the way.
1:06:56
Right, then use up the rest of your GPUs with data parallel, right. If your batch size ends up being too small,
1:07:02
you can do gradient accumulation to get better GPU utilization. Very simple strategy.
1:07:08
And this is borne out in the actual large scale parallel training libraries that you might use.
1:07:14
So I've added the link here to Megatron's guide to parallelizing MoEs.
1:07:21
But this idea actually applies to the dense models as well. And you see exactly the thing that I said, but in reverse order.
1:07:26
So guideline number one is minimize model parallelism, maximize data parallelism. Right, so use most of your GPUs to shard data if you can.
1:07:34
Now, if you're using GPUs, stay within NVLink. So that's one machine, or so one box, right.
1:07:41
Keep expert parallel and tensor parallel within one box. And then if you're going to go multi-node, use pipeline parallelism to go multi-node.
1:07:48
And then if you're MoE, then prefer expert parallel. And then finally, if you're doing long sequences,
1:07:54
use context parallel. Right, ring attention. So exactly what I've been trying to get across
1:08:00
to you through the lecture, but in practitioner form that you see here.
1:08:05
OK, the final component-- actually, maybe I can pause here, in case anyone's curious about 4D
1:08:12
parallelization stuff. Yeah. I know you said context parallel would
1:08:18
be a better name for decreasing parallel [INAUDIBLE]
1:08:25
Oh, yeah, yeah. Well, I mean, sequence parallel is often just you do it with tensor parallel
1:08:33
in order to reduce activations. It's more of an add on that you put in in order to reduce activation.
1:08:38
It's not its own standalone thing, in many cases. Yeah?
1:08:43
With the suspicions that this is like a loop transformer, like that, does that make things easier or harder?
1:08:51
OK. I don't how much I buy the loop transformer mythos rumors.
1:08:57
Maybe it is. It would be interesting. But to the question of how the systems structures would change,
1:09:04
it's a good question. I mean, I guess it might mess with things like FSDP. You can't really discard the weights.
1:09:11
It does have the advantage, though, that you don't have to keep getting weights. It is much more parameter efficient, and so maybe much of the model parallelism stuff
1:09:18
wouldn't be as important. It would be interesting. It would be definitely different for a lot of the FSDP style
1:09:25
things. Because the FSDP strategy is really predicated on getweights, discard weights, getweights, discard weights.
1:09:31
And in a loop, you're not discarding anything. Good, OK.
1:09:36
All right. So this is an old paper, but it is actually probably one of the best resources.
1:09:43
And also, it's not like the networking fundamentals changed that much. So I think the lessons from here are very relevant today.
1:09:53
And so this is a paper by NVIDIA and formerly unfortunately, Stanford folks, Matei and Deepak,
1:09:59
who did a bunch of runs of large scale parallel training across many different configurations
1:10:05
to basically tell you, as you're scaling up, how should your parallelization strategies change across different scales?
1:10:13
And you see exactly this prescription that I've been talking about before, which is, you have data parallel maxed out.
1:10:20
And then your tensor parallel increases until it hits 8, and then you stop because you don't want to go any higher.
1:10:26
And then from that point on, your pipeline parallel increases and increases and increases, right. And with sufficiently large scale, data parallel decreases.
1:10:35
And at the very end, you have a DP of six. And this is because you just need this much tensor and pipeline parallel to fit the stuff.
1:10:41
Right, and you're using the rest of your budget on data parallel.
1:10:47
And if you put them all together, right, even as you go to ludicrously large numbers
1:10:53
of GPUs, your utilization stays very flat and very good. And this is why I think you see this gigantic data center
1:11:01
build-outs. The parallelization strategies people have come up with are so effective. And the hardware for communication
1:11:06
is so good that even with the gigantic data centers or even cross data center training, you can get really good utilization
1:11:14
across these different regimes.
1:11:20
They have really nice quantitative evidence showing you, for different batch sizes for tensor parallel, what
1:11:27
is the right place to stop? And it's clearly tensor parallel size of 8. As you go beyond that, you're going to get into trouble.
1:11:35
And pipeline parallel, you also need big batch sizes in order to be able to utilize the machines effectively.
1:11:41
That's why there's a blue to orange gap. There's pipeline parallel. And the larger the pipeline parallel, the larger the gap
1:11:47
between orange and blue. And then I think another thing that maybe you have already
1:11:54
internalized from flash attention but maybe not is maybe you should do a bunch of activation recomputation.
1:12:00
Because if you do activation recomputation very cleverly, then you can get a bigger batch size, and the bigger batch size then allows
1:12:07
you to get better utilization. This is a, initially, I think, very counterintuitive observation that actually you should
1:12:14
do more computation via recomputation in order to end up getting better utilization,
1:12:21
because it allows you to save memory, and memory can be turned into batch size. OK.
1:12:27
So that was a more quantitative view of how to pick parallelism strategies.
1:12:33
The last thing I want to end with is just to look at just a very few training runs.
1:12:39
And looking at the training runs, maybe we can get a sense of how modern parallelization is used in practice and how it's evolving.
1:12:46
So Dolma by AI2 was a seven be open source model.
1:12:51
Sorry, Olmo, which was trained on the Dolma data set. And this isn't the Dolma paper for the details.
1:12:57
Was a 7B model trained fully with FSDP across a whole bunch
1:13:02
of accelerators. I forget exactly how many. But this is one example of FSDP scales
1:13:09
surprisingly well, even to many, many GPUs. As long as you're training a small model, FSDP is actually a pretty good strategy for parallelization.
1:13:18
I think many 7D-ish models are trained purely with FSDP.
1:13:23
DeepSeek, because of their systems skills, does do some pretty exotic and useful stuff.
1:13:31
So DeepSeek V1 was trained just with data parallel ZeRO stage 1 with tensor sequence and pipeline
1:13:39
parallel, as I talked to you before. DeepSeek V3, that's a MoE model, if you remember my MoE lecture.
1:13:45
So they have not only pipeline parallel, but they also have expert parallel instead of tensor parallel, right.
1:13:51
And their expert parallel actually is a little bit exotic. Because they have 64 way parallelism.
1:13:56
So they group eight different machines together, and that's their expert parallel domain.
1:14:02
And to enable this large expert parallel, they basically use the same tricks that they have from their pipeline parallel
1:14:10
pipelining in order to try to make sure that expert parallel doesn't have low utilization periods.
1:14:16
So this is actually quite a complicated trick that they do to get these very large expert parallel splits.
1:14:23
Another example, Yi, from Chinese open weights training setting. They use, once again, ZeRO stage 1 and tensor and pipeline
1:14:31
parallel. Right, this very much makes sense. It's the classic data parallel, tensor parallel, pipeline parallel combo.
1:14:38
And once again, once you go to MoEs, you replace the tensor parallelism with expert parallelism.
1:14:43
Right, they serve similar goals. But expert parallelism is just a little bit more efficient.
1:14:50
On the other extreme of a really big dense model, you have Llama3. The Llama3 report, I liked quite a bit,
1:14:57
because they're one of the few reports that have literally a full breakdown of their parallelism strategy.
1:15:03
Not just like in the abstract, but across all the different phases. So the cool thing that you see is
1:15:09
they have a pre pre-training stage, a little warm-up phase at the very start. You can ignore that first row.
1:15:14
And then they have their main pre-training phase in the middle row here. And this is a very standard parallelization strategy.
1:15:21
Tensor parallel of 8, one context parallel, 16 pipeline parallel, and 128 data parallel.
1:15:28
Big data parallel splits. And then at the very end, because they want a long context model, they do long context extension.
1:15:35
And for this, they crank up the context parallel, lower the data parallel. And that's how they parallelize this very memory hungry stage
1:15:41
for long context extension. And notice, we're in tensor parallel, not export parallel land. Because Llama3 405B is a gigantic dense model.
1:15:50
It's not a MoE model. And as a side note, right, one thing that I don't talk about,
1:15:57
but if you, for example, become an infra engineer at one of these companies, you'll notice
1:16:02
is GPUs fail all the time. Apparently during Llama3 405B training, GPUs failed 148 times.
1:16:12
And so you need lots of not just fast parallelism, you need redundancy in order to be
1:16:17
able to deal with all these kind of horrible things that can happen to your training. So there's a distributed systems challenge as well.
1:16:24
Gamma 2, which is a Google open source model, has FSDP plus tensor parallel and sequence parallel
1:16:34
and uses these two strategies only. And the reason why I brought up Gamma 2
1:16:39
is, as far as I understand, this is basically a realization of the Google claim that for TPUs, really
1:16:46
you don't need to do pipelines. You just take a really big toroidal mesh and you tensor parallel over that big mesh network
1:16:52
that you have. Unclear whether this can scale out forever. That has always been a little bit less clear to me.
1:16:59
But at least the gamma scales, this is definitely the case. Finally, I took a look at some of the models that were trained
1:17:07
this year, like Mistral 8x22B, and during my investigation, one of the things that I discovered was actually
1:17:13
if you're interested in model training and parallelism configurations in the wild, NVIDIA has a repository called
1:17:21
Megatron Bridge, where they release a lot of recommended training configurations for a lot of different model sizes
1:17:28
and model settings. And you can take a look at what they end up saying is the right model configuration for an 8x22B MoE or DeepSeek V3 modeling.
1:17:39
And so for Mixtral, they do expert parallel of 8, pipeline parallel of 4, and an additional tensor parallel of 4.
1:17:45
This one for the attention layers. And you see this follows the prescription of, keep your expert parallel roughly around eight.
1:17:54
Nemotron 3 Super follows the DeepSeek V3 model, has a bunch of expert parallel as well as context parallel,
1:18:00
because they were doing extensions, at least in this article. And then the last one that I want to talk about
1:18:07
is Qwen 3, which follows the DeepSeek recipe, which is a fairly large amount of expert parallel 32.
1:18:15
And they have eight pipeline parallel and two tensor parallel to split up things like the attention matrices.
1:18:23
It is actually interesting to see additionally the different kinds of benchmarking that NVIDIA folks have done for parallelizing different models.
1:18:32
And you see, I won't go into details of what this is. This is in the NVIDIA bridge repo, if you want to take a look.
1:18:38
There's all sorts of different configurations that you can pick even within tensor parallel that can significantly affect the performance of these.
1:18:45
So the systems integration is quite significant. So to put everything together, I decided maybe
1:18:53
I would build an overview. Really, the thing that's common to this is as much as you can,
1:18:59
all the models use data parallel. They maximize the data parallel domains to the extent that they can.
1:19:04
Tensor parallel almost always remains below eight. And expert parallel can sometimes be big now.
1:19:10
And I think that's partially because of DeepSeek V3 and a lot of the infra that they built for large scale expert parallel
1:19:16
training. Good. OK. So to put everything together, right.
1:19:23
The highest level point, right, of this lecture is we need to think about multi-GPU,
1:19:28
multi-node, maybe even multi-data center parallelism. And to think about efficiency in that regime,
1:19:34
we need to really bring to bear all the approaches that we have. Because you've got fast links, you've got slow links,
1:19:39
you've got techniques that make use of batch sizes and all these different resources that you all want
1:19:45
to consume as much as possible. But given all of these, it turns out that there's fairly simple rules of thumb
1:19:51
for combining all these different forms of parallelism to get in the end what is effectively
1:19:58
full utilization of your compute hardware. Great. And next week, I think we're talking about scaling laws.



&nbsp;



⬆️⬆️⬆️  END OF TRANSCRIPT  ⬆️⬆️⬆️

## §2 — Screenshot / visible source details  ·  layer: `raw_source_metadata`

*Nothing to paste here — just **drop the screenshot in chat** and the normalizer fills §0. (Or paste the visible title / channel / URL / date below.)*

## §3 — Interpretations & review log  ·  append-only (each reviewer adds a new entry; never overwrite)

### Review 001 — Knox / ChatGPT strategic read  ·  layer: `captured_interpretation_nonbinding`
- reviewer: `Knox / ChatGPT` · type: `AI assistant` · at: `TK` · purpose: `strategic source-local interpretation`

> Knox's read is the **strategic source-local interpretation** — paste **exactly what Knox outputs**, however rich, in ONE block. Do NOT split it into fields or trim it. Knox's output typically already carries: priority/depth · source type + authority context · why this matters for OMNI · core takeaway · OMNI translation · likely landing zones · doctrine/primitive pressure · keeper doctrine · what-not-to-import · hard read. (What we ASK Knox to produce = `_KNOX_STRATEGIC_READ_PROMPT.md`.) The agent's §3 Review 003 **formalizes** this (it does not re-derive it).

⬇️⬇️⬇️  PASTE KNOX'S / CHATGPT'S FULL READ BELOW (as-is)  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 002 — Nick operator note  ·  layer: `human_context_nonbinding`
- reviewer: `Nick` · type: `human founder/operator` · at: `TK`

⬇️⬇️⬇️  YOUR GUT NOTE BELOW  (optional)  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 003 — Opus / agent formal deep extraction  ·  layer: `analysis_nonbinding`  ·  (agent-authored, NOT a Nick paste)
- reviewer: `Opus` (or agent) · type: `AI assistant` · at: `TK` · purpose: `formalize Review 001 → structured per-source extraction → feed EVRUN concept registry` · binds nothing (`GRD-036`/`GRD-044`)

> The agent reads §1 verbatim + §3 Review 001 (Knox) IN FULL, then writes the formal per-source extraction HERE (not a sidecar). Formalize Review 001's strategic read — verify/sharpen it, do not re-derive. Per concept cluster: **concept · OMNI meaning · why · downstream homes (thesis §x / CNS / Build-OS / §C / capability-topology / Knowledge-Reservoirs / security / domain-contracts / UX-surfaces / product / future-watch) · source anchors (verbatim ≤12 words + timestamp) · stale-vs-v3 (ABSENT/PARTIAL/AFFIRM) · weight_tier (spine/vocabulary/low-authority-watch/no-op) · status (promote/watch/reject).** Then: **net-new primitives** (dedup vs registry — don't re-mint) + **reread flags**. Cross-source convergence is folded into the EVRUN **concept registry**, not duplicated here.

⬇️⬇️⬇️  AGENT WRITES THE FORMAL EXTRACTION BELOW  ⬇️⬇️⬇️

This one is full semantic / top-tier for OMNI’s distributed Build OS, memory sharding, FSDP-style source loading, pipeline/workflow scheduling, expert routing, topology-aware domain boundaries, and “4D parallelism” doctrine. It is the natural continuation of Lecture 7, but it goes deeper: not just what collective operations are, but how modern systems combine data, tensor, pipeline, sequence/context, and expert parallelism to make enormous systems actually fit and run. Transcript is from the uploaded file.

Rough metadata for Opus
source_platform: YouTube
source_url: https://youtube.com/watch?v=6-cXp-aOmdg
source_title: Stanford CS336 Language Modeling from Scratch | Spring 2026 | Lecture 8: Parallelism
channel_or_org: Stanford Online
course_context: Stanford CS336 / Language Modeling from Scratch
lecture_context: Lecture 8 / Parallelism / advanced distributed training
speaker_context: Likely Tatsunori Hashimoto based on transcript opening referencing Percy’s prior lecture; video description lists Percy Liang and Tatsunori Hashimoto
speaker_affiliation_context:
Percy Liang: Professor of Computer Science, Stanford University
Tatsunori Hashimoto: Assistant Professor of Computer Science
published_at: Apr 28, 2026
captured_at: 2026-06-10
captured_by: Nick
capture_method: screenshot + transcript paste
content_type: technical AI systems lecture / advanced parallelism / FSDP / ZeRO / tensor parallelism / pipeline parallelism / expert parallelism / sequence/context parallelism / hardware topology
source_reliability_context: Stanford course lecture | practical language-model distributed-training systems source
screenshot_metrics_at_capture: 5,062 views; 101 likes
priority: 5/5
depth: full_semantic
recommended_status: core systems substrate source for OMNI distributed execution, Build OS, memory-sharding, context routing, domain topology, and multi-agent orchestration doctrine
transcript_quality_note: Auto-generated transcript is usable. Some hardware/model names and new-chip references may contain caption artifacts or be time-sensitive; verify exact hardware claims before formal technical canonization.
topic_tags_light: [CS336, language_modeling_from_scratch, advanced_parallelism, distributed_training, 4D_parallelism, multi_GPU_training, multi_node_training, data_parallelism, DDP, ZeRO_stage_1, ZeRO_stage_2, ZeRO_stage_3, FSDP, optimizer_state_sharding, gradient_sharding, parameter_sharding, reduce_scatter, all_gather, all_reduce, communication_compute_overlap, activation_memory, activation_recomputation, activation_checkpointing, tensor_parallelism, pipeline_parallelism, micro_batches, pipeline_bubbles, zero_bubble_pipelining, backward_pass_scheduling, gradient_weight_derivative_split, tensor_parallel_forward_backward_duality, column_parallel, row_parallel, sequence_parallelism, context_parallelism, ring_attention, expert_parallelism, MoE, expert_dispatch, all_to_all, token_routing, DeepSeek_V3, DeepSeek_DPP, NVIDIA_Hybrid_EP, Megatron, Megatron_Bridge, topology_aware_parallelism, intranode_parallelism, internode_parallelism, NVLink, NVSwitch, InfiniBand, ethernet, RDMA, TPU_toroidal_mesh, GPU_fat_tree, TPU_vs_GPU, hardware_workload_fit, data_center_as_compute_unit, batch_size_resource, critical_batch_size, memory_vs_compute_bottleneck, communication_topology, 3D_parallelism, 4D_parallelism, model_parallelism, context_parallelism_long_sequence, expert_parallel_high_bandwidth, dense_model_parallelism, MoE_parallelism, Llama3_parallelism, OLMo_FSDP, Gemma_TPU_parallelism, Mixtral_parallelism, Qwen_parallelism, utilization, roofline, compute_bound, communication_bound, training_failures, redundancy, distributed_systems]
§0.1 rough people / authority context
primary instructor context:
name: Tatsunori Hashimoto
role_in_source: likely lecturer for this session based on transcript opening and lecture flow
affiliation_at_publication: Stanford University
speaker_type: AI researcher / data-centric ML / language-model systems / systems-aware training methodology
authority_context: Very high relevance for advanced distributed language-model training, memory sharding, FSDP, model parallelism, expert parallelism, hardware topology, and modern large-scale training configurations.
additional course instructor context:
name: Percy Liang
role_in_source: course instructor listed in video description and referenced as prior lecturer
affiliation_at_publication: Stanford University
speaker_type: AI researcher / language models / foundation-model systems methodology
authority_context: Very high relevance for CS336’s broader language-model training, data, architecture, and systems methodology.
publisher / channel: Stanford Online
course / event context: Stanford CS336: Language Modeling from Scratch
perspective / conflict notes: Technical systems source. Not healthcare-specific. Durable OMNI value comes from distributed-systems principles: memory sharding, activation sharding, communication/computation overlap, pipeline scheduling, topology-aware routing, expert dispatch, context parallelism, and multi-axis scaling. Do not convert GPU/TPU details directly into product doctrine; extract operating laws for distributed care/business/agent systems.
suggested_processing:
priority: 5/5
depth: full_semantic
EVRUN needed?: yes
likely landing zones: Build OS / Agent Work Blueprint / AI substrate / CNS orchestration / context packet strategy / Architecture Memory Control Plane / domain sharding / memory-control-plane / Provider Workspace / Patient-CNS / BIZOPS workforce routing / incident response / topology-aware execution / distributed proof and monitoring
Knox / ChatGPT read for §3 Review 001

Process this one as full semantic.

This is a core distributed-execution source for OMNI.

Lecture 7 gave the basic primitives:

broadcast,
scatter,
gather,
reduce,
all-gather,
reduce-scatter,
all-reduce,
all-to-all,
data parallel,
tensor parallel,
pipeline parallel.

Lecture 8 explains how the real frontier systems combine those primitives to make giant training runs feasible.

The source is not healthcare-specific.

But it is extremely relevant because OMNI has the same underlying scaling problem:

too much state,
too many workers,
too many specialized lanes,
too much communication,
too many bottlenecks,
too many memory layers,
and no single scaling strategy that wins everywhere.

Core takeaway

Modern large-scale training is not one kind of parallelism.

It is a composition of parallelism strategies:

data parallelism,
FSDP / ZeRO-style sharding,
tensor parallelism,
pipeline parallelism,
sequence/context parallelism,
expert parallelism,

chosen according to memory pressure, compute pressure, communication topology, batch size, activation cost, model shape, and hardware substrate.

OMNI translation:

OMNI will not scale through one generic agent, one global context, one universal workflow, or one queue.

It needs composed execution strategies:

replicate where cheap,
shard where state is too large,
pipeline where stages are natural,
route to experts where capability is specialized,
use local context where possible,
gather global context only when needed,
overlap waiting with useful work,
and choose boundaries based on communication cost.

Classification

Priority: 5/5
Depth: full semantic
Authority context: Stanford advanced parallelism / distributed language-model training lecture
OMNI relevance: top-tier for Build OS, Agent Work Blueprint, distributed CNS orchestration, context packet design, domain boundaries, expert routing, memory sharding, workload scheduling, and topology-aware architecture
Promotion posture: core systems analogy source; translate distributed execution principles, not literal GPU implementation details

The big OMNI translation

The new unit of compute is the data center.

The lecture explicitly says the new unit of compute is not the GPU.

It is the entire data center.

That is a major conceptual shift.

OMNI equivalent:

The unit of intelligence is not the model call.

It is the whole operating system:

sources,
domains,
agents,
providers,
patients,
policies,
queues,
traces,
reviews,
connectors,
and humans.

Keeper:

The system is the computer.

Parallelism exists because of two bottlenecks: compute and memory.

The lecture says the two reasons to parallelize are:

more compute than one chip can provide,

and

more memory/state than one chip can hold.

OMNI has the same two pressures.

Compute pressure:

too many tasks,
patients,
messages,
documents,
leads,
workflows,
patches,
reviews.

Memory pressure:

too much patient history,
architecture corpus,
D7 evidence,
longitudinal signal,
business state,
clinical context,
source provenance.

Keeper:

Scale pressure is compute plus memory.

Intranode vs internode is a core topology distinction.

Intranode links are fast.

Internode links are slower.

Different strategies belong on different links.

OMNI equivalent:

Some coordination happens inside a fast boundary:

one domain,
one clinic,
one patient episode,
one agent package,
one repo module.

Other coordination crosses slower boundaries:

external vendor,
human provider,
legal review,
partner system,
regulator,
separate domain.

Keeper:

Topology determines viable coordination.

Communication topology changes architecture.

TPUs historically used regular neighbor mesh patterns.

GPUs used fat-tree / all-to-all style networking.

The lecture argues workloads shape networking, and newer hardware may converge because MoE workloads need more all-to-all.

OMNI translation:

Workflow demand should shape architecture boundaries.

If work routes randomly to specialists, the system needs all-to-all routing infrastructure.

If work is staged and predictable, pipeline structure may work.

Keeper:

Workload shape should determine topology.

Hardware design is workload-driven.

The lecture’s TPU/GPU comparison is useful because it shows no hardware is universally best.

A mesh can be efficient for predictable neighbor communication.

A fat tree can be flexible for dynamic all-to-all patterns.

OMNI lesson:

No operating design is universally best.

Use different coordination patterns for different work.

Keeper:

Architecture follows workload, not ideology.

Brute-force connectivity has cost.

The Huawei Ascend example is used to show that brute-force high connectivity can solve communication bottlenecks but at huge power cost.

OMNI translation:

You can brute force workflow problems with:

more humans,
more model calls,
more meetings,
more all-context retrieval,
more dashboards,
more notifications.

But the cost can be ugly.

Keeper:

Brute-force coordination burns power.

Efficiency requires lossless resource use.

The lecture says we want to control memory, control compute, and use resources losslessly.

OMNI version:

The system should not waste:

provider attention,
staff time,
patient attention,
token budget,
review bandwidth,
agent context,
source retrieval,
model calls,
engineering effort.

Keeper:

Operational resources should be intentionally allocated.

Data parallelism is still the simplest scaling idea.

Split the batch across workers.

Each worker computes gradients.

Synchronize.

OMNI equivalent:

Many tasks can be split across workers using the same workflow:

file classification,
lead triage,
message labeling,
photo quality review,
source extraction,
eval case scoring.

Keeper:

Data-parallel work is the default easy scale path.

Data parallelism solves compute, not memory.

The lecture says basic data parallelism gives perfect-ish compute scaling but no memory scaling.

Every GPU has the whole model.

OMNI translation:

Giving every agent or staff member the same whole playbook can scale throughput for simple tasks, but it does not solve context bloat.

Keeper:

Replication helps throughput before it helps memory.

Data parallelism consumes batch size.

This is one of the most important points.

You cannot data-parallel beyond the useful batch.

Batch size becomes a resource.

OMNI analog:

You cannot simply add more workers to the same queue forever.

At some point, coordination, quality, review, or case availability limits returns.

Keeper:

Parallelism consumes usable batch.

Critical batch size means more parallelism can stop helping.

The lecture explains that eventually adding more batch elements gives less benefit than taking another optimization step.

OMNI equivalent:

A bigger review batch,
larger context packet,
more agents,
more simultaneous tasks,
or bigger corpus wave

can stop helping and start hurting.

Keeper:

Batch scale has diminishing returns.

Memory accounting is worse than parameters.

The lecture walks through weights, gradients, accumulators, optimizer state, first moments, second moments.

The model’s memory is not just parameters.

OMNI equivalent:

Patient memory is not just notes.

Architecture memory is not just files.

Operational memory is not just records.

You also need:

derived views,
review state,
audit state,
conflict state,
lineage,
summaries,
open tasks,
permissions,
versions,
history.

Keeper:

State is bigger than truth.

Optimizer state is a hidden memory monster.

In model training, optimizer state can dominate memory.

OMNI analog:

The hidden operational state may dominate the visible canonical truth.

Examples:

open obligations,
drafts,
review queues,
temporary context packets,
audit traces,
failed attempts,
decision rationale,
agent scratchpads,
notifications,
derived projections.

Keeper:

Hidden state can dominate system cost.

ZeRO Stage 1 shards optimizer state.

Each GPU is responsible for updating a slice of parameters, keeping only the optimizer state it needs.

The trick:

reduce-scatter gradients,
local update,
all-gather updated parameters.

Same communication cost as all-reduce.

OMNI translation:

Shard responsibility for maintaining expensive local state while keeping shared working state synchronized.

Keeper:

Shard expensive state; synchronize shared output.

ZeRO Stage 1 is “free” because of collective-operation equivalence.

All-reduce equals reduce-scatter plus all-gather in cost.

So you can get memory savings without extra communication.

OMNI lesson:

Sometimes a better decomposition gives a major operational improvement without new total cost.

Keeper:

Rearranging communication can create free wins.

ZeRO Stage 2 shards gradients too.

Instead of materializing all gradients, compute layer by layer and send gradient shards as they appear.

OMNI equivalent:

Do not materialize a huge intermediate work product if you can stream responsibility to owners as it is produced.

Keeper:

Stream partial state to owners instead of hoarding it centrally.

ZeRO Stage 2 is incremental work with immediate freeing.

Compute the piece.

Send it.

Free it.

OMNI translation:

For big workflows:

extract one section,
route it,
free local scratch;
process one patient cohort,
assign tasks,
do not keep everything in working memory.

Keeper:

Process, route, release.

ZeRO Stage 3 / FSDP shards everything.

Parameters, gradients, and optimizer state are all sharded.

Weights are gathered on demand layer by layer.

After use, they are freed.

OMNI analogy:

No single agent/domain holds the full system.

It loads only the needed authority/source slice for the current step.

Then releases it.

Keeper:

Load full context only when needed, then release.

FSDP is a wrapper that preserves the conceptual computation.

The model still computes as if it had full parameters.

But the runtime gathers and frees weights behind the scenes.

OMNI translation:

A domain workflow can appear simple to the user while the runtime handles source gathering, permission checks, context loading, and cleanup.

Keeper:

Good runtime hides complexity without hiding trace.

FSDP’s magic is on-demand materialization.

Gather weights for one layer.

Compute.

Free.

Gather for backward.

Compute.

Reduce-scatter gradients.

Free.

OMNI equivalent:

materialize provider packet,
review,
write structured decision,
release packet;
materialize D7 source bundle,
extract,
store observation candidate,
release source bundle.

Keeper:

Materialize working state just in time.

FSDP still has extra communication.

Stage 3 requires extra all-gather.

But it can be hidden by overlapping communication and computation.

OMNI translation:

A workflow can add necessary safety or context steps without slowing down if those steps are prepared in parallel while other work is happening.

Keeper:

Extra work can be hidden if scheduled correctly.

Communication/computation overlap is the key to scaling.

While computing forward on layer 0, gather layer 1.

While reviewing one packet, prepare the next packet.

While lab results are pending, prepare education or paperwork.

Keeper:

Overlap waiting with useful work.

Prefetching is a first-class runtime behavior.

FSDP requests the next layer’s parameters while current computation runs.

OMNI needs prefetching:

next source bundle,
next policy,
next patient packet,
next test,
next reviewer context,
next domain state.

Keeper:

Prefetch the next needed context.

Freeing memory is also first-class.

FSDP explicitly frees weights after use.

OMNI should explicitly release:

temporary context,
derived packet,
scratchpad,
stale draft,
intermediate source expansion.

Keeper:

Cleanup is architecture.

FSDP is elegant until you hit the next bottleneck.

The lecture says FSDP is clean, but not sufficient.

It does not solve everything.

OMNI lesson:

A beautiful architecture pattern will eventually hit limits.

Do not overcanonize one trick.

Keeper:

No single scaling strategy dominates.

Data parallelism still consumes global batch size.

Even FSDP-like approaches use the data-parallel axis.

When batch size cannot grow, you need other forms of model parallelism.

OMNI analog:

Even if context loading is efficient, if the work queue cannot be usefully split further, you need a different decomposition.

Keeper:

When one scaling resource saturates, switch axes.

Model parallelism shifts from moving parameters to moving activations.

The lecture makes a clean distinction:

FSDP moves parameters.

Model parallelism moves activations.

OMNI translation:

Some systems move source/state to the worker.

Other systems move intermediate work products between specialists.

Keeper:

Moving source and moving work product are different.

Pipeline parallelism cuts by layer/depth.

Each device owns a stage.

Activations move forward.

Gradients move backward.

OMNI equivalent:

intake -> identity -> clinical -> commerce -> D7 -> messaging -> follow-up.

Keeper:

Pipeline parallelism maps to staged workflows.

Naive pipelines are awful.

The lecture shows the depressing bubble chart:

one worker active at a time,
everyone else idle.

OMNI analog:

A staged workflow can look efficient on paper but waste huge time if each stage waits for the previous stage to fully finish.

Keeper:

Staged workflows create bubbles.

Micro-batches reduce pipeline bubbles.

Split work into smaller chunks so downstream stages can start before the whole batch finishes.

OMNI translation:

Do not wait for a whole project, patient cohort, corpus wave, or launch plan before moving useful pieces downstream.

Keeper:

Move coherent chunks early.

Batch size is a pipeline-utilization resource.

The lecture says larger micro-batch counts reduce bubble ratio.

OMNI analog:

A pipeline needs enough coherent work units in flight to keep every stage busy.

Keeper:

Pipelines need work in flight.

Pipeline parallelism is communication-efficient.

It passes activations point-to-point.

This is why it works on slower links.

OMNI translation:

Pipeline workflows are suitable across slower organizational or external boundaries because communication is localized.

Keeper:

Slow links favor staged point-to-point workflows.

Pipeline parallelism belongs on slow links.

The lecture says pipelines live on the slowest networking links.

OMNI equivalent:

External vendors,
legal review,
regulators,
separate business units,
human escalation,

should often be treated as pipeline boundaries, not tight tensor-like shared computation.

Keeper:

Slow boundaries should be pipeline boundaries.

Pipeline bubbles can be attacked with scheduling.

Different scheduling of forward/backward stages can reduce idle time.

OMNI version:

Better queue sequencing, staged review, and release choreography can reduce idle time without changing the whole workflow.

Keeper:

Scheduling is architecture.

Zero-bubble pipelining separates two backward-pass tasks.

Backward pass has:

propagate partial derivatives backward,

and

compute weight gradients.

The first is dependency-critical.

The second can be deferred.

OMNI translation:

Every workflow has dependency-critical work and deferrable work.

Critical:

what unlocks next stage.

Deferrable:

documentation polish,
secondary metrics,
non-blocking analysis,
retrospective evidence.

Keeper:

Separate blocking work from deferrable work.

Deferring non-blocking work fills pipeline gaps.

Zero-bubble scheduling uses deferred weight-gradient work to fill idle gaps.

OMNI equivalent:

Use idle capacity for non-blocking work:

documentation cleanup,
quality scoring,
source indexing,
follow-up prep,
report formatting,
review packet preparation.

Keeper:

Fill bubbles with deferrable work.

Tensor parallelism cuts by width.

Matrix multiplies split into smaller matrix multiplies.

Partial results are combined.

OMNI analogy:

A tightly coupled decision can be split by dimension only when communication is fast enough.

Keeper:

Width splitting is high-communication work.

Tensor parallelism needs fast interconnects.

The lecture repeats that tensor parallel is communication-hungry and should stay inside fast links.

OMNI version:

Do not split tightly integrated reasoning across slow domains, humans, or vendors.

Keeper:

Tightly coupled cognition needs fast coordination.

Tensor parallel forward/backward duality matters.

Forward copy / all-reduce flips in backward.

OMNI translation:

The flow that assembles a decision may mirror the flow that distributes responsibility afterward.

Keeper:

Assembly and accountability often reverse.

Small layers are replicated.

LayerNorms, nonlinearities, and routers are usually replicated because sharding them is not worth the overhead.

OMNI equivalent:

Replicate tiny common rules:

identity basics,
RBAC checks,
source citation norms,
tenant ID,
audit envelope,
status enums.

Keeper:

Replicate cheap common substrate.

Do not shard tiny things.

The overhead of splitting small operations can exceed the savings.

OMNI analog:

Do not create a separate domain, agent, or approval lane for every tiny responsibility.

Keeper:

Granularity has overhead.

Activation memory can dwarf parameter memory.

The lecture shows actual memory profiles where activations are huge.

OMNI equivalent:

Temporary working context can dwarf canonical state.

A patient source bundle, corpus extraction wave, provider packet, or agent context may be much larger than the final committed record.

Keeper:

Working context can dominate memory.

Maximum memory occurs during dynamic execution, not just static storage.

The memory peak happens when activations and gradients overlap.

OMNI translation:

Peak operational load occurs during transitions:

launch,
migration,
incident,
provider review,
large corpus pass,
patient surge,
campaign response.

Keeper:

Peak state appears during transitions.

Activation memory requires its own strategy.

Parameter sharding is not enough.

OMNI equivalent:

Canonical source sharding is not enough.

You need strategy for working packets, intermediate states, review bundles, traces, and temporary expansions.

Keeper:

Working-state strategy is separate from source-state strategy.

Tensor parallel reduces some activations but not all.

MLP/attention parts shrink, but layer norms/residual inputs remain.

OMNI translation:

Some context can be partitioned, but some common context must remain replicated.

Keeper:

Not all state shards cleanly.

Sequence parallelism handles leftover activation memory.

It splits lightweight operations along the sequence axis and materializes pieces on demand.

OMNI equivalent:

Long histories can be split into sequence slices:

recent messages,
prior episodes,
lab history,
longitudinal baseline,
old source docs,
architecture history.

Keeper:

Long sequences need sequence sharding.

Sequence parallelism is conceptually FSDP for activations.

Store activations sharded.

Gather when needed.

Reduce-scatter afterward.

OMNI translation:

Store long context in shards.

Materialize working slices only when a task requires them.

Keeper:

Shard active memory, not just source memory.

Context parallelism/ring attention is the long-context version.

The lecture distinguishes sequence parallel from context parallel/ring attention for long sequences.

OMNI equivalent:

Large longitudinal histories need context-parallel mechanisms, not only summaries.

Keeper:

Long context needs explicit parallelism.

Activation recomputation trades compute for memory.

Recompute attention rather than store huge softmax state.

OMNI analog:

Sometimes recomputing a derivative packet from source is better than storing every derivative forever.

Keeper:

Use compute to buy memory.

More computation can improve utilization.

The lecture notes recomputation can allow larger batch sizes, improving utilization.

OMNI translation:

Doing extra structured preprocessing or recomputation can improve total throughput if it reduces memory/review/context bottlenecks.

Keeper:

Extra computation can unlock capacity.

Expert parallelism is MoE’s natural parallelism.

Experts live on different devices.

Tokens route to them.

OMNI equivalent:

specialized domains/agents/providers/staff lanes receive the work that needs them.

Keeper:

Specialists need routing infrastructure.

Expert parallelism is like tensor parallel in bandwidth demands.

It is high-bandwidth, latency-sensitive, and all-to-all heavy.

OMNI lesson:

Specialist routing can be expensive if every task needs dynamic dispatch.

Keeper:

Specialization has dispatch cost.

Expert parallelism is often preferred over tensor parallel for MoE layers.

If the model already has experts, shard the experts.

OMNI analog:

If the organization already has natural specialist lanes, route work to them rather than splitting every task mechanically.

Keeper:

Use natural specialization first.

Expert parallel is complicated despite being conceptually obvious.

The lecture points to DeepSeek DPP and NVIDIA Hybrid EP as serious low-level libraries.

OMNI equivalent:

A real specialist routing layer is not a simple if/else.

It needs dispatch,
load balancing,
latency control,
queue awareness,
trace,
fallback,
capacity,
routing quality.

Keeper:

Routing is infrastructure.

MoE dispatch is all-to-all and latency-sensitive.

Tokens must arrive before computation can continue.

OMNI translation:

If a workflow must wait on a specialist, that handoff becomes latency-critical.

Keeper:

Critical expert handoffs need fast dispatch.

Frontier efficiency may require low-level optimization.

DeepSeek allegedly finds undocumented low-level instructions to optimize networking.

OMNI translation:

At frontier scale, sometimes the winning edge is operational detail, not conceptual doctrine.

Keeper:

Operational detail becomes strategy.

Parallelism strategies combine like LEGO blocks, except when they don’t.

The lecture says most strategies compose fairly naturally, but expert parallel has extra constraints.

OMNI analog:

Most workflows can combine lanes, but some specialist routing systems create coupling constraints.

Keeper:

Composition creates interactions.

Expert parallel applies unevenly to the model.

MoE changes MLPs, not attention.

So expert parallel helps one part but not another.

OMNI translation:

A specialist lane may solve one part of the workflow while leaving another bottleneck untouched.

Keeper:

Parallelism can be partial.

Attention may still need tensor parallel while MLP uses expert parallel.

Different submodules need different parallelism.

OMNI equivalent:

A patient workflow may need:

clinical expert routing for symptoms,
D7 context parallel for evidence,
commerce pipeline for billing,
identity shared substrate,
messaging local context.

Keeper:

Different subproblems need different scaling axes.

Decoupling parallelism by submodule is advanced but necessary.

The lecture describes separate tensor parallel settings for attention and MoE layers.

OMNI translation:

Do not force one decomposition across all domains.

Different parts of the system may need different execution strategies.

Keeper:

One global parallelism setting is too crude.

Context parallelism matters for long context extension and serving.

It splits activations across long sequences.

OMNI version:

Patient longitudinal histories, architecture corpora, and evidence archives need context-parallel processing.

Keeper:

Long-context serving needs architecture, not just more window.

There is no strictly dominant strategy.

The lecture’s big table marks drawbacks across all methods.

FSDP is good but does not solve activation.

Tensor parallel helps activation but needs fast network.

Pipeline helps slow links but has bubbles.

Expert parallel helps MoE but is complicated.

Context parallel helps long sequence but adds its own complexity.

Keeper:

Every scaling strategy has a red cell.

The art is graceful composition.

Use multiple strategies where each fits.

OMNI translation:

A serious operating system composes:

data-parallel batch work,
FSDP-like context loading,
pipeline workflows,
expert routing,
context sharding,
shared substrate,
local/global context,
load balancing.

Keeper:

Scaling is composition.

The lecture gives a simple prescription.

Until the model fits:

cut it up however necessary.

Use tensor/expert parallel on fast interconnects.

Use pipeline or FSDP the rest of the way.

Once it fits:

maximize data parallelism.

If batch is too small:

use gradient accumulation.

OMNI version:

Until work fits:

shard domain/source/context.

Use tight coordination only within fast boundaries.

Use pipelines across slower boundaries.

Once work fits:

batch/data-parallel the rest.

Keeper:

Fit first, then maximize parallel throughput.

Minimize model parallelism; maximize data parallelism.

The Megatron guideline is clear.

Model parallelism is necessary but costly.

OMNI analog:

Prefer simple replicated workflows when possible.

Use deep sharding/specialist routing only when needed.

Keeper:

Use the simplest scaling axis that works.

Keep tensor/expert parallel inside fast boundaries.

For GPUs, stay within NVLink when possible.

OMNI translation:

Keep tightly coupled work inside the same fast team/domain/system.

Keeper:

High-bandwidth collaboration needs proximity.

Use pipeline parallelism across nodes.

Pipeline tolerates slower links.

OMNI analog:

Use staged workflows across slow organizational/external boundaries.

Keeper:

Slow handoffs want pipelines, not constant sync.

Prefer expert parallel for MoEs.

If the work is naturally expert-routed, use expert routing.

OMNI equivalent:

If the patient/business/system work has natural specialist lanes, use them.

Keeper:

Route naturally specialized work to specialists.

Use context parallel for long sequences.

Long-context extension requires a specific strategy.

OMNI translation:

Do not make long patient histories or architecture corpora a giant global packet.

Keeper:

Long history needs context-specific parallelism.

Utilization depends on compute time vs communication time.

If compute takes longer than communication, communication can be hidden.

If communication dominates, you are bad side of roofline.

OMNI equivalent:

If useful work can happen while waiting, handoffs are okay.

If the system mostly waits on handoffs, redesign.

Keeper:

Hide communication under useful work.

Roofline thinking applies to workflows.

The lecture uses compute/communication roofline plots.

OMNI can classify workflows as:

review-bound,
context-bound,
API-bound,
model-bound,
provider-bound,
identity-bound,
policy-bound,
documentation-bound.

Keeper:

Find the roofline of the workflow.

Data parallel only works well at large enough batch sizes.

FSDP-only can be good when batch size is big.

At smaller batch sizes, communication dominates.

OMNI translation:

A workflow may be efficient only when there is enough coherent work in the lane.

Keeper:

Throughput strategy depends on work volume.

Adding model parallelism can extend efficient regimes.

When batch gets too small, tensor/model parallelism helps keep compute utilized.

OMNI analog:

When one queue does not have enough work, reconfigure work allocation or combine axes.

Keeper:

Switch scaling strategy when volume changes.

Large-scale examples validate the prescriptions.

OLMo: FSDP works well for smaller dense models.

DeepSeek: expert/pipeline/data combinations for MoE.

Llama 3 405B: tensor, pipeline, data, context parallelism.

Gemma/TPU: tensor/sequence over mesh.

Megatron Bridge configs: expert/tensor/pipeline/context combos.

Keeper:

Real systems use composed parallelism.

Dense and MoE models choose different strategies.

Dense giant models lean tensor/pipeline/context/data.

MoE models lean expert parallel plus pipeline/data/tensor for attention.

OMNI translation:

Different business/care architectures need different scaling strategies.

Keeper:

Architecture shape determines scaling shape.

Training failures are part of distributed systems.

The lecture notes Llama 3 405B had many GPU failures during training.

OMNI lesson:

At scale, components fail.

Providers leave,
agents fail,
APIs time out,
sources corrupt,
queues jam,
documents misparse,
patients churn,
payments fail.

Keeper:

Distributed systems need redundancy.

Redundancy is not optional.

Fast parallelism is not enough.

The system must survive failures.

OMNI equivalent:

retry,
fallback,
manual mode,
checkpoint,
audit log,
role backup,
source reprocessing,
agent rerun,
incident queue.

Keeper:

Scale requires fault tolerance.

Training configuration is itself a strategic artifact.

The lecture reads model reports for parallelism configs.

OMNI should treat execution configs the same way:

what decomposition,
what context strategy,
what routing,
what queue,
what review,
what proof,
what fallback.

Keeper:

Execution configuration is architecture.

Megatron Bridge is a precedent for recommended configurations.

NVIDIA publishes recommended configurations for model families.

OMNI Build OS can have recommended execution templates:

D3 scheduling slice,
D7 extraction wave,
architecture gem-pull,
patient triage,
provider packet,
incident response,
agent patch package.

Keeper:

Standard configurations speed build work.

Systems integration is significant.

The lecture says configurations within tensor parallel can significantly affect performance.

OMNI version:

Small differences in source layout, context packet, queue routing, or action schema can significantly affect outcomes.

Keeper:

Integration details matter.

Data parallel maxed out where possible.

The overview says models use data parallel as much as they can.

OMNI translation:

Use simple replicated batch processing for anything that does not need deep coupling.

Keeper:

Exploit easy parallelism first.

Tensor parallel almost always stays below a small fast-boundary size.

The exact number is hardware-specific, but the principle matters.

OMNI equivalent:

High-bandwidth collaboration should remain within a small fast coordination group.

Keeper:

Tight coupling should have bounded group size.

Expert parallel can grow larger with special infrastructure.

DeepSeek-style systems push larger EP domains using custom systems tricks.

OMNI analog:

Specialist routing can scale farther only if routing infrastructure is mature.

Keeper:

Expert scaling requires expert infrastructure.

4D parallelism is not complexity for complexity’s sake.

It is what happens when every single-axis strategy has limits.

OMNI version:

Multi-axis execution is not bureaucracy if each axis solves a real bottleneck.

Keeper:

Complexity is justified only by bottleneck relief.

This source validates Agent Work Blueprint strongly.

Agents need multiple execution strategies:

data-parallel file work,
pipeline build packages,
expert-specialist lanes,
context-parallel corpus reads,
FSDP-like just-in-time context loading,
all-to-all routing only where needed,
barriers only where correctness requires.

Keeper:

Agent systems need multi-axis parallelism.

This source validates Architecture Memory Control Plane.

The corpus is too large for one context.

Need:

source sharding,
gem-pull waves,
domain tiles,
context-parallel reading,
FSDP-like source loading,
reduce-scatter responsibilities,
canon all-reduce only after review.

Keeper:

Architecture memory needs distributed processing.

This source validates CNS as orchestrator.

CNS should not be the worker for everything.

It should decide:

what to replicate,
what to shard,
what to pipeline,
what to route,
what to gather,
what to synchronize,
what to prefetch,
what to free.

Keeper:

CNS is scheduler plus router, not monolith.

This source validates Provider Workspace design.

Provider context should not be all patient history all the time.

It should be:

current local packet,
compact patient state,
source fallback,
specialist packet,
risk-triggered deep gather,
review output,
assigned responsibility slices.

Keeper:

Provider workspace needs FSDP-like context loading.

This source validates Patient-CNS routing.

Routine patient message:

local packet.

High-risk clinical issue:

gather clinical/D7/current treatment state.

Billing issue:

commerce lane.

Identity issue:

identity resolver.

Aftercare issue:

D5/D7/provider lane.

Keeper:

Patient-CNS should activate by bottleneck and risk.

This source validates BIZOPS/workforce routing.

The same logic applies to people:

data-parallel routine tasks,
pipeline onboarding,
expert lanes,
capacity-aware routing,
micro-batches,
load balance,
backup/failure handling.

Keeper:

Workforce is distributed compute.

This source validates incident response.

Incident response needs multi-axis orchestration:

broadcast alert,
scatter investigation,
gather evidence,
reduce severity,
pipeline legal/comms/security,
expert-route technical tasks,
synchronize shared state,
maintain redundancy.

Keeper:

Incident response needs parallelism strategy.

The most important OMNI lesson is topology-aware decomposition.

If two pieces need constant high-bandwidth interaction, keep them close.

If interaction is sparse/staged, pipeline them.

If work is independent, data-parallel it.

If work is specialist, expert-route it.

If context is huge, context-parallel it.

Keeper:

Domain boundaries should follow communication frequency.

Likely OMNI landing zones

Build OS:

multi-axis agent execution
work package sharding
FSDP-like just-in-time context loading
prefetch and cleanup
micro-batching
communication/computation overlap
execution templates
proof and utilization metrics
failure/retry infrastructure

Agent Work Blueprint:

data-parallel agents for independent files/tasks
pipeline agent stages for staged build work
specialist lanes for architecture/security/DB/UI/domain work
context-parallel corpus reading
reduce-scatter responsibilities
all-reduce canon update only after review
barriers only where correctness requires

AI substrate:

model-router topology
long-context serving strategy
expert routing infrastructure
context-parallel architecture
resource allocation
communication-aware routing
memory sharding

CNS:

orchestration by bottleneck
domain activation
context gather/release
prefetch next state
route to expert lanes
synchronize shared truth
no monolithic all-context action

Context packet strategy:

local packet by default
source fallback
compact active state
sequence/context sharding
activation-like working memory
just-in-time materialization
cleanup/release semantics
prefetching

Architecture Memory Control Plane:

shard corpus by domain/status/authority
distributed gem-pull
context-parallel reading
pipeline extraction waves
canonical all-reduce
conflict routing
reduce-scatter domain remediation
full source fallback

D7 / Observation / Clinical Memory:

original source preserved
active state loaded on demand
temporary working packets freed
high-risk full gather
long history sequence sharding
source fallback after compact state

Provider Workspace:

selected review packets
risk-triggered deep gather
staged review pipelines
specialist routing
assigned slices after shared assessment

Patient-CNS:

sparse domain activation
local/global context switching
patient issue routing by risk/domain
avoid all-gather for routine messages
synchronize committed state after review

BIZOPS / Workforce:

task sharding
expert routing
pipeline bubbles
micro-batches
staff capacity
redundancy
failover roles

Governance / Security / Incident:

topology-aware escalation
broadcast/scatter/gather/reduce patterns
incident synchronization
failure tolerance
no silent degradation
authority barriers

Doctrine / primitive pressure

Potential concepts:

data_center_as_compute_unit
system_as_computer
compute_memory_dual_bottleneck
intranode_internode_boundary
topology_determines_coordination
workload_shapes_topology
bruteforce_coordination_cost
resource_losslessness
data_parallel_default
replication_before_memory_scaling
parallelism_consumes_batch
critical_batch_size
state_bigger_than_truth
optimizer_state_analogy
hidden_operational_state
shard_expensive_state
free_memory_win
communication_rearrangement_win
stream_partial_state
process_route_release
FSDP_context_loading
just_in_time_materialization
runtime_complexity_trace
communication_compute_overlap
prefetch_context
cleanup_as_architecture
single_strategy_limit
scaling_axis_switch
source_vs_work_product_movement
pipeline_stage_workflow
pipeline_bubble
microbatch_pipeline
semantic_microbatch
pipeline_on_slow_link
zero_bubble_workflow
blocking_vs_deferrable_work
bubble_filling
tensor_parallel_tight_cognition
forward_backward_accountability_duality
replicate_small_common_substrate
working_context_dominates_memory
peak_state_transition
activation_strategy
sequence_sharding
context_parallelism
recompute_for_memory
expert_parallel_infrastructure
dispatch_cost
natural_specialization
routing_as_infrastructure
latency_critical_expert_handoff
composition_interaction
partial_parallelism
subproblem_specific_scaling
one_global_strategy_too_crude
red_cells_everywhere
graceful_composition
fit_first_then_parallelize
max_data_parallel
fast_boundary_for_tight_work
slow_boundary_pipeline
long_history_context_parallel
workflow_roofline
training_config_as_architecture
standard_execution_config
multi_axis_parallelism
topology_aware_domain_boundary
distributed_memory_control_plane

Keeper doctrine candidates

The system is the computer.

Scale pressure is compute plus memory.

Topology determines viable coordination.

Workload shape should determine topology.

Architecture follows workload, not ideology.

Brute-force coordination burns power.

Operational resources should be intentionally allocated.

Data-parallel work is the default easy scale path.

Replication helps throughput before it helps memory.

Parallelism consumes usable batch.

Batch scale has diminishing returns.

State is bigger than truth.

Hidden state can dominate system cost.

Shard expensive state; synchronize shared output.

Rearranging communication can create free wins.

Stream partial state to owners instead of hoarding it centrally.

Process, route, release.

Load full context only when needed, then release.

Good runtime hides complexity without hiding trace.

Materialize working state just in time.

Extra work can be hidden if scheduled correctly.

Overlap waiting with useful work.

Prefetch the next needed context.

Cleanup is architecture.

No single scaling strategy dominates.

When one scaling resource saturates, switch axes.

Moving source and moving work product are different.

Pipeline parallelism maps to staged workflows.

Staged workflows create bubbles.

Move coherent chunks early.

Pipelines need work in flight.

Slow links favor staged point-to-point workflows.

Slow boundaries should be pipeline boundaries.

Scheduling is architecture.

Separate blocking work from deferrable work.

Fill bubbles with deferrable work.

Width splitting is high-communication work.

Tightly coupled cognition needs fast coordination.

Assembly and accountability often reverse.

Replicate cheap common substrate.

Granularity has overhead.

Working context can dominate memory.

Peak state appears during transitions.

Working-state strategy is separate from source-state strategy.

Not all state shards cleanly.

Long sequences need sequence sharding.

Shard active memory, not just source memory.

Long context needs explicit parallelism.

Use compute to buy memory.

Extra computation can unlock capacity.

Specialists need routing infrastructure.

Specialization has dispatch cost.

Use natural specialization first.

Routing is infrastructure.

Critical expert handoffs need fast dispatch.

Operational detail becomes strategy.

Composition creates interactions.

Parallelism can be partial.

Different subproblems need different scaling axes.

One global parallelism setting is too crude.

Every scaling strategy has a red cell.

Scaling is composition.

Fit first, then maximize parallel throughput.

Use the simplest scaling axis that works.

High-bandwidth collaboration needs proximity.

Slow handoffs want pipelines, not constant sync.

Long history needs context-specific parallelism.

Hide communication under useful work.

Find the roofline of the workflow.

Throughput strategy depends on work volume.

Real systems use composed parallelism.

Architecture shape determines scaling shape.

Distributed systems need redundancy.

Scale requires fault tolerance.

Execution configuration is architecture.

Standard configurations speed build work.

Integration details matter.

Exploit easy parallelism first.

Tight coupling should have bounded group size.

Expert scaling requires expert infrastructure.

Complexity is justified only by bottleneck relief.

Agent systems need multi-axis parallelism.

Architecture memory needs distributed processing.

CNS is scheduler plus router, not monolith.

Provider workspace needs FSDP-like context loading.

Patient-CNS should activate by bottleneck and risk.

Workforce is distributed compute.

Incident response needs parallelism strategy.

Domain boundaries should follow communication frequency.

What not to import blindly

Do not turn GPU/TPU details into OMNI doctrine directly.

Do not use “4D parallelism” as complexity theater.

Do not add multiple execution axes unless each relieves a real bottleneck.

Do not assume FSDP-like context loading solves activation/working-memory problems.

Do not all-gather full context for routine workflows.

Do not pipeline without addressing bubbles.

Do not micro-batch work so small that semantic context disappears.

Do not split tightly coupled decisions across slow organizational or external links.

Do not assume expert routing is simple just because specialists exist.

Do not route to experts without dispatch, load balancing, fallback, and trace.

Do not replicate shared state without synchronization.

Do not hide runtime complexity from audit traces.

Do not forget cleanup/freeing of temporary context.

Do not confuse fitting in memory with running efficiently.

Do not optimize average throughput while ignoring failure/retry/redundancy.

Do not treat model-training examples as exact analogues to care workflows.

Do not assume one global decomposition works across all OMNI domains.

Do not let standard configurations become unreviewed dogma.

Do-not-miss lesson

This source gives OMNI a multi-axis scaling law:

When a system grows, there is no single parallelism strategy that wins everywhere. You must compose strategies based on what is scarce: memory, compute, batch, context, bandwidth, review capacity, latency, or fault tolerance. The mature system knows what to replicate, what to shard, what to pipeline, what to expert-route, what to context-parallel, what to prefetch, what to free, and where communication topology makes a boundary cheap or expensive.

Shortest OMNI version

OMNI should scale like a composed distributed system: use data-parallel work where cases are independent; FSDP-like just-in-time context loading where state is too large; pipelines across slow/staged boundaries; expert routing for specialized work; context parallelism for long histories; micro-batches to keep workflows busy; prefetching to hide waits; cleanup to control working memory; and topology-aware domain boundaries so communication cost does not silently eat the company.

My hard read

Full semantic. Top-tier systems source.

This belongs with Lectures 5, 6, and 7, but it is more mature than all three.

Lecture 5 says memory movement is the bottleneck.

Lecture 6 says inspect what high-level abstractions compile into.

Lecture 7 says distributed work needs collectives.

Lecture 8 says real frontier systems compose all of it: memory sharding, pipeline scheduling, expert dispatch, tensor/context parallelism, activation strategy, hardware topology, roofline analysis, and redundancy.

The strongest OMNI line:

OMNI should not scale by giving every agent everything, sending every task everywhere, or routing every decision through one central brain. It should scale by composing execution axes: replicate the common substrate, shard oversized state, pipeline slow boundaries, route to experts only when needed, parallelize long context explicitly, prefetch what comes next, free what is temporary, measure the real bottleneck, and design domain boundaries around the cost of communication.


&nbsp;



⬆️⬆️⬆️  END Review 003  ⬆️⬆️⬆️

## §4 — Analysis pointers  *(agent fills at CLOSEOUT once analyzed — the "linked" step; leave `TK` until then)*
- EVRUN(s): `TK` (e.g. `EVRUN-2026-000001` ai-corpus) · concept_registry: `TK` (`{EVRUN}_{slug}_concept_registry_and_routing_map.md`) · source_anchor_ledger: `TK` (`…_source_anchor_ledger_receipts_only.md`) · per-source deep-read: §3 Review 003 (this file) · impact: `TK` (§A/§B/§C/CNS/Build-OS/security/domain-contracts/surfaces) · promotion: `TK` (watch | promote | reject | no-op)

## §5 — Change log
- `2026-06-11` — source file created.

> Authority/retrieval labels are defined once in `../../00_evidence_router.md` (operating principle #9 + §8). Sections carry their `layer:` inline; the legend is not restated per file.
