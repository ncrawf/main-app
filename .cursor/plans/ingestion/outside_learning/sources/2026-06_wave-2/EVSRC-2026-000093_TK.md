# EVSRC-2026-000093 — <title or TK>

Document type: `source_capture` · Lane / reservoir: `outside_learning`
**Status:** `raw_dropped` → `normalized` → `reviewed` → `analyzed` → `promoted | deferred`  ·  **current: `analyzed`**
Authority: `evidence_nonbinding` — captured source + interpretations only; binds nothing until routed + promoted.

> **HOW TO USE THIS FILE — two kinds of marks:**
> • `TK` = a field the normalizer (Opus) fills from the screenshot — **leave it alone.**
> • **⬇️ PASTE blocks = the ONLY spots Nick touches.** Exactly **3**: transcript (§1), **Knox read (§3 Review 001)**, gut note (§3 Review 002, optional). Plus: **drop the screenshot in chat** → Opus fills §0.
> Copy this into `sources/<YYYY-MM>/`, rename `EVSRC-2026-000093_TK.md` (id = highest EVSRC + 1), add a row to `../../00_index.md`.
>
> **Per-source flow (the standard):** Nick pastes transcript (§1) + Knox's strategic read (§3 **Review 001** — paste it AS-IS, however rich Knox makes it; do NOT reformat into fields) + optional gut note (§3 Review 002). Then the agent (Opus) writes **§3 Review 003 — formal deep extraction** (formalizing Review 001, NOT rediscovering it), updates the run's **concept registry** (cross-source synthesis), the coverage matrix, and fills **§4 pointers** at closeout. **The per-source deep read lives HERE in §3 — never in a separate sidecar/extraction file** (`GRD-044`). Cross-source synthesis lives in the EVRUN concept registry.

## §0 — Source identity / metadata  *(normalizer fills from the screenshot — leave `TK`)*
- evsrc_id: `EVSRC-2026-000093`  ·  filename: `EVSRC-2026-000093_TK.md`
- source_platform: `YouTube`  ·  source_url: `https://youtube.com/watch?v=l5BKi32IEa8`  ·  source_title: `Stanford CS25: Transformers United V6 | The Ultra-Scale Talk: Scaling Training to Thousands of GPUs`
- channel_or_org: `Stanford Online`  ·  speaker: `Nouamane Tazi`  ·  published_at: `2026-05-11`
- captured_at: `2026-06-11`  ·  captured_by: `Nick`  ·  capture_method: `transcript paste + screenshot`
- content_type: `seminar / technical lecture`  ·  source_reliability_context: `practitioner / vendor`  ·  topic_tags_light: `[ultra_scale_training, distributed_training, data_parallelism, fsdp, zero, tensor_parallelism, sequence_parallelism, pipeline_parallelism, context_parallelism, expert_parallelism, mixture_of_experts, gpu_clusters, communication_compute_overlap, model_scaling, hugging_face, nanotron]`

## §0.1 — People / authorship / authority context  *(normalizer fills from screenshot + description + transcript — this is what makes a source a reservoir, not a transcript dump)*
- primary speaker(s):
  - name: `Nouamane Tazi` · role_in_source: `guest speaker / lecturer` · affiliation_at_publication: `Hugging Face` · speaker_type: `practitioner / vendor / educator` · authority_context: `Lead author of the Ultra-Scale Playbook and core developer of Nanotron, Hugging Face's open-source distributed-training library. High relevance for practical distributed-training mechanics, GPU scaling, parallelism strategies, MoE scaling, communication bottlenecks, and infra tradeoffs. Technical operator/practitioner source, not strategic/company-building doctrine.` · identity_confidence: `high_from_screenshot`
- publisher / channel: `Stanford Online`  ·  interviewer / moderator / host: `Stanford CS25 instructors / seminar hosts`
- event_context: `Stanford CS25: Transformers United V6 seminar; seminar date shown as April 23, 2026; YouTube published May 11, 2026`  ·  perspective / conflict notes: `Hugging Face / open-source distributed-training perspective. Strong practical infra value; niche by views but high technical specificity (visible context: 1,266 views; 47 likes at capture). Useful to ground OMNI's AI-substrate / Build-OS assumptions, not to drive product thesis prose.`

> **Authority is descriptive, not worship** (`GRD-039`): record *who* said it (raises relevance), but every claim still routes through evidence → interpretation → gated promotion. (Clinical reservoir extends this block heavily — authors / affiliations / journal / DOI-PMID / study-type / evidence-level / COI / retraction — `FWREG-006`.)

## §0.5 — Processing checklist
**Nick drops:** [ ] transcript → §1 · [ ] screenshot in chat · [ ] **Knox strategic read → §3 Review 001** (paste as-is) · [ ] (optional) gut note → §3 Review 002
**Agent (Opus) does:** [ ] id+filename · [ ] §0 metadata from screenshot · [ ] takes labeled · [ ] **§3 Review 003 formal deep extraction** (formalize Review 001) · [ ] update EVRUN concept registry (cross-source) · [ ] update coverage matrix · [ ] **fill §4 pointers (closeout)** · [ ] NO sidecar extraction file (`GRD-044`)

## §1 — Verbatim transcript  ·  layer: `raw_source`  ·  IMMUTABLE

⬇️⬇️⬇️  PASTE THE FULL TRANSCRIPT BELOW  (do not summarize)  ⬇️⬇️⬇️

Search in video
0:00
0:05
Today, it's our pleasure to host Nouamane Tazi from Hugging Face. He's the lead author of the Ultra-Scale Playbook
0:13
and a core developer of Nanotronics, Hugging Face's open source distributed training library.
0:19
His work expands projects like Star StarCoder 2, Small LLM 3, and mixture of expert scaling
0:25
with several initiatives. And he is passionate about making large scale training practical and accessible.
0:30
So without further ado, let me hand it off to him. Thank you so much for the invitation.
0:36
So hello, everyone. My name is Nouamane Tazi.
0:43
I've been working with Hugging Face now for four years. And the talk for today is Scaling Training
0:49
to Thousands of GPUs. So today's talk is going to be mainly inspired by the book
0:58
that Hugging Face made. It's called Ultra-Scale Playbook. So make sure to check it out. It's available online and there is a printed version
1:05
if you need it. But I also added some elements since the book now dates from one year ago, especially
1:12
regarding MOE scaling and some new things that we can take a look at.
1:20
So without further ado, let's start with the first question is, why does scaling matter?
1:25
And if we take a look at the latest releases of the latest LLMs, we can see that the trend is still
1:33
to train larger and larger. And why is that? That's also because we can see that there
1:38
is a correlation with intelligence. It seems that the bigger the LLMs are, the smarter they are.
1:45
And there's just an example from this week, Kimi K2.6, which is also one trillion parameter model.
1:55
So to take just some numbers, models of these days are one trillion parameter large, or even larger.
2:03
They get trained on 15 trillion training tokens and for context lengths as far as one million.
2:11
So how can we do that? Now, to take some quick overview about what
2:20
does this entail from a infra point of view. So if you want to train this kind of model,
2:27
first, you need to load the data, which is about 15 trillion. Imagine that's stored on a storage somewhere,
2:34
and you need to load that continuously in your training loop. And each training iteration, even if the model
2:40
is as big as one trillion parameter model, needs to take about one second. And you're limited by the memory within each GPU or TPU
2:49
or whatever you're using. And you need to save checkpoints to that storage every now and then.
2:55
So you're putting a lot of pressure into these GPUs.
3:00
So the talk of today is going to be mainly focused on training, the training iteration or the training.
3:09
So let's start quickly on one GPU. So there is a model. We do forward, backward.
3:15
We compute gradients, and then we do the optimizer step and we have an updated model.
3:20
But for our case, we usually have global batch size of one million to 10 million, even like 50 million tokens.
3:28
So that, of course, doesn't fit on a single training iteration. So what we do usually is we can do gradient accumulation.
3:35
So we're going to divide the batch size to smaller batch sizes. And we're going to do multiple forward and backwards.
3:41
And we're going to accumulate gradients sequentially. The problem with this approach is
3:47
we're going to assume that the model already fits in memory, which is not always the case. Models now are so big, one trillion.
3:54
And we said that we're still limited by the available VRAM in each accelerator you're going to use.
4:00
So how can we solve this with more GPUs? The other thing is that if you train sequentially
4:07
with this approach, you're going to need to wait for a long time. So if we have more GPUs, how can we speed this up?
4:15
And for this, there is multiple forms of parallelism we can use. And each one of them is used in a specific use case.
4:21
And hopefully today, you can get at least an intuition which parallelism you can use for which use case.
4:27
And to help you-- so basically for today, I'm going
4:32
to mainly focus on the first two parallelisms because they're going to explain just the intuition of how we can
4:39
think about these parallelism. And the final three, we're going to just quickly skim over them
4:46
because there's not enough time. And I think that the approach can be generalized.
4:54
So before starting, a few notes.
5:00
So this approach can be applied to an accelerator, GPUs, TPUs. So in the slides, I use GPUs, but the same
5:06
can be applied for anything. And it's useful for pre-training, post-training,
5:11
distillation training, inference. So any type of workload can benefit from these parallelisms.
5:18
And it's applicable. You don't need 1,000 GPUs to benefit from this. As long as you have two, at least,
5:24
you can benefit from these parallelism. So naively, what we want to do?
5:30
So we said that the model doesn't fit. So ideally, we want to be able to shard the model, so either vertically or horizontally.
5:37
And that means we're going to also shard the gradients and the optimizer and shard the data.
5:43
Because we don't want to wait too long to train sequentially on data.
5:49
So let's deep dive into scaling. And first of all, we're going to start with data parallelism. And I'm assuming this is the basic form
5:55
that everyone is using. So I'm going to go quickly over it. So instead of training sequentially
6:02
over multiple batches, I'm going to feed each batch to a GPU.
6:07
So I'm going to have different batches per GPU. Thus, the name data parallelism. I'm going to shard the data.
6:14
And of course, if I have different data per GPU, each GPU is going to compute different gradients.
6:21
And to keep the same model duplicated on each GPU, I'm going to need to synchronize these gradients.
6:27
And how am I going to do that? It's going to be the same as accumulating gradient, but it's going to be in a distributed fashion.
6:34
Thus, we're going to need to use the AllReduce collective operation, which is just a distributed
6:41
sum over multiple GPUs. So we end up with the same gradients,
6:47
and thus, we can do the same optimizer step over all GPUs. And if you want to take a look at the code,
6:54
it's going to look like this. We can use the DDP class by PyTorch. And when you wrap your model with it,
7:02
when you do the forward and backward, it automatically handles the AllReduce part in the backward.
7:09
So there is a small problem with this. When we do the AllReduce, the GPU stay idle in that time.
7:18
And for that, there is a very nice tool called torch.profiler
7:23
that helps you visualize what the timeline is basically, or the workflow for the CPU and GPU.
7:30
And for example, if we apply it on a naive implementation of DDP, we can see that there is a CPU stream that is forward
7:36
and backward. And then there is the GPU streams, GPU computation and communication.
7:41
So there is two things. So the biggest thing that we want is we don't want GPU to stay idle.
7:48
So we definitely want the GPU computation stream to be always full, which is not the case in here,
7:56
as we can see here. The other thing is we need to make sure that the CPU is always ahead of the GPU,
8:03
so that we can always schedule kernels beforehand. And the third thing is, for example, in this case of DDP,
8:10
we can see that the AllReduce is not overlapped with the computation. So ideally, we want to overlap this communication
8:19
with computation. So this is a simplified diagram of the previous.
8:26
So how can we overlap this AllReduce?
8:32
We can just start AllReduce in the gradient once a bucket of the gradients is computed.
8:40
Basically when you do the forward, you start doing the backward from the last layers.
8:46
So you don't need to do the backward of the entire model to be able to AllReduce them.
8:52
So once, for example, you compute the gradient of the last layer, you can already start the AllReduce.
8:58
And this is what we do in practice. So for example, if 2 is the last layer, once the gradients of the last layer is computed,
9:06
we can AllReduce it and we can continue doing the backward. And this bucket can-- we control the size of this bucket
9:16
in PyTorch via this argument bucket cup micro-batch--
9:22
microbits. So you can control how big or how small the bucket should be, so that you have a better overlap of communication
9:30
and computation. And this is what goes through the mind of any engineer who
9:36
tries to scale models. Basically, when you have a lot of GPUs, you definitely want to overlap efficiently your computation
9:44
with communication so that you avoid any idle time here for the GPU computation and to benefit
9:53
of the tensor cores of GPUs. So this is the end goal of this presentation.
10:00
So quickly, you can go over a recap of DP. So it's easy to implement. It's very efficiently overlapped.
10:07
It's really good. And it's model agnostic, right? So we can just wrap any model and it's going to automatically reduce the gradients in the backward.
10:16
But the problem is that the optimizer step is duplicated across GPUs. Because if you have the same model in each of your GPU,
10:24
all of them are going to do the same optimizer step, which is bad.
10:29
It's duplicated work. The second thing is that if you scale more and more
10:34
in the number of GPUs, you're also going to need more and more batches of data. And this is what we call global batch size.
10:41
So your global batch size is also going to scale. But of course, you can't just scale indefinitely.
10:48
We're limited by tens of millions of tokens. And so you can just apply DP-- if you have,
10:54
I don't know, one million GPUs, you can just use the DP over all of your GPUs.
10:59
And the third thing, which is the biggest bottleneck, is that DP assumes that the training step fits in memory.
11:07
And this is not always the case, unfortunately. And so we need other approaches to optimize memory.
11:13
And we can look at the first one, which is DP ZeRO. So from the name, DP ZeRO 1 tries
11:21
to solve the optimizer step. Basically, we said that in DP we're
11:26
going to duplicate the model and all of them are going to do the same optimizer step. So how can we reduce the memory at least of the optimizer states
11:34
so that they're not duplicated and each GPU can do a different part of the optimizer-- of the optimization?
11:44
So the simplest approach is instead of having the optimizer
11:52
states of all the models, I'm going to shard them across my GPUs.
11:57
And when I do the backward step, I'm only going to reduce scatter the gradients.
12:03
So I'm not going to have all the reduce here. I'm only going to have reduce scatter. And then I'm going to do the optimizer step for each shard.
12:10
And this is what's really good about ZeRO 1. We're not going to have duplicated work. And once I have my updated parameter shards,
12:20
I need to all gather them before the next step. So this is ZeRO 1 in brief.
12:27
And there is s very important intuition that you can learn from this.
12:33
So before in Vanilla DP, we had an AllReduce in the backward, right? In here, we had an AllReduce.
12:39
And the trick that we made for ZeRO 1 to work is that we replaced the AllReduce with a reduce scatter
12:46
and an all gather. And this works great. And we're going to reuse this trick in the future.
12:54
Why? Because AllReduce, essentially, is just a reduce-scatter
12:59
plus an all-gather. So basically, we didn't add any communication.
13:07
It's the same communication. It's just that instead of doing the AllReduce, I did reduce-scatter first so that-- to compute
13:15
the gradients first. And then I can do the optimizer step. And then I all-gather the parameters.
13:24
The second thing is for the gradients, I didn't use all of them, right?
13:29
So when I did the reduce scatter, I only used the shards that I optimized later.
13:39
So I can just throw the parts that I don't need from the gradients. And this is ZeRO 2.
13:46
So ZeRO 2 optimizes or saves memory even further by just discarding the gradients that I don't use.
13:54
And this is the difference between ZeRO 1 and ZeRO 2. It's a small difference. It's just that I don't need to keep the gradients.
14:01
So it's easier said than done. Because in the implementation, it's annoying to just discard things.
14:08
So people usually just use ZeRO 1 and they keep the gradients.
14:13
And also an important note since now there is Muon optimizer,
14:20
optimizer requires the full tensors grads. So previously, people would take their entire parameters,
14:28
and they would just shard them. They would flatten everything. So we don't keep the notion of a tensor.
14:34
You just flatten all your parameters, and then you would shard them along the number of GPUs
14:41
you have. But the problem is for Muon, you can just split your parameters. You want your tensors to be full so that you can do the Newton
14:52
optimization on them. So the thing is here, when you want to split your parameters or your gradients on your GPUs,
15:01
you need to make sure to keep your tensors full. So for example, I'm going to give the first, I don't know,
15:08
five full tensors to the first GPU and the five-- the second five tensors to the second GPU.
15:16
So this is for Muon, but also for other optimizers. And lastly, for ZeRO 3, I also want
15:23
to save memory of the parameters. And this is the craziest part, because how can I save memory
15:32
by sharding the model's memory when I need them in the forward and the backward?
15:39
And there is a very nice trick that the ZeRO 3 engineers came
15:45
from-- came with is when I do the optimizer step,
15:50
I don't need to all gather right away. I can just keep the shards of the model on each GPU,
15:59
and whenever I need to do the forward of that layer, for example, for the first layer,
16:05
I'm just going to ask for these other shards from the other GPUs.
16:11
So for example, for the first layer, I'm going to ask for the other shards. I'm going to do the forward.
16:17
And then I'm going to free the other parameters. And then for the other layer, I'm going to do the same thing.
16:23
So this looks very slow, right? Because on each layer, you need to ask for all the--
16:31
for the rest of your parameters from your other GPUs, do the forward and then free them.
16:37
But in practice, it works very well. And the reason is--
16:43
by the way, it's the same for the backward. So for the backward, we also just do
16:48
the all-gather of the weights and then compute the backward and we free the weights.
16:56
So the reason is we prefetch the next layer
17:03
while computing the current layer. So this is the trick. So I had previously an all-gather
17:10
here after the optimizer step. But now, I just separated the all-gather on multiple layers.
17:18
And for example, for the first layer, I'm going to all-gather it here. I'm going to do the compute. And while I'm computing for the forward of the first layer,
17:26
I'm already all-gathering the next layer, et cetera.
17:32
And then I free it, et cetera. And so concretely, I never have more than two
17:40
what we call FSDP modules or FSDP units in my memory, because I always free one when I all-gather the other one.
17:50
And the same thing for the backward. And so I still do the distributed optimizer and I
17:56
repeat. So this is basically the best form of DP that we can have. Why?
18:02
Because I efficiently optimize my memory. I saved memory on optimizer states,
18:08
on gradients, and on the model. And I also prefetch.
18:14
So everything is very well-overlapped. And besides that, similar to DP's bucket size,
18:19
I can also control the size of the buckets, which
18:25
we call the size of the FSDP modules that we have.
18:31
And so for the code-- so there is two-- by the way, ZeRO 3 is also called FSDP. FSDP is fully sharded data parallel.
18:39
And there is two versions in PyTorch. So FSDP1, we used to just have a class FSDP
18:47
where we can wrap our model and we can define the wrap policy, which
18:52
is similar to the bucket size. And for FSDP2, there is now a function called fully shard.
18:59
And you just call it over your model and it's going to automatically transform
19:04
your modules to FSDP modules. So the difference between the two versions, the first one,
19:11
as I said, it flattens all of your parameters and we already talked of the problems of that.
19:18
For example, Muon and some optimizers, they need full tensors, and this is bad. So ideally, when you shard your parameters,
19:25
you want to keep full tensors on each GPU. So FSDP2 enables that.
19:33
The second thing is FSDP2 uses DTensor, which is a new utility in PyTorch.
19:43
And it helps being combined with other forms of parallelism as we're going to see.
19:49
And so to recap, please compare ZeRO 2 with ZeRO 3.
19:57
So in ZeRO 2, we had a big all-gather here to all-gather the parameters that we optimized.
20:05
And in ZeRO 3, I just divided this big all-gather
20:12
into smaller all gathers of the different FSDP modules. So it allows me to overlap this all gather with the forwards.
20:23
And I can control the size of the FSDP modules. And it's also model agnostic, because I don't care about the architecture of my model.
20:30
And it's just great. It works very good. So the pros and cons of DP ZeRO 3.
20:38
It creates memory for comms. So DP Vanilla, I didn't optimize memory at all.
20:44
I just assumed that the model fits in memory. But if I needed to save memory, I can just do ZeRO 1 or ZeRO 3
20:52
or ZeRO 2 or ZeRO 3. And this is something to pay attention to. So a lot of people just throw FSDP2 on all of their models.
21:00
And this is not a good thing. Because if your model fits on GPUs with ZeRO 1,
21:06
you don't need ZeRO 3 because ZeRO 3 you're going to add more communications than you're going
21:12
to make your training slower. So just use the ZeRO degree that you need to save
21:18
memory, because there is no other benefits. If ZeRO 1 fits, it's going to be faster than ZeRO 3.
21:25
So the second thing is the great overlap of comms with compute. As we've seen, everything is well-overlapped.
21:31
And the third thing is model-agnostic. So there is no headache of implementation. You can just-- whatever the PyTorch module is,
21:39
you can just wrap it with FSDP. You don't care if it's a state model. You don't care if it's anything.
21:45
It's going to work out of the box. The negatives, though, is that at scale,
21:52
since you need to all-gather the parameters for each FSDP module,
22:00
if you scale it too much, even if it's overlapped, you can't hide the communication anymore,
22:07
because it's not perfect. If you have a very slow network, you cannot overlap that anymore.
22:15
So there is a solution for that, which is called hybrid sharding or HSDP.
22:21
So you would use FSDP on a slow-- on a smaller circle. And on the larger circle, you can use Vanilla DP.
22:31
So this is something you can look up later. And the other thing is ZeRO 3 is--
22:38
we're still in data parallel. So we're still scaling with batches. So every GPU has a different batch.
22:46
And we still have this restriction. We can't scale indefinitely.
22:52
So what if I reached my global batch size?
22:57
So I have, I don't know, 10,000 GPUs. And at 1,000 GPUs, I already reach my global batch size.
23:06
I need another form of parallelism. So let's take a look at TP.
23:12
And the motivation behind tensor parallelism is that if I have a model, transformer model, for example,
23:19
I want to keep the same batch, so the same inputs, the same data, and chart the model so
23:25
that I do the same computation as if I had one GPU. So basically, instead of doing the computation with one GPU.
23:33
I would do it with two. And essentially, I'm going to shard
23:40
the memory for my model for my gradients optimizer states by the number of GPUs I have.
23:46
So the question is-- and this is a very fun question
23:55
that people like to ask in interviews, et cetera. So do we cope-- do we keep full or half activations
24:02
in tensor parallel? And we're going to answer that after a bit. So let's start with an easy example, which
24:08
is matrix multiplication. So if we take X multiplied by Y-- by W here, a simple approach, and we want
24:18
to parallelize that onto GPUs. We can just split our Matrix on columns.
24:25
And then I can all-gather the outputs.
24:31
Let's take the example of two matmuls now. So I have Y1, which is X multiplied YW1, and Y2, which
24:38
is Y2 multiplied by W2. So if I split my first weight by columns
24:44
and if I split the second one by rows, I can do the same computation, which is two multiplications.
24:52
And then I do an AllReduce here, and I would get the same result and the same computation
24:59
as if I did this with one GPU. So this is the power of tensor parallelism,
25:06
and this is the intuition behind it. If I had one GPU, I would do two matrix multiplication
25:13
with the full matrices. So I managed to distribute this operation on two GPUs
25:23
by splitting the weights. So each GPU only has half the weights
25:29
and each GPU only does half the compute.
25:34
But the caveat of this is that I need to do a distributed communication on AllReduce
25:42
to keep the correctness. How does this work for the backward, though?
25:49
It's the same thing. And again, I'm going to assume that I have the same upstream grad, and this is a big headache.
25:57
We can talk about it in a bit. So if we assume that I have the same upstream grad, which
26:03
is this, the Y2 and Y2, similarly, it's just going to be multiplied by the transpose of W2 and W1.
26:11
This is, of course, to compute the dx, which is the Dout, the out gradient.
26:19
So again, it's just-- I'm going to do two multiplication. And again, it's just half the weights.
26:26
And again, I just need to do an AllReduce. So to efficiently distribute two matrix multiplications
26:35
on multiple GPUs, I'm going to need one AllReduce in the forward and one AllReduce
26:41
in the backward under the assumption that I have the same upstream gradients, which is the outputs,
26:51
and I had the same inputs. So let's apply this on our MLP.
26:57
So in MLP, we have, for example, Y activation of X times A.
27:03
And then I multiply it by another matrix. So I always going to have two matrix multiplications.
27:09
So I apply the same trick. The first one is going to be a column linear. The second one is a row linear.
27:15
And I need an AllReduce to keep the math correct. And as we said, we assume-- and this is a big assumption.
27:23
In our transformer, we assume that I'm going to always have the same activations, the same inputs,
27:29
the same data, and I need to have the same output and later
27:34
the same upstream gradients. So all the operations after that must
27:39
be the same in the two GPUs.
27:45
And another thing to basically what we said, we're going to divide the compute by half.
27:52
So A1 and A2 are halves of the original matrix A, right?
27:58
So hidden_dim is sharded in here.
28:04
And this is what we just said. We need the same input. And we need the same upstream gradient
28:11
to keep the math correct. Let's take an example of attention.
28:16
So for attention, similarly, we have two matrix multiplication, which is the QKV projections and the output projections.
28:24
So the question would be should we shard along a hidden dimension or number of heads or both.
28:33
And what does it mean? So the question you really want to ask--
28:38
and this is a very generic question. If you have a model and you don't know how to implement tensor parallelism on it,
28:45
or you want to check that the implementation is correct, you should really ask, would GPUs
28:51
compute the same activations as without TP? So let's say I sharded a long head dimension.
29:00
Then when I do the softmax, which is Q times K, it's going to have a reduced hidden dimension.
29:06
And that would impact the softmax calculation. So it wouldn't give the same results as if I had a TP one.
29:16
But if I sharded a long number of heads, since the heads are independent, I
29:23
would get the same activations as if I didn't use TP.
29:30
So this is really the whole thing. So we showed a long number of heads
29:36
since the attention heads are independent to keep attention
29:43
correct. And again, we need the same inputs, and we need the same upstream gradients.
29:51
So as a recap, the pros is that we shard the model and compute across GPUs and we're
29:58
memory and sample efficient. So at the same time, I sharded memory.
30:03
So model parameters gradients and optimizer states. And I didn't need to have multiple batches.
30:11
So I used the same sample across my GPUs. The second thing I didn't need an AllReduce for the gradients
30:21
because all GPUs I'm assuming they're doing the same work. It's like I have one big GPU that I
30:28
distributed on smaller GPUs. And the cons is that communication is heavy
30:34
because at each attention, at each MLP block, I need to do an AllReduce for the forward
30:41
and for the backward. The second thing is-- and this is something we can see later.
30:48
The parameters that are outside the TP region, which is the region where the hidden dimension is sharded,
30:55
should stay synchronized. Otherwise, I won't have the same inputs
31:01
and the same upstream gradients because this is a big assumption for the matmuls to stay correct.
31:08
So in practice, the parameters that are outside the TP region,
31:14
they need to stay the same. They need to stay duplicated. So in practice, we all reduce their grads.
31:22
And this is something that screws up training when you use TP.
31:29
And just due to numerical precision-- because theoretically, you don't need to do this,
31:34
but due to numerical imprecisions, they drift apart. So in practice, we always use their grads.
31:41
And the third thing is, of course, complex implementation. And it's not easy if you change the architecture.
31:48
Let's say you want to use MLA or some other fancy attention.
31:54
It's a headache to apply tensor parallelism on it.
32:00
So let's take a look at sequence parallelism. So what's the motivation behind sequence parallelism,
32:06
which is a flavor to TP? So in TP, we've seen that we have
32:14
this TP region, which is the attention, and this TP region, which is the MLP. And we do the AllReduce here.
32:22
So the second layers, which are the row layers, they have an AllReduce. OK, cool. So in the TP region, the shape is a sequence patch
32:31
and we shard the hidden dimension we set. And after the second matrix in each TP region,
32:39
I'm going to get back the original shapes. So I'm going to get back the original hidden dimension.
32:46
So the question is these parts are duplicated across GPUs
32:51
and it's annoying, as we said, to keep them synchronized. As we said in practice, we need to always use this layer norm.
32:59
And if I had some other operation, I also need to make sure it's replicated.
33:06
So can we just distribute them as well? And this is what sequence parallelism tries to solve.
33:14
And we're going to use the trick that we talked about earlier. So we have an AllReduce. And we know that AllReduce, its reduce-scatter and all-gather.
33:23
So how can we apply it in this case? This is what sequence parallelism is.
33:30
So this AllReduce, I'm going to replace it with the reduce-scatter.
33:36
And I'm going to-- this identity-- we can call this an identity. This identity, I'm going to transform it to all-gather.
33:44
So in the TPU region, I'm going to shard my activations
33:50
along the hidden dimension. And then I reduce scatter along the sequence dimension.
33:56
So in this sequence region, SP region, I have my sequence, which is sharded by TP.
34:04
And then I all-gather it again to have H, which is sharded.
34:14
So basically, I play between sharding along the hidden dimension and along the sequence dimension.
34:21
So this is moving from TP to TP with SP. And what this helps me do is multiple things.
34:30
So first of all, the activations that are stored. So before, I had to store very big activations here,
34:36
which are SBH, but thanks to sequence parallelism my size is always divided by TP.
34:45
So this helps me avoid big activations.
34:50
The second thing is it's the same amount of communications
34:56
since AllReduce, as we've seen, it's just the reduce-scatter and all-gather.
35:01
So I didn't have to add any communication. And the last question is about layer norm.
35:07
So since we did this operation, should we think layer norms
35:14
grids in this case? And the answer is no.
35:20
And for this, let's take a look at the backward. So similarly to what we did before with the matmuls,
35:28
you can verify later that the backward of a reduce scatter is all gather and the backward of an all gather
35:36
is reduce-scatter. Basically reduce-scatter and all-gather, they are inverse of each other.
35:44
So if this is the forward, the backward becomes like this. So the backward is I'm going to do all gather here
35:51
and then reduce scatter here cetera. So we notice here that before layer norms
35:56
we have reduced scatter. And thanks to these reduced scatter that I now added they
36:05
take care of synchronizing the gradients. So it's really beautiful that it works out like this.
36:11
So thanks to the fact that now the GPUs, they don't do the same work, they see different batches.
36:19
And thanks to the fact that in the backward I have a reduce scatter here now,
36:25
instead of just assuming the perfect case that it's an identity and that the layer norm would always
36:32
stay synchronized. I just have this all-gather and reduce-scatter,
36:37
and they take care of synchronizing my gradients.
36:42
So you can verify this, the inverse reduce-scatter
36:49
and all-gather later. And the pros, cons, so let's take a recap.
36:54
So the pros is that now we're sharding the model and compute across GPUs.
37:00
And we're being memory and sample efficient especially sample efficient, which is what we didn't have in DP.
37:07
And again, we don't need grad AllReduce because GPUs are essentially doing the same work.
37:14
And thanks to SP, I don't need to AllReduce layer norms as well because they now have the reduce-scatter
37:21
in the backward. But we still have the same problem. We're communication heavy and the implementation is complex.
37:29
And for this communication heavy, this is why it's recommended if you take a look at the different distributed training libraries.
37:38
They recommend that you use TP within a single node. So if you have a node of 8 GPUs, they
37:45
say that it's better to use TP lower than 8 so that the communications for TP stay within a node,
37:53
so that when you do forwards and backwards, you don't get stuck waiting for the AllReduces
38:00
or the reduce-scatter and all-gather in forwards and backwards. So now, we've seen two axes of parallelism.
38:09
And before generalizing to the others, let's see what it means to combine them.
38:16
So we've seen the data parallel, which shards data.
38:21
Here, we have data parallel size three. And we've seen tensor parallelism, which shards
38:27
the model into dimensions.
38:34
So what does it mean to combine these two? Well, the cool thing about the forms of
38:39
parallelism that we've seen that they are orthogonal of each other. So if batch is sharded along the data axis, it's replicated.
38:50
So it's replicated here along the model axis and vice versa. If the model is sharded along the TP axis,
38:59
it's replicated along the data axis. So this helps us make a mesh of all the parallelism.
39:08
Now, we've only seen two, but later we can see five or there's even six or seven.
39:13
It depends on how many partners you want. And in the code, if you look at TorchTitan or megatron
39:21
or narratron, you're going to see that-- at first, they init the process group,
39:27
so they init all the WORLD_SIZE. Let's say you have six GPUs here.
39:32
So you're going to create your TP groups and your DP groups. And later, you can just pick on which
39:39
axis you want to do the communication operations. So for example, I want to already use along the TP group.
39:48
So I'm going to AllReduce like this and AllReduce like this along all the other axes.
39:53
So this is the beauty of this parallelism. We make sure that they are orthogonal of each other.
40:01
And now, we've gone through the first two big parallelisms,
40:08
the last three I'm going to go fast over them. So for PP, it's much easier than TP.
40:16
So we have our layers like this. And instead of sharding them horizontally,
40:24
I'm going to share them vertically. So I'm going to put some layers on the first GPU, some layers on the second GPU.
40:29
But you can see here that the problem is for GPU 1, at the beginning, it doesn't have any activations.
40:36
So it's just going to say keep waiting for the first GPU and for the first GPU after it goes--
40:45
after the forward goes through the first four layers, it just waits for the other GPU.
40:50
So how can we solve this? And for pipeline parallelism, there
40:55
is what we call PP schedules pipeline parallelism schedulers. So the schedulers, they define how
41:03
your GPUs are going to do-- are going to load the data, and they're going to communicate activations and gradients
41:10
with the other ranks. So let's start with a simple assumption. Each GPU has one layer of your model.
41:18
And we're going to start with the simplest scheduler, the all forward, all backward scheduler.
41:26
So we're going to have the first batch, which is this one. So it starts at the first GPU.
41:31
And then he sends the activations to the second GPU and then third and then fourth.
41:38
And then I have another batch. So the GPUs don't wait.
41:43
I'm going to schedule eight batches. So all of them, they're going to do the forwards.
41:48
And then they're going to do the backwards. So every time one GPU does the backward, it sends the gradients to the previous layer, et cetera.
41:57
And I repeat this. So this is all forward all backward. And I schedule multiple batches to minimize the idle time.
42:07
So the idle time is this. And it's the biggest problem in pipeline parallelism.
42:15
So there is-- the best way to solve
42:21
it is to have more complex schedulers. So this is the one forward, one backward scheduler.
42:30
And the name comes from the fact that if you prioritize backwards over forwards, you're going to have
42:36
at some point like in the middle, you're going to have a forward, backward. So they're interleaved basically, forward,
42:43
backward forward, backward. But we still didn't solve the pipeline bubble.
42:48
This just helps with the memory a little bit. So the best way to solve it would
42:56
be to start doing forwards from this side as well.
43:01
And this is what Deepseek has done in their DualPipe.
43:08
So basically, if you have eight devices,
43:13
you're going to distribute the layers in a round-robin fashion. So the first layer, you're going to put it
43:22
both on the first device and last device. So that both of them, they can start with data, et cetera.
43:28
So there is a forward that's going like this. And there's a forward that's going like this. And then a backward like this, and then a backward like this.
43:35
And you manage the overlap in the house. So as we said, it's easier said than done
43:41
because the implementation is very complex. And you need to keep track of the batches
43:47
and the forwards and backwards so that you send the correct activations, and this--
43:53
and the correct gradients. So in general, the pros and cons.
44:00
So the pros is that the communications are cheap. There is no AllReduce, nothing to have a headache about.
44:07
You just exchange the activations and the gradients. And the sharding is very efficient
44:14
because every GPU keeps track of just a shard of the model.
44:19
It's model-agnostic. I don't care about the nature's model, as long as I just have layers, and I just
44:26
send activations and gradients. And the cons is that I need to save activations
44:34
for multiple micro-batches, which is annoying. So for example, if in this case, I had eight backwards in here,
44:42
they scale 20 micro-batches. So I need to save 20 times the activations for the different.
44:50
So usually for PP and FSDP since they rely on activations a lot,
44:57
we offload the activations. So we use activation checkpointing or now there is even CPU offloading.
45:04
If you have a good GPU, you can offload the activations to the CPU, and then you just load them back
45:11
when you need them. The other annoying thing is, if you want to hide the PP bubble,
45:20
you need an advanced pipeline scheduler like dual pipe. And the implementation is complex.
45:26
It's very hard and it's tough to add to the library. And 13 is you need multiple micro-batches to hide PP bubble.
45:35
So in a sense, this is like the deep issue, is that you need to scale your global batch
45:41
size with the PP size. And the last two parallelisms, they
45:47
are specific to two use cases. For example, for CPP, the issue we want to solve
45:54
is sequence parallelism. So once you scale your sequence length, the activations,
46:01
they explode. So the question-- even with TP, even with PP, the question is, how can we
46:09
amortize the memory cost when doing long contact training?
46:15
So what we've seen previously in data parallelism, when we have a batch, we can shard it along the batch dimension.
46:23
But the other-- what we can do now is let's shard it along the sequence dimension.
46:31
So it's analogous to data parallelism. But if I do that, and if I give a different sequences
46:39
to the GPUs, what about attention? Because when I do the forward, each GPU
46:44
has only a part of the sequence. So how can I compute attention over my entire sequence?
46:51
This is where ring attention comes from. And the idea is similar to online softmax that
46:58
is used in flash attention. So you don't need to compute softmax out of the box.
47:04
You can compute it in an online fashion. So each GPU, they compute softmax locally
47:14
and then they exchange K and V with the other GPUs. Then they update the softmax, et cetera.
47:22
So this is the idea behind ring attention. So the problem with this, the small problem,
47:29
is that we need to communicate K and V inside the attention block. So inside each attention block, which
47:36
is a similar problem to TP, so it's also communication heavy.
47:43
And for that, we can use send receive operation for GPUs
47:48
to send the other case. And this you can also use all-gather for that.
47:55
So the pros is it's the only parallelism that efficiently partition large sequences memory, sorry.
48:03
And because the other parallelisms, they don't really shard the sequence. So if you have a big sequence, there's a problem.
48:12
The cons is that it's communication heavy because in each attention block, you need to exchange
48:17
K's and V's. And similar to DP, since each GPU sees different data,
48:24
we need to AllReduce the gradients and it doesn't help much for short sequences.
48:30
So really, it's really used if and only if you're doing long context training.
48:39
And for the last one EP, it's used for MoEs.
48:44
And for MoEs, we don't have time to go really through them. But how can we parallelize them?
48:53
So when you have your router and you want to route your tokens to different experts,
49:00
you can just shard your experts across GPUs. So the question would be, what comes
49:08
would I add so that I can do the routing? For routing, there is a switch transformer and top K version.
49:17
So let's talk about top K, which is the more generic one.
49:23
For this, let's take a look at all-to-all operation. So all-to-all, it's the most generic way
49:31
of exchanging messages. So basically, you have, for example, four processes, four GPUs.
49:37
Each one of them, they have four different messages. So all-to-all what it does is just each process,
49:46
it's going to send their messages to the other four. And each process is going to get messages from the other four.
49:57
So in a sense, all processes, they communicate with each other. It's like an n squared type of communication.
50:04
It's the more-- it's the most complex one. And how does this apply in MoEs?
50:10
So let's say each GPU has different experts. So when you're doing the routing,
50:19
each GPU is going to have some tokens that need to be routed to the first GPU.
50:25
So this is why all-to-all helps. So the first GPU here, which has the first expert,
50:32
needs to get the tokens associated to it from the other GPUs.
50:38
So how can we apply expert parallelism?
50:46
First, we shard the experts across GPUs and only the experts. So expert parallelism is really only applied in MoE.
50:55
It doesn't touch attention. And so to avoid duplicated work in attention,
51:03
we need to share data across GPUs. So EP is-- actually, it's not orthogonal to DP in this sense.
51:13
So you also shard data across expert parallelism. And we need an all-to-all communication to route tokens
51:21
to their respective experts, which is the operation that we call dispatch.
51:26
And of course, we also need to combine them after the experts
51:32
to retrieve the original sequences. So the problem and the bigger-- the biggest problem
51:40
that you're going to face when scaling MoEs. So how do we know what all-to-all dispatch to use?
51:50
So basically, in order to know how the tokens are routed, you need for the router to compute the scores.
51:59
So the CPU needs to wait for the router
52:05
to compute the expert scores. And this is annoying. Why?
52:10
Because the CPU needs to wait here for the GPU
52:15
to do the router calculation, what we call the dispatch preprocess.
52:21
And the GPU needs to tell us what are the buffer sizes, and what are the tokens per expert that we need.
52:30
And to solve this, there's, for example, deep EP by Deepseek and there is hybrid EP by NVIDIA.
52:37
And the only way to solve this is to use recent hardware so far, which has IBGDI and RDMA.
52:48
So these allow us to-- so they allocate specific SMs and specific memory
52:57
to basically-- whenever they calculate or they know
53:04
which tokens to route to each GPU, they have already allocated the memory for that calculation.
53:14
And this is a problem because for example, if you have H hundreds or if you don't have InfiniBand network,
53:22
you're stuck with this CPU-GPU sink. And this is why most labs who do MoE trainings,
53:29
they have very slow trainings just because of this problem of hardware.
53:34
And even the Deep EP, that Deepseek has open source,
53:40
you're going to find out that it only works on IBGDI, which is InfiniBand.
53:46
So the other labs have a hard time catching up with this.
53:52
So in summary, expert parallelism is the only way to distribute MoEs efficiently,
53:59
and it's communication heavy. It needs an all-to-all, every MoE block.
54:04
And since it has different data, it needs to all reduce gradients. And there is a CPU-GPU sync, which
54:11
slows down your training very much to launch this batch operation. So in summary, it is only to be used for MoE trainings and/or--
54:22
so the practical solution that people do to train with expert parallelism
54:28
is to combine it with PPE. And how do they do that? So we said that the dispatch and combine are slow.
54:35
And if you remember, for our one forward, one backward, we can overlap the forward and the backward.
54:42
So what we do here is-- so we have two batches, which is the blue and the green here. So when we want to do the dispatch, which
54:50
is for the blue batch, in parallel,
54:55
we can do the backward for the other batch. And this is nice because-- so MLP here doesn't need this batch.
55:03
And we can do the dispatch here. And once this is done, we can do the dispatch
55:08
for the other batch, et cetera. And this is already implemented in some libraries. For example in Megatron, you can use these two flags.
55:17
So in a nutshell, you can combine all five parallelisms. And you're going to have different communications
55:23
in different steps of your training. And we've seen that CP and EP-- so EP only applied to MoEs
55:33
and it only touches the MoE. And CP only touches self-attention here.
55:41
And yeah, all parallelism are orthogonal.
55:46
So you can combine all five of them. And yeah, as a recap, we've seen the five parallelisms.
55:55
And you can find a cheat sheet that we made in the Ultra-scale Playbook on how
56:01
to make decisions following the number of GPUs you have.
56:06
And finally, if you're more interested in the infra side of things, you can take a look at the small training
56:13
playbook in the infra section. We've made a lot of benchmarks on how infra can
56:19
handle these types of workload. And you can find more references in the two books mentioned.
56:26
Also, the Jax Scaling Book for TPU point of view. And if you're interested in these kind of things,
56:35
contributions are very welcome, so Nanotron, Torchtitan, Megatron-LM, and a lot of other libraries.
56:42
So feel free to reach out if you want to contribute. And one last thing is as we scale to thousands of GPUs,
56:50
let's be mindful of their energy impact and use them responsibly. Thank you.
56:55
Thank you.
57:01
OK. We'll take any questions in person or over Zoom. Any in the room?
57:06
57:15
Yeah, thanks for the talk. It seems that for expert parallelism, you also have to have the tokens routed evenly across ranks.
57:25
Because if all of your tokens end up routed to one GPU, then everything else is idle.
57:31
What are ways to fix this in practice? That's what the load balancing tries to do.
57:38
So depending on which approach, for example, Deepseek, they initially introduced the load balancing loss.
57:47
Sorry. So the load balancing acts basically in the loss, a factor that penalizes
57:56
uneven distribution of tokens. And there's multiple ways to do it. There's the auxiliary loss free where you just
58:03
add a bias term when you compute the router, and it would automatically adjust the distribution
58:11
so that it's even across GPUs.
58:22
We also have some on Slido. One is, do different parallelism strategies meaningfully
58:29
affect scaling efficiency model performance or convergence, or do they just enable reaching larger scales?
58:36
What's the first one, scaling convergence? Efficiency.
58:41
Convergence efficiency. I think they would like scaling laws of language models.
58:50
OK. So actually, the different policies do not change the forwards and backwards.
58:57
So ideally, if everything is implemented correctly,
59:02
it should have the same effect as if you had one GPU. So like the same scaling laws applies.
59:08
And you shouldn't find any difference in using PP over EP
59:14
from computation point of view.
59:19
59:26
Another is, how can we reduce GPU idle time during, say, RL training when CPU side checkpointing or environment
59:33
data processing becomes sequential and a bottleneck? Yeah. So for that, at least for text, you
59:42
can allocate a lot of workers beforehand. So the PyTorch data loaders, they
59:48
allow you to specify the number of workers that you can use, and they can pre-process the data
59:53
before the training workload. So that should at least help alleviate the pre-processing
1:00:00
so that it's done before you even go through the training iterations.
1:00:09
Any more in the room? Also, someone asked, do we have an automatic way
1:00:16
to decide the best parallelism? Yeah, there have been some papers for that. I think if you take a look at Jax book,
1:00:24
they've played with that and dilated the TPU decide on what's the best parallelism.
1:00:31
But it really depends on your specific. So model size, how many data you have,
1:00:37
the global batch size, and the network you have. So the biggest factor in what we've said when you scale,
1:00:46
of course, is the network bottleneck. So if you have, for example, let's say NVLink. So in GPUs, usually NVLink is either
1:00:53
four, eight, or for the most recent one, 32 or even 72. So it depends on that.
1:01:00
You can even use the more communication heavy forms of parallelism or the less communication heavy ones.
1:01:08
And for example, for GPUs, since they are in pods of 32, usually people, they allow themselves to use tensor parallelism over the 32 pods.
1:01:17
So yeah, it really depends on the hardware and the global batch size.
1:01:23
1:01:35
All right. Thanks, guys. Thanks so much Nouamane for the amazing insightful talk.
1:01:40
So let's give it a hand again for our speaker.



&nbsp;



⬆️⬆️⬆️  END OF TRANSCRIPT  ⬆️⬆️⬆️

## §2 — Screenshot / visible source details  ·  layer: `raw_source_metadata`

*Nothing to paste here — just **drop the screenshot in chat** and the normalizer fills §0. (Or paste the visible title / channel / URL / date below.)*

## §3 — Interpretations & review log  ·  append-only (each reviewer adds a new entry; never overwrite)

### Review 001 — Knox / ChatGPT strategic read  ·  layer: `captured_interpretation_nonbinding`
- reviewer: `Knox / ChatGPT` · type: `AI assistant` · at: `TK` · purpose: `strategic source-local interpretation`

> Knox's read is the **strategic source-local interpretation** — paste **exactly what Knox outputs**, however rich, in ONE block. Do NOT split it into fields or trim it. Knox's output typically already carries: priority/depth · source type + authority context · why this matters for OMNI · core takeaway · OMNI translation · likely landing zones · doctrine/primitive pressure · keeper doctrine · what-not-to-import · hard read. (What we ASK Knox to produce = `_KNOX_STRATEGIC_READ_PROMPT.md`.) The agent's §3 Review 003 **formalizes** this (it does not re-derive it).

⬇️⬇️⬇️  PASTE KNOX'S / CHATGPT'S FULL READ BELOW (as-is)  ⬇️⬇️⬇️

Process this one, but do not treat it like a thesis/company-building spine. This is a technical infrastructure source: useful for AI substrate realism, cost/latency, Build OS infra assumptions, and “don’t hand-wave scale.” Transcript is from the uploaded file.
Rough metadata for Opus
source_platform: YouTube
source_url: https://youtube.com/watch?v=l5BKi32IEa8
source_title: Stanford CS25: Transformers United V6 | The Ultra-Scale Talk: Scaling Training to Thousands of GPUs
channel_or_org: Stanford Online
speaker: Nouamane Tazi
published_at: May 11, 2026
captured_at: 2026-06-09
captured_by: Nick
capture_method: transcript paste + screenshot
content_type: seminar / technical lecture
source_reliability_context: practitioner / vendor
topic_tags_light: [ultra_scale_training, distributed_training, data_parallelism, fsdp, zero, tensor_parallelism, sequence_parallelism, pipeline_parallelism, context_parallelism, expert_parallelism, mixture_of_experts, gpu_clusters, communication_compute_overlap, model_scaling, hugging_face, nanotron]
§0.1 rough people / authority context
primary speaker(s):
name: Nouamane Tazi · role_in_source: guest speaker / lecturer · affiliation_at_publication: Hugging Face · speaker_type: practitioner / vendor / educator · authority_context: Lead author of the Ultra-Scale Playbook and core developer of Nanotron, Hugging Face's open-source distributed training library. High relevance for practical distributed-training mechanics, GPU scaling, parallelism strategies, MoE scaling, communication bottlenecks, and infra tradeoffs. Technical operator/practitioner source rather than strategic/company-building doctrine. · identity_confidence: high_from_screenshot
publisher / channel: Stanford Online
interviewer / moderator / host: Stanford CS25 instructors / seminar hosts
event_context: Stanford CS25: Transformers United V6 seminar; seminar date shown as April 23, 2026; YouTube published May 11, 2026
perspective / conflict notes: Hugging Face/open-source distributed-training perspective. Strong practical infra value. Niche source by views but high technical specificity. Visible screenshot context: 1,266 views; 47 likes at capture. Useful to ground OMNI's AI-substrate/build assumptions, not to drive product thesis prose.
Suggested processing
priority: 3.5/5
depth: targeted_semantic
EVRUN needed?: yes, but targeted unless the AI-substrate infra section becomes active
duplicate / sibling relationship: technical_substrate_sibling_to_Jensen_compute_source; complements_Lampinen_context_strategy_and_Karpathy/YCombinator_build_os_sources
likely landing zones: AI substrate infra appendix / Build OS scale assumptions / model-runtime strategy / cost-latency planning / agent-loop budget controls / eval-infra realism / future infra watchlist
Knox / ChatGPT read for §3 Review 001
Process this one. Targeted technical source.
This is not a “big OMNI thesis” source in the same way as Karpathy, YC, Jensen, or Lampinen. But it is valuable because it prevents architecture fantasy.
This is the “what actually happens when you try to scale model training across thousands of GPUs” source.
Classification
Priority: 3.5/5
Depth: targeted_semantic
Authority context: Hugging Face distributed-training practitioner / open-source infrastructure operator
OMNI relevance: AI substrate realism / Build OS infrastructure assumptions / cost-latency constraints / future model training posture / eval-infra realism
Promotion posture: technical_reference_candidate, not thesis_spine
Core takeaway
The keeper is:
At scale, intelligence is constrained by memory, communication, synchronization, network topology, batch size, activations, and hardware-specific bottlenecks — not just “use more GPUs.”
That matters for OMNI because we keep talking about agents, loops, context, model routing, evals, long-context packets, and AI-native operation. This source reminds us that those abstractions eventually cash out in infrastructure constraints.
The big OMNI translation
“Scale” is not one dimension.
The lecture walks through multiple parallelism strategies:
data parallelism,
ZeRO / FSDP,
tensor parallelism,
sequence parallelism,
pipeline parallelism,
context parallelism,
expert parallelism.
Each exists because a different bottleneck appears:
model weights do not fit,
gradients/optimizer states are too large,
global batch size hits limits,
long context explodes activations,
MoE routing creates all-to-all communication,
pipeline bubbles waste GPU time,
network speed prevents hiding communication.
OMNI translation:
Do not say “scale agents” as if scale is one knob.
Scale has dimensions:
agent count,
context size,
retrieval cost,
tool latency,
database contention,
human review bandwidth,
model cost,
eval replay volume,
trace storage,
workflow fan-out,
and safety review load.
Keeper:
Scaling is bottleneck choreography.
Communication/computation overlap is a real systems pattern.
A major repeated theme is hiding communication behind computation: AllReduce overlap, FSDP prefetching, reduce-scatter/all-gather tricks, pipeline scheduling, dispatch overlap in expert parallelism.
OMNI translation:
The same principle applies above the GPU layer.
OMNI should overlap work where safe:
retrieve context while waiting on tools,
precompute provider packets before review,
materialize projections before surfaces need them,
run low-risk validations before human review,
prepare candidate tasks before authorization,
cache stable context,
suppress loops that cannot be evaluated.
Keeper:
A good AI system does not merely do more work; it schedules work so bottlenecks stay covered.
Use only the parallelism you need.
The lecture makes a very practical point: if ZeRO 1 fits, do not blindly use ZeRO 3, because ZeRO 3 adds communication cost. More sophisticated infrastructure can make things slower if the simpler strategy already fits.
This is a useful Build OS law.
OMNI should not overuse the fanciest pattern:
Do not use a swarm where one agent works.
Do not use long context where retrieval is enough.
Do not use RAG where deterministic lookup is enough.
Do not use AI where code/rules are enough.
Do not use human review where policy safely automates.
Do not use automation where authority is unclear.
Keeper:
Use the minimum mechanism that satisfies correctness, authority, and scale.
Long context is not free.
Context parallelism exists because long sequence length explodes memory. The talk explicitly treats long-context training as a special case requiring its own parallelism strategy.
This reinforces Lampinen and IBM RAG/long-context doctrine.
OMNI should not romanticize long context.
Long context is powerful, but it has real cost:
memory,
latency,
attention dilution,
retrieval burden,
trace size,
privacy exposure,
and model confusion.
Keeper:
Long context is a capability, not a default.
MoE / expert routing has a direct conceptual echo for OMNI.
Expert parallelism routes tokens to experts, then combines outputs. The technical details are GPU-level, but the conceptual analogy is useful:
routing creates load-balancing problems,
all-to-all communication is expensive,
experts must be placed and invoked deliberately,
uneven routing creates idle capacity,
hardware/network determines what is feasible.
OMNI translation:
CNS routing to human/provider/staff/AI/domain “experts” has similar risks.
If every problem routes to Nick/provider/human review, the system stalls.
If every task routes to one AI capability, it overloads or fails.
If routing is uneven, capacity is wasted.
If the routing layer cannot see real constraints, it creates bottlenecks.
Keeper:
Routing is capacity management, not just classification.
The infra layer argues for cost-aware AI capability design.
This source supports a boring but important OMNI rule:
Every AI capability should have a runtime profile.
For each AI capability, OMNI should eventually know:
expected context size,
model/capability used,
latency class,
cost class,
tool/API dependencies,
trace requirement,
evaluation method,
retry policy,
fallback path,
human review requirement,
safety boundary.
That is not Day 0 for everything, but it is the right posture.
Keeper:
An AI capability without a runtime profile is not production architecture.
Model-training scale is probably not OMNI’s near-term business.
This is important.
OMNI should not overreact and decide it needs to train frontier-scale models or obsess over thousands-of-GPU training. The wedge is care/business operating substrate, not model lab.
The source is most useful as constraint literacy:
understanding why open/specialized models matter,
why inference cost matters,
why context routing matters,
why model selection matters,
why you should not casually promise continuous AI loops without budgets.
Keeper:
OMNI should be infra-literate, not infra-distracted.
Where it lands
AI substrate: targeted. Runtime cost, model routing, training/inference realism, specialized/open model posture.
Build OS: targeted. Agent loop budgets, eval replay cost, avoiding over-engineering, scheduling work around bottlenecks.
Knowledge Reservoirs / context strategy: medium. Long context and retrieval have real cost; context size must be treated as architecture.
CNS: medium. Routing/capacity/load-balancing analogy.
§C / external capability exchange: minor-to-medium. Capability invocation needs runtime and capacity profiles.
Thesis prose: light. This should not become a big v4 public thesis section unless infrastructure strategy becomes a major chapter.
Doctrine / primitive pressure
Potential concepts:
runtime_profile
capability_cost_class
context_cost_budget
loop_budget
agent_capacity_profile
communication_bottleneck
long_context_cost
minimum_sufficient_mechanism
parallelism_strategy
routing_load_balance
infra_literate_ai_substrate
eval_replay_budget
model_runtime_constraints
capacity_aware_routing
Keeper doctrine
OMNI should not choose the most impressive AI mechanism. It should choose the minimum mechanism that satisfies correctness, authority, cost, latency, and scale.
Second:
Long-running agent loops require budget, capacity, and bottleneck awareness.
Third:
Routing is not only semantic. Routing is also load, latency, authority, and capacity management.
Fourth:
The infra layer is not OMNI’s product, but ignorance of it will corrupt product promises.
What not to import blindly
Do not overfit OMNI around training frontier models.
Do not let GPU-cluster mechanics dominate care/business architecture.
Do not confuse training-time scale with inference-time product value.
Do not assume bigger models or more parallelism are automatically better.
Do not overuse complex architectures when simpler deterministic systems fit.
Do not turn this into “OMNI needs a model lab” unless a later business case proves it.
Do-not-miss lesson
This source gives OMNI constraint literacy.
The seductive AI-thesis sources tell us what may become possible. This one reminds us that possible becomes real only when memory, communication, synchronization, batching, network topology, hardware, and cost all cooperate.
Shortest OMNI version:
Agents are product abstractions. Underneath them are runtime budgets, context costs, tool latencies, queues, traces, evals, and capacity constraints.
My hard read
Targeted semantic, not full thesis spine.
Useful for hardening AI substrate and Build OS runtime discipline. Not a source to overpromote into v4 narrative, but it should be available when we define capability runtime profiles, loop budgets, context strategy, model routing, and capacity-aware CNS design.


&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 002 — Nick operator note  ·  layer: `human_context_nonbinding`
- reviewer: `Nick` · type: `human founder/operator` · at: `TK`

⬇️⬇️⬇️  YOUR GUT NOTE BELOW  (optional)  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 003 — Opus / agent formal deep extraction  ·  layer: `analysis_nonbinding`  ·  (agent-authored, NOT a Nick paste)
- reviewer: `Opus` (or agent) · type: `AI assistant` · at: `TK` · purpose: `formalize Review 001 → structured per-source extraction → feed EVRUN concept registry` · binds nothing (`GRD-036`/`GRD-044`)

> The agent reads §1 verbatim + §3 Review 001 (Knox) IN FULL, then writes the formal per-source extraction HERE (not a sidecar). Formalize Review 001's strategic read — verify/sharpen it, do not re-derive. Per concept cluster: **concept · OMNI meaning · why · downstream homes (thesis §x / CNS / Build-OS / §C / capability-topology / Knowledge-Reservoirs / security / domain-contracts / UX-surfaces / product / future-watch) · source anchors (verbatim ≤12 words + timestamp) · stale-vs-v3 (ABSENT/PARTIAL/AFFIRM) · weight_tier (spine/vocabulary/low-authority-watch/no-op) · status (promote/watch/reject).** Then: **net-new primitives** (dedup vs registry — don't re-mint) + **reread flags**. Cross-source convergence is folded into the EVRUN **concept registry**, not duplicated here.

⬇️⬇️⬇️  AGENT WRITES THE FORMAL EXTRACTION BELOW  ⬇️⬇️⬇️

**Review 003 — Opus formal deep extraction** · reviewer: `Opus` · at: `2026-06-12` · binds nothing (`GRD-036`/`GRD-044`)

**Tier of this source:** `targeted_semantic` (Knox 3.5/5; `technical_reference_candidate`, not `thesis_spine`). This is a **constraint-literacy** source — it hardens AI-substrate / Build-OS runtime discipline; it must NOT be over-promoted into v4 narrative. Formalizing Knox's Review 001 (which I affirm); transcript anchors verify and sharpen it. Net-new yield is small and mostly AFFIRMs/sharpens the existing inference-budget + model-plurality + minimum-sufficient families (`§2A` F + IBM 076) rather than minting fresh spine.

**Core keeper (single strongest OMNI line):** *Agents are product abstractions; underneath them are runtime budgets, context costs, tool latencies, queues, traces, evals, and capacity constraints — so "scale agents" is never one knob, and OMNI should pick the **minimum mechanism** that satisfies correctness, authority, cost, latency, and scale.*

### A. Concept clusters

| # | concept (Knox term) | OMNI meaning | why | downstream homes | source anchors (verbatim ≤12w + ts) | stale-vs-v3 | conflict_status | weight_tier | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | **Scaling is bottleneck choreography** (scale is multi-dimensional) | "Scale agents" is not one knob; each parallelism exists because a *different* bottleneck appears. OMNI scale has its own axes: agent count, context size, retrieval cost, tool latency, DB contention, human-review bandwidth, model cost, eval-replay volume, trace storage, workflow fan-out, safety-review load. | Prevents architecture fantasy; forces naming *which* bottleneck before "scaling." | Build-OS · CNS-contract · §B · future-watch | "each one of them is used in a specific use case" [4:15]; "still to train larger and larger" [1:25] | AFFIRM (affirms inference-budget cluster `§2A` F) | none | vocabulary | watch |
| 2 | **Communication/computation overlap** (schedule so bottlenecks stay covered) | The repeated GPU pattern — AllReduce overlap, FSDP prefetch, reduce-scatter/all-gather, pipeline scheduling, dispatch overlap — generalizes above the GPU layer: OMNI overlaps *safe* work (retrieve context while waiting on tools, precompute provider packets before review, materialize projections before surfaces need them, run low-risk validations pre-review). | A good AI system doesn't do more work; it *schedules* work so bottlenecks stay covered — without letting overlap cross an authority gate. | CNS-contract · Build-OS · §7.7/projections | "overlap efficiently your computation with communication" [9:36]; "we don't want GPU to stay idle" [7:41]; "prefetch the next layer while computing" [16:56] | PARTIAL (Build-OS gestures at scheduling around bottlenecks; not named) | none (guardrail: never overlap *past* a commit gate) | vocabulary | watch |
| 3 | **Minimum sufficient mechanism** (use only the parallelism you need) | Don't blindly use the fanciest mechanism: if ZeRO-1 fits, ZeRO-3 only adds comms and is *slower*. OMNI analog: don't use a swarm where one agent works, long-context where retrieval suffices, RAG where deterministic lookup suffices, AI where code/rules suffice, human review where policy safely automates, automation where authority is unclear. | A Build-OS law: complexity that doesn't buy correctness/authority/scale is negative ROI. Generalizes IBM "smallest-sufficient-model" (076) to *all* mechanisms. | Build-OS · §B · CNS-contract · §C | "If ZeRO 1 fits… faster than ZeRO 3" [21:18]; "use the ZeRO degree that you need" [21:12]; "a lot of people just throw FSDP2 on all… not a good thing" [20:52] | PARTIAL → sharpen (IBM 076 smallest-model; over-engineering avoidance) | none (reinforces anti-089 builder-optimism) | spine-adjacent vocabulary | promote (as Build-OS discipline) |
| 4 | **Long context is not free** (context parallelism / ring attention) | Long sequence length explodes activation memory → needs its own parallelism (CP) + online-softmax ring attention, "communication heavy," "only if doing long context." OMNI: long context costs memory, latency, attention dilution, retrieval burden, trace size, privacy exposure, model confusion. | Reinforces Lampinen + IBM RAG/long-context doctrine: long context is a *capability, not a default*. | Knowledge-Reservoirs · CNS-contract (context strategy) · §B | "once you scale your sequence length, the activations… explode" [45:54]; "communicate K and V inside the attention block" [47:29]; "used if and only if… long context training" [48:30] | AFFIRM (affirms context-cost discipline 059/065 + long-context-cost) | none (echoes RAG-vs-long-context tension, not a new conflict) | vocabulary | watch |
| 5 | **MoE expert routing → capacity-aware routing** (load balancing) | EP routes tokens to sharded experts via all-to-all; uneven routing idles GPUs; load-balancing loss / bias term rebalances. Conceptual echo for CNS: routing to human/provider/staff/AI/domain "experts" has the same risk — if everything routes to Nick/one human/one AI capability, the system stalls; uneven routing wastes capacity. | **Routing is capacity management, not just classification** — the router must see real downstream constraints (load, latency, authority), not only semantic fit. | CNS-contract · capability-topology · §A (attention routing) · §7.6/§12 | "tokens routed evenly across ranks… everything else is idle" [57:25]; "That's what the load balancing tries to do" [57:31]; "communication heavy. It needs an all-to-all" [54:04] | PARTIAL → sharpen (Attention-routing 086 + capability-topology; "capacity" dimension under-named) | none | vocabulary | promote (sharpen attention-routing) |
| 6 | **Capability runtime profile** (cost-aware AI capability design) | Every AI capability should eventually carry a runtime profile: expected context size, model/capability used, latency class, cost class, tool/API deps, trace requirement, eval method, retry policy, fallback path, human-review requirement, safety boundary. | "An AI capability without a runtime profile is not production architecture." Makes cost/latency/authority first-class per capability, not an afterthought. | §C / capability-topology · CNS-contract · Build-OS | "Every AI capability should have a runtime profile" (Knox); grounds in "the biggest factor… is the network bottleneck" [1:00:46] | PARTIAL → sharpen (extends `capability_envelope` REV-176 + `heterogeneous_path_cost` 064) | none | spine-adjacent vocabulary | promote (sharpen capability_envelope) |
| 7 | **Hardware/network determines feasibility** (NVLink / InfiniBand / IBGDA) | What's achievable is gated by network topology — TP within a node, EP needs IBGDA/RDMA/InfiniBand or you eat a CPU↔GPU sync; "most labs… slow trainings just because of this problem of hardware." OMNI: capability feasibility is bounded by infra you don't control; promise loops/long-context only where the substrate cooperates. | Grounds why open/specialized models, inference cost, and context routing matter; affirms Jensen/Rao compute-energy wall. | §B macro · future-watch · CNS-contract (loop budgets) | "the only way… recent hardware… IBGDI and RDMA" [52:37]; "slow trainings just because of… hardware" [54:29]; "it really depends on the hardware and the global batch size" [1:01:17] | AFFIRM (folds into compute/energy-wall watch row 066/087) | none | low-authority-watch | watch |
| 8 | **Parallelism does not change convergence** (it enables reach, not better learning) | Different parallelism strategies, if correct, reproduce single-GPU forward/backward — same scaling laws; they enable *reaching* larger scale, not better convergence. OMNI: scaling mechanism choice is about feasibility/cost, not about making the answer "more right" — correctness still comes from the domain, not the scale knob. | Anti-hype: bigger/more-parallel ≠ smarter per se; resist conflating training-scale with product value. | §B · §2 (foil) · future-watch | "the different policies do not change the forwards and backwards" [1:00:40]; "same effect as if you had one GPU" [59:02] | AFFIRM (anti-"bigger=better"; affirms models-not-magic foil) | none | low-authority-watch | watch |
| 9 | **Infra-literate, not infra-distracted** (model-lab is not the wedge) | Model-training scale is probably NOT OMNI's near-term business; the wedge is care/business operating substrate, not a model lab. Use this source as constraint literacy (why open/specialized models, inference cost, context routing, model selection matter; why not to promise continuous AI loops without budgets) — don't overreact into frontier training. | Keeps the corpus from hijacking the thesis; a posture/guardrail, not a build directive. | operator-context · §2 (what OMNI is not) · Build-OS posture | "you don't need 1,000 GPUs… As long as you have two" [5:18]; "be mindful of their energy impact… responsibly" [56:50] | PARTIAL/posture (guardrail; pairs `GRD-026` payload-noun ≠ domain) | none | low-authority-watch | watch (candidate guardrail) |
| 10 | **GPU-internal training mechanics** (DP/ZeRO/FSDP/TP/SP/PP/CP/EP, DTensor, DualPipe, Muon, all-reduce internals) | The literal mechanics (reduce-scatter/all-gather equivalence, FSDP module units, pipeline bubbles/schedulers, dispatch-preprocess CPU-GPU sync) are training-internals OMNI does not implement. | Import the *intuitions* (clusters 1–3, 5); the literal ops are not OMNI architecture (OMNI does not train frontier weights — `§2A` G7 guardrail). | (none — no-op) | "AllReduce… is just a reduce-scatter plus an all-gather" [12:54]; "never have more than two… FSDP units in memory" [17:40] | AFFIRM (RL/training-infra internals = no-op, per 054 guardrail) | none | no-op | reject (as primitive) |

### B. Net-new primitives (dedup vs `EVRUN-2026-000001 §2A` BEFORE minting)

**Genuinely net-new (small set — propose at destination gate, do not mint here):**
- `minimum_sufficient_mechanism` — **NEW (generalization).** Build-OS / §B law: choose the least-complex mechanism that satisfies correctness + authority + cost + latency + scale; added complexity that buys none of these is negative ROI. *Generalizes* IBM `smallest-sufficient-model` (076) and the over-engineering-avoidance gesture to ALL mechanisms (swarm-vs-agent, long-context-vs-retrieval, RAG-vs-lookup, AI-vs-rules, review-vs-policy). Strongest keeper from this source; reinforces anti-089 builder-optimism counter-doctrine.
- `capability_runtime_profile` — **NEW (sharpen of `capability_envelope`).** A per-capability runtime metadata object: {context_size, model/capability, latency_class, cost_class, tool/API deps, trace_requirement, eval_method, retry_policy, fallback_path, human_review_requirement, safety_boundary}. Makes cost/latency/authority first-class. Subsumes Knox's `runtime_profile` / `capability_cost_class` / `agent_capacity_profile` / `model_runtime_constraints` into one host.
- `capacity_aware_routing` — **NEW (sharpen of attention-routing 086 + capability-topology).** Routing decisions must account for downstream *capacity/load/latency/authority*, not just semantic classification; uneven routing wastes capacity (MoE load-balance analogy). Pairs `attention_routing_state` / `human_attention_budget` (`§2A` C) — does NOT duplicate them; adds the "routing = capacity management" axis.
- `bottleneck_choreography` (a.k.a. `work_overlap_scheduling`) — **NEW (vocabulary, low tier).** Above-GPU-layer overlap principle: schedule safe work so bottlenecks stay covered (retrieve-while-tool-waits, precompute-before-review, materialize-before-surface) — never overlapping *past* a commit/authority gate. Subsumes Knox's `communication_bottleneck`.
- `infra_literacy_posture` — **NEW (posture / candidate guardrail, low tier).** "Infra-literate, not infra-distracted": OMNI must understand model/inference/context constraints without becoming a model lab; pairs `GRD-026` (payload-noun ≠ domain). Subsumes Knox's `infra_literate_ai_substrate`.

**EXISTS-AS (do NOT re-mint — `§2A` already carries these; this source sharpens/affirms):**
- `loop_budget` — **EXISTS-AS** `iteration_budget` + `compute_allocation_policy` (`§2A` F, 065/084). Sharpen: loops need explicit budget/capacity/bottleneck awareness.
- `context_cost_budget` / `long_context_cost` — **EXISTS-AS** `compute_allocation_policy` (context budget) + context-engineering `context_compaction_policy` (059) + IBM long-context doctrine. Affirm "long context is a capability, not a default."
- `eval_replay_budget` — **EXISTS-AS** `compute_allocation_policy` (verify/eval budget, 065) + eval-harness discipline (054). Affirm.
- `capability_cost_class` / `model_runtime_constraints` / `agent_capacity_profile` — **EXISTS-AS / fold into** `capability_runtime_profile` (above) + `heterogeneous_path_cost` / `sparse_capability_network` (064). 
- `routing_load_balance` — **EXISTS-AS / fold into** `capacity_aware_routing` (above) + Attention-routing cluster (086).
- `parallelism_strategy` — **EXISTS-AS** no-op (GPU training internal). Only the *analogy* ("scale is multi-dimensional") survives as cluster-1 vocabulary; the literal primitive is rejected.
- `minimum_sufficient_mechanism` overlaps but is broader than IBM `smallest-sufficient-model` (076) — kept as NEW generalization (see above), not a re-mint.

### C. Reread flags
- **Metadata discrepancy (low):** Knox's rough block lists `captured_at: 2026-06-09`; the source packet §0 carries `2026-06-11`. Kept the existing `2026-06-11` (not a `TK` field). Seminar date (April 23, 2026) vs YouTube publish (May 11, 2026) both recorded in `event_context`; `published_at` set to `2026-05-11`. No substantive impact.
- **Capacity-aware-routing reconcile:** before promoting `capacity_aware_routing`, reopen the Attention-routing anchors (086-47/48/80) + Capability Topology Gate so the "capacity" axis is added to the existing routing primitive rather than spawning a parallel one.
- **`minimum_sufficient_mechanism` placement:** decide at Build-OS authoring whether this is a standalone admission law or a clause inside the existing inference-budget / model-tier-routing host (it touches §B, Build-OS, and §C). Do not let it inflate into a thesis-spine section (Knox: not thesis_spine).
- **No ungrounded anchors:** every cluster anchor is verbatim from §1; the MoE→CNS and overlap→OMNI analogies are Knox's interpretation (attributed), grounded in transcript mechanics, not invented transcript claims.

### D. One-line hard read
**Targeted technical / constraint-literacy — useful vocabulary + two promotable Build-OS disciplines (`minimum_sufficient_mechanism`, `capability_runtime_profile`), NOT thesis spine.** Strongest line: *Agents are product abstractions; underneath are runtime budgets, context costs, tool latencies, queues, traces, evals, and capacity constraints — so pick the minimum mechanism that satisfies correctness, authority, cost, latency, and scale.*

&nbsp;

⬆️⬆️⬆️  END Review 003  ⬆️⬆️⬆️

## §4 — Analysis pointers  *(agent fills at CLOSEOUT once analyzed — the "linked" step; leave `TK` until then)*
- EVRUN(s): `EVRUN-2026-000002` (ai-corpus-wave-2) · concept_registry: `EVRUN-2026-000002_ai-corpus-wave-2_concept_registry_and_routing_map.md` · source_anchor_ledger: `EVRUN-2026-000002_ai-corpus-wave-2_source_anchor_ledger_receipts_only.md` · per-source deep-read: §3 Review 003 (this file) · impact: `§B (AI-substrate) · Build-OS (runtime discipline) · CNS-contract (capacity-aware routing, overlap scheduling) · §C/capability-topology (capability_runtime_profile) · Knowledge-Reservoirs (long-context cost)` · promotion: `watch` (targeted technical reference; `minimum_sufficient_mechanism` + `capability_runtime_profile` = promote-candidates at their Build-OS/§C gates, not thesis spine)

## §5 — Change log
- `2026-06-11` — source file created.
- `2026-06-12` — Opus §3 Review 003 formal extraction written (10 clusters, 5 net-new + EXISTS-AS dedup vs `§2A`); §0/§0.1 metadata filled from Review 001 block + transcript; §4 pointers filled; status flipped `raw_dropped → analyzed`.

> Authority/retrieval labels are defined once in `../../00_evidence_router.md` (operating principle #9 + §8). Sections carry their `layer:` inline; the legend is not restated per file.
