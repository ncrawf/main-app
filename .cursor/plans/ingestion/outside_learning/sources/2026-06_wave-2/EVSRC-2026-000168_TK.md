# EVSRC-2026-000168 — <title or TK>

Document type: `source_capture` · Lane / reservoir: `outside_learning`
**Status:** `raw_dropped` → `normalized` → `reviewed` → `analyzed` → `promoted | deferred`  ·  **current: `raw_dropped`**
Authority: `evidence_nonbinding` — captured source + interpretations only; binds nothing until routed + promoted.

> **HOW TO USE THIS FILE — two kinds of marks:**
> • `TK` = a field the normalizer (Opus) fills from the screenshot — **leave it alone.**
> • **⬇️ PASTE blocks = the ONLY spots Nick touches.** Exactly **3**: transcript (§1), **Knox read (§3 Review 001)**, gut note (§3 Review 002, optional). Plus: **drop the screenshot in chat** → Opus fills §0.
> Copy this into `sources/<YYYY-MM>/`, rename `EVSRC-2026-000168_TK.md` (id = highest EVSRC + 1), add a row to `../../00_index.md`.
>
> **Per-source flow (the standard):** Nick pastes transcript (§1) + Knox's strategic read (§3 **Review 001** — paste it AS-IS, however rich Knox makes it; do NOT reformat into fields) + optional gut note (§3 Review 002). Then the agent (Opus) writes **§3 Review 003 — formal deep extraction** (formalizing Review 001, NOT rediscovering it), updates the run's **concept registry** (cross-source synthesis), the coverage matrix, and fills **§4 pointers** at closeout. **The per-source deep read lives HERE in §3 — never in a separate sidecar/extraction file** (`GRD-044`). Cross-source synthesis lives in the EVRUN concept registry.

## §0 — Source identity / metadata  *(normalizer fills from the screenshot — leave `TK`)*
- evsrc_id: `EVSRC-2026-000168`  ·  filename: `EVSRC-2026-000168_TK.md`
- source_platform: `YouTube`  ·  source_url: `TK`  ·  source_title: `TK`
- channel_or_org: `TK`  ·  speaker: `TK`  ·  published_at: `TK`
- captured_at: `2026-06-11`  ·  captured_by: `Nick`  ·  capture_method: `transcript paste + screenshot`
- content_type: `TK`  ·  source_reliability_context: `academic | practitioner | vendor | founder | investor | security-researcher | unknown`  ·  topic_tags_light: `[TK]`

## §0.1 — People / authorship / authority context  *(normalizer fills from screenshot + description + transcript — this is what makes a source a reservoir, not a transcript dump)*
- primary speaker(s):
  - name: `TK` · role_in_source: `TK` (speaker / presenter / interviewee / host / author) · affiliation_at_publication: `TK` · speaker_type: `TK` (founder | researcher | investor | operator | vendor | clinician | regulator | journalist | educator | other) · authority_context: `TK` · identity_confidence: `TK` (high_from_screenshot | inferred | unknown)
  - *(add a bullet per additional speaker)*
- publisher / channel: `TK`  ·  interviewer / moderator / host: `TK`
- event_context: `TK`  ·  perspective / conflict notes: `TK`

> **Authority is descriptive, not worship** (`GRD-039`): record *who* said it (raises relevance), but every claim still routes through evidence → interpretation → gated promotion. (Clinical reservoir extends this block heavily — authors / affiliations / journal / DOI-PMID / study-type / evidence-level / COI / retraction — `FWREG-006`.)

## §0.5 — Processing checklist
**Nick drops:** [ ] transcript → §1 · [ ] screenshot in chat · [ ] **Knox strategic read → §3 Review 001** (paste as-is) · [ ] (optional) gut note → §3 Review 002
**Agent (Opus) does:** [ ] id+filename · [ ] §0 metadata from screenshot · [ ] takes labeled · [ ] **§3 Review 003 formal deep extraction** (formalize Review 001) · [ ] update EVRUN concept registry (cross-source) · [ ] update coverage matrix · [ ] **fill §4 pointers (closeout)** · [ ] NO sidecar extraction file (`GRD-044`)

## §1 — Verbatim transcript  ·  layer: `raw_source`  ·  IMMUTABLE

⬇️⬇️⬇️  PASTE THE FULL TRANSCRIPT BELOW  (do not summarize)  ⬇️⬇️⬇️

Transcript


Search in video
0:00
0:05
So welcome back, everyone. So on Monday, Tatsu did an excellent job
0:11
of describing a high level overview of GPUs and how to think about performance and all the quirks
0:17
that come with GPUs. This lecture is going to be a continuation of that, where we're going to dive more deeply into the code,
0:25
write some Triton kernels, and as well as do some benchmarking and profiling.
0:30
So just to start, just to refresh what's going on with GPUs, here is
0:36
a simplified diagram of what a typical GPU looks like.
0:44
There's memory, and then the actual GPU chip.
0:49
And just to keep in your head what are the characteristics of a GPU.
0:56
So of course, we're talking about NVIDIA GPUs, but Tatsu also talked about CPUs, and there's AMD and so on.
1:03
But focusing on NVIDIA GPUs. We've had multiple generations, from A100s, to H100s, to B200s.
1:11
And in each generation, you have a GPU. It has a number of Streaming Multiprocessors, or SMs.
1:21
And the number of SMs is about 100, between 100 and 200.
1:27
So that hasn't really changed that much. And then within the individual SM, there's a set of registers.
1:38
So B200s have 65,000 registers for a total of 256K per SM.
1:49
And this also hasn't changed that much. In addition, there's a L1 cache as well as shared memory.
1:57
So remember, these are the same memory, shared memory you can control. L1, you can't. This is per SM.
2:04
And this is in the same order ballpark of size. And then there's L2 cache.
2:12
And this is not per SM, this is for the whole chip. And this is a bit larger.
2:17
And then finally, you have a High Bandwidth Memory, or HBM, which is large. And you see that this is the number that's
2:24
actually going up quite a bit. OK. And then in addition to size, you
2:32
can think about what's happening with the bandwidth. And essentially, it's inversely correlated.
2:40
So registers are very fast, L1 is slightly less fast,
2:45
L2 is less fast, and high bandwidth memory is the slowest. Although eight terabytes a second
2:53
is still not that slow in the grand scheme of things.
2:59
So this is the main hierarchy you should have in your head. Large memory is slow and far but big.
3:06
And fast memory like registers and L1 resides on the SM.
3:11
It's local and it's fast, but small. OK, that's the environment we're dealing with.
3:18
OK, so then, how do you program a GPU? So the program model is as follows.
3:24
So there's notions of threads, which each thread executes a piece of code on a small part of the data.
3:30
So each one of these arrows, you can think about it as a thread. These threads are organized into thread blocks,
3:37
also known as Concurrent Thread Arrays, or CTAs. And this is a group of threads. And then finally, you have a collection of thread blocks
3:45
which forms the grid. So when you launch a kernel, you're basically launching a grid of threads and thread blocks to all parallel
3:55
simultaneously do some computation. OK, so this is simplified.
4:02
There's some other things that are happening. For example, there's H100s and B200s also
4:07
have thread block clusters, which are clusters of thread blocks that
4:12
enable some amount of distribution memory. Also, B200s have tensor memory.
4:18
Now for tensor cores, which is somewhere between registers and shared memory. Some of these things are invisible to the programmer,
4:25
but they are in the hardware. But we won't worry about them for this intro lecture.
4:34
OK, so one thing you might ask about is, why do we have thread blocks? Why can't we just have a grid of threads,
4:40
and each thread just takes a piece of data and does something? And this would be normally fine if all you want to do
4:48
is element-wise operations, which we'll see. For example, GeLU is an activation function that applies element-wise.
4:54
And threads are pretty natural. Each thread processes one element. So it's like Fi for i equals ranging over your data set.
5:05
But for operations that involve communicating with threads,
5:13
such as softmax or matrix multiplication, this view isn't really enough.
5:19
And the reason is that, well, it could be enough if you were willing to pay the cost of writing and reading HBM.
5:29
Because then you can still have-- for example, in a matrix we'll see later, you can just have every element in a cell.
5:35
It goes and computes that element of the matrix. And it can just read and write HBM.
5:40
But as we've noticed, HBM is very slow, so this would not be a good strategy.
5:48
So instead, what we should do is use shared memory, which is local to SM.
5:56
And what the thread block allows you to think about is a collection of threads that are all going
6:02
to access this shared memory. So consequently, you can think about a thread block,
6:10
one of these thread blocks is being scheduled on one of these semantic--
6:17
not semantic, streaming multiprocessors. And it does its thing. And what it's going to do is it's
6:23
going to read a bunch of data from HBM and then process it where the processing might involve
6:30
communication between the threads via the shared memory, and then writes it back out.
6:37
OK. So this is a critical piece that comes up. Later, we'll talk about tiling, and that's the whole game here.
6:45
OK, in Triton, in fact, which we'll see as a primary way that we're going to write kernels these days,
6:52
we're going to think natively in thread blocks. And I think once you get the hang of thinking
6:58
of thread blocks, this makes a lot of sense and makes your life a lot easier, as we'll see.
7:04
OK, so the programming model is fairly simple, I think.
7:10
There's threads, thread blocks, and grids.
7:16
I think things get a bit more complicated, is the interaction between the programming model and the hardware.
7:23
So the programming model is actually very nice. It provides abstraction of the hardware. When you can write a kernel, all you have to know
7:32
is that there's a bunch of thread blocks. You define them and then define what computations each thread within the block or the thread block
7:42
has to do. And those are just writing-- it's like writing Python. So that part isn't hard.
7:49
And in fact, if you don't care about-- if you just care about correctness, that's all you need to know.
7:55
But in practice, the performance is very sensitive to the hardware. And so you need to really deeply understand the hardware
8:03
to obtain high performance. And in fact, the whole reason we're talking about GPUs and kernels is
8:08
that you're trying to squeeze out performance. So there's these two levels here where you're trying to understand the computation,
8:16
and that is only a part of the programming model. But how fast it runs is going to be strongly
8:23
dependent on the hardware. OK, so I'm going to give you some examples of why this
8:29
matters, and some of this will be review from what Tatsu talked about. But hopefully it will reinforce some of these concepts.
8:37
So I think there will be around five different examples. And this is just to give you a flavor of the considerations.
8:47
So there's something called warps, which I didn't talk about. So in the very simplified view, warps
8:54
are not really part of this clean picture of the programming model. You have threads and thread blocks or grids.
9:01
Technically, you can also exceed the warps, but you don't have to when you're just programming.
9:08
So what is a warp? So each thread blocks, remember, is a collection of threads.
9:14
But these threads are actually grouped into warps. And there's basically 32 threads per warp.
9:22
So for example, if you have 64 threads in a thread block, there's two warps, the first 32 and the second 32.
9:29
And as Tatsu mentioned last time, all threads within a warp, all of these T's, must
9:35
execute the same instruction in lockstep on SM. So every cycle, they have to execute
9:42
exactly the same instruction. And control divergence is when different threads within a warp
9:49
need to actually execute different instructions. For example, if you have branching, if something,
9:55
then A else B. And then what happens is that you can only do the A's in your threads in your warp,
10:04
and then you do the B. So this gets sequential. So this is bad and inefficient, and that's
10:09
why branching is something you want to generally avoid.
10:14
One thing that's, I think, really cool about warps is that a SM actually is running multiple warps.
10:23
There's a warp scheduler, and there's a bunch of resident warps that are about to run.
10:29
Each warp has some-- threads have registers. And the SM can switch between them with zero cost.
10:40
Right, this is not true in general, like on a CPU. But this way that it's designed is to hide latency.
10:48
So this is important, because one of the warps is, for example, reading from HBM,
10:54
which remember is very expensive, can take 100 cycles or something.
10:59
You don't want to just sit around waiting for that warp to do nothing. You switch immediately to another warp
11:05
where it can actually do some tensor core operations. OK, so that's one thing to note about the warp.
11:13
And another reason why warp comes up is this idea of occupancy.
11:20
More specifically, warp occupancy. So each thread, just the hardware constraint
11:26
says that each thread can use at most 255 registers. OK, and so what happens is that the SM has
11:35
a fixed number of registers. So the more registers each thread is using, the fewer threads you can have.
11:40
That's just math. And that can reduce your occupancy.
11:50
But that's not necessarily bad, because if you have fewer threads. But each thread is doing more work, that can actually be good.
11:57
So occupancy is something that you can measure, but it's not necessarily the larger, the better.
12:04
Because there are some other tradeoffs here. And in fact, an example where you
12:10
might want to have fewer threads is this idea of thread coarsening, where let's say
12:17
you have an element-wise operation and you can have one thread just perform work on one element.
12:27
OK, so then you have a lot of threads. But you can also say each thread processes multiple elements.
12:33
Like a constant, maybe eight. So that gives you fewer threads, which makes scheduling and these things easier,
12:40
but each thread is doing more work. So if your threads are very light, maybe you want to fatten them up a little bit.
12:47
OK. So just as an example of what might happen here.
12:53
So suppose you have a block, a thread block with 128 threads in it.
12:59
And each thread is using 160 registers. OK. So there's a hardware constraint.
13:06
So the B200s have maximum of registers of 65,000 per SM.
13:13
And it also has a constraint that says you can't run more than 64 warps at a time.
13:20
So then you can go and do some simple math to compute what is the occupancy here.
13:27
So number of registers per thread. This has to be less than 255. That's fine.
13:33
The number of registers that you're using per block is the number of threads per block times the number of registers per thread.
13:39
That's about 20,000. That means you can run at most three blocks on your SM
13:44
concurrently. And then that corresponds to 12 warps.
13:51
And because the number of maximum warps is 64, that means you have an occupancy of 18%.
13:58
So we're only running 18% the total number of warps you have.
14:04
And this is because you have a lot of registers use per thread. OK, so this is an example of where I guess this is memory,
14:14
is constraining you in terms of how much compute you can do.
14:21
OK, here's another example. Something called bank conflicts. This applies to shared memory.
14:28
So remember, shared memory is like L1. It's on an SM.
14:33
And just the way the hardware works is that shared memory is divided into 32 banks, each one four
14:40
bytes wide. OK, so these are my 32 banks, and this is my memory here.
14:48
Each bank has many elements here.
14:53
So there's a constraint that says every clock cycle,
14:58
each bank can only be accessed by one thread, at most one thread.
15:04
Assuming that's not a location. So for example, if you can't access this location and this location at the same time.
15:14
Which means that if you have multiple threads that are trying to access the same bank,
15:20
the accesses have to be serialized. And this is called a bank conflict.
15:25
In the worst case example, imagine this were a matrix that was laid out like this.
15:30
And you had some operation where 32 threads, you think, wow, this 32 is so great.
15:36
I can massively parallelize this, and you try to all access this first column. They're just going to just wait in line.
15:42
And this is a 32-way bank conflict, which is the worst possible setting you can be in.
15:50
Now you could say, well, OK, why did you do that? Just like access rows, of course. But this is unavoidable, because, for example,
15:56
if you're doing a matmul, you have to access rows of one matrix and columns of another matrix.
16:02
And sometimes you do transposes, so you can't always just get away with choosing the order in which you go down.
16:12
For element-wise operations, it's fine. You can go in any order. But if you're doing matmuls, you can't
16:17
control for every matrix which row major or column major you're doing.
16:24
There are some solutions here, which I won't get into. Something called swizzling, which arranges your shared
16:29
memory so that when you're going through, you can avoid bank conflicts.
16:37
OK, so that's another consideration. And when you're profiling, you can look at the bank conflicts,
16:43
you can look at occupancy. And you can see what's happening.
16:49
A final note which-- actually, two more things. So memory coalescing, which you talked about.
16:55
So I'll just really quickly remind people what this is about. So when you have 32 threads in a warp and they try to access HBM,
17:05
the memory accesses actually get combined into a transaction of 128 bytes, which are called cache lines.
17:14
And it goes and fetches it all at once. So imagine you have memory laid out like this.
17:21
This is 32.
17:27
And in the best case, which is called full coalescing,
17:34
all the threads are accessing the same cache line. So you have thread 1 accessing M00, thread 2 accessing M01,
17:42
and so on. Which means that all at once, you're going to grab this entire cache line.
17:49
Right, and whereas if you go down the columns, then you're going to fetch a lot of memory
17:59
here, which you're not going to use. Same for the second row and so on.
18:04
And so this feels similar to bank conflicts, but it's a very different constraint.
18:10
And this is about shared memory, and this is about HBM. OK.
18:15
OK, so final thing, block occupancy. So thread blocks are, remember, scheduled onto SMS.
18:23
But we live in a finite-- logically, you can define as many thread blocks as you want.
18:29
But physically on chip, you only have a certain number of SMs. For example, 148.
18:35
So if you launch 160 thread blocks, then you can only schedule 148 of them.
18:41
And then you have to wait until they're done, and then you schedule the 12. But what happens if you schedule 12
18:47
is that there's a bunch of SMs are just not doing anything. So that's called low occupancy.
18:55
When the last wave of thread blocks gets fewer than the total maximum number of thread blocks.
19:05
OK, so in general, maybe it's a good idea to make the number of thread blocks divide the SMs.
19:13
So maybe just to summarize here, there is a very elegant programming model
19:20
where you have a grid of thread blocks and the thread blocks have individual threads in them.
19:28
And in terms of the memory, HBM is global to everyone. Shared memory is local to a thread block.
19:35
And registers are local to a thread. But again, all the details of the hardware-- warps,
19:42
bank conflicts, memory coalescing, occupancy-- really determine performance.
19:49
And many of these details are hard to know, because, I mean,
19:56
the profiling tells you a bunch of information. But you have to know exactly how many SMs there
20:04
are and exactly the sizing of everything. Sometimes the scheduler does something
20:10
you don't really have control over what it's doing. So it's a little messier than the programming model.
20:19
OK, I will stop there for questions. Yeah? Yes, a quick question. So Cam, is there a scenario in which you
20:27
one can share with them or-- like, for example, in the problem that we see 140 SM [INAUDIBLE] blocks, right.
20:35
And we have to launch into two different ways. Is there no way in which a block can share an SM?
20:44
Yeah, so the question is, can a block share an SM? I think the issue is if you're doing things right, then the--
20:55
I guess it depends on the block. If you have a block that's for example, using most of the tensor cores on the SM,
21:03
then putting another block there isn't really going to speed things up.
21:09
I think fundamentally, there is this jagged problem here of unevenness.
21:16
Because the blocks have to stay together. So you can't take this and spread it out over here.
21:21
The thing you would do is to change your blocksize.
21:28
So you change the number of blocks so you don't get this tail here.
21:36
Any other questions?
21:44
OK, so let's move on. Hopefully you guys are getting more comfortable with GPUs now.
21:55
So now I'm going to talk about benchmarking and profiling. I'm not going to actually maybe say
22:02
that much in terms of content. But I do want to emphasize the philosophy here, which is,
22:09
here's a recipe for success. You benchmark and profile your code.
22:14
You make changes, and you benchmark your profile, your code again. And the reason I'm doing benchmarking and profiling
22:22
as opposed to teaching you try and earlier is because you should always just measure
22:27
what's going on in your code and figure out what the bottlenecks are before you start writing kernels.
22:36
OK, so benchmarking is basically how long things take.
22:43
And it gives you just this end to end time. It doesn't tell you where things are spent.
22:49
But nonetheless, it's pretty useful, because ultimately, that's the thing that you care about, how long things
22:56
are running for. And it also gives you, because it distills things into one number, you can see how things are scaling, let's say,
23:03
with dimension. So there's a nice tool for benchmarking.
23:09
But because this class is language models from scratch, I'm going to do it from scratch.
23:14
But I think I'm doing it just to highlight a few gotchas with benchmarking.
23:21
So let's say we have this operation, matrix multiplication.
23:28
So run operation 2 is this wrapper that basically instantiates two random square matrices of size dimension
23:36
by dimension. And then it returns a function to perform the operation. OK, so matmul, if you call this, basically
23:43
does the matmul of those two random matrices. OK, the random matrices are generated,
23:49
it's just like multiplying the two. OK, so how do you benchmark?
23:55
So the naive thing to do is just start time, run it, and then stop time.
24:01
But there's a few things. One is that always remember to do your warm-up.
24:07
I think I mentioned this on the second lecture, but I think it's worth emphasizing.
24:15
And this is because if you have some things are lazily compiled and you want to just make sure that that thing is--
24:24
that time doesn't factor into. Because most of the time, you care about how fast something is, because you're
24:30
going to run it over and over again, so the initial conditions don't really matter. And then you often want to time it multiple times
24:38
because there is some variance. The proper way to time things is to use these CUDA events that--
24:47
a start event and an end event, which you can call record on. Actually do the computation and then hit the end event record.
24:57
And then remember to synchronize to wait for the CUDA threads to finish, because everything on the GPU
25:04
is happening if you have an async. And this is a synchronization barrier. And then you record the time.
25:12
OK, and then you can do this again, and then again.
25:17
And then here, we're just taking the average.
25:24
If you are being very particular, you probably want to maybe look at the whole distribution,
25:30
the P95 or whatever. But we'll just do the average here.
25:35
And then one thing that you can do with benchmarking is you can, let's say, scale up your matrices
25:42
and see how the time changes. And you can see that in this case, matrix multiplication, as
25:51
expected, it should grow cubically. But notice that there is this flaw where up until you get up
25:58
to almost 2,000 dimensional matrices, things are basically a constant.
26:06
Right, and this is because, as we've discussed, the shapes of these GPUs are built for fairly large matrix
26:15
multiplications. And if you have a 2 by 2 matrix, it's going to be very inefficient.
26:23
OK. So really quickly on profiling.
26:29
Profiling tells you where time is actually spent. Hopefully all of you are familiar with profiling and are doing it.
26:37
Maybe less obviously, profiling, even if you don't care about the time, helps you figure out what's actually
26:43
happening under the hood. Because especially with these high level languages, you write some code, and then it runs and you get some result.
26:49
And sometimes it can be just good to understand what's actually going on. So PyTorch has a built-in profiler.
26:55
In your assignment, you'll use Nsight, which gives you more details.
27:01
But we're going to skip that in the interest of time. So let's start with just if you add two numbers and-- or sorry,
27:13
two tensors in PyTorch, again, a run operation creates two random matrices
27:21
and applies the operation. So in profiling, I'm warming up.
27:27
And then I'm just putting this context of-- the profiling
27:32
context and doing the run. And then let's look at what the profile looks like for just a
27:41
plus b in PyTorch. Normally doing PyTorch, you probably don't think about it.
27:48
It's like, OK, well, these two tensors just get added. So what's actually going on underneath the hood?
27:54
Right, so if you look at the things that it's calling, there is this long name, kernel at CUDA functor add.
28:05
So this is basically a kernel that adds to tensors.
28:11
And the times aren't going to be that interesting, because I'm only adding. So that's going to take 100% of the time.
28:18
But this tells you that, well, underneath the hood there is this thing called add.
28:24
What about matmul? In PyTorch, I'm doing a at b.
28:32
Similarly, there is this long name that describes this particular matmul kernel.
28:39
Cutlass f32, f32, 64x64x16, and so on.
28:46
Notice that if you change the dimensions-- so now I'm doing a 128 by 128 matmul--
28:53
you get actually a different one. If you look closely, that this is 64x64x16, this is 32x32x16.
29:02
OK, so underneath the hood, PyTorch, it looks like you're doing just at.
29:08
But underneath the hood, there could be all sorts of things that are happening.
29:13
So observations. Here, you can see which CUDA kernels are actually being called. These are generally the ones that have the long names.
29:21
And different CUDA kernels are invoked depending on the tensor dimensions.
29:27
The name actually tells you something about the implementation as well. So this name.
29:33
So cutlass is the NVIDIA CUDA library for linear algebra. SM100 corresponds to the Blackwell architecture.
29:42
So this is a kernel that's specifically designed for Blackwell. This is f32.
29:48
And then 64x64x16 is the shape of the tile,
29:54
which we're going to talk about later when we talk about tiling in the context of matmuls.
30:01
OK, so that's benchmarking profiling. Just remember to do it.
30:08
I think we make you do it on the assignment, so you have no choice. OK.
30:14
So let's apply this to another example here on the GeLU.
30:21
So remember, the GeLU activation function
30:27
is this function, which is a typical non-linearity that's
30:32
used. Often, it's approximated with this tanh approximation, which
30:39
is more computer friendly. OK, so naively, if you implement the GeLU,
30:49
you can do it in PyTorch just like this. So you take a tensor, you just take this equation
30:56
and you just put it into PyTorch, OK. That's fine. And you get some number.
31:03
PyTorch also has a built in. If you call functional GeLU, you can get that version as well.
31:12
And you can check that they're the same. You can run the two GeLU versions
31:21
and make sure that the answer is the same for a random input.
31:26
There's also something that people probably have discovered.
31:31
If you haven't encountered this, this is an important thing to know. Is that you can take any PyTorch function,
31:37
you call torch.compile on it, and it generates another function.
31:44
And it does the same thing. OK, so we have three horses in this race.
31:52
We have the naive implementation. We have the built-in implementation. We have the compiled implementation.
31:59
So we can benchmark them. Naive takes three.
32:05
I guess this is 3.75. Built-in is much faster and compiled
32:12
is much faster as well, but not maybe as quite as fast as built-in.
32:18
OK. So what's happening here? Why are these things different?
32:26
They all compute the same answer, but they have wildly different performance characteristics.
32:33
And so here we can pull up the profiler and see what's actually going on underneath the hood.
32:39
So if you look at the naive GeLU here and just do
32:45
a profile, this-- something's wrong with this view, so I'm going to go to this.
32:51
So you see that the profile shows how time is being spent.
32:59
There's a bunch of different kernels on this binary functor unary add.
33:06
tanh is a kernel here. And this corresponds to the fact that in PyTorch, when
33:13
we write the PyTorch expression, you look at the computation graph, and each primitive in the computation
33:18
graph is actually realized in the kernel. Right. And the reason this is slow is that when you launch a kernel,
33:29
the kernel has to read from HBM. Pull it all the way over to your SM, do the computation,
33:36
and write it back. And then the kernel picks that up from HBM and then writes it back, and so on and so forth.
33:43
So you're doing a lot of reads and writes back and forth. Because between kernel invocations,
33:49
things have to go back to HBM. So if you look at what the built-in is doing,
33:58
this is actually not that interesting. There's this GeLU CUDA kernel implementation, which is a single kernel that just implements the GeLU.
34:05
And so why does this exist? Well, because people use GeLU.
34:11
So someone wrote a kernel for it and put it in the standard library. I mean, there's nothing magical.
34:21
So compilation is really interesting here. I'm not going to say too much about how it works,
34:26
but it's really, I think, a fascinating topic where
34:33
you can take a naive implementation, which, remember,
34:38
it's in PyTorch, it has a computation graph. And run a compiler. Which basically, if you look at what's underneath the hood,
34:46
it's just a single kernel. And this is because it's figured out to look at the computation graph and essentially write
34:56
that kernel in Triton. So you can see that this is actually a Triton kernel.
35:03
OK, so naive implementation. Multiple kernels requires multiple reads and writes to and from HBM.
35:10
There's no kernel fusion here. This is slow. The built-in compiler version, there's one kernel.
35:18
Basically all the operation in the GeLU have been fused together into one kernel. So you read from HBM once, you write to HBM once per element.
35:27
And you see that the compiled kernel is a Triton kernel.
35:34
OK, so that maybe is a good segue to talk about what this Triton thing is all about.
35:41
Yeah? [INAUDIBLE]
35:51
Let's see. I mean, I guess it says CUDA kernel implementation, so I imagine someone wrote it in CUDA.
35:57
36:03
OK, any other questions? Yeah?
36:09
Why is Triton kernel faster than this one?
36:15
So why is the Triton kernel faster? So a Triton kernel is actually not faster in this case.
36:23
Oh, sorry. The compiled kernel is one Triton kernel,
36:30
and this is slower than the built-in. Yeah. I think last year when I did this, it was actually closer.
36:38
But these things change, and it's very hardware dependent. And I think none of this is terribly optimized.
36:48
This is just giving you the general idea here. OK, so let's write some Triton kernels.
36:57
So remember our programming model. You have a bunch of threads organized by Thread groups,
37:04
a thread blocks, and there's a grid of thread blocks. And if you were to write in CUDA, which was originally
37:13
developed by NVIDIA, and that's been for years the thing that you do when you write kernels,
37:19
the mental model is, what does each thread do? So you write a piece of code which essentially
37:25
has some ID that identifies which thread you're talking about, and it just executes the code.
37:31
So the nice thing about this is that it's very closely related to what is actually happening underneath the hood.
37:40
And it's fine-grained, gives you fine-grained control.
37:47
But there's cons here, which is that, remember, all these threads are in a thread block.
37:56
And some operations require them to communicate. So what has to happen is that they have to synchronize.
38:05
And they read from HBM all at once. And they have to synchronize and do the computation.
38:13
And you have to basically do that bookkeeping. So if you were doing all element-wise operations,
38:20
this CUDA is just fine. It doesn't really matter. But as you get more complex operations,
38:27
then try and provide some value of the abstraction. So Triton was developed by OpenAI.
38:35
I think by now, it's been pretty standard. You basically specify what each thread block does.
38:42
Generally it's powerful enough, especially for this class that you're getting started. If you really go and want to exploit every single new feature
38:51
of the latest hardware, it might not give you the full flexibility. But let's not worry about that.
38:59
And the conceptual framework to think about in Triton is that you're thinking about, what does a block do?
39:05
A block is going to load data into shared memory, operate it on, and write it back to global memory.
39:13
So in some sense, these blocks are intermediate point between thinking about what individual elements are doing
39:19
as well as thinking about the general operation. And so in PyTorch, you basically define these huge matrices
39:25
and you say multiply them together. And that's the atomic operation. And a lot of what you're thinking about
39:30
is, how do I get things into big matmuls? Right, and at some level, Triton is
39:37
a hybrid between that and the individual elements,
39:43
as we'll see. OK, so let's start with the GeLU example here.
39:50
So let's define an 8,000 dimensional vector and start writing some Triton.
39:57
OK, so Triton is basically going to be you write Python.
40:03
How many of you have written Triton before? Just a show of hands. OK, OK.
40:10
Not that many, which is good, because then you won't be bored.
40:16
OK, so this is normal PyTorch.
40:22
Right, there's no Triton here. But I'm just preparing. And so what I'm going to do is I take this tensor,
40:31
and I'm going to allocate an output tensor. OK, because in Triton, we're not thinking functionally anymore.
40:39
We're just thinking about moving. You have to explicitly read and write. So there's no returning value.
40:47
So I'm going to allocate the output tensor, which I'm going to-- the kernel's going to write to.
40:52
And then so this tensor can be arbitrarily big, right.
40:58
And I can't generally fit this all into one SM, because it's just too big.
41:07
So I need to break it up into blocks. So what I'm going to do is you can think about this x
41:16
as being this array. I'm going to chop it up into blocks. So the total number of elements is 8,000.
41:22
I'm just going to set the blocksize to 1,024 for now. And then I have eight blocks.
41:31
OK. So then I'm going to use this weird syntax to call the kernel.
41:42
So Triton GeLU kernel. This basically in brackets tells me essentially the shape
41:53
of the grid. So basically, this says the grid has num blocks.
41:59
Blocks. And I'm going to, for every one of these blocks,
42:05
invoke this function, Triton GeLU kernel, which I'll talk about in a bit.
42:10
I'm going to pass in an x, y num elements and the blocksize.
42:17
OK. All right, so unfortunately, I'm not going to be able to trace through this,
42:23
so I'll just show you what the code looks like here. So let me get rid of that.
42:30
OK, so this is probably the simplest kernel you can imagine.
42:37
So when you are looking at the triton gelu kernel, now this is before we had x and y.
42:46
So now these are pointers. Right, you can think about these as just integers,
42:52
they're addresses. So you have to get comfortable with that. And then we have the number of elements and the blocksize,
43:00
which are basically actually passed in from here.
43:09
OK, so the way to think about it is that for every block, we're going to have this function being called.
43:20
And what happens is the first thing I'm going to do
43:25
is I'm going to wake up. This block wakes up and say, who am I?
43:30
Well, the program ID is-- basically the PID is basically identifying the block. So PID would be 0 for this block 1, 2, and 3 here.
43:40
OK, so now I have to figure out what data I'm going to operate on. So that's PID times blocksize.
43:48
So this is the offset into x pointer. So if PID is 0, then start is 0.
43:56
If PID is 1, then start is blocksize. If PID is 2, then it's 2 times blocksize, and so on.
44:03
OK. So now, I'm going to figure out the span I'm operating on.
44:09
So offsets is start plus. This is the Triton library.
44:17
A range 0 blocksize, which conceptually gives you the integers 0 through blocksize minus 1.
44:24
So offsets is going to be essentially for-- let's say block 1, it's going
44:31
to be blocksize blocksize plus 1 all the way to blocks 2 times blocksize minus 1.
44:36
OK, so in this case, num elements divides by blocksize.
44:45
But in general, that's not the case. So you'll often see in train code, there's this masking. Which says, well, sometimes if the-- let's say the tensor only
44:54
goes up to here. Then I'm going to form a mask, which is going to be true up until that point
45:01
and false after that point. And for blocks that are not the final block,
45:07
it's going to be just all ones. OK, so now I've done the setup.
45:13
What I do is I read. And this is basically pointer arithmetic. So x pointer, remember, is the integer
45:20
that specifies the memory location of where x is. I'm going to add offsets to that,
45:25
which gives me the first blocksize number of elements
45:30
there. According to the mask, if I'm masked out, then I don't read it.
45:38
And then now I think you can just think about this a vector.
45:44
And then you do your normal computation. And then you get y.
45:52
And y is the same size as x. And then you do tl store y pointer
45:58
plus offsets y and the mask. OK, so this load loads from HBM, does some stuff,
46:07
and then it writes back to HBM. OK, I'm going to stop there and take any questions
46:17
about this first Triton kernel. Yeah?
46:22
[INAUDIBLE]
46:33
Yeah. So the difference is, what is the difference between CUDA? And for this element wise, it looks pretty much the same,
46:41
right. And in fact, CUDA is even, I think, simpler, because it really is element wise.
46:47
You wake up, and you identify the thread. And then you just operate on that element.
46:53
Now the only thing here is that it's like the vectorized version, where you have a block
47:00
and you operate on that block. Later, we'll see that operating on blocks
47:06
is doing more than-- if you do something more than element-wise, then CUDA is going
47:11
to be a lot more annoying to work with. So a bunch of questions. Yeah, back there.
47:17
How does this relate to if you wanted to use the tensor units? So how is this related to if you want to use the tensor units?
47:25
So I'll later show you what this code actually compiles down into.
47:31
And maybe I'll get back to that. But the short answer is that you don't control that.
47:37
The hardware figures out where to put things.
47:43
Yeah? Can you actually walk through what's happening at tf.load from HBM to share to register?
47:51
What's the step by step where data is actually flowing? Yeah, so the question is, what is actually happening when
47:57
this statement is executed?
48:02
So the short version is that this is in some sense a lie. Right, it's not like the GPU actually
48:10
calls the Triton library on the GPU and it's actually executing this code.
48:15
This is basically for our consumption to specify the computation.
48:21
The compiler takes this, as we'll see later, and writes it into something called ptx, and then it will actually do the work.
48:29
So that's mechanically what's happening,
48:35
which I'll get to later. If you're asking about the conceptual question,
48:42
the way to think about this is that this--
48:49
x pointer is a memory location in HBM.
48:55
And this basically specifies a range of memory locations. And load takes those memory locations
49:00
and returns the data associated with them. Here, I have a local variable called x.
49:08
In practice, this is going to be generally a register or shared memory.
49:16
Triton figures out what to do there. [INAUDIBLE]
49:22
--at the threat level. When this is invoked, it's not really clear.
49:28
Did I line up what is going to be in shared
49:33
and going to be at the register level beforehand, or is it happening now, which is way late?
49:39
And so then so that's just where all the threads are sitting idle to get a memory to flush in.
49:45
I'm sure it's ready to execute so that we're not
49:52
sitting there twiddling our thumbs, just waiting for data to come in from HBM. Yeah, yeah. So the question is, when does this actually get executed,
49:59
and doesn't this block? Let me try to come back to this question when I show you the ptx, and maybe hopefully it
50:05
will provide a bit more context on what's happening. OK, any other questions?
50:11
OK, so all the kernels are going to look something like this.
50:19
So I do want to make sure people understand just the general form, which is that you have generally
50:26
your inputs, your outputs. You wake up, you figure out which index you're going to look at.
50:31
You read. You do some stuff, and then you write to HBM.
50:37
OK. So let's talk about ptx briefly.
50:48
So let's see, does this work? Does this link work? OK, so when you write Triton, the compiler
51:00
generates ptx code, which is this intermediate assembly language for GPUs.
51:08
I am obviously not going to go through all of this, but just to give you the flavor of what this looks like.
51:17
Let me actually start with the observation. So a few things. So this is now what a thread is actually doing.
51:30
Not a thread block, because that's been compiled away.
51:35
And so if you look at--
51:41
a few notes here. So this LD global is basically saying load from HBM
51:50
into some registers. And the registers are denoted as the r are integer registers, fr,
51:59
floating point registers. And then you have statements like mov 0 into r5,
52:05
mov 0 into r6, and so on. And then you have multiplication. You're going to multiply this register by this constant
52:14
and then put it into this. So this code gets executed.
52:19
And at the bottom, you should see global store.
52:25
So this is writing back into the HBM. So this gets executed.
52:30
This is actually the code that gets executed on a thread.
52:36
And Triton is basically a layer up above that.
52:42
One other thing to notice is that you see all these blocks and what's going on there.
52:49
And what's going on there is I alluded to thread coarsening, which is that this is one thread.
52:55
But rather than processing a single element, it's actually processing eight elements. So the compiler decided that, well, actually, this thread
53:02
is pretty lightweight. It doesn't do that much, so let's just try to thicken it up a little bit.
53:07
So looking at ptx can give you some of appreciation for what's
53:14
going on underneath the hood. Yeah? So it makes this for each individual--
53:21
[INAUDIBLE]
53:27
So the question is, does it make this for each thread? So this is compiled once.
53:33
And it's the same piece of code that each thread runs.
53:39
And the way that the thread distinguishes itself is that this piece of code gets
53:45
passed basically the thread ID.
53:52
So here, the cta.x is the block index, and tid.x is the thread index.
53:58
So this basically, if this piece of code is running, this tells you which block I'm in.
54:07
And tid.x tells you which thread inside that block I'm in.
54:12
OK.
54:21
OK. Any other questions? And I'm trying to figure out how to--
54:26
OK.
54:32
OK, so that's generally a flavor of your first Triton kernel
54:39
load from HBM compute right back to HBM. And then we saw the ptx, which is the grungy what actually
54:50
happens underneath the hood. There's still a lot of things that are not specified in ptx.
54:57
Like, for example, which SM things are operating on and the warps and everything.
55:03
A lot of those are hardware controlled, so you don't even see. Yeah?
55:09
So ptx code is generated by the compiler, not something that you would normally code?
55:16
It looks like it's assembly code. Yeah, so the question is, is ptx just generated by the compiler?
55:24
So there are people who do write ptx if you really think you're better than the compiler.
55:33
And I think the NVIDIA compilers are generally pretty mature, but some other accelerators that are less developed.
55:42
I think sometimes you just have to reach in and actually handhold a bit more.
55:47
But generally, yeah, you shouldn't need to do that. Yeah? [INAUDIBLE]
55:55
My work will get scheduled onto an SM. I'll get to that tf.load and it's almost
56:01
like a CPU trap call where I'm just waiting for something to happen.
56:07
And now I am turning my thumbs and some other work will get scheduled over me. And then it will do operations.
56:14
Now that you have to upload is done, then I'll reschedule that work and then I'll continue on.
56:20
Is that-- Yeah, that's right. So just to repeat the question or the comment.
56:27
So if you look at this triangle code, these statements, this load, is going
56:34
to block for some number of cycles. And then so this is running on some thread on some warp on some SM.
56:40
And remember, the SM is running multiple warps at the same time. So when you get to that point, it
56:47
can just find another warp to run. And then when this is done, the warp scheduler
56:52
comes back and takes over. Why do you [INAUDIBLE]?
56:57
Sorry. Oh, sorry. I recall from [INAUDIBLE].
57:03
Yeah, so the question is, why four warp schedules?
57:09
I don't exactly the reason behind that.
57:15
OK, so now let's go through some other examples. Maybe just as a quick preview.
57:21
We're going to do three more examples. GeLU is the simplest form.
57:28
Even though the computation has a lot of messiness. It's just element wise.
57:33
So conceptually, in the context of this lecture, is actually very simple.
57:40
Now we're going to look at Softmax, where now you're going to have to do reduction.
57:46
But in this case, we're going to think about the case where row fits on a block. And then we're going to consider the case where
57:53
row doesn't fit on a block, and then we're going to do go up to matmul. And then hopefully by that point,
57:59
you'll have all the ingredients that you need to do the assignment and implement flash attention.
58:04
OK. So so far, we looked at element wise operations.
58:10
Now let's think about other operations that aggregate over multiple values.
58:16
So remember what the Softmax does.
58:21
Let's just think of as a matrix. You exponentiate and normalize each row of a matrix.
58:29
OK, so this is used in intention and used in generating output probabilities.
58:35
Generally a good thing to do. So let's just start with a naive implementation
58:43
and just keep track of what's happening. So here, I'm defining this tensor.
58:49
And here's the naive implementation.
58:55
Actually, I think this is on assignment one. OK, anyway.
59:00
So here, I have an M by N matrix. And so I need to, for every row, compute the--
59:10
I'm going to compute the max of each row. And this is for numerical stability. I'm going to subtract off the max.
59:19
And then I'm going to exponentiate element-wise. And I'm going to sum and compute the normalization
59:27
constant for every row. And I'm going to divide.
59:34
And then that's it. OK.
59:40
So if you count the number of reads and writes, remember, this is just plain PyTorch.
59:47
So this is a different kernel. This is a different kernel. And unless you call to torch and compile, these are going to be different operations.
59:54
And each operation is going to read and write, read and write from HBM. OK, so you have five MN reads, three MN writes.
1:00:06
And in principle, you should really only have much fewer.
1:00:13
OK, so this is a piece of code. Makes sense. Everyone should be familiar what the Softmax is doing.
1:00:21
OK. So let's write down the Triton kernel.
1:00:28
And in some sense, the form is going to be very similar.
1:00:34
Just like in the GeLU. Right, once you do the scaffolding, the core computation looks very much like the naive version.
1:00:42
So what we're going to do is we're going to say each row is a block.
1:00:47
OK. And why do I make each row a block? Well, because now, remember, each row,
1:00:53
I have to normalize and sum. So it's not element-wise. So Softmax is not an element wise. But it is row wise.
1:01:00
So the blocks don't interact. And the blocks don't have shared memory, so that's fine.
1:01:08
There's no shared memory across blocks. So then within each row, I'm just going to do some stuff.
1:01:16
OK, so let's see what happens. So again, I'm going to, in Python,
1:01:22
I'm going to allocate my output tensor. I have this M by N matrix.
1:01:28
I'm going to define the blocksize as basically
1:01:35
the number of columns. Go to the next power of 2 for good luck.
1:01:41
And then the number of blocks is just the number of rows. OK, and I'm going to call this kernel.
1:01:48
So how many blocks are there? M. M. Right, one for each row.
1:01:54
And each block, I'm going to pass the input pointer, the output pointer.
1:01:59
And then I'm going to also pass these strides which
1:02:06
tell me how far to move down.
1:02:14
OK, let me actually go and show you that Softmax kernel, OK.
1:02:20
OK, so what does this look like here?
1:02:28
So I wake up. I'm on a particular row, OK.
1:02:33
And this is going to give me all the columns from 0 to the number of blocksize, which I'm assuming
1:02:41
that's all the columns. I'm going to read.
1:02:48
So basically, I need to figure out where to read from memory, and that's going to be the start of my data.
1:02:55
Plus which row. And the row stride basically gives me
1:03:01
every row is-- basically, this is the number of columns,
1:03:06
essentially. It's going to tell me where how far to go down. And then this is x pointers is basically the addresses
1:03:14
of all the data I need to load. I load them up. And here, I do this thing where if it's masked out, then
1:03:23
I put minus infinity, because that's going to be the equivalent of a 0 for the Softmax operation.
1:03:33
And then this part is essentially the same as the naive Softmax. I'm just going to compute the max,
1:03:39
subtract it off, exponentiate, sum, and divide. And then I write it back.
1:03:45
OK. Yeah.
1:03:51
[INAUDIBLE]
1:03:56
Yeah, so this is the version where each block can just
1:04:03
span the entire row. And you can see that Triton makes this very easy, right.
1:04:13
Because this is as if you were just writing normal PyTorch code.
1:04:20
So if everything fits in a block, you basically-- the thing is, if you can fit it into a block,
1:04:25
you can just write normal PyTorch, almost. Yeah? What if my number of columns, the number of rows,
1:04:34
are bigger than blocksize? Yeah, so we'll get to that. Now the number of columns, rows, in general,
1:04:41
it's going to be much larger than the blocksize. So we'll come back to that, yeah. So if you wanted to do a Softmax per column,
1:04:47
do you have to do [INAUDIBLE]? So the question is if you want to do Softmax by column.
1:04:56
I think that should be fine, because here, we're tracking these pointers, right. So the pointers can be anything.
1:05:05
Basically, I think all you would have to do is change the stride here to basically access the columns.
1:05:12
Actually, it would be here. The column offsets, you would just multiply this
1:05:18
by the row stride, I think. OK, let's move on.
1:05:23
All right, so now, warming up to the matmul.
1:05:30
Suppose that your row doesn't fit into a block. OK, so what do you do in this case?
1:05:36
So for example, if you have, let's say, a row that's 4,000 columns, but the blocksize is only 1,024.
1:05:46
So you have to do something here. So here's the strategy.
1:05:52
We're going to break up the row into tiles. So in this case, there's going to be four tiles.
1:05:58
And each thread is going to iterate over the tiles and accumulate a sum.
1:06:04
And then finally at the end, we're going to do the reduction by summing everything
1:06:15
that each thread produced. So I'll show you an example of this. And now I'm switching from Softmax
1:06:21
to row sum because it's just easier to think about.
1:06:27
OK, so here, OK, well, this is not very interesting.
1:06:32
The built-in row sum does what you would expect. So the row sum operation just basically takes a matrix
1:06:42
and then computes the sum of each row, OK. So just no surprises there.
1:06:48
OK, so conceptually what we're going to do here is as follows. OK, so each block is still in charge of one row.
1:06:57
So that part hasn't changed. So suppose block one, row one. I wake up, and what do I do?
1:07:03
I'm going to remember, now, there's tiles. And suppose tile 0 is columns 0 through 3,
1:07:08
tile 1 is columns 4 through 7. And then tile 2 is columns 8 through 11.
1:07:15
OK, so what I'm going to do here is that I'm going to iterate now.
1:07:20
So first, I basically process this, all the threads.
1:07:26
Basically, each thread keeps a kind of accumulator and processes the first tile.
1:07:32
And it's going to move on to the next tile, and it's going to add the current element
1:07:38
to the accumulator. So here, I'm putting 3, 1, 4, 1. And then the second loop iteration,
1:07:47
I'm going to add 5 to this. I get 8. I add 9 to this, I get 10. 2 to 4, and 6 to 1, right.
1:07:54
So each of these four threads is going to be accumulating its own thing.
1:08:00
And then at the end, I do another for tile 2. I add 5 and 3 to the respective accumulators.
1:08:08
So at the end of the day, I have this, a vector of accumulators.
1:08:13
And then that is actually-- that, I can just sum up.
1:08:21
OK, so let's see what the code looks like here. So I'm just going to call the row sum kernel.
1:08:28
And let's see. OK, so here's what it looks like.
1:08:35
So wake up. I am on a particular row. And this is what one row looks like.
1:08:42
There's one tile, a second tile, a third tile, and so on. So n is the number of elements of that row.
1:08:50
And blocksize is the size of this tile.
1:08:58
OK. So remember, blocksize is the number of threads. I'm processing more data than the larger than the blocksize.
1:09:04
I just have to do it iteratively now. So I'm going to loop over all of the tiles.
1:09:10
So start is going to go from 0 to blocksize to 2 times blocksize, and so on and so forth.
1:09:18
So each time I'm jumping across, I am doing calls.
1:09:26
Basically get the offsets at the particular tile.
1:09:35
And then I'm going to load the data from HBM, and I'm going to add it to the accumulation.
1:09:42
So this accumulation is going to be either in registers or shared memory.
1:09:48
OK. And then finally after I loop over all the tiles, I process the whole row, I get basically--
1:09:54
for every thread I have accumulator of what that thread has picked up. And I can just do a sum to get a scalar, and I write it out.
1:10:03
OK, so this is a little bit more complicated than before, because now we have a for loop within a thread.
1:10:10
And this is necessary when your data doesn't fit within a block.
1:10:18
Yeah? [INAUDIBLE]
1:10:25
Just in case we don't have enough register. We have to put it in--
1:10:30
Yeah, so the question is I think if you can control where the accumulator resides. At least in this Triton program, you don't explicitly say.
1:10:39
And this is up to the Triton compiler to figure out where to put it.
1:10:45
But in general, if the blocksize is large enough, it has to go in shared memory.
1:10:52
OK. All right, does this make sense?
1:10:59
Just to, I think, make sure people are on the same page.
1:11:06
Remember when GeLU, we also split a row into a bunch of pieces.
1:11:11
But those were blocks. Right, and so each of those pieces was a block that was processed independently.
1:11:18
These are not blocks. These are tiles. The block corresponds to this whole row.
1:11:24
And the block has to basically process all the tiles. And so this is where it starts to not look like PyTorch,
1:11:32
because you're not able to process all your data in one nice kind of-- not everything fits into shared memory.
1:11:40
Yeah? [INAUDIBLE] --also stored in shared memory? Yeah.
1:11:45
Let's say the accumulator is stored in shared memory. Yeah.
1:11:51
OK. All right, so let's now maybe go on
1:11:57
to our finale, which is matmul. OK, so matmul, of course, is the bread and butter
1:12:03
of deep learning. It's been optimized to death. And it's, in some sense, very fundamental operation.
1:12:14
So you take two matrices, you multiply them. I'm going to add a little bit of a twist here. I'm going to do a matmul followed by a ReLU,
1:12:21
just because, just for kicks. OK. Well, this happens, right.
1:12:28
Because if you have one linear layer, it's a matmul. And then you apply a ReLU activation.
1:12:34
So this is not completely out of nowhere. But I'll show you later why I did this.
1:12:41
OK, so how do you build a matmul kernel? All right, so here's a naive approach.
1:12:48
OK, so here's my, let's say, a matrix. I'm multiplying A times B. And I'm
1:12:54
trying to write out the matrix C. And A is M by K, B is K by N.
1:13:01
So C is M by N. OK, so what I'm going to do is I'm going to fix one of these elements.
1:13:09
Let's say M equals 1, N equals 2. OK, so I'm processing-- actually, let's do M equals 1,
1:13:16
N equals 1. So I'm doing C5. And then, basically for every k, so I'm going to, yeah,
1:13:28
I'm going to iterate over the k, the rows of A and the columns of B. I'm going to read from HBM.
1:13:37
I'm going to multiply them, accumulate that. And at the end, I'm going to write out to this and this element.
1:13:44
So that is a valid matmul kernel. OK. So what's wrong with it?
1:13:51
1:13:57
It's correct. But if you look at how many reads and writes it's doing,
1:14:03
this is not good. Right, so basically for every M and N and K,
1:14:08
I have to read from HBM. So it's on the order of M times K times N reads.
1:14:14
Number of writes. I mean, it doesn't matter, but this is the bottleneck, right. And if you remember from the second lecture,
1:14:22
if you look at the number of operations you're doing divided by the number of bytes that were transferred, that's the arithmetic intensity
1:14:30
which you want to be high. So the number of operations is M times K times N order.
1:14:36
And then the number of reads is also the same. So arithmetic intensity is a constant, which is not good.
1:14:43
OK, so if you look carefully, you notice that there is a lot of redundant reads.
1:14:49
So imagine computing C4. You needed to read A4, A5, and A6.
1:14:56
And if you compute C5, you're going to have to read those over again as well.
1:15:02
So if you can just read those once,
1:15:07
then you really save on reads, and that's great. So let's try to use shared memory to do that.
1:15:14
So here's the idealized approach. I'm going to load all of A and B into shared memory.
1:15:21
And then I'm going to just compute C. So if I can do that, then I get--
1:15:28
now I don't have this cubic number of reads, I only get quadratic number of reads, which means I get the error intensity of order n, which
1:15:37
in the second lecture, I said was a kind of ideal thing you could hope for.
1:15:45
OK, so if you can do that, that's great, because you basically-- there's no redundant reads.
1:15:50
You read everything once into shared memory. You do the computation and you write it back. OK, but what's the problem with this idealized approach.
1:15:58
The problem is that A and B are usually
1:16:05
too large to fit into shared memory. Right. So then, what do you have to do now?
1:16:14
OK, so the idea here is this very classic idea of tiling.
1:16:19
And the idea basically is to, well, fit as much as you can to shared memory as you can, essentially.
1:16:26
So in some sense, it's going to look like the naive-- it's going to globally look like the naive
1:16:32
approach, but locally it look like the idealized approach is the way to think about it.
1:16:38
So here's the picture. I think Tatsu showed this as well.
1:16:43
So what we're going to do is we're going to take this matrix C, and instead-- remember, the naive approach just said for every element,
1:16:50
I'm going to compute it. But now I'm going to say for every tile, I'm going to do it.
1:16:55
So I break it up into tiles. And each of these tiles is going to be a thread block.
1:17:02
And I'm going to have a bunch of threads that is responsible for computing this. Now a different tile is going to be
1:17:09
computed completely separately by another thread block. OK, so I'm going to--
1:17:16
imagine I'm in Triton. I wake up. I'm looking at this thread block.
1:17:21
So what do I have to do? Well, in some sense, it's the same as the naive approach where, for every row tile of A,
1:17:32
I'm going to go scan across the rows. And for every column tile of B, I'm
1:17:38
going to load the corresponding tile A and the corresponding--
1:17:43
so say I'm here-- and the corresponding tile from B into shared memory.
1:17:48
I'm going to multiply these two together. And that's going to be like the idealized approach.
1:17:58
And I'm going to accumulate that in the partial sum. And this is all sitting in shared memory.
1:18:04
And then after I finish all the sweep of the row and the column here, then I can finally write this output tile to HBM.
1:18:11
OK, so that's the conceptually what's happening.
1:18:20
And then the arithmetic intensity here now goes up to order tile size. So you can generally not reach order n,
1:18:27
because that would require you to fit everything into shared memory. But if your tiles are big, then that's still not too bad.
1:18:36
OK, so just as a bonus. While you're doing all writing a kernel for this
1:18:43
anyway, sometimes if you want to apply an element-wise activation function, it's very easy to just put it on at the end.
1:18:51
And this is kernel fusion. OK, so very quickly, the implementation here.
1:18:58
So-- oops.
1:19:03
OK, so just a reminder about strides since this is going to show up.
1:19:09
So a tensor is a multidimensional array.
1:19:14
But in memory, it's linearized. And strides of a tensor tells you basically
1:19:19
how to map from a multidimensional index such as a row, a column, into an actual index.
1:19:26
And basically what you do is multiply the row by the stride, the plus the column by the stride of the column.
1:19:33
OK, so in this case, every time you go advance to the next row, you go four positions in your memory.
1:19:40
And every time you advance a column, you go one. And if it were to transpose, it would be flipped.
1:19:46
OK, so what does this kernel look like here?
1:19:54
OK, so the launch is not interesting. So you wake up and you are on tile M and N.
1:20:02
So it's like OK, I'm responsible for computing the c matrix,
1:20:07
but for the M comma N tile there's a bunch of index manipulation which you can--
1:20:15
I'll just gloss through, but it's straightforward but a little bit.
1:20:20
You just have to track the indices. So this basically tells you which rows of A, the matrix A,
1:20:29
I'm looking at. Which rows of-- oh sorry, which columns of B I'm looking at. And then this is just the numbers 1 through K.
1:20:38
Then I'm going to get the pointers into A and B at my tile location.
1:20:46
And then I'm going to set up this accumulator matrix. This is going to be in shared memory.
1:20:51
It's M by N. And then this is going to look like the row reduction, right.
1:20:57
You have this sum over all the tiles. But instead of just going across the tiles,
1:21:04
I'm now going across the row tiles and also simultaneously down the column tiles of B. I load A, the small matrix, I load B, the small B
1:21:13
tile. And then I perform this dot. So remember, whenever things are in shared memory, things look like PyTorch and I can just say, matmul it,
1:21:21
and it will do the thing. And then I advance to the next row tile of A
1:21:29
and the next column tile of B.
1:21:34
The bonus is that if I wanted to apply element-wise non-linearity, I might as well do that here.
1:21:42
Before I write it out to HBM, I can do some any operation on it. And then finally, I just write it out.
1:21:51
OK, so there's some indices that you have to pay attention to. But hopefully, the form of the algorithm is clear.
1:21:59
1:22:06
OK. Any questions about that?
1:22:12
All right. Maybe I'll just summarize, and you can ask me later. So today, we talked about there's
1:22:18
a programming model, which is you're talking about either PyTorch or Triton or ptx.
1:22:25
This is what the kind of programmer can control.
1:22:32
Right, and even ptx. You can write ptx and you can control every-- specialize however you want.
1:22:38
But this is not the full picture, because the reality is that your code has to run on hardware,
1:22:45
and there's only a finite number of SMs, a finite number of banks, and sizing of memory and registers.
1:22:54
All are finite. So you come in with your big matrix and transformer, and want to-- it has to fit in with the constraints
1:23:01
of the hardware. So that's why benchmarking and profiling
1:23:07
are really important to understand how the messiness of the hardware
1:23:13
translates to performance. We talked about Triton, which is,
1:23:18
I think, a pretty nice, neat language to think about thread blocks.
1:23:23
Hopefully by now you can appreciate that things are easier to think about thread blocks
1:23:30
than individual threads, because you don't have to think about explicitly synchronizing threads or doing
1:23:37
shared memory. And the way to think about it is that you figure out-- you have your computation.
1:23:42
You break it down into the thread blocks where you just need to read from shared memory. Do some stuff, and write it back into HBM.
1:23:50
And then we saw some examples of increasing difficulty. Element wise is the easiest. And then reduction over a row.
1:23:57
Reduction where it doesn't fit into a row. And we introduced the baby tiling.
1:24:04
And then matmul is the canonical example where you actually do tiling.
1:24:10
OK, so that's all I'm going to say about how to program a single GPU. Next time, we're going to go to more GPU and talk
1:24:18
about multi-GPU programming. Yeah, question. So if I want to write down kernels,
1:24:24
what are the alternatives? Do I have to try them and-- for each of my choices that I have, how close can I get to the optimal level?
1:24:33
So the question is, what are alternatives to Triton?
1:24:39
So there's a trade off between-- every language has an inductive bias, right.
1:24:47
That makes certain things easier and certain things harder. So most of what we do-- Triton was built by people who train transformers.
1:24:56
So anything involving transformers, I think it's going to be relatively easy there.
1:25:04
Of course, in the extreme, you can always go to ptx and write that. But I wouldn't advise that as a first step.
1:25:10
There are a bunch of other libraries. There's thunder kittens.
1:25:15
There's cute, delirious DSLs that allow you to give you-- they're not necessarily comparable
1:25:24
either up or down the stack. They just give you different characteristics.
1:25:30
Yeah? Do you have a high dimensional tensor magnification or high dimensional tensor processing?
1:25:40
What is the best approach? What I'm thinking is can I load unload the whole sensor, both
1:25:49
of the sensors on bunch of threads or thread blocks on my GPU at the same time and compute them?
1:25:57
Or would it be better to take each element,
1:26:03
just what we did here, right. A particular component of my sensor,
1:26:09
then process it and then write it back in the end. Yeah, so briefly, the question-- and we should probably wrap up--
1:26:15
is, is it better to read all at once or maybe process individual elements at a time?
1:26:24
I think it's hard to answer this in the abstract. It depends on the nature of the computation.
1:26:30
Maybe we can talk offline about that. OK, all right. See you next time.




&nbsp;



⬆️⬆️⬆️  END OF TRANSCRIPT  ⬆️⬆️⬆️

## §2 — Screenshot / visible source details  ·  layer: `raw_source_metadata`

*Nothing to paste here — just **drop the screenshot in chat** and the normalizer fills §0. (Or paste the visible title / channel / URL / date below.)*

## §3 — Interpretations & review log  ·  append-only (each reviewer adds a new entry; never overwrite)

### Review 001 — Knox / ChatGPT strategic read  ·  layer: `captured_interpretation_nonbinding`
- reviewer: `Knox / ChatGPT` · type: `AI assistant` · at: `TK` · purpose: `strategic source-local interpretation`

> Knox's read is the **strategic source-local interpretation** — paste **exactly what Knox outputs**, however rich, in ONE block. Do NOT split it into fields or trim it. Knox's output typically already carries: priority/depth · source type + authority context · why this matters for OMNI · core takeaway · OMNI translation · likely landing zones · doctrine/primitive pressure · keeper doctrine · what-not-to-import · hard read. (What we ASK Knox to produce = `_KNOX_STRATEGIC_READ_PROMPT.md`.) The agent's §3 Review 003 **formalizes** this (it does not re-derive it).

⬇️⬇️⬇️  PASTE KNOX'S / CHATGPT'S FULL READ BELOW (as-is)  ⬇️⬇️⬇️

This one is targeted semantic / high yield for OMNI’s Build OS, agent execution, kernel/runtime abstraction, profiling discipline, context-packet design, and “don’t trust high-level abstractions until you inspect what they compile to” doctrine. It pairs tightly with the prior GPU/TPU lecture, but moves one layer down into benchmarking, profiling, Triton, PTX, kernel fusion, reductions, tiling, and the gap between clean programming models and messy hardware reality. Transcript is from the uploaded file.

Rough metadata for Opus
source_platform: YouTube
source_url: https://youtube.com/watch?v=xnDHaNUvHBg
source_title: Stanford CS336 Language Modeling from Scratch | Spring 2026 | Lecture 6: Kernels, Triton, XLA
channel_or_org: Stanford Online
course_context: Stanford CS336 / Language Modeling from Scratch
lecture_context: Lecture 6 / Kernels, Triton, XLA / continuation of GPU systems section
speaker_context: Likely Percy Liang based on transcript reference to Tatsu's prior lecture; video description lists Percy Liang and Tatsunori Hashimoto
speaker_affiliation_context:
Percy Liang: Professor of Computer Science, Stanford University
Tatsunori Hashimoto: Assistant Professor of Computer Science, Stanford University
published_at: Apr 28, 2026
captured_at: 2026-06-10
captured_by: Nick
capture_method: screenshot + transcript paste
content_type: technical AI systems lecture / GPU kernels / Triton / PTX / benchmarking / profiling / kernel fusion / softmax / reductions / matmul tiling
source_reliability_context: Stanford course lecture | practical systems source for GPU programming, profiling, and performance-aware implementation
screenshot_metrics_at_capture: 5,376 views; 90 likes
priority: 4.5/5
depth: targeted_semantic
recommended_status: high-value technical systems substrate source; route to Build OS, AI substrate, agent resource accounting, context packet execution, profiling/measurement doctrine, and abstraction-boundary governance
transcript_quality_note: Auto-generated transcript is usable. Some lecture-title content mentions XLA, but the loaded transcript segment primarily covers GPUs, Triton, PTX, benchmarking, profiling, GeLU, Softmax, reductions, and matmul tiling.
topic_tags_light: [CS336, language_modeling_from_scratch, GPU_kernels, Triton, XLA, CUDA, PTX, GPU_programming, kernel_programming, PyTorch, torch_compile, benchmarking, profiling, CUDA_events, synchronization, warmup, profiler, Nsight, PyTorch_profiler, thread_blocks, grids, threads, warps, occupancy, bank_conflicts, memory_coalescing, HBM, shared_memory, registers, L1_cache, L2_cache, thread_block_clusters, tensor_memory, B200, H100, A100, hardware_abstraction, programming_model_vs_hardware, GeLU, kernel_fusion, naive_PyTorch, built_in_kernel, compiled_kernel, Triton_kernel, pointer_arithmetic, program_id, blocksize, masks, tl_load, tl_store, PTX_assembly, LD_global, ST_global, registers, thread_coarsening, softmax, rowwise_reduction, row_fits_block, row_tiling, row_sum, reduction_kernel, matmul_kernel, matrix_multiply, tiling, shared_memory_reuse, strides, linearized_tensor, matmul_plus_ReLU, kernel_fusion_activation, arithmetic_intensity, HBM_reads, exact_implementation, compiler_abstraction, hardware_messiness, performance_measurement, block_level_abstraction, workload_decomposition]
§0.1 rough people / authority context
primary instructor context:
name: Percy Liang
role_in_source: likely lecturer for this session based on transcript phrasing; also listed in video description
affiliation_at_publication: Stanford University
speaker_type: AI researcher / language-model systems and methodology educator
authority_context: High relevance for language-model training systems, practical kernel implementation, benchmarking/profiling discipline, and abstraction-aware AI systems engineering.
additional course instructor context:
name: Tatsunori Hashimoto
role_in_source: course instructor listed in video description; prior lecture referenced in transcript
affiliation_at_publication: Stanford University
speaker_type: AI researcher / systems-aware model training / data-centric ML
authority_context: High relevance for GPU systems, architecture-performance tradeoffs, and practical model-training methodology.
publisher / channel: Stanford Online
course / event context: Stanford CS336: Language Modeling from Scratch
perspective / conflict notes: Technical systems lecture. Strong event context: Stanford CS336: Language Modeling from Scratch`
perspective / conflict notes: Technical systems lecture. Strong source for execution discipline and implementation-performance thinking. Not healthcare-specific. Do not turn Triton/PTX mechanics into product doctrine directly; extract principles around profiling, abstraction boundaries, decomposition, kernel fusion, tiling, and measurement-before-optimization.
suggested_processing:
priority: 4.5/5
depth: targeted_semantic
EVRUN needed?: yes, narrow systems route
likely landing zones: Build OS / AI substrate / agent execution environment / profiling and benchmarking doctrine / context-packet execution / resource accounting / model-router strategy / infrastructure cost model / runtime optimization / compiler/abstraction boundary discipline / D7 derivative recomputation policy
Knox / ChatGPT read for §3 Review 001

Process this one as targeted semantic, high yield.

This is not healthcare doctrine.

It is a systems execution source.

It is most useful because it says:

do not stop at the clean programming model.

Benchmark.

Profile.

Inspect what actually runs.

Understand the abstraction layer.

Then optimize the actual bottleneck.

Core takeaway

High-level code can hide very different execution behavior.

A naive PyTorch expression, a built-in kernel, and a compiled Triton kernel may compute the same mathematical function but have wildly different performance because they move memory differently and compile into different kernels.

OMNI translation:

Two workflows can look equivalent at the product/spec level and still behave very differently in production.

A patient summary,
agent patch,
document extraction,
message triage,
or care-routing flow

may be logically correct but operationally bad if it creates too many tool calls, context loads, human handoffs, API roundtrips, materialized derivatives, or hidden review steps.

Classification

Priority: 4.5/5
Depth: targeted semantic
Authority context: Stanford language-model systems / kernel programming lecture
OMNI relevance: high for Build OS, AI substrate, profiling, benchmarking, agent execution, context packet design, and abstraction discipline
Promotion posture: technical substrate source; extract systems principles, not literal kernel implementation doctrine

The big OMNI translation

This lecture continues the systems story from the GPU/TPU lecture.

The prior lecture established the hardware model:

SMs,
warps,
shared memory,
HBM,
memory hierarchy,
tiling,
fusion,
recomputation.

This lecture asks:

how do you actually write and inspect code that runs on this hardware?

OMNI translation:

It is not enough to understand architecture diagrams.

You need executable work packages, traces, profiles, and proof that the architecture actually behaves the way you think.

Keeper:

Architecture needs execution inspection.

Programming models are clean; hardware reality is messy.

The lecture says the programming model has threads, thread blocks, and grids.

That part is elegant.

But actual performance depends on warps, occupancy, bank conflicts, coalescing, scheduler behavior, finite SMs, registers, and memory sizing.

OMNI equivalent:

The domain model can be clean:

D3,
D5,
D6,
D7,
CNS,
Messaging,
Clinical Memory.

But runtime behavior depends on:

queues,
latency,
human review,
source quality,
identity resolution,
permissions,
tool limits,
API failures,
context size,
agent behavior,
staff adoption.

Keeper:

Clean model does not guarantee clean execution.

Performance requires hardware knowledge.

The lecture says if you only care about correctness, the programming model may be enough.

If you care about performance, you need hardware knowledge.

OMNI translation:

If you only care about a demo, a high-level workflow spec may be enough.

If you care about production, you need operational substrate knowledge.

Keeper:

Production requires substrate literacy.

Warps are hidden from the simple programming model but shape performance.

A programmer may think in threads and blocks, but performance depends on warps of 32 threads executing in lockstep.

OMNI analogy:

A user may think “agent did task.”

But performance depends on hidden execution units:

retrieval calls,
tool calls,
review queues,
decision gates,
database writes,
model calls,
context assembly steps.

Keeper:

Hidden execution units shape real performance.

Control divergence is expensive.

If threads in a warp branch differently, they serialize.

OMNI equivalent:

If a supposedly standard workflow branches too much, it loses throughput.

Highly customized, exception-heavy, unstructured processes are harder to scale.

Keeper:

Branching has execution cost.

Branching is not bad when meaning requires it.

The lesson is not “remove all branches.”

It is:

branch deliberately.

In OMNI, branch for:

risk,
identity uncertainty,
consent,
clinical safety,
payment dispute,
privacy,
authority,
incident response.

Keeper:

Branch for risk, not randomness.

Occupancy is not simply “more is better.”

The lecture explains that high register use can reduce occupancy, but fewer threads doing more work may still be good.

OMNI translation:

Maximizing number of agents, tasks, reviewers, or automations is not automatically better.

Sometimes fewer, richer tasks with more context are more efficient.

Sometimes many small tasks are better.

Keeper:

Utilization metrics need interpretation.

Thread coarsening maps to task coarsening.

In GPU work, one thread may process multiple elements to reduce scheduling overhead.

OMNI analogy:

One agent step may handle a small bundle of related work instead of spawning separate actions for every tiny operation.

Examples:

extract + classify + route,
summarize + cite + propose,
read + patch + test,
triage + draft + queue.

Keeper:

Make work units neither too tiny nor too huge.

Bank conflicts are a hidden shared-memory contention problem.

Shared memory is fast, but if many threads access the same bank, accesses serialize.

OMNI equivalent:

Even a “fast internal substrate” can bottleneck if too many workflows hit the same dependency.

Examples:

one provider approval queue,
one admin bottleneck,
one identity resolver,
one source-of-truth table,
one staff role,
one manual finance review.

Keeper:

Shared resources can serialize parallel work.

Memory coalescing is a layout problem.

If neighboring threads access contiguous memory, reads are efficient.

If access is scattered, reads waste bandwidth.

OMNI translation:

Related source facts should be stored/retrieved together.

Patient episode bundles,
care obligation bundles,
incident timelines,
architecture domain packets,
provider review packets,
D7 source bundles.

Keeper:

Data layout should match access patterns.

Block occupancy creates tail inefficiency.

If you have more blocks than SMs but the final wave is small, hardware sits idle.

OMNI analogy:

Queues and review systems can have tail inefficiency.

A leftover small batch can still require a full cycle.

A single unresolved blocker can hold a release.

A few edge cases can consume disproportionate attention.

Keeper:

Tail work can dominate throughput.

The first big doctrine is benchmark before writing custom kernels.

The lecturer explicitly says:

benchmark and profile first,
then make changes,
then benchmark/profile again.

OMNI Build OS should adopt this as a core rule.

Do not optimize from vibes.

Keeper:

Measure before optimizing.

Benchmarking tells you how long it takes.

Benchmarking gives end-to-end time.

It does not explain why.

OMNI equivalent:

Business metrics tell you that something is slow or expensive.

They do not automatically tell you the bottleneck.

Keeper:

Outcome metrics need trace diagnostics.

Warm-up matters.

CUDA kernels may compile lazily, so initial timing includes setup.

OMNI analogy:

First-run setup cost differs from steady-state cost.

A new agent run,
new provider workflow,
new integration,
new retrieval index,
new employee onboarding,
new patient journey

may have cold-start cost.

Keeper:

Separate cold-start from steady-state.

Synchronization matters.

GPU work is asynchronous, so timing without synchronization can lie.

OMNI translation:

If you measure before all downstream effects complete, you lie to yourself.

A message may be drafted but not sent.

A task may be created but not reviewed.

A patch may be written but not tested.

A patient may be contacted but not resolved.

Keeper:

Measure after the work is actually complete.

Average is not enough.

The lecture mentions that if particular, you may want distribution or P95.

OMNI must measure tails.

Patient-facing latency,
provider review delay,
agent patch time,
tool-call spikes,
support burden,
escalation time.

Keeper:

P95 matters.

Profiling explains where time is spent.

The profiler shows actual CUDA kernels called.

OMNI equivalent:

Traces show actual system steps.

For agents, trace:

files read,
sources retrieved,
model calls,
tool calls,
patches,
tests,
errors,
human reviews,
state commits.

Keeper:

Profile the execution trace.

High-level expressions hide lower-level kernels.

a + b becomes a CUDA add kernel.

A matmul becomes a specific CUTLASS kernel depending on dimensions.

OMNI equivalent:

A high-level action like “summarize chart” may expand into:

fetch records,
extract documents,
normalize labs,
retrieve prior notes,
call model,
cite sources,
route review.

Keeper:

Know what an action expands into.

Different dimensions choose different kernels.

Changing tensor dimensions can cause PyTorch to invoke different kernels.

OMNI translation:

Changing context size, source count, patient history depth, or workflow branch can invoke very different operational behavior.

Keeper:

Shape changes can change execution plan.

Kernel names expose implementation details.

CUTLASS names reveal architecture, type, and tile shape.

OMNI equivalent:

Traces and logs should reveal:

which model,
which prompt,
which context packet,
which source set,
which policy version,
which action schema,
which reviewer.

Keeper:

Implementation metadata belongs in traces.

GeLU is a simple example of hidden performance difference.

The naive GeLU in PyTorch launches multiple kernels.

The built-in GeLU launches one fused kernel.

The compiled version may generate a Triton kernel.

All compute the same function.

Performance differs because of memory movement.

Keeper:

Equivalent output can have different execution cost.

Naive composition creates unnecessary memory roundtrips.

Each primitive reads/writes HBM.

OMNI analogy:

Naive workflow composition creates unnecessary handoffs.

Example:

agent reads source,
writes summary,
another agent reads summary,
another classifies,
another routes,
another drafts.

Keeper:

Naive modularity can create memory/hand-off waste.

Built-ins are fast because someone encoded the common pattern.

GeLU has a built-in kernel because people use it.

OMNI translation:

Common workflows should become built-ins.

Examples:

patient-message triage,
provider-summary packet,
D7 source extraction,
identity confidence check,
lead source classification,
aftercare escalation,
architecture gem-pull.

Keeper:

Repeated patterns deserve first-class kernels.

Torch.compile is automatic fusion.

The compiler recognizes the graph and emits a fused Triton kernel.

OMNI analogy:

The platform can optimize obvious workflow graphs.

But it must preserve boundaries where authority matters.

Keeper:

Compiler-like optimization is useful only inside safe boundaries.

Triton is a block-level abstraction.

CUDA asks what each thread does.

Triton asks what each block does.

This is a major abstraction lesson.

OMNI agents should often operate at the work-package/block level, not atomic micro-step level.

Keeper:

Block-level abstraction can be the right middle layer.

PyTorch is too high-level for some optimization.

PyTorch thinks in whole tensors and operations.

CUDA thinks in threads.

Triton sits between them.

OMNI translation:

Some system layers need a middle abstraction between product workflow and database/action primitives.

Examples:

context packet,
work package,
domain slice,
review packet,
agent job,
care obligation bundle.

Keeper:

The right abstraction lives between macro workflow and micro operation.

Triton kernels have a repeated shape.

Wake up.

Identify program/block ID.

Compute offsets.

Load data.

Apply mask.

Do computation.

Store output.

OMNI Build OS analogy:

Agent job shape:

wake up,
identify work package,
load authorized context,
apply scope/mask,
do task,
write result,
emit proof.

Keeper:

Standard execution scaffold improves reliability.

Masks are important.

Triton uses masks when blocksize exceeds actual data.

OMNI equivalent:

Context/action masks define what an agent may see or do.

Examples:

PHI scope,
tenant scope,
patient scope,
role authority,
time window,
source visibility,
domain permissions.

Keeper:

Masks are security and correctness primitives.

Pointer arithmetic maps to explicit source addressing.

Triton code manually computes where data lives.

OMNI equivalent:

Do not let agents vaguely refer to “the docs.”

Use explicit source pointers:

file,
section,
line,
version,
domain,
artifact ID,
patient,
episode,
event.

Keeper:

Precise addressing prevents context drift.

PTX shows what actually runs.

Triton compiles to PTX, an intermediate GPU assembly language.

The lecture inspects PTX to show loads, stores, registers, thread IDs, and thread coarsening.

OMNI translation:

Sometimes you need to inspect below the comfortable abstraction.

For agents:

inspect traces,
tool calls,
retrieval payloads,
source IDs,
intermediate drafts,
database diffs,
test logs.

Keeper:

Inspect the compiled behavior.

PTX is not normally hand-written, but it is useful to understand.

Most people should not write PTX.

But seeing it helps explain performance.

OMNI equivalent:

Not everyone writes low-level schema/migrations/action logs.

But key architects need to understand them.

Keeper:

Architects need one layer deeper than normal users.

Thread coarsening is compiler-chosen.

The compiler may decide one thread processes eight elements because the thread is lightweight.

OMNI analogy:

Execution planners may bundle small operations automatically.

But the system should reveal that bundling in traces.

Keeper:

Automatic optimization should remain inspectable.

Softmax introduces reduction.

Element-wise GeLU is easy because each element is independent.

Softmax requires aggregation across a row.

OMNI translation:

Some tasks are independent.

Some require collective context.

Element-wise classification is easy.

Longitudinal synthesis, source reconciliation, identity merge, clinical adoption, and incident timeline construction require reduction.

Keeper:

Know when the task requires aggregation.

Row-wise Softmax maps to scoped aggregation.

Each row can be processed independently.

OMNI equivalent:

Many aggregations have a natural scope:

one patient,
one episode,
one document,
one appointment,
one service occurrence,
one incident,
one domain.

Keeper:

Aggregate within the right scope.

One block per row is clean if the row fits.

If a row fits into one block, Triton code looks almost like PyTorch.

OMNI analogy:

If the required context fits into one context packet, execution is simple.

Keeper:

Keep scoped work small enough to fit the working context.

If the row does not fit, tile it.

When the row is too large, iterate over tiles and accumulate partial results.

OMNI translation:

If the patient/corpus/domain context is too large, process in tiles:

current episode,
recent labs,
active messages,
prior adverse events,
historical baseline,
policy packet.

Keeper:

Large context requires tiled accumulation.

Accumulators are compact state.

The row-sum example accumulates partial sums across tiles, then reduces them.

OMNI equivalent:

A long-running workflow should carry compact state:

risk score,
open obligations,
source coverage,
conflict list,
known facts,
unresolved questions,
confidence.

Keeper:

Carry compact state across context tiles.

Tiling lets a global result emerge from local chunks.

This is one of the most transferable ideas.

OMNI can process huge corpora or longitudinal records by local chunks as long as it preserves correct accumulator state.

Keeper:

Global synthesis needs local passes plus state.

Matmul is the canonical tiling example.

Naive matmul rereads the same data repeatedly from HBM.

Ideal matmul loads everything into shared memory but cannot fit.

Tiled matmul loads manageable submatrices and reuses them.

OMNI translation:

Do not repeatedly reread the same source.

Do not try to load everything.

Load reusable source chunks into working context.

Keeper:

Reuse source chunks inside bounded work.

The idealized approach is often impossible.

Loading all A and B into shared memory would be great, but they are too big.

OMNI equivalent:

A full patient history,
full architecture corpus,
all messages,
all documents,
all policies

may be ideal but too large or expensive.

Keeper:

Ideal context is usually too big.

Tiling is the compromise between naive and ideal.

Globally, you sweep the larger structure.

Locally, you act as if a small ideal world fits into memory.

This is exactly context-packet doctrine.

Keeper:

Context packets are local ideal worlds.

Matmul + ReLU shows fusion opportunity.

If you already computed the matmul tile and it is in shared memory, you can apply ReLU before writing out.

OMNI translation:

If you already have the right context loaded, do nearby low-risk work before writing/retrieving again.

Examples:

extract fields,
score quality,
flag missing source,
create review packet,
apply formatting,
generate citation map.

Keeper:

Use loaded context fully before releasing it.

Fusion should not cross authority boundaries.

The kernel can fuse ReLU because there is no authority separation.

OMNI must not fuse:

AI extraction + clinical adoption,
draft + send,
charge + resolve dispute,
identity candidate + merge,
incident detection + disclosure.

Keeper:

Fuse computation, not commitments.

Strides matter because tensors are linearized.

A multidimensional tensor is stored as linear memory; strides map logical index to physical address.

OMNI analogy:

Logical objects live in physical/source representations.

A patient episode may be spread across notes, messages, labs, forms, photos, payments, and tasks.

Keeper:

Logical structure needs address mapping.

Source layout affects retrieval cost.

If OMNI stores logically related data in scattered places with no index, retrieval becomes slow or error-prone.

Keeper:

Storage layout is product behavior.

Benchmarking/profiling are a recipe, not an event.

The lecture’s recipe:

benchmark/profile,
change code,
benchmark/profile again.

OMNI Build OS should do the same:

trace baseline,
patch,
run tests/evals,
measure before/after,
report proof.

Keeper:

Optimization is a loop.

Profiling can reveal incorrect mental models.

You may think PyTorch is doing one operation, but profiler shows many kernels.

OMNI translation:

You may think a workflow is simple, but trace shows many hidden steps.

Keeper:

Traces correct architecture fantasies.

Triton is valuable because it matches transformer workload shape.

The Q&A says Triton was built by people training transformers, so transformer-like tasks are relatively easy.

OMNI lesson:

Tools have inductive bias.

Choose tools whose abstraction matches your workload.

Keeper:

Tool choice encodes assumptions.

Alternatives exist at different abstraction levels.

CUDA, PTX, Triton, ThunderKittens, CuTe, DSLs.

Each gives different control and difficulty.

OMNI equivalent:

Use different layers depending on the problem:

prompt,
tool call,
workflow engine,
domain service,
schema,
database trigger,
human SOP,
code.

Keeper:

Choose the lowest layer only when necessary.

Correctness is easier than performance.

A naive matmul kernel can be correct but terrible.

OMNI translation:

A workflow can produce the right answer once, but fail at scale because of cost, latency, review burden, or brittleness.

Keeper:

Correct once is not production-ready.

Row-fits-block assumption is a hidden constraint.

Softmax is simple when row fits in block.

When it does not, the algorithm changes.

OMNI context-packet equivalent:

Many workflows are easy until the context no longer fits.

Keeper:

Document fit assumptions.

Masks handle edge cases at the boundary.

When num elements does not divide blocksize, masks prevent reading invalid memory.

OMNI needs equivalent boundary handling:

missing data,
partial identity,
incomplete patient history,
stale source,
revoked consent,
unknown role,
partial upload.

Keeper:

Boundary conditions need explicit masks.

The lecture validates context packets as execution units.

A Triton block loads the right data, works locally, writes back.

An OMNI context packet should load the right source set, support local reasoning/action, and write a structured result.

Keeper:

Context packet is OMNI’s block abstraction.

The lecture validates work packages as Build OS units.

A work package should be neither a whole ocean nor a single grain.

It should be a bounded tile with:

scope,
inputs,
allowed outputs,
constraints,
tests,
proof.

Keeper:

Build OS work packages are execution tiles.

The lecture reinforces “source custody beats derivative chaos.”

The kernel reads from HBM, computes, writes output.

It does not pretend intermediate state is source truth.

OMNI should preserve source and compute derivatives with lineage.

Keeper:

Intermediate state is not canonical truth.

The lecture reinforces “inspectability beats magic.”

A black-box “this is faster” is not enough.

Profiler, PTX, kernel names, tile shapes, and benchmarks explain why.

OMNI should require:

trace,
source list,
decision rationale,
action log,
eval result,
test proof.

Keeper:

No magic in production systems.

The lecture reinforces “abstractions should compile.”

A high-level abstraction is acceptable if it compiles into efficient execution.

OMNI equivalent:

A doctrine sentence is useful only if it compiles into:

schema,
action,
policy,
test,
runbook,
UI,
trace.

Keeper:

Doctrine must compile into operations.

The lecture reinforces “compiler-like agents need ground truth.”

Torch.compile can optimize because it has a graph.

OMNI agents can optimize workflows only if the system has explicit graph/state.

Keeper:

Agents need graphs, not fog.

The lecture reinforces “performance claims need measurement.”

This is the key Build OS doctrine.

Any claim that a patch, workflow, context strategy, prompt, or agent is better needs:

baseline,
measurement,
case set,
trace,
failure analysis,
before/after.

Keeper:

No performance claim without measurement.

This source is mostly execution discipline.

It should not become healthcare canon.

Its durable value is:

measure,
profile,
inspect,
decompose,
tile,
fuse safely,
mask boundaries,
carry compact state,
and choose the right abstraction layer.

Keeper:

Execution discipline is architecture discipline.

Likely OMNI landing zones

Build OS:

benchmark/profile loop
agent trace inspection
work packages as tiles
proof reports
before/after measurement
hidden-step detection
resource accounting
compile doctrine into tests/actions

AI substrate:

abstraction layers
model/runtime profiling
compiler/toolchain awareness
kernel-level vs graph-level vs prompt-level work
task decomposition
resource-aware inference

Context packet strategy:

context packets as blocks
tiled context processing
compact accumulators
scope-specific aggregation
masks for visibility/permission/boundaries
local context fully used before release

D7:

source vs derivative
recomputable intermediate state
original artifact custody
transformation lineage
source addressing

CNS:

scoped aggregation
action masks
runtime traces
workflow profiling
fuse vs separate decisions
branch cost awareness

Architecture Memory Control Plane:

corpus chunks as tiles
gem-pull as tiled extraction
compact state across passes
source addressing
doc fit assumptions
inspect compiled behavior of doctrine

Agent Work Blueprint:

standard agent job scaffold
load authorized context
apply masks
execute
write output
emit proof
inspect trace
benchmark patch

Governance / Security:

action masks
authority boundaries
do-not-fuse commitments
trace metadata
boundary-condition handling
no black-box production magic

Doctrine / primitive pressure

Potential concepts:

execution_inspection
programming_model_vs_hardware_reality
production_substrate_literacy
hidden_execution_units
branching_execution_cost
occupancy_interpretation
task_coarsening
shared_resource_serialization
data_layout_access_pattern
tail_work_throughput_cost
benchmark_before_kernel
outcome_metric_needs_trace
cold_start_vs_steady_state
synchronization_for_measurement
p95_latency
profile_execution_trace
action_expansion_trace
implementation_metadata_trace
equivalent_output_different_cost
naive_modularity_handoff_waste
first_class_common_kernel
safe_compiler_fusion
block_level_abstraction
middle_layer_abstraction
standard_execution_scaffold
mask_as_security_boundary
precise_source_addressing
compiled_behavior_inspection
one_layer_deeper_architect
automatic_optimization_inspectability
aggregation_task_detection
scoped_aggregation
context_fit_assumption
tiled_context_accumulation
compact_state_accumulator
local_pass_global_synthesis
bounded_source_reuse
ideal_context_too_big
context_packet_local_ideal_world
loaded_context_full_use
fuse_computation_not_commitment
logical_to_physical_address_mapping
storage_layout_product_behavior
optimization_loop
trace_corrects_fantasy
tool_inductive_bias
lowest_layer_when_necessary
correct_once_not_production_ready
boundary_mask
context_packet_block_abstraction
work_package_execution_tile
intermediate_not_truth
inspectability_over_magic
doctrine_compile_requirement
explicit_graph_for_agents
measurement_required

Keeper doctrine candidates

Architecture needs execution inspection.

Clean model does not guarantee clean execution.

Production requires substrate literacy.

Hidden execution units shape real performance.

Branching has execution cost.

Branch for risk, not randomness.

Utilization metrics need interpretation.

Make work units neither too tiny nor too huge.

Shared resources can serialize parallel work.

Data layout should match access patterns.

Tail work can dominate throughput.

Measure before optimizing.

Outcome metrics need trace diagnostics.

Separate cold-start from steady-state.

Measure after the work is actually complete.

P95 matters.

Profile the execution trace.

Know what an action expands into.

Implementation metadata belongs in traces.

Equivalent output can have different execution cost.

Naive modularity can create memory/hand-off waste.

Repeated patterns deserve first-class kernels.

Compiler-like optimization is useful only inside safe boundaries.

Block-level abstraction can be the right middle layer.

The right abstraction lives between macro workflow and micro operation.

Standard execution scaffold improves reliability.

Masks are security and correctness primitives.

Precise addressing prevents context drift.

Inspect the compiled behavior.

Architects need one layer deeper than normal users.

Know when the task requires aggregation.

Aggregate within the right scope.

Keep scoped work small enough to fit the working context.

Large context requires tiled accumulation.

Carry compact state across context tiles.

Global synthesis needs local passes plus state.

Reuse source chunks inside bounded work.

Ideal context is usually too big.

Context packets are local ideal worlds.

Use loaded context fully before releasing it.

Fuse computation, not commitments.

Logical structure needs address mapping.

Storage layout is product behavior.

Optimization is a loop.

Traces correct architecture fantasies.

Tool choice encodes assumptions.

Choose the lowest layer only when necessary.

Correct once is not production-ready.

Document fit assumptions.

Boundary conditions need explicit masks.

Context packet is OMNI’s block abstraction.

Build OS work packages are execution tiles.

Intermediate state is not canonical truth.

No magic in production systems.

Doctrine must compile into operations.

Agents need graphs, not fog.

No performance claim without measurement.

What not to import blindly

Do not turn Triton syntax into OMNI doctrine.

Do not overuse GPU metaphors as proof.

Do not assume every workflow should be optimized like a kernel.

Do not fuse across clinical, legal, financial, identity, or external-send authority boundaries.

Do not treat profiler-style metrics as the only truth; human and safety outcomes still matter.

Do not optimize throughput if latency or safety is the real bottleneck.

Do not treat successful one-off execution as production readiness.

Do not assume the high-level workflow is what actually runs.

Do not hide automatic optimization decisions from traces.

Do not let agents operate from vague source references.

Do not process oversized context without a tiling/accumulator strategy.

Do not trust a context packet until you know its fit assumptions.

Do not let intermediate derived state become canonical truth.

Do not use the lowest abstraction layer unless the higher layer cannot satisfy correctness/performance.

Do-not-miss lesson

This source gives OMNI an execution law:

High-level architecture is only trustworthy when you can inspect what it compiles into.

For OMNI, every doctrine, workflow, agent job, context packet, and action schema should be able to compile into traceable execution with explicit inputs, masks, source addresses, work units, outputs, proof, and performance measurements.

Shortest OMNI version

OMNI should build like a profiled systems stack: define clean abstractions, but inspect execution traces; benchmark before optimizing; use bounded work packages; tile large context; carry compact state; fuse low-risk adjacent work; preserve authority boundaries; and require proof before claiming the new workflow is better.

My hard read

Targeted semantic. High yield for Build OS and agent execution discipline.

This is not healthcare doctrine.

It belongs with the GPU/TPU lecture, but one layer lower.

Lecture 5 says memory movement is the bottleneck.

Lecture 6 says: now inspect the code, profile the kernels, choose the right abstraction, and prove the execution actually does what you think.

The strongest OMNI line:

OMNI cannot stop at beautiful abstractions. Every abstraction must compile into observable execution: what context loaded, what scope mask applied, what source addresses were used, what work tile ran, what intermediate state was created, what authority boundary was preserved, what output was written, and what measurement proves it improved the system.






&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 002 — Nick operator note  ·  layer: `human_context_nonbinding`
- reviewer: `Nick` · type: `human founder/operator` · at: `TK`

⬇️⬇️⬇️  YOUR GUT NOTE BELOW  (optional)  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 003 — Opus / agent formal deep extraction  ·  layer: `analysis_nonbinding`  ·  (agent-authored, NOT a Nick paste)
- reviewer: `Opus` (or agent) · type: `AI assistant` · at: `TK` · purpose: `formalize Review 001 → structured per-source extraction → feed EVRUN concept registry` · binds nothing (`GRD-036`/`GRD-044`)

> The agent reads §1 verbatim + §3 Review 001 (Knox) IN FULL, then writes the formal per-source extraction HERE (not a sidecar). Formalize Review 001's strategic read — verify/sharpen it, do not re-derive. Per concept cluster: **concept · OMNI meaning · why · downstream homes (thesis §x / CNS / Build-OS / §C / capability-topology / Knowledge-Reservoirs / security / domain-contracts / UX-surfaces / product / future-watch) · source anchors (verbatim ≤12 words + timestamp) · stale-vs-v3 (ABSENT/PARTIAL/AFFIRM) · weight_tier (spine/vocabulary/low-authority-watch/no-op) · status (promote/watch/reject).** Then: **net-new primitives** (dedup vs registry — don't re-mint) + **reread flags**. Cross-source convergence is folded into the EVRUN **concept registry**, not duplicated here.

⬇️⬇️⬇️  AGENT WRITES THE FORMAL EXTRACTION BELOW  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END Review 003  ⬆️⬆️⬆️

## §4 — Analysis pointers  *(agent fills at CLOSEOUT once analyzed — the "linked" step; leave `TK` until then)*
- EVRUN(s): `TK` (e.g. `EVRUN-2026-000001` ai-corpus) · concept_registry: `TK` (`{EVRUN}_{slug}_concept_registry_and_routing_map.md`) · source_anchor_ledger: `TK` (`…_source_anchor_ledger_receipts_only.md`) · per-source deep-read: §3 Review 003 (this file) · impact: `TK` (§A/§B/§C/CNS/Build-OS/security/domain-contracts/surfaces) · promotion: `TK` (watch | promote | reject | no-op)

## §5 — Change log
- `2026-06-11` — source file created.

> Authority/retrieval labels are defined once in `../../00_evidence_router.md` (operating principle #9 + §8). Sections carry their `layer:` inline; the legend is not restated per file.
