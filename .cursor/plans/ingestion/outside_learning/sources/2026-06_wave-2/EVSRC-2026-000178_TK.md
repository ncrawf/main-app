# EVSRC-2026-000178 — Stanford CS336 L17: Alignment — Multimodality (CLIP → VLMs → Chameleon)

Document type: `source_capture` · Lane / reservoir: `outside_learning`
**Status:** `raw_dropped` → `normalized` → `reviewed` → `analyzed` → `promoted | deferred`  ·  **current: `analyzed`**
Authority: `evidence_nonbinding` — captured source + interpretations only; binds nothing until routed + promoted.

> **HOW TO USE THIS FILE — two kinds of marks:**
> • `TK` = a field the normalizer (Opus) fills from the screenshot — **leave it alone.**
> • **⬇️ PASTE blocks = the ONLY spots Nick touches.** Exactly **3**: transcript (§1), **Knox read (§3 Review 001)**, gut note (§3 Review 002, optional). Plus: **drop the screenshot in chat** → Opus fills §0.
> Copy this into `sources/<YYYY-MM>/`, rename `EVSRC-2026-000178_TK.md` (id = highest EVSRC + 1), add a row to `../../00_index.md`.
>
> **Per-source flow (the standard):** Nick pastes transcript (§1) + Knox's strategic read (§3 **Review 001** — paste it AS-IS, however rich Knox makes it; do NOT reformat into fields) + optional gut note (§3 Review 002). Then the agent (Opus) writes **§3 Review 003 — formal deep extraction** (formalizing Review 001, NOT rediscovering it), updates the run's **concept registry** (cross-source synthesis), the coverage matrix, and fills **§4 pointers** at closeout. **The per-source deep read lives HERE in §3 — never in a separate sidecar/extraction file** (`GRD-044`). Cross-source synthesis lives in the EVRUN concept registry.

## §0 — Source identity / metadata  *(normalizer fills from the screenshot — leave `TK`)*
- evsrc_id: `EVSRC-2026-000178`  ·  filename: `EVSRC-2026-000178_TK.md`
- source_platform: `YouTube`  ·  source_url: `https://youtube.com/watch?v=26FtDO8ZpOU`  ·  source_title: `Stanford CS336 Language Modeling from Scratch | Spring 2026 | Lecture 17: Alignment - Multimodality`
- channel_or_org: `Stanford Online`  ·  speaker: `Percy Liang (likely; CS336 co-instructors Percy Liang + Tatsunori Hashimoto)`  ·  published_at: `2026-06-04`
- captured_at: `2026-06-11`  ·  captured_by: `Nick`  ·  capture_method: `transcript paste + screenshot`
- content_type: `technical AI lecture / multimodal models / VLMs / CLIP / SigLIP / LLaVA / Qwen-VL / Chameleon / image-tokenization / multimodal alignment`  ·  source_reliability_context: `academic`  ·  topic_tags_light: `[multimodality, omni_model, VLM, CLIP, SigLIP, ViT, patch_tokenization, AnyRes, dynamic_resolution, OCR_fidelity, multimodal_RoPE, explicit_video_timestamps, modality_token_budget, LLaVA, Qwen_VL, Chameleon, VQ_VAE, continuous_vs_discrete_encoder, understanding_vs_generation, long_context_multimodal, visual_localization, layout, artifact_representation_pluralism]`

## §0.1 — People / authorship / authority context  *(normalizer fills from screenshot + description + transcript — this is what makes a source a reservoir, not a transcript dump)*
- primary speaker(s):
  - name: `Percy Liang` · role_in_source: `lecturer (inferred from transcript framing + course sequence)` · affiliation_at_publication: `Stanford University (Professor of Computer Science)` · speaker_type: `researcher (foundation models / multimodal systems / evaluation)` · authority_context: `very high for multimodal-model architecture, VLM training methodology, modality-token framing, vision-encoder→LLM attachment` · identity_confidence: `inferred` (auto-transcript; video description lists Percy Liang + Tatsunori Hashimoto)
  - name: `Tatsunori Hashimoto` · role_in_source: `course co-instructor (listed in description; not confirmed as this lecture's speaker)` · affiliation_at_publication: `Stanford University (Assistant Professor of Computer Science)` · speaker_type: `researcher (data-centric ML / LM training + systems)` · authority_context: `high for the broader CS336 training/data/post-training/systems frame` · identity_confidence: `inferred`
- publisher / channel: `Stanford Online`  ·  interviewer / moderator / host: `n/a (lecture)`
- event_context: `Stanford CS336 — Language Modeling from Scratch, Spring 2026, Lecture 17 (Alignment / Multimodality)`  ·  perspective / conflict notes: `Technical multimodality overview; durable OMNI value = architectural principles (modality tokenization, semantic-vs-fidelity representations, dynamic resolution, OCR/document fidelity, video token budgeting, modality weighting, continuous-encoder vs discrete-token tradeoffs, multimodal data design). NOT healthcare-specific. Do NOT import benchmark rankings / specific model claims without verification (GRD-039).`

> **Authority is descriptive, not worship** (`GRD-039`): record *who* said it (raises relevance), but every claim still routes through evidence → interpretation → gated promotion. (Clinical reservoir extends this block heavily — authors / affiliations / journal / DOI-PMID / study-type / evidence-level / COI / retraction — `FWREG-006`.)

## §0.5 — Processing checklist
**Nick drops:** [ ] transcript → §1 · [ ] screenshot in chat · [ ] **Knox strategic read → §3 Review 001** (paste as-is) · [ ] (optional) gut note → §3 Review 002
**Agent (Opus) does:** [x] id+filename · [x] §0 metadata from screenshot · [x] takes labeled · [x] **§3 Review 003 formal deep extraction** (formalize Review 001) · [ ] update EVRUN concept registry (cross-source — folded by Opus-main via registry-fold packet) · [ ] update coverage matrix (Opus-main) · [x] **fill §4 pointers (closeout)** · [x] NO sidecar extraction file (`GRD-044`)

## §1 — Verbatim transcript  ·  layer: `raw_source`  ·  IMMUTABLE

⬇️⬇️⬇️  PASTE THE FULL TRANSCRIPT BELOW  (do not summarize)  ⬇️⬇️⬇️

Transcript


Search in video
0:00
So originally, the plan was to talk more
0:08
about reinforcement learning. But I thought since I only had one lecture to give, I think this class would be a little bit
0:14
incomplete without saying something about multimodality, which is so pervasive if you look at all the major models.
0:21
So this will be an overview of multimodal models.
0:27
Of course, this could be a whole class in itself, and I'll try to do what I can in one lecture.
0:32
OK, so let's dive into multimodal models. So far in this class, we have exclusively
0:38
focused on language models. And language models are already pretty general.
0:43
They give you the ability to go from any piece of text to any text. So this could be any language, code, poetry, anything you like,
0:53
even, let's say, DNA, for example. But the world is multi-modal.
1:00
You have text, you have images, audio, video. And if you think about a North Star of where we want to be
1:09
is what people call omni model, which is ability to take any combination of these modalities
1:17
and output any combination of any of these modalities. So, for example, I can give you an image and a video
1:24
and I can ask a question about the image and the video.
1:29
I can generate images. I can convert the audio into an image. You can imagine all sorts of different applications
1:37
by having an omni model. So in this class, I'm not going to build up to a whole omni model.
1:42
But I'll give you some of the pieces that at least to allow you to ingest a little bit of images, which we'll mostly
1:52
focus on today. So how do you build such a thing?
1:57
Well, this is a weird way to motivate this, but the reality is that transformers work really well.
2:06
And despite the best efforts of people to try other things, those are across all of these modalities still at scale
2:14
the best thing we have. So we have to figure out how to use them. And now, transformers were designed for text.
2:21
So they have this property that they speak tokens. So they take as input a bunch of tokens
2:27
and then output some tokens. And here, I'm expanding the notion
2:34
of tokenization to be not just a discrete token that we've seen in text but also potentially continuous tokens.
2:40
You can think about these as embedding of tokens. And the key thing is that a token
2:46
should represent some semantic unit of information. For example, in natural language, tokens are subwords.
2:55
And these are somewhat meaningful. Whereas, for example, a pixel is certainly not
3:02
meaningful by itself. So somehow we must convert everything,
3:07
including audio and images, into either discrete or continuous
3:13
tokens. Now, notice that we've already had to do this even with text. In the first lecture, we talked about tokenization.
3:21
And there, it was not too bad. I mean, we had a BP tokenizer.
3:26
You guys implemented it. It's fine. One could wish for a better tokenizer or something that
3:37
doesn't have tokenizers at all. But it's not the worst thing in the world.
3:43
Now, for non-text modalities, we have to scratch our heads a lot more and figure out
3:49
what is the equivalent of the BP tokenizer that will take an image and produce things that a transformer can digest?
3:57
OK, so you have to answer two questions here. One is, for non-text data, for example, images or videos
4:04
or audio, how do you input these into the transformer?
4:09
And then how do you generate them out? And this lecture will mostly focus on question number one.
4:17
OK, so let's start back and rewind the clock back to 21.
4:25
So the CLIP model, which some of you might be familiar with, is a lot of the foundation of modern VLMs.
4:34
And so we can go through this a bit in more detail.
4:39
So CLIP stands for Contrastive Language Image Pre-Training. And a bit of historical context is around
4:46
that time GPT-3 had already happened.
4:52
GPT-2. All the language models had already gone into this foundation model era.
4:59
Neovision was still-- there were some efforts, but traditionally had been based on large annotated data
5:08
sets, such as ImageNet, and training models, such as the ResNet, on these data sets
5:15
and doing a bunch of other things, like data augmentation, and getting really good performance.
5:21
And so the researchers at OpenAI were wondering, is it possible to leverage the large amount of image
5:29
and textual captions out there? So inspired by this idea in language where you could just
5:35
scrape the internet, you have all this text, it's very noisy. But somehow if you train a large enough model,
5:42
you can make sense out of this and generate something useful. So what was the equivalent of that for images?
5:49
So as a result, CLIP was born. And the idea of CLIP is fairly simple and straightforward.
5:58
So the idea here is that suppose I give you a bunch of image-text pairs.
6:05
So let's say I give you 32,000 of these. OK, so what I'm going to do is for each image
6:13
I'm going to encode it using an image encoder, which I'll explain later, into a vector.
6:20
So I'm going to have 32,000 of these image encodings.
6:26
I'm going to also do the same thing with the corresponding text. So that will give me T1 through TN.
6:33
So these are the embeddings of the text. These are embedding of the images.
6:39
Now, the objective I want to set up is that I want the image-- let's take I1.
6:48
So I1 is aligned with T1. So I want this alignment, the dot product between these two
6:57
embeddings to be much larger than the dot product between I1 and all the other texts, OK.
7:04
And conversely, I also want for this text to say that this dot product is larger
7:11
than the dot product between T1 and all the other images. So that, in a nutshell, is the CLIP objective.
7:18
And you can imagine doing-- there's basically 2 times N different softmax classification problems.
7:30
So here's the code. So you get a n by d matrix for the image encodings
7:40
and n by d times for the textual embeddings. You normalize and you take this dot stop product
7:48
with some temperature. And then you compute the cross entropy, OK.
7:56
So it's basically a multi-class classification problem, where the examples are structured in this m
8:05
by n matrix form. OK, any questions about that?
8:11
All right, so some more details about how CLIP was built.
8:24
So where does the data come from? There are not too many details in the paper about the data.
8:32
Roughly, you take a bunch of queries from somewhere
8:39
and then you go search online for these and you mine a bunch of image and text pairs.
8:47
And this resulted in 400 million image-text pairs.
8:53
The data set wasn't released, but there was an effort by this paper called
9:00
OpenCLIP, which replicated and further extended the CLIP ideas.
9:09
So there was a data set called LAION-5B, which was released 5 billion images
9:14
with textual descriptions. And there was a bunch of processing that happened.
9:19
Interestingly, they actually used CLIP for the data filtering and then train OpenCLIP. So there's some bootstrapping happening.
9:26
But at least this is a model where you can point to the data set that it was trained on and the code as well.
9:34
So a few other details.
9:41
So images come in all sorts of resolutions. If you find an internet image, it
9:47
might be long and skinny or tall-- sorry.
9:52
Long and skinny or the other way. So arbitrary dimensions. And one thing that you learn about neural nets
10:01
is that they don't like things to be dynamic. They want things to be fixed size.
10:06
So there is a somewhat heuristic processing that happens is they resize the image so that the shorter side
10:16
is 336 or whatever target size. It could be 224 as well.
10:23
And then they center crop to get a square. OK, this is obviously for convenience.
10:29
Later, we'll see that you can do better than this. But this is just for expediency.
10:35
And also at this time the CLIP authors were thinking about ImageNet and classification.
10:42
So usually, the object is in the middle and you're just trimming off some background.
10:48
So it doesn't matter too much.
10:55
So what is the vision encoder? So this is the general algorithm. But I haven't specified what the image and text encoders are.
11:05
Yeah. [INAUDIBLE]
11:24
So the question I think is why they train on image-text pairs as opposed to images. [INAUDIBLE]
11:33
So there is a line of work that tries to encode images into representations
11:41
based on data augmentation. So, for example, if you take an image-- and I think
11:46
this idea called SimCLR. You augment it. You crop it. You randomly perturb it.
11:52
Rotate it. And you want to say that that is the same image. They should have similar embeddings.
11:58
And that's useful for low level details.
12:05
But the problem is that you won't data augment your way from one type of dog to another dog.
12:14
So by using the text, it gives you a higher level semantic representations of images.
12:23
And, yeah, that's a good question. And later, we'll see how these semantic representations are
12:29
useful if you look at the types of tasks that people are trying to solve.
12:36
OK, so what is the vision encoder that CLIP uses? They actually experimented with a bunch of different ones,
12:44
ResNets and vision transformers, which had just come out. They found out that vision transformers performed the best.
12:51
So when people say CLIP, they usually mean the VIT version. And so a vision transformer does the following.
13:03
So you take an image and you break it up into these patches.
13:10
I think the original paper was 16 by 16 and the CLIP used 14 by 14.
13:18
But anyway, you have these little patches, which are basically vectors.
13:24
So each patches in some sense a-- well, a token for this vision transformer.
13:31
You add the positional embeddings just like you would if you were training a language model, and then you just put it through a standard transformer.
13:42
And if you want to classify, then you would--
13:50
well, you would usually want to-- OK, sorry.
13:55
The transformer encoder produces a sequence of vectors.
14:03
And the CLIP paper does something a little bit different. You can average all these vectors.
14:08
That gives you a single vector. But they thought it was better to do attention pooling, which
14:14
means that you take the global average of all the activations, but then you also do another round of attention
14:22
with that query against the key and values of each position. And that gives you another vector,
14:29
which is maybe a little bit more informed rather than just a straight up average.
14:37
OK, so that's the VIT. And the CLIP paper found that the best model is what is called
14:45
VIT-L/14. So to decipher, this is a large VIT.
14:54
I think it has 24 layers or so. Not quite sure about that.
15:00
And 14 by 14 patches. Each patch is RGB, so it has three channels.
15:07
And they trained on 336 by 336 resolution images.
15:13
Actually, they trained on a lower res
15:19
I guess for speed because high resolution images take longer. And then for the latter part of training,
15:25
they train on higher res. Yeah. For the position embedding, would it
15:31
be more complicated than the task? For example, task only has [INAUDIBLE].
15:36
Yeah, so the question is, can you
15:44
be smarter about the positional embeddings because this is just linear 0 through 9?
15:51
I think in this paper, they actually-- in the CLIP paper, I believe they tried some 2D version of this.
15:57
And the one DNA found that it doesn't really matter that much. I think it's also--
16:05
I always have to take these results with the idea that they're interested in classification in mind.
16:11
So for classification, maybe it doesn't matter. Later, we'll see a fancier positional embeddings
16:16
that do take into account the spatial structure.
16:24
OK, so the text encoder is a standard transformer.
16:30
It's a GPT-2 style transformer since that was from the same group that developed GPT-2.
16:37
And in order to get a single vector out of a sequence,
16:42
they prepended BOS and appended EOS. And then they took EOS, the activation at the highest layer
16:51
as the representation of that whole sequence.
16:57
OK, so you have the vision encoder. You have the text encoder. And then you can do this process to train.
17:06
You pick a batch. You encode all the text. You decode all the images. And then you form these two end cross-entropy losses.
17:14
And then you go from there. OK, so the headline result, which
17:21
got a lot of people excited about CLIP back in 2021, is that on the ImageNet benchmark,
17:28
0 shot CLIP outperform a ResNet that was trained on 1.2 million ImageNet images.
17:36
So if you think about this, 1.2 ImageNet images, this was many, many hours of Amazon Mechanical Turk worker
17:44
time to do this annotation. And now you have CLIP that was trained on more organic web
17:52
data. Of course, this was the labor of a bunch of people on the web. But if you're able to leverage that or existing data already,
18:01
then you can just zero shot it. And the zero shot technique is basically
18:07
to explain I don't have slide support here. But you take an image and then you have a bunch of text.
18:14
And then you basically take the dot product with the various labels and see which one's the highest.
18:19
Oh, yeah. It is. Sorry.
18:25
It is on this part of the slide. OK, good point. So this is a zero shot prediction,
18:32
where you take an image and then you-- yeah, what I said.
18:40
Cool. Any questions? Yeah. [INAUDIBLE]
19:00
Yeah, so the question is if you have a dog and there's maybe other captions that also have dogs, would that confuse the model?
19:08
In general, this process is going to be noisy. It's OK if there's another dog because, on average, it's
19:18
unlikely that there's always going to be a dog. Sometimes it's going to be an apple or a cat or something.
19:23
But also another point is that these images are basically
19:29
scraped from images on the web with textual text next to them
19:34
or in the alt image. And so they're extremely noisy. There's been studies that look at when
19:41
you have a caption of an image, it doesn't necessarily just verbatim say what's in the image.
19:48
Because if you have an image and it's a dog, you don't need to say a dog. So this process is very noisy.
19:55
So it's maybe somewhat surprising but also interesting
20:02
that it works. I mean, there was a lot of data filtering that is needed. So if you take arbitrary images on and text on that web,
20:09
it's probably going to be too way too noisy. One other thing to note, which relates
20:15
to a point I'll come back to at the very end, is they also try this alternative where
20:21
instead of doing this ranking, they can predict a text from the images. So they can set an objective where
20:28
they try to take the images and predict the text, either as a bag of words or as a language model.
20:38
And they show that maybe, I don't know, somewhat surprisingly, that if you use a stronger model,
20:47
it actually does worse or at least is less efficient compared to using bag of words, CLIP.
20:55
So this is saying that-- I think saying that for actually the representations
21:05
that you're trying to learn-- or, again, this is ImageNet accuracy. So it's classification.
21:10
Actually modeling the exact token sequences of the caption isn't so important for getting the rough representation
21:20
of the image. OK, we'll come back to this point at the end.
21:29
So what did we learned so far? So we've encoded images using CLIP.
21:35
And it captures the semantics of the image because it's paired with text.
21:40
And generally, text talks about the semantics. It is important to note that the design decisions here are
21:48
based on image classification. So it's not very fine-grained.
21:53
But yet, it's still, as we'll see, serves as a robust starting point for everything
21:58
we're going to do later. One technical downside of CLIP is that it does require large
22:07
batch sizes, like 30,000. If you have a batch size of 1, clearly, it doesn't work, or even 10, it doesn't work.
22:14
And furthermore, the softmax operation operates over the full batch.
22:20
So it's not really very decomposable. Whereas if you think about normal language model training, a batch of examples, all the sequences parallelize
22:30
and you just do aggregation at the end.
22:37
So this point will be addressed by another paper from Google
22:42
that came out called SigLIP. And so think about this as basically an improved version
22:49
of CLIP. So it stands for Sigmoid Loss for Language Image Pre-training.
22:56
And here, the main difference is that CLIP does multi-class classification.
23:02
It says, I have this aligned text and image. And I'm going to say this is positive against all
23:09
the other alternative images or against all the alternative text. SigLIP, in some sense, is a lot simpler.
23:16
It basically says for any given image text pair, are they aligned or not?
23:23
So if I reference this diagram up here, it's basically binary classification
23:28
where the diagonals are positive examples and the diagonal are negative examples. That's it.
23:36
So here's the algorithm. You take the embeddings. You normalize.
23:41
You take the dot product as usual. But now you're forming the labels which are minus 1
23:50
on the off-diagonal, and diagonals you have one. And then you do log_sigmoid.
24:00
So the objective is very simple. Yeah. [INAUDIBLE]
24:17
Yeah, so the question is, does this require any sophisticated sampling strategies?
24:23
At least in the initial paper, they were just operating on literally the same type of matrix.
24:31
You could imagine I think you're probably thinking in general for these contrastive methods.
24:37
The negative examples need to be-- sometimes you need to balance the positives and the negatives, or the negatives need to be like tight negatives
24:45
so that you're not biased. But at least in the initial version, this was fairly straightforward.
24:54
So to speak about the data, this was done at Google.
25:01
This was the data set. They used another paper, which was another language image model around 2022, which got a WebLI data set with order
25:11
billion image text pairs. They also did some extra work. For example, images that have text in them.
25:18
They did OCR. And that's another way to form text image pairs and did some filtering.
25:24
This was multilingual. The main point of this paper was that it was much more
25:33
efficient to train than CLIP. So CLIP was trained 10 days on 256 TPUv3,
25:42
and CLIP was five days on 32v4s. And you might think, oh, v4s actually
25:48
should be faster than v3. In terms of flops per second, they're actually not. They're better because you can put more of them in a pod
25:56
and the interconnect is better. But at this scale, it's really actually not faster.
26:04
Actually, 60% slower or something.
26:09
So the way that this is faster-- and, of course,
26:15
I think CLIP probably did not try to optimize the code for the maximum throughput.
26:21
It was just let's get this thing to work. But the SigLIP paper showed that you can parallelize this
26:30
as follows. So you have-- basically, if you recall your systems lecture,
26:36
so this is you can think about it as DDP if you like, where each device stores a subset of the image text pairs.
26:51
However, there's interactions between the examples, unlike in language model training, where everything just
26:58
factors. So in the first pass, each device just computes all the losses on whatever image text
27:09
pairs it has locally.
27:15
And then you send the text. So basically, device 1 gets T5 through T8.
27:24
And now it's able to compute the negatives here. And then it gets T9 through T12.
27:30
So you rotate around until you cover all the off-diagonal block
27:35
entries. Another thing that was nice about SigLIP
27:42
is that you effectively decoupling batch size from the loss.
27:48
Whereas in CLIP, the loss is tied to the batch size. If you change the batch size, it's a different loss function.
27:54
So they were able to experiment with much smaller batch sizes less than 16K.
28:00
And in that regime, it's much better than CLIP because the loss function, if you have too many too small a batch size, it just degrades.
28:10
Whereas for smaller batch size for CLIP, you have more variance
28:16
but it's the same in expectation. It's the same loss. They also say, well, you can go up to much larger batch sizes.
28:24
But that doesn't really help. And we saw that critical batch sizes at some limit.
28:30
And 32K was essentially their critical batch size.
28:37
OK, so now we have CLIP and SigLIP. These are image encoders that take an image, in this version,
28:44
just a fixed size image, 336 by 336, let's say, and maps it into a vector, OK, which contains some semantics.
28:56
So now let's start building VLMs, Vision Language Models.
29:03
And I'm going to talk about two families of models, LLaVA and Qwen.
29:09
The two are very similar in the broad template. There are some details that are different,
29:15
which we'll go through, and the basic idea is that we're going to take these embeddings
29:20
and then just inject them into our language model. So this is going to be more of a flavor of mid-training
29:26
or post-training, where we take an existing image encoder. We take an existing LLM, and then we're
29:33
going to stitch it together rather than training something from scratch.
29:40
So LLaVA paper came out in 2023.
29:46
And this got people excited because around this time, the closed models, like GPT, I think it was GPT-4,
29:55
was able to do visual reasoning. And they were able to show that they could do
30:02
some visual reasoning as well. It wasn't as good as GPT-4, of course,
30:07
but I think it was an open model and people got to see what went under the hood. So for VLMs, there's essentially a few pieces here.
30:19
There's the vision encoder. They use CLIP here. For the text decoder, they use a language model called Vicuna,
30:26
which is the first Llama model that was fine-tuned on some shared GPT conversations.
30:33
So these are conversations that people have with ChatGPT and share them on this website.
30:38
I don't think this is up anymore. So the data that they used was based on MS COCO.
30:50
So this was an annotated data set where they had Mechanical Turk workers go and annotate
30:55
a bunch of images with bounding boxes and captions describing what was in the image.
31:01
So they basically synthesized a data set for their model.
31:07
So they prompted GPT-4 with the captions or the detected objects and GPT-4 to generate questions or conversations.
31:15
For example, this is an image in the MS COCO data set. This was an annotated caption done by a human.
31:24
And also here is also annotated by a human.
31:31
And then you ask GPT-4, generate me a conversation
31:37
and you might get a question and an answer. You might ask GPT-4 generate me a detailed description.
31:44
It generates something, I guess, similar to the caption. And then you can ask GPT-4 to generate complex reasoning.
31:50
And it does something like that. OK, so those are synthesized data, 158,000 examples.
32:03
And then they trained a model on this. So the model-- so they use CLIP, in particular,
32:12
the highest performing CLIP version, VITL/14.
32:18
So 14 by 14 patches. And then they-- let's just look at this image.
32:25
So they take images. They send it through this vision encoder CLIP. That gives you a vector.
32:32
But this vector isn't really in the same space, so to speak, as the text.
32:38
So then they multiply by a matrix W to get another vector, which is in the same space as the text
32:47
embeddings. And for text, you basically just use your standard embeddings to get vectors.
32:54
So what's basically happening is that the text gets encoded into these vectors and the image also gets
33:01
encoded into these vectors. And then this whole sequence of vectors
33:06
just goes through a standard transformer and produces output.
33:11
OK, so we're in some sense converting these images into textual tokens.
33:18
So I can leverage the pre-trained language model.
33:24
So to train this model, they do it in two stages.
33:30
The first stage is that they freeze the vision encoder and the language model and only train this matrix W.
33:39
And this is what they call the alignment phase, which is essentially making sure that the image gets
33:46
mapped into the same space. Because if you just use a random W,
33:54
these images are not going to be-- I mean, certainly, they're not embeddings,
34:00
representing any natural language token. So the goal of training W is that they
34:06
look like natural language token embeddings. And the second stage is that they now
34:12
still freeze a vision encoder and then train W and the language model. So they're fine-tuning the language model on their data.
34:22
So remember, their data is basically here's an image and here is a conversation or a description
34:28
or a complex reasoning example. So this is all image plus text to text.
34:37
OK, so this is one example from their paper.
34:43
It's interesting. So you have an input image and a conversation.
34:48
The user says what's unusual about this image. And their model is able to tell you that you don't usually
34:54
iron on the back of a minivan. And they also make a point that their model is pretty good
35:00
because even if your user prompt is not really
35:06
prompting the unusualness, it still talks about it, whereas--
35:12
GPT-4, obviously, is able to do this, but other models at the time were not able to.
35:20
OK, any questions about LLaVA? OK, so that was 2023.
35:29
And I'm going to skip forward to LLaVA OneVision, which is 2024.
35:35
There is a sequence of papers after the LLaVA paper came out of LLaVA 1.5, LLaVA-Next.
35:44
But I'll capture all of these innovations, I think, in what I'll describe next.
35:51
The main thing that they did is it's essentially the same recipe, but they are trying
35:56
to be more ambitious in terms of the types of multimodal applications they could do.
36:02
So they were able to handle multiple images and videos. So here's the same diagram.
36:10
Now they can take a single image or multiple images
36:15
or a video, which is essentially a sequence of images corresponding to a sampling of the frames.
36:23
And here are the vision encoder. They upgrade it to SigLIP. The text decoder, they use Qwen-2 now,
36:31
which was probably the best language model that was out at that time.
36:38
And then for the projector or the adapter, which is the thing that takes the output of the vision encoder
36:46
and turns it into the input of the language model is a two-layer MLP instead of a linear projection.
36:55
OK, so it's like you have a system and you're just upgrading the parts.
37:01
But it's the same rough system. So let's talk a little bit about data processing.
37:10
So one thing that they wanted to do is to do OCR. And the thing with OCR is that you
37:16
need to preserve very fine-grained information. Otherwise, a J looks like an I, and that's not good.
37:24
So remember that CLIP resizes and crops to 336 by 336.
37:30
And clearly, if you have a document and you crop to 336 by 336, you can't read it.
37:36
So their solution is this idea called AnyRes, which was actually introduced in the LLaVA 1.5
37:45
paper. And the idea is basically, I mean, it's fairly straightforward.
37:51
You break up an image into multiple pieces.
37:56
And the idea is that each piece is the resolution of whatever the vision encoder is going to take, 336 by 336, for example.
38:05
So you're going to code all the pieces into a bunch of vectors. And then you concatenate the vectors.
38:12
OK, so you're basically noticing that your vision encoder can't handle high resolution.
38:18
So rather than downsampling, you're just going to crop and look at different parts of image.
38:25
And now if you have really super high resolution images or videos then, you get too many tokens.
38:31
So then they downsample. But the idea behind AnyRes is that it's adaptive.
38:38
And this is nice because the transformer is already adaptive. Language can be-- your sentences can be any length
38:46
and the transformer already handles that pretty well. So it turns out images can be any resolution.
38:51
So that's picking back on the same dynamic ability.
38:58
So here is what they actually do for images here. So let's say you have this image of a paper.
39:07
They do downsample and have one path, which is basically a downsampled encoding of the entire paper.
39:15
But then they break it up into these chunks, where each chunk is the resolution that the vision encoder expects, and then encodes each of these
39:23
separately. And then you concatenate all these patches. And if you have too many, then you
39:28
reduce that the number of patches by interpolation.
39:36
OK, so then they're able to handle images, multiple images,
39:42
and video. So you think about, well, all of these are technically all reducible to images,
39:49
but they put their thumb on the scale a little bit because they want to make sure all the modalities are roughly
39:59
comparable. Because videos can be very long, they don't want their data set to be dominated by a bunch of repetitive frames.
40:07
So here's what they do here. So for a single image, they basically have the full image downsampled and then
40:14
a bunch of crops up to N crops there, up to 9.
40:20
And if you are presented with multiple images, then they just use the base resolution.
40:28
So if you have a single image, I get to look at it more carefully. If I have multiple images, I'm just going to look at it from afar.
40:37
And for video, we're going to use even lower resolution
40:43
or fewer tokens to represent each frame because the idea is that, well, videos can be-- they only use up to 32 frames,
40:54
but you start running into context length problems for viewing.
41:01
And later, we'll see how a big part of being able to handle multimodal
41:06
is to deal with longer context. OK, so the data here that they use is, I would say,
41:22
the state of philosophy is they want to curate high quality data and quantity. Another way to interpret this is that it's very targeted data.
41:31
If you look at many of these, it's very task-based whether you're doing visual question answering or answering
41:40
questions about tables. So this is definitely post-training territory,
41:45
where you're trying to get your model to do these tasks, and therefore, you create a bunch of these tasks.
41:52
And this work is also unabashedly, basically,
41:57
distilling GPT-4 models so that they can get the best performance, which is, I guess, not ideal.
42:07
But this is what you do if you don't have an annotation budget.
42:14
And so they have single images, multiple images, and videos.
42:20
So there's a lot of different pieces. Some of these are very specific.
42:26
You give two images and you're trying to spot the difference. But one thing that--
42:33
OK, I'll come back to this at this point later. So for training, it's roughly the same idea as before,
42:42
where the first stage is that you're focused on alignment.
42:48
So in particular, you only train the projector,
42:54
which is the adapter. And then in the second stage and the third stages
43:02
you train the full model. And before, we had two stages. Now, we have three stages.
43:09
I'm not sure there's any particular principled reason for this, except for this second stage is
43:16
trying to put high quality data but more focused on knowledge. And the final stage is focusing on examples that look
43:24
like your downstream tasks.
43:30
OK, so one thing that they found in this paper, which was interesting, is that you get transfer between modalities.
43:41
So even though they have only single image data for diagrams and charts, but this actually
43:47
generalizes to multiple images. So at training time, you never saw an example
43:54
where you have a table and a chart and you're asking questions about it. But at test time, you have two images, one for the table
44:04
and one for the chart. And you're able to have some of conversation about it.
44:09
At training time, you only have OCR data on single image data, and you only have relational reasoning from multiple images.
44:18
But you can generalize to these cases which are useful for powering GUI agents,
44:25
where you show it a bunch of screenshots. This is multiple images and it can generate a description
44:32
of the screenshots. Another example is that they have
44:39
this idea of visual prompting, where you have this circle, which is a part of the image,
44:45
but that's meant to highlight the part of the image that you're supposed to do something about.
44:51
And this type of data only exists for single images. And now it can generalize to videos
44:59
where the user says describe the player highlighted in the video. And this player is across multiple frames.
45:06
So when you first look at this and say, oh, boy.
45:11
You're basically targeting each of these tasks. It's supervised learning. But if you have enough tasks, these models
45:19
seem to do some transfer, which is, I guess, reassuring.
45:26
OK, so again, this is a fairly standard VLM
45:33
template, vision encoder plus the projector into the language model.
45:39
There's a lot of work that goes into the data curation. They lean pretty heavily into synthesize task-specific data.
45:48
The nice thing about the LLaVA series is that it's one of the few works that open sources.
45:53
They're not just the model weights, but also the data. So you can really replicate and study this stuff.
46:01
OK, now let's talk about the Qwen Models.
46:06
So Qwen started also training in 2023 multimodal models.
46:11
So the first version was Qwen-VL. And hopefully, I'll go through this
46:17
quickly because you hopefully see the pattern. The vision encoder, they use Open-CLIP.
46:23
Remember, OpenCLIP was the open reproductions of the CLIP model. So it's basically a CLIP encoder.
46:31
And for the adapter, they did one layer of cross-attention,
46:39
incorporate 2D positional embeddings and maps to a fixed size of 256, which is a little bit--
46:47
I guess this will be changed later because this is definitely
46:54
not very dynamic, but neither is the vision encoder at this point. I guess this got cut off.
47:00
But the special tokens, they have an image tag and a box tag for bounding box,
47:06
and a ref tag for descriptions. This is what happens when your slides are in HTML.
47:14
So for training, they have three stages, just like with the LLaVA models.
47:19
In the first stage, the idea is to-- I mean, they call it pre-training but you're not
47:26
pre-training from scratch. This is generally large-scale, low quality data.
47:33
They freeze the language model and train the vision encoder and adapter.
47:41
So this is a bit different from the LLaVA models, in that, they train the vision encoder at this point.
47:51
And you can see some of the data sets that they are training on, 1.4 billion examples.
47:58
Stage 2, they move to higher quality task-specific data. And you can see some of the usual suspects of VQA data sets,
48:08
chart question answering, and so on. And then they train all the parameters here.
48:13
And finally, stage 3 is instruction tuning data.
48:19
You freeze the visual encoder and you train the adapter
48:25
and the language model. Here are some examples of the type of things that they're able to do.
48:30
So, of course, this is Qwen. So some of these examples are in Chinese.
48:37
Their interest is also making a model that is supposed to be subsuming a language model.
48:44
So a language model can do code. So we want the vision model to be good at code as well.
48:51
And you can have this example, if you can see it, is an image. And can you find Spider-Man and the Hulk.
48:58
And it can output bounding boxes as well. It doesn't actually output an image. It just outputs a bounding box.
49:07
And then you can do OCR. You can compare-- so usual stuff.
49:16
OK, so then Qwen-2 was, again, a upgrade.
49:22
There's some new ideas here, which I'll go through. So use a larger vision encoder.
49:29
They also started doing dynamic resolution. I think this was the main thing.
49:34
We remember when we went from LLaVA to LLaVA OneVision, they realized, oh, we need to handle
49:39
images of different sizes. And if you try to do video, it's clear that you need some dynamic resolution.
49:47
So the model is basically-- so let's say you have this picture
49:54
here, that might be mapped to 11,000 tokens.
50:00
This tiny picture of an equation might be only mapped to 8 tokens
50:06
and so on and so forth. So how do they do this?
50:13
Well, basically each-- it's the same idea AnyRes idea
50:18
from LLaVA, where each 224 by 224 patch is encoded with a VIT.
50:26
And remember the VIT that they started with is the OpenCLIP bit.
50:32
But this gets fine-tuned. And then trying to reduce the contact size,
50:40
they compress every 2 by 2 into 1 so that they ultimately edge every patch generates 66 tokens.
50:50
For eight videos, they sample 2 frames a second but max out at 16,000 tokens.
50:59
OK, so one thing coming back to this question about positional embeddings, so they did
51:05
do something different here with Qwen-2. So they used this idea called multimodal rotary, multimodal
51:14
RoPE. So remember, we saw RoPE before. And the idea behind RoPE is that you have embedding such
51:22
that the inner product between the vectors depends only on the distance.
51:29
So the distance is defined in 1D by just the number of tokens.
51:34
You're away from it. So now, the multidimensional version is essentially the same thing.
51:41
Except for now you have 3D. So you have height. You have width and you have time.
51:47
So each position, each patch now is a triple
51:53
defined by the coordinates. And then to compute the M-RoPE, basically, for every dimension,
52:01
you compute the RoPE and then you concatenate. So it's a fairly straightforward idea.
52:09
Although, later, with Qwen-3, we'll see how this is actually a bit suboptimal.
52:17
So they initialize the language model with-- this is the Qwen-VL model.
52:23
So, of course, they're initializing with Gwen. And then they have three stages of training,
52:30
which is very similar to what we did before. And then they were able to show a bunch
52:37
of different capabilities, some video understanding. It can do math and code, function calling,
52:46
and so on and so forth. OK, so quickly, I'll talk about Gwen-3.
52:53
So again, there's a few changes here.
52:59
I wouldn't say these are structural big changes, but they're probably changes that do impact
53:05
the quality of the model. So this is from last year Gwen3-VL report.
53:11
And so it's roughly the same diagram here.
53:16
I'll point out maybe-- I'll come back to this diagram. I'll go through some of the changes.
53:24
So one is that now they're using Qwen-3 models. They have a series of dense and MoE models.
53:29
And the Qwen-3 models are really, really good, and it really helps the quality of the final model.
53:35
One thing that they're vested in is long context understanding. So their context length can go up to 256K,
53:42
which is really important if you're trying to do long video.
53:48
Let's talk about the vision encoder. So they use SigLIP-2, which is basically
53:53
an improved version of SigLIP. It's actually identical architecture, is designed to be backward compatible.
54:00
And then they improved their RoPE. And the way to explain this is, before, you
54:10
had the three dimensions, time, width, and height. And basically, when to basically allocate
54:20
the first block of dimensions to the time, then the next block to width and the next to height.
54:28
And the problem, if you remember RoPE is that each component refers represents
54:34
a different frequency. So this would mean that maybe all the temporal dimensions
54:39
are low frequency and all the high dimensions are high frequency. So they basically interleave them when they realize that.
54:48
And they now all the axes are exposed
54:55
to both low and high frequency. Another thing they did was explicit video timestamps.
55:04
So if you look closely at this--
55:12
wait. OK, this is Qwen-3. Oops. So if you look--
55:17
this is probably a little bit too small. But before, the time stamp was implicit
55:25
in the positional encodings. Each frame in the video intrinsically
55:30
gets a different-- there's a notion of time just because it's in a different position.
55:37
But here, they made the time explicit. So these are actually tokens zero seconds, is a token that is now you have a token that
55:45
represents the time. And they found this to be helpful presumably because it's
55:51
something that you can directly refer to, like what happened after two seconds.
55:59
So that is one of the main changes.
56:05
They also did this square root normalized per token loss. So the idea is that some examples that
56:14
involve video are very long and some examples that involve a single image are very short.
56:20
And the normal thing is that every token is treated the same. And this would mean that the video examples
56:26
are going to dominate. So they basically-- the details aren't too clear.
56:33
But I believe they normalized each example by the square root of the length to basically downweight
56:41
the impact of really long examples. The final thing that is worth noting, which is interesting,
56:49
is that they did something interesting with adapter. So remember we started the adapter is the thing that connects the vision encoder to the language
56:56
model. And LLaVA was the simplest. It was just a linear projection. And then we got an MLP. And then we got some cross-attention.
57:03
And DeepStack, which is actually a paper from the DeepSeek team, but when used here is more sophisticated.
57:14
They noticed that, well, the vision encoder already computes a stack of vision embeddings.
57:23
And we're basically going to basically add these directly into the residual stream of the language model.
57:34
OK, so this is a bit more of a deep fusion of the vision
57:42
encoder into the language model as opposed to the vision encoder
57:47
is a black box that just outputs a sequence of vectors.
57:54
OK, so training is-- so they have two phases.
58:00
Pre-training has actually four stages. Post-training has three stages. So you can see that this pipelines are
58:06
getting quite complicated now. So they first train the adapter.
58:11
This is the same as all of these models. And then they have three stages where they progressively train
58:18
on longer and longer sequences, from 8K, to 32K, to 256K.
58:25
Most of the tokens are in the stages 2 and 3.
58:32
And then for post-training, they do SFT on long chain of thought data, knowledge distillation,
58:39
and then some reinforcement learning. At this point, it's really a systems paper.
58:46
There's a bunch of-- I highlighted the core new ideas,
58:51
but there's a lot of details that are different. And if you look at the final results,
58:56
this is a pretty good model. I mean, if you look at these benchmarks as compared
59:01
to the closed models Gemini, GPT-5, and Opus 4.1,
59:09
these numbers are-- the bold means that it's the best number in that row.
59:15
So the Qwen models are actually quite strong.
59:21
OK, maybe the-- yeah, a question. [INAUDIBLE]
59:54
Yeah, so the question is what about video generation and how do you know whether there should be a video or text?
1:00:02
So first of all, these models don't generate videos or images. All the multimodal stuff is on the input side.
1:00:09
You're always generating text.
1:00:16
And then you're always in most of these stages, except for RL,
1:00:21
you're supervising every token. So there's no LLM as a judge or quality on how well
1:00:31
the description is working. It's just basically whatever is in your data set. Now, with RL, you can play around with different rewards.
1:00:39
Yeah. I'll ask is the training of multimodal models
1:00:45
harder than training a pure LLM from a system perspective?
1:00:51
[INAUDIBLE]
1:01:16
Yeah, so the two questions are trained multimodal models harder
1:01:22
on the system side? And, I mean, certainly, it's not easier.
1:01:27
I think you pointed out that the data sets are larger. Video data is just-- even loading it can be a bottleneck.
1:01:36
Whereas generally, we have not really focused on data loading for talking about language models
1:01:41
because it's very, very cheap. So that needs to be taken into account.
1:01:47
And all the same principles of making sure that your data loading is happening async with actual computation needs to be taken into account.
1:01:56
And the second part was-- sorry, what was the second question? [INAUDIBLE]
1:02:02
Oh, yeah. So the videos and images can have more-- especially
1:02:07
video have more tokens. So that's one reason we saw this normalization
1:02:14
to make sure that we're not giving more weight to video. I mean, you can always down weight.
1:02:20
So if you have more tokens, if you don't like that, you can down weight. We talked in the last or two weeks ago about data mixtures.
1:02:29
So you can always weight things according to whatever makes sense.
1:02:35
But there's also a lot of text tokens out there. So I think, in general, if you look at, yeah,
1:02:44
the models are trained on tens of trillions of tokens. I think there's a-- yeah, I wouldn't
1:02:50
say that the number of multimodal tokens vastly outnumbers text tokens.
1:02:56
Yeah, oh, I think you were first. [INAUDIBLE]
1:03:23
So the question is, when you do this alignment, how does it know what the language-- is the model language
1:03:29
model pre-trained? And it definitely has to be pre-trained because otherwise it
1:03:34
doesn't make sense to align it. So the language model is frozen. And you're just training the adapter
1:03:40
to connect the given vision encoder with a given language
1:03:47
model. And the way you train it is, well, you pick a token budget, 67 billion tokens
1:03:54
and you just train. There's not an adaptive threshold here.
1:04:00
Yeah. [INAUDIBLE]
1:04:32
Yeah, so the question is, is the number of parameters in the vision coder much smaller?
1:04:39
And the answer is yes, in general. I'm trying to find-- where is the other place I--
1:04:45
not here. OK, the reason is that the vision encoder
1:04:54
is in some sense doing a very local operation. It's looking at a patch. A patch is very small.
1:05:00
And it's just trying to understand the patches. There's not much knowledge.
1:05:05
So the patch, we're not reasoning. So most of the capabilities of the model are still in the language model.
1:05:14
I'm trying to find the-- OK, where? Maybe it was up here.
1:05:21
OK, I can't-- oh, yeah. So if you look at the LLaVA model, so the full--
1:05:28
so 72 billion parameter language model.
1:05:35
Sorry. What-- OK, I don't know why this number is so-- oh, sorry.
1:05:46
This is 72 million, OK. So this is much smaller. The projector is much smaller.
1:05:51
And the VIT is generally less than a billion, parameters.
1:05:58
Yeah, I guess mostly because it's fairly a local operation.
1:06:05
I guess that's the easy way to say it. OK, I'm going to think some other questions. I'm going to try to move on now.
1:06:11
So I Qwen-3 is the last vision language model.
1:06:18
I'll talk about this SOTA, Save Our Performance.
1:06:24
There's a lot of data work that happens. Not too many details about the data
1:06:31
and the data mix in these later Qwen models. So you'll have to look at the LLaVA paper or the--
1:06:41
AI2 has a [INAUDIBLE] paper, which I didn't get a chance to talk about that has more details here.
1:06:49
And from Qwen to Qwen-2, Qwen-3, many of these changes were
1:06:56
mostly-- there's some changes there.
1:07:01
But the overall framework remains the same. And mostly, it's scaling up, curating more data sets,
1:07:09
noticing that your context isn't-- you have to handle long context and maybe sharpening your image processing and so on.
1:07:18
OK, so I'm going to now jump to something quite different.
1:07:26
So there's this paper called Chameleon from Meta in 2024.
1:07:32
So, so far, VLMs encode images into these vectors
1:07:37
and then they inject them into a language model. And because it's a language model,
1:07:42
you can only generate text. You can't generate images.
1:07:49
So there's many ways you can go about fixing this. You can just take a VLM.
1:07:55
You can also attach a diffusion head. And now you can generate images or-- but I want to talk about this idea here,
1:08:04
which is Chameleon says, what if we mapped everything into discrete tokens?
1:08:10
And in some ways aesthetically this is-- well, maybe this reflects that I'm a language person.
1:08:16
This is appealing because now you can analyze and generate
1:08:22
images in the same way. Everything is a discrete token. If you want to do what--
1:08:30
let's say you want to generate images. You can say, here's a text prompt.
1:08:37
And then you can just-- these are more just tokens that you can generate.
1:08:42
Or you can flip the language and the text around as well.
1:08:49
They have these examples from the Chameleon paper you can prompt in say I'm bored.
1:08:55
Can you show me some birds? And then it would be text, and then it would be images, and then some more text and some images.
1:09:01
So it's interleaved. So in some sense, the vision of an omni model
1:09:06
is that text and images truly live in the same space. And this is accomplished by making everything look
1:09:14
like text. That's one way to do it. OK, so I'll talk a little bit about how this is done.
1:09:20
So the thing you need to do now is to map images
1:09:28
into discrete tokens. So there's this idea rather old from Oord 2017,
1:09:38
which is called vector quantized variational autoencoder. And the basic idea here is to map an image
1:09:47
into a discrete code. And what you learn is a mapping that maps this
1:09:54
into a continuous vector. And this gets rounded to the nearest code.
1:10:00
So you have a codebook which has, let's say, 8,000 codes.
1:10:06
So these are prototypical vectors that correspond to what patches.
1:10:12
And then so you pick one of these that's the closest. And that would be your representation.
1:10:19
So that's your encoder. It goes through a continuous phase and then you round it to the nearest code.
1:10:25
And now the decoder is you take that code and you try to reconstruct the image. And then you train these VQ-VAEs by basically minimizing
1:10:36
the reconstruction loss. There are some other terms that you add because this is not differentiable, which
1:10:43
I won't have time to get into. OK, but, at the end of the day, you have 512 by 512 images that
1:10:52
are converted into 1,024 tokens, and each token comes from
1:10:59
a vocabulary of 8,000. OK, so now you have basically text.
1:11:06
You have your images, which are basically these codes.
1:11:12
They train a new tokenizer because now you have different-- your data looks different than if it were
1:11:19
just normal natural language. The training is now actually very straightforward.
1:11:29
This is just normal language model training. There's no adapter. There's no turn the vision encoder.
1:11:36
There's just a language model now. So this is the part that's appealing.
1:11:44
So there's still two stages. But even for language model training, there's usually two or more stages,
1:11:51
where the first stage is the bulk of training. It's largely unsupervised. They have both text and text and image.
1:11:59
And then stage 2, they mix in high quality data. OK, so this is really great and elegant.
1:12:09
There's a few problems with this. One is that they found that the training was not stable.
1:12:16
And the reason is that text and images, despite occupying the same space,
1:12:22
just behave very differently. So just calling things discrete tokens
1:12:30
isn't hiding the fact that there's an image living there. And in particular, text tokens, if you
1:12:36
think about predicting the next word, this has relatively lower entropy. Most words are predictable, whereas image tokens
1:12:45
have very high entropy. I don't know what shade of blue this exact token is going to be.
1:12:50
And so as a result, they found that training with this mixture led to a lot of the norms of the parameters
1:12:59
to grow and to generate loss instability.
1:13:05
They were able to mitigate or fix this to some extent via QK norm and z-loss regularization, which
1:13:12
controls the norm growth. So I'm not even going to show the results.
1:13:20
But I wanted to highlight this work because there's a certain elegance here because it's just one model that treats
1:13:28
really all modalities the same. But the downside is that it turned out
1:13:35
this model was not really as performant, and the discretization does definitely lose its information.
1:13:42
So think about OCR. Again, if you discretize very small print,
1:13:51
you're not going to be able to read it anymore. And finally, training with multi modalities is also tricky.
1:13:57
We saw this a little bit with the Qwen models and we had to play around with the weights. But this is more exacerbated here.
1:14:05
So VQ-VAEs were popular for a while.
1:14:10
And in fact, many people were using this for image generation.
1:14:15
The main reason to do that is, well, you have a transformer. How do you generate from a transformer?
1:14:21
Well, you have to generate discrete things and you basically put all your data into this discrete form.
1:14:28
But then diffusion models came out and became popular and viable for generation.
1:14:35
So this flavor of method is less popular than it used to be.
1:14:43
OK, so maybe just reflect and summarize here.
1:14:48
Frontier models these days are expected to be multimodal or even more strongly natively multimodal
1:14:55
or omni models. Unfortunately, I think when Gemini comes out
1:15:01
or GPT comes out, they tout or touted as being natively multimodal and handle all these modalities.
1:15:07
And in fact, they do. But of course, there's no details about how these are built.
1:15:14
I think that it's probably combination of having a continuous encoder because you don't want
1:15:22
to lose information and then diffusion for the generation because-- but that's my speculation.
1:15:31
The fundamental challenge, I think, when you're dealing with multimodality is how do you handle non-text modalities?
1:15:39
It's also interesting that there's a asymmetry between understanding the modality and generation.
1:15:45
And there's no one universal encoder let's say. For example, when we looked at CLIP,
1:15:53
you only cared for classification to capture high level semantics. So these vectors could be fairly small
1:15:59
and capture the high level semantics. Whereas if you wanted to do OCR or whether you wanted
1:16:05
to generate an image, then you need really fine-grained detail. You need to-- that's why diffusion is so good because it
1:16:12
can really micro optimize the low-high frequency information.
1:16:20
I think, in general, when you're dealing with multiple modalities, you have to think carefully
1:16:26
about weighing them properly. For example, video certainly has lower information density
1:16:34
than text. So you don't want a video to overwhelm your text.
1:16:41
And like I said before, at least the current best thing
1:16:47
is having continuous encoders. It seems even CLIP, even though it's five years old,
1:16:55
or similar ideas are still the go-to way to capture semantics of images.
1:17:03
Transformers are still there. And then diffusion models, which I didn't talk about, are great for generation.
1:17:12
OK, so I will stop there. That's it for the lecture on multimodality.
1:17:22
I guess we don't have any homework on doing this. But if you're curious, I think I encourage
1:17:29
you to play around with training some of these models.


&nbsp;



⬆️⬆️⬆️  END OF TRANSCRIPT  ⬆️⬆️⬆️

## §2 — Screenshot / visible source details  ·  layer: `raw_source_metadata`

*Nothing to paste here — just **drop the screenshot in chat** and the normalizer fills §0. (Or paste the visible title / channel / URL / date below.)*

## §3 — Interpretations & review log  ·  append-only (each reviewer adds a new entry; never overwrite)

### Review 001 — Knox / ChatGPT strategic read  ·  layer: `captured_interpretation_nonbinding`
- reviewer: `Knox / ChatGPT` · type: `AI assistant` · at: `TK` · purpose: `strategic source-local interpretation`

> Knox's read is the **strategic source-local interpretation** — paste **exactly what Knox outputs**, however rich, in ONE block. Do NOT split it into fields or trim it. Knox's output typically already carries: priority/depth · source type + authority context · why this matters for OMNI · core takeaway · OMNI translation · likely landing zones · doctrine/primitive pressure · keeper doctrine · what-not-to-import · hard read. (What we ASK Knox to produce = `_KNOX_STRATEGIC_READ_PROMPT.md`.) The agent's §3 Review 003 **formalizes** this (it does not re-derive it).

⬇️⬇️⬇️  PASTE KNOX'S / CHATGPT'S FULL READ BELOW (as-is)  ⬇️⬇️⬇️

This one is full semantic / top-tier for OMNI’s multimodal substrate, image/document/video ingestion, D7 artifact handling, visual evidence, OCR fidelity, multimodal context packets, modality weighting, and “not all tokens carry the same kind of information” doctrine. It is especially relevant to D7, Observation, Provider Workspace, Patient-CNS, medspa photos, documents, screenshots, lab PDFs, forms, before/after images, GUI agents, and multimodal agent work. Transcript is from the uploaded file.

Rough metadata for Opus
source_platform: YouTube
source_url: https://youtube.com/watch?v=26FtDO8ZpOU
source_title: Stanford CS336 Language Modeling from Scratch | Spring 2026 | Lecture 17: Alignment - Multimodality
channel_or_org: Stanford Online
course_context: Stanford CS336 / Language Modeling from Scratch
lecture_context: Lecture 17 / Alignment / Multimodality / VLMs / image encoders / multimodal models
speaker_context: Likely Percy Liang based on transcript style and lecture framing; video description lists Percy Liang and Tatsunori Hashimoto
speaker_affiliation_context:
Percy Liang: Professor of Computer Science
Tatsunori Hashimoto: Assistant Professor of Computer Science
published_at: Jun 4, 2026
captured_at: 2026-06-10
captured_by: Nick
capture_method: screenshot + transcript paste
content_type: technical AI lecture / multimodal models / VLMs / CLIP / SigLIP / LLaVA / Qwen-VL / Chameleon / image-tokenization / multimodal alignment
source_reliability_context: Stanford course lecture | high-authority overview source for multimodal model architectures, visual encoders, VLM training pipelines, dynamic resolution, multimodal data mixtures, and modality-token tradeoffs
screenshot_metrics_at_capture: 1,708 views; 49 likes
priority: 5/5
depth: full_semantic
recommended_status: core OMNI multimodal substrate source; route to D7 artifacts, Observation, Provider Workspace, Patient-CNS, medspa photo workflows, document/OCR ingestion, multimodal context packets, source fidelity, visual-evidence governance, and agent GUI/video/screenshot workflows
transcript_quality_note: Auto-generated transcript is usable. Some model names and acronyms may have caption artifacts, especially Qwen/Qwen3, SigLIP/SigLIP-2, AnyRes, and DeepStack. The architectural lessons are durable; exact benchmark/model-release claims should be verified before external reporting.
topic_tags_light: [CS336, language_modeling_from_scratch, multimodality, omni_model, vision_language_model, VLM, multimodal_alignment, CLIP, OpenCLIP, LAION_5B, image_text_pairs, contrastive_learning, contrastive_language_image_pretraining, SigLIP, sigmoid_loss, WebLI, image_encoder, text_encoder, vision_transformer, ViT, patch_tokenization, image_patches, attention_pooling, zero_shot_classification, ImageNet, semantic_image_representation, fixed_resolution, center_crop, dynamic_resolution, AnyRes, OCR, document_understanding, high_resolution_images, image_tokens, video_tokens, frame_sampling, modality_weighting, multimodal_token_budget, LLaVA, LLaVA_OneVision, Qwen_VL, Qwen_2_VL, Qwen_3_VL, OpenCLIP_encoder, SigLIP_2, adapter, projector, cross_attention_adapter, MLP_projector, multimodal_RoPE, 3D_RoPE, temporal_positional_encoding, explicit_video_timestamps, DeepStack, residual_stream_fusion, long_context_multimodal, 256K_context, visual_prompting, bounding_boxes, OCR_data, chart_QA, table_QA, GUI_agents, screenshots, video_understanding, GPT_4_distillation, synthetic_task_data, MS_COCO, VQA, multimodal_transfer, modality_transfer, Chameleon, VQ_VAE, discrete_image_tokens, unified_token_space, interleaved_text_image_generation, image_generation, diffusion_models, continuous_encoders, discrete_tokenization_loss, high_entropy_image_tokens, z_loss, QK_norm, modality_entropy, modality_information_density, modality_generation_asymmetry, source_fidelity, visual_evidence, D7_artifacts, multimodal_context_packet]
§0.1 rough people / authority context
primary instructor context:
name: Percy Liang
role_in_source: likely lecturer based on transcript framing and course sequence
affiliation_at_publication: Stanford University
speaker_type: AI researcher / foundation models / multimodal systems / evaluation and AI methodology
authority_context: Very high relevance for broad multimodal-model architecture, VLM training methodology, modality-token framing, and how vision encoders are attached to language models.
additional course instructor context:
name: Tatsunori Hashimoto
role_in_source: course instructor listed in video description
affiliation_at_publication: Stanford University
speaker_type: AI researcher / data-centric ML / language-model training and systems methodology
authority_context: High relevance for the broader CS336 training, data, post-training, and systems frame.
publisher / channel: Stanford Online
course / event context: Stanford CS336: Language Modeling from Scratch
perspective / conflict notes: Technical multimodality source. Durable OMNI value comes from architectural principles: modality tokenization, semantic vs high-fidelity visual representations, dynamic resolution, OCR/document fidelity, video token budgeting, modality weighting, continuous encoder vs discrete token tradeoffs, and multimodal training data design. Do not import current benchmark rankings or specific model claims without verification.
suggested_processing:
priority: 5/5
depth: full_semantic
EVRUN needed?: yes
likely landing_zones: D7 artifact doctrine / Observation / Provider Workspace / Patient-CNS / medspa photo workflows / clinical image intake / document and OCR ingestion / multimodal context packet strategy / source fidelity / GUI-agent workflows / Architecture Memory Control Plane screenshots and diagrams / AI substrate / eval harness / multimodal safety and governance
Knox / ChatGPT read for §3 Review 001

Process this one as full semantic.

This is a core multimodal substrate source for OMNI.

It is not healthcare-specific.

But it is extremely relevant because OMNI will not live in text alone.

OMNI will ingest and act on:

patient photos,
before/after medspa images,
lab PDFs,
forms,
consents,
ID cards,
screenshots,
clinical documents,
provider notes,
uploaded media,
charts,
tables,
videos,
device outputs,
telemetry screenshots,
chat attachments,
portal artifacts,
and eventually voice/video/real-world streams.

The lecture’s biggest point is:

the world is multimodal, but transformers speak tokens.

So every modality must be converted into some representation the system can reason over.

OMNI translation:

D7 cannot treat a photo, a PDF, a screenshot, a chart, a video, and a text note as the same kind of artifact just because they all become “context.”

Each modality has different information density, fidelity needs, token cost, failure modes, and source-of-truth rules.

Classification

Priority: 5/5
Depth: full semantic
Authority context: Stanford multimodal model architecture / VLM overview
OMNI relevance: top-tier for D7 artifacts, Observation, Provider Workspace, Patient-CNS, multimodal context packets, medspa photos, document/OCR ingestion, GUI/video agents, and AI substrate
Promotion posture: core substrate source; translate multimodal operating laws, not exact VLM benchmark claims

Core takeaway

Multimodal AI is not “just add images.”

It is a stack of hard choices:

how to tokenize non-text data,
what resolution to preserve,
what semantic level to encode,
how to align modalities,
how to inject visual tokens into a language model,
how to handle long visual context,
how to weight image/video/text examples,
how to avoid losing fine-grained evidence,
and how to distinguish understanding from generation.

OMNI translation:

Multimodal OMNI needs an artifact operating system, not a blob uploader.

The big OMNI translation

The world is multimodal.

The lecture starts by saying language is general, but the world includes text, images, audio, and video.

OMNI is exactly this.

Patients do not only produce text.

They send photos, forms, screenshots, videos, voice, labs, and documents.

Keeper:

Text is not the whole patient record.

The “omni model” North Star is any input modality to any output modality.

Image + video + question -> answer.

Audio -> image.

Text -> image.

Any combination in, any combination out.

OMNI does not need to build this fully on Day 0, but it needs the architecture to anticipate it.

Keeper:

Modality expansion should be expected.

Transformers speak tokens.

This is the substrate law.

Text is already tokenized.

Images, audio, and video need conversion into tokens or embeddings.

OMNI translation:

Every artifact must be represented before it can be reasoned over.

Keeper:

Artifacts need representation contracts.

A token should represent a semantic unit of information.

The lecturer says a pixel by itself is not meaningful like a subword token.

OMNI:

a raw pixel is not a clinical observation.

A screenshot is not a resolved workflow fact.

A photo is not a diagnosis.

A PDF is not a structured lab result.

Keeper:

Raw media is not structured truth.

Tokenization is easy-ish for text and hard for everything else.

BPE is annoying but manageable.

Image/video/audio tokenization is much harder.

OMNI:

text notes are easier than visual evidence.

Do not underestimate photo/PDF/video ingestion.

Keeper:

Non-text ingestion is a separate architecture problem.

There are two questions: input and generation.

How do you input non-text into a transformer?

How do you generate non-text out?

The lecture mostly focuses on input.

OMNI should similarly separate:

understanding uploaded artifacts,

from

generating documents, images, forms, videos, or visual outputs.

Keeper:

Understanding and generation are separate modality problems.

CLIP is foundational because it aligns image and text.

CLIP takes image-text pairs and learns embeddings where matching images/texts are close.

OMNI translation:

multimodal alignment means connecting raw artifact evidence to language concepts.

Keeper:

Alignment connects media to meaning.

CLIP learns semantic image representations from text.

The reason image-text pairs help is that text captions often describe high-level semantics.

OMNI:

a photo can be mapped to concepts like redness, swelling, room, label, product, before/after area, document type, chart, or UI state.

Keeper:

Text supervision gives visual semantics.

But web captions are noisy.

Captions do not always describe what is in the image.

Alt text can be incomplete, contextual, promotional, or wrong.

OMNI:

attachments may be noisy too.

Filename, surrounding message, user description, and visible content may not agree.

Keeper:

Multimodal metadata is noisy.

CLIP works despite noisy data because scale and filtering help.

This is useful but dangerous.

OMNI cannot assume scale will rescue small high-stakes datasets.

Keeper:

Noisy web-scale success does not excuse noisy clinical ingestion.

CLIP design was optimized for classification.

The lecture repeatedly emphasizes this.

CLIP captures high-level semantics, not fine-grained details.

OMNI:

a general image embedding may be fine for “this is a face photo” or “this is a lab report.”

It may not be enough for OCR, small rash changes, document values, medication labels, subtle visual changes, or timestamps.

Keeper:

Semantic encoding is not evidence fidelity.

Fixed resolution and center crop are convenience assumptions.

CLIP resizes and crops images.

That works for ImageNet-style centered objects.

It is bad for documents, screenshots, forms, labels, charts, and clinical photos where edges and fine details matter.

Keeper:

Preprocessing can destroy evidence.

OCR requires high resolution.

LLaVA OneVision and Qwen variants add dynamic resolution because documents and text-in-image need fine-grained information.

OMNI:

lab PDFs, consents, IDs, screenshots, product labels, chart images, and forms need OCR-grade fidelity.

Keeper:

OCR needs fidelity, not just semantics.

AnyRes is a key primitive.

Instead of downsampling the whole image, split it into chunks/crops at encoder resolution and concatenate their visual tokens.

OMNI:

large artifacts should be tiled.

A full-page lab PDF or screenshot should not be smashed into a thumbnail.

Keeper:

Tile large artifacts.

Downsampled global view plus local crops is a strong pattern.

LLaVA OneVision keeps a global downsample and local high-res crops.

OMNI should use the same idea:

global artifact overview,

plus

high-fidelity tiles for relevant regions.

Keeper:

Use overview plus detail.

Multiple images and video require token budgeting.

Single image can get more detail.

Multiple images get less per image.

Video gets fewer tokens per frame because otherwise the context explodes.

OMNI:

a single suspicious lesion photo can deserve high detail.

A batch of 20 before/after photos needs sampling.

A video needs frame selection.

Keeper:

Visual attention is budgeted.

Video is not just many images.

Video adds time.

Temporal position matters.

Qwen explicitly adds timestamps as tokens because users ask questions like “what happened after two seconds?”

OMNI:

video artifacts need temporal anchors.

Keeper:

Video evidence needs time references.

Explicit timestamps are better than only implicit position.

If time exists only in positional encoding, it is harder for the language model to refer to.

OMNI:

source timestamps, photo capture time, upload time, procedure time, and frame time should be explicit.

Keeper:

Make time explicit.

Modality tokens can dominate if not weighted.

Qwen uses square-root-like length normalization because video examples can have many more tokens than image examples.

OMNI:

long videos, huge PDFs, or screenshot sequences can dominate context and training/eval.

Keeper:

Token count is not importance.

Information density differs by modality.

The lecture says video has lower information density than text.

OMNI:

one minute of video may produce many tokens but not more important signal than one critical lab value or consent line.

Keeper:

Weight by signal, not raw tokens.

SigLIP improves CLIP by changing the loss.

CLIP’s loss ties batch size to the objective.

SigLIP uses binary image-text alignment and decouples loss from batch size.

OMNI lesson:

representation-learning objectives matter operationally.

Sometimes a different objective makes training more scalable and stable.

Keeper:

Objective design affects systems feasibility.

Batch size can be part of the loss.

In CLIP, changing batch size changes the loss behavior.

OMNI analogy:

some workflows accidentally change the task when you change batch size.

A review rubric built for one case at a time may not work on batch review.

Keeper:

Operational scale can change the objective.

Critical batch size appears again.

SigLIP found larger batch sizes stopped helping past a point.

OMNI:

multimodal ingestion also has critical batch sizes.

More images/docs/videos in one context is not always better.

Keeper:

Multimodal batching has ceilings.

VLMs usually stitch a vision encoder to a language model.

LLaVA pattern:

vision encoder,

adapter/projector,

language model.

OMNI:

multimodal reasoning often comes from adding artifact encoders to existing language/reasoning substrate.

Keeper:

Use encoders as modality adapters.

The adapter is a serious boundary.

The projector maps vision embeddings into language-model embedding space.

OMNI equivalent:

artifact-to-domain adapters convert source media into structured context.

Examples:

photo -> visual findings candidate,

PDF -> structured document fields,

screenshot -> UI state,

video -> temporal event summary.

Keeper:

Adapters define what crosses modality boundaries.

Alignment phase trains only the adapter.

LLaVA first freezes vision encoder and language model, training only the projector.

OMNI analogy:

before changing domain truth, learn how to align artifact representation to existing system semantics.

Keeper:

Align before full fine-tuning.

Later stages fine-tune more of the system.

After alignment, LLaVA trains adapter plus language model.

Qwen stages train different components.

OMNI:

multimodal integration should be staged.

Do not immediately let raw image/PDF ingestion mutate clinical memory or workflow truth.

Keeper:

Stage multimodal adoption.

Synthetic task data powers many VLMs.

LLaVA uses MS COCO captions/object annotations and GPT-4-generated conversations/descriptions/reasoning.

LLaVA OneVision distills GPT-4-style models heavily.

OMNI:

synthetic multimodal tasks can bootstrap behavior, but native data is needed for differentiated workflows.

Keeper:

Synthetic data bootstraps; native data differentiates.

Task-specific data creates task-shaped models.

VQA, chart QA, OCR, table QA, visual prompting, screenshot tasks.

OMNI:

if we need medspa photo comparison, lab PDF extraction, screenshot GUI work, or consent review, we need those task types in eval/training.

Keeper:

Train the tasks you need.

Enough task diversity can create transfer.

LLaVA OneVision shows transfer from single-image chart/OCR reasoning to multiple images and videos.

OMNI:

if the system learns enough primitive multimodal tasks, it may compose them.

But transfer must be tested.

Keeper:

Task diversity enables transfer.

Transfer across modalities is real but not guaranteed.

Training on single-image OCR does not automatically prove multi-image document reasoning is safe.

Keeper:

Test cross-modality transfer.

GUI agents depend on screenshot understanding.

The lecture explicitly notes multiple screenshots support GUI agents.

OMNI:

agent workflows will need screenshot/UI understanding for vendor portals, EHRs, web dashboards, and internal tools.

Keeper:

Screenshots are agent context.

Visual prompting is a useful primitive.

Circles/highlights in an image direct attention.

OMNI:

users may annotate photos, screenshots, or documents.

A provider may circle a lesion.

A manager may highlight a UI issue.

A patient may point to a body area.

Keeper:

Annotations are part of the artifact.

Bounding boxes and references are structured visual outputs.

Qwen-VL can output bounding boxes.

OMNI:

visual claims should ideally point to regions, not just describe the whole image.

Keeper:

Visual evidence needs localization.

Multimodal RoPE encodes height, width, and time.

Qwen adds spatial/temporal positional structure.

OMNI:

artifact position matters.

Row/column in table,

field position on form,

region in image,

frame time in video,

screen location in UI.

Keeper:

Position is evidence.

Interleaving temporal/spatial frequencies matters.

Qwen-3 improved RoPE by exposing all axes to low/high frequencies.

OMNI high-level lesson:

do not collapse one dimension of evidence into low-resolution treatment while another gets high resolution.

Keeper:

All evidence axes need adequate resolution.

Long context is central to multimodality.

Qwen-3-VL emphasizes 256K context because images/videos/documents create huge token loads.

OMNI:

multimodal workflows are context-heavy.

Long patient histories plus photos/docs/videos will stress context architecture.

Keeper:

Multimodal means long-context pressure.

Long context alone is not enough.

You still need token budgeting, cropping, normalization, timestamps, adapters, and task-specific data.

Keeper:

Bigger window does not solve multimodality.

Better language models improve VLM quality.

Qwen upgrades the language model and gets better multimodal behavior.

OMNI:

the visual system is not isolated.

The reasoning substrate matters.

Keeper:

Multimodal quality depends on the language/reasoning core.

Vision encoder is often smaller than the language model.

The lecturer notes the vision encoder is local and smaller; most capability remains in the language model.

OMNI:

artifact encoders provide evidence; reasoning/decision lives elsewhere.

Keeper:

Encoder is not the decision-maker.

Vision encoders do local representation, not full reasoning.

A patch encoder sees local visual structure.

The language model reasons over encoded tokens.

OMNI:

OCR/vision extraction produces candidates.

Clinical/business/domain reasoning still belongs to authorized domains.

Keeper:

Perception is not decision.

Qwen pipelines get complicated.

Qwen-3 has multiple pretraining stages, longer context stages, SFT, distillation, RL.

OMNI:

serious multimodal behavior will require a pipeline, not one upload feature.

Keeper:

Multimodal capability is pipeline-built.

Data loading becomes a systems bottleneck.

Video/image data loading can be much more expensive than language data loading.

OMNI:

artifact ingestion pipelines need asynchronous loading, preprocessing, OCR, tiling, metadata extraction, and queueing.

Keeper:

Media ingestion has systems cost.

Multimodal training requires weighting modalities properly.

Video tokens can overwhelm text.

Long artifacts can dominate.

OMNI:

do not let giant documents/videos drown small high-authority facts.

Keeper:

Modality balance is governance.

Chameleon tries a unified discrete-token approach.

Everything becomes text-like tokens.

This is elegant because one model can understand and generate interleaved text/images.

OMNI:

a unified artifact representation is attractive.

But elegance has costs.

Keeper:

Unified representation is tempting.

Discrete image tokens lose detail.

VQ-VAE discretization can fail OCR or fine-grained image evidence.

OMNI:

do not compress clinical/legal/identity/document evidence into lossy tokens if exact details matter.

Keeper:

Discrete compression can destroy evidence.

Text and image tokens have different entropy.

Image tokens are high entropy and behave differently from text.

Training instability appears when pretending they are the same.

OMNI:

not all context tokens are equal.

A word token, lab value, image patch, video frame, PDF tile, and screenshot region have different information structure.

Keeper:

Tokens are not fungible.

QK norm and z-loss stabilize multimodal token mixing.

The specific technique is less important than the principle:

heterogeneous modality mixing can create instability and needs normalization/regularization.

OMNI:

mixed evidence streams need stabilizing rules.

Keeper:

Mixed modalities need normalization.

Continuous encoders remain the best current understanding path.

The lecture says current best practice likely uses continuous encoders for understanding because they preserve more information.

OMNI:

for high-fidelity ingestion, continuous/source-preserving representations should be preferred over lossy discrete approximations.

Keeper:

Use continuous encoders for evidence fidelity.

Diffusion remains strong for generation.

The lecture does not cover it deeply but says diffusion is good for image generation.

OMNI:

understanding patient photos and generating images are different substrate choices.

Keeper:

Generation may need different machinery.

There is asymmetry between understanding and generation.

Understanding image input is not the same as generating image output.

OMNI:

reading a lab PDF and generating a signed clinical document are different.

Understanding a before/after photo and generating marketing imagery are different.

Keeper:

Input and output modalities have different governance.

There is no universal encoder.

Classification needs high-level semantics.

OCR needs fine detail.

Generation needs pixel-level/high-frequency detail.

Video needs temporal structure.

OMNI:

artifact encoding should depend on task.

Keeper:

Encoder choice is task-dependent.

Fine-grained evidence needs high-fidelity paths.

OCR, small print, visual measurements, subtle skin changes, consent text, and timestamps need fidelity.

Keeper:

High-stakes details need high-resolution treatment.

Semantic embeddings are useful but not enough.

A CLIP-like vector may know “lab report” or “face photo.”

It may not preserve exact lab values or subtle adverse change.

Keeper:

Semantic retrieval is not extraction proof.

Multimodal artifacts must preserve original source.

Because encoders are lossy or task-shaped, D7 must keep the original artifact.

Keeper:

Always preserve the artifact.

Visual claims need source links and region references.

When a model says “the rash looks redder” or “the document says X,” it should point to the artifact and region/text span.

Keeper:

Multimodal claims need evidence handles.

OCR/text extraction should not erase layout.

Tables, charts, forms, signatures, screenshots, and labels depend on layout.

OMNI:

D7 extraction should preserve layout or link to regions.

Keeper:

Layout is meaning.

Multimodal context packets need modality budgets.

A context packet should specify:

which artifact,
which page/frame/region,
which resolution,
which OCR text,
which visual summary,
which source metadata,
which role can see it.

Keeper:

Context packets need modality controls.

Multiple representations may coexist.

For a single artifact:

original file,

thumbnail/global view,

tiles,

OCR text,

layout graph,

semantic embedding,

structured extraction,

domain candidate.

OMNI should not collapse these.

Keeper:

One artifact has many representations.

Representation is not commitment.

A visual encoder output or OCR result is a candidate.

It is not domain truth.

Keeper:

Perception outputs are candidates.

D7 is the natural home for multimodal source custody.

Photos, videos, PDFs, screenshots, consents, IDs, and attachments need D7 custody, linkage, retention, access, and source metadata.

Keeper:

D7 owns artifact custody.

Observation owns structured measured/observed values after validation.

A visual artifact may produce an observation candidate.

But Observation should own the structured temporal measured value only after appropriate verification/adoption.

Keeper:

Observation owns validated values, not raw media.

Clinical Memory should not absorb raw visual claims without authority.

A model saying “this looks infected” is not clinical truth.

Provider/domain commit required.

Keeper:

Visual AI proposes; clinicians/domains commit.

Patient-CNS needs multimodal triage.

Patients may send photos or screenshots.

The system must decide:

routine response,

ask for better photo,

route to provider,

extract document,

flag safety issue,

or create D7 artifact.

Keeper:

Multimodal intake needs routing.

Provider Workspace needs selected multimodal packets.

Providers should not get a giant pile of every image and extracted tile.

They need selected evidence, summaries, source links, and drill-down.

Keeper:

Provider visual context should be curated.

Medspa workflows are strongly multimodal.

Before/after photos,

skin concerns,

treatment areas,

consent forms,

product labels,

procedure documentation,

marketing assets.

OMNI/Bloom needs visual evidence doctrine early.

Keeper:

Aesthetics business is visual-data-heavy.

Photo comparison requires controlled capture context.

The lecture does not cover this directly, but the implication is clear:

visual reasoning depends on preprocessing and representation.

Before/after comparisons need lighting, angle, camera, time, treatment metadata, and body/face region alignment.

Keeper:

Image evidence needs capture protocol.

Multimodal evals must match fidelity requirements.

Do not evaluate OCR with image classification metrics.

Do not evaluate photo triage with generic VQA only.

Keeper:

Eval the modality task, not the model label.

Data curation remains central.

LLaVA and Qwen improvements come heavily from data curation, targeted tasks, distillation, and staged training.

OMNI:

multimodal performance will depend on curated real tasks.

Keeper:

Multimodal quality is data-shaped.

Open-source data is valuable for replication.

LLaVA’s open data helps study the system.

OMNI:

internal multimodal eval/training data should be cataloged and reproducible.

Keeper:

Data visibility enables improvement.

Closed frontier multimodal details are opaque.

The lecturer says Gemini/GPT-style native multimodality lacks public build detail.

OMNI should not rely on vendor marketing claims.

Keeper:

Native multimodal claims need caution.

Multimodal models can understand but may not generate other modalities.

Many VLMs ingest images/video but output only text.

OMNI:

a system that reads artifacts is not the same as one that can generate regulated documents, annotated images, or visual outputs.

Keeper:

Input multimodality is not output multimodality.

Tool use may bridge output modalities.

Even if the model only outputs text, it can emit bounding boxes, commands, structured annotations, or tool calls to generate/manipulate artifacts.

OMNI:

visual workflows may output structured actions rather than images.

Keeper:

Structured visual actions matter.

Multimodal agent workflows need artifact pointers.

A GUI agent should refer to screenshots/regions/time.

A document agent should refer to page/field/table cell.

A photo agent should refer to region/body part/time.

Keeper:

Agents need artifact addressing.

Multimodal safety is not just text safety.

Images can include PHI, minors, body areas, IDs, addresses, sensitive documents, or unsafe requests.

OMNI:

visual privacy and consent are first-class.

Keeper:

Visual data has privacy gravity.

Source metadata matters.

Capture time, upload time, device, patient identity confidence, user-provided description, procedure context, consent, access rights.

Keeper:

Artifact metadata is clinical/business context.

Multimodal ingestion must separate patient-supplied description from model observation.

A patient saying “this is infected” is different from model detecting redness and provider diagnosing infection.

Keeper:

Speaker/source boundaries remain.

Visual summaries can hallucinate.

VLMs can invent details, OCR wrong values, misread charts, or infer beyond evidence.

Keeper:

Visual summaries need source audit.

Multimodal artifacts increase context explosion.

Every photo/page/frame can create many tokens.

OMNI must combine Lecture 17 with Lectures 4, 7, 8, 10, and 12:

sparse selection,
parallelism,
context paging,
inference cost,
evaluation.

Keeper:

Multimodal is a context-cost multiplier.

The strongest OMNI doctrine is artifact pluralism.

Do not force all artifacts into one representation.

Use the right representation for the task:

semantic embedding for retrieval,

OCR for text,

layout graph for forms/tables,

tiles for high-res inspection,

original source for proof,

structured extraction for domains,

human review for high stakes.

Keeper:

Represent artifacts by purpose.

Likely OMNI landing zones

D7:

artifact custody
original media preservation
page/frame/region addressing
OCR/linkage/layout
thumbnails/global views
high-res tiles
media metadata
retention/access/consent
visual source provenance

Observation:

visual-to-observation candidates
measured value extraction
artifact-backed evidence
verification/adoption boundary
temporal/frame anchoring
device/source fidelity

Clinical Memory:

source-grounded visual facts
no raw VLM claim as clinical truth
provider adoption boundary
current view from validated multimodal evidence
conflict/reconciliation for image-derived claims

Provider Workspace:

curated visual packets
before/after comparison
image/document drill-down
source/region links
OCR confidence
chart/table summaries
high-risk artifact flags

Patient-CNS:

photo/document intake
ask-for-better-image flows
screenshot triage
safety routing
D7 artifact creation
patient description vs observed evidence separation
multimodal escalation rules

Medspa / Bloom:

treatment photos
skin concern images
consent photos/forms
product/lot labels
before/after capture protocol
marketing vs clinical photo separation
provider review of aesthetic visual evidence

AI substrate:

modality encoders
artifact representation registry
multimodal context packets
dynamic resolution
OCR pipelines
video/frame sampling
modality weighting
multimodal model-router strategy

Context packet strategy:

modality budgets
global overview plus detail tiles
page/frame/region references
OCR plus original source
shared artifact blocks
visual context paging
source fallback

Build OS / Agent Work Blueprint:

screenshot agents
GUI agents
document agents
visual trace artifacts
region-addressed proof
multimodal eval tasks
artifact-aware agent memory

Architecture Memory Control Plane:

screenshots/diagrams as sources
diagram OCR/visual parsing
source artifact preservation
multimodal extraction proof
document-layout aware ingestion
no visual evidence collapsed into text summary only

Eval harness:

OCR eval
visual QA eval
chart/table eval
document fidelity eval
before/after eval
region localization eval
privacy/safety eval
modality-transfer eval
dumb baselines and trace audit

Governance / Safety:

visual PHI
consent and access controls
image/body-area privacy
document ID/privacy handling
patient-upload metadata
source vs model observation separation
hallucinated visual claim checks

Doctrine / primitive pressure

Potential concepts:

text_not_whole_record
modality_expansion_expected
artifact_representation_contract
raw_media_not_structured_truth
non_text_ingestion_architecture
understanding_vs_generation
media_to_meaning_alignment
multimodal_metadata_noisy
semantic_encoding_not_evidence_fidelity
preprocessing_can_destroy_evidence
OCR_requires_fidelity
tile_large_artifacts
overview_plus_detail
visual_attention_budget
video_time_reference
explicit_time_tokens
token_count_not_importance
signal_weighting
operational_scale_changes_objective
modality_adapter
align_before_full_finetune
stage_multimodal_adoption
synthetic_bootstrap_native_differentiation
task_shaped_multimodal_data
cross_modality_transfer_test
screenshots_as_agent_context
visual_prompting
visual_localization
position_as_evidence
long_context_multimodal_pressure
encoder_not_decision_maker
perception_not_decision
media_ingestion_systems_cost
modality_balance_governance
unified_representation_temptation
discrete_compression_evidence_loss
tokens_not_fungible
mixed_modality_normalization
continuous_encoder_for_fidelity
modality_input_output_asymmetry
task_dependent_encoder
semantic_retrieval_not_extraction_proof
artifact_preservation
multimodal_evidence_handle
layout_as_meaning
modality_controls_in_context_packet
artifact_representation_pluralism
perception_output_candidate
visual_AI_proposes_domain_commits
multimodal_intake_routing
curated_provider_visual_context
visual_data_capture_protocol
visual_privacy_gravity
artifact_addressing
multimodal_context_cost_multiplier
represent_by_purpose

Keeper doctrine candidates

Text is not the whole patient record.

Modality expansion should be expected.

Artifacts need representation contracts.

Raw media is not structured truth.

Non-text ingestion is a separate architecture problem.

Understanding and generation are separate modality problems.

Alignment connects media to meaning.

Text supervision gives visual semantics.

Multimodal metadata is noisy.

Noisy web-scale success does not excuse noisy clinical ingestion.

Semantic encoding is not evidence fidelity.

Preprocessing can destroy evidence.

OCR needs fidelity, not just semantics.

Tile large artifacts.

Use overview plus detail.

Visual attention is budgeted.

Video evidence needs time references.

Make time explicit.

Token count is not importance.

Weight by signal, not raw tokens.

Objective design affects systems feasibility.

Operational scale can change the objective.

Multimodal batching has ceilings.

Use encoders as modality adapters.

Adapters define what crosses modality boundaries.

Align before full fine-tuning.

Stage multimodal adoption.

Synthetic data bootstraps; native data differentiates.

Train the tasks you need.

Task diversity enables transfer.

Test cross-modality transfer.

Screenshots are agent context.

Annotations are part of the artifact.

Visual evidence needs localization.

Position is evidence.

All evidence axes need adequate resolution.

Multimodal means long-context pressure.

Bigger window does not solve multimodality.

Multimodal quality depends on the language/reasoning core.

Encoder is not the decision-maker.

Perception is not decision.

Multimodal capability is pipeline-built.

Media ingestion has systems cost.

Modality balance is governance.

Unified representation is tempting.

Discrete compression can destroy evidence.

Tokens are not fungible.

Mixed modalities need normalization.

Use continuous encoders for evidence fidelity.

Generation may need different machinery.

Input and output modalities have different governance.

Encoder choice is task-dependent.

High-stakes details need high-resolution treatment.

Semantic retrieval is not extraction proof.

Always preserve the artifact.

Multimodal claims need evidence handles.

Layout is meaning.

Context packets need modality controls.

One artifact has many representations.

Perception outputs are candidates.

D7 owns artifact custody.

Observation owns validated values, not raw media.

Visual AI proposes; clinicians/domains commit.

Multimodal intake needs routing.

Provider visual context should be curated.

Aesthetics business is visual-data-heavy.

Image evidence needs capture protocol.

Eval the modality task, not the model label.

Multimodal quality is data-shaped.

Data visibility enables improvement.

Native multimodal claims need caution.

Input multimodality is not output multimodality.

Structured visual actions matter.

Agents need artifact addressing.

Visual data has privacy gravity.

Artifact metadata is clinical/business context.

Speaker/source boundaries remain.

Visual summaries need source audit.

Multimodal is a context-cost multiplier.

Represent artifacts by purpose.

What not to import blindly

Do not treat a VLM image summary as clinical truth.

Do not assume an image embedding preserves exact evidence.

Do not downsample documents, screenshots, labels, or clinical photos without checking what evidence is lost.

Do not use image-classification-oriented encoders for OCR or fine-grained evidence without validation.

Do not collapse raw media, OCR, visual summary, and structured extraction into one field.

Do not let video or large PDFs dominate token budgets just because they are long.

Do not treat token count as importance.

Do not assume cross-modality transfer is safe without testing.

Do not use generic VQA benchmarks as proof for medspa, clinical, legal, or document workflows.

Do not assume “native multimodal” vendor claims reveal how the system works.

Do not mix patient images across contexts without consent/access controls.

Do not let visual artifacts become source truth without D7 custody and metadata.

Do not let OCR become canonical without source link and confidence/proof.

Do not generate clinical/aesthetic conclusions from photos without provider/domain commit.

Do not assume visual model localization is correct unless region evidence is checked.

Do not use lossy discrete image tokens for high-stakes fine detail.

Do not ignore layout in forms, tables, charts, and screenshots.

Do not forget capture protocol for before/after or clinical comparison photos.

Do-not-miss lesson

This source gives OMNI a multimodal law:

Every artifact needs the right representation for the job. A semantic image embedding may be enough to retrieve or classify, but not enough to prove an OCR value, compare a subtle before/after photo, read a consent form, interpret a chart, or support a clinical decision. OMNI must preserve the original artifact, create task-appropriate representations, attach source/region/time handles, and keep perception outputs as candidates until the owning domain commits.

Shortest OMNI version

OMNI should not “just add images.” It should build artifact infrastructure: D7 custody for originals, OCR/layout/tiles/embeddings as separate representations, multimodal context packets with page/frame/region/time controls, dynamic resolution for fine detail, source fallback for every visual claim, modality weighting so video/PDFs do not swamp text, and domain commit boundaries so visual AI proposes but does not become truth.

My hard read

Full semantic. Top-tier multimodal substrate source.

This belongs in D7, Observation, Provider Workspace, Patient-CNS, medspa photo workflows, AI substrate, context packet strategy, GUI-agent design, eval harness, and multimodal governance.

The strongest OMNI line:

OMNI’s multimodal layer should preserve artifacts like evidence, encode them like signals, route them like context, evaluate them by task, and commit them only through the owning domain. A photo, PDF, screenshot, table, chart, or video is not “just more tokens.” It is source material with fidelity, layout, time, privacy, and authority constraints.





&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 002 — Nick operator note  ·  layer: `human_context_nonbinding`
- reviewer: `Nick` · type: `human founder/operator` · at: `TK`

⬇️⬇️⬇️  YOUR GUT NOTE BELOW  (optional)  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 003 — Opus / agent formal deep extraction  ·  layer: `analysis_nonbinding`  ·  (agent-authored, NOT a Nick paste)
- reviewer: `Opus` (or agent) · type: `AI assistant` · at: `2026-06-13` · purpose: `formalize Review 001 → structured per-source extraction → feed EVRUN concept registry` · binds nothing (`GRD-036`/`GRD-044`)

> The agent reads §1 verbatim + §3 Review 001 (Knox) IN FULL, then writes the formal per-source extraction HERE (not a sidecar). Formalize Review 001's strategic read — verify/sharpen it, do not re-derive. Per concept cluster: **concept · OMNI meaning · why · downstream homes (thesis §x / CNS / Build-OS / §C / capability-topology / Knowledge-Reservoirs / security / domain-contracts / UX-surfaces / product / future-watch) · source anchors (verbatim ≤12 words + timestamp) · stale-vs-v3 (ABSENT/PARTIAL/AFFIRM) · weight_tier (spine/vocabulary/low-authority-watch/no-op) · status (promote/watch/reject).** Then: **net-new primitives** (dedup vs registry — don't re-mint) + **reread flags**. Cross-source convergence is folded into the EVRUN **concept registry**, not duplicated here.

⬇️⬇️⬇️  AGENT WRITES THE FORMAL EXTRACTION BELOW  ⬇️⬇️⬇️

**Review 003 — formal deep extraction · Opus · 2026-06-13 · EVRUN-2026-000002 (wave-2) · binds nothing (`GRD-036`/`GRD-044`)**

**Orientation (formalizes Review 001; verified against §1 verbatim).** This is Stanford CS336 L17 — the canonical *academic mechanism ORIGIN* of the multimodal-evidence-substrate family the wave-2 corpus already carries: it is the **input/understanding-side mechanics** under **159** (Victoria Lin — representation *rationale*), the substrate-pipeline of **098/099** (`media_to_observation_pipeline` / `multimodal_context_packet` / `media_artifact_preservation`), the imaging instance **157** (`artifact_multi_observation`), the generation-side twin **161** (CME296 diffusion), and the care-multimodal slice **142**. It is **NOT healthcare** — durable OMNI value = the operating LAWS of how non-text artifacts get represented, budgeted, located, and committed. Posture (per `GRD-039`/`GRD-026`): treat as **AFFIRM-spine + a focused net-new MECHANISM set the family gestured at but never named** (much like 181/180 anchored 124). Reject all benchmark/leaderboard/param numerics and architecture math; keep model nouns as Lens-B comparators. **Naming guardrail (inherit from 159):** the lecture's "omni model" (any-modality-in → any-modality-out) is the speaker's term and ≠ OMNI the governed substrate — do not conflate. Knox's Review 001 is largely affirmed and sharpened; I disagree with nothing material, but I down-rank several Knox "Keepers" to EXISTS-AS to avoid re-minting the 098/099/159 spine.

**A. Concept clusters**

| concept (Knox term) | OMNI meaning | why | downstream homes | source anchors (verbatim ≤12w + ts) | stale-vs-v3 | conflict_status | weight_tier | status |
|---|---|---|---|---|---|---|---|---|
| transformers-speak-tokens / artifact must be represented (Artifacts need representation contracts) | every non-text artifact (photo/PDF/screenshot/video) must be converted to a representation before reasoning; "a token = a semantic unit" — a raw pixel is not an observation | mechanism floor under 098/159: representation is a prior, not a free byproduct | §B AI-substrate · D7 · 098 `media_to_observation_pipeline` · 159 rationale | "the world is multi-modal" [1:00]; "they speak tokens" [2:21]; "a pixel is certainly not meaningful by itself" [2:55] | AFFIRM (098/099/159) | none | spine (anchor) | watch (no-mint) |
| understanding ≠ generation asymmetry (Understanding and generation are separate) | input-multimodality (read a lab PDF) ≠ output-multimodality (generate a signed doc/image); the two have different machinery AND different governance | reading evidence vs producing regulated artifacts must be separated authority-wise | §A safety · §B · D7 · AI #12 modality-aware eval | "asymmetry between understanding the modality and generation" [1:15:39]; "these models don't generate… all on the input side" [1:00:02] | AFFIRM (159 `understanding_generation_asymmetry`) | tension (AFFIRMS T92) | spine (anchor) | watch (EXISTS-AS 159) |
| semantic-encoding ≠ evidence-fidelity (Semantic encoding is not evidence fidelity) | a CLIP-style embedding captures "this is a lab report / face photo" (semantics) but NOT exact values/subtle change; semantic retrieval ≠ extraction proof | the load-bearing safety line for medspa photos, lab PDFs, consents | D7 · Observation · 159 `representation_choice_consequence` · `GRD-042` (retrieval≠authority) | "captures the semantics… not very fine-grained" [21:48]; "0 shot CLIP" classification framing [21:10] | AFFIRM/PARTIAL (159) | none | spine | watch (SHARPEN 159) |
| preprocessing-destroys-evidence / dynamic-resolution (AnyRes; OCR needs fidelity; Tile large artifacts; Overview+detail) | resize/center-crop/downsample silently destroys fine detail ("J looks like I"); fix = adaptive per-artifact resolution: a global downsample + local full-res crops, concatenated, with a token cap | the concrete D7-ingestion mechanism 098/159 implied but never named | **D7 ingestion** · 098 · CNS context-assembly · 168 tiling (reconcile) | "a J looks like an I" [37:16]; "crop to 336 by 336, you can't read it" [37:30]; "break up an image into multiple pieces… concatenate" [37:51]; "downsampled… entire paper… then break it up into chunks" [38:58] | PARTIAL (159 rationale; mechanism net-new) | tension (adaptive↔fixed-size) | spine | **promote-candidate (net-new ★)** |
| signal-weighted modality budget (Token count is not importance; Weight by signal; Modality balance is governance) | weight modalities by information density/signal, NOT raw token count; long video/huge PDF must not dominate context or training just because they emit more tokens | governance law for multimodal context packets + corpus mixing | **CNS context-assembly** · D7 · 098 `multimodal_context_packet` · 164 `context_mixture_policy` (reconcile) | "video certainly has lower information density than text… don't want a video to overwhelm your text" [1:16:26]; sqrt-normalize "downweight really long examples" [56:33] | ABSENT (un-named) | tension (token-count↔signal) | spine | **promote-candidate (net-new ★)** |
| explicit artifact coordinate (Position is evidence; Make time explicit; Video needs time references; Artifact addressing) | time/space position must be an EXPLICIT addressable handle (token), not only implicit position — region/page/frame/cell/timestamp; supports "what happened after 2s" + visual localization/bounding-box claims | multimodal claims need region/time evidence handles, not whole-artifact gestures | D7 region/page/frame addressing · Observation · 157 `artifact_multi_observation` · 165 anchor-relative (reconcile) | "they made the time explicit… a token that represents the time" [55:37]; "what happened after two seconds" [55:51]; mRoPE "height… width… time" [51:41]; "output bounding boxes" [48:58] | ABSENT (un-named) | none | spine | **promote-candidate (net-new ★)** |
| artifact→domain adapter + align-before-commit (Use encoders as adapters; Adapters define what crosses; Align before fine-tuning; Stage adoption) | the projector/adapter is the *typed boundary* mapping source-media → domain-candidate (photo→finding, PDF→fields, screenshot→UI-state, video→event); align representation to existing semantics FIRST, stage adoption before mutating domain truth | names the adapter+staging mechanism *inside* 098's pipeline; AFFIRMS core law | Build-OS · domain-contracts · 098 `media_to_observation_pipeline` (reconcile) · §A | "freeze the vision encoder and language model and only train this matrix W… alignment phase" [33:30]; "converting these images into textual tokens" [33:11] | PARTIAL (098 pipeline; adapter/staging net-new) | none | spine | **promote-candidate (net-new ★)** |
| artifact representation pluralism (One artifact has many representations; Represent by purpose; Representation is not commitment) | ONE artifact → MANY task-shaped representations (original / global-thumb / tiles / OCR / layout-graph / embedding / structured-extraction / domain-candidate); none is canonical; all link to the preserved original | the strongest single OMNI law in this source; extends 159 dual→N | D7 · 098 `media_artifact_preservation` · 159 `dual_representation_need` (extend) | "no one universal encoder" [1:15:45]; "classification… high-level semantics… OCR… fine-grained detail" [1:15:53] | PARTIAL (159 `dual_representation_need`) | tension (unify-all↔plural-by-purpose) | spine | **promote-candidate (SHARPEN-spine 159 ★)** |
| continuous-encoder-for-fidelity vs discrete-unification loss (Discrete compression destroys evidence; Unified representation is tempting; Tokens are not fungible; Continuous for fidelity) | Chameleon's "make everything discrete tokens" is elegant but lossy + unstable (image tokens are high-entropy, behave unlike text) → prefer continuous/source-preserving for high-fidelity understanding | the canonical anchor for 159 `modality_unification_unsolved`; anti-lossy-unification | §B · D7 · 159 `representation_choice_consequence`/`modality_interpretation_distance` | "everything into discrete tokens" [1:08:04]; "image tokens have very high entropy" [1:12:45]; "discretize very small print… can't read it" [1:13:42]; "current best thing is continuous encoders" [1:16:41] | AFFIRM (159) | tension (AFFIRMS T92/T93; sharpens T93) | spine (anchor) | watch (SHARPEN 159) |
| perception ≠ decision (Encoder is not the decision-maker; Perception is not decision; Visual AI proposes, domains commit) | the vision encoder is small/local (perception); reasoning/decision lives in the language core; visual extraction = candidate, never clinical/business truth | textbook AFFIRM of OMNI core law via the encoder/LLM split | §A core law · 098 `visual_assertion_candidate` · Clinical-Memory/Observation commit boundary | "vision encoder… a very local operation" [1:05:00]; "most of the capabilities… still in the language model" [1:05:05] | AFFIRM (core law + 098) | none | spine (anchor) | watch (EXISTS-AS) |
| layout-as-meaning (Layout is meaning) | tables/forms/charts/signatures/UI depend on layout; OCR/extraction must preserve or link to layout regions, not flatten to a text blob | D7 extraction-fidelity facet not separately named | D7 · 098 · Observation | "OCR… preserve very fine-grained information" [37:16]; AnyRes document tiling [38:58] | ABSENT (un-named) | none | vocabulary→spine | **promote-candidate (net-new, thin ★)** |
| visual privacy gravity (Visual data has privacy gravity) | a visual artifact carries denser/involuntary PHI (faces/minors/body-areas/IDs/addresses/background) than its caption — a photo leaks more than its description | security-lane facet: visual context-minimization is harder than text | security lane · 135 context-minimization (reconcile) · D7 consent/access · Patient-CNS | Knox R001 ("Images can include PHI, minors, body areas, IDs"); transcript implicit (capture/ingest framing) | ABSENT | none | vocabulary | promote-candidate (net-new, thin) |
| capture protocol (Image evidence needs capture protocol) | before/after & clinical comparison validity needs standardized capture metadata (lighting/angle/camera/time/region/treatment) | medspa/Bloom load-bearing; comparison reasoning depends on controlled capture | Observation · medspa/Bloom · D7 · 142 `care_space_observation` (reconcile) | Knox R001 (capture-context implication); AnyRes/region anchors [38:58] | ABSENT | none | vocabulary | promote-candidate (net-new, thin) |
| multimodal = context-cost multiplier + reasoning-core dependence (Multimodal means long-context pressure; Bigger window doesn't solve it; Quality depends on the language core) | every page/frame/photo explodes tokens → long-context + serving pressure; but window size alone is insufficient; multimodal quality rides on the reasoning core | AFFIRMS serving/inference-economics + context-engineering; not a new spine | §B serving (173/179) · CNS · 109 context-eng · 159 `language_reasoning_skeleton` | "handle multimodal is to deal with longer context" [41:01]; "data loading can be a bottleneck" [1:01:27]; "Qwen-3 models really good… helps quality" [53:29] | AFFIRM | none | vocabulary | watch (EXISTS-AS) |
| media-to-meaning alignment / noisy metadata (Alignment connects media to meaning; Multimodal metadata is noisy) | CLIP/contrastive alignment is the ORIGIN mechanism of `media_to_observation_pipeline`; captions/filenames/user-descriptions are noisy and may disagree with content | mechanism anchor + AFFIRM of record-noise discipline | 098 · 099 `media_context_packet` · 153 record-noise | "text… higher level semantic representations" [12:14]; "captions… don't verbatim say what's in the image" [19:41] | AFFIRM | none | vocabulary | watch (EXISTS-AS) |

**B. Net-new primitives** (deduped vs `EVRUN-2026-000001 §2A` + wave-2 §2A; ★ = load-bearing)

- `dynamic_resolution_ingestion` ★ (a.k.a. anyres_artifact_tiling) — adaptive per-artifact resolution: a downsampled global view + local full-res crops, concatenated under a token-budget cap, so fine detail (OCR/small lesions/consent text) survives. **EXISTS-AS: net-new** (fidelity-preserving ingestion). **Reconcile-watch vs 168 `tiled_context_processing`/`compact_accumulator_state`** — different axis: 168 = synthesize oversized *context*; this = preserve *artifact fidelity* at intake. Two faces of one tiling family.
- `signal_weighted_modality_budget` ★ — weight modalities by information density/signal, not raw token count; cap so video/large PDFs don't swamp text. **EXISTS-AS: net-new.** Reconcile vs 164 `context_mixture_policy` (training mix) + 098 `multimodal_context_packet` (this is the budget/weighting law inside the packet).
- `explicit_artifact_coordinate` ★ — region/page/frame/cell/timestamp must be an explicit, addressable handle (not implicit position); enables localized claims + "what happened after 2s." **EXISTS-AS: net-new.** Reconcile vs 165 anchor-relative-care-context + 157 `artifact_multi_observation` + Knox `artifact_addressing`/`visual_localization`.
- `artifact_domain_adapter` ★ + `align_before_commit` — the typed boundary mapping source-media → domain-candidate; align representation to existing semantics first (freeze/train-adapter), stage adoption before mutating domain truth. **EXISTS-AS: net-new (sharpens 098 `media_to_observation_pipeline`)** — names the adapter+staging mechanism inside the existing pipeline.
- `artifact_representation_pluralism` ★ — one artifact → many task-shaped representations, none canonical, all linked to the preserved original; "represent by purpose." **EXISTS-AS: SHARPEN of 159 `dual_representation_need`** (extend dual→N) — but load-bearing enough to surface as its own row.
- `layout_preserving_extraction` ★ — layout/structure (tables/forms/charts/signatures/UI) is itself evidence; extraction preserves or links to layout regions. **EXISTS-AS: net-new (thin; D7 facet of 098).**
- `visual_privacy_gravity` — visual artifacts carry denser/involuntary PHI than their captions. **EXISTS-AS: net-new (thin).** Reconcile vs 135 context-minimization + PHI-min.
- `capture_protocol` / `controlled_capture_context` — comparison validity (before/after, clinical photo) requires standardized capture metadata. **EXISTS-AS: net-new (thin).** Reconcile vs 142 `care_space_observation`.

EXISTS-AS / SHARPEN (do **not** re-mint — Knox "Keeper" candidates folded back to the family):
- `understanding_vs_generation` → 159 `understanding_generation_asymmetry` (this lecture = mechanism anchor; AFFIRMS T92).
- `continuous_encoder_for_fidelity` / `discrete_compression_evidence_loss` / `unified_representation_temptation` / `tokens_not_fungible` → SHARPEN 159 `representation_choice_consequence` + `modality_interpretation_distance` + `modality_unification_unsolved` (Chameleon = canonical anchor).
- `raw_media_not_structured_truth` / `perception_not_decision` / `visual_AI_proposes_domain_commits` / `perception_output_candidate` → §A core law + 098 `visual_assertion_candidate`.
- `media_to_meaning_alignment` → 098 `media_to_observation_pipeline` (CLIP = origin mechanism).
- `multimodal_metadata_noisy` → 099 `media_context_packet` + 153 record-noise.
- `semantic_retrieval_not_extraction_proof` → 159 + `GRD-042` (retrieval≠authority).
- `multimodal_context_cost_multiplier` / `long_context_multimodal_pressure` / `media_ingestion_systems_cost` → serving family (173/179) + 109 context-eng.
- `multimodal_quality_depends_on_reasoning_core` → 159 `language_reasoning_skeleton`.
- `screenshots_as_agent_context` / `visual_prompting` / `structured_visual_actions` → Build-OS agent-context + 098 + folds into `explicit_artifact_coordinate`.
- `synthetic_bootstrap_native_differentiation` / `task_shaped_multimodal_data` / `cross_modality_transfer_test` → Build-OS eval/data (054/164) + 158 `human_ai_disagreement_corpus`.
- `objective_design_affects_systems` / `operational_scale_changes_objective` / `multimodal_batching_ceiling` (CLIP→SigLIP batch-in-loss; critical batch size) → CS336 systems family (167/171/172) — Lens-B vocabulary; thin governance analogy ("a rubric built one-case-at-a-time may break on batch review") = low-authority-watch, no mint.
- `DeepStack residual-stream fusion` (deep vs black-box encoder fusion) → AFFIRMS 168 `fuse_computation_not_commitment` boundary discipline (fuse representation freely; never fuse a commit seam). Vocabulary.

**C. Reread flags**
- `dynamic_resolution_ingestion` vs 168 tiling: confirm the parent wants ONE tiling family (artifact-fidelity axis + context-synthesis axis) per existing reconciliation-watch #64 — flagged, not resolved here.
- `artifact_representation_pluralism` vs 159 `dual_representation_need`: I treat as SHARPEN-extend (dual→N). If the parent prefers a distinct named law (it is arguably the single strongest line in this source), elevate — flagged for Knox.
- Caption artifacts in transcript (Qwen↔Gwen, SigLIP↔CLIP swaps, "DNA"=did, "Neovision"=likely "computer vision") — architectural lessons are robust; do not quote exact model-version/benchmark strings externally (`GRD-039`).
- `visual_privacy_gravity` + `capture_protocol` are Knox-implied, not transcript-explicit; kept thin/low-tier pending a security-lane / medspa second look.

**D. One-line hard read**
Full-semantic, but **AFFIRM-spine, not new-spine**: this is the academic *mechanism origin* of the multimodal-evidence-substrate the corpus already owns (098/099/159/157/161/142), earning its keep via a tight net-new mechanism set — `dynamic_resolution_ingestion` ★, `signal_weighted_modality_budget` ★, `explicit_artifact_coordinate` ★, `artifact_domain_adapter`+`align_before_commit` ★, and the SHARPEN-to-spine `artifact_representation_pluralism` ★. **Strongest OMNI line:** *every artifact needs the right representation for the job — preserve the original like evidence, encode it like a task-specific signal, address it by region/page/frame/time, budget it by information density not token count, and keep every perception output a candidate until the owning domain commits.* Reject all benchmark/param numerics and architecture math (`GRD-039`/`GRD-026`); the lecture's "omni model" ≠ OMNI.

⬆️⬆️⬆️  END Review 003  ⬆️⬆️⬆️

## §4 — Analysis pointers  *(agent fills at CLOSEOUT once analyzed — the "linked" step; leave `TK` until then)*
- EVRUN(s): `EVRUN-2026-000002` (ai-corpus wave-2) · concept_registry: `EVRUN-2026-000002_ai-corpus-wave-2_concept_registry_and_routing_map.md` · source_anchor_ledger: `EVRUN-2026-000002_ai-corpus-wave-2_source_anchor_ledger_receipts_only.md` · per-source deep-read: §3 Review 003 (this file) · impact: `D7 artifact ingestion (dynamic-resolution/AnyRes tiling, layout-preserving extraction, explicit coordinate addressing, artifact preservation) · §B AI-substrate + CNS context-assembly (signal-weighted modality budget) · §A core law (perception≠decision; understanding≠generation) · Observation/medspa (capture protocol) · security lane (visual privacy gravity) · Build-OS/domain-contracts (artifact→domain adapter + align-before-commit)` · promotion: `watch` (AFFIRM-spine for the 098/099/159/157/161/142 multimodal-evidence family + 5 net-new ★ mechanism primitives; nothing promoted — `GRD-036`)

## §5 — Change log
- `2026-06-11` — source file created.
- `2026-06-13` — §3 Review 003 formal deep extraction written (Opus, EVRUN-2026-000002): formalized Knox Review 001 against §1 verbatim; AFFIRM-spine for multimodal-evidence-substrate family (098/099/159/157/161/142) + net-new mechanism set (`dynamic_resolution_ingestion` ★, `signal_weighted_modality_budget` ★, `explicit_artifact_coordinate` ★, `artifact_domain_adapter`+`align_before_commit` ★, SHARPEN-spine `artifact_representation_pluralism` ★, thin: `layout_preserving_extraction`/`visual_privacy_gravity`/`capture_protocol`); REJECT all benchmark/param numerics + architecture math (`GRD-039`/`GRD-026`); naming guardrail (lecture "omni model" ≠ OMNI). §4 pointers filled; status raw_dropped → analyzed. Registry-fold packet returned to Opus-main for EVRUN-2026-000002 fold (no sidecar — `GRD-044`).

> Authority/retrieval labels are defined once in `../../00_evidence_router.md` (operating principle #9 + §8). Sections carry their `layer:` inline; the legend is not restated per file.
