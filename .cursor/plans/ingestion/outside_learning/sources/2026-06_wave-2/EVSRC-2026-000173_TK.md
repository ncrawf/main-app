# EVSRC-2026-000173 — <title or TK>

Document type: `source_capture` · Lane / reservoir: `outside_learning`
**Status:** `raw_dropped` → `normalized` → `reviewed` → `analyzed` → `promoted | deferred`  ·  **current: `raw_dropped`**
Authority: `evidence_nonbinding` — captured source + interpretations only; binds nothing until routed + promoted.

> **HOW TO USE THIS FILE — two kinds of marks:**
> • `TK` = a field the normalizer (Opus) fills from the screenshot — **leave it alone.**
> • **⬇️ PASTE blocks = the ONLY spots Nick touches.** Exactly **3**: transcript (§1), **Knox read (§3 Review 001)**, gut note (§3 Review 002, optional). Plus: **drop the screenshot in chat** → Opus fills §0.
> Copy this into `sources/<YYYY-MM>/`, rename `EVSRC-2026-000173_TK.md` (id = highest EVSRC + 1), add a row to `../../00_index.md`.
>
> **Per-source flow (the standard):** Nick pastes transcript (§1) + Knox's strategic read (§3 **Review 001** — paste it AS-IS, however rich Knox makes it; do NOT reformat into fields) + optional gut note (§3 Review 002). Then the agent (Opus) writes **§3 Review 003 — formal deep extraction** (formalizing Review 001, NOT rediscovering it), updates the run's **concept registry** (cross-source synthesis), the coverage matrix, and fills **§4 pointers** at closeout. **The per-source deep read lives HERE in §3 — never in a separate sidecar/extraction file** (`GRD-044`). Cross-source synthesis lives in the EVRUN concept registry.

## §0 — Source identity / metadata  *(normalizer fills from the screenshot — leave `TK`)*
- evsrc_id: `EVSRC-2026-000173`  ·  filename: `EVSRC-2026-000173_TK.md`
- source_platform: `YouTube`  ·  source_url: `TK`  ·  source_title: `TK`
- channel_or_org: `TK`  ·  speaker: `TK`  ·  published_at: `TK`
- captured_at: `2026-06-11`  ·  captured_by: `Nick`  ·  capture_method: `transcript paste + screenshot`
- content_type: `TK`  ·  source_reliability_context: `academic | practitioner | vendor | founder | investor | security-researcher | unknown`  ·  topic_tags_light: `[TK]`

## §0.1 — People / authorship / authority context  *(normalizer fills from screenshot + description + transcript — this is what makes a source a reservoir, not a transcript dump)*
- primary speaker(s):
  - name: `TK` · role_in_source: `TK` (speaker / presenter / interviewee / host / author) · affiliation_at_publication: `TK` · speaker_type: `TK` (founder | researcher | investor | operator | vendor | clinician | regulator | journalist | educator | other) · authority_context: `TK` · identity_confidence: `TK` (high_from_screenshot | inferred | unknown)
  - *(add a bullet per additional speaker)*
- publisher / channel: `TK`  ·  interviewer / moderator / host: `TK`
- event_context: `TK`  ·  perspective / conflict notes: `TK`

> **Authority is descriptive, not worship** (`GRD-039`): record *who* said it (raises relevance), but every claim still routes through evidence → interpretation → gated promotion. (Clinical reservoir extends this block heavily — authors / affiliations / journal / DOI-PMID / study-type / evidence-level / COI / retraction — `FWREG-006`.)

## §0.5 — Processing checklist
**Nick drops:** [ ] transcript → §1 · [ ] screenshot in chat · [ ] **Knox strategic read → §3 Review 001** (paste as-is) · [ ] (optional) gut note → §3 Review 002
**Agent (Opus) does:** [ ] id+filename · [ ] §0 metadata from screenshot · [ ] takes labeled · [ ] **§3 Review 003 formal deep extraction** (formalize Review 001) · [ ] update EVRUN concept registry (cross-source) · [ ] update coverage matrix · [ ] **fill §4 pointers (closeout)** · [ ] NO sidecar extraction file (`GRD-044`)

## §1 — Verbatim transcript  ·  layer: `raw_source`  ·  IMMUTABLE

⬇️⬇️⬇️  PASTE THE FULL TRANSCRIPT BELOW  (do not summarize)  ⬇️⬇️⬇️

Transcript


Search in video
0:00
0:05
All right. Let's get started. So last time, Tatsu talked about scaling laws,
0:11
and we're going to take a little break from that and talk about an inference.
0:17
So the problem of inference is very simple. You've trained a model, and you're given a prompt,
0:24
and you want to produce the response usually as accurately and as quickly as you can.
0:31
So inference turns out to be only one lecture, but it's actually of growing importance.
0:39
And it shows up in many places. So clearly, once you've trained a model, you don't just sit there.
0:45
And if you're a researcher, you put a plot in your paper of here's how my model works, but for everyone else, you actually want to use this model.
0:54
So what does use look like? It could be chatting with an AI assistant or chatbot,
0:59
it could be doing code completion. These days, agents are very popular,
1:04
and that requires inference. Batch data processing as well.
1:12
It's also used for evaluation. For evaluation that requires generation in particular.
1:19
And it's also used inside training. If you're doing reinforcement learning, you need to have a model, you generate rollouts,
1:25
and you score them, and you update the weights appropriately. So inference shows up all over the place.
1:32
And efficiency, as is the theme of this class,
1:38
really matters more than ever here. And even if you just look at the actual use app of vantage point,
1:48
training is a one-time cost. It could be very expensive. It is very expensive. But once you're done with it, that's it.
1:55
But inference is a repeated cost. You kind of incur them every single day.
2:01
So OpenAI is estimated to produce 8.6 trillion tokens a day.
2:09
And just for reference, DeepSeek-V4, which came out earlier this year, was trained on 32 trillion tokens.
2:16
So in less than four days, the number of tokens that OpenAI has to produce, and therefore the compute
2:22
is at least DeepSeek-V4. And of course, even the frontier models
2:29
might be trained on more tokens, but still, inference, the number of tokens generated is going to be higher.
2:37
So that's just some perspective. And moreover, I think the importance of inference
2:43
has grown in the last year because once upon a time, we thought of language models as primarily
2:50
as chatbots or assistants, where you put in a prompt, you get back a response, and presumably,
2:58
the goal of the human is to read the response. But now, as we move more into a genetic world,
3:04
what it looks like is that a query goes in, and the agent is going to do a bunch of stuff.
3:11
It's going to think, it's going to reason, it's going to call some tools, it's going to introspect. And at the end of the day, it will produce some output
3:18
for a human to read. So most of the tokens that the agent produces is actually not for reading.
3:27
And so you should really think about the number of tokens generated as really the compute spend.
3:32
And there's no limit. If you have an ambitious enough problem, you're going to need much compute and a lot of tokens.
3:41
So if you were in the chatbot world, maybe inference after a certain point,
3:47
it's fast enough, because humans can only read so fast. But for an agent, there's no limit to how much a value
3:56
you can get out of squeezing more out of inference. So there's a lot of people doing inference
4:03
on the commercial side. Of course, all the closed API providers have to serve their models, so inference
4:11
is a big deal for them. And there is also a bunch of providers serving open weight models and providing inference as well.
4:20
And in the open source community, there's a bunch of packages. So VLM is probably a popular one.
4:25
Kind of a go-to. SG Lang is another one that's particularly
4:31
good for agentic workloads, but maybe not as popular yet. And then there's TensorRT from NVIDIA, which is really fast,
4:39
but it's more narrow. And then Llama CPP, if you want to run inference on CPU,
4:47
this is a popular package. So hopefully, I've argued that inference is huge.
4:55
If you can make inference twice as fast or even 10% faster, that is a big deal.
5:02
So now, the question is, what does fast mean? So there's a few metrics that capture the notion of fast.
5:08
And they're going to be applicable in different settings and have different trade offs.
5:13
So one metric of fast is time to first token or TTFT.
5:19
This is essentially how long a user waits before any generation happens. So you put in a query into ChatGPT,
5:28
and some number of milliseconds passes by, and then
5:36
from the point the first token comes in, that is the time. And this is generally useful for interactive applications
5:43
because that latency where you're just waiting and doing nothing is--
5:48
the longer that is, the worse the user experience, and as soon as tokens come in, it doesn't maybe
5:54
have to be that fast, because if you're going to read it anyway, you can't read that fast.
6:00
So the second metric is latency. And this is from the standpoint of an individual user.
6:06
How fast are tokens appearing for one query? This is also important for interactive applications.
6:14
So basically, this is how fast the tokens are kind of streaming through.
6:19
And then the second related concept is throughput, measuring tokens per second,
6:24
this is how fast tokens are appearing for many queries. So latency or one latency and throughput
6:31
are clearly very related. It's very kind of aligned-- in general,
6:38
many interventions will make latency and throughput better. But as we'll see later, there's actually a trade off here.
6:46
And throughput is useful for if you're doing a bunch of batch processing.
6:52
You have a petabyte of data, and you want to process it with a language model. You just want that job to get done.
6:59
It doesn't matter if one query is coming in faster, sooner, or later.
7:04
OK. So what determines the efficiency by either any of these metrics of inference.
7:14
So the high level bit is as follows. And this is-- maybe one high level bit
7:21
to remember from the lecture is that when you're doing training, you see all the tokens at once, because
7:28
in supervised fine-tuning, you see all the tokens, and you can parallelize over the sequence.
7:33
Remember, think about in the transformer, the calculation for attention, MLP,
7:38
the sequence is just a dimension. So it's a big tensor that gets multiplied. And so you, essentially, process all the tokens at once.
7:47
In inference, you can't do that. You have to generate because of the autoregressive nature
7:52
of inference. Tokens sequentially. One at a time. So this is the fundamental problem
7:59
of why inference is a very different workload than training, because you can't parallelize across the sequence
8:05
dimension. And therefore, as we'll see later, it's going to be harder to have high arithmetic intensity
8:12
or fully utilize the compute. OK.
8:18
All right. So let's go on. So first, I'm going to-- just maybe a preview of what's
8:24
in this lecture. First, I'm going to do some math to understand the arithmetic
8:31
intensity and throughput and latency, how to think about that for a transformer.
8:37
And then I'm going to talk about various techniques to reduce the cost by making the kv cache smaller,
8:46
quantizing model pruning, and then finally, I'll talk about speculative decoding, and then some kind of practical concerns.
8:55
All right. So this is going to be a bit of a review, mostly kind of swapping in, making sure we're
9:00
on the same page with respect to notation. Much of this lecture is based on the scaling book from Google
9:06
on transformers and inference. So I do recommend that people go check it out.
9:11
It's a very nicely written. And some of the figures are generously
9:20
lifted from that book. OK. So just to establish a bit of notation
9:27
to understand this diagram. So we're going to use symbols.
9:33
Think about it in ienops to denote dimensions, but also they're kind of length.
9:38
So B is going to be the batch dimension and also the number of sequences. T is going to be the sequence dimension, which
9:44
is also the number of tokens. D is the model dimension, H is the dimension. So if I'm going to write this, this
9:53
is taking a product between a tensor with dimensions B, T, and D, and another matrix of D and H, and I produce BTH.
10:03
So what is going on here is that there are some dimensions marked red, which are going to be contracting.
10:10
These dimensions appear in both the operands and disappear from the results.
10:16
And then there's going to be other regular dimensions in black that appear only in one operand and stay in the result.
10:25
So this is the general form. Another case is if you have blue dimensions, that
10:31
means they're attaching dimensions, and they appear in both, but stay in the result. They do not get contracted or reduced.
10:40
OK. So the interpretation here, as we'll see later, is that we have this tensor for every sequence
10:46
and token position. You have a vector and you want to multiply that by a matrix.
10:53
And then here, you're basically taking a bunch of dot products between two tensors.
10:58
OK. So with that in mind, this in all its full glory, is the description of a transformer block.
11:08
So it looks a little bit like a circuit diagram. There's a lot of details here, but I actually find this probably the most crisp definition
11:17
of what a transformer is. You see a lot of description of transformer, and frankly, a lot of them are-- it's
11:24
hard to understand what is exactly happening. This one basically tells you exactly the shapes of the tensors and allows you to reason about the dependency.
11:33
So for transformer block, you take the X, which is the activations of one layer,
11:39
you feed it through a tension, and then you feed it through MLP. And just as a kind of a quick refresher,
11:50
you projected using a query matrix, a key matrix and a value
11:55
matrix, you'll see here that we have
12:02
N, which is the number of heads, and H is the dimension of each head. So the query matrix is batch times sequence times number
12:10
of heads times the head dimension, and K and V, instead of having N,
12:15
we have K, which we'll see later, which we have a potentially smaller number key value heads.
12:23
And then this is the attention operation, where notice that there's batching dimensions.
12:29
So B appears in both. And we're contracting over the head dimension.
12:36
And we'll come back to why this B, B in both, makes inference hard.
12:43
Why attention is kind of a bottleneck there. So just hold on to that thought.
12:49
And the MLP is very simple and straightforward.
12:55
You just do some math models. This is the gating matrix.
13:03
This is the upper direction and a down projection. So thank you guys. I have implemented this. So I don't want to belabor it, but just
13:09
to swap in the notation. By convention, we're going to assume that in the MLP,
13:18
the MLP up projects the D dimensional, model dimension into four times that.
13:23
So F is always-- think about it as 4D whenever you see F. And then the model dimension gets split across the N heads.
13:32
So the model dimension is always equal to number of heads times the head dimension.
13:38
And then the number of heads get split,
13:43
in the case of group query attention, into the number of groups and the number of heads per group.
13:51
And finally, we have two variables, S and T, which both represents the sequence dimension.
13:59
And the difference is that S is going to represent the number of input tokens,
14:05
and T is going to represent the number of output tokens. So in training time, these two are the same,
14:10
because we're predicting all the same inputs as outputs. And inference time, T is going to be 1,
14:18
and S is going to be the input. OK.
14:23
So another review for arithmetic intensity. So I talked about this in the second lecture. So just as a warm up.
14:29
Suppose I'm multiplying two matrices; a B by D matrix and a D by F matrix.
14:34
So intuition. B is the batch dimension, D is the hidden dimension or the model dimension, and F is the projection dimension
14:41
in the MLP. So remember, what are the arithmetic intensity?
14:47
I have to count flops, and I have to count the amount of memory moved.
14:52
So we can do this as follows. So what do you have to do to multiply these matrices?
15:01
Now, remember, the systems, lectures, you have to read X from HBM.
15:08
And if you're storing everything in bf16, which is the case for inference, we're
15:13
going to have 2 times B times D, you're going to read W. That's going to be another 2 times D times F.
15:21
You're going to do the matmul. So that's number of flops is 2 times B times D times F
15:29
And then you're going to write it back. OK? So every time you read and write,
15:36
it's basically the number of entries. It's kind of a quadratic-like term, and matmuls are qubit.
15:42
And remember, this is going to be important because that's how you're going to get high arithmetic intensity.
15:48
So total number of flops is the cubic, and then the bytes transferred is the sum of the reads
15:57
and writes. So remember, arithmetic intensity is how much compute
16:03
we do per byte transfer. And we want this to be high. So the intensity is going to be equal to flops
16:13
over bytes transfer. And I'm representing all these things symbolically, because it will be easier to see and work
16:20
with rather than numbers. One just simplification we can make
16:27
is that suppose that the batch dimension here is much less than D and F, then we can simplify this.
16:34
So in particular, what this is doing is saying D equals C times D and F equals C times B and let C goes to infinity,
16:42
so both D and F are really large and B is smaller, and this just reduces to the intensity of B.
16:51
So the upshot of that is that remember when we did arithmetic intensity for matmul,
16:59
it was something like N/3. And this is the kind of analog, where
17:07
instead of just having square matrices,
17:12
we have non-square matrices. OK. So just to compare, so the accelerator intensity
17:18
of a hardware is you look at the flops per second
17:24
in the spec sheet, you look at how much memory bandwidth, how fast memory gets moved between HBM, you divide,
17:31
and that is the accelerated intensity. And now, you compare your computations intensity
17:40
with your accelerator intensity. And if it's greater then your compute bound, which is good,
17:47
if you're less, then you're memory bound, and that's bad. So in this case, for H100, for this particular matmul
17:54
operation, you're compute bound if the batch size is greater than 295.
18:03
So here's an extreme case. So suppose you just have one example.
18:11
What happens is you have one example, then your arithmetic intensity is going to be one.
18:16
And this is going to be memory bound. And you can see what's going on here.
18:23
You're reading this DF matrix, but B is only one. So you're effectively doing only 2 times D times F flops.
18:31
And this is basically the workload
18:37
that you'll see in inference. You don't get these full matrices, you get these very thin matrices or tensors.
18:45
OK. Let me stop there in case there's any questions. Yeah.
18:51
In the transformer slide, I just want to confirm [INAUDIBLE].
18:56
19:03
You said, across [INAUDIBLE] can search across [INAUDIBLE]?
19:09
Yeah, should that be across tables instead of cubes?
19:17
Let's see. So the number of groups here is--
19:30
wait, I'm just making sure. So K is the number key value heads.
19:36
And so you have g per KV head.
19:45
My understanding is [INAUDIBLE] each key path will have one
19:54
[INAUDIBLE] correspond to it. And [INAUDIBLE] then g is the number of [INAUDIBLE] p.
20:06
That means we have k. That's my question. Yeah, OK.
20:11
Yeah, I think you're right. So k should be the number of groups, and g should be the number of the heads per one
20:20
of those groups. Yes. Yeah, OK. Thanks for that. I'll fix that later.
20:27
OK. So let's talk a little bit more about the arithmetic intensity of inference.
20:34
So here's how to think about what's happening in inference.
20:41
Let's say you just do it naively. So here is a prompt never going to give you.
20:48
I'm going to the transformer. And the transformer does generates keys and values
20:55
and activations. And at the end of the day, it generates logits over the output vocabulary.
21:02
And you sample a token, and that token gets concatenated with the prompt,
21:09
and then you just do this again. You generate never. You attach that to the prompt and so on and so forth.
21:16
So this is the most naive thing. If you have a black box that takes a sequence
21:21
and outputs a distribution over tokens, which is exactly what a transformer does, you can just apply this repeatedly.
21:28
OK, so this works, but this is really bad because each time you
21:36
generate one token, you actually take order T squared time where T is the number of tokens
21:43
that you've generated so far. So generate T tokens is actually T cubed because the attention is
21:52
already T squared, and you have to do that once for every token.
22:00
So this is pretty bad, but the observation is that you don't actually have to do this.
22:09
A lot of the work can actually be shared across prefixes. So for example, if you're over generating network versus you're
22:15
generating up, you're actually computing a lot of the same key values for never going to give.
22:24
So these tokens, they shouldn't change, OK? And this is because it's a causal transformer.
22:30
If it was bidirectional, then if you attach a token, then everything changes. But if it's causal, then the activations
22:36
here don't change based on any tokens you append.
22:41
So with that observation, the first obvious thing is to store a KV cache in HPM, so
22:50
that, between successive token generations, you can just reuse the KV cache. So that means when you are trying to generate gonna, you
23:02
don't actually have to compute all the keys and activations of these previous tokens.
23:10
So this is what it looks like with a KV cache. So there's going to be two stages pre-fill.
23:16
So you get the prompt, and you populate the KV cache, which is the set key and value pairs that the transformer computes,
23:25
and then you generate the logit-- just the same computation as we did before. And now you have the KV cache here and then this distribution
23:36
which you sample at a token. And now you feed that through the transformer, which uses this cache and then produces
23:47
both the distribution of the next tokens, but a new KB, which is basically activations
23:54
corresponding to the token up. And then this gets fed in.
23:59
So now you have the command to KV cache. You have this logits you sample, and then that
24:05
feeds to the transformer. You output the distribution over next tokens as well as the new token which you add to the KB cache
24:15
and so on and so forth. So the KV cache formula is for every sequence--
24:24
so there's B of them. For every token, there's S of them. And for every layer and every head,
24:32
you store H dimensional vector.
24:38
And just to reiterate for inference, there's prefill. You given a prompt. You encode the prompt into these vectors.
24:48
And this is parallelizable just like in training because you see the entire prompt. You can compute the KV cache.
24:55
And then in generation, you generate the new response tokens sequentially.
25:00
But at least you don't have to pay for generating the KV cache of the tokens that you already looked at.
25:08
So is everyone comfortable with the KV cache? Any questions about this?
25:22
All right, so let's try to figure out the FLOPs and memory
25:27
I/O for both MLP and attention layers. So remember S is the number of tokens
25:33
we're kind of conditioning on, and T is the number of tokens we're generating.
25:39
So we're going to do this abstractly. And then later, we'll specify to the prefill where T equals S,
25:44
and generation T equals 1. So for the MLP layers-- and through all of this,
25:51
I'm just going to look at the MAmmoTH because the MAmmoTH are the thing that actually require a lot of work.
25:57
Everything else can is not that many FLOPs and can be potentially fused into the MAmmoTH too.
26:06
OK. So let's do this calculation. So it's the same as the matrix calculations.
26:12
I'm not going to maybe belabored step through every single detail here, but just the form is you read X
26:19
from high bandwidth memory. You read all the parameters.
26:27
You compute the upper projection,
26:33
and then you write it to HBM. You compute the gate, and you write HBM,
26:42
and then you compute basically the down projection of that,
26:51
and you write it to HBM. So the number of flops depends on B and T
26:59
and D and F. So batch size, sequence length, model dimension, and the feedforward dimension,
27:07
MLP dimension. And the bytes transferred is this expression.
27:15
So now you can compute the arithmetic intensity which, again, is FLOPs divided by bytes transferred.
27:22
So this is some expression. We're going to assume, that just like before, B times T
27:27
is much smaller than D and F. And with that, then we see
27:33
that intensity is B times T.
27:38
So this is analogous to just the MAmmoTH case. There's more matrices and there's more dimensions,
27:44
but fundamentally it's the same thing. And that makes sense. An MLP is basically a big MAmmoTH.
27:50
And it also makes sense because the batch dimension
27:55
and the sequence length dimension are-- everything is independent.
28:01
And for the MLP, they don't interact. Attention is different story. And so B times T. So as long as you have--
28:12
in the pre-fill, if you make B times T large enough-- large batches, long sequences-- you'll be fine.
28:19
Now, let's look at generation. So remember, generation, there's two problems here.
28:24
One is that generation T equals 1. You're only generating one token at a time.
28:33
And so that means your arithmetic intensity is going to be B, but B in generation
28:40
is the number of concurrent requests. And so if you're in a batch setting, you can control that.
28:46
But if you're, let's say, serving a chatbot, it's the number of concurrent requests is essentially how many users, concurrent users there are?
28:55
Which can be high. It be low. So it's a bit unpredictable. It can be changing over time. So that's something we're going to address when we
29:01
talk about continuous batching. But overall, this is not bad because as long
29:10
as you have large batches, the sequence length isn't going to really help you. But if you have large batches, you should be good.
29:18
So now let's look at the attention layer. So again, S is in the previous tokens already generated.
29:25
T is the number of tokens you want to generate logits for. So in attention, you read the QKV matrices from HPM.
29:40
You compute the attention. You compute the softmax which doesn't really matter,
29:47
and compute the value matrix, and then you write the result to HBM.
29:54
So the FLOPs is B times S times T times D, and the bytes transferred is this expression.
30:04
Everything's is a MAmmoTH, right? So this should be always one degree higher of a polynomial
30:13
than the bytes transferred because it's a MAmmoTH. The only question is, what is that factor look like?
30:21
So that factor for attention is S times T over S plus T.
30:29
So let's look at the prefill. So prefill, what this is saying is
30:37
that the prefill intensity is S over 2 when T equals S. So this is good, right?
30:44
Because as long as you have long sequence lengths for attention, you're going to maintain high arithmetic intensity.
30:54
Notice that the batching dimension doesn't happen here. I'll explain a bit why later.
31:02
But for generation, this is bad news.
31:07
So the generated nation intensity is S over S plus 1, and that's less than 1, or even let's just call it 1.
31:16
And aromatic intensity, remember 1 is bad. We want it to be something like 295 for H100
31:22
to be to saturate the compute. And so this is really the bottleneck.
31:29
So we did all this analysis for MLP attention,
31:35
prefilled generation, and we've found this to be a bottleneck. So let's try to contemplate why this is a bottleneck.
31:46
So unlike MLPs-- so MLP is for generation is actually OK as long as your sequence is--
31:52
sorry, your batch is large enough.
31:59
So the problem with this ML attention is that--
32:07
let's look at MLPs. Every sequence hits the same MLP weights.
32:14
So these don't depend on B. Whereas, in attention layer,
32:19
each sequence has its own KV cache. So these all depend on MLP.
32:27
And so you can think about this as what's going on here
32:36
is that in the MLP case, having B being big actually
32:43
is helpful because you get to load these MLP weights once.
32:48
And you can use it for all your sequences. That's how you get high arithmetic intensity
32:54
because you use it-- simplifying a bit, you load it once. You do all your batch processing,
32:59
and then that is good. Whereas, in attention, all these depend on B. So increasing B
33:09
doesn't help. It's like for every sequence, you're basically doing a MAmmoTH.
33:15
So they're all independent. So doing more MAmmoTHs isn't helpful.
33:21
Just if you remember in the very beginning, I showed you this example.
33:27
This also has pretty bad arithmetic intensity. It's not a MAmmoTH because we're essentially
33:33
batching by a coordinate. And this is essentially basically the same
33:39
as doing a dot product, which has horrible arithmetic intensity. And remember, this is in the attention.
33:46
This blue B is the cause of why the attention is
33:52
arithmetic intensity doesn't scale with B, and why that is a bottleneck.
33:58
So just to summarize here. So prefill is compute-bound. Generation is memory-bound.
34:04
So if you look at the MLP intensity for prefill,
34:10
it's B times S, great. Prefill for attention, intensity S over 2--
34:18
not as good, but workable. Generation MLP intensity, also workable,
34:24
requires long concurrent requests. But it is really the generation attention
34:29
intensity which is a fundamental bottleneck, and that's it.
34:36
If you're sticking with a transformer, you can't really improve this.
34:43
OK. Pause there for questions.
34:57
So now whenever you hear people say, oh, inference is memory bound, you know why.
35:03
OK. So let's now use these intuitions and calculations
35:13
to think about our inference metrics throughput and latency and also TFRT.
35:21
All right. So inference is memory bound.
35:27
Now the main-- I mean, in some ways this simplifies a lot of things because when we think about how long things take,
35:34
you just look at how much memory needs to be transferred, OK? Because assuming you overlap communication and computation,
35:44
the bottleneck is going to be just the amount of memory that you have to deal with.
35:49
So in some ways, it's nice because it's simpler. But in other ways, it's frustrating
35:54
that your accelerators are sitting there not doing anything.
36:00
So let's walk through an example. So for Llama 2.13B on an H100.
36:09
So what is the latency and the throughput? So remember Llama 2.13B has a particular shape.
36:16
So this is a sequence length, the model dimension, feedforward dimension, number of query, number key value heads.
36:25
There's no GQA here. So the N equals k, head dimension, number of layers of vocab size, and the memory bandwidth
36:34
for H100.
36:41
So now, using this config, let's compute the transformer performance statistics, OK?
36:50
So these are our inputs here. And just the statistics I'm going to compute
36:58
are going to be number of parameters, the memory, usage
37:05
latency, and throughput. So first of all, what takes memory?
37:16
So the parameters take memory. So we compute the number of parameters.
37:23
And so you look at the embeddings, the MLP layers,
37:29
the projections of the KQV. At the end of the day, you get some number of parameters.
37:39
Assuming that we're doing a bf16, which is always the case for inference, then parameters
37:46
take up this many bytes. Also in memory is the KV cache.
37:53
And the KV cache is the number of tokens in your sequence
37:58
times the number of heads and the KV heads times the head
38:07
dimension times the number of layers. You have one for the key and one for the value,
38:12
and then you have multiple two for bf16. So that's the size of the KV cache.
38:18
And the total memory usage is-- this is for each sequence you have B sequences.
38:24
So that's B times that plus the parameter size. And that's the amount of memory you need.
38:32
And now, what's the latency? The latency is determined by the memory I/O, OK?
38:41
Because inference is memory-bound, most assuming you overlap communication and compute.
38:49
All the memory, it's going to be on shuffling parameters back
38:56
and forth between HPM and SRAM.
39:01
So that's how long it takes to move memory around.
39:06
Throughput is the inverse of latency, but we're generating B tokens in parallel.
39:12
So this is tokens per second. This is seconds per token.
39:17
This is tokens per second, as well as having an additional B because you're processing a batch of B.
39:27
So now, let's compute for this config what are these actual values?
39:33
So num parameters is 13 billion.
39:39
So a good sanity check-- that is advertised as a 13 billion parameter model. Memory is this term, which is about 838 million times B.
39:53
So it's basically a linear function of B plus some other term.
39:58
So this is a KV cache which grows as B. This is the parameters, which is double the number of params.
40:06
Now, latency is just this scale by the memory bandwidth, so it has the same form as the memory.
40:15
And throughput is B over that.
40:20
So notice that latency, as you increase B, grows because the KV cache grows.
40:28
And in order to process stuff, you have to copy the KV cache back and forth. And now, throughput is more interesting
40:35
because, as you increase B, throughput does improve but up to a limit.
40:45
Throughput improves because now you're amortizing the cost over a larger batch.
40:50
But also, the speed at which you're processing this obviously is also increasing over time.
40:58
So this asymptotes. It's not going to-- throughput can't possibly go to infinity.
41:03
Any questions about this so far?
41:10
So basically, you just have to remember
41:16
latency is a linear function in B. This is where the constant is the size of KV cache, and then this
41:22
is the number of parameters. And then throughput is some proportional to B over B
41:30
plus something. So now, let's instantiate this for a bunch of situations, OK?
41:40
So if you have batch size 1, so this is what you would get.
41:46
You get a latency of 0.008 seconds per token.
41:55
And the throughput is 124 tokens per second. So what happens if you increase the batch size,
42:02
you'll see that the latency goes up,
42:07
but the throughput also goes up. So latency gets worse, but the throughput improves.
42:13
So this is an interesting thing because we think about, oh, we just want to make it go fast.
42:18
But fast actually has two meanings here, which actually, depending on which one you care about, is completely opposite.
42:24
If you want to tune your batch size, that's going to really determine if you
42:30
want latency or throughput. And what happens if you increase-- let's say, increase--
42:36
we really want high throughput because we're processing lots of documents. Let's increase the batch size even more, OK?
42:42
So the latency gets even worse. I mean, yeah, it gets worse. Throughput gets even better.
42:49
But the main problem here is that your memory, you run out of memory because the memory, for storing this KV cache,
42:59
is going to exceed your H100 memory. And if you have B200s, you can increase the batch size more,
43:06
but eventually you hit some of limit. So there's limitations to how much
43:11
you can increase your throughput. You'll never get to the asymptote because you'll hit the memory.
43:20
And also, your throughput is getting-- your gains are kind of diminishing as well.
43:27
So increasing batch size worsens the latency
43:34
because now you have a larger KV cache to read and write.
43:40
And remember, it's batched. So if you're an individual query, you have to wait for everyone to finish.
43:45
So you have to basically-- you're waiting for a bus, and the latency is pretty high.
43:51
You wait and then you go. Whereas, the throughput of a bus is pretty good
43:56
because you can move everyone at once. So the throughput improves as batch size increases
44:04
because, remember, the parameters are shared, and you load that once into memory.
44:12
And you can process a lot of sequences.
44:17
So trade-off between latency and throughput, just to make sure everyone's aligned-- smaller batch sizes
44:23
use better latency but worse throughput. Large batch sizes yield better throughput but worse latency.
44:29
OK. I'm not going to really talk about parallelism too much. There's also another dimension of inference,
44:35
which is you can shard your model across multiple all devices.
44:43
You can look at the scaling book chapter on inference if you want to know more. Just as a very trivial example, if you
44:50
launch M copies of the model, the latency is the same, and the throughput increases by M.
44:58
And then the other metric we didn't talk about is time to first token. And this is essentially the time it
45:04
takes to do prefill because after you finish prefill, then you can basically start generating.
45:11
So if you want to fast--
45:20
if you want faster TTFT, you should use smaller batch sizes,
45:26
and you want larger batch sizes to improve throughput. So hopefully, that is clear.
45:34
Any questions about throughput and latency and how their intention with each other?
45:47
OK. So now that we have a conceptual framework for thinking about how efficient inference is in terms of arithmetic intensity
45:57
and throughput and latency, now let's try to make it faster.
46:03
How do we make inference faster? There's a bunch of different techniques which are quite varied, ranging from changing the model architecture
46:11
to doing systems optimization and everything in between. So inference in some sense is a fairly rich cross-cutting topic.
46:20
So the most maybe now in hindsight obvious thing
46:27
you can think about is hopefully beat it into your head that memory is the bottleneck for inference,
46:35
and the KV cache takes up a lot of memory. And it could even be larger than the number of parameters
46:42
if for a large enough batch size. So let's just try to reduce the size of the KV cache.
46:48
Now, you have to be careful about how you do this because you want to make sure you don't lose too much accuracy in the process.
46:56
So here is one thing you can do, which we already talked about, which is grouped query attention.
47:03
And just as a reminder here, so multi-headed attention--
47:09
basically for every token, you have a key and a value and a query. And if you do grouped query attention,
47:16
you basically compute the same number of queries, but you have only a smaller number of groups,
47:24
and you compute key and value for each group. So K is the number of groups here.
47:36
And so in the multi-headed attention, K is N, no reduction. There's something called multi query attention, which no one
47:44
uses because it's really bad. K equals 1. And somewhere in between is hopefully
47:50
where we'll find a balance between accuracy and speed. So this is the paper that introduces GQA from 2023.
47:59
And they show that if you look at time per sample, which
48:05
is related to both latency and throughput, you see that the MHA-- so Multi-Head Attention.
48:15
Full attention has this high time. Whereas, if you start K equals 1, it's much faster.
48:24
And then you can actually keep on increasing the K to K equals 8, and it's still pretty good.
48:30
And eventually, your time goes up quite a bit.
48:36
OK. So why does GQA improve latency and throughput?
48:43
Well, it reduces the KV cache by a factor of N over K. And just
48:53
as a friendly reminder, reducing memory usage leads to speed up because we're memory-bound.
49:00
So let's revisit our friendly Llama model here.
49:06
Remember in the initial configuration, we're just using K equals N. So there's no multi-headed-- there's multi-head attention,
49:15
no reduction in the number key and values. And so for this one, remember using a batch size of 64,
49:25
we get this throughput and latency. Now if you do GQA, we're going to put in, let's say,
49:32
a kind of a sparsity of 1 one five. That reduces the memory quite a bit,
49:44
which, in turn, improves the latency and also improves the throughput.
49:49
So it's not that latency and throughput are always at odds. If you reduce the amount of memory, then it improves both.
49:56
It's mainly the batch dimension that allows. That is the point of tension.
50:01
OK. So this is great. And let's actually just increase the batch size even more
50:13
and see what happens. So before, if we had a batch size of 256,
50:20
we ran out of memory. But now, it fits in memory. And we see that, the latency suffers a bit because we're
50:32
increasing the batch size, but the throughput goes up proportionally.
50:40
So sometimes you play with these parameters jointly. You can reduce the KV cache, but that
50:47
allows you to increase the batch size and allow making other trade-offs.
50:52
So the final thing you have to do whenever you do some lossy change is that you make
50:58
sure the accuracy doesn't drop. And this paper shows that for a GQA, the time is better.
51:08
But across a bunch of evals, basically it works well.
51:14
So now with these accuracy values, I think you always have to take it with a grain of salt because this is for a particular model.
51:20
Later, the deep sea paper should show that it actually does hurt. So I guess, take everything that's
51:28
not just math with a grain of salt here. OK. So speaking of DeepSeek, here's another idea
51:36
to reduce the KV cache. So the theme is reduce the KV cache and latency and throughput
51:43
improve. So this is multi-headed attention,
51:49
same number of queries and keys and values for every token.
51:54
And GQA, remember, we have reduced the number of keys and values for every token.
52:01
Sorry, not for every token. We basically have reduced the number of values and keys.
52:07
And now, the multi-latent attention for DeepSeek says we're actually going to leave the number of keys
52:15
and values the same one for every token essentially.
52:21
But I'm going to parameterize-- I'm going to compress these. So normally, you have some--
52:27
how do you compute your keys and values? You have your activations, and you multiply them
52:33
by some matrix to get K and some other matrix to get V. And these are generally N times H dimensions, like your model
52:40
dim, which is big. So MLA says I'm going to actually project
52:46
these activations down into C dimensions. So DeepSeek-V2 reduced it from 16,000 to 512,
52:55
so this is quite aggressive compression here. And then I'm going to compute the k and the V
53:00
from this compressed representation. So now, I can just store C. This is much smaller.
53:07
And then when I need my keys and values, I can just materialize them.
53:14
So there is one wrinkle here, which is that MLA is not compatible with rope, which operates directly on the keys and values.
53:22
So what they do is add additional dimensions for handling the rope.
53:30
But more or less, it's still a pretty big reduction.
53:36
So the latency and throughput improvements follow by just simple math.
53:42
The smaller the KV cache, the faster you go.
53:47
It's almost kind of linear scaling up until at some point. And then, remember, you need to check
53:54
whether your model is accurate. So first of all, this is the result
54:00
that kind of contradicts or is in tension with the GQA paper.
54:07
They show that GQA actually isn't that great.
54:17
So this is MHA. This is GQA. These numbers are smaller than these numbers.
54:23
But they show that their method, MLA, Multi-Latent Attention
54:29
works even a little bit better than MHA. But let's just say it's about the same.
54:35
So this column and this column are much better than-- I guess there's no GQA on this table.
54:41
You have to compare over here.
54:47
So I'll show you an-- yeah. How does this compare to reducing the number
54:54
of dimensions, the model? So the question is, how does this compare with reducing the dimension of the model?
55:02
So that's a good question. These ablations don't show that. My guess is that reducing the model dimension
55:08
just makes things worse because you're indiscriminately just reducing everything.
55:15
I think the trick in all of this kind of is to find places in the model where you can squeeze.
55:23
And this apiary, I don't think you can necessarily know for sure. You just have to do a bunch of experimentation
55:29
and see what works. So here's another idea to reduce your KV cache.
55:34
This is called cross-layer attention. So the idea is that normally, every layer has KV, K's and V's.
55:43
But let's say instead of doing that, we're just going to compute KVs for a subset of the layers.
55:53
And then, just for this layer, I'm just going to use the previous layers on KV cache.
55:59
56:05
So this is another way of sharing. Just like GQA shares K across heads,
56:11
now I'm sharing KVs across layers. And empirically, this paper shows that doing this
56:19
improves the Pareto frontier. So each of these models is better than--
56:28
given a method you can always sweep this the size of the KV
56:33
cache by changing the K and the head dimension, which, I guess,
56:41
kind of relates to your point about changing model dimension. But if you do this CLA cross layer attention, it's better.
56:53
So moving on, whirlwind tour of different techniques
56:59
for reducing KV cache. There's a local or sliding window attention.
57:04
This is a fairly kind of old idea and a very kind of natural idea. So if you look at the full attention matrix,
57:12
it's N squared. And instead of doing that, if you're going to generate a token, you just look at the last K tokens.
57:22
So you essentially have a sliding window. For every token you generate, you just depend on the last K.
57:29
And now, if you do that, the effective--
57:34
so the KV cache is now independent of the sequence length. It's just the number of the batch times the other variables,
57:46
which is great. And this is especially great for long context.
57:51
Now, because of the number of layers, the effective context length is actually larger than the number of the stated context length
57:59
because information can propagate farther in if you go down the layers.
58:05
Now, you can do fancier things. You can maybe not do a dense selection of layers,
58:11
but you can space it out. You can also do this global plus sliding window where aware you have attention to a fixed
58:20
grid of different token points, plus a local sliding window.
58:25
So you can do various things. Now, the problem with this is that it actually still hurts accuracy.
58:32
So this reduces expressivity. There's no free lunch here, or at least this
58:38
was an expensive lunch. So the solution that people come up with is that they interleave local attention with global attention.
58:47
So these hybrid models have full attention for some of the layers
58:52
and some local layers for some of the other layers. And you're basically always trying
58:59
to-- and then so that allows you to essentially reduce the KV cache a little bit.
59:05
And you're always trying to balance accuracy with the speed.
59:12
Yeah. Hybrid model-wise, is there a difference between the trade-off in using a linear time invariant
59:18
versus a sliding window? Yeah. So the question is, what about linear attention versus sliding
59:23
window attention? So I'm not going to talk about linear attention. But very quickly, there's a bunch of methods that where,
59:33
instead of storing KV cache, since you basically
59:40
compute some of compressed representation of all the history.
59:45
So linear, the most naive linear attention is you just sum the KV values up into a single vector.
59:53
So that's definitely independent of the sequence length. You can do fancier things. There's gated net, delta nets, and Mamba
59:58
which allows you to try to compress but not forget as much.
1:00:04
Now the question is, how do those compare? Those have also been used in place of sliding window attention, and people have gotten good results
1:00:14
with them. You can also use a combination of full attention, sliding window intention, and the linear attention
1:00:20
because they capture different aspects. If you care about local high-resolution stuff,
1:00:28
then sliding attentions better. If you just want broad summaries of the past,
1:00:34
then the other linear attention might be better. So then for a long context sentence,
1:00:40
would you say your attention would be a better setting for that?
1:00:45
So the question is, for long context with linear attention be better?
1:00:54
So there's no free lunch, right? Let's say you have a very long context,
1:00:59
and you're solving a needle in a haystack problem. If you have to compress your entire history
1:01:07
into a small context, you're just going to lose information, and you might just not be able to retrieve it.
1:01:17
I guess I feel like what it seems like people go with hybrid architectures that you'll always need some of longer attention, but I'm just
1:01:24
trying to understand in my head, what the trade-off between using a sliding window versus some of a Mamba, delta net layer
1:01:30
would be. Is using a delta net layer better than using sliding window consistently?
1:01:36
Or what is the actual maybe representative trade-offs? Yeah.
1:01:41
Maybe I'll say that the Mamba and delta net are more powerful than the sliding window.
1:01:46
Attention maybe you can think about the Mamba as probably can-- it certainly can represent some of the aspects of sliding window
1:01:54
attention because you can just-- as you're doing the recurrence, it can just look at the last state.
1:01:59
So yeah, maybe you can think about the linear attention
1:02:06
or its extensions as being better. They have more room.
1:02:12
Once you do sliding window attention, you're done. There's nothing else you can-- [INAUDIBLE] Yeah.
1:02:17
OK. So let me just quickly go through this. Just highlight this DeepSeek.
1:02:23
DeepSeek continues to innovate different types of attention
1:02:29
mechanisms. So remember, they came up with the multi-latent attention, which compresses the key values.
1:02:36
There is this thing called-- now they have compressed sparse attention, DeepSeek
1:02:41
sparse attention, and heavily compressed attention. I never remember all these acronyms and what they mean,
1:02:48
but let's look at this diagram. So normally, have your KV tokens and your query token.
1:02:56
So compressed attention is going to basically compress every M tokens into one token.
1:03:04
And then there's this thing called DeepSeek sparse attention, which basically selects
1:03:11
a subset of those to keep. And the way you select a subset is that you actually
1:03:18
compute some LiDAR weight queries and keys, and then you do a smaller attention
1:03:24
to get these index scores so you know to keep. So a lightning fast way to figure out
1:03:30
what tokens you need to keep. And then you use those. And then, there's some more compression that happens.
1:03:40
So in the interest of time, I'll move on. The goal of this section is to reduce the KV cache because KV
1:03:48
cache is related to memory. And we saw that inference is memory-bound. So that directly translates to improvements
1:03:57
in throughput and latency. And the key is to do this without hurting accuracy.
1:04:02
So you can do lower dimensional KV caches across layers
1:04:07
by across heads and across head dimension.
1:04:13
You can do local attention. You can do linear attention, which was discussed earlier,
1:04:19
and there's much more. And there's also diffusion models, which is a non-autoregressive way
1:04:24
to generate which can be much faster. OK. So let's talk about a few other ideas which are important.
1:04:33
So quantization is more of a, I would say, less of a architecture and much more
1:04:38
of a systems of perspective on how to make things smaller.
1:04:44
So the key idea here is just reduce the precision of numbers. Less memory means higher latency and throughput.
1:04:51
And obviously, you have to worry about accuracy. So quantization is-- there's many options here all the way
1:05:05
from bf16 all the way down to int4.
1:05:11
And so one thing you can do, if you're scared that quantization is going to mess you up,
1:05:19
is that you train a model with quantization in mind. So this is quantization aware training.
1:05:24
And during the feedforward pass during training, you quantize and dequantize, and you're basically
1:05:31
simulating these quantization errors as you train. So then, generally, now the weights
1:05:37
are adapted towards quantization. Things will work better. But the con is that it requires expensive large-scale training.
1:05:45
And so what typically people do is they train models, and then they quantize after the fact.
1:05:53
So this is post-training quantization. It's much cheaper often.
1:05:59
There's a naive way to do it is that for every layer or tensor,
1:06:05
you basically determine the scale and the 0 point for each, let's say, tensor.
1:06:11
And then you quantize that separately. This generally doesn't work as well.
1:06:18
You can use this idea called GPTQ, which uses some Hessian information to quantize layer
1:06:27
by layer. And then you keep track of the errors which get propagated into the non-quantized weights.
1:06:33
And so it allows you to correct for the errors. And now, activation-aware quantizing
1:06:40
is more of a sophisticated way where the observation is
1:06:47
that some activation channels are large and those the weights that interact with those matter more.
1:06:54
So let's allocate more precision to these weights. So let's look at this picture.
1:06:59
So normally, if you take fp16 weight matrix,
1:07:05
and you quantize it, you get-- let's say you quantize it down to int3. But what you're going to do is you figure out
1:07:13
which of these are active? So these are maybe activation channels.
1:07:19
And some of these activation channels are large. So if they're large in general, then you
1:07:25
basically allocate like, let's say, fp16 for this channel. You keep everything else as in three.
1:07:32
And for a few important channels, you use higher precision.
1:07:39
So another idea here is to do model pruning.
1:07:45
So here you just take a large model, and you rip out pieces of it, and you fix it up.
1:07:51
It's a very crude way, but it turns out to work. So there's this paper from NVIDIA where, essentially--
1:08:00
let me actually-- let's see. So you first have to estimate the importance
1:08:08
of the different parts of the model and choose the most important parts.
1:08:14
And then you basically remove different hidden units
1:08:19
and different even layers. And now, we have a model.
1:08:26
It's not going to be very good. And so what you do is you post-train it.
1:08:31
You train it some more on the data or the tasks that you care about to heal it.
1:08:37
So this is, in some sense, a training way to reduce the KV
1:08:46
cache, but where you initialize it with parts of a good model.
1:08:52
So this seems to work pretty well. So they were able to take a 15B model and reduce it to an 8B
1:08:59
model. And it doesn't really hurt accuracy by too much.
1:09:06
And the amount that you use to train the model or through this process is much less.
1:09:13
OK. So just to summarize here, the game
1:09:21
is to reduce the inference complexity without hurting accuracy.
1:09:26
You can think about this as mostly reducing the number of parameters or CPU cache.
1:09:31
You can define a faster model architecture to train it, or you can define a faster model architecture.
1:09:39
Initialize the weights from original model, which might have a different architecture,
1:09:44
but you just make this Frankenstein thing, and then you repair the faster model with distillation.
1:09:53
OK. Yeah. Could you say more about you distinguish the important from the unimportant layers?
1:10:00
So how do you distinguish the important layers from the unimportant layers? So in general, you have a calibration set,
1:10:10
and you pass the inputs through the model. And you're basically looking at the magnitude
1:10:18
of the activations. And the ones that are-- some of them, especially if they're dead units,
1:10:23
will be kind of close to 0. And the ones that are large, you want to keep. So that's a high-level idea.
1:10:30
I guess why does it matter if the activation is high? It could just be like-- what if it's just always high, for instance?
1:10:37
Or is that-- So the question is, what if all the activations are high?
1:10:43
So in general, this is a kind of an empirical observation that some of the channels will be much higher than others.
1:10:52
If this weren't true, then these techniques wouldn't necessarily work, but it happens to be true because that's
1:11:00
how these models ended up being trained. And then you can exploit that.
1:11:07
Say a neuron, which is always value 100 across all the samples. Would that mean that it's necessarily meaningful?
1:11:14
Or maybe it's just an artifact of training that I'm just like [INAUDIBLE]?
1:11:20
Yeah, I see. So the question is, what if a neuron is always 100? I mean, if that's the case, you can also look
1:11:27
at variance-related questions. If it's 100, you can't just remove it
1:11:32
because then everything is going to be broken. If it's high mean and low variance, maybe there's another a way to just incorporate the bias
1:11:41
essentially. OK. Let me quickly go through this other idea.
1:11:46
So far, we've looked at lossy methods which basically really crunch down the KV cache, but it could hurt accuracy.
1:11:54
This is a very elegant way of doing this in a lossless way. This is called speculative sampling or speculative
1:12:00
decoding. So remember that if you're doing prefill, you can encode all the tokens in parallel.
1:12:07
This also gives you probabilities. This is fast. This is compute-bound and all nice things.
1:12:13
And in generation, it's one at a time. So checking is faster than generation.
1:12:19
If I give you a sequence, it's fast to tell me how good it is, much faster than it is to generate one at a time.
1:12:26
So you can exploit this asymmetry using the following idea. So what we're going to do is use a cheap draft model
1:12:34
to basically generate from the guest a few tokens, let's say four tokens.
1:12:40
And then we're going to use the model we actually care about, the target model Q, which to basically review these tokens
1:12:47
and accept or not accept them. So things are kind of chosen to be balanced.
1:12:54
The draft model is just smaller and cheaper. And so even though it's memory-bound, and it has to generate one at a time, is not too bad.
1:13:01
Whereas, the target model is big and expensive, but what we're doing is asking it to process a batch of tokens in parallel
1:13:08
so it won't also be too bad. OK, so here's the video that shows how things work.
1:13:17
So if you use a big model token by token, it's going to be pretty slow.
1:13:22
But if you are doing speculative decoding,
1:13:27
then you can see the small model generating a bunch of tokens,
1:13:35
and then the large model basically critiquing them.
1:13:42
And then, so you basically can get this burst of tokens and then maybe another burst of tokens
1:13:47
and so on and so forth. OK. So here is the algorithm for-- there's
1:13:56
a few papers that came out with speculative decoding around the same time. This is one of them.
1:14:04
And so the idea here is that in order to generate, we're going to generate K tokens.
1:14:11
And we're just going to sample from this draft model. So p is the draft model. We're going to sample K tokens.
1:14:18
And then in parallel, we're going to compute the logits of these draft tokens using q.
1:14:27
And then now we have to determine whether we accept or not. And this is where you do a bit of math and probability
1:14:34
and statistics. You basically are going to accept with probability Min 1
1:14:41
over this ratio of q over p. So if q is much larger than p, the larger the q is,
1:14:52
the more likely you want to accept it.
1:14:57
And otherwise you sample from this residual distribution
1:15:03
and exit. So this is basically rejection sampling
1:15:09
except for rejection sampling. Sometimes when you reject you get nothing.
1:15:14
But here we always are guaranteed to get an exact sample from the target model.
1:15:20
I'm going to skip this simple proof.
1:15:28
It's basically the same arguments as rejection sampling to show that it's the exact probabilities from the target model.
1:15:40
And the initial paper shows that this fast. And generally, if you have too few draft tokens,
1:15:50
you're not really leveraging the batching on the target model side. And if you have too many, then you're
1:15:57
going to reject more often. So there's a sweet spot around, in this case, three or four.
1:16:04
And in general, the draft model is much smaller than the target model. And ideally, you want the model draft model
1:16:11
to be as close to the target, which means that you want to distill it,
1:16:17
which means that actually a lot of the same ideas that we just talked about are applicable to speculative decoding as well.
1:16:24
So basically, the idea is that let's try to reduce your KV cache via all the different shenanigans.
1:16:32
And if you end up with a model you're happy with, just serve that. If you're not happy with it, then
1:16:37
it at least can be a draft model and you can use your main model to fix things up.
1:16:43
There's a bunch of whole literature on speculative decoding right now that improve over the original, which I'll skip for now.
1:16:55
So very quickly now, dynamic workloads.
1:17:01
So this is the use case, is you're serving a live website, and users come and chat with your model.
1:17:09
The requests arrive at different times. They have different shared prefixes,
1:17:14
and they have different lengths. So it's kind of pretty messy.
1:17:19
It's far from this very simple training where you have these blocks of the same number of tokens
1:17:27
all at once. So what do you do in this case? So there's this idea-- there's a system called
1:17:35
Orca that was built. This is actually very early on which introduced this idea of continuous batching.
1:17:41
So the idea is that you get a bunch of requests that look like this.
1:17:46
So here's a prefix of the first request, and you're generating this token. Here's the second one.
1:17:52
Here's the third one, the fourth one. It's jagged because every prefix has a different length. And what we're going to do is we're
1:17:58
going to decode step by step. So every step you decode one token for all the sequences.
1:18:05
Next step, you decode another token for all the sequences. And then if you end, you just eject that sequence.
1:18:13
And then as new requests arrive to the batch, then you basically put it in the batch,
1:18:19
and then you just continue. So that's why it's called continuous batching because it's
1:18:25
this batch is dynamically being updated with either old finished sequences being evicted
1:18:32
and new ones coming in. So now, one problem here is that everything
1:18:39
we've seen batching works when all the sequences have the same dimensionality. You have tensors.
1:18:44
Every slice has the same dimensionality. But if you request here has a different length,
1:18:50
so what do you do about that? So there's this idea called selective batching where let's say you have a length 3, length 9, and a length
1:19:00
5. So here in the attention computation, you can't really do anything about this
1:19:06
because attention depends on the length of your sequence.
1:19:11
So if you have a 3 by 3, a 9 by 9 computation, you can't really share the tensor effectively.
1:19:21
But for the non-attention, the MLP layers, which takes up a lot of FLOPs, you can actually just
1:19:28
concatenate all the sequences together to form a mega sequence, and you process that.
1:19:34
So final idea is page attention. This was introduced in the BLM paper.
1:19:41
Of course, BLM has many other bells and whistles now, but this is the core idea at that time.
1:19:49
So the question is, how is the KV cache stored?
1:19:54
So if you think about requests coming in,
1:20:00
you have to put them in memory somewhere. And in general, there's this problem that you get fragmentation.
1:20:06
This is what happens to-- or used to happen to your hard drive.
1:20:11
And you have to defrag your hard drive back in the day. So there's two types of fragmentation.
1:20:19
One is that you have to allocate enough buffer so that-- because you don't when you're going to stop.
1:20:26
So you might have a max token limit of 1,024. So you have to allocate all this memory.
1:20:32
And you can't put anything in there because you're going to just generate until you hit the max tokens, and that's very wasteful.
1:20:39
That's internal fragmentation. And then there's also you have-- there could be space between different requests.
1:20:46
And that space is maybe too small to get used effectively. So that's just wasted space.
1:20:52
So the solution here is just these are systems people. So they know their operating systems.
1:20:58
They said, OK, well we've solved this problem once before, so let's just use the same idea here.
1:21:04
So we're going to divide the KV cache of a sequence into non-contiguous blocks, OK?
1:21:11
So if you have this sequence, four score and seven years ago our fathers brought forth, we're
1:21:16
just going to chunk it up into these blocks of size 4. Doesn't matter where the blocks go, but they're
1:21:21
going to be aligned according to the block. So there's some uniformity there.
1:21:27
So when two requests share the same--
1:21:32
can actually share the same KV cache.
1:21:40
So you might have this block and this block. So this block might go here and here,
1:21:48
and this block might be over there. So they're interspersed. But as long as you have the indices
1:21:54
and keep track of where everything is, it's fine. So in particular, if you have, system prompts, then
1:22:05
you can actually just cache these system prompts, the KV cache and the system prompts, once.
1:22:12
And that can be useful for all the queries, OK? So this is very useful because if a lot of people
1:22:17
are using the same system prompt, then you don't have to compute the cache for every request.
1:22:24
Also there are many applications where you have the same prompt, and you actually want to generate multiple responses.
1:22:31
So in that case, you can also just share the KB cache for the prompt and just
1:22:39
have unique responses coming out. OK, So for example, if you were to generate,
1:22:46
let's say, multiple generations from score and seven years ago are, blank, and so what would happen here
1:22:55
is that you would have score and seven. And you would start by having years ago or our.
1:23:02
And then this is called copy on write semantics. You keep this.
1:23:08
And then we have these two samples. And if they had happened to sample let's say the same token,
1:23:14
you just continue with that. But if they sample different tokens, you split the block,
1:23:19
and then you can continue there. So you're basically sharing as much of the prefix cache
1:23:25
as you can. There's a bunch of other optimizations like kernels
1:23:33
that I'm not going to have time to go over. But the general idea is that you're
1:23:39
using these operating systems metaphors to manage
1:23:44
your inference. So summary here-- inference is really, really important.
1:23:51
It's very different from training. Even though it's the same model, but you're asking the model
1:23:57
to do something very different, ends up being very memory-bound. And it's also dynamic if you're in a live chatbot use case.
1:24:06
We saw a variety of different techniques to improve inference. You can quantize.
1:24:12
You can come up with new architectures. You can prune and distill. You can also use speculative sampling.
1:24:19
But all of these are driven by the same principle here, which is reduce your KV cache
1:24:24
but don't hurt accuracy too much. And then there's ideas from systems like paging
1:24:33
and speculative execution that can be brought to bear for actually live inference servers.
1:24:40
One thing we didn't really get a chance to talk about, which was discussed briefly, is that I think that new architectures have actually a huge potential
1:24:48
for improvement, things like state space models or linear attention or diffusion.
1:24:54
At some level, the KV cache and the way that attention is built fundamentally
1:25:01
makes it an inference unfriendly kind of architecture. So if you can come up with a new architecture that
1:25:08
is designed for inference in the way that the transformer was not, this can maybe unlock a lot.
1:25:15
OK, so I will stop there. And next class, Tatsu will return and talk
1:25:21
about scaling laws part 2.


&nbsp;



⬆️⬆️⬆️  END OF TRANSCRIPT  ⬆️⬆️⬆️

## §2 — Screenshot / visible source details  ·  layer: `raw_source_metadata`

*Nothing to paste here — just **drop the screenshot in chat** and the normalizer fills §0. (Or paste the visible title / channel / URL / date below.)*

## §3 — Interpretations & review log  ·  append-only (each reviewer adds a new entry; never overwrite)

### Review 001 — Knox / ChatGPT strategic read  ·  layer: `captured_interpretation_nonbinding`
- reviewer: `Knox / ChatGPT` · type: `AI assistant` · at: `TK` · purpose: `strategic source-local interpretation`

> Knox's read is the **strategic source-local interpretation** — paste **exactly what Knox outputs**, however rich, in ONE block. Do NOT split it into fields or trim it. Knox's output typically already carries: priority/depth · source type + authority context · why this matters for OMNI · core takeaway · OMNI translation · likely landing zones · doctrine/primitive pressure · keeper doctrine · what-not-to-import · hard read. (What we ASK Knox to produce = `_KNOX_STRATEGIC_READ_PROMPT.md`.) The agent's §3 Review 003 **formalizes** this (it does not re-derive it).

⬇️⬇️⬇️  PASTE KNOX'S / CHATGPT'S FULL READ BELOW (as-is)  ⬇️⬇️⬇️

This one is full semantic / top-tier for OMNI’s production-runtime economics, agent-token cost, latency/throughput tradeoffs, context-cache design, speculative execution, dynamic batching, and “serving cost beats build cost” doctrine. It pairs tightly with Scaling Laws Lecture 9: Lecture 9 said training-optimal is not serving-optimal; this lecture explains why inference becomes the real operating cost once the system is alive. Transcript is from the uploaded file.

Rough metadata for Opus
source_platform: YouTube
source_url: https://youtube.com/watch?v=EfM546A79aM
source_title: Stanford CS336 Language Modeling from Scratch | Spring 2026 | Lecture 10: Inference
channel_or_org: Stanford Online
course_context: Stanford CS336 / Language Modeling from Scratch
lecture_context: Lecture 10 / Inference
speaker_context: Likely Percy Liang based on transcript opening and closing references to Tatsu teaching adjacent lectures; video description lists Percy Liang and Tatsunori Hashimoto
speaker_affiliation_context:
Percy Liang: Professor of Computer Science
Tatsunori Hashimoto: Assistant Professor of Computer Science
published_at: May 11, 2026
captured_at: 2026-06-10
captured_by: Nick
capture_method: screenshot + transcript paste
content_type: technical AI systems lecture / inference / KV cache / latency / throughput / batching / speculative decoding / inference serving
source_reliability_context: Stanford course lecture | language-model inference systems and serving-economics source
screenshot_metrics_at_capture: 4,742 views; 76 likes
priority: 5/5
depth: full_semantic
recommended_status: core OMNI production-runtime source; route to AI substrate, Build OS, agent runtime economics, Patient-CNS latency design, Provider Workspace packet design, context cache, speculative execution, and serving-cost governance
transcript_quality_note: Auto-generated transcript is usable. Some caption artifacts appear, including likely “matmul” rendered as “MAmmoTH” in places. Hardware/model numeric details should be verified before formal technical canonization.
topic_tags_light: [CS336, language_modeling_from_scratch, inference, inference_efficiency, serving_cost, recurring_cost, agent_tokens, internal_tokens, TTFT, time_to_first_token, latency, throughput, tokens_per_second, batch_processing, interactive_applications, autoregressive_generation, sequential_generation, prefill, decode, generation, transformer_inference, arithmetic_intensity, compute_bound, memory_bound, HBM, SRAM, KV_cache, key_value_cache, KV_cache_memory, batch_size_tradeoff, latency_throughput_tradeoff, MLP_generation, attention_generation, attention_bottleneck, grouped_query_attention, GQA, multi_query_attention, MQA, multihead_latent_attention, MLA, DeepSeek_MLA, compressed_KV_cache, cross_layer_attention, CLA, sliding_window_attention, local_attention, hybrid_attention, global_attention, linear_attention, state_space_models, Mamba, DeltaNet, DeepSeek_sparse_attention, compressed_attention, KV_cache_reduction, quantization, post_training_quantization, quantization_aware_training, GPTQ, activation_aware_quantization, model_pruning, distillation, pruning_healing, speculative_decoding, speculative_sampling, draft_model, target_model, rejection_sampling, exact_sampling, continuous_batching, selective_batching, dynamic_workloads, paged_attention, vLLM, KV_cache_paging, fragmentation, internal_fragmentation, external_fragmentation, copy_on_write, shared_prefix_cache, system_prompt_cache, OS_metaphors, inference_unfriendly_transformer, production_serving, agentic_workloads, runtime_cost]
§0.1 rough people / authority context
primary instructor context:
name: Percy Liang
role_in_source: likely lecturer based on transcript opening and closing references
affiliation_at_publication: Stanford University
speaker_type: AI researcher / language models / foundation-model systems methodology
authority_context: Very high relevance for inference serving, runtime cost, latency/throughput tradeoffs, agentic token economics, and systems-aware model deployment.
additional course instructor context:
name: Tatsunori Hashimoto
role_in_source: course instructor listed in video description; referenced as prior and next lecturer in transcript
affiliation_at_publication: Stanford University
speaker_type: AI researcher / data-centric ML / systems-aware model training
authority_context: Very high relevance for CS336’s broader systems, scaling, data, and inference methodology.
publisher / channel: Stanford Online
course / event context: Stanford CS336: Language Modeling from Scratch
perspective / conflict notes: Technical inference systems source. Not healthcare-specific. Durable OMNI value comes from production runtime economics: repeated serving cost, context/cache memory, latency vs throughput tradeoffs, dynamic workloads, speculative execution, and operating-system-style memory management. Do not import exact GPU formulas as product doctrine; translate the operating laws.
suggested_processing:
priority: 5/5
depth: full_semantic
EVRUN needed?: yes
likely landing_zones: AI substrate / Build OS / Agent Work Blueprint / Patient-CNS / Provider Workspace / context packet strategy / runtime-cost governance / inference router / D7 source-cache policy / Architecture Memory Control Plane / BIZOPS operations / product latency design / speculative execution and verifier doctrine
Knox / ChatGPT read for §3 Review 001

Process this one as full semantic.

This is a core production-runtime source for OMNI.

It is not healthcare-specific.

But it is extremely relevant because OMNI is not going to be judged only by whether it can build a clever model, run a smart agent, or produce a good answer once.

OMNI will be judged by whether it can run every day.

Across:

patients,
providers,
staff,
agents,
source systems,
messages,
documents,
tasks,
follow-ups,
incidents,
workflows,
and business operations.

Lecture 9 said:

training-optimal is not serving-optimal.

Lecture 10 explains why.

Core takeaway

Training is a one-time cost.

Inference is a repeated cost.

In an agentic world, most generated tokens may not even be read by a human.

They are internal compute.

That means inference is not just UX.

Inference is runtime economics.

OMNI translation:

The cost of OMNI will not be only building the system.

It will be operating the system:

agent tokens,
retrieval,
context loading,
provider review,
patient wait time,
tool calls,
source cache,
document extraction,
audit trace,
routing,
and repeated daily workflows.

Classification

Priority: 5/5
Depth: full semantic
Authority context: Stanford language-model inference systems / serving economics
OMNI relevance: top-tier for AI substrate, Build OS, Patient-CNS, Provider Workspace, agent-runtime cost, context cache, speculative execution, dynamic batching, and serving-cost governance
Promotion posture: core production-runtime doctrine; translate inference laws, not literal hardware formulas

The big OMNI translation

Inference matters because it is repeated.

Training happens once.

Inference happens every day.

The lecture says this explicitly.

OMNI equivalent:

Build cost happens once.

Runtime cost happens every day.

Keeper:

Operating cost beats build cost.

Inference becomes dominant when the product is used.

A model in a paper is not a product.

A model serving users is a product.

OMNI translation:

Architecture diagrams are not the business.

Daily care operations are the business.

Keeper:

A system is real when it serves.

Agentic workloads make inference more important.

The lecture contrasts chatbots with agents.

In a chatbot, tokens are mostly for a human to read.

In an agent, many tokens are internal reasoning, tool calls, scratch, and planning.

OMNI translation:

Agent work can burn enormous hidden runtime cost.

An agent may read files,
plan,
retry,
call tools,
patch,
test,
summarize,
and produce internal tokens that no human ever sees.

Keeper:

Agent tokens are operating cost.

Internal tokens are not free because they are invisible.

This is a huge OMNI doctrine point.

If an agent writes 10,000 tokens of scratch and only 200 tokens reach the user, the 10,000 tokens still cost money, latency, risk, and trace storage.

Keeper:

Invisible work still consumes the system.

Inference efficiency is product capacity.

If inference is 10% faster, that can matter commercially.

OMNI equivalent:

If patient triage, provider packet generation, D7 extraction, or agent build loops become 10% cheaper/faster, the operating system’s capacity changes.

Keeper:

Runtime efficiency is business leverage.

Fast has multiple meanings.

The lecture defines:

time to first token,

latency,

throughput.

These are not the same.

OMNI translation:

Do not use one generic “fast” metric.

Patient-facing response speed,
provider packet load time,
batch corpus processing,
agent patch throughput,
back-office queue completion,

are different kinds of fast.

Keeper:

Fast is not one metric.

Time to first token maps to first visible response.

TTFT is how long the user waits before anything appears.

OMNI equivalent:

how long before the patient sees acknowledgment,

how long before provider sees first useful summary,

how long before staff gets routed task,

how long before agent starts producing visible proof.

Keeper:

First signal matters.

Latency maps to individual experience.

How fast does one query progress?

OMNI:

How long does one patient wait?

How long does one provider case take?

How long does one agent patch run?

How long does one document extraction complete?

Keeper:

Latency is individual experience.

Throughput maps to batch operations.

Tokens per second across many queries.

OMNI:

documents per hour,
patient messages per hour,
cases per reviewer,
files per extraction wave,
agent patches per sprint,
claims per billing run.

Keeper:

Throughput is system capacity.

Latency and throughput can trade off.

Increasing batch size can improve throughput while worsening latency.

OMNI translation:

Batching patient messages may improve staff efficiency but worsen individual patient wait.

Batching provider review may improve throughput but delay one urgent case.

Keeper:

Throughput gains can hurt individual latency.

Interactive workflows and batch workflows need different optimization.

Chatbot = TTFT and latency.

Batch data processing = throughput.

OMNI equivalent:

Patient-CNS needs low TTFT and safe latency.

Architecture corpus extraction needs throughput.

Provider Workspace needs balanced latency and accuracy.

Back-office ops need throughput.

Keeper:

Optimize by workflow class.

Training parallelizes over sequence; inference cannot.

Training sees all tokens and processes them together.

Autoregressive inference must generate one token at a time.

OMNI analogy:

Some workflows can be processed in parallel.

Some require sequential decisions because each step depends on the prior output.

Keeper:

Sequential generation has a dependency cost.

Autoregressive agent loops are structurally expensive.

Agent steps often depend on previous tool results.

That makes them inference-like:

plan,
act,
observe,
revise,
act again.

Keeper:

Agent loops are runtime-bound.

Prefill and generation are different phases.

Prefill processes the prompt/context.

Generation produces response tokens sequentially.

OMNI equivalent:

load context first,

then act step-by-step.

Examples:

load patient packet,
then generate reply.

load architecture source bundle,
then patch code.

load D7 evidence,
then extract observation.

Keeper:

Context loading and action generation are different costs.

Prefill is more parallelizable.

The lecture says prefill can process the whole prompt and is compute-bound.

OMNI translation:

Preparing a context packet or evidence bundle can often be done in parallel.

Keeper:

Setup can be parallel; action may be sequential.

Generation is memory-bound.

The lecture’s core technical point is that attention during generation is memory-bound.

OMNI translation:

Once acting begins, the bottleneck is often not raw intelligence.

It is moving the right state/context at each step.

Keeper:

Runtime bottleneck is memory movement.

“Inference is memory-bound” now has a reason.

The lecture walks through arithmetic intensity to explain why generation attention has poor compute utilization.

OMNI lesson:

Do not accept slogans without mechanism.

Keeper:

Know why the bottleneck exists.

MLP generation can batch well; attention generation cannot batch as cleanly.

MLP weights are shared across sequences.

KV cache is sequence-specific.

OMNI analogy:

Some common work can be amortized across many cases.

Some patient-specific context cannot.

Keeper:

Shared work amortizes; personal context does not.

KV cache is the core inference memory object.

The KV cache stores prior key/value states so the model does not recompute the whole prefix every token.

OMNI equivalent:

working context cache:

patient current packet,
episode state,
open obligations,
source pointers,
provider packet,
agent build state,
architecture read state.

Keeper:

Working memory cache is runtime infrastructure.

KV cache makes inference feasible but creates memory pressure.

Without KV cache, generation recomputes too much.

With KV cache, memory grows with sequence/batch/layers/heads.

OMNI translation:

Caching context avoids repeated retrieval, but cached context can dominate memory and cost.

Keeper:

Cache saves compute but spends memory.

Cache size grows with batch and sequence.

The lecture repeatedly shows batch size and sequence length create memory pressure.

OMNI equivalent:

more concurrent patients,
longer histories,
larger context packets,
more agents,
deeper source bundles,

increase runtime memory.

Keeper:

Concurrency plus context length drives cost.

The model parameters are shared; the KV cache is per request.

OMNI analogy:

system doctrine is shared.

patient-specific context is not.

Provider packet is per patient.

Agent scratch is per task.

Keeper:

Shared substrate and per-task state have different economics.

Increasing batch size improves throughput by amortizing parameters.

You load the model weights once and process many sequences.

OMNI equivalent:

a batch process can reuse common policy, prompt, tool setup, or source parser across many cases.

Keeper:

Batching amortizes shared setup.

Increasing batch size worsens latency because each request waits for the batch.

The lecture uses the bus analogy.

OMNI equivalent:

batching staff work improves queue throughput but makes the individual wait.

Keeper:

The bus is efficient and annoying.

Memory limits cap throughput.

You cannot increase batch size forever because KV cache runs out of memory.

OMNI equivalent:

you cannot increase work-in-flight forever because working context, review bandwidth, staff attention, and queue state overflow.

Keeper:

Throughput hits working-memory limits.

TTFT is prefill time.

Time to first token is basically how long it takes to process prompt/context before generating.

OMNI:

time to first useful response depends on context packet preparation.

Keeper:

First response depends on prefill.

Smaller batch helps TTFT.

For patient-facing workflows, smaller batch may matter.

OMNI should not optimize Patient-CNS like corpus processing.

Keeper:

Patient-facing systems need low first-response latency.

Batch jobs can tolerate worse latency for better throughput.

Architecture memory extraction, document OCR, eval sweeps, and backfills can batch aggressively.

Keeper:

Batch systems can ride the bus.

Reduce KV cache; improve latency and throughput.

This is the lecture’s recurring optimization law.

OMNI equivalent:

reduce working context without losing decision quality.

Keeper:

Smaller working state improves runtime.

But reducing KV cache can hurt accuracy.

GQA, MLA, cross-layer attention, sliding windows, quantization, and pruning all need accuracy checks.

OMNI equivalent:

summaries, compression, context pruning, source selection, and model choice all need downstream validation.

Keeper:

Compression requires eval.

Grouped Query Attention is shared memory across heads.

GQA reduces key/value heads and shrinks KV cache.

OMNI analogy:

share common context across related reasoning lanes instead of duplicating it.

Keeper:

Share where independence is unnecessary.

Multi-Query Attention is too aggressive.

The lecture says MQA reduces too much and is bad.

OMNI equivalent:

over-compression can destroy quality.

Keeper:

Maximum compression is not optimal.

GQA is a compromise.

Between full expressive KV cache and too-aggressive sharing.

OMNI:

shared patient/common context plus selected specialist context.

Keeper:

Good runtime design is compromise compression.

Multi-Latent Attention compresses KV into a smaller latent and materializes keys/values when needed.

This is highly OMNI-relevant.

Store compact state.

Materialize full context only when needed.

Keeper:

Store compactly; materialize on demand.

MLA is FSDP-like for inference memory.

Keep compressed latent C.

Reconstruct K/V when needed.

OMNI:

store compact patient current state,
but pull D7 source/evidence when needed.

Keeper:

Compact cache must preserve source fallback.

Deep compression needs empirical validation.

The lecture notes MLA performs well in one DeepSeek setting, but GQA results can conflict across papers.

OMNI lesson:

Do not assume compression techniques generalize without local evals.

Keeper:

Compression wins are workload-specific.

Cross-layer attention shares KV across layers.

OMNI analogy:

reuse context across workflow stages instead of recomputing/reloading it every time.

Keeper:

Share working state across adjacent layers.

Cross-layer sharing improves the frontier if done carefully.

This supports OMNI’s “do not duplicate shared substrate” idea.

Identity, authority, source pointers, and task state can be shared across adjacent operations.

Keeper:

Shared substrate reduces repeated memory.

Sliding-window attention caps memory by looking only at recent tokens.

OMNI:

local context only:

current message,
recent interaction,
active episode,
latest lab,
current task.

Keeper:

Recent context is cheap and useful.

Sliding windows hurt long-range recall.

The lecture says local attention reduces expressivity.

OMNI equivalent:

routine local context is not enough for edge cases, history, legal, safety, or longitudinal care.

Keeper:

Local context misses old needles.

Hybrid local/global attention is the practical answer.

Some layers local, some global.

OMNI:

local context by default,
global source fallback when risk/uncertainty rises.

Keeper:

Hybrid context wins.

Linear attention / state-space models compress history into state.

The lecture briefly says they may summarize all history into a fixed representation.

OMNI:

patient current state,
clinical memory current view,
architecture canon digest,
open obligation state.

Keeper:

Compressed state is useful but lossy.

Needle-in-haystack breaks pure compression.

If the answer depends on one old specific fact, compressed history may miss it.

OMNI:

do not rely only on summaries for contraindications, incidents, consent, identity, legal, or safety.

Keeper:

High-stakes needles require source retrieval.

Different memory mechanisms capture different things.

Sliding window = high-resolution local.

Linear/state-space = broad compressed past.

Full attention/source retrieval = exact long-range.

OMNI should combine them.

Keeper:

Memory has modes.

DeepSeek sparse attention uses cheap indexing before expensive attention.

This repeats Lecture 4’s lesson.

OMNI:

cheap source selector first,
expensive reasoning second.

Keeper:

Index cheaply; reason deeply.

Reducing KV cache is the common objective across many methods.

GQA,
MLA,
CLA,
sliding window,
linear attention,
sparse attention.

OMNI equivalent:

reduce runtime context footprint while preserving enough evidence and authority.

Keeper:

Runtime context is the object to optimize.

Quantization reduces precision to reduce memory.

OMNI analogy:

summaries, labels, embeddings, risk scores, and extracted observations are lower-precision versions of source.

Keeper:

Lower precision is a governed compression.

Quantization-aware training is expensive but safer.

Train with quantization in mind.

OMNI:

design workflows with runtime constraints from the start.

Keeper:

Build for runtime constraints early.

Post-training quantization is cheaper but riskier.

Apply compression after the model is trained.

OMNI:

compress existing docs, workflows, source packets, or agent traces after the fact.

Keeper:

Post-hoc compression needs calibration.

GPTQ-like error correction maps to compression with residual tracking.

Quantize layer by layer and compensate for propagated errors.

OMNI:

when summarizing or extracting, track what was lost, uncertain, or carried forward as unresolved.

Keeper:

Compression needs error accounting.

Activation-aware quantization preserves important channels.

Keep high-importance channels in higher precision.

OMNI:

preserve high-risk/high-signal fields in high fidelity.

Examples:

allergies,
contraindications,
payment amount,
consent,
identity,
adverse event,
source citation,
provider decision.

Keeper:

Important channels deserve higher precision.

Pruning removes parts of the model, then heals with further training.

OMNI analogy:

remove low-value workflow branches, fields, agents, or steps, then repair the process through focused retraining/eval.

Keeper:

Prune, then heal.

Importance estimation needs calibration data.

The lecture says pruning depends on calibration inputs and activation magnitudes.

OMNI:

do not prune workflows based on opinion alone.

Use representative cases.

Keeper:

Pruning needs calibration sets.

Large activations are not automatically meaningful.

The Q&A highlights that high magnitude alone may not mean importance.

OMNI:

high volume is not the same as high value.

High-frequency tasks may be noise.

Keeper:

Importance needs more than magnitude.

Distillation repairs faster models.

A compressed/pruned model can be healed through distillation.

OMNI:

a simplified workflow can be trained from the richer workflow.

A smaller agent can learn from a stronger agent/human.

Keeper:

Compress capability through distillation.

Speculative decoding is the most important OMNI analogy in this lecture.

Cheap draft model proposes multiple tokens.

Target model verifies them.

Accepted tokens move forward.

Keeper:

Draft cheap; verify expensive.

Speculative decoding exploits checking being faster than generation.

It is faster to evaluate a proposed sequence than generate it token by token.

OMNI:

it may be cheaper for a senior reviewer/model/human to verify a draft than create from scratch.

Keeper:

Verification can be cheaper than creation.

Speculative decoding can be exact.

The algorithm can preserve exact target distribution through acceptance/rejection math.

OMNI analogy:

Some speculative workflows can be safe if verifier semantics are strict.

Keeper:

Speculation needs acceptance rules.

Draft model should be smaller and close to target.

If draft is too bad, rejections rise.

If draft is too big, no speedup.

OMNI:

junior agent/draft workflow should be cheap but competent.

Keeper:

A draft lane needs calibrated quality.

Too few draft tokens underuse batching; too many get rejected.

OMNI:

too little speculative work wastes reviewer capacity.

too much speculative work creates rejection/rework.

Keeper:

Speculation has a batch sweet spot.

If compressed model is good enough, serve it directly.

If not, use it as draft model.

OMNI:

if smaller workflow is safe enough, use it.

If not, use it to draft for higher-authority verification.

Keeper:

Compression can serve or draft.

Speculative decoding maps to AI-human workflows.

AI drafts.

Human verifies.

But only for tasks where verification is genuinely easier than creation.

Keeper:

Human-in-the-loop only helps when review is cheaper than authorship.

Dynamic workloads are messy.

Live website requests arrive at different times, share prefixes, and have different lengths.

OMNI:

patients, providers, agents, documents, and workflows arrive unpredictably and vary in length.

Keeper:

Production workloads are jagged.

Training batches are neat; serving batches are not.

Training has rectangular tensors.

Serving has jagged sequences.

OMNI:

planned corpus jobs are neat.

real care/business operations are jagged.

Keeper:

Runtime is not a clean batch.

Continuous batching dynamically updates the batch.

Finished sequences leave.

New requests enter.

OMNI:

runtime queues should continuously admit and complete work, not wait for fixed cohorts.

Keeper:

Use continuous batching for live work.

Continuous batching improves throughput for unpredictable arrivals.

OMNI:

patient messages, task queues, agent jobs, and provider packets need dynamic queue management.

Keeper:

Dynamic arrivals need dynamic batching.

Selective batching handles jagged sequence lengths.

Attention cannot fully batch across different lengths.

MLP/non-attention parts can concatenate sequences.

OMNI analogy:

some work is case-specific and cannot be batched;

some common work can be batched across cases.

Keeper:

Batch shared work; isolate case-specific work.

Paged attention is operating-system memory management for KV cache.

The lecture says systems people reused paging concepts.

OMNI should do the same.

Keeper:

Inference serving is an OS problem.

Fragmentation wastes memory.

Internal fragmentation: allocated but unused capacity.

External fragmentation: gaps too small to use.

OMNI:

unused provider capacity,
oversized context packets,
empty schedule gaps,
half-filled review batches,
stale cached packets,
abandoned draft work.

Keeper:

Fragmentation is hidden waste.

Paged attention chunks KV cache into blocks.

The cache is stored in non-contiguous blocks with an index.

OMNI:

source/context can be block-addressed:

patient episode block,
D7 evidence block,
system prompt block,
policy block,
architecture domain block.

Keeper:

Context should be block-addressable.

Shared prefixes can share KV cache.

System prompts and repeated prefixes can be cached once and reused.

OMNI:

shared instructions, policy, system map, tenant config, source schema, and domain contracts should be cached and reused.

Keeper:

Cache shared prefixes.

Copy-on-write preserves sharing until divergence.

Two generations share prefix cache until they diverge.

OMNI:

similar workflows can share common setup until their cases branch.

Keeper:

Share until divergence.

System prompts are cacheable infrastructure.

If many requests use the same system prompt, cache it once.

OMNI:

common operating rules should not be reloaded/reinterpreted for every task.

Keeper:

Shared doctrine should be preloaded.

Paged attention is a direct context-packet doctrine source.

Context should not be one giant contiguous blob.

It should be paged, indexed, shared, and released.

Keeper:

Page context, do not dump context.

Inference architecture borrows from operating systems.

Paging,
speculative execution,
batching,
cache sharing,
copy-on-write.

OMNI should also borrow OS concepts.

Keeper:

Care/business AI needs operating-system primitives.

Transformer architecture is inference-unfriendly.

The lecture says attention/KV cache makes inference awkward.

OMNI lesson:

A system can be excellent for one phase and awkward for another.

A build-friendly architecture may not be run-friendly.

Keeper:

Build architecture and runtime architecture differ.

New architectures may unlock inference gains.

State-space models, linear attention, diffusion-style generation may be more inference-friendly.

OMNI:

future OMNI runtime may need different AI architectures for different workloads.

Keeper:

Model architecture should match serving workload.

The lecture validates Lecture 9’s production-overtraining point.

If serving dominates, optimize for inference, not just training.

OMNI:

spend more upfront to make daily runtime cheaper and safer.

Keeper:

Invest upfront to lower recurring cost.

Inference metrics should become OMNI runtime metrics.

OMNI equivalents:

time to first patient acknowledgment,

provider packet load latency,

agent patch throughput,

document extraction throughput,

patient-message resolution latency,

review queue throughput,

source-cache hit rate,

context cache memory,

draft acceptance rate.

Keeper:

Runtime needs its own dashboard.

KV cache hit/miss maps to source/context reuse.

If context must be rebuilt every time, runtime cost explodes.

Keeper:

Context reuse should be measured.

Draft acceptance rate maps to speculative workflow quality.

If the verifier rejects too much, speculation is wasting time.

OMNI:

track AI draft acceptance,
provider edit burden,
agent patch rejection,
test failure rate.

Keeper:

Speculation quality is measurable.

Batch size maps to queue policy.

Small batch:

low latency,
low throughput.

Large batch:

high throughput,
worse individual delay.

OMNI needs per-workflow batch policy.

Keeper:

Queue policy is product behavior.

Memory-bound means “better model” may not fix runtime.

If context movement is the bottleneck, a stronger model may be irrelevant.

Keeper:

Optimize the binding runtime constraint.

Model compression can improve both latency and throughput.

If the memory footprint shrinks, the tradeoff frontier improves.

OMNI:

better source packets, cache, summaries, and templates can improve both speed and capacity.

Keeper:

Reducing state can move the frontier.

Accuracy evaluation is mandatory after every speed trick.

The lecture repeats this.

OMNI:

no compression, cache, speculative draft, pruning, batching, or shortcut without downstream eval.

Keeper:

Speed changes need safety evals.

Serving live agents requires runtime scheduler.

Continuous batching, paged cache, and dynamic admission are scheduler concepts.

OMNI:

agent runtime needs scheduling, not just prompts.

Keeper:

Agents need a runtime, not just instructions.

Patient-CNS should distinguish interactive and background work.

Interactive:

acknowledgment,
triage,
safety escalation.

Background:

summary,
documentation,
longitudinal update,
analytics,
follow-up prep.

Keeper:

Separate foreground from background.

Provider Workspace should prefill before provider arrives.

If provider packet preparation is prefill, do it before the provider needs it.

Keeper:

Prefetch provider context.

D7 extraction can be throughput-optimized.

D7 backfills and OCR/extraction jobs are batch processing.

Optimize throughput, not TTFT.

Keeper:

Background evidence work rides throughput lanes.

Clinical escalation should be latency-optimized.

High-risk patient messages need low latency and possibly smaller batches.

Keeper:

Safety work does not wait for the bus.

Architecture Memory Control Plane can cache shared doctrine.

System map,
domain contracts,
authority routing,
read graph,
standard extraction template.

Do not reload everything for every agent.

Keeper:

Architecture context needs prefix caching.

Build OS can use speculative execution.

Cheap agent proposes patch or extraction.

Verifier agent/test suite/human accepts or rejects.

But acceptance criteria must be explicit.

Keeper:

Speculative Build OS requires verifier gates.

Speculative execution without verifier is just hallucination at speed.

The exactness of speculative decoding comes from the verifier algorithm.

OMNI equivalent:

draft-only workflows are not safe unless verified before commit.

Keeper:

Speculation without verification is unsafe acceleration.

Copy-on-write maps to workflow branching.

Shared setup:

patient context,
policy,
source bundle.

Branch:

clinical,
billing,
identity,
D7,
messaging.

Keeper:

Fork workflows only after shared setup.

Paged context helps avoid monolithic context dumps.

Instead of one giant prompt, maintain blocks and pointers.

OMNI:

context_packet should likely be assembled from reusable blocks.

Keeper:

Block references beat giant blobs.

Fragmentation is a BIZOPS/workforce concept too.

Half-filled appointment slots,
unused staff time,
or fragmented provider availability reduce effective capacity.

Keeper:

Memory fragmentation has business analogues.

The strongest technical doctrine is “reduce memory without losing accuracy.”

For OMNI:

reduce context,
reduce handoffs,
reduce source movement,
reduce review burden,

without losing safety, authority, or outcome quality.

Keeper:

Efficiency is safe compression.

The strongest product doctrine is “serving is the product.”

The user experiences inference, not training.

The provider experiences packet latency, not architecture elegance.

The patient experiences response time and resolution, not system map beauty.

Keeper:

Runtime is where trust is earned.

Likely OMNI landing zones

AI substrate:

inference cost model
model/router selection by latency/throughput
KV-cache analog
GQA/MLA/compression lessons
speculative decoding strategy
continuous batching and paged cache
runtime scheduler

Build OS:

agent token accounting
speculative draft/verify loop
patch acceptance rate
context-cache reuse
foreground/background task separation
dynamic batching of agent jobs
proof before commit
test/verifier as target model

Agent Work Blueprint:

cheap draft agent
high-authority verifier
acceptance rules
no invisible scratch explosion
cache shared doctrine
copy-on-write work branches
dynamic queues
background throughput lanes

Context packet strategy:

prefill vs generation split
compact context cache
source fallback
paged context blocks
shared prefix cache
high-fidelity channels
hybrid local/global memory
cache hit/miss tracking

Patient-CNS:

TTFT for patient acknowledgment
low-latency clinical escalation
background longitudinal updates
continuous batching for normal traffic
high-risk requests skip throughput bus
local context plus source fallback

Provider Workspace:

provider packet prefetch
selected context cache
shared policy/system context
source drill-down
reduce packet size without losing clinical signal
packet latency metrics

D7:

batch extraction throughput
source-cache blocks
artifact paging
source vs derivative storage
on-demand materialization
compression safety checks
high-fidelity source retention

Architecture Memory Control Plane:

shared prefix doctrine cache
paged source blocks
agent read-state cache
context reuse across extraction waves
copy-on-write branch for domain-specific passes
throughput-optimized corpus jobs

BIZOPS:

queue batching policy
capacity fragmentation
foreground/background operational lanes
staff time as memory/cache analog
throughput vs latency by workflow type

Governance / Security:

high-risk facts stay high precision
speculative outputs need verifier gates
no draft-to-commit without authority
runtime traces for hidden tokens/actions
compression evals
cache visibility and access control

Doctrine / primitive pressure

Potential concepts:

operating_cost_beats_build_cost
serving_is_product
agent_tokens_as_operating_cost
invisible_work_cost
TTFT_patient_ack
latency_individual_experience
throughput_system_capacity
fast_metric_taxonomy
interactive_vs_batch_workflow
prefill_vs_generation
context_loading_vs_action_generation
memory_bound_runtime
working_context_cache
cache_saves_compute_spends_memory
shared_substrate_vs_per_task_state
batch_amortization
latency_throughput_tradeoff
working_memory_limit
reduce_working_state
compression_requires_eval
share_where_independence_unneeded
compact_cache_materialize_on_demand
hybrid_context_memory
local_context_misses_old_needles
cheap_index_deep_reasoning
quantization_as_governed_compression
high_importance_channel_precision
prune_then_heal
draft_cheap_verify_expensive
verification_cheaper_than_creation
speculation_acceptance_rules
draft_acceptance_rate
dynamic_runtime_workload
continuous_batching
selective_batching
runtime_jaggedness
paged_context
context_block_addressing
shared_prefix_cache
copy_on_write_workflow
system_prompt_cache
OS_primitives_for_AI
inference_unfriendly_architecture
build_architecture_vs_runtime_architecture
runtime_dashboard
agent_runtime_scheduler
foreground_background_lane
safe_compression
runtime_trust_surface

Keeper doctrine candidates

Operating cost beats build cost.

A system is real when it serves.

Agent tokens are operating cost.

Invisible work still consumes the system.

Runtime efficiency is business leverage.

Fast is not one metric.

First signal matters.

Latency is individual experience.

Throughput is system capacity.

Optimize by workflow class.

Throughput gains can hurt individual latency.

Sequential generation has a dependency cost.

Agent loops are runtime-bound.

Context loading and action generation are different costs.

Setup can be parallel; action may be sequential.

Runtime bottleneck is memory movement.

Know why the bottleneck exists.

Shared work amortizes; personal context does not.

Working memory cache is runtime infrastructure.

Cache saves compute but spends memory.

Concurrency plus context length drives cost.

Shared substrate and per-task state have different economics.

Batching amortizes shared setup.

The bus is efficient and annoying.

Throughput hits working-memory limits.

First response depends on prefill.

Patient-facing systems need low first-response latency.

Batch systems can ride the bus.

Smaller working state improves runtime.

Compression requires eval.

Share where independence is unnecessary.

Maximum compression is not optimal.

Good runtime design is compromise compression.

Store compactly; materialize on demand.

Compact cache must preserve source fallback.

Compression wins are workload-specific.

Share working state across adjacent layers.

Shared substrate reduces repeated memory.

Recent context is cheap and useful.

Local context misses old needles.

Hybrid context wins.

Compressed state is useful but lossy.

High-stakes needles require source retrieval.

Memory has modes.

Index cheaply; reason deeply.

Runtime context is the object to optimize.

Lower precision is a governed compression.

Build for runtime constraints early.

Post-hoc compression needs calibration.

Compression needs error accounting.

Important channels deserve higher precision.

Prune, then heal.

Pruning needs calibration sets.

Importance needs more than magnitude.

Compress capability through distillation.

Draft cheap; verify expensive.

Verification can be cheaper than creation.

Speculation needs acceptance rules.

A draft lane needs calibrated quality.

Speculation has a batch sweet spot.

Compression can serve or draft.

Human-in-the-loop only helps when review is cheaper than authorship.

Production workloads are jagged.

Runtime is not a clean batch.

Use continuous batching for live work.

Dynamic arrivals need dynamic batching.

Batch shared work; isolate case-specific work.

Inference serving is an OS problem.

Fragmentation is hidden waste.

Context should be block-addressable.

Cache shared prefixes.

Share until divergence.

Shared doctrine should be preloaded.

Page context, do not dump context.

Care/business AI needs operating-system primitives.

Build architecture and runtime architecture differ.

Model architecture should match serving workload.

Invest upfront to lower recurring cost.

Runtime needs its own dashboard.

Context reuse should be measured.

Speculation quality is measurable.

Queue policy is product behavior.

Optimize the binding runtime constraint.

Reducing state can move the frontier.

Speed changes need safety evals.

Agents need a runtime, not just instructions.

Separate foreground from background.

Prefetch provider context.

Background evidence work rides throughput lanes.

Safety work does not wait for the bus.

Architecture context needs prefix caching.

Speculative Build OS requires verifier gates.

Speculation without verification is unsafe acceleration.

Fork workflows only after shared setup.

Block references beat giant blobs.

Efficiency is safe compression.

Runtime is where trust is earned.

What not to import blindly

Do not turn KV-cache formulas into OMNI schema doctrine.

Do not assume patient-care workflows can be optimized like token generation without safety constraints.

Do not optimize throughput for high-risk patient interactions.

Do not optimize TTFT for background corpus jobs.

Do not treat all “fast” metrics as interchangeable.

Do not hide agent internal token cost.

Do not assume larger batch is better.

Do not reduce context without downstream eval.

Do not compress clinical/legal/identity/payment facts too aggressively.

Do not let summaries replace source evidence.

Do not treat speculative drafts as committed outputs.

Do not rely on human review unless review is actually cheaper and safer than authorship.

Do not use continuous batching if tasks have incompatible safety/authority requirements.

Do not share cached context across tenants, patients, or roles without access control.

Do not let cached shared doctrine become stale.

Do not allow copy-on-write branches to lose provenance.

Do not treat runtime cache as canonical truth.

Do not assume one model architecture is best for all runtime workloads.

Do not optimize average throughput while ignoring tail latency.

Do not make patient safety wait behind batch efficiency.

Do-not-miss lesson

This source gives OMNI a runtime law:

The real cost of an AI operating system is not the impressive one-time build. It is the repeated act of serving: loading context, carrying working state, generating internal tokens, batching dynamic requests, caching shared prefixes, verifying speculative outputs, and preserving latency for the humans and patients who cannot wait for the throughput bus.

Shortest OMNI version

OMNI should treat inference like production operations: track time-to-first-useful-signal, individual latency, batch throughput, context-cache size, draft acceptance rate, hidden agent-token spend, cache reuse, and memory fragmentation. Use compact working state, shared prefix cache, paged context blocks, speculative draft-plus-verifier loops, continuous batching for routine work, low-latency lanes for safety, and source fallback whenever compression could hide the truth.

My hard read

Full semantic. Top-tier production-runtime source.

This belongs with Scaling Laws Lecture 9, Attention Alternatives Lecture 4, and Parallelism Lectures 7/8.

Lecture 9 says production economics change the optimum.

Lecture 10 explains the runtime mechanics behind that: inference is repeated, agent tokens explode, generation is memory-bound, KV/context cache dominates, latency and throughput trade off, and production serving requires operating-system primitives like batching, paging, caching, copy-on-write, and speculative execution.

The strongest OMNI line:

OMNI will not win by building the smartest one-time answer. It will win by serving the right answer repeatedly, cheaply, quickly, safely, and with the right amount of context: small where routine, deep where risky, cached where shared, paged where large, verified where speculative, and never compressed so far that source truth disappears.


&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 002 — Nick operator note  ·  layer: `human_context_nonbinding`
- reviewer: `Nick` · type: `human founder/operator` · at: `TK`

⬇️⬇️⬇️  YOUR GUT NOTE BELOW  (optional)  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END  ⬆️⬆️⬆️

### Review 003 — Opus / agent formal deep extraction  ·  layer: `analysis_nonbinding`  ·  (agent-authored, NOT a Nick paste)
- reviewer: `Opus` (or agent) · type: `AI assistant` · at: `TK` · purpose: `formalize Review 001 → structured per-source extraction → feed EVRUN concept registry` · binds nothing (`GRD-036`/`GRD-044`)

> The agent reads §1 verbatim + §3 Review 001 (Knox) IN FULL, then writes the formal per-source extraction HERE (not a sidecar). Formalize Review 001's strategic read — verify/sharpen it, do not re-derive. Per concept cluster: **concept · OMNI meaning · why · downstream homes (thesis §x / CNS / Build-OS / §C / capability-topology / Knowledge-Reservoirs / security / domain-contracts / UX-surfaces / product / future-watch) · source anchors (verbatim ≤12 words + timestamp) · stale-vs-v3 (ABSENT/PARTIAL/AFFIRM) · weight_tier (spine/vocabulary/low-authority-watch/no-op) · status (promote/watch/reject).** Then: **net-new primitives** (dedup vs registry — don't re-mint) + **reread flags**. Cross-source convergence is folded into the EVRUN **concept registry**, not duplicated here.

⬇️⬇️⬇️  AGENT WRITES THE FORMAL EXTRACTION BELOW  ⬇️⬇️⬇️



&nbsp;



⬆️⬆️⬆️  END Review 003  ⬆️⬆️⬆️

## §4 — Analysis pointers  *(agent fills at CLOSEOUT once analyzed — the "linked" step; leave `TK` until then)*
- EVRUN(s): `TK` (e.g. `EVRUN-2026-000001` ai-corpus) · concept_registry: `TK` (`{EVRUN}_{slug}_concept_registry_and_routing_map.md`) · source_anchor_ledger: `TK` (`…_source_anchor_ledger_receipts_only.md`) · per-source deep-read: §3 Review 003 (this file) · impact: `TK` (§A/§B/§C/CNS/Build-OS/security/domain-contracts/surfaces) · promotion: `TK` (watch | promote | reject | no-op)

## §5 — Change log
- `2026-06-11` — source file created.

> Authority/retrieval labels are defined once in `../../00_evidence_router.md` (operating principle #9 + §8). Sections carry their `layer:` inline; the legend is not restated per file.
