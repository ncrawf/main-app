# EVRUN-2026-000001 · G12 IBM explainers — FULL CONCEPT EXTRACTIONS (preservation record)

> **Role (`GRD-044`):** durable per-source extraction record for batch G12. Concept Registry = synthesized workbench; THIS file = underlying detail. **This file exists because the registry row for G12 was over-collapsed (one row); these are the full per-source clusters it compresses.**
> **Batch G12:** EVSRC-075 (IBM McHugh/Olende "What is an AI stack"), 082 (IBM Martin Keen "AI periodic table"), 076 (IBM Martin Keen "LLM vs SLM vs FM"), 078 (IBM Martin Keen "10 use cases for AI agents"). Extracted/re-extracted 2026-06-08 (agents 01c7e905 / 8b75be6e / 60a5b629 / 35bb0fb2).
> **Honest tier: ALL AFFIRM existing clusters — 0 net-new spine, 0 net-new primitives.** OMNI delta = the authority/commit family generic AI taxonomies OMIT. These are Lens-B teaching vocabulary; the value is clean §B articulations (AI-stack layers, agent anatomy, RAG reaction recipe, autonomy ladder, periodic-table families) + the authority-family delta — NOT new architecture. The build concepts they name (agent anatomy, RAG, orchestration grammar, autonomy ladder, memory tiers) have verified homes in the registry: agentic-patterns(071), CNS-orchestration-grammar(077), context-engineering(059), inference-budget(064/065), model-plurality(069), learned-simulators(088), eval-harness(054).

---

# EVSRC-2026-000075 — IBM (What is an AI stack) — 18 clusters

1. **AI stack as layered reference architecture** — AI product = stack (infra→models→data→orchestration→application), each layer determines whether system solves problems or just generates answers. OMNI = governed care/business AI stack, not prompt wrapper. §B/§3.5 (IBM explainer Lens-B comparator), CNS (orchestration = separate layer; AFFIRM scope), Build-OS minimum-stack checklist, capability-topology (app+integrations → §C), KR (data tier w/ stronger governance), security (safety cross-layer). Anchors: 'key components of the AI technology stack' 0:05 · 'all the layers of the AI stack' 2:28. **AFFIRM → vocabulary → watch (§B/§3.5).**
2. **Infrastructure layer (where models run)** — compute/deployment substrate (GPU/AI-hardware; on-prem/cloud/local); deployment constrains viable models. OMNI consumes inference infra behind envelopes (route by latency/cost/risk/residency `AI_state_home`), not moat. §B/§3.5 (dedupe 064/069/076), CNS routing-layer, Build-OS (declare infra assumptions for eval reproducibility), capability-topology (model/API adapters at §C), security (on-prem→residency/inspectability). Anchors: 'infrastructure that that model will run on' 0:48 · 'deployed in one of three ways' 2:58. **AFFIRM → vocabulary → watch.**
3. **Model layer (open/proprietary · size · specialization)** — model = ONE piece; choose open-vs-proprietary, large-vs-small, task-specialized; catalog scale makes selection a routing problem. AFFIRMS model-plurality/speciation (route by task/latency/cost/risk/competence). §B/§3.5 (dedupe 076→064/065/073), CNS (model selection = orchestration decision), Build-OS (name model tier per workflow risk), capability-topology (model APIs = governed endpoints), KR (models ≠ reservoirs), security (open-vs-closed → inspectable harness). Anchors: 'Model is an important layer of the stack' 0:41 · 'open versus proprietary' 3:51. **AFFIRM → vocabulary → watch.**
4. **Model-not-product guardrail** — 'Model is an important layer… but just one piece of the puzzle'; real systems need full stack; product≠model-selection. OMNI is NOT an AI feature or model wrapper — governed stack for care/business work. §B/§3.5 (sharpen §B opening: model≠product), CNS (model generates; CNS+domains commit), Build-OS (Build Entry Gate rejects 'model+UI' w/o stack articulation), capability-topology (§C = integrations/tools not 'the model'), KR (truth in domains/reservoirs not weights), security (safety stack-wide not model-badge). Anchors: 'just one piece of the puzzle' 0:41 · 'do more than just generate answers' 0:12. **AFFIRM → vocabulary (high teaching value) → watch (promote phrasing into §B glossary).**
5. **Data layer (sources · pipelines · preprocessing · vector stores)** — supplements model knowledge via external sources + pipelines + retrieval infra (vector DBs); exists b/c cutoffs + incomplete corpora. Maps to KR family — but OMNI data layer FAR richer (identity/CM/obs/D7/scheduling/commerce/messages/care-obligations/external-evidence/operator-traces). §B/§3.5, CNS (orchestration finds; domains/reservoirs own truth), Build-OS (Evidence Plane + promotion gates = OMNI data-layer pattern), KR PRIMARY HOST (sharpen w/ authority classes FWREG-007), security (pipeline/PII/consent boundaries — OMNI-specific, absent in IBM). Anchors: 'Next is data' 1:08 · 'data sources themselves to supplement' 4:57. **PARTIAL → vocabulary (OMNI authority-label delta) → watch.**
6. **RAG / vector retrieval (generic vs authority-labeled)** — RAG = embed→store→retrieve→augment; **OMNI delta: retrieval must return AUTHORITY-LABELED context** (raw-source/interpreted-evidence/promoted-doctrine/patient-source/clinical-assertion/observation/policy/historical-only); retrieval finds candidates not authority (GRD-042). §B/§3.5 (RAG = Lens-B; OMNI delta = 6th authority/commit family), CNS (typed+provenance-linked context packets), Build-OS (promotion gates: RAG can't bypass evidence→interpretation→promotion), KR PRIMARY sharpening (production-RAG ≠ OMNI reservoirs), security (reject silent promotion of retrieved chunks to clinical truth). Anchors: 'retrieval systems, also known as RAG' 5:18 · 'vectorized into embeddings' 5:25. **AFFIRM on RAG / PARTIAL→sharpen on authority-labeled → vocabulary → watch.**
7. **Knowledge cutoff & domain supplementation** — base models trained to cutoff dates; domain tasks need fresh external data. AFFIRMS freshness/recency first-class across reservoirs; OMNI longitudinal mandate makes cutoff STRUCTURAL. §B/§3.5, CNS (context assembly declares recency per field), Build-OS (Evidence Plane capture dates + promotion state), KR `literature_recency_state`/obs timestamps FWREG-006/007, security (stale context = amplified risk). Anchors: 'models typically have a knowledge cutoff date' 1:14 · 'papers from the past three months' 1:20. **AFFIRM → vocabulary → watch.**
8. **Orchestration layer (plan · decompose · execute · review)** — break input into subtasks, plan, identify data, summarize/answer, optionally review; complex tasks exceed single-prompt. IBM's plain-language CNS: source-event→candidate→context-packet→run→tool-calls→review→policy/domain-check→commit/no-op. 'Model reasons; orchestration coordinates work.' §B/§3.5 (dedupe 075→077/059/087), CNS PRIMARY AFFIRM HOST, Build-OS (loop agents declare orchestration graph), capability-topology (tool calls = §C governed), KR (orchestration queries; doesn't write truth w/o commit gates), security (policy/domain checks = OMNI additions IBM omits). Anchors: 'Next would be the orchestration layer' 1:26 · 'break that user query up into different parts' 1:40. **AFFIRM → vocabulary → watch.**
9. **Agentic feedback loops & LLM self-critique** — orchestration includes review step (LLM critiques own response, initiates feedback loops). AFFIRMS bounded critique/review loops in CNS/Build-OS (eval recipes, driver/sim/critic 049/070) — always subordinate to authority gates + human/domain commit, never autonomous clinical commit. §B/§3.5, CNS (critique → candidates; commit gated), Build-OS (closed-loop eval before promotion; pairs 049/070), security (self-critique ≠ safety case; reject autonomy inflation), future-watch (agenticness spectrum 061/048 still gated). Anchors: 'reviewing, where an LLM can provide its own critique' 6:33 · 'initiate feedback loops to even improve' 6:40. **AFFIRM → vocabulary → watch.**
10. **MCP & evolving orchestration protocols** — orchestration rapidly evolving (MCP, coordination architectures); AFFIRMS MCP as §C integration protocol (commodity wire format) NOT trust (pairs 061 'MCP ≠ trust'). §B/§3.5 (MCP Lens-B, Stripe-idempotency-class wire), CNS (tool invocation transport; authority layer approves), Build-OS (MCP servers in proving grounds w/ sandbox+least-privilege), capability-topology PRIMARY HOST (tool/agent adapter surface), security (MCP gateway/allowlists/shadow-server audit), future-watch (protocol churn). Anchors: 'new protocols like MCP' 6:47 · 'orchestrate increasingly complex tasks' 6:53. **AFFIRM → vocabulary → watch.**
11. **Tool / function calling (execution sub-layer)** — model invokes external capabilities in multi-step completion. Maps to §C GCE: neural proposes tool use; deterministic layers + authority gates commit. AFFIRMS tool-use-as-appendage / capability-addressable (084-24/9). §B/§3.5, CNS (tool calls logged/scoped/consented/audited per run), Build-OS (each loop agent declares tool envelope), capability-topology PRIMARY HOST (capabilities = addressable endpoints), security (least-privilege grants/AI-gateway). Anchors: 'execution, where the model does tool calling' 6:20 · 'function calling' 6:27. **AFFIRM → vocabulary → watch.**
12. **Application layer (interfaces · modalities · usability)** — where users meet system; inputs/outputs, modalities beyond text, usability. Maps to P5 surfaces/projections (provider workbench/patient portal-chat-voice/operator cockpit/agent inbox/evidence review/build cockpit/review queues). Usable AI = interaction design not answer generation. §B/§3.5, CNS (surfaces receive orchestration outputs as proposals), Build-OS (build-agent cockpit/evidence workbench), capability-topology (surface-triggered requests → §C), KR (evidence review reads staging; promotion gated), security (modalities expand attack/consent surface). Anchors: 'Finally is the application layer' 1:59 · 'text in and text out' 2:11. **AFFIRM → vocabulary → watch.**
13. **Revision affordance & citation affordance (trust UX)** — usability needs revisions (edit/inquire) + citations (provenance). OMNI delta: needs provenance/freshness/authority/applicability/adoption-state — citations alone INSUFFICIENT for care (068). §B/§3.5 (citation vs governed-provenance-packet), CNS (outputs carry adoption/review state not just links), Build-OS (evidence surfaces show promotion state), capability-topology (external evidence adapters passthrough citation metadata), KR (authority labels per chunk), security (prevent citation-washing of unpromoted evidence). Anchors: 'ability to do things like revisions' 7:45 · 'citations so that when the user sees' 7:51. **PARTIAL → vocabulary → watch.**
14. **Integrations (bidirectional workflow embedding)** — connect AI to existing tools (systems send inputs + consume/automate outputs into workflows: EHR/email/Slack/billing/scheduling). Core §C/capability-topology mandate — OMNI as substrate coordinates rails; integrations = governed capability edges w/ care-governance semantics (consent/authority/audit/federation). §B/§3.5 (Shopify/RingCentral-class Lens-A → governed exchange), CNS (handoffs = orchestrated commits/events), Build-OS (integration test lanes), capability-topology PRIMARY HOST (adapters/envelopes/delegation), KR (integration events → obs/evidence, not auto-truth), security (OAuth/delegation/non-human-identity/audit). Anchors: "there's also the issue of integrations" 2:16 · 'send inputs to the AI system' 8:12. **AFFIRM → vocabulary → watch.**
15. **Cross-stack tradeoffs: quality · speed · cost · safety** — choices at every layer jointly determine quality/speed/cost/safety; stack thinking required. AFFIRMS inference-budget/cost-discipline + safety-as-architecture (not bolt-on). §B/§3.5 (four-factor frame), CNS (route by latency/cost/risk tier), Build-OS (proving grounds measure quality/speed/cost; care adds safety-case gates), capability-topology (grants weighted by risk class), KR (retrieval cost/freshness tradeoffs), security (safety fourth factor → zero-trust/gateway). Anchors: 'quality, its speed, its cost and its safety' 2:45 · 'across the stack, from the hardware' 2:39. **AFFIRM → vocabulary → watch.**
16. **Build vs buy / managed multi-layer services** — build-from-scratch or use managed-layer services; must understand layer boundaries. OMNI builds governed substrate core, consumes commodity AI infra/models/protocols behind boundaries (generic agent OS/MCP/RAG/routing = commodity by 2030). §B/§3.5 (consume-commodity/own-moat), CNS (managed vendors still subject to authority gates), Build-OS PRIMARY (build-entry criteria; prove ownership of evals/guardrails), capability-topology (managed services = adapters not truth sources), KR (promotion stays in OMNI regardless of hosted-RAG vendor), security (vendor-managed ≠ trusted; zero-trust at boundary). Anchors: 'building a solution from scratch or using solutions' 2:34 · 'manage several of these layers for you as a service' 2:34. **AFFIRM → vocabulary → watch (reject watsonx product chrome).**
17. **IBM Lens-B teaching vocabulary & authority-family omission (OMNI delta)** — IBM explainer = enterprise-friendly shared map (infra→models→data/RAG→orchestration→application/integrations); useful baseline checklist, NOT frontier insight; watsonx-framed (GRD-039). **★Critical OMNI delta: generic AI stack maps OMIT a 6th governance family — identity/consent/source-authority/human-review/policy-envelope/domain-commit/audit/revocation — OMNI must keep explicit atop IBM's five layers.** §B/§3.5 PRIMARY (append IBM explainer series row), CNS (IBM confirms layer, OMNI adds gates), Build-OS ('name your layer + authority gate' checklist), capability-topology (wire formats ≠ governance), KR (authority labeling = OMNI moat over IBM data layer), security (don't import generic enterprise security as sufficient for healthcare). Anchors: 'What is an AI stack and why does it matter' §2 · (Knox) 'do not treat IBM's stack as sufficient for healthcare'. **AFFIRM on layering / PARTIAL on 6th-family naming → vocabulary (delta = spine sharpening hosted elsewhere) → watch (reject spine promotion of IBM claims).**
18. **Minimum-stack agent checklist (Build-OS onboarding scaffold)** — Knox-derived preflight: where does it run? model route? data retrieved? authority labels preserved? orchestration loop? tools allowed? interface? integrations touched? safety/cost/latency tradeoffs? Non-binding Build-OS/agent boot-review scaffold — discipline not architecture. §B/§3.5 cross-ref, CNS (mirrors run lifecycle fields), Build-OS PRIMARY (admission/review template), capability-topology (tools/integrations → §C audit), KR (authority-label column mandatory), security (safety/cost/latency column mandatory). Anchors: Knox checklist §3 + 'clear understanding of how they fit together' 8:37. **ABSENT as named checklist → vocabulary (procedural artifact) → watch (Build-OS template candidate).**

**Net-new primitives (075): NONE confirmed.** All AFFIRM/sharpen existing (CNS-orchestration 077/059/087; KR/authority-labeled-retrieval FWREG-007/068; model-plurality/inference-budget 064/065/073/076; §C/MCP 061+security; Surface/projection plane). Only OMNI-specific sharpening (6th authority/commit family) already captured in registry G12 — not a new primitive from 075. Knox candidate labels (`AI_stack_layer`, `infrastructure_layer`...) = vocabulary tags not promoted primitives. **Reread flags (075):** FWREG-007 contract (clusters 5-6 data/RAG vs authority-labeled retrieval — reopen 5:18–5:47 + 'RAG is not enough'); CNS contract pass (clusters 8-11 orchestration/critique/MCP/tools — 5:53–6:53 alongside 077/059; don't let generic orchestration flatten CNS); §C/capability-topology gate (MCP 6:47 with 061 'MCP ≠ trust'); clinical/evidence surfaces (cluster 13 citations with 068 — citations ≠ clinical adoption); G12 batch dedupe (single §3.5 comparator row, don't replicate per-video spine); watsonx vendor chrome = reject.

---

# EVSRC-2026-000082 — IBM (AI periodic table) — 13 clusters

1. **AI periodic-table taxonomy (rows × families)** — two-axis map: rows = maturity {primitives·compositions·deployment·emerging}; families = {reactive·retrieval·orchestration·validation·models}; unofficial metaphor to decode pitches. OMNI table needs 6th family (authority/commit) IBM omits. §B glossary cross-ref, §3.5 Lens-B comparator (GRD-039), Build-OS optional review lens. Anchors: 'row number 1… primitives' 1:26 · 'no official AI periodic table' 0:32. **ABSENT as frame / PARTIAL substance → vocabulary → watch (glossary; don't canonize IBM layout).**
2. **Reaction grammar (elements combine into predictable reactions)** — value = reaction prediction not memorization; every OMNI AI workflow declares its reaction (components+sequence+gates). Sharpens CNS orchestration grammar(077)/agentic-patterns(071)/intent(058) — adds 'name the reaction' checklist. §B compositional discipline, Build-OS `feature_reaction_spec` review checklist (not schema host), CNS chain-vs-graph (077). Anchors: "periodic tables aren't just for memorization" 12:29 · 'predicting reactions' 12:36. **PARTIAL → vocabulary → watch (Build-OS review gate, not spine).**
3. **Production-RAG reaction (Em→Vx→Rg→Pr→Lg [+Gr])** — embed docs→vector store→retrieve chunks→augment prompt→generate; smart deployments add guardrails. Maps Evidence Plane/KR ingestion→retrieval→draft; OMNI adds authority labels/provenance/freshness/PHI/human-commit. §B/§A (retrieval≠authority GRD-042 AFFIRM), KR (production-RAG vocabulary), CNS (retrieve→summarize→answer chain 077), security (Gr necessary not sufficient), Build-OS ingestion EVRUN pattern. Anchors: 'production RAG' 13:59 · 'add in one more element… guardrails' 13:49. **AFFIRM → vocabulary → watch (OMNI delta = authority layer).**
4. **Agentic-loop reaction (Ag↔Fc, think-act-observe, deployed via Fw)** — goal-driven agent decomposes; function-calling invokes tools; think-act-observe loop until goal; framework = deployment plumbing. Sense/Act loop vocabulary — bounded/gated/never auto-commit. §B/§8 (loop = candidate generator), CNS loop-depth-cap/kill-switch (089), §C/capability-topology (Fc behind envelopes+approval), Build-OS (framework = Lens-B plumbing LangChain/LangGraph 077). Anchors: 'think-act-observe loops' 5:14 · 'agentic reaction' 15:21. **AFFIRM → vocabulary → watch.**
5. **Reactive-family maturity ladder (control→action→autonomy)** — prompt(control)→function-calling(action)→agent(autonomy)→multi-agent(emerging); autonomy ramps with verification (no-eval-no-autonomy + autonomy-graduation 049); each step increases action + attack surface. §B/§A (autonomy-scales-where-verification-scales), CNS `autonomy_level` spectrum, security (autonomy↔attack-surface), Build-OS pattern-maturity-tiers (071). Anchors: 'from control to action to autonomy' 5:47 · 'multi-agent… row number 4 emerging' 10:25. **PARTIAL → vocabulary → watch.**
6. **Retrieval-family memory time-scales (encode·store·bake-in)** — embeddings(encode-meaning)→vector-DB(store-for-search)→fine-tuning(bake-into-weights) = three memory time-scales; fine-tuning ≠ clinical truth; domain commits stay discrete. KR (reservoir vs index vs model-adaptation), §A (fine-tune ≠ adopt clinical memory), §B (adaptation under eval-first gates), CM contract (commits ≠ weights). Anchors: 'three time scales of memory' 9:03 · 'fine tuning… baking memory directly' 8:50. **PARTIAL → vocabulary → watch.**
7. **Orchestration emergence rule + post-framework gap** — orchestration (G3) has no row-1 primitive ('can't orchestrate one thing'); emerges only from composition; row-4 empty emerging-orchestration slot beyond frameworks. OMNI answer = governed CNS runtime (event-driven/durable-runs/authority-packets/policy-gates), NOT 'better LangChain.' §B/§7.6 (CNS > framework plumbing), CNS governed-orchestration-substrate, §3.5 (LangChain/LangGraph Lens-B 077), Build-OS no-op on framework brands. Anchors: 'orchestration only emerges when combining' 7:17 · 'no clear emerging orchestration paradigm yet' 11:26. **PARTIAL/AFFIRM → low-authority-watch → watch (corroborates CNS spine; don't promote from this alone).**
8. **Authority/commit family (★OMNI delta — IBM validation ≠ care authority)** — IBM validation family (guardrails/red-teaming/interpretability) ≠ OMNI authority/governance (identity/consent/source-authority/human-review/policy-envelope/domain-commit/audit/revocation/visibility); generic tables stop at validation; care-grade OMNI needs authority/commit as FIRST-CLASS family. §A/§B (authority family on AI-axis map), §C/capability-topology (commit boundaries), CNS (candidate→gate→commit), KR (retrieval carries authority labels), Identity/RBAC (consent/visibility). Anchors: 'group four… validation' 7:43 · 'interpretability… frontier safety' 11:56. **AFFIRM (v3 authority spine; IBM lacks it) → vocabulary (sharpens spine) → promote (§B glossary delta + 'name authority gate + commit boundary' checklist).**
9. **Guardrails necessary, not sufficient** — production RAG wraps guardrails (runtime filters/schema validation) but wrapping output ≠ fixing bad retrieval/authority/clinical-adoption. §security/AI-gateway (inbound/outbound scan every boundary), §A (guardrails ⊂ validation, ⊄ authority), KR (source provenance before RAG), Build-OS (eval fixtures include injection/leakage). Anchors: 'wraps the whole thing in safety filters' 13:55 · 'leaking sensitive information' 13:59. **AFFIRM → vocabulary → watch.**
10. **Model-route adequacy (thinking model vs small model)** — pitch-review: thinking-model (test-time-compute/CoT) when small/distilled suffices? Reinforces smallest-sufficient-model + escalate-on-uncertainty/consequence/risk. §B+CNS `inference_budget_policy`/`risk_weighted_compute_route` (dedupe 064/065), Build-OS (eval proves sufficiency), §A (frontier reasoning raises candidate quality, never auto-commits). Anchors: 'thinking models… test time compute' 12:16 · 'thinking model when perhaps a small model' 16:17. **AFFIRM → vocabulary → watch.**
11. **Build-readiness gate — feature must name its reaction** — if a feature can't name elements/reaction/missing-safety/over-engineering, it's NOT build-ready. Build-OS architecture-review admission (components/model-route/retrieval/tools/validation/AUTHORITY-gate/commit-boundary), §B (inspectable AI workflows), CNS (no orphan agent loops), capability-topology (published capabilities only 056). Anchors: 'what elements are they using' 16:06 · 'over-engineering the orchestration' 16:17. **PARTIAL → vocabulary → watch (sharpen Build-OS checklist).**
12. **Encyclopedic element inventory (Pr/Em/Lg/Fc/Vx/Rg/Gr/Mm/Ag/Ft/Fw/Rt/Sm/Ma/Sy/In/Th)** — individual technique labels w/ IBM symbols; industry-standard terms, no OMNI-specific insight. §B glossary cross-refs, §3.5 Lens-B technique index (red-teaming→security; synthetic-data→watch; interpretability→diagnostic 070; multimodal→surface affordance), all other homes no-op. Anchors: 'element of Pr for prompting' 2:06 · 'element Ag… agents' 5:14. **AFFIRM (distributed across corpus) → no-op → reject (coverage matrix only).**
13. **IBM vendor framing (watsonx cert promo, unofficial-table disclaimer)** — educational explainer w/ watsonx cert promo; presenter-owned taxonomy, not industry standard. §3.5 (IBM Technology explainer series note), future-watch (classroom-poster metaphor), all architecture homes no-op (GRD-039). Anchors: 'There is no official AI periodic table' 0:38 · 'at least my attempt at it' 12:22. **no-op → reject.**

**Net-new primitives (082): NONE.** `feature_reaction_spec`→Build-OS admission checklist (dedupe 073/054/058; not schema host); `AI_component_map`/`component_family`→no-op glossary; `authority_family`→vocabulary sharpen (binds existing §A/`human_accountable_commit`/candidate→commit — not net-new); `governed_CNS_runtime`→already claimed (087/072/059); production-RAG/agentic-loop reactions→vocabulary labels. **Reread flags (082):** authority-family delta (cluster 8 → §B AI-axis map; reopen §3 'authority as first-class family' + §1 validation-family before v4 §B; reconcile §A spine, don't duplicate security); reaction-grammar checklist (Build-OS `feature_reaction_spec` gate; reopen 12:43–15:21 + Knox flow examples; dedupe 077 graph grammar + 071); G12 IBM batch coherence (re-read w/ 075 AI-stack + 076 model-class + 077 orchestration — collapse duplicate vocabulary; 082 = compositional map layer); guardrails insufficiency (cluster 9 only if clinical-RAG contract — pair 068 + 079 gateway); SKIP element-inventory(12)/watsonx(13)/thinking-vs-small(10) reread (absorbed by 064/065).

---

# EVSRC-2026-000076 — IBM (LLM vs SLM vs FM) — 11 clusters (AFFIRMS inference-budget + model-plurality; 0 net-new)

1. **`task_model_fit` / smallest-sufficient-model escalation** — CNS/runtime chooses model class by task stakes (SLM bounded-pattern, LLM multi-domain-synthesis, frontier only when breadth/uncertainty/agentic-depth); deterministic validators + human gates above all tiers. §B (model-route doctrine), CNS bind `inference_budget_policy`/`risk_weighted_compute_route`/`model_reasoning_escalation`, Build-OS per-route evals+regression gates, capability-topology (route = envelope property), security (escalation by risk not prestige), future-watch (route by competence not labels). Anchors: 'match the model to the task' 13:28 · 'matching capability to need' 13:35. **PARTIAL → vocabulary (AFFIRM) → watch (append to inference-budget + model-plurality §1A).**
2. **`slm_specialist_not_degraded_llm`** — small models = efficient specialists not 'worse LLMs'; well-tuned SLMs match/beat larger on focused high-volume work (classification/routing/tagging/summarization in envelope); OMNI SLM candidates = intake-classify/message-route/metadata-extract/dup-detect/doc-tag/admin-triage. §B speciation vocabulary, CNS SLM-class task registry, Build-OS champion-challenger SLM-vs-LLM, capability-topology (`sparse_capability_network` specialized workers), security (narrow ≠ safe). Anchors: 'efficient specialists' 0:30 · 'match or even beat these bigger models' 2:24. **AFFIRM → vocabulary → watch (merge model-plurality + jagged-intelligence).**
3. **`inference_cost_latency_budget` / frontier-everywhere anti-pattern** — fewer params → less compute/faster/cheaper; frontier-for-everything strategically wrong; routine work must not consume frontier budget. §B (cost/latency first-class routing inputs), CNS `overthinking_guard`/default-cheap-route, Build-OS cost caps + token/compute meters, capability-topology (expensive routes need grant/escalation), security denial-of-wallet, future-watch (energy/compute macro 066/064). Anchors: "Why don't I just use Frontier models for everything?" 3:54 · 'less computation per inference' 5:51. **PARTIAL→AFFIRM → vocabulary → watch (anchor append).**
4. **`deploy_where_data_lives` / on-prem SLM governance route** — sensitive/regulated payloads (PHI/finance) may require on-prem/residency-bound SLM inference; compliance-by-placement not just policy text. Strongest care-relevant slice; AFFIRMS 069 open/self-host + 064 state-placement + §C `AI_state_home`/PHI-residency; pairs 079 residency. §B (model placement = substrate decision), CNS (route includes `AI_state_home`+residency), Build-OS (on-prem route eval+lineage), capability-topology (residency as envelope dimension), security (external-call minimization for PHI). Anchors: 'running an SLM on premise' 6:32 · 'data never leaves that environment' 6:32. **PARTIAL → vocabulary → watch (merge model-plurality/AI-state-placement).**
5. **`slm_classification_routing_pipeline`** — high-volume classify→tag→route = SLM-class (pattern matching/metadata/queue-assignment, not multi-domain reasoning); maps OMNI intake/message/doc routing. §B example route, CNS SLM-worker for Sense-stage triage (candidate labels only), Build-OS classification eval harness, capability-topology (read-only classify vs write/commit split), security (classification output = candidate not authority). Anchors: 'document classification and routing' 4:17 · 'perfect job for a small language model' 5:36. **AFFIRM → vocabulary → watch.**
6. **`llm_multi_source_synthesis`** — LLM-tier when synthesizing multiple sources (billing+config+history) w/ high query variability + cross-domain reasoning; produces candidate understanding not committed truth. Maps patient-message+med-history+appointment+entitlement synthesis. §B (LLM = synthesis worker not authority), CNS multi-source `context_assembly` at LLM route (059), Build-OS synthesis-evals-with-source-grounding, capability-topology (LLM behind read envelopes; commit via domain APIs), security (untrusted-ingest firewall before synthesis). Anchors: 'synthesizes all of that data' 8:12 · 'nuanced reasoning about how concepts relate' 9:50. **AFFIRM → vocabulary → watch.**
7. **`frontier_agentic_escalation` / multi-step investigation with HITL** — frontier tier for multi-step agentic (plan→tool/API→evaluate→adjust across long chains: incident-response/root-cause/cross-domain-seam-failures); TODAY usually co-pilot+guardrails+human-signoff not full autonomy. §B (frontier = escalation tier not default), CNS `model_reasoning_escalation`+loop-depth-cap, Build-OS incident-response eval in production-shaped sandbox, capability-topology (API/restart behind high-trust envelope), security (tool allowlists+kill-switch). Anchors: 'multi-step investigation and execution' 11:50 · 'human signoff' 12:10. **PARTIAL→AFFIRM → vocabulary → watch.**
8. **`model_class_taxonomy_llm_slm_fm` / Lens-B glossary (unstable boundaries)** — LLM=umbrella, SLM=smaller-efficient (<~10B), FM=highest-capability frontier; NOT three rigid ontologies (capabilities+tool-use blur lines). §B glossary, CNS (route enums may reference tier names; competence evals override), Build-OS (model registry metadata tags non-binding). Anchors: 'not really three separate categories' 0:23 · 'LLM, it's really the umbrella term' 0:23. **ABSENT as taxonomy → vocabulary → watch (pair 075 AI-stack).**
9. **`parameter_count_routing_heuristic` — REJECT as routing law** — param counts (tens-of-B/SLM<10B/FM hundreds-of-B) = pedagogical shorthand ONLY; OMNI routes by task/risk/eval not parameter tables; tool-use erodes size=frontier. counter-doctrine only. Anchors: 'tens of billions of parameters' 1:01 · 'plenty of smaller models, they can use tools now' 3:10. **AFFIRM (eval-first > size) → no-op → reject for routing policy (keep as anti-pattern).**
10. **IBM watsonx / product exemplar chrome** — Granite 4.0/Mistral/watsonx-cert = vendor marketing, not OMNI input (GRD-039). **no-op → reject.**
11. **`models_propose_domains_commit` — Knox keeper (cross-source AFFIRM)** — no model class (SLM/LLM/frontier) owns OMNI authority; all tiers produce candidates; domains+humans commit care/commerce truth. §A AFFIRM 'AI proposes/domain commits', CNS orchestration_state ≠ domain_truth, capability-topology (commit via published capabilities), security (bigger model ≠ safer action). Anchors: 'human in the loop here at least today' 12:16 · "most teams today aren't running fully autonomous agents" 12:02. **AFFIRM → vocabulary → watch (bind Trust-posture + HITL-permanence).**

**Net-new primitives (076): NONE.** All fold into `inference_budget_policy`/`risk_weighted_compute_route`/`model_reasoning_escalation` (064/065/073), `AI_model_registry`/`model_route_approval`/`AI_state_home` (069/064/056), `sparse_capability_network` (069+jagged), authority spine (candidate→commit/HITL/state≠truth). DO NOT mint: `model_route_policy`/`small_model_worker`/`frontier_reasoning_worker`/`classification_service`/`incident_response_agent` (aliases/examples). **Reread flags (076):** §B model-route prose (4:05–13:50 + 'smallest sufficient intelligence' — examples stay illustrative not canonized workflow inventory); §C `AI_model_registry`/residency (6:25–6:54 on-prem/governance — PHI path must exceed generic 'finance and healthcare'); pair 075 AI-stack (075 = full stack, 076 = model-tier zoom — dedupe so thesis doesn't double-count IBM explainers). §1A anchor append: '076: match the model to the task 13:28 · running an SLM on premise 6:32 · human signoff 12:10 · not really three separate categories 0:23.'

---

# EVSRC-2026-000078 — IBM (10 use cases for AI agents) — 14 clusters (AFFIRMS autonomy/AOP/front-door; 0 spine, 0 net-new)

1. **Agent loop baseline (stateful reason-plan-act vs one-shot)** — agents maintain state, decompose goals, serial/parallel execution, replan on intermediate results; OMNI loop terminates in action-candidate→policy-gate→domain/human-commit. §B/§8, CNS `loop_agent`/convergence-loops, Build-OS overnight/patrol loops, capability-topology (macro-actions behind loops), UX agent-inbox HITL. Anchors: 'reason and act autonomously to achieve goals' 0:03 · 'maintain state and can break down complex tasks' 0:08. **PARTIAL/AFFIRM → vocabulary → watch (merge Autonomy-loops + Agentic-patterns).**
2. **Five-part agent anatomy (goal→planner→memory→executor→action)** — reusable decomposition; OMNI care-grade insertion: goal-authorization→planner→authority-labeled-memory/context→executor→policy-gate→action-candidate→commit-boundary→trace/audit; IBM 'action' = OMNI pre-commit surface only. §B/§8, CNS (planner/executor roles, orchestration grammar 077), Build-OS (steps map to AOP), §A (gates between executor and commit), domain-contracts (action lands in owning domain API), UX (loop stage + gate state). Anchors: 'start with a goal' 1:42 · 'goal, planner, memory, executor and action' 8:50. **PARTIAL → vocabulary → watch (keeper: 'Agents are loops. OMNI makes those loops governed.').**
3. **Use cases are capability patterns, not domains (anti-'healthcare agent blob')** — IBM vertical list must NOT become OMNI domain agents; extract capability patterns (sensor-driven/RAG-grounded/multi-agent/anomaly/workflow-automation/replanning), route each through CNS + envelopes + domain commit. §B (payload-noun ≠ domain GRD-026), CNS (specialist roles not vertical silos), Build-OS (AOP/BPO wedge filter), capability-topology, domain-contracts (owned seams D7/Obs/CM/Messaging). Anchors: 'follows the same fundamental pattern' 8:47 · 'analyzing lab results and managing prescriptions' 8:01 (warning anchor — too vague). **AFFIRM → vocabulary → watch (reject literal vertical agents).**
4. **Sensor-driven closed loop (IoT/device → plan → actuate → replan)** — continuous Sense→plan→act on changing streams (wearables/home-devices/clinic-sensors/lab-interfaces); IoT = Lens-B analogy for governed device actuation behind envelopes. §8 Sense triggers, CNS event-driven activation, capability-topology/§C (`device_actor`/action-surfaces), domain-contracts (Obs/Messaging), future-watch (robot/device actors 074). Anchors: 'continually updating, planning, execution and action' 3:11 · 'based upon changing sensor data' 3:14. **PARTIAL → low-authority-watch → watch (fold into Physical-agents/Sense-Act).**
5. **RAG-grounded iterative drafting (retrieve→draft→self-critique→revise)** — load fresh docs, chunk/embed into task-scoped memory, retrieve per-section, draft, critique gaps, re-search; OMNI: retrieved text carries authority labels; retrieval ≠ authority. §A (GRD-042), KR, CNS context-assembly (059), Build-OS ingestion pipelines, domain-contracts (D7 artifacts/CM adoption). Anchors: 'retrieval augmented generation or RAG' 4:32 · 'critique its own writing by asking questions' 5:26. **PARTIAL/AFFIRM → vocabulary → watch (dedupe 059/068/071).**
6. **Multi-agent coordination (coordinator + specialists + shared situational map)** — strongest IBM→OMNI analogy: coordinator + parallel specialists read/write shared map, executor proposes actions; OMNI CNS = message/lab/scheduling/commerce/doc/risk specialists over authority-labeled role-scoped context packets (not free-for-all blob). §7.6/§8, CNS (coordinator/specialist roles, `context_field`, graph grammar 077), Build-OS (fleet registry, dedup/conflict), capability-topology, domain-contracts (each specialist owns commit boundary), UX provider-packet/attention-cockpit. Anchors: 'coordinator agent, working with specialist agents' 6:26 · 'shared situational map that all agents can read from and write to' 6:49. **PARTIAL → vocabulary → watch (dedupe 071/077/087).**
7. **Reflection / iterative self-improvement inside loop** — critique output, detect unsupported sections, re-retrieve, adjust; maps Ng reflection pattern under governance. §B, CNS `reflection_loop`/`draft_critique_revise`, Build-OS eval bundles, §A (critique ≠ commit), UX. Anchors: 'self-improving where the agent learns from results' 3:17 · 'If it finds gaps, it goes back to search' 5:37. **AFFIRM → vocabulary → watch (append to Agentic-design-patterns 071).**
8. **Workflow automation / multistep enterprise procedures (HR onboarding archetype)** — long multistep processes w/ enterprise writebacks (Workday/SAP → OMNI staff-onboarding/prior-auth-chase/lab-follow-up/doc-assembly); = AOP territory. CNS `agent_operating_procedure`, Build-OS (process_readiness/accuracy-ladder), §2/§5 BPO wedge, capability-topology (`web_agent_writeback`), domain-contracts. Anchors: 'execute multistep processes, like onboarding new employees' 8:09 · 'automatically integrating with enterprise systems' 8:12. **ABSENT in v3 but already promoted via 057/056 — 078 adds zero new pressure → vocabulary → watch (deduped → AOP cluster).**
9. **Customer-experience / sentiment-adaptive response** — analyze tone to adapt responses; patient anxiety/tone = routing signal not authority mover; front-door + messaging w/ escalation when sentiment/confusion exceeds envelope. §7.7, UX (`front_door_agent`/messaging), CNS escalation, §A (tone adapts, gates don't move), capability-topology. Anchors: 'sentiment analysis' 7:50 · 'analyze customer tone to adjust their responses' 7:50. **PARTIAL → low-authority-watch → watch (dedupe Front-door 063).**
10. **Streaming anomaly detection (fraud archetype → safety/drift flags)** — continuous ingest + anomaly models → flag abnormal patterns (workflow drift/safety/billing anomalies); Sense-layer candidate generation not autonomous enforcement. §8 Sense, CNS risk candidates, §A (flag ≠ commit), security, domain-contracts, Build-OS. Anchors: 'continuously ingesting transaction data' 7:39 · 'anomaly detection models to flag fraud' 7:43. **PARTIAL → vocabulary → watch (fold into Sense/safety; no-op finance vertical).**
11. **IT ops auto-remediation (alert triage→root-cause→script fix)** — parse alerts, identify root cause, execute remediation scripts = Build-OS/loop-agent patrol archetype (089), not clinical path. Build-OS (`loop_agent`/kill-switch), CNS patrol workers, security, UX operator cockpit (058), §8. Anchors: 'automated remediation, parsing thousands of system alerts' 8:21 · 'executing scripts to fix issues' 8:26. **PARTIAL/AFFIRM → vocabulary → watch (dedupe Loops-fleets 089 + workbench 058).**
12. **Dynamic replanning under changing conditions (transportation archetype)** — continuously recalculate plans as conditions change (scheduling reshuffles/care-plan timing/follow-up deferrals); CNS real-time convergence, not logistics SKU. §7.6/§8, CNS (convergence/suppression), domain-contracts (D3 scheduling), UX. Anchors: 'dynamic replanning' 8:37 · 'continuously recalculating optimal routes as conditions change' 8:40. **PARTIAL/AFFIRM → vocabulary → watch (merge CNS dynamic-system 087).**
13. **Vague 'healthcare multi-agent' (lab+Rx coordination) — WARNING ONLY** — IBM names lab-results + prescription subagents w/o domain seams/authority/commit; OMNI must decompose into D7/Obs/CM/Rx/fulfillment/messaging/audit — NOT a 'healthcare agent.' §A (reject), domain-contracts (decomposition mandate), CNS (specialist ownership), clinical safety, Build-OS. Anchors: 'specialized sub agents handle different tasks' 7:58 · 'analyzing lab results and managing prescriptions' 8:01. **AFFIRM → low-authority-watch → reject (watch as warning).**
14. **Generic enterprise verticals (banking/supply-chain/transportation/predictive-demand)** — illustrative breadth; no OMNI mapping beyond C8-C12 patterns. §3.5 color at most. Anchors: 'rapid fire through the remaining seven' 7:31. **no-op → reject (register, don't route).**

**Net-new primitives (078): NONE.** Knox candidates (`agent_goal`/`goal_authorization`/`planner`/`memory_context`/`executor`/`action_candidate`/`policy_gate`/`shared_situational_map`/`specialist_agent`/`coordinator_agent`/`multi_agent_context_packet`/`dynamic_replanning`/`agent_feedback_loop`/`agent_capability_pattern`) all under existing registry pressure: Trust-posture/§A/Autonomy-loops; Agentic-patterns(071); CNS-orchestration(077); Context-engineering(059); CNS-dynamic-system(087); AOP(057/056); Front-door(063); Agent-workbench(058). 078 CONFIRMS taxonomy; doesn't justify new rows/spine. **Reread flags (078):** R078-1 before adding goal→planner→memory→executor→action to §B/CNS prose (reopen 1:42/2:58/8:50 + Knox governed-loop correction; insert authority/commit layers — don't quote IBM 'action' as final commit); R078-2 before disaster-response as CNS analogy (6:26–7:23 + 071 + 087; label disaster Lens-B); R078-3 before any 'healthcare agent'/lab-Rx language (7:54–8:04 + mandatory domain-decomposition check D7/Obs/CM/Rx); R078-4 dedupe glossary vs 071/059/057; R078-5 G12 anchor append (078: 'goal, planner, memory, executor and action' 8:50 · 'shared situational map' 6:49 · 'retrieval augmented generation or RAG' 4:32 → Agentic-patterns/CNS-multi-agent/KR, not standalone spine row).

---

## G12 batch synthesis (preservation note)
All four IBM explainers = **Lens-B teaching vocabulary, AFFIRM-only, 0 net-new spine, 0 net-new primitives.** They restate concepts owned by higher-authority corpus sources (077 orchestration grammar, 071 agentic patterns, 059 context engineering, 064/065 inference-budget, 069 model-plurality, 088 simulators, 054 eval-harness, 049 autonomy-graduation, 057/056 AOP, 063 front-door, 068 clinical-RAG). **The one genuine OMNI sharpening: the authority/commit family (identity/consent/source-authority/human-review/policy-envelope/domain-commit/audit/revocation) that all four IBM taxonomies OMIT** — OMNI's 6th family atop IBM's five layers. Registry G12 row carries this delta; this file preserves the full per-cluster detail the row compresses.