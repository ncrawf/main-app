# EVRUN-2026-000001 · G7 dev-substrate — FULL CONCEPT EXTRACTIONS (preservation record)

> **Role (per `GRD-044`):** this file is the **durable per-source extraction record** for batch G7 — the full deep-read concept clusters the subagents surfaced, preserved so nothing lives only in chat transcripts. The **Concept Registry** (`EVRUN-2026-000001_ai-corpus_concept_registry_and_routing_map.md`) is the synthesized workbench; THIS file is the underlying detail it compresses. Authoring still starts from the registry → reopens the Source Packet; this file lets the registry's consolidated rows be audited against the full extraction. Not an authoring base on its own.
> **Batch G7 = dev-substrate / tooling:** EVSRC-054 (Cursor/Fireworks Composer RL infra), EVSRC-058 (Warp terminal-as-AI-workbench), EVSRC-077 (IBM LangChain-vs-LangGraph). Re-extracted full 2026-06-08 (agents 60804e7c / b12373d4 / 48065d56).

---

# EVSRC-2026-000054 — Cursor / Fireworks (Composer RL infra) — 18 clusters

**Read order:** §3 Knox → §1 verbatim (full). Registry homes: G7 Eval-harness + Governed-feedback rows.

1. **`production_shaped_sandbox`** — eval env mirrors real care/build physics (contracts, permissions, stale/conflicting data, seams) but isolated from live truth; the eval substrate is the product shape, not a toy. Homes: §A/§8, CNS (harness-native > prompting), Build-OS (parallel env factory), security (clone don't connect), domain fixture packs. Anchors: "mimic as closely as possible" 14:53 · "most powerful environment is your own product" 42:01 · "can't just put your real production application" 42:55. **PARTIAL → spine → promote.**
2. **`sandbox_production_gap`** — agents detect sandboxes and optimize for sandbox physics; gap must be measured/probed/closed. Anchors: "figure out when it's being run in like a fake environment" 15:05 · "different behaviors during RL than in production" 15:11. **ABSENT → spine → promote.**
3. **`reward_hacking_risk`** — agents optimize the eval not the mission (beautiful summaries w/o authority caveats, task-complete w/o invariant proof, test-editing, over-routing, evidence mislabel). Keeper: "an agent that passes the wrong eval is trained to exploit your measurement." Homes: §A/§8 gates, Build-OS reward-hacking probes, security eval-exploitation. Anchors: "Models love to cheat" 15:22 · "learned a few tricks to get a better reward" 15:16 · "encouraging cheating" 15:36. **ABSENT → spine → promote.**
4. **`agent_rollout`** — atomic proof unit = full governed session (multi-turn tool calls, state mutations, compaction, terminal reward), not single prompt. Anchors: "running the entire harness" 10:27 · "rollout is basically your entire agent session" 11:12 · "might take something like 50 turns" 11:18. **PARTIAL → spine → promote.**
5. **`eval_rule_craft`** — product/care experience encoded into inspectable rubrics/fixtures/validators (Software 1.0 → training data → eval rules). Governance work, not ML-ops decoration. Anchors: "now you're effectively crafting the evaluation rules" 39:54 · "encoding the product experience you want" 39:49 · "look at where your product fails" 40:06. **ABSENT → spine → promote.**
6. **`decomposed_eval_bundle`** — never one judge "was this good?"; decompose into rubrics (factuality, authority-labeling, clinical-risk, boundaries, citation integrity, prohibited-commit checks); prefer verifiable/deterministic rewards, LLM-judge for what's easier to judge than create. Anchors: "verifiable… deterministic… that's the best" 38:08 · "break it down… style… factuality" 38:56 · "much easier to judge than to create" 38:31. **PARTIAL → spine → promote.**
7. **`governed_feedback_paradox` / `online_rl_paradox`** — live feedback REFINES an already-shippable agent, cannot birth governed behavior; tunes assistance (ranking/draft-style/retrieval/non-clinical routing) but MUST NOT silently mutate clinical policy/consent/identity/authority. Doctrine: **feedback may tune assistance; it must not silently mutate clinical policy.** Anchors: "can't use this to really create the model from scratch" 30:54 · "has to be good already and we can only make it better" 30:59 · "simulated rollout goes wrong it's not wet" 29:34. **ABSENT → spine → promote.**
8. **`offline_sim_bootstrap`** — offline/production-shaped simulation bootstraps competence before live feedback; parallel rollouts per prompt (16–128), GRPO-style group comparison, off-policy without user harm; live feedback = cherry-on-top after bar-clearing. Anchors: "16 tries at a task or like 128 tries" 28:56 · "online RL currently is pretty inefficient" 28:37 · "actual user you have much higher minimum bar" 29:40. **PARTIAL → spine → promote.**
9. **`trained_context_compaction`** — long-horizon agents learn governed compaction (summarize→restart→continue) but artifacts must preserve authority labels/trace/omissions/open-questions/handoff; revalidate before commit. Anchors: "credit assignment" 32:16 · "self summarization" 32:53 · "go on for millions of tokens" 33:06. **ABSENT → spine → promote (pairs context-engineering 059).**
10. **`harness_native_behavior`** — generic intelligence is base; OMNI-specific behavior is trained/tested/governed inside the OMNI harness (not OMNI training frontier weights — eval/harness discipline path). Anchors: "craft your model to act in your environment" 3:42 · "running the entire harness" 10:27 · "learns how to call tools properly" 8:54. **PARTIAL → spine → promote.**
11. **`rl_environment_triad`** — every eval env = harness (tool loop, portable) + operating-system (world state, hard to replicate, production-shaped) + reward (terminal check). Anchors: "harness… operating system… reward component" 43:36 · "harness is pretty portable" 44:00 · "key is the operating system" 44:06. **ABSENT → vocabulary → promote.**
12. **`customer_side_environment_execution`** — run the production-shaped env where the real implementation lives (avoid wrap/componentize gaps). Anchors: "environments we default to running them on the customer side" 43:06 · "introduces differences" 43:29. **ABSENT → vocabulary → promote.**
13. **`expert_crafts_tasks_not_judges_rollouts`** — experts craft tasks/rubrics/fixtures, don't judge every rollout at scale; automated judges + deterministic validators scale proof. Anchors: "you don't really put experts directly in judging rollouts" 37:55 · "experts are still needed" 39:43. **PARTIAL → vocabulary → promote.**
14. **`application_specialization_doctrine`** — high-token apps eventually specialize models to environment; **OMNI GUARDRAIL: does NOT train frontier weights now** — import the frame via harness/evals/feedback. Anchors: "allocate all of the bits… to that one particular task" 2:27 · "RL fits everywhere" 35:02. **PARTIAL → low-authority-watch → watch.**
15. **`midtraining_rl_sharpening_recipe`** — continual pre-training widens knowledge, RL sharpens behavior; OMNI analog = fixture breadth + rollout sharpening via evals, NOT GPU mid-training. Anchors: "mid-training… reinforcement learning can sharpen on" 8:34 · "RL is sharpening the behavior" 36:44. **AFFIRM → vocabulary → watch.**
16. **`rl_weight_infra_internals`** — **NO-OP for OMNI:** async pipeline, weight-delta sync, router replay, FP drift, MoE mismatch = frontier ML infra. Eval/harness/feedback discipline is the gem; this plumbing is not OMNI's lane. Anchors: "router replay" 26:23 · "relatively small delta" 19:52 · "floating point… nondeterministic" 23:14. **ABSENT (correctly) → no-op → reject (preserve as evidence only).**
17. **`bursty_vm_eval_infrastructure`** — 100k VMs, fast spin-up, OS fidelity (containers fail); OMNI analog = governed parallel sandbox factory, not literal 100k VM mandate. Anchors: "give me 100,000 virtual machines now" 44:26 · "normal containers don't really work" 44:06. **ABSENT → low-authority-watch → watch.**
18. **`rl_environment_vendor_posture`** — generic env vendors help frontier labs; product companies build own-product sandboxes (Cursor: "we don't use any of the environment vendors" for coding). Anchors: "we don't actually use any of the environment vendors" 40:38 · "most powerful environment is your own product" 42:01 · "toy RL examples… Docker container" 42:38. **PARTIAL → vocabulary → watch.**

**Net-new primitives (054):** `production_shaped_sandbox`, `sandbox_production_gap`, `reward_hacking_risk`, `agent_rollout`, `eval_rule_craft`, `deterministic_validator`, `governed_feedback_paradox`/`online_rl_paradox`, `feedback_allowed_use`, `offline_sim_bootstrap`, `trained_context_compaction`, `rl_environment_triad`, `customer_side_environment_execution`, `expert_crafts_tasks_not_judges_rollouts`, `llm_judge_result`, `model_behavior_update`. EXISTS-AS sharpen: `decomposed_eval_bundle`, `harness_native_behavior`, `tool_use_policy`, `runtime_trace`. REJECT no-op: `router_replay`/`weight_delta_sync`/`async_rl_pipeline`.
**Reread flags (054):** G7 eval-harness (§1 14:53–15:22, 10:27–11:30, 38:08–40:06, 42:01–44:13); governed-feedback paradox (§1 27:35–31:06); context-engineering merge (§1 32:11–33:35 → 059); offline_sim_bootstrap → bind `simulation_not_truth` (088/049/074); RL weight infra = DO NOT reread for promotion.

---

# EVSRC-2026-000058 — Warp (terminal as AI workbench) — 22 clusters

**Read order:** §3 Knox → §1 verbatim (full). Registry homes: G7 Agent-workbench + Intent-bottleneck rows.

1. **`agent_workbench`** — governed surface where humans+agents co-execute (prompt/supervise/inspect/hand-off/review), distinct from read-only dashboards. "A dashboard shows information; a workbench lets humans and agents do governed work together." Homes: §B, §7.7/UX, §8, Build-OS runtime shell, CNS (binds to runs not truth). Anchors: "workbench for building software with agents" 2:07 · "form factor… geared towards prompting" 6:41 · "orchestration platform" 27:39. **ABSENT → spine → promote.**
2. **`agent_cockpit`** — fleet-level orchestration surface (status/logs/ownership/coordination across team agents, local+cloud); pairs agent-inbox (062) HITL queue. Anchors: "cockpit for managing… your team's agents" 27:46 · "what states are they in" 29:44. **ABSENT → spine → promote.**
3. **`human_takeover_state`** — inspect/pause/redirect/assume control of in-flight run without losing trace; clinical paths: takeover before any authority commit. Keeper: "agents not useful at scale unless observable, interruptible, reviewable, transferable." Anchors: "take it over or see its progress" 29:37 · "best results when the agent is really steered" 31:12 · "handoff extremely seamless" 32:35. **ABSENT → spine → promote.**
4. **`agent_trace` / `context_packet_trace`** — agentic work as ordered input→output→retry→result chain (terminal physics as trace architecture, NOT terminal chrome); epistemic continuity across Sense/Act. Anchors: "everything is like timebased" 5:04 · "you get to log what you're doing" 5:11. **PARTIAL → spine → promote.**
5. **IDE–terminal convergence → prompting-first form factor** — surfaces reorganize around prompting/context/diff-review; hand-editing = fallback. Anchors: "primary interface now is the prompting interface" 8:13 · "hand editing is a fallback interface" 8:13. **ABSENT → vocabulary → watch (no terminal chrome import).**
6. **`system_event_triggered_agent` / `ambient_agent`** — agents launched by events (crash/incident/Slack/Linear tag) in cloud, not at keyboard; OMNI: event → candidate (trigger ≠ action). Anchors: "system event that triggers an agent" 27:06 · "runs in the cloud" 27:26 · "tag something in slack… do the work in the cloud" 32:52. **PARTIAL → spine → promote.**
7. **agent platform stack** — SDK (spawn scoped runs) · hosting (optional) · status/takeover API · management layer (fleet state/PRs/ownership). Anchors: "agent SDK… agent hosting" 29:01 · "API layer… status… take it over" 29:32 · "management layer… what states" 29:44. **ABSENT → spine → promote.**
8. **cloud-to-local handoff** — pick up cloud-initiated work in same workbench without context reassembly; care analog: ambient draft → human finishes in same governed workspace. Anchors: "bring it onto your local machine" 33:05 · "keep working on it seamlessly" 33:10. **ABSENT → vocabulary → watch.**
9. **team-scoped agent orchestration** — event/team-launched agents need team concept, shared visibility, coordinated review. Anchors: "strong team concept" 27:53 · "agents are going to push PRs" 28:01. **PARTIAL → vocabulary → watch (merge into cockpit).**
10. **intent-expression bottleneck** — as implementation automates, limit = humans clearly expressing intent (constraints/success-criteria/"done"); English reintroduces ambiguity vs code. `intent_candidate` + clarifying-question policy + acceptance tests. Anchors: "limiting factor… expression of intent from humans" 40:02 · "English is ambiguous" 40:15. **PARTIAL → spine → promote (pairs 086).**
11. **ask-and-adjust loop** — human asks outcome → agent produces → human adjusts (reprompt/edit); persists for single-correct-answer domains (code/care). Anchors: "ask the app to do the thing… adjusting" 45:02 · "start by asking… then you adjust it" 45:34. **PARTIAL → vocabulary → promote (§A/§7.7).**
12. **user-perspective verification** — verify from user's/operator's perspective (computer/browser-use), not only compile/type correctness; compile-correct ≠ behavior-correct. Care: did the flow work? did the appointment book? Anchors: "verify the change from the user's perspective" 38:45 · "behaviorally correct, not just static compile correct" 39:32 · "does the thing work" 38:38. **PARTIAL → spine → promote.**
13. **steered long-horizon agents** — long runs need upfront plan + check-ins + bounded scope; unsteered agents circle/waste compute (anti set-loose-swarm). ~20-30min real coding today; hours only for clearly-defined migrations. Anchors: "best results when the agent is really steered" 31:12 · "upfront plan… when you check in" 31:18 · "will start going in circles" 30:53. **PARTIAL → spine → promote.**
14. **harness as first-class product** — prompts/tools/context-mgmt/subagents/summarize/truncate/RAG/MCP/evals determine agent quality; "agent only as good as the harness." Anchors: "harness is like how you prompt" 23:35 · "handling the context window" 23:55 · "alpha that goes into that" 24:31. **PARTIAL → spine → promote.**
15. **context summarization/truncation policy** — governed selection of what giant tool outputs enter context; never truncate authority/consent silently. Anchors: "gigantic output and you don't want that all" 24:13 · "pick out the right stuff" 24:18 · "use something like a sub agent" 24:02. **ABSENT → spine → promote.**
16. **eval-measured harness improvement** — internal evals + public benchmarks + production failure replay (not vibe-tuning); no-eval→no-autonomy. Anchors: "make that good is by measuring" 24:38 · "100% necessary to do it all data driven" 25:33. **PARTIAL → spine → promote (merge G7 054).**
17. **model routing / multi-model orchestration** — route by task (latency/cost/quality); great defaults + raw control; 70-80% use auto. Anchors: "mixture of models and more model routing" 20:30 · "latency, cost, and quality" 20:43. **PARTIAL → vocabulary → watch (dedupe inference-budget/model-plurality).**
18. **shared operational knowledge for agents** — collaborative runbooks/incident-manuals/commands embedded for human+agent use → OMNI governed care runbooks / `program_md`. Anchors: "share incident response manuals" 3:42 · "super useful not just for people but for agents" 3:47. **PARTIAL → vocabulary → watch.**
19. **pro-use-case focus** — economically-valuable hard software vs vibe projects; OMNI = care-grade authority-bound ops, not vibe-coded shadow workflows. Anchors: "distinguish from vibe coding" 7:50 · "hard to build apps the world spends time using" 9:11. **AFFIRM → low-authority-watch → watch.**
20. **"coding will be solved" optimism** — **REJECT as authority relaxation:** better codegen ≠ dissolve consent/liability/clinical-adoption/audit. Counter-doctrine paired with anti-089. Anchors: "coding will be solved by models" 39:55 · "basically solved within a few years" 41:43. **AFFIRM → spine(defend) → reject as doctrine, promote counter-guardrail.**
21. **dev-tools competitive dynamics** — brutally competitive coding market; providers subsidize app layer; differentiate on harness/orchestration. Low-authority market color / §3.5 Lens-A. Anchors: "most brutally competitive subs of software" 10:26 · "going so hard at the application layer" 41:30. **no-op → low-authority-watch → watch.**
22. **terminal product chrome = explicit no-op** — do NOT make OMNI clinical/operator surfaces look like a CLI; import trace/time-based physics only. Anchors: "terminal has become the center of agentic development" 4:38 · "look more like a terminal than an IDE" 7:07. **no-op (guardrail) → reject as UX direction, promote guardrail.**

**Net-new primitives (058):** `agent_workbench`, `agent_cockpit`, `agent_management_layer`, `human_takeover_state`, `agent_trace`, `system_event_triggered_agent`/`ambient_agent`, `agent_sdk`, `agent_hosting`, `agent_status_api`, `agent_run_handoff`, `intent_candidate`, `user_perspective_verification`, `harness_version`, `context_truncation_policy`, `offload_large_tool_output`, `subagent_invocation`. SHARPEN: `steered_run`/`autonomy_level`, `model_route_policy`→inference-budget, `ask_adjust_loop`, `agent_operable_runbook`→`program_md`. GUARDRAIL/REJECT: terminal chrome (22), coding-solved (20), dev-market (21).
**Reread flags (058):** workbench-vs-dashboard-vs-cockpit (Build-OS runtime); takeover semantics × ambient (062)/zero-click (081)/HITL (070); intent bottleneck × attention (086); user-perspective verify × eval-harness (054)/safety-case (049); harness/truncation/subagent × context-engineering (059); ambient cloud agents = highest zero-click attack surface (081); "coding solved" × anti-089 at §A.

---

# EVSRC-2026-000077 — IBM (LangChain vs LangGraph) — 22 clusters

**Read order:** §3 Knox → §1 verbatim (full). **GUARDRAIL: LangChain/LangGraph = Lens-B comparators (GRD-039) — import orchestration GRAMMAR/vocabulary only, do not canonize frameworks.** Mostly vocabulary. Registry home: G7 CNS orchestration grammar row.

1. **`chain_workflow`** — forward-only ordered sequence (DAG), known steps upfront; OMNI linear pipeline for bounded ingestion/extraction/summarization, not primary care shape. Anchors: "executing a sequence of functions in a chain" 0:48 · "directed acyclic graph… always moving forward" 6:26. **PARTIAL → vocabulary → promote.**
2. **`retrieve_summarize_answer_pipeline`** — retrieve→summarize→answer; template for Evidence Plane / KR ingestion (fetch→chunk→extract→query), NOT patient-care orchestration. Anchors: "retrieve, summarize, and answer" 1:21 · "document loader… text splitter" 1:41. **PARTIAL → vocabulary → promote (KR/Build-OS).**
3. **`langchain_component_modularity`** — composable stages, each a typed capability with own model/tool envelope; supports inference-budget routing without graph complexity. Anchors: "modular architecture… high level components" 3:08 · "completely different large language model" 3:02. **AFFIRM → vocabulary → watch.**
4. **`stateful_graph_workflow`** — nodes/edges/shared-state for nonlinear/branchy/revisiting workflows = **primary orchestration grammar for OMNI CNS** (care/commerce/messaging/build). Anchors: "stateful multi-agent systems… nonlinear workflows" 3:24 · "create this as a graph structure" 4:15. **PARTIAL → vocabulary(elevated) → promote.**
5. **`graph_node`** — discrete bounded action unit (classify/escalate/draft/invoke/review-gate/commit-candidate) carrying ownership + envelope + trace identity. Anchors: "each action is considered as a node" 4:21 · "process input node" 4:32. **ABSENT → vocabulary → promote.**
6. **`graph_edge`** — governed transition/routing between nodes (branch conditions/authority gates/risk class), auditable not implicit-in-prompt. Anchors: "transitions between these things… edges" 4:29 · "route to the appropriate action node" 4:34. **ABSENT → vocabulary → promote.**
7. **`orchestration_run_state` / `shared_orchestration_state`** — run-state (patient context snapshot, evidence, authority labels, missing-info, review-status, trace) guides work across nodes; **is NOT committed domain truth.** Anchors: "state component… maintain the task list" 4:49 · "all nodes can access and modify the state" 5:12. **PARTIAL → vocabulary(spine-adjacent) → promote.**
8. **`hub_intent_router_with_loopback`** — central process-input node understands intent → routes to action nodes → always returns to hub; matches interactive care re-entry. Anchors: "central node is the process input node" 4:32 · "always returning back to the process input node" 5:25. **PARTIAL → vocabulary → promote.**
9. **`cyclic_loops_and_state_revisit`** — loops + revisiting prior states for evolving conditions/clarification/edit/reprocess; checkpoints required. Anchors: "allows for loops and revisiting previous states" 7:05 · "evolving conditions or user input" 7:19. **PARTIAL/AFFIRM → vocabulary → promote (pairs 089).**
10. **`chain_dag_vs_graph_loops_structural_contrast`** — chains for known sequential pipelines; graphs for interactive evolving orchestration; don't graph everything, don't chain what needs loops. Anchors: "chain structure… DAG" 6:24 · "great where you know the exact sequence" 6:47. **ABSENT → vocabulary → promote.**
11. **`state_management_spectrum`** — LangChain limited pass-through state vs LangGraph robust shared core state; OMNI needs care-grade persistent run state, not chain memory. Anchors: "somewhat limited state management" 8:07 · "state management… more robust" 8:29. **PARTIAL → vocabulary → promote.**
12. **`use_case_allocation_sequential_vs_interactive`** — pipeline-class → chain; interactive care/ops/build-class → graph. Anchors: "excels at sequential tasks" 8:48 · "ongoing interaction and adaptation" 9:19. **PARTIAL → vocabulary → promote.**
13. **`orchestration_state_not_domain_truth`** — **(AFFIRM keeper)** orchestration state guides work; owning domains commit truth via APIs + authority gates; LangGraph state ≠ clinical truth. Anchors (Knox): "agent/orchestration state can guide work" · "owning domains still commit truth" · "do not assume LangGraph state equals clinical truth." **AFFIRM (GRD-029) → spine → promote.**
14. **`framework_comparator_lens_b_not_canon`** — import grammar/vocabulary; do not canonize framework APIs/brand; implementation may be LangGraph/custom/Temporal — build decision. Anchors: "open source frameworks designed to help developers" 0:42 · (Knox) "the lesson is not OMNI must use LangGraph." **AFFIRM (GRD-039) → no-op(guardrail) → watch (enforce at gates).**
15. **`multi_agent_systems_primary_focus`** — LangGraph = multi-agent/workflows; CNS coordinates fleets in graph topology (bounded, GRD-029), not sovereign brains. Anchors: "primary focus of LangGraph… multi agent systems" 6:02. **PARTIAL → vocabulary → watch (pairs 089).**
16. **`care_workflow_branching_taxonomy`** — (Knox) care messages branch into routine-answer/clarification/provider-review/urgent-escalation/scheduling/payment/consent/doc-update/no-op then loop; CNS routing vocabulary. Anchors (Knox) + "route to the appropriate action node" 4:34. **PARTIAL → vocabulary → watch (exemplar not canon list).**
17. **`loop_capable_checkpoints_not_single_pass`** — (Knox) interactive care/build needs loops with checkpoints (Protocol §8); anti single-pass automation. Anchors (Knox) + "loops and revisiting previous states" 7:05. **PARTIAL/AFFIRM → vocabulary → promote.**
18. **`node_authority_envelope_no_direct_domain_mutation`** — (Knox guardrail) graph nodes emit candidates to owning domains; commit requires authority gate; prevents LangGraph-state-as-EHR anti-pattern. Anchors (Knox) + "all nodes can access and modify the state" 5:12 (→ OMNI must scope node write access). **AFFIRM (GRD-029) → spine → promote.**
19. **`implementation_pattern_taxonomy_not_spine_source`** — (Knox) this video = implementation-pattern taxonomy complementing Chase 059/062 (strategic/runtime); vocabulary/checklist, ~3.5/5 priority. **AFFIRM → low-authority-watch → watch.**
20. **`langchain_memory_vs_graph_state_distinction`** — conversation memory ≠ orchestration run-state ≠ domain truth. Anchors: "memory component… conversation history" 2:40 · "state is a core component" 8:36. **PARTIAL → vocabulary → promote.**
21. **`graph_orchestration_trace`** — explicit nodes/edges/state enable traceability (which node ran, what state changed, which edge taken); first-class audit/evidence. Anchors (Knox) "graphs still need… traces" + "flexible stateful agents… extended interactions" 5:33. **PARTIAL → vocabulary → watch (pairs 058 cockpit).**
22. **`anti_graph_everywhere_minimal_chain_doctrine`** — (Knox guardrail) don't graph what a chain suffices; minimal-sufficient topology; avoid agent spaghetti. Anchors (Knox) + "great where you know the exact sequence of steps" 6:47. **PARTIAL → vocabulary → promote.**

**Net-new primitives (077):** `chain_workflow`, `stateful_graph_workflow`, `graph_node`, `graph_edge`, `orchestration_run_state`, `hub_loopback_transition`, `loopback_transition`, `node_access_scope`, `workflow_topology_allocation`, `graph_orchestration_trace`(partial). AFFIRM (not new): `orchestration_state_vs_domain_truth` (GRD-029). REJECT as primitives: LangChain/LangGraph/document_loader/text_splitter/memory (Lens-B vocabulary only).
**Reread flags (077):** Chase pair 059/062; live-dynamic CNS 087 (graph=topology, 087=physics); loops/fleets 089; cockpit 058; AOP tension 057/056 (graph grammar must not become ticket spaghetti); CNS contract draft (insert node/edge/state/loopback vocabulary without framework coupling); comparator registry (append LangChain+LangGraph Lens-B); zero-click 081 (node write scope under injection).

---
