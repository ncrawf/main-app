# v4 — INS-G0 — Patient + Provider Lens · kickoff subagent (VERBATIM PRESERVATION SLOT)

> ## ▶ HOW TO PASTE — read this line only, then scroll to the markers
> Two paste regions below: **PROMPT** then **RESPONSE**. Replace the placeholder line inside each marker pair with your exact text. **Paste raw.** Do not clean, trim, reformat, de-duplicate, fix typos, or strip client UI chrome — chrome is expected and gets annotated *outside* the block afterwards. Nothing else in this file needs editing.
>
> **If this lens returned the patient and provider views as two separate bodies**, paste both inside the response markers in the order you have them and say so — Gate 0 commissioned them as *two lenses with conflicts preserved rather than reconciled*, and that separation must survive.

Document type: `evidence_or_ingestion` (independent commissioned subagent submission, preserved verbatim)
Authority: `analysis_nonbinding` (`D0THES-GRD-036` — capture broad, promotion gated). Binds nothing. Promotes nothing. Not OMNI doctrine.
Status: `awaiting_operator_paste` · `preserved_verbatim` · `not_rewritten` · `not_promoted` · `source_object_not_interpretation`
Domain(s): `insurance_payer_oop` · `care_operating_model` · `architecture_governance` · `cross_cutting`
Lifecycle role: **SOURCE OBJECT.** The exact, unedited assignment and response from the **patient/consumer + provider/institution** subagent commissioned at Insurance Gate-0 kickoff. This file is evidence, not interpretation.
Source-of-truth relationship: authoritative **only** as to what the commissioned subagent actually said, at the time it said it, under the recorded assignment. It is **not** an OMNI architectural position and creates no OMNI truth. Primary repository sources and the Gate-0 / Gate-1b adjudication lineage control.
Supersedes: nothing
Superseded by: none
Manifest action: `add_tier2` **PROPOSED** — catalog row + read-graph evaluation owed at the parent integration transaction, **not landed here**
Review gate: `user_knox_required`

Packet: `v4_INS_G0_kickoff_subagent_verbatim_index_2026-08-08.md` · Parent arc: `INS-G0-MIXEDFIN`
Precedent: `v4_C4_6_gemini_a_blind_submission_verbatim.md` · `v4_C4_2C_task_d_gemini_b_partial_adversary_verbatim.md` · `v4_C4_2B_task_d_opus_builder_p0_verbatim.md` · `v4_INS_G1A_phase_a_independent_derivation_verbatim_2026-08-07.md`

---

## PRESERVATION LAW (binding on every future agent touching this file)

1. **This file is a source object.** The Gate-0 carrier is interpretation. **Never use the carrier, a summary, or any agent's memory to fill a missing byte of this raw.** Doing so is fabricating evidence.
2. **Do not edit, repair, normalize, reformat, summarize, truncate, translate or "clean up" the preserved body.** Errors, hedges, dead ends, formatting artifacts and unsupported claims inside the submission are **part of its evidence status**.
3. **Do not reconcile it** against the Gate-0 carrier, the Gate-1b carrier, or later findings — and in particular **do not average or resolve the patient↔provider conflict.** Gate 0 commissioned these as *"two separate lenses, conflicts preserved rather than reconciled"* and records at `§G.5` item 3 that *"patient vs provider interests genuinely conflict, and softening it would be dishonest."* Flattening that conflict here destroys the finding.
4. **Commentary, classification, correction and freshness notes live outside the verbatim regions** — in the receipts section below, the packet index, or a separate interpretation artifact. Never inside the paste blocks.
5. **External claims made inside this report are not promotion-grade evidence** merely because they are preserved here. Separate *evidence observed*, *inference* and *forecast* before relying on anything. If OMNI later relies on a specific external claim, that claim's **primary source** goes through the Evidence Plane router at that time (`D0THES-GRD-036`).
6. **Client UI chrome is expected** (conversation headers, share/nav lines, export artifacts). It is preserved as pasted and identified in the receipts section — **never deleted from the block**.

---

## Provenance and independence

| Field | Value |
|---|---|
| Lens (Gate-0 `§G.4` ledger) | **E — Patient/consumer + provider/institution** |
| Assignment, as recorded by Gate-0 | *"two separate lenses, conflicts preserved rather than reconciled"* |
| Independence condition | **external sources only** |
| Gate-0 disposition, recorded contemporaneously | *"**Adopted.** The patient/provider conflict analysis (`§I.3`) and the multi-entity TIN/NPI reality are its work."* |
| **Why the raw matters** | The carrier keeps the *conclusion* that patient and provider interests conflict. **Only this raw keeps the lived operating detail** — comprehension, redress and appeal burden, institution-specific economics, multi-entity TIN/NPI reality, and the delegated-behaviour material — that produced it. This is the lens closest to the burden-incidence and administrative-friction work Gate 1b later named as load-bearing (`§10.B.9`). |
| Preserved disagreement | Gate-0 `§G.5` item 3 — *"Patient vs provider interests genuinely conflict, and softening it would be dishonest."* Also item 4: whether appeal automation helps is recorded **unresolved**, with one lens arguing near-free appeals invert the economics of speculative denial and another arguing it redistributes surplus and raises transaction volume. **Both supported. Do not resolve here.** |
| Origin | Insurance Gate-0 kickoff, operator-commissioned subagent |
| Model / thread identity | `operator_supplied_if_known` — recorded at receipt time |
| Recovery route | operator-held local copy, pasted manually into this slot |
| Author receipt | **not sought.** Gate-0 `§G.4` is the contemporaneous receipt; a retrospective re-read would be weaker evidence and risks producing a *new* report. Any later author note is a **dated, append-only interpretation outside the raw**. |

---

## VERBATIM — EXACT ASSIGNMENT / PROMPT (do not edit)

<!-- NICK_VERBATIM_PROMPT_BEGIN -->
PASTE EXACT PROMPT HERE
<!-- NICK_VERBATIM_PROMPT_END -->

---

## VERBATIM — EXACT FINAL RESPONSE (do not edit)

<!-- NICK_VERBATIM_RESPONSE_BEGIN -->
PASTE EXACT RESPONSE HERE
<!-- NICK_VERBATIM_RESPONSE_END -->

---

## Integrity receipts *(completed by the agent AFTER the paste; never inside the blocks above)*

| Field | Value |
|---|---|
| Prompt region — bytes | `pending_paste` |
| Prompt region — SHA-256 | `pending_paste` |
| Response region — bytes | `pending_paste` |
| Response region — SHA-256 | `pending_paste` |
| Client UI chrome present | `pending_paste` |
| Patient and provider views separately identifiable in the body | `pending_paste` |
| External-evidence cutoff, if stated in the body | `pending_paste` |
| Paste completeness confirmed by operator | `pending_paste` |
| Preserved-at commit | `pending_paste` |
