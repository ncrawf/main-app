# v4 — FAI — PRE-0 — Reconciliation Ledger

Document type: `handoff_or_readiness_gate` (disposition ledger)
Authority: `analysis_nonbinding`. **This ledger disposes of findings; it does not adopt them by listing them.**
Status: **`drafted_awaiting_review`** — mechanical draft complete 2026-08-10 from the three frozen carriers. **No plan amendment has been made.**
Domain(s): `architecture_governance` · `cross_cutting`
Lifecycle role: **the anti-appetizer mechanism.** Every PRE-0 finding gets a row and a disposition.
Manifest action: `add_tier2` **PROPOSED** — not landed.
Review gate: `user_knox_required`

> ## ★ THE BINDING RULE
> **G1 and G3 CANNOT CLOSE while any row here is undisposed.**
>
> **Row granularity:** every **materially distinct** architecture claim, omission, counterexample or plan defect gets a row. **Duplicate formulations are grouped — but every source passage is cited.**
>
> **Closure differs by source:** **plan-audit blockers (Agent C) must be resolved BEFORE G0.** **Alternative-design findings (A/B) may flow into G1/G3 with a bounded disposition.**
> This is Gate-1b §15.1 part E — *a named consuming gate that cannot pass without disposing of the finding* — applied to the preflight.

## Disposition vocabulary — fixed in advance
`adopted` · `adopted_narrowed` · `already_present` *(name the OMNI carrier)* · `rejected_with_reason` · `out_of_scope` · `open`

**Two dispositions have hard requirements, because both are how a ledger becomes a graveyard:**
- **`open`** is valid ONLY with **owner + trigger + target gate + blocking/non-blocking status.** "Open with an owner" alone is a parking lot.
- **`out_of_scope`** requires **an actual destination artifact** — not "later" and not "another arc."

**Every row carries four fields beyond its disposition:** `source_ref` (file + section) · `blocking_scope` (blocks G0 / blocks G1 / blocks G3 / blocks G4 / non-blocking) · `owner_or_destination` · `trigger_or_target_gate`.

## The defect rule
**The plan is defective only where it cannot express, evaluate, adopt, reject or disposition a *materially necessary* capability or relationship surfaced by the counter-design.** Blind agents will produce generic enterprise assumptions, vendor-captive shapes, god-ontologies and designs that violate independent-principal physics. **Difference is evidence. Difference is not authority.**

---

## §0 — Draft posture, and what "mechanical" did and did not mean

**Method actually run.** All three carriers read in full at `539a966`. Every A/B row was then checked for **estate presence** by search before disposition, because the instruction "no finding is adopted merely because two models agree" is only enforceable if agreement is tested against what OMNI already has. Estate-presence results are recorded per row and are the basis for `already_present` vs `adopted`.

**Agent C's factual challenges were verified against the repository before being accepted, and C was right on every one checked.** This matters because C challenged claims *I* authored:

| C's challenge | Verified result | Consequence |
|---|---|---|
| The two taxonomies are **not** "mutually invisible" | **C correct.** `00_architecture_artifact_index.md` line 8 explicitly states the binding taxonomy authority remains `00_document_governance_and_taxonomy_2026-05-19` | Charter §4 / Handoff §5 diagnosis is **factually wrong** and must be rewritten |
| Role table has **13** rows, not 12 | **C correct.** 13 | Count corrected |
| "16 contract files" not reproducible | **C correct.** 15 top-level `contracts/*.md` | Count corrected; G3's "16 domain contracts" scope line inherits the error |
| Plan has **no §3.1** despite `§3` committing to it | **C correct.** No `§3.1` exists | Blocker |
| Plan says "Ten acceptance tests" while enumerating **11** | **C correct.** Line 206 header vs rows 1–11 | Blocker (state integrity) |
| Charter §14 still names **execution plan R4** | **C correct.** Lines 185 and 195 | Blocker (state integrity) |
| **G3 lands the exact transaction G4 must prove** | **C correct.** Plan line 200 lands "Reactor and GCE installed into their standard roles · maps migrated"; line 204 makes that same migration G4's proof object | **Hard execution blocker** |

**This is the single most important result of PRE-0.** The arc whose founding diagnosis is *"OMNI re-derives and mis-states its own state"* shipped a packet containing a false estate characterisation, two wrong counts, two stale version fields, a missing normative section, and a gate collision. Agent C found them from a cold read. **The failure mode is not historical; it reproduced inside the corrective arc itself.**

**One G0 obligation discharged during this draft, unrelated to the audit:** both G0 thread carriers were operator-populated (13,190 lines) and moved from `awaiting_operator_population` to `verbatim_preserved` at `539a966`.

---

## §1 — Agent A + B findings — architecture the alternative designs contain
*(model-generated alternative designs. **NOT external comparators** — those are G1 Lane 1 primary sources. A model's reading of FHIR/NIST is not a comparator study.)*

| # | Finding | Agent | `source_ref` | Materially necessary? — **basis, not agreement** | Disposition | `owner_or_destination` | `blocking_scope` | `trigger_or_target_gate` |
|---|---|---|---|---|---|---|---|---|
| **AB-01** | **Shared-mechanism sovereignty disclosure as a required artifact form** — every shared mechanism must state: what it may own · what it may **never** own · how independent-principal authority survives it · its failure boundary · how it is replaced | A + B | A §1.4 (7-component register); B "Shared-mechanism sovereignty ledger" (8 rows) | **YES.** Both produced the *same artifact form* independently under a prompt that asked only for a disclosure. OMNI has GCE but **no required disclosure section**. Adopted for the form, not the agreement | `adopted_narrowed` — adopt the **required section**; adopt **neither** component list | G1 content model | **blocks G1** | G1 content-model authoring |
| **AB-02** | **The loss test** — "if the component vanishes, the system degrades in availability, latency or discovery — **never in authority or correctness**" | A | A §1.3 | **YES.** Estate search: **absent as a named criterion** (sole hit = A's own carrier). A **alone** stated it; B did not. Adopted on merit + absence, explicitly *not* on convergence | `adopted` — law candidate | G1 standard candidate → G3 reconcile vs GCE + Reactor | **blocks G1** | G1 standards authoring |
| **AB-03** | **Minimality budget** — a shared component may answer *"did this happen, in this form, at this time, signed by this key"* and may **never** answer *"is this allowed, is this correct, is this the same, does this bind"* | A | A §13 closing | **YES.** Absent as a stated rule. Sharpest single sentence in either submission; it is the *generalisation* of OMNI's own no-god-domain doctrine into a testable predicate | `adopted` — law candidate; pairs with AB-02 | G1 → G3 | **blocks G1** | G1 standards authoring |
| **AB-04** | **Two-implementation rule** — nothing becomes mandatory until two independently controlled implementations pass the same suite | A | A §1.3, §12 | **CONTESTED.** Absent from estate. But **A itself** names it "the rule most likely to be quietly abandoned" and chronically unfunded (§14). Adopting it here would be adopting an unfunded mandate | `open` — owner **Nick** (business/funding decision, not an architecture decision); trigger = first OMNI rule proposed as **mandatory across independent operators**; target gate **G3**; **non-blocking** for G0/G1 | Nick | non-blocking | first cross-operator mandatory rule |
| **AB-05** | **Local admission — publication ≠ adoption; no forced upgrade.** A principal's adoption/admission record, not the publisher's desired state, determines what is active locally | B + A | B `A14`, §3.6, §5.5, #11, #13; A §1.4 #7 (pull-based adoption, per-instance declared state) | **YES.** This is the direct architectural answer to the operator's standing question — *how does OMNI serve ~1000 instances with customer-specific deviation without forking*. Estate has fleet language; **no adoption/admission object** | `adopted` — adoption/admission becomes a first-class architecture object | G1 change lifecycle → G2 schema → G4 test 8 | **blocks G1** | G1 change-model authoring |
| **AB-06** | **Version skew is a normal operating state, not an incident** | A + B | A §1.4 #7 failure boundary; B §5.5 capability negotiation | **YES**, but it is a corollary of AB-05, not a separate law | `adopted_narrowed` — fold into AB-05's object; do not mint a second law | G1 | **blocks G1** | with AB-05 |
| **AB-07** | **Change capsule** as the unit of architectural change — delta · rationale · impacts · reviews · approvals · **refusals** · conditions · migration · rollout · rollback · evidence | B + A | B §3.1–§3.3, `A12`; A §5 lifecycle | **YES — and it is the constructive fix for C's missing §3.1.** The "architecture change/work-package manifest" C says must be defined already has a candidate shape here | `adopted_narrowed` — **input** to the §3.1 schema; do not adopt B's field list wholesale | G1 → G2 | **blocks G1** | G1; discharges part of C-02 |
| **AB-08** | **Artifact taxonomy granularity — 17 classes (B) vs 5 tiers (A)** | B **vs** A | B §1 `A1`–`A17`; A §3 (five tiers) | **YES, and they disagree.** OMNI currently has 13 roles + a separate governance enum. Two blind designs reached materially different granularities; neither is authority | `open` — owner **G1 author**; trigger = G1 artifact-metamodel authoring; target gate **G1**; **blocks G1 close**. Both are inputs; **adopt neither on sight** | G1 author | **blocks G1** | G1 metamodel authoring |
| **AB-09** | **No rule may live only in a vendor product** — rules live in spec + executable tests; forbidden: "rules that exist only in a vendor UI, prompt, SaaS workflow, proprietary database, or undocumented model behavior" | A + B | B `A6`, §3.6; A §1.3 | Necessary — **and already OMNI law** | `already_present` — carrier: **execution plan G4 acceptance test 11 (substrate independence / DNA test)**. External corroboration only; **do not re-mint** | — | non-blocking | — |
| **AB-10** | **Conformance is an attributed claim** — a conformance result carries issuer · profile · version · environment · exceptions; plural certifiers may coexist | A + B | A §1.4 #6, §9; B §7.1, `A10`, #12 | **PARTIAL.** The *claim shape* is necessary now. The *plural-certifier ecosystem* is premature for OMNI's single-operator posture | `adopted_narrowed` — adopt the claim shape; **do not** adopt plural certification | G1 conformance model; G4 receipts | **blocks G1** | G1 conformance authoring |
| **AB-11** | **Standards posture** — FHIR Consent / Provenance / AuditEvent and W3C PROV are **interchange adapters, not substitutes**; NIST zero-trust ≠ legal or professional authority; IETF RATS / SCITT witness ≠ adjudication | B | B "Standards posture" + refs [1][2][3] | **YES.** This is the operator's explicit question — *at what point does the arc actually look at FHIR / ISO / the named comparators*. It lands in **G1 Lane 1** | `adopted_narrowed` — adopt as **named inputs** to G1 Lane 1. **B's citations are a model's reading and may not be cited as the comparator finding**; G1 goes to primary sources | G1 Lane 1 | **blocks G1** | G1 Lane 1 external pass |
| **AB-12** | **Four planes deliberately not fused** (attribution · interpretation · coordination · governance) — **and the governance plane is self-hosted on the same primitives it governs** | A | A §1.2 | The plane split is already OMNI's (P0–P6). **The self-hosting constraint is the new part** — and it is exactly this arc's own thesis: the architecture-management system should be expressed in the machinery it describes | `adopted_narrowed` — adopt **self-hosting** as a G1 design constraint; plane taxonomy `already_present` (Surface Map header) | G1 | **blocks G1** | G1 operating-model authoring |
| **AB-13** | **Explicit rejections** — no global patient index · no global workflow/orchestration engine · no global semantic layer · no global event graph · no global policy decision point · no universal admin view | A + B | A §1.5; B "Explicit rejections" 1–6 | Necessary — **already OMNI's position** | `already_present` — carriers: `D0THES-GRD-026` (payload-noun ≠ domain) · no-god-domain doctrine · Insurance **Axis 1**. Corroboration only | — | non-blocking | — |
| **AB-14** | **Portability is relationship portability, and is measured by drills** — export without **warrant re-anchoring** and **commitment novation** delivers an archive, not continuity | A + B | A §13.9, §1.3 fork test, §9 exit drills; B §5.7, `A15`, #17 | **YES.** Estate has portability language; **novation + re-anchoring + drill-measurement are absent**. Fork-rights-against-OMNI-governance is a **business** question, not architecture | `adopted_narrowed` — adopt relationship portability + drill proof; **fork rights → Nick** | G1 contract + G4 drill test; fork rights → Nick | **blocks G1** | G1 portability contract |
| **AB-15** | **One statement, one issuer (B) vs joint/collective assertions with multiple signers (A)** — B forbids multi-issuer objects outright; A says a tumour-board decision *has no single author* and forcing one **falsifies the record** | B **vs** A | B #18, planting table; A §13.1 | **YES — genuine planting-time disagreement between two independent blind designs.** This is the highest-value row in the ledger: not a duplicate, not a convergence, an actual fork | `open` — owner **Nick adjudicates** after G1 presents both sides; trigger = G1 information-contract authoring; target gate **G1**; **blocks G1 close** | Nick (adjudication) via G1 | **blocks G1** | G1 information contract |
| **AB-16** | **Refusal is a directed speech act with delivery receipts** — "recorded" ≠ "known-to"; **overridden-by-law is a fourth state that is emphatically not consent**; four states, never defaulting | A | A §13.3, §12 planting | **YES.** OMNI has consent/refusal in Care + Insurance; the **four-state model with delivery receipts and non-defaulting** is sharper and is planting-time | `adopted` — law candidate | G1 → G3 reconcile vs Care consent model | **blocks G1** | G1 authoring |
| **AB-17** | **Silence must not default to consent under time pressure** — and the silence-vs-refusal classification rate is a measurable signal | A | A §9.2, §14 | Necessary, but a member of AB-16's state set plus one metric | `adopted_narrowed` — fold state into AB-16; route the **metric** to G4 | G1 / G4 | **blocks G1** | with AB-16 |
| **AB-18** | **Interest-disclosure slot on assertions** — "cheap now, meaningless if added retroactively" | A | A §12 planting; A §13.6 | **YES as a planting-time claim.** The surrounding firewall doctrine is already OMNI's | `adopted_narrowed` — adopt the planting-time slot; firewall itself `already_present` (`v4_C4_6…` gate **C6** neutrality/incentive firewall + Insurance Gate-2 firewall laws) | G3 reconcile vs C4.6 C6 | **blocks G3** | G3 deep reconcile |
| — | **OMISSIONS the designs declared against themselves** *(A §10 · B §8) — these are the half most likely to be lost* | | | | | | | |
| **AB-19** | **No economic model — whoever pays becomes sovereign regardless of the charter** | A + B | A §10.4 ("probably the most dangerous omission because it defeats the design **silently**"); B §8.8 anti-capture | **YES**, and **owned elsewhere in OMNI**: the Non-Labor Operator-Economics + Counterparty Gate-0 lane exists and is `not_started` | `open` — owner **OPECON lane**; trigger = OPECON activation; target gate **OPECON G0**; **non-blocking** for FAI G0/G1, but **blocks any FAI claim of governance independence** | OPECON lane (`not_started`) | non-blocking to FAI; blocks FAI independence claims | OPECON activation |
| **AB-20** | **Compelled forgetting** — append-only + correction-without-erasure is **legally false**; erasure orders, sealed records, expungement, safety redaction. Requires cryptographic redaction + attested destruction + tombstones, **and cannot be retrofitted onto a hash chain** | A + B | A §10.1; B §8.6, #15 | **YES.** Estate presence confirmed (34 carriers incl. Care capture, Insurance G1b) but **depth unverified**. A's un-retrofittable claim makes this **planting-time**, so "present somewhere in prose" is not sufficient | `open` — owner **G3**; trigger = G3 deep reconcile of correction/retention law; target gate **G3**; **blocks G3 close** | G3 | **blocks G3** | G3 correction/retention reconcile |
| **AB-21** | **Temporal algebra** — four clocks are not enough alone; retroactive warrant revocation × effective-dated correction × clock disagreement interact combinatorially. "Design the temporal algebra before anything else" | A + B | A §10.2; B planting (partial-order time, explicit unknown states) | **Already owned.** C4.5 Temporal Integrity & As-Of Reconstruction Gate-0 charter accepted (`gate_0_charter_accepted`, not promoted; read-graph #9k, `D0THES-DEC-038`) | `already_present` — carrier **C4.5** — **with a live obligation**: C's Test-2 amendment requires as-of resolution to be *acceptance-tested*, which C4.5 has not done | C4.5 (owning arc) + FAI G4 test 2 | **blocks G4** | G4 test authoring |
| **AB-22** | **Identity linkage and reversible un-merge** — no matching model, no wrong-link detection, and **un-merge is catastrophic, common and irreversible in most real systems** | A + B | A §10.3; B §8.2, #3, #14, planting | **YES**, planting-time. Estate presence thin (14 carriers, shallow) | `open` — owner **G3**; trigger = G3 identity/federation reconcile; target gate **G3**; **blocks G3 close** | G3 | **blocks G3** | G3 identity reconcile |
| **AB-23** | **Key and signature lifecycle across decades** — evidence signed in 2031 must verify in 2071; rotation, compromise, dead CAs, deceased signers, post-quantum migration | A + B | A §10.6; B §8.4, planting | **YES** for a 40-year longitudinal record. Estate presence is **ingestion-only** (10 hits, all in evidence clusters `v31`/`v34`/`cluster_v29-v32`) — captured as outside evidence, **never promoted to architecture** | `open` — owner **FWREG**, destination artifact `.cursor/plans/doctrine/future_work_registry.md` (new row owed); trigger = first cryptographic commitment in an OMNI contract; target gate **G3 classify**; **non-blocking** for G0/G1 | FWREG row | non-blocking | first crypto commitment in a contract |
| **AB-24** | **Brownfield / degraded attribution** — most inbound facts arrive with **no verifiable principal at all** (HL7v2, fax, DICOM, portals, phone, paper). Requires an honest degraded-attribution class — *"received by fax from a claimed sender"* — rather than a pipeline that **manufactures a signature to satisfy the schema** | A + B | A §10.8; B §8.5, `A7` connector contract | **YES — and this is the architectural form of the operator's "connect-to-Slack/bank/Gmail/payroll tsunami" concern.** Those connectors are precisely weak sources with no verifiable principal | `adopted_narrowed` — adopt the **degraded-attribution class**; route connector/counterparty depth to the existing external-engagement carrier | G1 information contract; depth → external-engagement lane | **blocks G1** | G1 information contract |
| **AB-25** | **Adversarial principals** — the model assumes honest-but-independent. Real behaviour includes upcoding, coerced consent, sybil operators, fabricated warrants, and **strategic divergence asserted to create delay or leverage**. No cost function for bad-faith assertion | A | A §10.7 | **YES.** Estate: **absent** (sole hit = A's own carrier). OMNI's entire independent-principal model inherits this assumption unexamined | `adopted` — a threat/misuse-case section becomes a required artifact class (B's `A8` supplies the form) | G1 metamodel (`A8`-equivalent) → G4 negative tests | **blocks G1** | G1 metamodel |
| **AB-26** | **Candour penalty** — rigorous attribution increases liability exposure, which rationally reduces candid documentation and pushes reasoning into side channels. **"The better it works as proof, the worse this gets."** A offers **no replacement** and declines to design it away | A | A §10.9, §13.12 | It is **not a capability** — it is a standing falsifier. Estate: absent | `adopted_narrowed` — adopt as a **standing uncertainty + falsifier row**, explicitly **not** as a design requirement. A refused to solve it; OMNI must not pretend to | G1 uncertainty register (destination artifact owed at G1) + G4 falsifier set | **blocks G1** (the register must exist) | G1 |
| **AB-27** | **Cognitive load — the view becomes the truth.** A divergence-preserving record is harder to read; if views are the only usable surface, **views become authoritative in practice and the attribution plane is decorative** | A | A §10.5 ("threatens the entire premise and I have no design for it"); A §11 falsifier | **YES — this is a falsifier of OMNI's own projection law**, not a duplicate of it. `D0THES-DEC-033` says projections own no truth; A supplies the mechanism by which that law *silently fails in practice* | `adopted` — required negative test / falsifier | G4 acceptance set (extends test 7 negative controls) + G1 projection contract | **blocks G4** | G4 test authoring |
| **AB-28** | **Robotics and physical safety** — digital authorisation and audit do not make a physical action safe; **a local safety boundary cannot be added later by audit** | B | B §8.10, §4.7, planting | Estate presence is broad (87 carriers). The **planting-time local-boundary constraint** is the sharp part | `adopted_narrowed` — adopt the planting-time constraint; surrounding material `already_present` (platform loop capture · pre-spine map · late-builder gap register) | G3 classify | **blocks G3** classification completeness | G3 |
| **AB-29** | **Emergency and degraded-mode regime** — partitions, disasters, cyber incidents, staff shortage and emergency powers force action outside ordinary authority evidence | A + B | A §10 context + runners-up; B §8.9, §3.5, §4.6 | **YES.** Estate partial (Reactor degraded operation, CNS) | `open` — owner **G3**; trigger = G3 deep reconcile; target gate **G3**; **blocks G3 close** | G3 | **blocks G3** | G3 |
| **AB-30** | **Jurisdiction-by-jurisdiction principal and authority matrix** — the architecture supplies the *grammar* of authority but not the legal answer for minors, incapacity, public health, court orders, employed professionals | B | B §8.1; B "Uncertainties deliberately left unresolved" | **YES**, and B is right that it must be **profiled, not silently decided** | `adopted_narrowed` — adopt as a **required profile axis** (the architecture must be able to *express* the variation); the legal answers themselves are `out_of_scope` → destination `future_work_registry.md` row | G1 profile model; legal content → FWREG | **blocks G1** | G1 profile model |
| **AB-31** | **Semantic / medical-knowledge drift control** — an attributed mapping can still be dangerously wrong, and mapping volume plus model-generated interpretation can overwhelm review | B | B §8.7, `A5` | **YES.** Distinct from AB-11: this is about *review capacity*, not standards choice | `open` — owner **G3**; trigger = G3 semantic reconcile; target gate **G3**; **non-blocking** for G0/G1 | G3 | non-blocking | G3 semantic reconcile |
| **AB-32** | **A full clinical human-factors and safety programme** — perfect provenance still permits unsafe interfaces, alarm fatigue, misread conflict, dangerous delay | B | B §8.3 | **YES**, and it is the *institutional* form of AB-27 | `out_of_scope` for FAI — destination artifact: **new `future_work_registry.md` row**, cross-referenced from the G1 uncertainty register. Not deferrable to "later"; the row is owed at G1 | FWREG row + G1 register | non-blocking | G1 register authoring |

---

## §2 — Agent A + B inverse answers — where generic design FAILS under independent principals
*(the half that finds what frontier practice gets wrong about healthcare. **A §13 (12 items) + B's 22-item table**, grouped.)*

| # | Assumption that fails | Agent | Does OMNI already avoid it? | Disposition | Carrier |
|---|---|---|---|---|---|
| **INV-01** | **Divergence is symmetric** — all conflicting assertions held as peers | A §13.4 — **A's own self-declared "deepest error in the design"** | **YES — this is OMNI's existing law.** A designed symmetric divergence, then reversed itself to *locus-of-authority typing per proposition class*: "truth is federated but not homeless" | `already_present` — **and this is the strongest corroboration PRE-0 produced.** An agent blind to OMNI reached OMNI's law **by first getting it wrong and self-correcting.** Per the standing rule, that is a reason **not to re-mint**, not a reason to adopt | Insurance Gate-1b **Axis 1** `NO_SHARED_FINANCING_TRUTH_SUBSTRATE` + **Axis 2** · GCE · source-authority law |
| **INV-02** | **The platform can evaluate warrants at runtime** — whether a POA is valid, whether break-glass was justified, whether a clinician acted in scope | A §13.2; B #4, #5 | **PARTIALLY.** OMNI enforces capability checks (`requireCapability`) and Reactor admissibility. **The retroactive adjudication hook is absent** — the ability to mark a past access unauthorised *without rewriting it* | `adopted_narrowed` — adopt (a) "record as **asserted** authority", (b) the **retroactive adjudication hook**. Remainder `already_present` | G3 reconcile vs Reactor admissibility + `lib/auth/capabilities.ts` · **blocks G3** |
| **INV-03** | **A digital signature proves an act was authorised** — it proves control of a signing mechanism, not capacity, scope, consent, representation or purpose | B #4; A §13.2 | **PARTIALLY** — OMNI separates authority from authentication in doctrine, not in a stated law | `adopted_narrowed` — bind signature → actor · issuer principal · authority basis · scope · time · jurisdiction · verifier decision | G1 authority model · **blocks G1** |
| **INV-04** | **A consent engine returns the authoritative permit/deny** | B #5 | **NO explicit position.** Sharp correction: the engine returns **evidence + a recommendation to a named controller**, who issues its own decision and records its basis | `adopted` | G1 authority model · **blocks G1** |
| **INV-05** | **Refusal propagates** — refusal recorded at operator A binds principal B who never received it | A §13.3 | **NO.** Legally wrong as stated; some refusals are lawfully overridden | `adopted` — see **AB-16** | G1 → G3 Care consent reconcile · **blocks G1** |
| **INV-06** | **Commitment state can be shared** — one state machine across principals | A §13.5; B #7, falsifier 2 | **PARTIALLY** — CNS orchestrates; whether it *picks a winner* is unverified | `adopted_narrowed` — no shared cross-principal state machine; the interaction view is a **vector of principal-local states**; mismatch is a named condition routed to humans and **never auto-corrected** | G3 CNS reconcile · **blocks G3** |
| **INV-07** | **A compact/protocol engine can declare a binding transaction exists** — message compatibility ≠ legal formation | B #6 | **NO explicit position.** Correction: label the computed result **protocol completeness only**; require independently signed commitments | `adopted` | G1 interaction model · **blocks G1** |
| **INV-08** | **The platform can enforce separation of clinical meaning from financial interest** — many principals are legitimately both | A §13.6; B #16, falsifier 12 | **YES.** OMNI already chose **detection and visibility over prevention** | `already_present` — corroboration; plus AB-18's planting-time disclosure slot | `v4_C4_6…` gate **C6** neutrality/incentive firewall · Insurance Gate-2 firewall laws |
| **INV-09** | **Machine agents can hold warrants** — liability does not attach to software; a warrant held by an agent is a **liability void** | A §13.7; B #21, falsifier 11 | **PARTIALLY** — Agent Runtime & Harness capture exists; **non-nullable accountable principal is not established** | `adopted_narrowed` — adopt the **non-nullable accountable-principal field** and "agents are instruments, never delegates"; remainder `already_present` | G3 Agent Runtime reconcile · **blocks G3** |
| **INV-10** | **A machine agent's action is the principal's own judgment** — delegation may authorise execution without authorising professional judgment | B #21 | **NO explicit position.** Correction: distinguish proposal · decision support · ministerial execution · delegated commitment, each requiring its own grant | `adopted` | G1 agent model · **blocks G1** |
| **INV-11** | **Correction never erases** — immutability as an absolute is not lawful | A §13.8; B #15 | **PARTIALLY** — see **AB-20** | `adopted_narrowed` — separate the three acts A conflates: **retraction** (I no longer stand behind this) · **redaction** (compelled concealment + attested destruction + tombstone) · **supersession** (a newer claim); relying parties carry an explicit **propagation obligation** | G3 correction/retention reconcile · **blocks G3** |
| **INV-12** | **Portability means data export** | A §13.9; B #17 | **PARTIALLY** — see **AB-14** | `adopted_narrowed` | G1 + G4 drill · **blocks G1** |
| **INV-13** | **A continuity bundle becomes the receiving operator's master record** — the receiver did not issue the statements and cannot assume their authority | B #17 | **NO explicit position** | `adopted` — import as **attributed evidence**; receiving-principal interpretations are created separately | G1 portability contract · **blocks G1** |
| **INV-14** | **The substrate is neutral** — anything defining envelope, addressing, time and conformance **exercises power** | A §13.10 | **NO.** OMNI has not stated a position. A's replacement is sharp: **abandon neutrality as a goal; pursue minimality + exit** | `adopted` — pairs with **AB-03** minimality budget | G1 → G3 · **blocks G1** |
| **INV-15** | **Patients are principals like other principals** — structurally they are the weakest: no legal team, no engineering capacity, no ability to fork. **Every non-captivity mechanism in the design is available to operators and unusable by patients** | A §13.11 | **NO.** OMNI has patient-centred prose; the **inversion** — model any right requiring patient action as **unexercised**, enforce patient rights by **default system behaviour** — is absent | `adopted` — law candidate. Squarely OMNI's declared centre of gravity, and OMNI does not currently state it | G1 → G3 Care reconcile · **blocks G1** |
| **INV-16** | **Rigorous attribution improves the record** | A §13.12 | **NO** — see **AB-26**. A offers no replacement | `adopted_narrowed` — standing falsifier only | G1 uncertainty register · **blocks G1** |
| **INV-17** | **A universal assertion schema, even with an opaque body** — the envelope still imposes an epistemic unit | A §13.1 | **Open** — see **AB-15**, the A-vs-B fork | `open` — owner **Nick** via G1; trigger = G1 information contract; target gate **G1**; **blocks G1 close** | G1 |
| **INV-18** | **One logical operator instance = one authority domain** — an instance hosts patients, clinicians, contractors and external principals with different liabilities | B #1 | **NO explicit position** — and this is load-bearing for the ~1000-instance question | `adopted` — separate **logical instance** from **principal cell**; record authority context per action | G1 deployment/variation model · **blocks G1** |
| **INV-19** | **A principal can maintain "the authority graph"** — authority evidence is disputed, jurisdiction- and time-dependent, and accepted differently by different relying principals | B #2 | **NO** — OMNI has no stated position against a resolved authority graph | `adopted` — attributed authority claims + **verifier-specific acceptance projection**; no globally resolved authority graph | G1 authority model · **blocks G1** |
| **INV-20** | **A shared directory determines who a person or organisation is** — directory bindings are statements by issuers, not identity | B #3 | **PARTIALLY** — see **AB-22** | `adopted_narrowed` — scoped identifiers + **reversible** link assertions; each relying principal records acceptance | G3 identity reconcile · **blocks G3** |
| **INV-21** | **An evidence graph represents the complete case** — no observer is guaranteed to see all lawful, private, offline or disputed statements | B #8 | **NO explicit position.** Correction: every graph is an **observer-scoped projection**; **expose known omissions** | `adopted` — the "expose known omissions" clause is the non-obvious half | G1 projection contract · **blocks G1** |
| **INV-22** | **A semantic mapping normalises multiple principals into one meaning** | B #9 | **YES** in doctrine (no global semantic layer) | `already_present` — corroboration; preserve original coding, mappings are attributed and versioned | `D0THES-GRD-026` + AB-13 rejection list |
| **INV-23** | **Source sovereignty means the source contains the truth** | B #10 | **CRITICAL NEAR-MISS.** OMNI's source-authority law **can be misread exactly this way.** B's correction: sovereignty is authority **for what the source committed or recorded**, *not* for the underlying reality | `adopted` — adopt as an explicit **clarifying constraint on OMNI's own existing law**. This is a finding *about* an OMNI law, not a new law | G1 → G3 source-authority reconcile · **blocks G1** |
| **INV-24** | **A fleet desired state determines which rules are active** | B #11 | **NO** — see **AB-05** | `adopted` — desired state is a **candidate**; local adoption + admission determine activation | G1 fleet model · **blocks G1** |
| **INV-25** | **A conformance certificate establishes compliance** — technical conformance ≠ legal compliance or counterparty acceptance | B #12 | **PARTIALLY** — see **AB-10** | `adopted_narrowed` | G1 conformance model · **blocks G1** |
| **INV-26** | **A federation decision binds dissenting members** | B #13 | **NO explicit position.** Correction: federation governance may publish and may restrict its own services; it **cannot record adoption for a member** | `adopted` | G1 federation model · **blocks G1** |
| **INV-27** | **A global revocation or emergency switch safely controls all parties** | B #14 | **NO** — see **AB-29** | `adopted_narrowed` — scoped revocation + hazard statements; **non-enforcement must be visible** | G3 · **blocks G3** |
| **INV-28** | **An operator-hosted audit log proves what another principal did** — it proves what the **operator observed**, not the principal's intent, authority or source execution | B #20 | **NO explicit position.** Directly constrains OMNI's audit claims | `adopted` | G1 evidence model · **blocks G1** |
| **INV-29** | **A professional and the employing organisation can be one statement** | B #19 | **NO explicit position** | `adopted_narrowed` — permit clinician statement, organisational endorsement, or both, each separately attributable | G1 authority model · **blocks G1** |
| **INV-30** | **A majority governance process can resolve all architecture disputes** — a majority cannot vote away an independently liable principal's refusal, a patient right, or a professional duty | B #22 | **NO** — **and this bears directly on C-01**, the FAI arc's own missing cross-cutting decision authority | `adopted` — publication governance for common artifacts + **independent adoption** for principals; multi-party obligations require the affected principals' commitments. **Constrains how G0 may define its own authority set** | G0 authority set (**with C-01**) + G1 governance model · **blocks G0** |

---

## §3 — Agent C findings — independent plan audit
*(**blockers here close before G0.** C's verdict: `PLAN_SURVIVES_WITH_EXACT_AMENDMENTS`. C inspected the pinned commit `185d473` and its factual challenges were **independently re-verified** — see §0.)*

| # | Finding | `source_ref` | Class | Disposition | `owner_or_destination` | **Blocks G0?** | Patch required? |
|---|---|---|---|---|---|---|---|
| **C-01** | **Cross-cutting architectural decision authority is not instantiated.** The packet defines every participant *except* the party that can accept or reject a cross-cutting architectural decision. G1 can produce an operating model and G3 can reconcile the foundation, but neither becomes accepted architecture without an authority that can adjudicate cross-cutting content **without appropriating domain-owned truth** | C §1.1 | authority | `adopted` — G0 must name the authority set and distinguish proposal · domain-owner approval · cross-cutting approval · repository integration · GitHub administration · Nick/Knox review rights · the disagreement stop. **Integrator remains non-sovereign.** Constrained by **INV-30** | G0 | **YES** | **Amendment 4** |
| **C-02** | **Plan §3 commits to "three DISTINCT objects, three schemas — §3.1" and there is no §3.1.** The file jumps §3 → §3.9. G2 must then build the canonical metadata contract, work-package machinery, generated graph and effective snapshot from a section that does not exist | C §1.2 — **verified: no §3.1** | omission | `adopted` — define the three objects (resource descriptor · change/work-package manifest · immutable effective snapshot) with identity, canonical-vs-generated status, fields, version/temporal semantics, relationships, writer + approval authority, validation, generation rules. **AB-07 supplies a candidate capsule shape** | G0 → plan §3.1 | **YES** | **Amendment 3** |
| **C-03** | **No forward-only / irreversible change class.** "Evolvable" says a change can be "reversed" and test 10 requires rollback — but published identifiers, profile inheritance, externally consumed schemas and historical proof semantics can be **forward-only**. The plan could pass Evolvable by demonstrating a repository rollback **while breaking historical resolution or external consumers** | C §1.3, §4 | irreversibility | `adopted` — add change classes (`reversible` · `reversible_with_migration` · `forward_only` · `compensatable_not_reversible` · `externally_committed`); change "reversed" to "reversed where semantically safe, otherwise superseded, compensated, or advanced through explicit forward migration" | G0 → plan §0.5 + test 10 | **YES** | **Amendment 9** |
| **C-04** | **PRE-0 does not unambiguously preserve Agent C's findings.** Protocol §4 guarantees ledger entry for **Stage-A** submissions; §8 preserves C's verbatim output, but **preservation is not disposition.** A blocking plan defect found by C could remain only in the verbatim file — *recreating the exact anti-appetizer failure the protocol exists to prevent* | C §1.4 | omission | `adopted` — Protocol §4 must require a ledger row for every material **A, B and C** finding; C plan defects close or are explicitly rejected **before G0**. **This ledger discharges the instance; the protocol text is still owed** | G0 → protocol §4 | **YES** | **Amendment 8** |
| **C-05** | **G3 and G4 attempt the same transaction.** G3 lands "Reactor and GCE installed into their standard roles · maps migrated or superseded"; G4 then names *that same migration* as the real end-to-end proof. **The proof transaction no longer exists.** C: "This is a direct execution blocker" | C §3.1 — **verified: plan lines 200 and 204** | sequencing | `adopted` — G3 reconciles, adjudicates, freezes target state and **stages an unmerged migration change set**; G4 runs it through proposal → impact → ownership review → conformance → propagation → integration → post-change resolution, **and only then lands it** | G0 → plan G3/G4 | **YES** | **Amendment 5** |
| **C-06** | **G2 is an implementation lane the packet does not classify as one.** G2 requires JSON Schema validation, CI policy checks, a resource-claim checker, generators, workflows and CODEOWNERS changes — but the charter's non-actions say *no implementation*, and repository boot law requires **Build Entry admission** for implementation lanes. The packet never says whether G0 acceptance substitutes for that admission | C §3.2 | dependency | `adopted` — classify G2 explicitly as an `architecture_operations_tooling` implementation lane requiring a **Build Entry verdict**; or limit G2 to canonical documents + non-executable schema prototypes and move tooling to a later admitted lane | G0 → plan G2 | **YES** | **Amendment 6** |
| **C-07** | **Several G2 outputs are not repository-writable objects.** Editing CODEOWNERS is an ordinary file edit; configuring rulesets, marking checks required and enabling a merge queue are **hosted GitHub settings**. The checkpoint workflow itself states it becomes merge-blocking only when marked required in branch protection — **which the workflow file cannot grant.** The packet names no repository administrator, no settings request, no receipt, and no stop condition when the control cannot be enabled | C §3.3 | authority | `adopted` — split G2 into **repository-authored outputs** and **repository-administration actions**; the latter need a named owner and a receipt recording requested settings, applied settings, evidence and **unapplied controls**. **No gate may claim a protection is active merely because a file exists** | G0 → plan G2 | **YES** | **Amendment 6** |
| **C-08** | **Tool adoption is decided before the evidence gate intended to decide it.** §3 fixes the tool decisions; G1 then runs the comparison but is **forbidden from changing the selection**. C: "select tools; run the comparison; forbid the comparison from changing the selection." The cited inheritance does not repair this — Build OS Step 5 expressly calls its mechanism list a maturity target, **"NOT final design"**, and makes no vendor selection | C §3.4 | acceptance | `adopted` — reclassify §3 selections as **candidate defaults**; G1 must issue adopt / narrow / reject / defer on each | G0 → plan §3 + G1 | **YES** | **Amendment 7** |
| **C-09** | **No gate-output transaction contract.** Gates name activities and verdicts but not, consistently: output carrier · exact writable surfaces · author · approving authority · evidence bundle · accepted-vs-proposed state · stop condition. Most material at **G1**, where a fresh agent must package the metamodel, operations loop, graph semantics, change lifecycle, resolution model and decision matrix **before `/architecture` exists** | C §3.5 | omission | `adopted` — add **one table** to the plan with a row for PRE-0 and G0–G4 carrying those fields. C is explicit that a new process framework is unnecessary | G0 → plan | **YES** | **Amendment 4** |
| **C-10** | **Current-state blockers are real and correctly represented, but must be discharged explicitly.** The live checkpoint still points at the Insurance checkpoint; `PRESPINE-PHASEA-INTEGRATOR` is **vacant**; both boot surfaces agree; the FAI handoff **is not the checkpoint**. No shared control-plane landing is authorised. Missing: a **G0 receipt** proving integrator appointment · freshness/collision check · checkpoint repoint · exact catalog/read-graph changes · first authorised G1 write | C §3.6 | dependency | `adopted` — G0 records the receipt. **"G0 accepted" alone does not establish repository authority** | G0 (Nick appoints) | **YES** | **Amendment 4** |
| **C-11** | **State-integrity defects — the arc reproduced its own founding failure mode.** Charter §14 names "execution plan **R4**" twice while §1 correctly names R5; Handoff §5 restates the full gate sequence **while asserting the plan is the only place it lives**; Plan §§0/5 say "**ten** acceptance tests" while enumerating **eleven** | C §2.4 — **verified: charter lines 185/195; plan line 206 vs rows 1–11** | acceptance | `adopted` — delete the duplicated sequence from the handoff and leave a pointer; correct the version and count fields rather than maintaining another copy | G0 → charter/plan/handoff | **YES** | **Amendment 1** |
| **C-12** | **The taxonomy source diagnosis is factually wrong.** The claim that the two taxonomies "do not reference each other" / are "mutually invisible" **is not supported**: the Artifact Index explicitly points to the document taxonomy and states it remains binding. The role table has **13** rows, not 12. The "**16 contract files**" count did not reproduce — 15 top-level contract files plus five seam contracts | C Amendment 2 — **verified: index line 8; 13 rows; 15 files** | **evidence gap** *(not a plan defect)* | `adopted` — restate as: *the estate has one binding governance-category taxonomy and one ratified architecture-role index; they are linked one-way but are **not modelled as independent passport axes**, and the role index is incomplete for the proposed roles.* Correct both counts; G3's "16 domain contracts" scope line inherits the error | G0 → charter §4 + handoff §5 + plan G3 | **YES** | **Amendment 2** |
| **C-13** | **Test 2 (Applicability) is under-specified** — it does not require deterministic resolution **for an explicit `as_of` point**, nor that ambiguous/contradictory profile-variation-exception combinations **fail closed with a named owner and reason** | C §6 | acceptance | `adopted` — extend test 2. **Pairs with AB-21**: C4.5 owns as-of semantics but has never acceptance-tested them | G0 → plan test 2 | **YES** | **Amendment 9** |
| **C-14** | **Test 10 (Upgrade/rollback) is too narrow** — it tests rollback only, and must cover all change classes including a **forward-only** change that must be superseded or migrated rather than rolled back | C §6 | acceptance | `adopted` — extend test 10 to supersession, compensation, coexistence and forward migration. Same root as **C-03** | G0 → plan test 10 | **YES** | **Amendment 9** |
| **C-15** | **Mandatory G1 study of already-deferred tool families.** Plan §3 defers OpenRewrite/Moderne, Argo CD/GitOps and LangGraph/LangSmith — yet G1 Lane 1 still makes all three part of the **mandatory** external-mechanism pass | C §2.1 | overbuilt | `adopted` — convert to **conditional consult triggers**: OpenRewrite only when a repeatable transformation with a decisive verifier exists · Argo CD only after a deployment substrate makes desired-vs-live reconciliation concrete · LangGraph/LangSmith only when Agent Runtime owns a durable-execution implementation lane | G1 Lane 1 | no | Amendment 7 |
| **C-16** | **Tool-specific adapters at G2** — OPA/Conftest encoding, Backstage entity generation and OpenTelemetry mapping are not needed to satisfy the four roots. The plan itself says these carry mechanism not authority, and **test 11 requires the architecture to survive their removal** — so the tool-neutral models are load-bearing and the adapters are not | C §2.2 | overbuilt | `adopted` — keep vendor-neutral contracts in G2; move each adapter to a triggered task **after** the canonical model survives G3 and G4 | G2 | no | Amendment 7 |
| **C-17** | **Mandatory merge queue at G2** — Build OS permits "merge queue **or** serialized fast-forward" and labels its tooling posture a maturity target. The plan hardens the option into a mandate without demonstrating the concurrency that would require it | C §2.3 | overbuilt | `adopted` — protected serialized integration + required checks is sufficient for G2/G4; add a merge queue when measured queueing pressure requires it | G2 | no | Amendment 7 |
| **C-18** | **Later implementation debt — explicitly *not* plan defects.** The impact engine, effective compiler, propagation engine, fleet reconciliation, runtime observability and real-world fleet proof do not exist. C records that the plan **openly states this** and correctly separates descriptor/CI conformance from later runtime and fleet proof | C "Defect classification" | **implementation debt** | `already_present` — carrier: plan's **two-receipt split** (`architecture_operations_v0_conformance` vs `runtime_and_fleet_proof` = `not_yet_due`). **No patch. Recorded so it is never mistaken for a defect** | — | no | none |
| **C-19** | **Could a fresh agent execute the plan without redesigning it? — "No."** C enumerates ten items the agent would have to design that R5 claims are specified | C §6 | acceptance | `adopted` — this is the **aggregate verdict** of C-01…C-14, not an independent defect. It resolves when they do | G0 | **YES** *(aggregate)* | Amendments 1–9 |

---

## §4 — Verdicts

| Agent | Verdict | Notes |
|---|---|---|
| **A** (blind design, Anthropic) | design delivered; **self-critical half is the higher-value half** | Declared its **own deepest error** (symmetric divergence → locus-of-authority typing) and thereby re-derived OMNI's existing law from a cold start. Declined to solve the candour penalty rather than papering it. Supplied the two sharpest testable predicates in PRE-0: the **loss test** and the **minimality budget** |
| **B** (blind design, OpenAI) | design delivered; **broadest coverage, most mechanism** | 17-class artifact taxonomy, 8-row sovereignty ledger, change capsule, five-part fleet vector, 22-row inverse table. Strongest on **fleet, adoption and deployment** — the ~1000-instance question. Standards posture is a model's reading and must not be cited as comparator evidence |
| **C** (plan audit, OpenAI) | **`PLAN_SURVIVES_WITH_EXACT_AMENDMENTS`** | "I found **no fatal conceptual omission** requiring the foundational arc to be discarded or restarted." Nine bounded amendments. **Every factual challenge C raised was re-verified true**, including three that contradicted claims in the packet |
| **A vs B** | **one genuine unresolved fork** | **AB-15 / INV-17** — one statement/one issuer (B) vs joint multi-signer assertions (A). Planting-time. Unresolved by design, not by omission |

---

## §5 — Reconciliation outcome

| Field | Value |
|---|---|
| Rows total / disposed | **81 / 81** — 32 (§1 A+B) + 30 (§2 inverse) + 19 (§3 C). **Zero undisposed.** Counts machine-verified against the tables, not asserted |
| `adopted` | **39** |
| `adopted_narrowed` | **24** |
| `already_present` | **7** — each names its OMNI carrier (AB-09, AB-13, AB-21, INV-01, INV-08, INV-22, C-18) |
| `open` | **10** — AB-04, AB-08, AB-15, AB-19, AB-20, AB-22, AB-23, AB-29, AB-31, INV-17. **Each verified to carry all four required fields** (owner · trigger · target gate · blocking status) |
| `out_of_scope` | **1** (**AB-32**) — destination artifact named (`future_work_registry.md` row + G1 uncertainty register); **not** "later" |
| `rejected_with_reason` | **0** — no A/B/C finding was rejected. Two were **narrowed to contested/open** rather than adopted (**AB-04** two-implementation rule, **AB-15** issuer fork) because adopting them would have been adoption-by-agreement or adoption-of-an-unfunded-mandate |
| Material omissions found | **14 architecture omissions** (AB-19…AB-32) + **14 blocking C findings** = **13 plan defects** (C-01…C-11, C-13, C-14) + **1 evidence gap** (C-12). **C-19 is the aggregate verdict, not a 15th defect** — 15 rows carry `blocks_G0` |
| Machinery removed or deferred | **C-15 · C-16 · C-17** — already-deferred tool study, G2 vendor adapters, mandatory merge queue. C confirms removal preserves all four roots |
| **Findings adopted on convergence alone** | **ZERO.** Every convergent A+B claim was estate-checked before disposition. Four convergent claims resolved to `already_present` (AB-09, AB-13, INV-01, INV-08) precisely *because* the estate already held them |
| **Findings rejected for conflicting with the plan** | **ZERO.** C-01…C-14 all contradict the plan and all were adopted |
| Amendment cycle used | **one — pending.** Candidate amendments drafted in §6; **the plan has NOT been amended** |
| Third agent required | **no** — C's audit was decisive and independently verifiable; no tie needed breaking |
| **G0 recommendation** | **`G0_NOT_YET_ACCEPTABLE — ONE BOUNDED AMENDMENT CYCLE OWED`** (see §7) |

---

## §6 — Candidate amendments *(drafted, NOT applied)*

C's nine amendments, plus two forced by A/B material that C could not see (C was blind to both submissions).

| # | Amendment | Source rows | Surface |
|---|---|---|---|
| **1** | **Repair state integrity** — Charter §14 `R4`→`R5` (both); Plan "ten"→"eleven" acceptance tests; Handoff §5 duplicated gate sequence → pointer only | C-11 | charter · plan · handoff |
| **2** | **Correct the taxonomy diagnosis** — replace "mutually invisible"; 12→**13** roles; 16→**15** contract files (+5 seam); fix G3's inherited "16 domain contracts" | C-12 | charter §4 · handoff §5 · plan G3 |
| **3** | **Add §3.1** — three machine-readable objects with identity · canonical-vs-generated · fields · version/temporal semantics · relationships · writer + approval authority · validation · generation rules | C-02, AB-07 | plan §3.1 |
| **4** | **Add the G0 authority + gate-output contract** — named cross-cutting steward/authority set · domain-owner rights · integrator rights **and prohibitions** · GitHub-admin owner · gate-by-gate carriers and write sets · acceptance authority · disagreement stop · first authorised G1 action · integrator appointment · freshness/collision receipt · checkpoint repoint. **Constrained by INV-30: a majority may not vote away an independently liable principal's refusal** | C-01, C-09, C-10, INV-30 | charter · plan · G0 receipt |
| **5** | **Re-sequence G3/G4** — G3 reconciles, adjudicates, freezes target state, **stages an unmerged change set**; G4 executes and lands it through the operations loop | C-05 | plan G3 · G4 |
| **6** | **Classify and admit G2 correctly** — executable work = implementation lane requiring Build Entry; split repository-authored outputs from hosted GitHub settings; **administration receipt** before claiming any protection is active | C-06, C-07 | plan G2 |
| **7** | **Let G1 ratify the tool choices** — §3 becomes candidate defaults; G1 issues adopt/narrow/reject/defer per candidate; defer OPA, Backstage, OTel, merge queue, OpenRewrite, Argo CD, LangGraph to named triggers | C-08, C-15, C-16, C-17 | plan §3 · G1 · G2 |
| **8** | **Bind all PRE-0 findings** — Protocol §4 requires a ledger row for every material **A, B and C** finding; C plan defects close or are explicitly rejected before G0 | C-04 | protocol §4 |
| **9** | **Repair Evolvable and its tests** — add change reversibility classes; test 2 gains `as_of` + fail-closed ambiguity; test 10 gains supersession, compensation, coexistence, forward migration | C-03, C-13, C-14, AB-21 | plan §0.5 · tests 2, 10 |
| **10** | **★ NEW — not visible to C.** Add the **shared-mechanism sovereignty disclosure** as a required artifact section, and the **loss test** + **minimality budget** as G1 standard candidates | AB-01, AB-02, AB-03 | plan G1 |
| **11** | **★ NEW — not visible to C.** Add **local admission** (publication ≠ adoption) as a first-class object, and route the **A-vs-B issuer fork** to G1 for adjudication rather than silent resolution | AB-05, AB-15, INV-17, INV-24 | plan G1 · G2 · test 8 |

**Amendments 10 and 11 exist because C was blind to A and B by design.** C audited the plan against the estate; A and B pressured the architecture from outside it. Neither pass alone produces this set — which is the strongest available evidence that splitting PRE-0 into blind design and grounded audit was the right structure.

---

## §7 — G0 recommendation

**`G0_NOT_YET_ACCEPTABLE — ONE BOUNDED AMENDMENT CYCLE OWED`**

**Not a rejection.** C's own verdict is that the arc survives, and I agree: **no fatal conceptual omission was found, and no finding requires restarting or discarding the foundational arc.** The four root requirements (Explicit · Resolvable · Evolvable · Observable) survive intact, and C confirms the proposed removals do not damage them.

**But G0 cannot be accepted at the pinned state**, for reasons that are mechanical rather than conceptual:

1. **Fourteen C findings block G0** — thirteen plan defects (C-01…C-11, C-13, C-14) plus one evidence gap (C-12), with C-19 as their aggregate verdict. They are not stylistic. Two are hard: **G3/G4 perform the same transaction** (C-05), and **the plan references a normative section that does not exist** (C-02). A fresh agent cannot execute either.
2. **The packet contains a false estate claim** (C-12). An arc founded on *"OMNI mis-states its own state"* cannot be accepted while mis-stating the estate in its own charter.
3. **Repository authority is not established** (C-10). The integrator is vacant, the live checkpoint still points at Insurance, and no shared control-plane landing is authorised. **G0 acceptance does not by itself confer that authority.**

**What is now genuinely settled and should not be relitigated:** the arc's necessity, the four roots, the PRE-0 three-agent structure, the gate skeleton PRE-0 → G0 → G1 → G2 → G3 → G4, and the two-receipt split between `architecture_operations_v0_conformance` and `runtime_and_fleet_proof`. C tested all of these and did not break them.

**Sequence recommended:** Knox reviews this ledger → Nick adjudicates **AB-15/INV-17** (the issuer fork) and **AB-04** (two-implementation rule) → **one** amendment cycle applies §6 → G0 acceptance with the C-10 receipt → G1 opens.

**One caution against over-correction.** Eleven amendments on an unstarted arc is close to the ceiling before the plan becomes ceremony — the exact pathology `D0CKPT-GRD-003` warns about. Amendments 1, 2, 3, 5 and 8 are **corrections of defects**. Amendments 4, 9, 10 and 11 **add machinery**. If the cycle produces more than these eleven, that is evidence the plan is being redesigned rather than repaired, and it should stop.

**STOP: `ledger_drafted_awaiting_knox_review_and_nick_adjudication`**
