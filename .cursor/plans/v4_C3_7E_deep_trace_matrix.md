# v4 — C3.7E: Deep-Trace Matrix (access action-loop on the core breakers)

Document type: `analysis` (C3.7 arc — G3, artifact E) · Authority: `analysis_nonbinding` (`GRD-036`)
Status: `populated_G3_pending_review` 2026-06-14. Traces the ~35-row core set (D §3C) through the **access action-loop**; verdicts: **HOLDS** (existing substrate carries) · **EXTEND** (bounded extension of a named contract) · **NET-NEW** (no owner) · **ROUTE-OUT** (belongs to general care substrate, C3.7 subscribes). Reads-with: `v4_C3_7D_scenario_library.md`, `v4_C3_7C_…map.md`.

## §0 The access action-loop (the inner test — 9 questions per breaker)
`Q1 entry` (patient-initiated vs care-event) · `Q2 source-of-truth` (which plane owns the fact? `source_authority_map`) · `Q3 permission rung` (`research_permission_stack`) · `Q4 candidate vs commit` (CNS candidate≠commit) · `Q5 integrity gate` (display/misconception/visibility/inducement/slot-truth) · `Q6 incentive bend?` (firewall: accrual/own/clinic) · `Q7 who may see` (`candidate_visibility_scope` / `knowledge_partition`) · `Q8 honest terminal` (enroll/screen-fail/waitlist/honest-null/standing-match) · `Q9 ladder + owner` (which existing contract owns it; rung→target).

## §1 Firewall cluster — the load-bearing trust claim
- **D-124** (sponsor pays more for faster enroll) → Q6 payment must not touch ranking. → **HOLDS as a policy on D6 `payment_care_firewall` + CNS Network-Governance**; the *display policy* (rank by clinical-fit only) is the **EXTEND** (a new CNS-governance policy object), not a new domain.
- **D-135** (sponsor trial no slots; non-sponsored better fit+slot) → Q5 display-policy + Q6 firewall + Q8 honest-null. → **HOLDS**; proves the firewall must compose with `cohort_slot_state` (the non-sponsored option wins on fit AND availability). Load-bearing: *availability is part of honest display.*
- **D-136 / D-143 / D-146 / D-150** (OMNI's OWN economics) → Q6 self-conflict. → **NET-NEW discipline, not a new object**: the recommendation engine must be **economically-blind + auditable** — a *property/invariant* enforced by CNS Network-Governance + an audit trail (`ai_decision_log`/`cns_decision` from C3.5/C3.6). **The blindness is an architectural invariant, the audit is `trace_lineage` (reuse).** This is the sharpest claim and it HOLDS only if blindness is structural (engine cannot read payment facts), not policy.
- **D-254** (academic trial office suppresses competitor trial; internal accrual lagging) → same firewall under P5. → **HOLDS** — the firewall is **posture-invariant**: clinic-retention (G), sponsor-accrual (I/J), and academic-internal-accrual (P5) are the SAME invariant from three institutional directions. *This is the proof that one substrate serves all five postures.*
- **D-182** (research scan billed to insurance by mistake = double-bill) → Q6 + coverage grid. → **EXTEND D6**: `sponsor_site_contract`/`payment_schedule` must encode the coverage-analysis grid (sponsor XOR insurer per item, CMS NCD 310.1). Reuse `payment_care_firewall`; extend with the billing grid.

## §2 Clinic-retention cluster (P1 viability test)
- **D-091 / D-096 / D-097 / D-101 / D-105** (clinic loses patient to out-referral / wants to hide outside trials / wants matching only for its own trials) → Q6 `clinic_retention ≠ patient_best_pathway`. → **HOLDS via the same firewall + display-policy invariant** (out-referral must surface even when it costs the buyer). **D-105 is the disqualifier test**: "match only my own trials" must be *unrepresentable* in the display policy, not merely discouraged. Verdict: clinic-first is ethically viable **iff** the display policy is economically + retention blind. No new object — an **EXTEND** of the display-policy invariant.

## §3 Pre-consent visibility cluster
- **D-122** (sponsor wants identified pre-screen list) → Q7 BLOCK. → **HOLDS via C3.6 `knowledge_partition` + RBAC + Federation grants** → this IS `candidate_visibility_scope` = an **EXTEND/compose**, not net-new. Pre-consent = aggregate/de-id feasibility only; identified only with legal basis.
- **D-121 / D-087 / D-049 / D-030 / D-250** (aggregate-only / site-wants-all / HIPAA-not-for-sponsor / no-contact-auth-route-to-onc / leak-via-access-logs) → all resolve to `candidate_visibility_scope` + audit. **HOLDS** (compose); D-250 adds an **audit/access-log** obligation (reuse `audit_events`/`trace_lineage`).

## §4 Therapeutic-misconception / honest-null cluster
- **D-015 / D-075 / D-161 / D-203 / D-246** ("no trial / SOC better" · goals-of-care · vulnerable "last hope" · "does the mouse result mean it works" · "you're eligible" before review) → Q5 misconception-guard + Q8 honest-null. → **`honest_null_terminal` = NET-NEW (semantics)**: a first-class non-failure terminal the substrate must represent (never scored as lost accrual); **`recruitment_content_gate`/`therapeutic_misconception_guard` = EXTEND** of Messaging 8-gate + IRB-material + D7. D-246 ("eligible" before human review) → **HOLDS via candidate≠commit** (CNS) — the surface must show "candidate, not eligible yet."
- **D-151** (AI "may help you live longer") → Messaging gate + content gate. **EXTEND** (no new object).

## §5 Care-event trigger + option-preservation cluster (the Nick-surfaced bridge)
- **D-016** (radiology lung-mass → latent watch) → Q1 care-event entry. → **HOLDS as a SUBSCRIBER** on C3.5 P13 proactive-orchestration + P24 signal + CNS Patient-CNS coherence + LI ladder. `research_watch_trigger` = a **policy/subscription, NOT a new domain object.** Confirms the §10 reframe.
- **D-023 / D-215 / D-216 / D-217 / D-218 / D-224** (steroids-before-biopsy · chemo-closes-first-line · radiate-measurable-lesion · steroids-vs-brain-edema-conflict · option-preservation-vs-urgent-care) → these are **ROUTE-OUT**: `trial_option_preservation_signal` is a **general care-quality primitive** (don't take an action that forecloses a better downstream option) belonging to **Clinical Memory + Observation + CNS**, NOT C3.7. C3.7 *subscribes*. **D-217/D-224 prove urgent care ALWAYS wins** — the signal warns, never blocks (candidate≠commit at the care layer).

## §6 Slot / supply / source-authority cluster
- **D-031 / D-045 / D-175 / D-251** (registry "recruiting" but cohort closed · portal vs CTMS disagree · "cohort-closed" ≠ "ineligible" · academic public-page vs trial-office/PI/IRB) → Q2 source-of-truth for "actually open?". → **`cohort_slot_state` / `trial_availability_state` = NET-NEW (a freshness/availability truth)**, governed by the **same source-authority discipline as C3.6 `source_authority_map`** (positional: the *site/PI/CTMS* is the authority on availability, NOT the public registry). D-175 forces the screen-fail-reason distinction (cohort-closed vs ineligible) — a `screen_failure_state` enum (EXTEND).
- **D-230** (vendor score; OMNI requires source-backed rationale) → `match_explanation` must carry criterion-level provenance. **EXTEND** (CNS `cns_decision` + source_authority_map).

## §7 Translational / cross-person cluster (2040 layer)
- **D-196 / D-197** (consent care-not-model · model becomes commercially valuable) → **EXTEND C3.6 `custody_chain` + `chain_of_identity` + D7 consent** → `biospecimen_lineage` + `specimen_use_consent_scope`. NOT re-mint.
- **D-198 / D-210** (model from ANOTHER patient informs this patient / cohort) → **cross-person inference HAZARD** → **HOLDS via `chain_of_identity`** (C3.6) + a labeling rule: **analog/population evidence, NOT "your tumor."** D-210 = the same as C3.5 chain-HH cross-person duty.
- **D-200 / D-203** (organoid suggests sensitivity but SOC better / "does mouse mean it works") → **guardrail HOLDS**: model response = **translational evidence, NOT clinical truth**; routes through `model_to_trial_signal` → `trial_match_candidate` → human review, never patient-facing recommendation. `translational_model_evidence` / `model_to_trial_signal` = **small NET-NEW**, but bounded to research-signal semantics.
- **2040 honesty:** this cluster's readiness = *leave-the-door-open* (chain_of_identity + source_authority_map + P35 already make it expressible); NOT build-ready.

## §8 Seam + yield + anti-middleware + adversarial
- **D-054 / D-120** (pre-screen auth but protocol consent not begun / enrolled→C3.6) → **the C3.6 seam HOLDS**: `research_permission_stack` (EXTEND of D7 consent + Federation grants + RBAC) hands off at protocol informed consent. Clean boundary.
- **D-076 / D-090** (1,000-panel funnel / don't convert every patient to a lead) → **`screening_yield_funnel` = NET-NEW (a `REV-174` Operating-Intelligence projection)**; the anti-spam/anti-lead-gen property is the firewall + display-policy invariant. HOLDS.
- **D-231** (sponsor refuses write-back; OMNI still owns workflow+reconciliation) → **anti-middleware HOLDS via P35 `external_capability` ladder** (C3.5/C3.6). Confirms OMNI ≠ middleware even at low write-access.
- **D-243 / D-244 / D-245** (falsified records · staff games accrual status · AI-hallucinated trial location) → **source-trust + audit**: HOLDS via `source_authority_map` (integrity) + `audit_events` + `ai_decision_log`; D-245 forces a **supply-feed source-trust gate** (EXTEND `trial_supply_feed`). D-244 = accrual-status audit (the firewall's audit side).

## §9 Synthesis — what HOLDS / EXTENDS / is NET-NEW
- **HOLDS (substrate carries, posture-invariant):** the firewall (D6 `payment_care_firewall` + CNS Network-Governance) across ALL five postures (D-124/135/254/096/105); candidate≠commit (D-246/016); knowledge_partition→visibility (D-122); anti-middleware ladder (D-231); the C3.6 seam (D-054/120); cross-person via chain_of_identity (D-198/210); ALCOA/audit via trace_lineage (D-244/250).
- **EXTEND (bounded, named owner):** `trial_option_display_policy`/`match_explanation` (CNS-governance policy + econ-blind invariant); `candidate_visibility_scope` (knowledge_partition+RBAC+Federation); `recruitment_content_gate`/`misconception_guard` (Messaging+D7+IRB); `research_permission_stack` (D7+Federation+RBAC); `sponsor_site_contract`/`payment_schedule`+coverage-grid (D6); `screen_failure_state` enum; `biospecimen_lineage`/`specimen_use_consent_scope` (C3.6 custody_chain+chain_of_identity); `trial_supply_feed` source-trust gate.
- **NET-NEW (small, bounded):** `trial_match_candidate`+`eligibility_gap` (the matching object); `cohort_slot_state`/`trial_availability_state` (freshness/availability truth); `honest_null_terminal` (non-failure terminal semantics); `screening_yield_funnel` (Operating-Intelligence projection); `model_to_trial_signal`/`translational_model_evidence` (bounded research-signal, 2040). **`research_watch_trigger` = SUBSCRIBER, not net-new.**
- **ROUTE-OUT (general care substrate, C3.7 subscribes — Nick's point):** `line_of_therapy_state`, `molecular_readiness_state`/`tissue_availability_state`, `trial_option_preservation_signal`, recurrence/tumor-board/goals-of-care, `trial_preference_profile`. **NOT C3.7-owned.**
- **The economically-blind recommendation engine is the load-bearing invariant** — it holds across clinic/patient/site/sponsor/academic postures (D-136/143/146/150/254), which is the proof of posture-invariance.

## Stop / authority
- `analysis_nonbinding` (`GRD-036`); no contract edits. Next: **F** (disposition vs 15 contracts + key-question verdicts), then STOP for review before G4. Standing flag: git identity unset.
