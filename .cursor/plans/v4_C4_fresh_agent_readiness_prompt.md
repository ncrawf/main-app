# v4 — C4 Fresh-Agent Readiness Prompt + Gate (pass BEFORE any v4 spine prose)

Document type: `plan_or_roadmap` (C4 named gate #5 — the comprehension gate) · Authority: `analysis_nonbinding` (`GRD-036`) — the gate definition + the prompt to boot the fresh v4 authoring agent. **No spine prose here.**
Status: `accepted` 2026-06-15 (Nick + Knox). C4 runway package. Operationalizes the **C4 Author Readiness Note** named in `v4_C4_readiness_bridge.md` §15. **Mirrors the C3 comprehension gate** — this is what makes the mid-stream agent switch safe.

## §0 Why this gate exists
A fresh v4 authoring agent boots without this conversation's context. Before it writes ANY spine prose it must PROVE — in a short Readiness Note reviewed by Nick (+ Knox) — that it understands the frame, the source base, the method, and the anti-flatten/anti-hijack rules. **Fail = a "beautiful framework" with the right names but lost depth / wrong frame → back to the note.** (Same failure the project has hit repeatedly.)

## §1 Boot / read-first (the fresh agent loads these before writing the Note)
- **AGENTS.md** + the current checkpoint (`HANDOFF_2026-06-14_…pre_c4.md`) + the HOME controlling plan banner (boot-freshness triad must agree).
- **The source base = `v4_C2_source_base_declaration.md`** (the canonical 9-category estate; CLOSED/current; **incl. the video corpus — actively MINE it**).
- **Method:** C3 ratification note · C3.1 lens audit · **C4.0 Depth Protocol** · this runway package (`v4_C4_spine_authoring_plan` · `…spine_shape_plan` · `…thesis_synthesis_plan` · `…thesis_shape_plan`).
- **Frame:** ORIENTATION-2026-06-10 §2 (execution substrate) + v3 integrated spine §0 + v2 preservation source.
- **Pressure inputs (via their artifact-use maps, NOT as the frame):** C3.5 `G4 §11` · C3.6 `G §8.5` · C3.7 `G §7`; **REV-184** — §0 is **canonical for claims/spine-lines**; §1–§R3.x are **derivation/evidence** (consult when building a C4.0 Depth Packet for the governed-resolution section; never resurrect superseded Round-1 clinical-only framing).
- **`08`** REV-179…199 (REV-184 CLOSED; others open inputs; **191–194 = C4-pre bet-check · 195–198 = thin-spots/reframed · 199 = Reflexive Build Substrate / Open-Tools-Closed-Commit / Build-Alpha-Firewall**). **Knowledge Reservoirs: consume `doctrine/cns_and_knowledge_reservoirs_frontier_2026-06-06.md` + resolve its 6 open Qs at §8 — depth exists, don't re-derive.**

## §2 The Readiness Note the fresh agent MUST produce (then Nick + Knox review)
The agent writes a short note demonstrating, in its OWN words:
1. **What "the source base" is** — the full C2 estate (name it); confirm it includes + that the agent will actively MINE the video corpus (registry/§3 reads), not just cite §2A.
2. **The broad v4 frame** — governed care/business **execution substrate** atop v2/v3 domain physics; Sense/Decide/Act/Prove-Learn loops + authority gates; AI cross-cutting axis; routing; Intelligence Foundry; connector governance; security/assurance; Build-OS/agent-harness; federation-boundary; repo-as-OS; OMNI-Core vs Federation-Network vs layered-CNS. **Stated WITHOUT collapsing into the one shiny action-substrate insight.**
3. **The method** — spine-first → per-section worktable → bottom-up pressure (contracts/control-planes/registry) → bidirectional reconciliation → tension-register adjudication → open-review → timeless prose; C4.0 Depth Packets.
4. **The pressure arcs are INPUTS, not the frame** — explicitly: C3.5/C3.6/C3.7/REV-184 sharpen sections; they are NOT the section hierarchy, the frame, or the source base. There is NO hospital/oncology Part.
5. **REV-184's 7 spine lines** (carry into the Decide/AI/authority sections; field-set/state-machine = C5) — recited correctly from §0 (canonical for claims; the derivation rounds are evidence for Depth Packets, not a source of superseded framing).
6. **What it will NOT do** — no contracts, no schemas, no repo restructure, no spine prose before this note passes; no journey/changelog prose; no cherry-picked guardrails; no subset source base.
7. **The C3.1 covered-thin families + the acceptance gate** it will be held to.

## §3 Pass / fail criteria (Nick + Knox)
- **PASS** = the Note proves frame + source base + method + anti-hijack/anti-flatten understanding, in the agent's own words, without latest-insight hijack or arc-as-frame collapse.
- **FAIL** (→ back to the Note) = collapses to "just the videos" / "just the system map" / "hospital+oncology synthesis"; recites names without depth; treats REV-184 as a clinical-only or still-pending item; can't state the full source base; proposes prose/contracts before the gate.

## §4 Failure modes this gate prevents (from `spine_authoring_plan` §5)
latest-insight hijack · video-only/system-map-only authoring · Epic/action-lane overfocus · **pressure arcs becoming the frame** · repo-as-OS/Build-OS as the product · generic AI-agent-company framing · dated journey prose · flattened lenses · cherry-picked guardrails · subset/ambiguous source base.

## Stop / authority
- `analysis_nonbinding`; the gate definition + boot prompt. **No spine prose authored until a fresh agent passes this gate (Nick + Knox).** Owed: catalog/read-graph. Completes the C4 runway package (5/5). STOP for trifecta review of the package. Standing flag: git identity unset.
