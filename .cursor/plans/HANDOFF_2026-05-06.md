# HANDOFF — 2026-05-06 (mid-Phase-4 / pre-apply)

**Read this first when picking up where this session left off.** Then read the binding source-of-truth files listed in §6 below before touching any code.

This snapshot was written at the end of a long Cursor chat session that hit an IDE bug (MCP approval bubble timing out after many turns). Everything is safely committed; nothing is lost. This file captures the live state + decisions + DO-NOT-re-litigate list so the next agent doesn't drift.

---

## 1. Live state (as of commit `4af4daa`)

| Phase | What it built | Status | Last commit |
|---|---|---|---|
| 4A | `record_intake_emissions_batch` orchestrator (SECURITY DEFINER, 21 target branches) + `writeEmissions` TS wrapper + 21 per-target write handlers + `recordIntakeResponse` + smoke test | ✅ shipped | `5423f56` |
| 4B-arch | System map amendments (5 new Sections 1R/1S/1T/1U/1V + primitives addendum + reconciliation pointers) + companion doc + plan/critique log snapshot | ✅ shipped | `7047eb9` (+ `b63bf68` snapshot save) |
| 4C-pre | `orgs` + `brands` tables; helpers `current_org_id()` + `current_data_env()`; primitives + tenancy columns on canonical tables; orchestrator v2 with cross-org rejection | ✅ shipped | `61d7c09` |
| 4C-pre source-wiring fix | Orchestrator v3 with `source_kind` + `source_id` auto-defaulting on 7 non-clinical INSERT branches | ✅ shipped | `35cd03f` |
| 4C-runtime | `resolveEmissions()` WRITE-path resolver — pure function, 21/21 tests green | ✅ shipped | `3888496` |
| 4D | Section 1O artifact pipeline: `patient_document_routing` manifest + `external_clinical_documents` + `payer_eligibility_documents` + `route_patient_document` RPC + canonical `patient_documents` storage bucket; 91/91 tests green | ✅ shipped | `6d88b91` |
| 4E | Section 1G.3 outbound jobs reconciliation: rename `job_type → kind`; expand status enum (4 → 8); 25 new columns; `outbound_job_dispatches` sub-table; `enqueue_outbound_job` + `pick_next_outbound_job` + `mark_outbound_job_dispatch` RPCs; structural `data_environment` dispatch gate; 78/78 tests green | ✅ shipped | `90513e1` |
| 4E bug fix | `enqueue_outbound_job` COMMENT/GRANT had 28-param signature for 27-param function; removed extra `text`. Caught during pre-apply review | ✅ shipped | `4af4daa` |
| **NEXT: APPLY** | All 6 new migrations to Supabase project `sxblfnokjtxltbhbthep` + run smoke test | 🔴 PENDING | — |
| 4F | Event_type catalog completion (1H + 1Q.7) + CI lint typed registry | not started | — |
| 4G | Section 1R search adapter (`searchEntities`) + first staff-search surface | not started | — |
| 4H | Section 1Q rules + templates engine runtime + Section 1V governance/RTBF runtime | not started | — |
| defer | Section 1S streaming runtime, Section 1T embedding tables, materialized views | deferred | — |

## 2. The 6 unapplied migrations (Supabase project `sxblfnokjtxltbhbthep`)

```
20260504120000_intake_foundation_v1.sql                          (Phase 3 — 19 tables)
20260505120000_intake_emission_orchestrator_v1.sql               (Phase 4A — orchestrator v1)
20260506120000_phase_4c_pre_primitives_tenancy.sql               (Phase 4C-pre — orchestrator v2 + orgs/brands/primitives)
20260507120000_phase_4c_pre_source_wiring.sql                    (Phase 4C-pre fix — orchestrator v3 + source_kind wiring)
20260508120000_phase_4d_artifact_pipeline.sql                    (Phase 4D — Section 1O)
20260509120000_phase_4e_outbound_jobs_reconciliation.sql         (Phase 4E — outbound_jobs)
```

**Plus all pre-Phase-3 migrations** (`20260418` → `20260430`) that establish the underlying tables (patients, audit_events, staff_profiles, care_programs, treatment_orders, clinical_visits, lab_orders, patient_lab_observations, patient_diagnostic_reports, messages, message_threads, patient_timeline_events, outbound_jobs early shape, etc.). On a clean nuke + reset, ALL migrations apply in order.

**Pre-apply review status**: clean. 1 bug found (`enqueue_outbound_job` 28-vs-27-param signature in COMMENT + GRANTs) and fixed in `4af4daa`. All 5 RPCs verified parameter-count-correct. Migration ordering correct. FK chains valid. SECURITY DEFINER pattern verified. Session-context trick (set_config + DEFAULT firing helper) verified across all 4 RPCs that use it.

## 3. Apply path (project: `sxblfnokjtxltbhbthep`)

User confirmed: NO production data; clean nuke + reset is acceptable.

Two options for the next agent:

**A) MCP path** (preferred — no CLI install needed):
1. `list_projects` → confirm `sxblfnokjtxltbhbthep` exists.
2. `execute_sql` with `DROP SCHEMA public CASCADE; CREATE SCHEMA public; GRANT ALL ON SCHEMA public TO postgres, anon, authenticated, service_role;` (also drop any storage buckets we created if they conflict).
3. `apply_migration` for each migration in `supabase/migrations/` in order (timestamp-sorted).
4. After all applied, run the 5-item smoke test (see §4).

**B) CLI path** (if MCP unavailable):
```bash
brew install supabase/tap/supabase                # or via npm/npx
cd ~/Desktop/main-app
supabase login                                     # browser flow
supabase link --project-ref sxblfnokjtxltbhbthep   # prompts for DB password
supabase db reset --linked                         # nukes + replays all migrations
```

## 4. Verification — the 5-item smoke test

After migrations apply, run:

```bash
export NEXT_PUBLIC_SUPABASE_URL=https://sxblfnokjtxltbhbthep.supabase.co
export SUPABASE_SERVICE_ROLE_KEY=<from dashboard>
npx tsx scripts/test-record-intake-response.ts
```

Expected output (all 5 items GREEN):
```
[1/5] intake_responses primitives OK
[2/5] patient_clinical_assertions primitives OK
[3/5] audit_events primitives OK (... rows; all carry org_id + actor_kind)
[4/5] cross-org mismatch rejected before write
[5/5] patient_address primitives + source_kind + source_id OK
PASS — Phase 4C-pre verification (5/5 items green).
```

Two more pure-function unit tests don't need a live DB and have already passed locally:
- `npx tsx scripts/test-resolve-emissions.ts` → 21/21 (Phase 4C-runtime)
- `npx tsx scripts/test-document-routing-matrix.ts` → 91/91 (Phase 4D)
- `npx tsx scripts/test-outbound-jobs-types.ts` → 78/78 (Phase 4E)

## 5. DO NOT re-litigate (settled architectural decisions)

These came up during this session and were settled with explicit rationale. The next agent should NOT reopen them unless the user specifically asks. Full reasoning in `phase_4b_arch_plan_and_critique_log.md`.

| Decision | Settlement | Rationale (1-line) |
|---|---|---|
| Identity resolution | Already covered by Section 1J (1J.1–1J.10) | ChatGPT misread; Section 1J is comprehensive |
| Partial failure / transaction strategy | Locked in 4A: orchestrator is one Postgres transaction; outbox pattern via outbound_jobs | ChatGPT framing wrong; we already decided |
| Security boundary thinking | Section 1D + 1J.10 + 1Q.7 + 1N already cover | ChatGPT misread |
| `data_environment` duplication | KEEP replicated columns on every patient-scoped table | Standard multi-tenant Postgres pattern; required for RLS efficiency + dispatch worker simplicity; the "no duplication" advice is pure normalization theory |
| Identical AUTHORED_BY enum across all tables | NO — compatible vocabularies, not identical | AUTHORED_BY on claim ledger; source_assertion_id back-pointer on entities; implicit on non-clinical |
| `1R/1S/1T` as map Sections | KEEP as Sections (not demoted to companion doc) | Map is binding source-of-truth; companion docs lose authority; directional Sections prevent drift |
| Primitives orchestrator-only injection | Internal callers CAN pass overrides; HTTP handlers MUST NOT propagate from req.body | This is a route-handler concern, not orchestrator |
| Reject-missing-primitives at orchestrator | NO — fallback to patient lookup → main/production. Reject MISMATCH (cross-org), not missing | Pre-account flow has no patient_id; system writes legitimately pass nothing |

## 6. Binding source-of-truth files (read these before touching code)

In strict priority order:

1. **`AGENTS.md`** — workspace rule pointer. Says: "@AGENTS.md → system_map. RLS as coarse is_staff_user / is_staff_admin. requireCapability for staff/cron mutations."

2. **`.cursor/plans/system_map_three_layers_60706286.plan.md`** — the binding architectural source-of-truth. 9657 lines. Section 1K.0.5 is the data-routing spine. Sections 1R/1S/1T/1U/1V are the new Phase 4B-arch additions. The system primitives addendum is at the top under "Layer 1 foundation". When this file conflicts with anything else, this file wins.

3. **`.cursor/plans/data_layers_reconciliation_v1.md`** — Phase 4B-arch readable companion. 268 lines. 11-layer reconciliation table + early migration audit + integrated mermaid + Phase 4C-H roadmap + 15-rule CI lint catalog + misread citations.

4. **`.cursor/plans/phase_4b_arch_plan_and_critique_log.md`** — the FULL evaluation log of every external critique round (R1+R2+R3+R4) with verdicts. Snapshot of the plan file as it stood when 4B-arch shipped. Read this if you're tempted to revisit decisions in §5.

5. **`.cursor/plans/HANDOFF_2026-05-06.md`** — this file.

## 7. Open questions / parking lot

These are flagged for future passes. NOT blocking 4F.

- **Video / audio / transcript pattern** — Section 1O.1 future flag; not in v1 input_type vocabulary. When telehealth recordings start, Section 1O.x extension.
- **Amazon-style click/view product analytics events** — explicitly NOT in `audit_events` (that's staff-action / system-action). Belongs in a separate analytics-event sink (Amplitude / PostHog or dedicated table). Defer until growth team has traction.
- **Cross-org patient-data export contract** — Section 1U future host-org / tenant-org model. Defer until 2nd org onboards.
- **Section 1W SLO ownership** — deferred. Map's existing 1H.1 / 1H.2 directional language stays; revisit when SRE hire or first production incident pattern.
- **Subprocessor inventory + data-sharing contract registry** — deferred until 5+ subprocessors active.

## 8. What 4F looks like (the next phase if user asks)

Per companion doc roadmap: event_type catalog completion (1H + 1Q.7).

Scope:
- Lock the canonical `audit_events.action` + `patient_timeline_events.event_type` vocabulary as a typed code-as-config registry (mirrors `lib/intake/events.ts` Phase 4A pattern).
- Inventory existing event_type values across early migrations + Phase 4 (probably ~60-80 unique values).
- Add CI lint that refuses inline string event_types at write sites.
- Add reconciliation pointer in Section 1Q.7 (privacy-gate event vocabulary part of typed registry).

Estimated: ~250-line TS registry + ~100-line CI lint + ~150-line audit script. No new migration. ~30 minutes of work for an experienced agent.

## 9. Tone note for the next agent

The user has been doing intense architectural work for a long stretch and is anxious about drift. They've explicitly pushed back when external critique (ChatGPT) suggested over-formalization or re-litigating settled decisions. They appreciate:
- Honest verdicts (valid / partial / push-back) with reasoning, not rubber-stamps.
- Concrete code over more planning.
- Pre-apply review (catching the 27-vs-28 param bug above came from a careful read).
- Tight commits with substantive messages — they read commit messages.

If unsure: read the critique log first. Most likely we already considered it.

---

**End of handoff. Good luck. Don't drift.**
