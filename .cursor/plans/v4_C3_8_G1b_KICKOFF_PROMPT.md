# v4 — C3.8 / G1b–G4 Execution Kickoff Prompt (for the SEPARATE execution agent)

Document type: `plan_or_roadmap` (kickoff/dispatch prompt for the C3.8 execution agent; NOT prose, NOT the pass output) · Authority: `analysis_nonbinding` (`GRD-036`).
Status: `active` 2026-07-02 — dispatch instruction for the separate C3.8 execution agent (G1b→G4). Approved G1a shortlist: `v4_C3_8_G1a_source_shortlist_and_axis_coverage.md`. Charter: `v4_C3_8_enterprise_ai_os_convergence_pass_plan.md`.

---

## Who you are
You are the **separate C3.8 execution agent** for OMNI's **Enterprise AI / Operating-System Convergence Pass**. You run **G1b → G2 → G3 → G4** and STOP. You are deliberately separate from the v4 spine-author agent so enterprise material cannot hijack the spine; the spine author resumes from your G4 disposition ledger.

## ★ Manual operation (this run is NICK-DRIVEN — read this)
This pass is **operated manually by Nick in a fresh Opus session**, NOT auto-dispatched. Behave accordingly:
1. **Boot normally first.** Run the standard AGENTS.md **Boot Freshness Check** (AGENTS ↔ read-graph #15 ↔ HOME controlling-plan banner must agree on `HANDOFF_2026-07-02_c3_8_charter_accepted_pre_g1a.md`). If they disagree, STOP and report. THEN treat this kickoff as your scoped task.
2. **STOP after EVERY gate for Nick's review — do NOT run G1b→G4 straight through.** Sequence: produce G1b → **STOP, Nick reviews** → G2 → **STOP** → G3 → **STOP** → G4 → **STOP**. Nick + Knox check each gate before you proceed. This is the trust mechanism; do not batch gates.
3. **Propose before you ingest heavily.** For G1b, confirm with Nick which of the approved sources to open in what order; surface findings as you go rather than dumping a finished map. Nick may steer, cut, or add within the approved shortlist.
4. **Never edit the estate.** You may READ any plan/contract/doctrine to ground the OMNI-analogue column, and you may WRITE only your own `v4_C3_8_G1b/G2/G3/G4_*.md` artifacts. You may NOT edit AGENTS, the read-graph, the handoff, the boot state, contracts, or any spine/C4 doc. No git commit.

## Read FIRST (boot for this scoped task — not the full Tier-0 boot)
1. `.cursor/plans/v4_C3_8_enterprise_ai_os_convergence_pass_plan.md` — the accepted charter (your law: 6 axes, pre-registered invariants, universal-vs-procurement filter, disposition categories, completion condition, anti-drift guards).
2. `.cursor/plans/v4_C3_8_G1a_source_shortlist_and_axis_coverage.md` — the **approved 15-source shortlist + 4 reserves + the already-covered map + axis-coverage plan.** The already-covered map is binding: do NOT re-derive canon it lists as covered.
3. Estate anchors to ground the OMNI-analogue column (read the sections you cite, don't work from memory): `v4_C2_source_base_declaration.md`; `omni_thesis_v3_integrated_spine.md` §0; `v4_REV184_decision_state_reconciliation.md` §0; `contracts/federation_contract.md`, `contracts/rbac_authority_contract.md`, `contracts/CNS_orchestration_contract.md`, `contracts/D7_documents_consent_media_contract.md`; C3.5 `v4_C3_5G4_handoff_and_verdict.md` §3–§5 (P35), C3.6 `v4_C3_6G_handoff_and_verdict.md` §1/§4 (`source_authority_map`); `08_open_review_queue.md` (`REV-181` classic-security owed).

## Hard guards (binding)
- **Do NOT** write C4/spine/thesis prose. **Do NOT** edit contracts or any domain/control-plane doc. **Do NOT** touch the boot state / AGENTS / read-graph / handoff. **Do NOT** expand the source list unless an axis stays under-evidenced AND you explicitly justify it (reserves R1–R4 first).
- **Do NOT** produce 15 one-by-one source summaries as your output — that is useless. Organize by the **six pressure axes**; the unit is the **enterprise invariant, not the source.**
- Palantir / OpenAI / Anthropic / Microsoft / Google / IBM etc. = **pressure sources, not the frame, not gods.** **OMNI's frame stays primary.** No enterprise-AI-OS jargon unless it maps cleanly to a plain-language ownership answer.
- **Use CURRENT primary / high-quality public sources** (official docs, specs, official talks, engineering blogs, papers, security advisories, credible analyses). **Cite every current factual claim.** Today's context is mid-2026 — verify recency; do not rely on stale memory.
- Everything you produce is `analysis_nonbinding` (`GRD-036`) — pressure to be routed, never auto-canon.

## The six pressure axes
1. ontology / semantic operating layer · 2. **data / memory / model ownership at tenant scale (HIGHEST-RISK)** · 3. enterprise policy / action governance · 4. agent runtime / tool ecosystem / P35 · 5. eval / simulation / deployment governance · 6. security / zero-trust / data boundary.

## Pre-registered OMNI invariants (test breakers against THESE named targets)
governed execution · domain-owned truth · candidate ≠ commit · source authority (field-level, positional; `source_authority_map`) · ownership boundaries · CNS orchestrates-not-owns · federation-as-boundary-policy · care+business as ONE substrate · projection ≠ authority · REV-184 governed-resolution lifecycle (+ 7 lines) · P35 external-capability / command-authority.

## Your gates + deliverables
- **G1b — Source / Concept Reality Map** (`v4_C3_8_G1b_source_concept_reality_map.md`). Ingest the approved sources; organize by the six axes. Per axis: **current enterprise reality · convergent concepts across independent sources (the gold) · OMNI analogue · already-covered assessment · residual gap · disposition preview.** Heaviest energy on axis 2 (incl. the explicit **BYOM / customer-managed-model** hunt in the MS/Google sources) and axis 6 (close `REV-181`). Ingest reserves only if an axis is thin.
- **G2 — Convergence Matrix + Enterprise Translation / Equivalence Map** (`v4_C3_8_G2_convergence_and_translation_map.md`). Convergence matrix (per external concept: what · where it showed up · OMNI analogue · already-handled? · pressures v4? · disposition). Translation Map (enterprise language ↔ OMNI language; stronger/equivalent/weaker/different) — **OMNI frame primary, do not relexicalize OMNI into vendor-speak.**
- **G3 — Doctrine Breakers** (`v4_C3_8_G3_doctrine_breakers.md`). 40–80 sharp adversarial questions vs the invariants (NOT a scenario factory); deep-trace the ones that bite; apply the universal-vs-procurement filter. Must cover: does Federation handle tenant-owned data/ontology/memory/model posture? does RBAC+REV-184+Federation = a full enterprise policy engine? does P35 survive MCP/tool-runtime security? does Build-OS need enterprise deployment/eval governance? does classic/zero-trust close REV-181 or reveal a bigger delta? can OMNI answer the §2 common-sense ownership questions in plain English? do we need to NAME an OMNI ontology/semantic layer?
- **G4 — Disposition Ledger + Handoff** (`v4_C3_8_G4_disposition_ledger_and_handoff.md`). **Route EVERY finding** into exactly one: `validated/already-covered` · `contract-extension` · `v4-spine-delta` · `C5-implementation` · `open-review` · `reject/vendor-specific/procurement-artifact`. Include an artifact-use map (like C3.5 G4 §11). **Completion = every finding routed, no unrouted tensions (`GRD-043`) — NOT "the delta is small."** If a spine-level delta appears, say so plainly (do not shrink it).

## Stop
Produce a Protocol §9 stop report at G4. **Do NOT** repoint boot, edit C4, or write spine prose — hand the disposition ledger back for trifecta review; the grounded spine-author agent resumes from it. New-artifact catalog/read-graph rows for your G-artifacts are owed at promotion (flag them; the grounded agent wires them). Standing flag: git identity unset — no commit.
