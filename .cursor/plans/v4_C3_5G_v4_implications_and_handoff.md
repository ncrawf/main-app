# v4 — C3.5G: v4 Implications + Handoff to the Continuity Agent  (SHELL)

Document type: `plan_or_roadmap` (C3.5 arc artifact G) · Authority: `analysis_nonbinding` (`GRD-036`)
Status: `shell_pending_population` 2026-06-13 — **populated by the SEPARATE C3.5 pressure-test agent**.
Gate: **G4 (Handoff Accepted)**. Purpose: the synthesis the **v4 continuity/integration agent** carries into C4. Must cite A–F. Honest verdict required.

> SHELL — fill below. False confidence is failure AND false retreat is failure.

## 1. Honest readiness verdict
Where OMNI can **match or exceed** hospital-grade capability · where it is **replacement-shaped in slices** · where it is **overlay-first but must preserve the authority runway** · where it can **pick off smaller/non-Epic incumbents as a wedge** · where it is **not ready** · what must be **designed NOW so the path isn't foreclosed**. (Not a yes/no slogan.)

## 2. What C4 must carry (cite A–F)
thesis-positioning implications · execution-substrate requirements · which C4 sections are affected.

## 3. Contract pressure
which domain/control-plane contracts must be revisited (C5).

## 4. Primitive / control-plane gaps
new primitives or control planes the gap matrix surfaced.

## 5. C5 deferrals
what is downstream build, not v4.

## 6. Open-review items → `08`
unresolved decisions (with proposed `D0THES-REV-*` rows).

## 7. Do-NOT-import
findings that should NOT enter v4 (with reason).

## 8. Routing
how each item routes (C4 spine section · contract · C5 · open-review). Obeys C4.0 depth on integration; `GRD-036` (gated, not auto-canon).
