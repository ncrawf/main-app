# v4 — INS-G0 — Kickoff Subagent Verbatim Packet — INDEX

Document type: `manifest_or_catalog` (packet index for a preserved verbatim source set)
Authority: `derived_nonbinding`. Binds nothing. Promotes nothing. Routes only.
Status: `awaiting_operator_paste` · `packet_incomplete_until_all_three_raws_pasted` · `not_promoted`
Domain(s): `insurance_payer_oop` · `architecture_governance` · `cross_cutting`
Lifecycle role: the single entry point for the three preserved Gate-0 kickoff subagent reports. **Routes by question so no future agent loads three long raws by default.**
Source-of-truth relationship: consumes and points to preserved source objects. The **raws** are authoritative only as to what each subagent said; the **Gate-0 carrier** is the authoritative record of the assignments and their dispositions; **primary repository sources and the Gate-0/Gate-1b adjudication lineage control all architecture questions.**
Supersedes: nothing
Superseded by: none
Manifest action: `add_tier2` **PROPOSED** — catalog rows + one triggered read-graph route owed at the parent integration transaction, **not landed here**
Review gate: `user_knox_required`

Parent arc: `INS-G0-MIXEDFIN` · Packet branch: `preservation/ins-g0-kickoff-subagent-raws`

---

## §0 — SCOPE, stated bluntly so nobody re-litigates it

**This packet preserves the three exact Gate-0 kickoff subagent reports recovered and supplied by the operator. It does not claim that only three analytical lenses existed.**

The Gate-0 carrier `§G.4` "Helper-agent method ledger" records **seven analytical lenses, A–G**. **Seven lenses is not seven report objects** — one subagent can carry several lenses, one conversation can answer several requested perspectives in a single final body, and some lens results were the main Gate-0 agent's own work. **The Gate-0 carrier remains the authoritative ledger of the A–G assignments, independence conditions, and dispositions.**

**No additional Gate-0 raw is claimed preserved here. No Gate-1b reconnaissance raw is claimed preserved here.** Those are recorded below as **not recovered**, which is deliberately distinct from *not commissioned* and from *discarded*. If an exact raw surfaces later it is preserved then, as its own exact object — **never reconstructed.**

---

## §1 — The three preserved raws, routed by question

**Load this index first. Open a raw only when your question requires it.** Preserve broadly, read narrowly.

| Open this raw when your question is… | Artifact | Lens |
|---|---|---|
| *How would a first-principles decomposition carve this up, with no knowledge of our answer?* — owner splits, write-authority zones, the god-object risk in any joining service | `v4_INS_G0_build_architecture_decomposer_verbatim_2026-08-08.md` | **C** blind decomposer |
| *What is the strongest case that OMNI is wrong, unnecessary, or already absorbed?* — incumbent absorption, Epic-plus-Foundry, crowded-category attack, kill conditions, forward market trajectory | `v4_INS_G0_anti_omni_adversary_verbatim_2026-08-08.md` | **D** anti-OMNI adversary |
| *What does this actually feel like for a patient and for a provider institution, and where do they conflict?* — comprehension, redress and appeal burden, institutional economics, multi-entity TIN/NPI reality | `v4_INS_G0_patient_provider_lens_verbatim_2026-08-08.md` | **E** patient + provider |

### Why these three, in the carrier's own words

| Lens | Gate-0 `§G.4` disposition, contemporaneous |
|---|---|
| **C** | *"**Adopted as the carrier's spine.**"* Contributed the five-zone write-authority model, the fourth-gate reasoning, the five-machines-not-one-case finding. **Later narrowed:** Gate-1b `§13.1` records the five-zone model as **`Not adopted — demoted by Gate-0 at 0R`** |
| **D** | *"**Adopted and it changed a verdict.** Verdict 7 was rewritten because of it. Its kill conditions are carried verbatim into `§I.4`."* |
| **E** | *"**Adopted.** The patient/provider conflict analysis (`§I.3`) and the multi-entity TIN/NPI reality are its work."* |

**All four of Gate-0's preserved disagreements (`§G.5`) come from these three lenses** — C's self-attack on its own twelve-owner split, D-versus-F on the wedge, E's patient↔provider conflict, and the unresolved appeal-automation economics. That is the strongest available evidence that these are the load-bearing returns.

---

## §2 — Recovery status of every Gate-0 lens (absence made visible)

| Lens | Assignment (Gate-0 `§G.4`) | Access | Raw recovered? |
|---|---|---|---|
| **A** | Estate archaeologist — prior OMNI treatment of insurance/payer/claims/financing | repo read-only | **not recovered** — likely main-agent work; not claimed preserved |
| **B** | Payer/RCM operator — US mechanics, standards, mandates, failure modes | no repo, primary sources only | **not recovered** — not claimed preserved |
| **C** | Blind decomposer — first principles, forbidden the repo | fully blind | ✅ **preserved in this packet** |
| **D** | Anti-OMNI adversary — kill the thesis | external only | ✅ **preserved in this packet** |
| **E** | Patient/consumer + provider/institution | external only | ✅ **preserved in this packet** |
| **F** | Enterprise-platform mechanism — ten comparators | external only | **not recovered** — carried in Gate-0 `§G.1–G.3`; raw not claimed preserved |
| **G** | 2030/2035 trajectory — four-tier evidence/forecast separation | external only | **not recovered as a distinct object.** Carried in Gate-0 `§J` with tier labels. **Open question:** the forward-looking material may sit inside lens **D**'s body — resolved by inspecting the pasted raw, recorded in that file's receipts, **not assumed** |

**Separately: the four Gate-1b reconnaissance reports** (care truth / disagreement-correction · economics-money-obligation ownership · external-party-counterparty-agreement · insurance implementation forensics) were commissioned by the Gate-1b agent and **never returned to any Opus context**. Gate-1b `§0.5` states plainly: *"they have still not returned to me… **No claim in this carrier rests on a helper report.**"* Status: **`not_recovered`** — no exact raw is identified, and **no reconstruction is permitted**.

---

## §3 — Reading rules

1. **The raws are source objects, not doctrine.** They are authoritative only as to what each subagent said. Every architecture question resolves against primary repository sources and the Gate-0/Gate-1b adjudication lineage.
2. **Never edit a preserved body**, and never fill a missing byte from the carrier, a summary, or memory. Each raw carries its own binding Preservation Law.
3. **Preservation is not promotion.** External claims inside these reports — market facts, competitor behaviour, regulatory state, and especially anything forward-looking — do **not** become promotion-grade evidence by sitting in the repository. If OMNI later relies on one, that claim's **primary source** goes through the Evidence Plane router at that time (`D0THES-GRD-036`, capture broad / promotion gated).
4. **Do not average the disagreements.** Gate-0 `§G.5` preserved four unresolved tensions on purpose. Flattening them destroys the finding.
5. **A raw's conclusion being later demoted is a reason to keep it, not to discard it.** Lens C is the worked example: its headline contribution was demoted at 0R, and the reasoning behind it survives nowhere else.

---

## §4 — Routing proposed at the parent integration transaction (nothing landed here)

- **Catalog:** one row per artifact — **4 rows** (this index + three raws).
- **Read graph:** **one** triggered cold-entry route to **this index**, hung off the existing Insurance route (Gate-0 → Gate-1a → Gate-1b → Gate-2 brief). **No universal-boot promotion**; nothing here is Tier-0 or boot-visible.
- **Gate-2 brief:** cites an exact raw **only** where a finding, disagreement or comparator actually requires it — not as background reading.
- **`E1` proof obligations:** every raw body hash matches the operator's source · every file retrievable from a fresh clone at the parent head · catalog rows exist · the index route resolves · **no raw is represented as doctrine or as superseded by a derivative.**

---

## §5 — Packet completion state

| Artifact | State |
|---|---|
| `v4_INS_G0_build_architecture_decomposer_verbatim_2026-08-08.md` | `awaiting_operator_paste` |
| `v4_INS_G0_anti_omni_adversary_verbatim_2026-08-08.md` | `awaiting_operator_paste` |
| `v4_INS_G0_patient_provider_lens_verbatim_2026-08-08.md` | `awaiting_operator_paste` |
| this index | `awaiting_operator_paste` — flips to `preserved` when all three raws carry integrity receipts |

**STOP: `paste_slots_open_pending_operator_verbatim_paste`**
