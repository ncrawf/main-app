# OMNI — Claude / Cursor compatibility pointer

Thin compatibility file only. It hosts no authority and duplicates nothing.

- **Shared OMNI + repository law:** `AGENTS.md` (Cursor loads it directly — this file does **not** re-import it, to avoid loading the constitution twice).
- **Repository-native Cursor collaborator profile (the full Opus operating constitution):** `.cursor/rules/00_omni_opus_boot.mdc` (always-applied Cursor rule).

Do not treat this compatibility file as a separate authority source.
