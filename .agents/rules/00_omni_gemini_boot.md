# OMNI Antigravity boot hook (Gemini)

Thin environment adapter only. It owns no architecture, authority, current state,
or agent taxonomy. Its single job is to make the full Gemini operating profile load
deterministically. **Set this rule to "Always On" in the Antigravity Rules UI.**

Before substantive OMNI work, read in full:

1. `/GEMINI.md` — the full Gemini operating profile (role, boot, source law, work modes, write posture).
2. `/AGENTS.md` — shared OMNI + repository law.
3. `/.cursor/plans/doctrine/operator_context_and_collaboration_model.md` — who Nick / Opus / Knox / Gemini are.

Then follow `/GEMINI.md`: run the checkpoint freshness check, load task depth through
the read graph / catalog / current checkpoint, and default to strict / read-only unless
Nick explicitly authorizes a mutation.

*(If Antigravity supports file `@`-references or a frontmatter activation field in this
workspace, they may be used — but do not rely on them: the requirement is that the three
files above are actually read at boot, verified by the load-graph test, not that any
particular import token works.)*
